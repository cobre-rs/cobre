# Owner gate — test-corpus (2026-09, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`)

Run in the main session as AskUserQuestion rounds over this digest. Inputs: `calibration.json` (8 minted entries, 50 seed folds, 10 retire proposals, 4 fix-shape refusals, 7 dismissed deltas, 9 target gaps, 3 dup-of), `verdicts.json` (defender `_needsHuman`: 120 items), `gate-substrate.json`, `inventory.json` (15 figures) and `verification.md` (shared 7/7 + station 10/10 PASS). Presentation order per the ticket: figures first, then the entries, the dup-of merges, the drift adjudications, every needs-human item, and last the informational substrate. Every count below names its definition (binary = depth-1 `tests/*.rs`, what §2.1's `find -maxdepth 1` counts and Cargo links; file = the recursive sweep) and cites inventory.json, never a figure frozen in the yardstick. No option anywhere deletes, skips, disables or re-baselines a test.

## 1. Presentation

### 1.1 Re-measured figures — ratified FIRST (round R0)

All 15 figure records are `measured` at `077dbe2c` (E08-1 `remeasure.sh`); no record is unmeasured or estimated. The ticket's a136840d figures (cobre-sddp 37 binaries / 53 files, naive slow-tests grep 73 / 54) are superseded by the pin's values below and in 1.2.

| # | Figure | Source | Definition | Value at the pin | Command |
|---|---|---|---|---|---|
| 1 | Integration-test binaries, per crate (depth-1 tests/*.rs — what Cargo links) (`int-binaries`) | ta-2.1 | binary | 87 | `for c in cobre cobre-core cobre-io cobre-stochastic cobre-solver cobre-comm cobre-sddp cobre-cli cobre-mcp cobre-tui cobre-flow cobre-uc cobre-emt cobre-python; do printf '%s %s\n'` |
| 2 | …that statically link the solver (crates depending on cobre-solver: cobre-sddp, cobre-cli, cobre-solver) (`int-binaries-solver-linking`) | ta-2.1 | binary | 62 | `for c in cobre-sddp cobre-cli cobre-solver; do printf '%s %s\n' "$c" "$(git ls-tree --name-only 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c crates/$c/tests/ \| grep -c '\.rs$')"; done` |
| 3 | Unit + integration test count (cargo nextest list --features test-support) (`nextest-list`) | ta-2.1 | test | 6371 | `cargo nextest list --features test-support` |
| 4 | Doctests (cargo test --doc) (`doctests`) | ta-2.1 | doctest | 330 | `cargo test --doc` |
| 5 | pytest (cobre-python) collected tests (`pytest-collect`) | ta-2.1 | test | 218 | `pytest crates/cobre-python --collect-only -q` |
| 6 | Golden bit-exact cases: the tests/fixtures/parity_baselines* decks × two backends (HiGHS / CLP) (`golden-cases`) | ta-2.1 | deck | 10 | `for d in parity_baselines parity_baselines_clp; do printf '%s %s\n' "$d" "$(git ls-tree --name-only 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c crates/cobre-sddp/tests/fixtures/$d/ \|` |
| 7 | to_bits/ULP determinism assertions (files carrying to_bits) (`to-bits`) | ta-2.1 | file | 77 | `git grep -c 'to_bits' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**/*.rs' \| wc -l ; git grep -o 'to_bits' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**/*.rs' \| ` |
| 8 | proptest! sites (`proptest-sites`) | ta-2.1 | file | 7 | `git grep -l 'proptest!' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**/*.rs' \| wc -l ; git grep -c 'proptest!' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**/*.rs'` |
| 9 | Slow-gated (slow-tests) attributes — .rs files carrying the token (git-tracked census at the pin) (`slow-tests-attrs`) | ta-2.1 | file | 13 | `git grep -c 'slow-tests' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**/*.rs' \| wc -l ; git grep -l 'slow-tests' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/**' \|` |
| 10 | .config/nextest.toml presence (§2.3: no profiles, retries, partitioning, JUnit or archive) (`nextest-config`) | ta-2.3 | n/a | absent | `git cat-file -e 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c:.config/nextest.toml 2>/dev/null && echo present \|\| echo absent` |
| 11 | Per-job runner split in .github/workflows/ci.yml (cargo test in the HiGHS Test job vs cargo nextest run in the (`ci-runner-split`) | ta-2.3 | line | 2 | `git show 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c:.github/workflows/ci.yml \| grep -n 'cargo test --workspace\\|cargo nextest run'` |
| 12 | slow-tests inside NON_SOLVER_FEATURES (the PR feature set), so the slow suite runs on every PR (`slow-tests-in-pr-features`) | ta-2.3 | line | yes | `git show 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c:.github/workflows/ci.yml \| grep -n 'NON_SOLVER_FEATURES'` |
| 13 | invariance-shuffle.yml cadence: workflow_dispatch only, nightly cron commented out (`shuffle-cadence`) | ta-2.3 | line | workflow_dispatch-only | `git show 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c:.github/workflows/invariance-shuffle.yml \| grep -n 'workflow_dispatch\\|schedule\\|cron'` |
| 14 | test-support declarers versus consumers across crates/*/Cargo.toml; StubComm/Rank0Of2 home (`test-support-declarers-vs-consumers`) | ta-3.2 | line | declarers: 5; consumers: 5; neither: 8 | `git grep -n 'test-support' 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c -- 'crates/*/Cargo.toml' \| sed 's#^077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c:##' ; git grep -n 'struct StubComm\` |
| 15 | Inline-giant versus extracted-sibling homing split (§3.2 item 4): sibling src/**/tests.rs files vs source file (`homing-split`) | ta-3.2 | file | siblingTestsRsTotal: 19; inlineCfgTestFilesTotal: 312 | `git ls-tree -r --name-only 077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c crates \| grep '/src/.*tests\.rs$' \| sed 's#crates/\([^/]*\)/.*#\1#' \| sort \| uniq -c ; for c in cobre cobre-` |

### 1.2 Definition-ambiguous counts (rounds R1, R2 — asked one crate per round)

- **cobre-sddp** — 40 binaries (`git ls-tree --name-only 077dbe2c crates/cobre-sddp/tests/ | grep -c '\.rs$'`, depth-1 = what Cargo links) vs 56 files (`git ls-tree -r --name-only 077dbe2c crates/cobre-sddp/tests/ | grep -c '\.rs$'`, recursive). Every entry that quotes either count already names its definition; no minted entry flips between them (calibration.json `needsHuman` is empty).
- **cobre-io** — 12 binaries (`git ls-tree --name-only 077dbe2c crates/cobre-io/tests/ | grep -c '\.rs$'`, depth-1 = what Cargo links) vs 13 files (`git ls-tree -r --name-only 077dbe2c crates/cobre-io/tests/ | grep -c '\.rs$'`, recursive). Every entry that quotes either count already names its definition; no minted entry flips between them (calibration.json `needsHuman` is empty).

### 1.3 Minted entries (rounds R3 Sev B, R4 Sev C)

8 ids minted in (lens, yardstickRef, candidateRef) order: TD-074..077 (test-bloat), CD-116..118 (architecture: two prose-drift rows at C under the claimKind gate, one tree-fact), OD-051 (over-engineering). Severity `new (reviewer: original)` where the gate downgraded.

| # | ID | Sev | claimKind | Alignment | Definition | Claim (narrowed) | Fix-shape (one line) | NH |
|---|---|---|---|---|---|---|---|---|
| 1 | TD-074 | B | tree-fact | neutral | n/a | Narrower than the title: the defensible residue is the provision-versus-select mismatch inside mpi-slurm.yml at the pin, NOT that the batch-scheduler and multi-node tiers belong on an automatic cadenc… | cadence tiering: make the provisioning step and the mode selection agree per trigger. On whichever automatic trigger the owner keeps the compose cluster being built and health-waited (mpi-slurm.yml:10… | 2 |
| 2 | TD-075 | B | tree-fact | neutral | n/a | Narrowed to one module and one trigger: the push path list at .github/workflows/mpi-slurm.yml:18-24 - the trigger that serves the merge-to-develop cadence testing-architecture.md:499 and :522-526 assi… | cadence tiering - add crates/cobre-sddp/src/cut/** to the push list at :18-24 and keep the pull_request list at :7-13 byte-identical to it, or widen both to crates/cobre-sddp/src/** so the enumeration… | 2 |
| 3 | TD-076 | C | tree-fact | neutral | n/a | The defect is exactly the numeral 'ten' in the one clause at crates/cobre-python/tests/_cobre_cli.py:4-5: twelve test modules import the helper at the pin and each of them both calls run_cli and drive… | re-homing the census fact out of the frozen docstring literal into the invariant the import graph already owns: state what the helpers are for, every parity module that shells out to the CLI binary, s… | 1 |
| 4 | TD-077 | C | tree-fact | neutral | n/a | The defensible residue is one manifest line: crates/cobre-cli/Cargo.toml:62 activates cobre-core/test-support for the crate's test profile alone, while the three items that feature gates (crates/cobre… | feature-surface unification - settle the status of the single dev-dependency line at crates/cobre-cli/Cargo.toml:62 without touching the corpus: either drop the entry so the crate's dev-graph declares… | 1 |
| 5 | CD-116 | C (reviewer: B) | prose-drift | neutral | file (10) | Narrowed to the trailing clause of the §2.2 map: the words 'constructing fixtures per-file' at docs/design/testing-architecture.md:78 are contradicted for exactly two crates at the pin, crates/cobre-s… | re-homing — the tree side is the true side and no file moves: both shared harness modules stay where they are, each a thin crate-local case-builder layer over its crate's test_support re-exports, and … | 2 |
| 6 | CD-117 | C | tree-fact | neutral | file (13) | Exactly one crate names the shared integration prelude off-convention: crates/cobre-io/tests/helpers/mod.rs, declared 'mod helpers;' by 6 of cobre-io's 12 depth-1 binaries, fills the same source-inclu… | re-homing: move cobre-io's shared prelude from crates/cobre-io/tests/helpers/mod.rs to crates/cobre-io/tests/common/mod.rs and change the six 'mod helpers;' declarations (integration.rs:10, invariance… | 1 |
| 7 | CD-118 | C (reviewer: B) | prose-drift | neutral | binary (40) | Not the two-regime split as such, and not cobre-solver, whose narrow gate and fully cfg-gated consumers are deliberate and documented: the residue is that the 5.2 Adoption paragraph at docs/design/tes… | feature-surface unification: give crates/cobre-sddp/Cargo.toml the self dev-dependency its three sibling declarers already carry (crates/cobre-core/Cargo.toml:40, crates/cobre-io/Cargo.toml:54, crates… | 3 |
| 8 | OD-051 | C | tree-fact | neutral | n/a | Narrowed from the fixture surface to one 23-line item and one condition: crates/cobre-sddp/src/test_support.rs:177-199 is unreached at the pin AND its only differentiator from eq at :152-171, the iden… | consolidation — retire the zero-caller eq_with_anticipated and leave one GeometryDims construction path: eq at :152-171 for the no-anticipated shape, plus the exhaustive or struct-update literal the t… | 1 |

### 1.4 Seed dispositions (rounds R5 folds, R6 retire; the Rule 2 decision in R12-nh-rule2-carveout)

The 72 TD seeds the crate stations queued here: **50 delta-confirmed** (the defender's verified delta folded onto the seed id — TD-001, TD-002, TD-003, TD-006, TD-010, TD-013, TD-016, TD-021, TD-022, TD-025, TD-026, TD-027, TD-028, TD-030, TD-032, TD-035, TD-036, TD-037, TD-038, TD-039, TD-040, TD-041, TD-042, TD-043, TD-044, TD-046, TD-048, TD-049, TD-050, TD-051, TD-052, TD-054, TD-055, TD-057, TD-058, TD-059, TD-060, TD-061, TD-062, TD-063, TD-064, TD-065, TD-066, TD-067, TD-068, TD-069, TD-070, TD-071, TD-072, TD-073), **7 delta-dismissed** (entry unchanged — TD-008, TD-020, TD-031, TD-033, TD-045, TD-053, TD-056), **1 dup-of** (TD-047 → the mega-file mirror item), **10 stale-at-pin retire proposals** and **4 fix-shape refusals**.

| Retire proposal | Status bullet on the entry | Reason at the pin | Resolving-commit candidates |
|---|---|---|---|
| TD-004 | fixed (2026-09-15) — the six struct comparators in model/re | Premise fails at the pin. The seed claims ad-hoc bounds comparators where the yardstick calls for one shared comparator, with the risk that a newly added bounds field is silently ignored. At the pin a | eb0b82ef, 9c5bbf5a, 7c28de14 |
| TD-005 | fixed (2026-09-15) — one entity-builder family in crates/co | Premise fails at the pin: the consolidation the seed asks for has already happened. One of the five test sites the seed names, crates/cobre-core/src/topology/network.rs, does not exist at the pin (the | eb0b82ef, 8a72c69d, dd03c153, 2bf30ece |
| TD-007 | fixed (2026-09-15) — write_parquet, write_parquet_batches | Premise resolved between the seed's mint pin and the register pin. A crate-level test_support.rs was added holding single definitions of write_parquet, write_json, make_global, make_minimal_case, writ | eb0b82ef, 6dec2390, 883c022e, 09381faa |
| TD-009 | fixed (2026-09-15) — make_stats_batch(id_column, …) plus t | Resolved at the pin. The triplicated batch builder and the five common non-determinism cases are now single definitions in the crate test-support module, consumed by all three statistics parsers, and  | 09381faa, ac2e608a |
| TD-014 | fixed (2026-09-15) — the eight VALID_*_JSON constants, wr | Resolved at the pin. The minimal-case corpus and write_file are now single definitions in the crate test-support module, and tests/helpers/mod.rs re-exports make_minimal_case and write_file from there | eb0b82ef, 09381faa, bd29eb5e, ac2e608a |
| TD-017 | — (none) | Every anchor is absent at the pin. thermal.rs is 3784 lines with a single cfg(test) module at 1361 and no nested boundary_tests module, and none of override_at_t_minus_1_acceptance_boundary, test_ther | eb0b82ef, 6dec2390, ac2e608a, 19521701 |
| TD-018 | fixed (2026-09-15) — make_system/make_config/make_outpu | Premise dead at the pin. output/convergence_reader.rs does not exist, so the byte-identical pair the seed measured cannot be compared. make_config, make_system and make_output_context are now single d | 797ba443, 2a14fe56, 09381faa, bd29eb5e |
| TD-023 | fixed (2026-09-15) — parquet_helpers.rs gained its inline | Premise false at the pin. parquet_helpers.rs is 486 lines, not 166, and carries a cfg(test) module at 125 with 33 tests that pin the missing-column and wrong-type message contract directly for all six | 6dec2390, 09381faa, bd29eb5e, ac2e608a |
| TD-024 | fixed (2026-09-15) — forward_sampler.rs, conformance.rs | Premise false at the pin. git grep -n 'fn make_bus\|fn make_hydro' 077dbe2c -- crates/cobre-stochastic/tests returns nothing: no integration binary declares its own bus or hydro builder any more, they | 59c5c09f, 2bf30ece, ebba0508, cf010133 |
| TD-029 | fixed (2026-09-15) — saa_golden_value.rs drops its private | Premise false at the pin. tests/saa_golden_value.rs no longer declares a local identity_correlation; :16 is use common::{StageSpec, identity_correlation};, so the copy the seed names has already been  | 2bf30ece, ebba0508 |

Fix-shape refusals (entry stands, deletion-only fix-shape refused under lens-rules.md Rule 2): TD-011, TD-012, TD-015, TD-019 — decided with the carve-out question.

### 1.5 dup-of merges into the open mirror items (round R8)

- **Oracle test-harness duplication** (`docs/design/reserved-seams-and-deferred-debt.md:308`) ← test-bloat-04 — sharpened anchor: `crates/cobre-sddp/tests/extensive_form_oracle.rs::close` + `crates/cobre-sddp/tests/branching_value_oracle.rs::close` (four carriers at the pin: also `crates/cobre-sddp/tests/node_native_backward_gate.rs:76`, `crates/co
- **Mega-file / inline-test-giant asymmetry** (`docs/design/reserved-seams-and-deferred-debt.md:711`) ← test-bloat-37 (seed TD-047) — sharpened anchor: `crates/cobre-sddp/src/lp/builder/entries.rs:1680` and `crates/cobre-sddp/src/lp/builder/columns.rs:1305` inline `#[cfg(test)]` vs the extracted `crates/cobre-sddp/src/lp/builder/template/tests.rs` (`template.rs:1328`) a
- **Python-binding Rust tests invisible to CI** (`docs/design/reserved-seams-and-deferred-debt.md:347`) ← test-bloat-65 — sharpened anchor: `crates/cobre-python/Cargo.toml:34` ([dev-dependencies] names no cobre-sddp test-support) — the mirror's 'never compiled in CI' premise is superseded at the pin by `.github/workflows/ci.yml:567` (cargo test --manifest-pa

### 1.6 Drift adjudications (rounds R9-R11) and the §5 target gaps (round R7)

| Claim | Sentence | Verdict (defender) | claimKind | targetNotDefect | Recorded as |
|---|---|---|---|---|---|
| §5.8 StubComm/Rank0Of2 — doc side | `docs/design/testing-architecture.md:524-526` (restated :415-416) | test-bloat-30 dismissed | target-gap | true | roadmap item, no id |
| §5.8 StubComm/Rank0Of2 — tree side | canonical pair `crates/cobre-sddp/tests/common/mod.rs:32` / `:86` + private copies | test-bloat-43 dismissed | target-gap | true | roadmap item, no id (seed TD-053 stands) |
| §5.2 Adoption paragraph | `docs/design/testing-architecture.md:425-431` vs no manifest enabling cobre-sddp/test-support | architecture-00 confirmed | prose-drift | false | CD-118 (Sev C, reviewer B) |
| §2.2 map clause | `docs/design/testing-architecture.md:76-78` vs two shared harnesses | architecture-01 confirmed | prose-drift | false | CD-116 (Sev C, reviewer B) |

Target gaps dismissed `target-not-defect` (9), recorded as roadmap items with no id and actionable = no: test-bloat-03 (ta-5.1); test-bloat-18 (ta-5.1); test-bloat-25 (ta-3.2, seed TD-031); test-bloat-30 (ta-5.8); test-bloat-43 (ta-5.8, seed TD-053); test-bloat-68 (ta-2.3); test-bloat-69 (ta-5.5); test-bloat-70 (ta-5.5); performance-01 (ta-5.1).

### 1.7 Cleared and dismissed — presented read-only, no decision taken

- **test-bloat-06** (seed TD-008) — dismissed `premise-false-at-pin`, citation crates/cobre-io/src/scenarios/correlation.rs:411
- **test-bloat-08** (seed TD-020) — dismissed `premise-false-at-pin`, citation crates/cobre-io/src/output/stochastic.rs:786-789
- **test-bloat-17** — dismissed `premise-false-at-pin`, citation crates/cobre-solver/src/backends/highs/solver.rs:580
- **test-bloat-26** (seed TD-033) — dismissed `premise-false-at-pin`, citation crates/cobre-stochastic/src/seeds.rs:104
- **test-bloat-27** — dismissed `premise-false-at-pin`, citation scripts/ci/check-infra-genericity.sh:98-100
- **test-bloat-35** (seed TD-045) — dismissed `premise-false-at-pin`, citation crates/cobre-sddp/src/lp/builder/test_support.rs:65
- **test-bloat-46** (seed TD-056) — dismissed `premise-false-at-pin`, citation crates/cobre-sddp/src/simulation/state.rs:412
- **test-bloat-67** — dismissed `deliberate-and-documented`, citation .github/workflows/ci.yml:26-27
- **over-engineering-01** — dismissed `deliberate-and-documented`, citation crates/cobre-sddp/tests/permute_helpers.rs:1-3
- **performance-00** — dismissed `cost-accepted-by-rule`, citation .claude/rules/testing.md:113
- **performance-02** — dismissed `deliberate-and-documented`, citation .github/workflows/ci.yml:155-157

### 1.8 Worker needs-human items (120 items → 14 themed questions + 32 single questions; round R12)

Items that pose the same decision are asked once and the answer is written on every member; every other item is its own question. Each question lists the recommended option first.

**rule2-carveout** — Rule 2 Boundary — folds of exact-duplicate / strict-subset assertions change the nextest count (10 items; + the four fix-shape-refused seeds TD-011 / TD-012 / TD-015 / TD-019 and TD-067 / TD-068 (lens-rules.md § Rule 2 'Boundary'))

- `test-bloat-01#1` (TD-001, confirmed): The fold in coverageNeutralShape merges two test functions into one, so cargo nextest list reports one fewer test; lens-rules.md Rule 2 Boundary records that count carve-out as an owner-gate decision, not a defender's. The owner picks the fold or the count-neu
- `test-bloat-02#1` (TD-003, confirmed): The consolidation fold changes the cargo nextest list count by the number of folded functions; lens-rules.md Rule 2 Boundary records that carve-out (an exact-subset assertion folded into a named retained superset on an identical fixture) as an owner-gate decis
- `test-bloat-16#1` (TD-040, confirmed): TD-040's registered fix-shape removes both tests under either branch, a cargo nextest list delta of two, while this confirmation's residue keeps both: the owner must decide at the E08 gate whether the ratified removal direction or lens-rules.md Rule 2 Boundary
- `test-bloat-28#1` (TD-036, confirmed): Count parity: the shared-body fold above is count-neutral, but TD-036's own direction retires the inline copies, which changes the cargo nextest list count by the number of folded duplicates; whether the ratified duplicate-deletion carve-out extends to body-id
- `test-bloat-29#1` (TD-035, confirmed): Does the ratified duplicate-deletion carve-out recorded in lens-rules.md Rule 2 Boundary (TD-011, TD-015, TD-051, TD-067, TD-068) reach compile-time assertion duplicates, so TD-035's removal half may execute as written against src/factory.rs:420 and tests/fact
- `test-bloat-41#1` (TD-051, confirmed): Folding the weaker count-mismatch probe into its retained sibling drops the cargo nextest list --features test-support count by one; lens-rules.md Rule 2 'Boundary' leaves that carve-out for the ratified duplicate class (TD-051) to the E08 owner gate, not to t
- `test-bloat-49#1` (TD-063, confirmed): TD-063's upstream fix-shape also folds the five format_report_* cases into one table-driven test, which lowers the cargo nextest list count; this verdict confirms only the count-neutral repoint and leaves that fold to the owner-gate carve-out recorded in lens-
- `test-bloat-53#1` (TD-067, confirmed): The consolidation fold removes exact-duplicate assertions and therefore changes the cargo nextest list count; lens-rules.md section Rule 2 Boundary records that carve-out for the ratified duplication class (TD-067 here, and the sibling routed seed) as an owner
- `test-bloat-54#1` (TD-068, confirmed): The fold changes the cargo nextest list count: by four under TD-068's registered direction, or by five once the newly verified domination of missing_buses_json_stdout_contains_error (crates/cobre-cli/tests/cli_validate.rs:131-142) by validate_failure_report_in
- `test-bloat-62#1` (TD-071, confirmed): The shape is additive, so the crate's cargo nextest list count rises rather than staying equal; the owner must say whether section 6 count parity and lens-rules Rule 2's count test admit added coverage or need an explicit allowance for it.

**s51-threshold** — §5.1 homing threshold (~500 test-LOC / ~40 test fns) and the inline-versus-sibling direction (7 items)

- `test-bloat-03#1` (defended, dismissed): If the owner ratifies the section 5.1 homing threshold, decide whether cobre-core system/mod.rs (43 test fns from line 615) and model/resolved/bounds.rs (37 from line 1193) enter the already-registered re-homing scope as additional instances, or stay out becau
- `test-bloat-05#1` (TD-021, confirmed): Destination form is an owner call, not settled here: the 5.1 inline-versus-sibling threshold is unratified at the pin (claim row ta-5.1-homing-threshold is class target, measured as no ratified threshold, TD-031), so whether the re-homed checkpoint tests land 
- `test-bloat-06#1` (TD-008, dismissed): TD-008 is a registered entry whose claim is that the scenarios/ homing inconsistency is the durable threshold-INDEPENDENT residue; this dismissal is of the sharpening only (rule 12 bars re-litigating the upstream severity), but the owner should note that the d
- `test-bloat-06#2` (TD-008, dismissed): Carried and re-scoped from the attacker: 'which homing direction should the scenarios directory adopt' decides nothing for scenarios/ after this read, since the directory already satisfies any threshold between its largest inline module and its sibling; the li
- `test-bloat-18#1` (defended, dismissed): figures.homing-split.perCrate.cobre-solver.inlineCfgTestModules = 8 counts files carrying the literal #[cfg(test)], which for this crate is the wrong set on both sides: it includes three files with no inline test body (backends/clp/mod.rs:14 and backends/highs
- `test-bloat-25#1` (TD-031, dismissed): Ratify or reject the section 5.1 homing threshold (roughly 500 test-LOC or 40 test fns). Until it is ratified no candidate can name which inline tails must move, and if it is ratified the files it selects in cobre-stochastic are sampling/external.rs, sampling/
- `test-bloat-48#1` (TD-062, confirmed): Whether an intra-crate module re-home counts as a 'rename' under the section 5.1 migration invariant at docs/design/testing-architecture.md:351-354: the two function names and the total count are unchanged, but the fully qualified listed ids move from commands

**fixture-destination** — Destination of shared fixtures and harness doubles (crate-local now vs the §5.2 collapse; the rank-shape Communicator double) (15 items)

- `test-bloat-11#1` (TD-016, confirmed): Destination pick for the shared make_pumping: cobre-io own test_support (which widens that crate test-support export surface, against the blast-radius sentence at crates/cobre-io/src/test_support.rs:8-9) versus cobre-core beside the PumpingStation type it buil
- `test-bloat-19#1` (TD-032, confirmed): Destination for the three consolidated undated presets: crates/cobre-stochastic/src/test_support.rs, whose module doc scopes it to this crate's OpeningTree, SeasonMap and InflowModel shapes so entity presets widen that declared scope, or cobre_core::test_suppo
- `test-bloat-20#1` (TD-028, confirmed): Owner must pick ONE destination for this crate's duplicated entity presets: crates/cobre-stochastic/src/test_support.rs (crate-local, reachable from inline unit tests with no feature change) or cobre_core::test_support beside the existing HydroSpec and StageSp
- `test-bloat-22#1` (TD-026, confirmed): Which home the folded generator takes: crates/cobre-stochastic/src/test_support.rs, already gated at src/lib.rs:32 on cfg(any(test, feature = test-support)) and therefore reachable by downstream crates, or a private cfg(test) helper under src/par/fitting/ that
- `test-bloat-24#1` (TD-030, confirmed): Home for the two new statistical assertion helpers: `cobre-stochastic`'s `tests/common/mod.rs` (crate-local, neutral, three consumers) or `cobre_core::test_support` beside the generic `norm_cdf` and `approx_erf` it already owns (`crates/cobre-core/src/test_sup
- `test-bloat-33#1` (TD-043, confirmed): Destination of the folded constructors: this verdict names the currently wired tests/common/ home, since src/test_support.rs holds no neutral resolved-layer constructor and its existing ones are private. Whether the fold lands there or waits for the section 5.
- `test-bloat-47#1` (TD-057, confirmed): Whether cobre-cli may gain a crate-local tests/common helper module now, or whether its shared harness home must wait for the section 5.2 collapse of tests/common into the test-support surface so the crate does not build a convention that collapse would undo: 
- `test-bloat-51#1` (TD-065, confirmed): Feature-unification caveat on the recommended shape: cobre-cli takes cobre-io as a normal dependency at crates/cobre-cli/Cargo.toml:22 (schema feature) and as a dev-dependency at :63, so one cobre-io instance is shared during a test build and the cobre binary 
- `test-bloat-51#2` (TD-065, confirmed): Sharpens the attacker's third item: whether cobre-cli's shared fixture home is the existing cobre-io test-support surface now or must wait for the section 5.2 collapse. My read shows the surface already carries seven of the eight consts plus write_file plus th
- `test-bloat-54#3` (TD-068, confirmed): Whether cobre-cli may gain a tests/common helper module now, or whether its shared harness home must wait for the section 5.2 collapse of tests/common into the test-support surface. That decision gates the count-neutral alternative shape (one shared fixture bu
- `test-bloat-58#2` (TD-058, confirmed): Carried from the attacker: whether the shared fixture belongs in test_study.py as a module fixture (matching test_outputs.py:24-31, and the only trained-policy consumers sit in that one module) or in conftest.py as TD-058's registered fix-shape words it. The t
- `test-bloat-28#3` (TD-036, confirmed): Sequencing with the cobre-comm test-support surface: crates/cobre-comm/Cargo.toml declares only mpi, numa and shared-memory features and no [dev-dependencies] at the pin, so this fold's shared body would land on the same new test-support surface test-bloat-30 
- `test-bloat-41#2` (TD-051, confirmed): Where the shared 47-line two-rank echo stub is homed: crates/cobre-sddp/tests/common/mod.rs (this verdict's shape, neutral under Part IV/V) versus cobre-comm's test-support surface named by the section 5.8 target at docs/design/testing-architecture.md:524-526,
- `test-bloat-43#1` (TD-053, dismissed): Destination for a single shared rank-shape Communicator double remains an owner call: testing-architecture.md:415-416 and :524-526 assign StubComm/Rank0Of2 to cobre-comm's test-support surface, but cobre-comm declares no such feature at the pin, while cobre-sd
- `test-bloat-46#2` (TD-056, dismissed): Where a shared parameterized stub communicator would live (the sddp test harness at tests/common/mod.rs versus the communication crate's exported test-support surface) remains the station's adjudicate row ta-5.8-stubcomm-home. TD-056's registered fix shape sti

**cli-activation** — cobre-cli's cobre-core/test-support dev-dependency activation (Cargo.toml:62) with no consumer (3 items)

- `test-bloat-57#1` (TD-077, confirmed): Owner picks between the two coverage-neutral resolutions of crates/cobre-cli/Cargo.toml:62 - drop the unconsumed cobre-core/test-support activation, or keep it with an explicit reservation note beside the crate's slow-tests declaration; the tree gives no evide
- `test-bloat-47#2` (TD-057, confirmed): Whether the cobre-core test-support activation at crates/cobre-cli/Cargo.toml:62 is a reserved seam or a stale dev-dependency feature activation: git grep for test_support over crates/cobre-cli returns nothing at the pin and no cobre-cli test file references c
- `test-bloat-51#4` (TD-065, confirmed): Whether the cobre-core test-support activation at crates/cobre-cli/Cargo.toml:62 is a reserved seam for planned CLI fixture use or a stale activation: no file under crates/cobre-cli/tests/ references cobre_core at the pin, so it has zero consumers, but CLAUDE.

**sddp-self-dev-dep** — cobre-sddp self dev-dependency on its own test-support feature (CD-118 tree side) (1 items)

- `architecture-00#1` (CD-118, confirmed): Whether cobre-sddp gains the self dev-dependency now or command-line activation is accepted until the 5.2 collapse lands (attacker item 1, still live): the self dev-dependency makes the five feature-gated items in crates/cobre-sddp/tests/deterministic.rs:8708,

**s52-doc-side** — §5.2 doc side — the Adoption paragraph's uncarried list, cobre-solver's narrow gate, the two small harnesses as collapse sources (5 items)

- `architecture-00#2` (CD-118, confirmed): Settling claim-class row ta-5.2-adoption-uniform on the doc side: whether the Adoption paragraph's uncarried-bullet list at docs/design/testing-architecture.md:427-430 is corrected to include bullet 1's two placements (generic scaffolding to cobre-core, StubCo
- `architecture-00#3` (CD-118, confirmed): Whether cobre-solver's narrow gate (crates/cobre-solver/src/lib.rs:163, crates/cobre-solver/src/backends/highs/solver.rs:6 and :580) is recorded in 5.2 as a sanctioned exception to the uniform gate (attacker item 3, still live): the rationale is documented at 
- `architecture-01#1` (CD-116, confirmed): Carried from the attacker and narrowed by this read: whether the §5.2 plan text should still name crates/cobre-io/tests/helpers/ and crates/cobre-stochastic/tests/common/ as collapse sources even though both already re-export their crate's test_support surface
- `architecture-02#1` (CD-117, confirmed): Whether the §5.2 collapse source list (docs/design/testing-architecture.md:417-421, adjudicate row ta-5.2-adoption-uniform at :425-431) extends to crates/cobre-io/tests/helpers/ and crates/cobre-stochastic/tests/common/, which decides whether this residue is a
- `test-bloat-30#1` (defended, dismissed): The section 5.2 Adoption paragraph at docs/design/testing-architecture.md:425-431 lists only two bullets as not yet carried out and omits the bullet-1 placements (StubComm/Rank0Of2 to cobre-comm, generic scaffolding to cobre-core); whether that omission is its

**slurm-trigger** — mpi-slurm.yml full mode: which trigger runs the sbatch tiers (TD-074) (2 items)

- `test-bloat-71#1` (TD-074, confirmed): Trigger pick for full mode is an owner call among push to the release branch, tags only, and a nightly schedule; it spends the same cluster-minutes budget as CD-101's shuffle-cadence needs-human (BACKLOG.md:6500), so the two should be decided together rather t
- `test-bloat-71#2` (TD-074, confirmed): Whether the reduced path's cluster build and health-wait at mpi-slurm.yml:107-108 is intended as a cluster-boot smoke check on every automatic run; the answer decides whether trimming the provisioning on reduced triggers is admissible at all or whether it woul

**slurm-path-filter** — mpi-slurm.yml path filter: enumerate src/cut/** or widen to the crate source tree (TD-075) (2 items)

- `test-bloat-72#1` (TD-075, confirmed): Trigger shape: enumerate the omitted module or widen both lists to the whole solver crate source tree. Enumerating keeps the cluster job rare but rots the next time a module moves, which is the failure mode that produced this gap; widening is durable but spend
- `test-bloat-72#2` (TD-075, confirmed): Ordering against the 5.6 / 5.8 cadence targets: if the owner ratifies moving this deck off the pull-request tier, only the push list needs the added pattern and the pull_request block changes shape anyway, so the owner decides whether the filter fix lands befo

**runner-cadence-targets** — §5.5 / §5.6 runner and cadence targets — sequencing notes, not defects (dismissed target gaps and CI-shape rows) (10 items)

- `test-bloat-67#1` (defended, dismissed): Cadence policy, already owner-gated by the yardstick (§5.6 should be ratified explicitly; §6 phase 4 Ratify the tradeoff first): whether to split NON_SOLVER_FEATURES into a per-PR set without slow-tests plus a full set on push-to-develop or nightly, at the cos
- `test-bloat-68#1` (defended, dismissed): Owner call at the 5.5 adoption rather than at this station: the paired cargo test --doc step must land in the same change as the nextest switch because .github/workflows/ci.yml:114 is the only doctest execution at the pin, and the owner should state whether th
- `test-bloat-69#1` (defended, dismissed): Sequencing only, no defect: if the owner ratifies section 6 phase 2 (commit a .config/nextest.toml and move the fail-fast, slow-timeout and retry knobs off the three inline run lines), decide whether that profile lands before or after CD-101's edit to the same
- `test-bloat-70#1` (defended, dismissed): If the owner ratifies the §5.5 runner adoption, the paired cargo test --doc step must land in the same change: nextest runs no doctests, so unifying the runner without that step removes the 329-passing doctest tier from CI. Carried over from the attacker's run
- `test-bloat-70#2` (defended, dismissed): Whether the two CLP-gated doctest bodies (crates/cobre-solver/src/backends/clp/solver.rs:37 and :520) should ever be compiled under the CLP feature combination is an owner call on doctest-tier scope: their assertions are already covered by CLP-combination unit
- `performance-00#1` (defended, dismissed): Cadence boundary: does a pull request keep all three link-bearing jobs (Test at .github/workflows/ci.yml:114, CLP at :223 and :233, Coverage at :497), or does one backend gate pull requests while the second backend's test run and the instrumented pass move to 
- `performance-00#2` (defended, dismissed): Target selection inside the CLP job: the build at .github/workflows/ci.yml:223 passes --all-targets, which also builds the criterion bench targets declared at crates/cobre-sddp/Cargo.toml:92-110; whether the CLP job narrows its target selection to the test sur
- `performance-02#1` (defended, dismissed): Cadence only, not shape: does the sole codegen-and-link gate over the bench and example targets (the build step at .github/workflows/ci.yml:223) stay on the pull-request tier, or move to merge-to-develop or nightly with the rest of the link-bearing passes? Sec
- `performance-02#2` (defended, dismissed): Owner note, not a finding: if the linked-executable count of the criterion targets is ever revisited, it needs its own entry, because this candidate records the bench declarations as sanctioned (.claude/rules/testing.md:12, docs/design/testing-architecture.md:
- `performance-01#1` (defended, dismissed): Owner call: is the #[path] submodule shape already working at crates/cobre-sddp/tests/template_integration.rs:4400-4420 the ratified Layer 1 shape, and what sets the grouping boundary - docs/design/testing-architecture.md:327-328 leaves exact membership a star

**scope-extensions** — Newly found copies that widen a seed's scope at the pin (11 items)

- `test-bloat-00#1` (TD-002, confirmed): Census scope: a second identical out-of-owning-module pair exists at the pin that this candidate does not name, entities/hydro.rs:612 and model/penalty.rs:353 (its cfg(test) module opens at model/penalty.rs:335), sharing their own sixteen-field sequence; the f
- `test-bloat-11#2` (TD-016, confirmed): Whether the two further identical zero-penalty bodies I read at crates/cobre-io/src/broadcast.rs:199 and crates/cobre-io/src/output/simulation_writer.rs:2252 are folded under this entry at calibration or minted separately -- naming them in the surviving claim 
- `test-bloat-22#2` (TD-026, confirmed): A third copy of the same generator exists outside this candidate's declared crate scope at crates/cobre-io/src/scenarios/estimation/tests.rs:1172, identical to the cobre-stochastic estimation copy except its doc-comment first line. cobre-io already depends on 
- `test-bloat-19#2` (TD-032, confirmed): Scope call TD-032 does not cover: whether the four-site dated make_hydro variant (seeds.rs:180, sampling/external.rs:2403, sampling/historical.rs:1665, par/lag_transition.rs:1141) folds into the same shared preset behind operational_start_date and mirror_unit_
- `test-bloat-13#1` (TD-037, confirmed): Scope call on the third fixture family: TD-037's fix-shape names the empty batch as one of the three fixtures to give a single declaration, but the three zero-row RowBatch bodies at src/freeze.rs:286, tests/sentinel_inf_row_probe.rs:76 and src/backends/profile
- `test-bloat-34#1` (TD-044, confirmed): Scope of the shared home beyond the anticipated pair: crates/cobre-sddp/tests/filling_commissioning.rs carries one further copy in each of the same three classes (:1532 in the minority default_hydro_bounds digest, :1541 in the minority default_hydro_block_boun
- `test-bloat-44#1` (TD-054, confirmed): Whether the ninth home (crates/cobre-sddp/src/simulation/pipeline/tests.rs:160) is inside the ratified TD-054 scope at all: it is a counting/recording double like the four recording doubles the entry excludes by name, so the nine-site blast radius is a fact wh
- `test-bloat-31#1` (TD-041, confirmed): How far the penalties fold reaches: the widened constructor could be pub(crate) for just these src homes, or pub and adopted by the roughly twenty further byte-identical all-zero penalty literals elsewhere in the crate (tests/common/builders.rs:207, tests/anti
- `test-bloat-32#2` (TD-042, confirmed): Whether the 7 carriers equal to the HydroSpec::default() penalties at crates/cobre-core/src/test_support.rs:219-221 should adopt cobre_core::test_support::make_hydro itself, which cobre-sddp imports in 0 files at the pin, is an adoption decision that widens th
- `test-bloat-39#1` (TD-049, confirmed): Whether the anticipated-hydro pair, named as a collapsible family in TD-049's ratified direction but shown here to differ on inflow_lags and block layout, is dropped from that direction or re-filed as its own item: removing a family from a ratified direction i
- `test-bloat-40#1` (TD-050, confirmed): TD-050's upstream fix shape names crates/cobre-sddp/tests/common/parity_hash.rs:230 as a caller of the new helper while the same entry's claim concedes that site as out of scope; whether the golden label mapper rebases onto the shared base-path helper touches 

**direction-reversals** — Registered deletion / weakening directions the defenders refused; the coverage-neutral direction supersedes (9 items)

- `test-bloat-10#1` (TD-022, confirmed): Direction call on TD-022: the register frames the count-only bodies as a redundant surface to remove, this verdict confirms the opposite direction (strengthen them to the name vector); only the removal direction changes the cargo nextest list count, so the own
- `test-bloat-59#1` (TD-059, confirmed): This verdict replaces a ratified seed's direction rather than forwarding a conflict: TD-059's fix-shape drops the hand-copied CLI golden literals, which weakens a retained test's only assertions, so the confirmation keeps them and folds the solve instead. The 
- `test-bloat-60#1` (TD-060, confirmed): TD-060's ratified fix-shape directs one Hive-partitioned parquet collector parameterised by entity directory; this verdict withdraws that clause because the empty-tolerant collector's return is the asserted value at test_anticipated_lanes_output_parity.py:236 
- `test-bloat-61#1` (TD-061, confirmed): Carried from the attacker: TD-061 was ratified with a direction that deletes test_run_via_study_emits_contract_output. This verdict refuses that half as inadmissible under cost discipline and confirms only the fixture fold. The owner must ratify the re-shape o
- `test-bloat-52#3` (TD-066, confirmed): Whether TD-066's ratified removal half (dropping the two remaining run-based colour cases because banner.rs:70-73 and main.rs:127-132 pin both claims at the unit tier) is admitted under the lens-rules Rule 2 Boundary carve-out. It is declined in this verdict a
- `test-bloat-55#1` (TD-069, confirmed): TD-069's upstream fix-shape also deletes test_1dtoy_files_have_descriptions (templates.rs:183-188) and test_1dtoy_files_have_relative_paths (:191-197); lens-rules.md Rule 2 forbids deletion as a fix-shape at this station, so whether that ratified removal is ad
- `test-bloat-45#2` (TD-055, confirmed): Whether a zero-assertion test fn may be removed outright rather than strengthened: the upstream direction deletes k1_byte_stability_verdict, which carries no coverage, while Rule 2 forbids deletion as a fix-shape and its Boundary paragraph reserves such carve-
- `test-bloat-50#1` (TD-064, confirmed): Whether the eight panic-only smoke tests over the four printers may be retired once the content assertions land on the shipped path: the upstream fix shape collapses the eleven does_not_panic tests, which lowers the cargo nextest list count, so lens-rules.md R
- `test-bloat-32#1` (TD-042, confirmed): The TD-042 handover carries two incompatible fix shapes for the same entry: its registered claim states the residue adds no constructor to cobre-sddp's test_support and spreads the base at the call sites, while its upstreamFixShape promotes crates/cobre-sddp/t

**python-parity-scope** — §5.11 'keep the pytest output-parity suite unchanged' versus fixture hygiene in the parity modules (inbound from the cli-python gate) (4 items)

- `test-bloat-63#1` (TD-072, confirmed): Owner call: closing the second borrow site needs an import-line edit inside test_parity_filling_sigma.py, but docs/design/testing-architecture.md:563 (section 5.11) directs that the pytest output-parity suite be kept unchanged and TD-072s fix shape freezes all
- `test-bloat-61#2` (TD-061, confirmed): Not adjudicated here: the seed also observed that cobre.run.run solves the same deck twice (:224 and :264). Sharing one module-scoped python-output fixture (the d02_python_output shape) would remove one full train-and-simulate, but that broadens past this cand
- `test-bloat-64#1` (TD-073, confirmed): Owner call on fixture isolation: routing test_load_stochastic_missing_artifacts_raises onto the shared module-scoped run_output tree makes an assertion about an ABSENT artifact depend on no sibling test ever enabling exports.stochastic into that shared tree, w
- `test-bloat-59#2` (TD-059, confirmed): A once-initialised shared fixture in a LazyLock static never drops, so the owner must decide whether the fold may hold the shared directory in a counted guard, or whether the Contracts bullet that test output goes to TempDir and generated artifacts self-delete

**recording-notes** — Recording details with no decision to take (6 items)

- `test-bloat-01#3` (TD-001, confirmed): The candidate files yardstickRef ta-3.2, but none of the eight claim-class rows supplied for section 3.2 covers tautological assertions; the verdict therefore rests on the tree fact and on the testing.md Contracts tautology bullet (:42-45) rather than on any s
- `test-bloat-09#2` (TD-013, confirmed): The attacker's question of whether the CI schemas diff is authoritative for the export set is ANSWERED at the pin, not open: scripts/ci/check_schemas.sh runs a recursive diff of schemas/ against a fresh export (absent-file-as-empty, so removals fail) from .git
- `test-bloat-20#2` (TD-028, confirmed): The registered TD-028 claim describes the external/historical make_hydro pair as 'one-cosmetic-line' and calls mod.rs's copy a '2024 plus mirror-unit-group' divergent variant; neither holds at this pin (the pair is byte-identical, and src/sampling/mod.rs:893 i
- `test-bloat-62#2` (TD-071, confirmed): Calibration must choose which figure it records for perCrate.cobre-python.inlineCfgTestModules: 5 under the inventory's file census, or 4 files that actually declare a module, since convert.rs:104 and errors.rs:209 are prose matches and not declarations.
- `test-bloat-64#2` (TD-073, confirmed): Wording call for calibration: this station's count-neutrality bar is stated as cargo nextest list parity, which never lists the cobre-python pytest suite, so neutrality for this candidate can only be checked with a pytest collection count. Confirm the entry ma
- `architecture-01#2` (CD-116, confirmed): Recording detail for the ingest owner: the §2.2 sentence spans :76 and :78 because a line-final hyphen renders 'behavioral only, constructing fixtures per-file' as a stray list item. Whether that render break is a second, separable prose defect or part of this

**severity-rerating** — Severity re-rating requests on registered seeds (3 items)

- `test-bloat-14#2` (TD-038, confirmed): Whether TD-038's ratified severity C moves now that the surviving widening is one carrier rather than the two the attacker filed; rule 12 leaves registered severity to the owner gate.
- `test-bloat-45#1` (TD-055, confirmed): Severity: TD-055 is ratified at the lowest rank and the attacker escalates to the top rank. After this read the escalation rests on one stale roster entry out of 46 plus a guard whose substantive content is asserted elsewhere in the same binary, so the owner d
- `test-bloat-26#1` (TD-033, dismissed): TD-033 stays registered upstream and is not re-litigated here: the owner decides whether its recorded zero-behavioral-coverage basis survives the caller-tier literals at crates/cobre-stochastic/src/seeds.rs:195 and crates/cobre-stochastic/src/seeds.rs:333, whi

**Single questions**

- `test-bloat-00#2` (TD-002, confirmed): Fixture-value locality: whether both sites consume the new named constructor, or only the system-level postcard copy delegates while the value-asserting resolved-penalties module keeps its local fixture, is an owner call; both variants keep every assertion and
- `test-bloat-01#2` (TD-001, confirmed): Carried from the attacker, still live after this read: whether strengthening a retained assertion counts as coverage-neutral under Rule 2's second question, given that the retained test then asserts a strict superset of what it asserted before. Removal is inad
- `test-bloat-02#2` (TD-003, confirmed): Narrowed from the attacker's item: strengthening in place is moot for the five members that already carry a complete struct literal, but test_contract_type_equality at crates/cobre-core/src/entities/energy_contract.rs:87 constructs nothing, so its only non-fol
- `test-bloat-05#2` (TD-021, confirmed): Where the two facade-crossing version-marker tests at :1457 and :1487 belong: checkpoint.rs can import super::codec::serialize_checkpoint_manifest exactly as it already does at :13-17, or the pair can stay at the facade as the only genuinely cross-submodule te
- `test-bloat-07#1` (TD-010, confirmed): Direction for the single-consumer builder: inline make_referential_violation_case into its only caller (integration.rs:159) per the documented blast-radius principle at crates/cobre-io/src/test_support.rs:8-9, or promote it to test_support anyway for symmetry 
- `test-bloat-08#1` (TD-020, dismissed): Residue for the owner to fold or drop, not a new entry: four byte-identical unwrap schema-only preludes remain at hydro_models.rs:515-517, :705-707, :816-818 and training_writer.rs:987-989; TD-020 already dispositions that class as optional homing cleanup, so 
- `test-bloat-09#1` (TD-013, confirmed): Owner call on the expected-set source: tightening :258 to set equality needs an authoritative source for the 18 names, and the project's documented choice at crates/cobre-cli/tests/cli_schema.rs:43-44 is to derive both sides from artifacts rather than freeze a
- `test-bloat-12#1` (TD-006, confirmed): Does the MPI broadcast payload have a ratified compactness budget? No budget, const or doc exists anywhere in cobre-io at the pin (the ceiling literal at broadcast.rs:399 is its only occurrence), so whether the tightened ceiling is stated as a named budget - o
- `test-bloat-14#1` (TD-038, confirmed): Whether the name-versus-doc mismatch on test_highs_solve_iterations_positive (crates/cobre-solver/src/backends/highs/tests.rs:342 claims positive while its own doc at :339-340 states any u64 is valid) earns a separate register entry: its body is correct and do
- `test-bloat-15#1` (TD-039, confirmed): After the fold the conformance binary holds two clp tests asserting the same objective and primals at two tolerances, 1e-9 from the moved test and 1e-8 at conformance.rs:1450. Whether both are retained (count-neutral and admissible) or the tighter bound replac
- `test-bloat-27#1` (defended, dismissed): The INVERSE of this finding is real and is an owner call: crates/cobre-stochastic/src/test_support.rs is a feature-gated public module that the canonical gate excludes on the basename test heuristic (scripts/ci/check-infra-genericity.sh:98-100), and only the o
- `test-bloat-27#2` (defended, dismissed): Carried from the attacker: whether the two private one-token gates (crates/cobre-stochastic/tests/reproducibility.rs:195 and crates/cobre-io/tests/genericity_gate.rs:6) keep their unexcluded whole-src scope or fold onto the canonical runner is a cross-crate ow
- `test-bloat-28#2` (TD-036, confirmed): Reconciling the one same-name divergent pair, test_local_broadcast_root0_noop, by widening the inline fixture (src/local.rs:444, two elements against a literal) to the integration one (tests/local_conformance.rs:94, three elements against a captured copy) remo
- `test-bloat-29#2` (TD-035, confirmed): Because pub trait Communicator: Send + Sync at crates/cobre-comm/src/traits.rs:90 makes assert_communicator::<FerrompiBackend>() at tests/factory_tests.rs:97 already entail the bound its same-file sibling at :90 asserts, that copy is redundant to a test six li
- `test-bloat-32#3` (TD-042, confirmed): The third bullet of the cost-discipline rule states that the one-place property is scoped to the integration-test layer and does not hold workspace-wide for inline cfg(test) literals. Whether the 30 src-side carriers are in scope for the same de-duplication as
- `test-bloat-35#1` (TD-045, dismissed): TD-045's fix-shape wording says the local doc comment's two stated reasons are both false at the pin; at the baseline both check out (the named symbol exists at crates/cobre-sddp/src/lp/builder/test_support.rs:65 and an external test binary genuinely cannot se
- `test-bloat-36#1` (TD-046, confirmed): Fix-shape fork this defense surfaced and did not settle: the production rustdoc at noise.rs:211 links the oracle by bracketed intra-doc link, so the owner picks between moving the oracle into mod tests and demoting that link to plain backticks, or narrowing th
- `test-bloat-46#1` (TD-056, dismissed): TD-056 registers the rank field of the local stub as a dead-field defect; at the pin crates/cobre-sddp/src/simulation/state.rs:409 reads comm.rank() and passes it to assign_scenarios at :412, so rank is a consumed input fixed at 0, not a dead field. Correcting
- `test-bloat-49#2` (TD-063, confirmed): After the repoint, format_report_summary_header_present cannot keep asserting both counts nonzero (no shipped path emits it); whether it is split into the two reachable header cases, which raises the listed count by one, is an owner call under Rule 2's same-co
- `test-bloat-50#2` (TD-064, confirmed): Whether the re-homed assertions may be strengthened from contains() to a whole-render equality over the returned lines, which is the only shape that would pin the set and order of lines, since that is additive coverage beyond re-homing and would make any futur
- `test-bloat-52#1` (TD-066, confirmed): Whether an escape-absence assertion may stand as the --color never integration probe at all: cli_color.rs:130-131 records that auto-detection disables colour on a piped stderr, so the absence proves nothing about the flag unless the child environment forces co
- `test-bloat-52#2` (TD-066, confirmed): Whether the colour suite's flag-to-global wiring assertion may be re-homed onto the solve-free init subcommand path, which changes which subcommand owns the assertion while keeping the predicate identical (carried from the attacker).
- `test-bloat-53#2` (TD-067, confirmed): Whether the accumulator mean and sample-standard-deviation assertions at crates/cobre-cli/src/progress.rs:987-1007 may be re-homed to the crate that declares WelfordAccumulator, since moving a test across a crate boundary shifts which crate's test count carrie
- `test-bloat-54#2` (TD-068, confirmed): Whether the exit-code-only layer is a wanted coarse-to-fine diagnostic split: an assert_cmd Assert chain stops at the first failing predicate, so a merged test reports the exit-code failure and the message failure one at a time, while the present split gives a
- `test-bloat-55#2` (TD-069, confirmed): Whether the duplicate count pin at scripts/ci/check-docs-examples.sh:35 (EXPECTED_INPUT_FILES=11) is retired once the unit test derives the set: that script's header at :5-7 documents its constants as deliberate bump-on-change and calls itself the single sourc
- `test-bloat-56#1` (TD-070, confirmed): Whether the surviving definition stays a named private helper or the call sites adopt the inline standard-formatter form already used at summary.rs:858: the body is a no-op re-derivation of that formatter, so the choice decides whether the added table-driven t
- `test-bloat-58#1` (TD-058, confirmed): TD-058's other half (test_run.py:38 asserting a subset of test_outputs.py:33-50 on the shared 1dtoy deck) falls outside this candidate's scope; the owner decides whether a sibling candidate carries it or calibration folds it into this entry, since removing tha
- `test-bloat-60#2` (TD-060, confirmed): The fold replaces four divergent builder docstrings with one contract statement plus per-call-site notes. Each current docstring names a different case-specific reason (D41 is training-only; the anticipated cases ship simulation disabled; several deterministic
- `test-bloat-51#3` (TD-065, confirmed): Whether the deliberate-duplication comment at crates/cobre-cli/tests/cli_color.rs:20-21 is overruled. It states the run-versus-colour fixture duplication is an intentional self-containment choice, so folding that pair retires a recorded decision and needs the 
- `test-bloat-66#1` (TD-076, confirmed): Whether the doc-integrity default of no frozen count without a guard extends to Python docstrings and Rust doc comments is an owner call; it decides whether this fix-shape generalizes to sibling prose or stays a single-clause correction in this module.
- `over-engineering-01#1` (defended, dismissed): The module doc at crates/cobre-sddp/tests/common/anticipated_structural_assertions.rs:1 calls the helpers shared by the anticipated-thermals integration tests, plural, while at the pin each of its three public helpers has exactly one call site, in one test fun
- `over-engineering-00#1` (OD-051, confirmed): Scope call beyond this candidate: `eq` at crates/cobre-sddp/src/test_support.rs:152-171 has exactly one caller at the pin (crates/cobre-sddp/tests/basis_trajectory_probe.rs:282), so retiring only its zero-caller sibling leaves a one-consumer positional helper 

### 1.9 Phase-0a gate substrate — presented read-only (round R13)

`gate-substrate.json`: golden roster `crates/cobre-sddp/tests/parity.rs` `mod parity_hash_highs` (crates/cobre-sddp/tests/parity.rs:40) and `mod parity_hash_clp` (crates/cobre-sddp/tests/parity.rs:200), cases D06, D15, D30, D34, D41, helpers in `crates/cobre-sddp/tests/common/parity_hash.rs` (compute_parity_hash :48, run_golden_case :375, regen_golden_case :390, assert_permutation_hash :406); determinism gates `crates/cobre-sddp/tests/mpi_wire.rs` (17 `mod` blocks; the ticket's ten resolve at their stated lines); baseline decks `crates/cobre-sddp/tests/fixtures/parity_baselines` and `parity_baselines_clp` (five `.sha256` each). Cadence fact: the roster is `slow-tests`-gated, `slow-tests` sits inside `NON_SOLVER_FEATURES` (ci.yml:32) and the HiGHS job (ci.yml:114) runs it on every pull request. No finding, no id, no proposed change.

### 1.10 Verification status (E08-6)

`verification.md`: 18 PASS rows, 0 FAIL rows — shared verify-station.sh 7/7 and the station verifier 10/10 over the scaffold heading; the three checkers exit 0.

## 2. Round plan

Assembled before any question is asked. Options are capped at four per question; the recommended option is listed first; per-entry exceptions inside a batch are named through the free-text option. Figures first, then one dedicated round per definition-ambiguous crate, then the severity batches, the seed folds and retire proposals, the target gaps, the dup-of round (no option issues a fresh id), one drift-adjudication round per present-tense claim, the needs-human questions, the informational substrate and the handoff confirmation.

| Round | Kind | Subject | Options |
|---|---|---|---|
| R0-figures | figures-first | the 15 re-measured figures in inventory.json (9 §2.1 rows + 6 §2.3/§3.2 prose figures; all measured, none estimated) | ratify as the sole citation base · re-measure named figures · accept with named gaps · defer |
| R1-def-sddp | definition-ambiguous | cobre-sddp integration tests: 40 binaries (depth-1 git ls-tree, what Cargo links) vs 56 files (recursive, sweeps tests/common/*.rs and tests/fixtures/**) — the ticket's 37/53 is the scaffold-pin figure | both, labelled (every count names its definition) · binary authoritative · file authoritative · needs-human stays |
| R2-def-io | definition-ambiguous | cobre-io integration tests: 12 binaries vs 13 files — asked separately | both, labelled · binary authoritative · file authoritative · needs-human stays |
| R3-sevB | severity-batch | minted Sev B entries: TD-074, TD-075 | accept all as recorded · downgrade (name) · reject (name) · defer (name + trigger) |
| R4-sevC | severity-batch | minted Sev C entries: TD-076, TD-077, CD-116, CD-117, CD-118, OD-051 (CD-116 and CD-118 sit at C under the claimKind gate, reviewer B) | accept all as recorded · downgrade / reject (name) · promote (name) · defer (name + trigger) |
| R5-folds | seed-folds | 50 confirmed deltas folded onto their seed TD ids and 7 dismissed deltas (entries unchanged) | ratify all folds as recorded · name folds to reject · defer |
| R6-retire | seed-retire | 10 seeds stale at the pin — retire proposals (TD-004, TD-005, TD-007, TD-009, TD-014, TD-017, TD-018, TD-023, TD-024, TD-029); 9 already carry a Status: fixed (2026-09-15) bullet, TD-017's anchors are all absent | retire all ten → Cleared · retire the nine with a Status: fixed bullet, hold TD-017 · hold all for E11 · defer |
| R7-target-gaps | target-gaps | 9 §5 target gaps dismissed target-not-defect (test-bloat-03, test-bloat-18, test-bloat-25, test-bloat-30, test-bloat-43, test-bloat-68, test-bloat-69, test-bloat-70, performance-01) | ratify as roadmap items, no id · promote named to Sev C actionable=no entries · defer |
| R8-dup-of | dup-of | three dup-of merges into the open mirror items (oracle close, lp/builder inline giants, python CI-visibility premise superseded) — no option issues a fresh id | accept merges · sharpen wording (name) · reject a claim (name) · defer |
| R9-drift-5.8 | drift-adjudication | §5.8 StubComm/Rank0Of2 sentence (:524-526, :415-416) — both defenders: target stated in present tense (test-bloat-30 doc side, test-bloat-43 tree side) | target, not defect (as recorded) · prose drift · both · defer |
| R10-drift-5.2 | drift-adjudication | §5.2 Adoption paragraph (:425-431) vs the tree — CD-118 confirmed prose-drift (no manifest enables cobre-sddp/test-support; bullet-1 placements uncarried) | prose drift (as recorded) · target, not defect · both · defer |
| R11-drift-2.2 | drift-adjudication | §2.2 map clause 'constructing fixtures per-file' (:76-78) vs two shared harnesses — CD-116 confirmed prose-drift | prose drift (as recorded) · target, not defect · reject · defer |
| R12-nh-rule2-carveout | needs-human (themed) | Rule 2 Boundary — folds of exact-duplicate / strict-subset assertions change the nextest count — 10 items: test-bloat-01#1, test-bloat-02#1, test-bloat-16#1, test-bloat-28#1, test-bloat-29#1, test-bloat-41#1, test-bloat-49#1, test-bloat-53#1, test-bloat-54#1, test-bloat-62#1 | Ratify the carve-out (Recommended) · Uphold Rule 2 strictly · Carve-out for exact duplicates only · Defer to E11 |
| R12-nh-s51-threshold | needs-human (themed) | §5.1 homing threshold (~500 test-LOC / ~40 test fns) and the inline-versus-sibling direction — 7 items: test-bloat-03#1, test-bloat-05#1, test-bloat-06#1, test-bloat-06#2, test-bloat-18#1, test-bloat-25#1, test-bloat-48#1 | Not at this gate — §5.1 stays a Proposal (Recommended) · Ratify ~500 LOC / ~40 fns now · Ratify a different threshold · Defer |
| R12-nh-fixture-destination | needs-human (themed) | Destination of shared fixtures and harness doubles (crate-local now vs the §5.2 collapse; the rank-shape Communicator double) — 15 items: test-bloat-11#1, test-bloat-19#1, test-bloat-20#1, test-bloat-22#1, test-bloat-24#1, test-bloat-33#1, test-bloat-47#1, test-bloat-51#1, test-bloat-51#2, test-bloa | Crate-local home now (Recommended) · Wait for the §5.2 collapse · cobre-comm gains test-support now · Defer |
| R12-nh-cli-activation | needs-human (themed) | cobre-cli's cobre-core/test-support dev-dependency activation (Cargo.toml:62) with no consumer — 3 items: test-bloat-57#1, test-bloat-47#2, test-bloat-51#4 | Drop the activation (Recommended) · Keep it with a reservation note · Keep it silently · Defer |
| R12-nh-sddp-self-dev-dep | needs-human (themed) | cobre-sddp self dev-dependency on its own test-support feature (CD-118 tree side) — 1 items: architecture-00#1 | Add the self dev-dependency now (Recommended) · Keep command-line activation until the §5.2 collapse · Defer |
| R12-nh-s52-doc-side | needs-human (themed) | §5.2 doc side — the Adoption paragraph's uncarried list, cobre-solver's narrow gate, the two small harnesses as collapse sources — 5 items: architecture-00#2, architecture-00#3, architecture-01#1, architecture-02#1, test-bloat-30#1 | Correct the paragraph, record the exception, name the two harnesses as consumers (Recommended) · Re-scope the paragraph to bullets 2 and 3 only · Leave the yardstick as is · Defer |
| R12-nh-slurm-trigger | needs-human (themed) | mpi-slurm.yml full mode: which trigger runs the sbatch tiers (TD-074) — 2 items: test-bloat-71#1, test-bloat-71#2 | Nightly schedule runs full; PR/push keep reduced with the boot smoke check (Recommended) · Push to develop/main runs full · Tags only run full · Defer |
| R12-nh-slurm-path-filter | needs-human (themed) | mpi-slurm.yml path filter: enumerate src/cut/** or widen to the crate source tree (TD-075) — 2 items: test-bloat-72#1, test-bloat-72#2 | Widen both lists to crates/cobre-sddp/src/** (Recommended) · Add src/cut/** to both lists · Fold into the cadence decision · Defer |
| R12-nh-runner-cadence-targets | needs-human (themed) | §5.5 / §5.6 runner and cadence targets — sequencing notes, not defects (dismissed target gaps and CI-shape rows) — 10 items: test-bloat-67#1, test-bloat-68#1, test-bloat-69#1, test-bloat-70#1, test-bloat-70#2, performance-00#1, performance-00#2, performance-02#1, performance-02#2, performance-01#1 | Record as roadmap sequencing notes, no decision now (Recommended) · Ratify §5.5 nextest adoption now · Ratify the §5.6 tier split now · Defer |
| R12-nh-scope-extensions | needs-human (themed) | Newly found copies that widen a seed's scope at the pin — 11 items: test-bloat-00#1, test-bloat-11#2, test-bloat-22#2, test-bloat-19#2, test-bloat-13#1, test-bloat-34#1, test-bloat-44#1, test-bloat-31#1, test-bloat-32#2, test-bloat-39#1, test-bloat-40#1 | Fold the newly found copies into the same entry's scope (Recommended) · Keep the registered scope · Decide per entry · Defer |
| R12-nh-direction-reversals | needs-human (themed) | Registered deletion / weakening directions the defenders refused; the coverage-neutral direction supersedes — 9 items: test-bloat-10#1, test-bloat-59#1, test-bloat-60#1, test-bloat-61#1, test-bloat-52#3, test-bloat-55#1, test-bloat-45#2, test-bloat-50#1, test-bloat-32#1 | The defender's coverage-neutral direction supersedes (Recommended) · Keep the registered direction · Decide per entry · Defer |
| R12-nh-python-parity-scope | needs-human (themed) | §5.11 'keep the pytest output-parity suite unchanged' versus fixture hygiene in the parity modules (inbound from the cli-python gate) — 4 items: test-bloat-63#1, test-bloat-61#2, test-bloat-64#1, test-bloat-59#2 | §5.11 freezes assertions, not fixtures (Recommended) · §5.11 freezes the modules entirely · Decide per delta · Defer |
| R12-nh-recording-notes | needs-human (themed) | Recording details with no decision to take — 6 items: test-bloat-01#3, test-bloat-09#2, test-bloat-20#2, test-bloat-62#2, test-bloat-64#2, architecture-01#2 | Record as notes, no decision (Recommended) · Open one of them as a decision · Drop them |
| R12-nh-severity-rerating | needs-human (themed) | Severity re-rating requests on registered seeds — 3 items: test-bloat-14#2, test-bloat-45#1, test-bloat-26#1 | Keep every registered severity (Recommended) · Re-rate named entries · Defer |
| R12-nh-test-bloat-00#2 | needs-human (single) | TD-002 / test-bloat-00 #2: Fixture-value locality: whether both sites consume the new named constructor, or only the system-level postcard copy delegates while the value-asserting resolve… | Both sites take the named constructor (Recommended) · Only the postcard copy delegates · Defer |
| R12-nh-test-bloat-01#2 | needs-human (single) | TD-001 / test-bloat-01 #2: Carried from the attacker, still live after this read: whether strengthening a retained assertion counts as coverage-neutral under Rule 2's second question, giv… | Strengthening a retained assertion is coverage-neutral (Recommended) · Strengthening needs its own entry · Defer |
| R12-nh-test-bloat-02#2 | needs-human (single) | TD-003 / test-bloat-02 #2: Narrowed from the attacker's item: strengthening in place is moot for the five members that already carry a complete struct literal, but test_contract_type_equa… | Keep test_contract_type_equality as the one structural pin; fold the derive-discharged lines (Recommended) · Keep all five modules' comparison lines · Defer |
| R12-nh-test-bloat-05#2 | needs-human (single) | TD-021 / test-bloat-05 #2: Where the two facade-crossing version-marker tests at :1457 and :1487 belong: checkpoint.rs can import super::codec::serialize_checkpoint_manifest exactly as it… | Keep the two version-marker tests in checkpoint.rs importing super::codec (Recommended) · Move them beside the codec · Defer |
| R12-nh-test-bloat-07#1 | needs-human (single) | TD-010 / test-bloat-07 #1: Direction for the single-consumer builder: inline make_referential_violation_case into its only caller (integration.rs:159) per the documented blast-radius prin… | Inline the single-consumer builder into its only caller (Recommended) · Keep the builder in test_support · Defer |
| R12-nh-test-bloat-08#1 | needs-human (single) | TD-020 / test-bloat-08 #1: Residue for the owner to fold or drop, not a new entry: four byte-identical unwrap schema-only preludes remain at hydro_models.rs:515-517, :705-707, :816-818 an… | Fold the four schema-only preludes into the existing helper as a residue of TD-020 (Recommended) · Drop the residue · Defer |
| R12-nh-test-bloat-09#1 | needs-human (single) | TD-013 / test-bloat-09 #1: Owner call on the expected-set source: tightening :258 to set equality needs an authoritative source for the 18 names, and the project's documented choice at cr… | The CI schemas diff (scripts/ci/check_schemas.sh) is the authoritative expected set (Recommended) · Keep the loose superset assertion · Defer |
| R12-nh-test-bloat-12#1 | needs-human (single) | TD-006 / test-bloat-12 #1: Does the MPI broadcast payload have a ratified compactness budget? No budget, const or doc exists anywhere in cobre-io at the pin (the ceiling literal at broadc… | No compactness budget exists; record the ceiling literal as a tautology to strengthen, not a contract (Recommended) · Ratify a byte budget now · Defer |
| R12-nh-test-bloat-14#1 | needs-human (single) | TD-038 / test-bloat-14 #1: Whether the name-versus-doc mismatch on test_highs_solve_iterations_positive (crates/cobre-solver/src/backends/highs/tests.rs:342 claims positive while its own … | Fix the name-versus-doc mismatch as part of TD-038's fold (Recommended) · Leave the mismatch · Defer |
| R12-nh-test-bloat-15#1 | needs-human (single) | TD-039 / test-bloat-15 #1: After the fold the conformance binary holds two clp tests asserting the same objective and primals at two tolerances, 1e-9 from the moved test and 1e-8 at confo… | Keep both tolerances, document why (Recommended) · Unify to the tighter tolerance · Defer |
| R12-nh-test-bloat-27#1 | needs-human (single) | defended / test-bloat-27 #1: The INVERSE of this finding is real and is an owner call: crates/cobre-stochastic/src/test_support.rs is a feature-gated public module that the canonical gate e… | Record the inverse as a follow-up for the genericity gate owner (E11), no entry here (Recommended) · Mint an entry now · Defer |
| R12-nh-test-bloat-27#2 | needs-human (single) | defended / test-bloat-27 #2: Carried from the attacker: whether the two private one-token gates (crates/cobre-stochastic/tests/reproducibility.rs:195 and crates/cobre-io/tests/genericity_ga… | Keep the two private one-token gates (Recommended) · Fold them into the canonical gate · Defer |
| R12-nh-test-bloat-28#2 | needs-human (single) | TD-036 / test-bloat-28 #2: Reconciling the one same-name divergent pair, test_local_broadcast_root0_noop, by widening the inline fixture (src/local.rs:444, two elements against a literal)… | Widen the inline fixture to the integration shape and keep one body (Recommended) · Keep both bodies · Defer |
| R12-nh-test-bloat-29#2 | needs-human (single) | TD-035 / test-bloat-29 #2: Because pub trait Communicator: Send + Sync at crates/cobre-comm/src/traits.rs:90 makes assert_communicator::<FerrompiBackend>() at tests/factory_tests.rs:97 al… | Drop the redundant compile-time assertion as a strict duplicate under the carve-out (Recommended) · Keep it as documentation · Defer |
| R12-nh-test-bloat-32#3 | needs-human (single) | TD-042 / test-bloat-32 #3: The third bullet of the cost-discipline rule states that the one-place property is scoped to the integration-test layer and does not hold workspace-wide for inl… | The one-place property stays scoped to the integration layer (Recommended) · Extend one-place workspace-wide · Defer |
| R12-nh-test-bloat-35#1 | needs-human (single) | TD-045 / test-bloat-35 #1: TD-045's fix-shape wording says the local doc comment's two stated reasons are both false at the pin; at the baseline both check out (the named symbol exists at… | TD-045's fix-shape wording is corrected: both local reasons hold at the pin (Recommended) · Keep the wording · Defer |
| R12-nh-test-bloat-36#1 | needs-human (single) | TD-046 / test-bloat-36 #1: Fix-shape fork this defense surfaced and did not settle: the production rustdoc at noise.rs:211 links the oracle by bracketed intra-doc link, so the owner picks… | Keep the intra-doc link in production rustdoc; move the oracle body (Recommended) · Replace the link with prose · Defer |
| R12-nh-test-bloat-46#1 | needs-human (single) | TD-056 / test-bloat-46 #1: TD-056 registers the rank field of the local stub as a dead-field defect; at the pin crates/cobre-sddp/src/simulation/state.rs:409 reads comm.rank() and passes … | Correct TD-056: the rank field is read at state.rs:409 (Recommended) · Keep TD-056 as registered · Defer |
| R12-nh-test-bloat-49#2 | needs-human (single) | TD-063 / test-bloat-49 #2: After the repoint, format_report_summary_header_present cannot keep asserting both counts nonzero (no shipped path emits it); whether it is split into the two r… | Split format_report_summary_header_present into the two reachable halves (Recommended) · Keep one test asserting both · Defer |
| R12-nh-test-bloat-50#2 | needs-human (single) | TD-064 / test-bloat-50 #2: Whether the re-homed assertions may be strengthened from contains() to a whole-render equality over the returned lines, which is the only shape that would pin t… | Strengthen to whole-render equality over the returned lines (Recommended) · Keep contains() · Defer |
| R12-nh-test-bloat-52#1 | needs-human (single) | TD-066 / test-bloat-52 #1: Whether an escape-absence assertion may stand as the --color never integration probe at all: cli_color.rs:130-131 records that auto-detection disables colour on… | Keep the escape-absence assertion as the --color never probe (Recommended) · Replace with a forced-tty probe · Defer |
| R12-nh-test-bloat-52#2 | needs-human (single) | TD-066 / test-bloat-52 #2: Whether the colour suite's flag-to-global wiring assertion may be re-homed onto the solve-free init subcommand path, which changes which subcommand owns the ass… | Re-home the flag-to-global wiring assertion onto the solve-free init path (Recommended) · Keep it on the run path · Defer |
| R12-nh-test-bloat-53#2 | needs-human (single) | TD-067 / test-bloat-53 #2: Whether the accumulator mean and sample-standard-deviation assertions at crates/cobre-cli/src/progress.rs:987-1007 may be re-homed to the crate that declares We… | Re-home the Welford mean / std assertions to the crate that declares the accumulator (Recommended) · Keep them in cobre-cli · Defer |
| R12-nh-test-bloat-54#2 | needs-human (single) | TD-068 / test-bloat-54 #2: Whether the exit-code-only layer is a wanted coarse-to-fine diagnostic split: an assert_cmd Assert chain stops at the first failing predicate, so a merged test … | Keep the exit-code-only layer as the coarse diagnostic (Recommended) · Merge into the fine-grained tests · Defer |
| R12-nh-test-bloat-55#2 | needs-human (single) | TD-069 / test-bloat-55 #2: Whether the duplicate count pin at scripts/ci/check-docs-examples.sh:35 (EXPECTED_INPUT_FILES=11) is retired once the unit test derives the set: that script's h… | Retire the duplicate count pin in check-docs-examples.sh once the unit test derives the set (Recommended) · Keep both pins · Defer |
| R12-nh-test-bloat-56#1 | needs-human (single) | TD-070 / test-bloat-56 #1: Whether the surviving definition stays a named private helper or the call sites adopt the inline standard-formatter form already used at summary.rs:858: the bod… | Adopt the inline standard-formatter form at every call site (Recommended) · Keep the named helper · Defer |
| R12-nh-test-bloat-58#1 | needs-human (single) | TD-058 / test-bloat-58 #1: TD-058's other half (test_run.py:38 asserting a subset of test_outputs.py:33-50 on the shared 1dtoy deck) falls outside this candidate's scope; the owner decide… | TD-058's other half (test_run.py subset of test_outputs.py) is re-stated under the Rule 2 decision (Recommended) · Drop the other half · Defer |
| R12-nh-test-bloat-60#2 | needs-human (single) | TD-060 / test-bloat-60 #2: The fold replaces four divergent builder docstrings with one contract statement plus per-call-site notes. Each current docstring names a different case-specific… | One contract statement plus per-call-site notes (Recommended) · Keep four docstrings · Defer |
| R12-nh-test-bloat-51#3 | needs-human (single) | TD-065 / test-bloat-51 #3: Whether the deliberate-duplication comment at crates/cobre-cli/tests/cli_color.rs:20-21 is overruled. It states the run-versus-colour fixture duplication is an … | Overrule the deliberate-duplication comment (Recommended) · Honour the comment · Defer |
| R12-nh-test-bloat-66#1 | needs-human (single) | TD-076 / test-bloat-66 #1: Whether the doc-integrity default of no frozen count without a guard extends to Python docstrings and Rust doc comments is an owner call; it decides whether thi… | The no-frozen-count default extends to Python docstrings and Rust doc comments (Recommended) · Single-clause correction only · Defer |
| R12-nh-over-engineering-01#1 | needs-human (single) | defended / over-engineering-01 #1: The module doc at crates/cobre-sddp/tests/common/anticipated_structural_assertions.rs:1 calls the helpers shared by the anticipated-thermals integration tests, … | Keep the aggregator as recorded; correct the module doc's plural (Recommended) · Re-open as an OD entry · Defer |
| R12-nh-over-engineering-00#1 | needs-human (single) | OD-051 / over-engineering-00 #1: Scope call beyond this candidate: eq at crates/cobre-sddp/src/test_support.rs:152-171 has exactly one caller at the pin (crates/cobre-sddp/tests/basis_traject… | Retire eq_with_anticipated; keep eq while it has a caller (Recommended) · Retire both positional constructors · Defer |
| R13-substrate | informational | Phase-0a gate substrate (gate-substrate.json): golden parity_hash_* roster, 18 mpi_wire.rs modules, the two parity_baselines decks — reported, not proposed | keep informational · expand report (name) · drop from report · defer |
| R14-handoffs | handoffs | E9 / E10 / E11 / roadmap handoffs and the RETURNED marker | confirm · amend (name) |

**No round is offered for:** the coverage-reduction candidates — none reached the gate (the attackers dropped the four deletion-only seeds before dispatch and the E08-4 screen dismissed none; the four seeds are presented as fix-shape refusals under the Rule 2 question); the slow-tests feature declarations, the mpi-slurm SLURM job's existence and the parity baselines themselves (sanctioned / out of scope — no re-baselining decision is offered); the 11 defender dismissals on a tree or rule basis (presented read-only in 1.7).

## 3. Decision record

**Decision: ratified** — 2026-09-19, main session, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`. Presented 8 minted entries, 72 seed dispositions, 3 dup-of merges, 4 drift adjudications, 9 target gaps and 120 needs-human items over 15 AskUserQuestion calls. Every answer was the recommended option unless the Rationale column says otherwise.

### 3.1 Figures and measurement definitions (rounds R0-R2)

- **R0-figures** — Ratify: The fifteen re-measured figures in inventory.json are the sole citation base; every entry cites them with its definition and no figure is quoted from testing-architecture.md (the yardstick's 37/53 and 73/54 snapshots are superseded).
- **R1-def-sddp** — Both, labelled: Both keys are kept; every entry states which definition it used and no entry may flip between them. (cobre-sddp 40 binaries / 56 files)
- **R2-def-io** — Both, labelled: Both keys are kept, consistent with cobre-sddp; every entry names its definition. (cobre-io 12 binaries / 13 files)

### 3.2 Minted entries (rounds R3-R4)

- **R3-sevB** — Accept all as recorded: TD-074 and TD-075 enter the actionable set at Sev B, tree-fact, neutral; the trigger and the filter shape are decided in the needs-human rounds.
- **R4-sevC** — Accept all as recorded: TD-076, TD-077, CD-116, CD-117, CD-118 and OD-051 enter the actionable set at Sev C; the two prose-drift rows keep 'C (reviewer: B)' under the claimKind gate and route their doc side to E11.

| ID | Decision | Severity | claimKind | Definition | Alignment | Rationale (owner) | Trigger / handoff |
|---|---|---|---|---|---|---|---|
| TD-074 | accept | B | tree-fact | n/a | neutral | as recorded (R3-sevB: Accept all as recorded); R12-nh-slurm-trigger: Nightly runs full; PR/push keep reduced + boot smoke check | - |
| TD-075 | accept | B | tree-fact | n/a | neutral | as recorded (R3-sevB: Accept all as recorded); R12-nh-slurm-path-filter: Widen both lists to crates/cobre-sddp/src/** | - |
| TD-076 | accept | C | tree-fact | n/a | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-test-bloat-66#1: Extend the default to docstrings and doc comments | - |
| TD-077 | accept | C | tree-fact | n/a | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-cli-activation: Drop the activation | - |
| CD-116 | accept | C (reviewer: B) | prose-drift | file (10) | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-s52-doc-side: Correct, record the exception, name the two harnesses as consumers; R12-nh-recording-notes: Record as notes, no decision | - |
| CD-117 | accept | C | tree-fact | file (13) | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-s52-doc-side: Correct, record the exception, name the two harnesses as consumers | - |
| CD-118 | accept | C (reviewer: B) | prose-drift | binary (40) | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-sddp-self-dev-dep: Add the self dev-dependency now; R12-nh-s52-doc-side: Correct, record the exception, name the two harnesses as consumers | - |
| OD-051 | accept | C | tree-fact | n/a | neutral | as recorded (R4-sevC: Accept all as recorded); R12-nh-over-engineering-00#1: Retire eq_with_anticipated; keep eq while it has a caller | - |

### 3.3 Seed dispositions (rounds R5-R6, the Rule 2 question)

- **R5-folds** — Ratify all folds as recorded: The 50 sharpenings become the entries' current reading; the 7 dismissed deltas are recorded as dissent only; their needs-human items are decided in the R12 rounds. (50 of 50 folds ratified; the 7 dismissed deltas leave their entries unchanged)
- **R6-retire** — Retire all ten → Cleared: TD-004, TD-005, TD-007, TD-009, TD-014, TD-017, TD-018, TD-023, TD-024 and TD-029 are stale at the pin (nine already carry a Status: fixed bullet; TD-017's anchors are absent); a same-line Retired clause is appended to each entry and E11 confirms the exact resolving commit. (retired → Cleared: TD-004, TD-005, TD-007, TD-009, TD-014, TD-017, TD-018, TD-023, TD-024, TD-029)
- **Rule 2 Boundary** — Ratify the carve-out: A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, their deletion-only fix-shapes stay refused, and their residue is re-stated as a fold under this carve-out; E11 restates lens-rules Rule 2 accordingly. (fix-shape refusals TD-011, TD-012, TD-015, TD-019)

### 3.4 dup-of merges (round R8)

- **R8-dup-of** — Accept merges: All three stay merged into the existing mirror items with their pin anchors; the sharpened wording, including the superseded python CI-visibility premise, travels to E11's mirror write-back; no fresh TD id.
- **Oracle test-harness duplication** (`docs/design/reserved-seams-and-deferred-debt.md:308`) ← test-bloat-04 — merge stands; sharpened anchor to E11: `crates/cobre-sddp/tests/extensive_form_oracle.rs::close` + `crates/cobre-sddp/tests/branching_value_oracle.rs::close` (four carriers at the pin: also `crates/cobre-sddp/tests/node_native_backward_gat
- **Mega-file / inline-test-giant asymmetry** (`docs/design/reserved-seams-and-deferred-debt.md:711`) ← test-bloat-37 — merge stands; sharpened anchor to E11: `crates/cobre-sddp/src/lp/builder/entries.rs:1680` and `crates/cobre-sddp/src/lp/builder/columns.rs:1305` inline `#[cfg(test)]` vs the extracted `crates/cobre-sddp/src/lp/builder/template/tests.rs` (`
- **Python-binding Rust tests invisible to CI** (`docs/design/reserved-seams-and-deferred-debt.md:347`) ← test-bloat-65 — merge stands; sharpened anchor to E11: `crates/cobre-python/Cargo.toml:34` ([dev-dependencies] names no cobre-sddp test-support) — the mirror's 'never compiled in CI' premise is superseded at the pin by `.github/workflows/ci.yml:567` (carg

### 3.5 Drift adjudications and target gaps (rounds R7, R9-R11)

| Claim | Decision | targetNotDefect | Recorded as | Rationale (owner) |
|---|---|---|---|---|
| §5.8 StubComm/Rank0Of2 (doc + tree sides) | Target, not defect | true | roadmap item, no id (seed TD-053 stands) | Both sides: targetNotDefect = true; the §5.8 sentence is a §5 target stated in present tense under the Proposal status line; the relocation is a roadmap item with no TD id, seed TD-053 stands as registered, and the shared rank-shape double's destination is decided in the fixture-destination round. |
| §5.2 Adoption paragraph (CD-118) | Prose drift, as recorded | false | CD-118 Sev C prose-drift; doc side → E11 | CD-118 stands at C prose-drift (reviewer B): the Adoption paragraph's present-tense claims are contradicted at the pin; the doc side goes to E11 with the s52-doc-side direction and the tree side is decided in the self-dev-dependency round. |
| §2.2 map clause (CD-116) | Prose drift, as recorded | false | CD-116 Sev C prose-drift; doc side → E11 | CD-116 stands at C prose-drift (reviewer B): the §2.2 clause is contradicted by two shared harnesses at the pin and by the doc's own 'only LARGE' qualifier; the tree side is the true side and nothing moves; E11 corrects the clause. |
| 9 §5 target gaps | Ratify as roadmap items, no id | true | roadmap items, no id, actionable = no | The yardstick is a Proposal whose adoption is not owner-ratified; the nine gaps travel to E11 with the §5 ratification, never as code defects. |

### 3.6 Worker needs-human answers (round R12, 120/120)

| # | Entry / seed | From | Round | Question | Answer | Rationale |
|---|---|---|---|---|---|---|
| 1 | TD-001 | defender:test-bloat-01 #1 | R12-nh-rule2-carveout | The fold in coverageNeutralShape merges two test functions into one, so cargo nextest list reports one fewer test; lens-rules.md Rule 2 Boundary records that count carve-out as an owner-gate decision, not a defender's. T… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 2 | TD-003 | defender:test-bloat-02 #1 | R12-nh-rule2-carveout | The consolidation fold changes the cargo nextest list count by the number of folded functions; lens-rules.md Rule 2 Boundary records that carve-out (an exact-subset assertion folded into a named retained superset on an i… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 3 | TD-040 | defender:test-bloat-16 #1 | R12-nh-rule2-carveout | TD-040's registered fix-shape removes both tests under either branch, a cargo nextest list delta of two, while this confirmation's residue keeps both: the owner must decide at the E08 gate whether the ratified removal di… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 4 | TD-036 | defender:test-bloat-28 #1 | R12-nh-rule2-carveout | Count parity: the shared-body fold above is count-neutral, but TD-036's own direction retires the inline copies, which changes the cargo nextest list count by the number of folded duplicates; whether the ratified duplica… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 5 | TD-035 | defender:test-bloat-29 #1 | R12-nh-rule2-carveout | Does the ratified duplicate-deletion carve-out recorded in lens-rules.md Rule 2 Boundary (TD-011, TD-015, TD-051, TD-067, TD-068) reach compile-time assertion duplicates, so TD-035's removal half may execute as written a… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 6 | TD-051 | defender:test-bloat-41 #1 | R12-nh-rule2-carveout | Folding the weaker count-mismatch probe into its retained sibling drops the cargo nextest list --features test-support count by one; lens-rules.md Rule 2 'Boundary' leaves that carve-out for the ratified duplicate class … | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 7 | TD-063 | defender:test-bloat-49 #1 | R12-nh-rule2-carveout | TD-063's upstream fix-shape also folds the five format_report_* cases into one table-driven test, which lowers the cargo nextest list count; this verdict confirms only the count-neutral repoint and leaves that fold to th… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 8 | TD-067 | defender:test-bloat-53 #1 | R12-nh-rule2-carveout | The consolidation fold removes exact-duplicate assertions and therefore changes the cargo nextest list count; lens-rules.md section Rule 2 Boundary records that carve-out for the ratified duplication class (TD-067 here, … | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 9 | TD-068 | defender:test-bloat-54 #1 | R12-nh-rule2-carveout | The fold changes the cargo nextest list count: by four under TD-068's registered direction, or by five once the newly verified domination of missing_buses_json_stdout_contains_error (crates/cobre-cli/tests/cli_validate.r… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 10 | TD-071 | defender:test-bloat-62 #1 | R12-nh-rule2-carveout | The shape is additive, so the crate's cargo nextest list count rises rather than staying equal; the owner must say whether section 6 count parity and lens-rules Rule 2's count test admit added coverage or need an explici… | **Ratify the carve-out** | A fold that keeps every distinct assertion in the retained sibling and folds only exact-duplicate or strict-subset assertions may lower the nextest count; additive shapes may raise it. TD-011, TD-012, TD-015 and TD-019 keep their entries, t |
| 11 | defended | defender:test-bloat-03 #1 | R12-nh-s51-threshold | If the owner ratifies the section 5.1 homing threshold, decide whether cobre-core system/mod.rs (43 test fns from line 615) and model/resolved/bounds.rs (37 from line 1193) enter the already-registered re-homing scope as… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 12 | TD-021 | defender:test-bloat-05 #1 | R12-nh-s51-threshold | Destination form is an owner call, not settled here: the 5.1 inline-versus-sibling threshold is unratified at the pin (claim row ta-5.1-homing-threshold is class target, measured as no ratified threshold, TD-031), so whe… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 13 | TD-008 | defender:test-bloat-06 #1 | R12-nh-s51-threshold | TD-008 is a registered entry whose claim is that the scenarios/ homing inconsistency is the durable threshold-INDEPENDENT residue; this dismissal is of the sharpening only (rule 12 bars re-litigating the upstream severit… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 14 | TD-008 | defender:test-bloat-06 #2 | R12-nh-s51-threshold | Carried and re-scoped from the attacker: 'which homing direction should the scenarios directory adopt' decides nothing for scenarios/ after this read, since the directory already satisfies any threshold between its large… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 15 | defended | defender:test-bloat-18 #1 | R12-nh-s51-threshold | figures.homing-split.perCrate.cobre-solver.inlineCfgTestModules = 8 counts files carrying the literal #[cfg(test)], which for this crate is the wrong set on both sides: it includes three files with no inline test body (b… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 16 | TD-031 | defender:test-bloat-25 #1 | R12-nh-s51-threshold | Ratify or reject the section 5.1 homing threshold (roughly 500 test-LOC or 40 test fns). Until it is ratified no candidate can name which inline tails must move, and if it is ratified the files it selects in cobre-stocha… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 17 | TD-062 | defender:test-bloat-48 #1 | R12-nh-s51-threshold | Whether an intra-crate module re-home counts as a 'rename' under the section 5.1 migration invariant at docs/design/testing-architecture.md:351-354: the two function names and the total count are unchanged, but the fully… | **Not at this gate — §5.1 stays a Proposal** | The three dismissals stand as roadmap items; homing entries keep their registered claims; the threshold is ratified, if ever, with the yardstick itself at E11; an intra-crate re-home is not a rename under :351-354. |
| 18 | TD-016 | defender:test-bloat-11 #1 | R12-nh-fixture-destination | Destination pick for the shared make_pumping: cobre-io own test_support (which widens that crate test-support export surface, against the blast-radius sentence at crates/cobre-io/src/test_support.rs:8-9) versus cobre-cor… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 19 | TD-032 | defender:test-bloat-19 #1 | R12-nh-fixture-destination | Destination for the three consolidated undated presets: crates/cobre-stochastic/src/test_support.rs, whose module doc scopes it to this crate's OpeningTree, SeasonMap and InflowModel shapes so entity presets widen that d… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 20 | TD-028 | defender:test-bloat-20 #1 | R12-nh-fixture-destination | Owner must pick ONE destination for this crate's duplicated entity presets: crates/cobre-stochastic/src/test_support.rs (crate-local, reachable from inline unit tests with no feature change) or cobre_core::test_support b… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 21 | TD-026 | defender:test-bloat-22 #1 | R12-nh-fixture-destination | Which home the folded generator takes: crates/cobre-stochastic/src/test_support.rs, already gated at src/lib.rs:32 on cfg(any(test, feature = test-support)) and therefore reachable by downstream crates, or a private cfg(… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 22 | TD-030 | defender:test-bloat-24 #1 | R12-nh-fixture-destination | Home for the two new statistical assertion helpers: cobre-stochastic's tests/common/mod.rs (crate-local, neutral, three consumers) or cobre_core::test_support beside the generic norm_cdf and approx_erf it already owns (c… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 23 | TD-043 | defender:test-bloat-33 #1 | R12-nh-fixture-destination | Destination of the folded constructors: this verdict names the currently wired tests/common/ home, since src/test_support.rs holds no neutral resolved-layer constructor and its existing ones are private. Whether the fold… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 24 | TD-057 | defender:test-bloat-47 #1 | R12-nh-fixture-destination | Whether cobre-cli may gain a crate-local tests/common helper module now, or whether its shared harness home must wait for the section 5.2 collapse of tests/common into the test-support surface so the crate does not build… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 25 | TD-065 | defender:test-bloat-51 #1 | R12-nh-fixture-destination | Feature-unification caveat on the recommended shape: cobre-cli takes cobre-io as a normal dependency at crates/cobre-cli/Cargo.toml:22 (schema feature) and as a dev-dependency at :63, so one cobre-io instance is shared d… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 26 | TD-065 | defender:test-bloat-51 #2 | R12-nh-fixture-destination | Sharpens the attacker's third item: whether cobre-cli's shared fixture home is the existing cobre-io test-support surface now or must wait for the section 5.2 collapse. My read shows the surface already carries seven of … | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 27 | TD-068 | defender:test-bloat-54 #3 | R12-nh-fixture-destination | Whether cobre-cli may gain a tests/common helper module now, or whether its shared harness home must wait for the section 5.2 collapse of tests/common into the test-support surface. That decision gates the count-neutral … | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 28 | TD-058 | defender:test-bloat-58 #2 | R12-nh-fixture-destination | Carried from the attacker: whether the shared fixture belongs in test_study.py as a module fixture (matching test_outputs.py:24-31, and the only trained-policy consumers sit in that one module) or in conftest.py as TD-05… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 29 | TD-036 | defender:test-bloat-28 #3 | R12-nh-fixture-destination | Sequencing with the cobre-comm test-support surface: crates/cobre-comm/Cargo.toml declares only mpi, numa and shared-memory features and no [dev-dependencies] at the pin, so this fold's shared body would land on the same… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 30 | TD-051 | defender:test-bloat-41 #2 | R12-nh-fixture-destination | Where the shared 47-line two-rank echo stub is homed: crates/cobre-sddp/tests/common/mod.rs (this verdict's shape, neutral under Part IV/V) versus cobre-comm's test-support surface named by the section 5.8 target at docs… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 31 | TD-053 | defender:test-bloat-43 #1 | R12-nh-fixture-destination | Destination for a single shared rank-shape Communicator double remains an owner call: testing-architecture.md:415-416 and :524-526 assign StubComm/Rank0Of2 to cobre-comm's test-support surface, but cobre-comm declares no… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 32 | TD-056 | defender:test-bloat-46 #2 | R12-nh-fixture-destination | Where a shared parameterized stub communicator would live (the sddp test harness at tests/common/mod.rs versus the communication crate's exported test-support surface) remains the station's adjudicate row ta-5.8-stubcomm… | **Crate-local home now** | Each crate homes its own fixtures now: the crate's gated test_support for helpers its inline unit tests also reach, tests/common (or a module-scoped pytest fixture) for integration-only helpers; engine-specific doubles never enter cobre-cor |
| 33 | TD-077 | defender:test-bloat-57 #1 | R12-nh-cli-activation | Owner picks between the two coverage-neutral resolutions of crates/cobre-cli/Cargo.toml:62 - drop the unconsumed cobre-core/test-support activation, or keep it with an explicit reservation note beside the crate's slow-te… | **Drop the activation** | Remove the unconsumed cobre-core/test-support activation from cobre-cli's dev-dependency; no test, fixture or CI trigger changes and the nextest count is unchanged. |
| 34 | TD-057 | defender:test-bloat-47 #2 | R12-nh-cli-activation | Whether the cobre-core test-support activation at crates/cobre-cli/Cargo.toml:62 is a reserved seam or a stale dev-dependency feature activation: git grep for test_support over crates/cobre-cli returns nothing at the pin… | **Drop the activation** | Remove the unconsumed cobre-core/test-support activation from cobre-cli's dev-dependency; no test, fixture or CI trigger changes and the nextest count is unchanged. |
| 35 | TD-065 | defender:test-bloat-51 #4 | R12-nh-cli-activation | Whether the cobre-core test-support activation at crates/cobre-cli/Cargo.toml:62 is a reserved seam for planned CLI fixture use or a stale activation: no file under crates/cobre-cli/tests/ references cobre_core at the pi… | **Drop the activation** | Remove the unconsumed cobre-core/test-support activation from cobre-cli's dev-dependency; no test, fixture or CI trigger changes and the nextest count is unchanged. |
| 36 | CD-118 | defender:architecture-00 #1 | R12-nh-sddp-self-dev-dep | Whether cobre-sddp gains the self dev-dependency now or command-line activation is accepted until the 5.2 collapse lands (attacker item 1, still live): the self dev-dependency makes the five feature-gated items in crates… | **Add the self dev-dependency now** | Mirror the three sibling declarers so cobre-sddp's forty integration binaries see the gated surface in every dev build; align the two crate-local promises (Cargo.toml:51-53, test_support.rs:9-10); the five newly unconditional gated items ar |
| 37 | CD-118 | defender:architecture-00 #2 | R12-nh-s52-doc-side | Settling claim-class row ta-5.2-adoption-uniform on the doc side: whether the Adoption paragraph's uncarried-bullet list at docs/design/testing-architecture.md:427-430 is corrected to include bullet 1's two placements (g… | **Correct, record the exception, name the two harnesses as consumers** | E11 lists bullet 1's placements as uncarried in the §5.2 Adoption paragraph, records cobre-solver's narrow gate as the sanctioned raw-FFI exception, and states that cobre-io/tests/helpers and cobre-stochastic/tests/common are consumers of t |
| 38 | CD-118 | defender:architecture-00 #3 | R12-nh-s52-doc-side | Whether cobre-solver's narrow gate (crates/cobre-solver/src/lib.rs:163, crates/cobre-solver/src/backends/highs/solver.rs:6 and :580) is recorded in 5.2 as a sanctioned exception to the uniform gate (attacker item 3, stil… | **Correct, record the exception, name the two harnesses as consumers** | E11 lists bullet 1's placements as uncarried in the §5.2 Adoption paragraph, records cobre-solver's narrow gate as the sanctioned raw-FFI exception, and states that cobre-io/tests/helpers and cobre-stochastic/tests/common are consumers of t |
| 39 | CD-116 | defender:architecture-01 #1 | R12-nh-s52-doc-side | Carried from the attacker and narrowed by this read: whether the §5.2 plan text should still name crates/cobre-io/tests/helpers/ and crates/cobre-stochastic/tests/common/ as collapse sources even though both already re-e… | **Correct, record the exception, name the two harnesses as consumers** | E11 lists bullet 1's placements as uncarried in the §5.2 Adoption paragraph, records cobre-solver's narrow gate as the sanctioned raw-FFI exception, and states that cobre-io/tests/helpers and cobre-stochastic/tests/common are consumers of t |
| 40 | CD-117 | defender:architecture-02 #1 | R12-nh-s52-doc-side | Whether the §5.2 collapse source list (docs/design/testing-architecture.md:417-421, adjudicate row ta-5.2-adoption-uniform at :425-431) extends to crates/cobre-io/tests/helpers/ and crates/cobre-stochastic/tests/common/,… | **Correct, record the exception, name the two harnesses as consumers** | E11 lists bullet 1's placements as uncarried in the §5.2 Adoption paragraph, records cobre-solver's narrow gate as the sanctioned raw-FFI exception, and states that cobre-io/tests/helpers and cobre-stochastic/tests/common are consumers of t |
| 41 | defended | defender:test-bloat-30 #1 | R12-nh-s52-doc-side | The section 5.2 Adoption paragraph at docs/design/testing-architecture.md:425-431 lists only two bullets as not yet carried out and omits the bullet-1 placements (StubComm/Rank0Of2 to cobre-comm, generic scaffolding to c… | **Correct, record the exception, name the two harnesses as consumers** | E11 lists bullet 1's placements as uncarried in the §5.2 Adoption paragraph, records cobre-solver's narrow gate as the sanctioned raw-FFI exception, and states that cobre-io/tests/helpers and cobre-stochastic/tests/common are consumers of t |
| 42 | TD-074 | defender:test-bloat-71 #1 | R12-nh-slurm-trigger | Trigger pick for full mode is an owner call among push to the release branch, tags only, and a nightly schedule; it spends the same cluster-minutes budget as CD-101's shuffle-cadence needs-human (BACKLOG.md:6500), so the… | **Nightly runs full; PR/push keep reduced + boot smoke check** | A scheduled run selects full mode over the provisioned cluster; pull-request and push runs keep reduced mode and their cluster-boot health-wait as the smoke check; decided together with CD-101's cadence budget. |
| 43 | TD-074 | defender:test-bloat-71 #2 | R12-nh-slurm-trigger | Whether the reduced path's cluster build and health-wait at mpi-slurm.yml:107-108 is intended as a cluster-boot smoke check on every automatic run; the answer decides whether trimming the provisioning on reduced triggers… | **Nightly runs full; PR/push keep reduced + boot smoke check** | A scheduled run selects full mode over the provisioned cluster; pull-request and push runs keep reduced mode and their cluster-boot health-wait as the smoke check; decided together with CD-101's cadence budget. |
| 44 | TD-075 | defender:test-bloat-72 #1 | R12-nh-slurm-path-filter | Trigger shape: enumerate the omitted module or widen both lists to the whole solver crate source tree. Enumerating keeps the cluster job rare but rots the next time a module moves, which is the failure mode that produced… | **Widen both lists to crates/cobre-sddp/src/**** | Durable: the enumeration cannot rot as modules move; the pull_request and push lists stay byte-identical; the reduced/full switch is untouched. |
| 45 | TD-075 | defender:test-bloat-72 #2 | R12-nh-slurm-path-filter | Ordering against the 5.6 / 5.8 cadence targets: if the owner ratifies moving this deck off the pull-request tier, only the push list needs the added pattern and the pull_request block changes shape anyway, so the owner d… | **Widen both lists to crates/cobre-sddp/src/**** | Durable: the enumeration cannot rot as modules move; the pull_request and push lists stay byte-identical; the reduced/full switch is untouched. |
| 46 | defended | defender:test-bloat-67 #1 | R12-nh-runner-cadence-targets | Cadence policy, already owner-gated by the yardstick (§5.6 should be ratified explicitly; §6 phase 4 Ratify the tradeoff first): whether to split NON_SOLVER_FEATURES into a per-PR set without slow-tests plus a full set o… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 47 | defended | defender:test-bloat-68 #1 | R12-nh-runner-cadence-targets | Owner call at the 5.5 adoption rather than at this station: the paired cargo test --doc step must land in the same change as the nextest switch because .github/workflows/ci.yml:114 is the only doctest execution at the pi… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 48 | defended | defender:test-bloat-69 #1 | R12-nh-runner-cadence-targets | Sequencing only, no defect: if the owner ratifies section 6 phase 2 (commit a .config/nextest.toml and move the fail-fast, slow-timeout and retry knobs off the three inline run lines), decide whether that profile lands b… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 49 | defended | defender:test-bloat-70 #1 | R12-nh-runner-cadence-targets | If the owner ratifies the §5.5 runner adoption, the paired cargo test --doc step must land in the same change: nextest runs no doctests, so unifying the runner without that step removes the 329-passing doctest tier from … | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 50 | defended | defender:test-bloat-70 #2 | R12-nh-runner-cadence-targets | Whether the two CLP-gated doctest bodies (crates/cobre-solver/src/backends/clp/solver.rs:37 and :520) should ever be compiled under the CLP feature combination is an owner call on doctest-tier scope: their assertions are… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 51 | defended | defender:performance-00 #1 | R12-nh-runner-cadence-targets | Cadence boundary: does a pull request keep all three link-bearing jobs (Test at .github/workflows/ci.yml:114, CLP at :223 and :233, Coverage at :497), or does one backend gate pull requests while the second backend's tes… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 52 | defended | defender:performance-00 #2 | R12-nh-runner-cadence-targets | Target selection inside the CLP job: the build at .github/workflows/ci.yml:223 passes --all-targets, which also builds the criterion bench targets declared at crates/cobre-sddp/Cargo.toml:92-110; whether the CLP job narr… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 53 | defended | defender:performance-02 #1 | R12-nh-runner-cadence-targets | Cadence only, not shape: does the sole codegen-and-link gate over the bench and example targets (the build step at .github/workflows/ci.yml:223) stay on the pull-request tier, or move to merge-to-develop or nightly with … | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 54 | defended | defender:performance-02 #2 | R12-nh-runner-cadence-targets | Owner note, not a finding: if the linked-executable count of the criterion targets is ever revisited, it needs its own entry, because this candidate records the bench declarations as sanctioned (.claude/rules/testing.md:… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 55 | defended | defender:performance-01 #1 | R12-nh-runner-cadence-targets | Owner call: is the #[path] submodule shape already working at crates/cobre-sddp/tests/template_integration.rs:4400-4420 the ratified Layer 1 shape, and what sets the grouping boundary - docs/design/testing-architecture.m… | **Record as roadmap sequencing notes, no decision now** | The §5.5 / §5.6 targets travel to E11 with the §5 ratification; the only binding note is that a nextest switch must land in the same change as the paired cargo test --doc step. |
| 56 | TD-002 | defender:test-bloat-00 #1 | R12-nh-scope-extensions | Census scope: a second identical out-of-owning-module pair exists at the pin that this candidate does not name, entities/hydro.rs:612 and model/penalty.rs:353 (its cfg(test) module opens at model/penalty.rs:335), sharing… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 57 | TD-016 | defender:test-bloat-11 #2 | R12-nh-scope-extensions | Whether the two further identical zero-penalty bodies I read at crates/cobre-io/src/broadcast.rs:199 and crates/cobre-io/src/output/simulation_writer.rs:2252 are folded under this entry at calibration or minted separatel… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 58 | TD-026 | defender:test-bloat-22 #2 | R12-nh-scope-extensions | A third copy of the same generator exists outside this candidate's declared crate scope at crates/cobre-io/src/scenarios/estimation/tests.rs:1172, identical to the cobre-stochastic estimation copy except its doc-comment … | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 59 | TD-032 | defender:test-bloat-19 #2 | R12-nh-scope-extensions | Scope call TD-032 does not cover: whether the four-site dated make_hydro variant (seeds.rs:180, sampling/external.rs:2403, sampling/historical.rs:1665, par/lag_transition.rs:1141) folds into the same shared preset behind… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 60 | TD-037 | defender:test-bloat-13 #1 | R12-nh-scope-extensions | Scope call on the third fixture family: TD-037's fix-shape names the empty batch as one of the three fixtures to give a single declaration, but the three zero-row RowBatch bodies at src/freeze.rs:286, tests/sentinel_inf_… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 61 | TD-044 | defender:test-bloat-34 #1 | R12-nh-scope-extensions | Scope of the shared home beyond the anticipated pair: crates/cobre-sddp/tests/filling_commissioning.rs carries one further copy in each of the same three classes (:1532 in the minority default_hydro_bounds digest, :1541 … | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 62 | TD-054 | defender:test-bloat-44 #1 | R12-nh-scope-extensions | Whether the ninth home (crates/cobre-sddp/src/simulation/pipeline/tests.rs:160) is inside the ratified TD-054 scope at all: it is a counting/recording double like the four recording doubles the entry excludes by name, so… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 63 | TD-041 | defender:test-bloat-31 #1 | R12-nh-scope-extensions | How far the penalties fold reaches: the widened constructor could be pub(crate) for just these src homes, or pub and adopted by the roughly twenty further byte-identical all-zero penalty literals elsewhere in the crate (… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 64 | TD-042 | defender:test-bloat-32 #2 | R12-nh-scope-extensions | Whether the 7 carriers equal to the HydroSpec::default() penalties at crates/cobre-core/src/test_support.rs:219-221 should adopt cobre_core::test_support::make_hydro itself, which cobre-sddp imports in 0 files at the pin… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 65 | TD-049 | defender:test-bloat-39 #1 | R12-nh-scope-extensions | Whether the anticipated-hydro pair, named as a collapsible family in TD-049's ratified direction but shown here to differ on inflow_lags and block layout, is dropped from that direction or re-filed as its own item: remov… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 66 | TD-050 | defender:test-bloat-40 #1 | R12-nh-scope-extensions | TD-050's upstream fix shape names crates/cobre-sddp/tests/common/parity_hash.rs:230 as a caller of the new helper while the same entry's claim concedes that site as out of scope; whether the golden label mapper rebases o… | **Fold the newly found copies into the same entry's scope** | Each entry's fix-shape covers every copy the defender verified at the pin; the anticipated-hydro pair and the parity_hash caller are dropped from their families as the defender recorded. |
| 67 | TD-022 | defender:test-bloat-10 #1 | R12-nh-direction-reversals | Direction call on TD-022: the register frames the count-only bodies as a redundant surface to remove, this verdict confirms the opposite direction (strengthen them to the name vector); only the removal direction changes … | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 68 | TD-059 | defender:test-bloat-59 #1 | R12-nh-direction-reversals | This verdict replaces a ratified seed's direction rather than forwarding a conflict: TD-059's fix-shape drops the hand-copied CLI golden literals, which weakens a retained test's only assertions, so the confirmation keep… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 69 | TD-060 | defender:test-bloat-60 #1 | R12-nh-direction-reversals | TD-060's ratified fix-shape directs one Hive-partitioned parquet collector parameterised by entity directory; this verdict withdraws that clause because the empty-tolerant collector's return is the asserted value at test… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 70 | TD-061 | defender:test-bloat-61 #1 | R12-nh-direction-reversals | Carried from the attacker: TD-061 was ratified with a direction that deletes test_run_via_study_emits_contract_output. This verdict refuses that half as inadmissible under cost discipline and confirms only the fixture fo… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 71 | TD-066 | defender:test-bloat-52 #3 | R12-nh-direction-reversals | Whether TD-066's ratified removal half (dropping the two remaining run-based colour cases because banner.rs:70-73 and main.rs:127-132 pin both claims at the unit tier) is admitted under the lens-rules Rule 2 Boundary car… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 72 | TD-069 | defender:test-bloat-55 #1 | R12-nh-direction-reversals | TD-069's upstream fix-shape also deletes test_1dtoy_files_have_descriptions (templates.rs:183-188) and test_1dtoy_files_have_relative_paths (:191-197); lens-rules.md Rule 2 forbids deletion as a fix-shape at this station… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 73 | TD-055 | defender:test-bloat-45 #2 | R12-nh-direction-reversals | Whether a zero-assertion test fn may be removed outright rather than strengthened: the upstream direction deletes k1_byte_stability_verdict, which carries no coverage, while Rule 2 forbids deletion as a fix-shape and its… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 74 | TD-064 | defender:test-bloat-50 #1 | R12-nh-direction-reversals | Whether the eight panic-only smoke tests over the four printers may be retired once the content assertions land on the shipped path: the upstream fix shape collapses the eleven does_not_panic tests, which lowers the carg… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 75 | TD-042 | defender:test-bloat-32 #1 | R12-nh-direction-reversals | The TD-042 handover carries two incompatible fix shapes for the same entry: its registered claim states the residue adds no constructor to cobre-sddp's test_support and spreads the base at the call sites, while its upstr… | **The defender's coverage-neutral direction supersedes** | Each entry's fix-shape is re-stated as the confirmed direction; the registered deletion or weakening wording is superseded and never re-litigated; TD-042 keeps the constructor-free residue. |
| 76 | TD-072 | defender:test-bloat-63 #1 | R12-nh-python-parity-scope | Owner call: closing the second borrow site needs an import-line edit inside test_parity_filling_sigma.py, but docs/design/testing-architecture.md:563 (section 5.11) directs that the pytest output-parity suite be kept unc… | **§5.11 freezes assertions, not fixtures** | Fixture hygiene (shared module-scoped fixtures, import-line edits, a counted guard around a shared directory) is admissible when every parity assertion is kept; an assertion about an absent artifact keeps its own isolated tree. |
| 77 | TD-061 | defender:test-bloat-61 #2 | R12-nh-python-parity-scope | Not adjudicated here: the seed also observed that cobre.run.run solves the same deck twice (:224 and :264). Sharing one module-scoped python-output fixture (the d02_python_output shape) would remove one full train-and-si… | **§5.11 freezes assertions, not fixtures** | Fixture hygiene (shared module-scoped fixtures, import-line edits, a counted guard around a shared directory) is admissible when every parity assertion is kept; an assertion about an absent artifact keeps its own isolated tree. |
| 78 | TD-073 | defender:test-bloat-64 #1 | R12-nh-python-parity-scope | Owner call on fixture isolation: routing test_load_stochastic_missing_artifacts_raises onto the shared module-scoped run_output tree makes an assertion about an ABSENT artifact depend on no sibling test ever enabling exp… | **§5.11 freezes assertions, not fixtures** | Fixture hygiene (shared module-scoped fixtures, import-line edits, a counted guard around a shared directory) is admissible when every parity assertion is kept; an assertion about an absent artifact keeps its own isolated tree. |
| 79 | TD-059 | defender:test-bloat-59 #2 | R12-nh-python-parity-scope | A once-initialised shared fixture in a LazyLock static never drops, so the owner must decide whether the fold may hold the shared directory in a counted guard, or whether the Contracts bullet that test output goes to Tem… | **§5.11 freezes assertions, not fixtures** | Fixture hygiene (shared module-scoped fixtures, import-line edits, a counted guard around a shared directory) is admissible when every parity assertion is kept; an assertion about an absent artifact keeps its own isolated tree. |
| 80 | TD-001 | defender:test-bloat-01 #3 | R12-nh-recording-notes | The candidate files yardstickRef ta-3.2, but none of the eight claim-class rows supplied for section 3.2 covers tautological assertions; the verdict therefore rests on the tree fact and on the testing.md Contracts tautol… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 81 | TD-013 | defender:test-bloat-09 #2 | R12-nh-recording-notes | The attacker's question of whether the CI schemas diff is authoritative for the export set is ANSWERED at the pin, not open: scripts/ci/check_schemas.sh runs a recursive diff of schemas/ against a fresh export (absent-fi… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 82 | TD-028 | defender:test-bloat-20 #2 | R12-nh-recording-notes | The registered TD-028 claim describes the external/historical make_hydro pair as 'one-cosmetic-line' and calls mod.rs's copy a '2024 plus mirror-unit-group' divergent variant; neither holds at this pin (the pair is byte-… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 83 | TD-071 | defender:test-bloat-62 #2 | R12-nh-recording-notes | Calibration must choose which figure it records for perCrate.cobre-python.inlineCfgTestModules: 5 under the inventory's file census, or 4 files that actually declare a module, since convert.rs:104 and errors.rs:209 are p… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 84 | TD-073 | defender:test-bloat-64 #2 | R12-nh-recording-notes | Wording call for calibration: this station's count-neutrality bar is stated as cargo nextest list parity, which never lists the cobre-python pytest suite, so neutrality for this candidate can only be checked with a pytes… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 85 | CD-116 | defender:architecture-01 #2 | R12-nh-recording-notes | Recording detail for the ingest owner: the §2.2 sentence spans :76 and :78 because a line-final hyphen renders 'behavioral only, constructing fixtures per-file' as a stray list item. Whether that render break is a second… | **Record as notes, no decision** | Each item is written on its entry or seed row as a note; cobre-python keeps the inventory's file census (5) with the 4-module reading noted; the neutrality bar is stated as nextest parity for Rust and pytest collection parity for the bindin |
| 86 | TD-038 | defender:test-bloat-14 #2 | R12-nh-severity-rerating | Whether TD-038's ratified severity C moves now that the surviving widening is one carrier rather than the two the attacker filed; rule 12 leaves registered severity to the owner gate.… | **Keep every registered severity** | TD-038 stays C, TD-055 stays C (the escalation rests on one stale roster entry), TD-033 keeps its registered basis; the deltas are recorded as sharpenings only. |
| 87 | TD-055 | defender:test-bloat-45 #1 | R12-nh-severity-rerating | Severity: TD-055 is ratified at the lowest rank and the attacker escalates to the top rank. After this read the escalation rests on one stale roster entry out of 46 plus a guard whose substantive content is asserted else… | **Keep every registered severity** | TD-038 stays C, TD-055 stays C (the escalation rests on one stale roster entry), TD-033 keeps its registered basis; the deltas are recorded as sharpenings only. |
| 88 | TD-033 | defender:test-bloat-26 #1 | R12-nh-severity-rerating | TD-033 stays registered upstream and is not re-litigated here: the owner decides whether its recorded zero-behavioral-coverage basis survives the caller-tier literals at crates/cobre-stochastic/src/seeds.rs:195 and crate… | **Keep every registered severity** | TD-038 stays C, TD-055 stays C (the escalation rests on one stale roster entry), TD-033 keeps its registered basis; the deltas are recorded as sharpenings only. |
| 89 | TD-002 | defender:test-bloat-00 #2 | R12-nh-test-bloat-00#2 | Fixture-value locality: whether both sites consume the new named constructor, or only the system-level postcard copy delegates while the value-asserting resolved-penalties module keeps its local fixture, is an owner call… | **Both sites take the named constructor** | Both duplicated sites consume HydroPenalties::uniform; asserted values stay local literals only where they are the subject of the assertion. |
| 90 | TD-001 | defender:test-bloat-01 #2 | R12-nh-test-bloat-01#2 | Carried from the attacker, still live after this read: whether strengthening a retained assertion counts as coverage-neutral under Rule 2's second question, given that the retained test then asserts a strict superset of … | **Strengthening a retained assertion is coverage-neutral** | Asserting more is admissible under Rule 2's second question; only weakening or dropping an assertion is a coverage reduction. |
| 91 | TD-003 | defender:test-bloat-02 #2 | R12-nh-test-bloat-02#2 | Narrowed from the attacker's item: strengthening in place is moot for the five members that already carry a complete struct literal, but test_contract_type_equality at crates/cobre-core/src/entities/energy_contract.rs:87… | **Keep the one structural pin; fold the derive-discharged lines** | test_contract_type_equality stays as the single structural pin; only the derive-discharged comparison lines fold; the five complete struct literals stay. |
| 92 | TD-021 | defender:test-bloat-05 #2 | R12-nh-test-bloat-05#2 | Where the two facade-crossing version-marker tests at :1457 and :1487 belong: checkpoint.rs can import super::codec::serialize_checkpoint_manifest exactly as it already does at :13-17, or the pair can stay at the facade … | **Keep them in checkpoint.rs importing super::codec** | The facade-crossing version-marker tests stay where the checkpoint contract is asserted; the import already exists. |
| 93 | TD-010 | defender:test-bloat-07 #1 | R12-nh-test-bloat-07#1 | Direction for the single-consumer builder: inline make_referential_violation_case into its only caller (integration.rs:159) per the documented blast-radius principle at crates/cobre-io/src/test_support.rs:8-9, or promote… | **Inline into its only caller** | make_referential_violation_case is inlined into integration.rs:159 per the documented blast-radius principle; no test changes. |
| 94 | TD-020 | defender:test-bloat-08 #1 | R12-nh-test-bloat-08#1 | Residue for the owner to fold or drop, not a new entry: four byte-identical unwrap schema-only preludes remain at hydro_models.rs:515-517, :705-707, :816-818 and training_writer.rs:987-989; TD-020 already dispositions th… | **Fold the four preludes as a residue of TD-020** | No new entry; the four byte-identical schema-only preludes fold into the existing helper as a residue recorded on TD-020. |
| 95 | TD-013 | defender:test-bloat-09 #1 | R12-nh-test-bloat-09#1 | Owner call on the expected-set source: tightening :258 to set equality needs an authoritative source for the 18 names, and the project's documented choice at crates/cobre-cli/tests/cli_schema.rs:43-44 is to derive both s… | **check_schemas.sh's export set is authoritative** | Tighten the schema-export assertion at :258 to set equality against the exported set scripts/ci/check_schemas.sh already checks. |
| 96 | TD-006 | defender:test-bloat-12 #1 | R12-nh-test-bloat-12#1 | Does the MPI broadcast payload have a ratified compactness budget? No budget, const or doc exists anywhere in cobre-io at the pin (the ceiling literal at broadcast.rs:399 is its only occurrence), so whether the tightened… | **No budget exists; strengthen the shape assertion, not a byte contract** | The ceiling literal at broadcast.rs:399 is recorded as a tautology to strengthen into a payload-shape assertion; no byte budget is ratified. |
| 97 | TD-038 | defender:test-bloat-14 #1 | R12-nh-test-bloat-14#1 | Whether the name-versus-doc mismatch on test_highs_solve_iterations_positive (crates/cobre-solver/src/backends/highs/tests.rs:342 claims positive while its own doc at :339-340 states any u64 is valid) earns a separate re… | **Fix the mismatch as part of TD-038's fold** | test_highs_solve_iterations_positive's name and doc agree after the fold; no assertion changes. |
| 98 | TD-039 | defender:test-bloat-15 #1 | R12-nh-test-bloat-15#1 | After the fold the conformance binary holds two clp tests asserting the same objective and primals at two tolerances, 1e-9 from the moved test and 1e-8 at conformance.rs:1450. Whether both are retained (count-neutral and… | **Keep both tolerances, document why** | The 1e-9 and 1e-8 assertions pin different contracts; a one-line doc states it; nothing is weakened. |
| 99 | defended | defender:test-bloat-27 #1 | R12-nh-test-bloat-27#1 | The INVERSE of this finding is real and is an owner call: crates/cobre-stochastic/src/test_support.rs is a feature-gated public module that the canonical gate excludes on the basename test heuristic (scripts/ci/check-inf… | **E11 follow-up, no entry; keep the private gates** | The canonical genericity gate's exclusion of cobre-stochastic's feature-gated test_support module is handed to E11 for the gate's owner; no entry is minted here. |
| 100 | defended | defender:test-bloat-27 #2 | R12-nh-test-bloat-27#2 | Carried from the attacker: whether the two private one-token gates (crates/cobre-stochastic/tests/reproducibility.rs:195 and crates/cobre-io/tests/genericity_gate.rs:6) keep their unexcluded whole-src scope or fold onto … | **Keep the two private one-token gates** | reproducibility.rs:195 and genericity_gate.rs:6 stay as crate-local guards, not duplicates of the canonical gate. |
| 101 | TD-036 | defender:test-bloat-28 #2 | R12-nh-test-bloat-28#2 | Reconciling the one same-name divergent pair, test_local_broadcast_root0_noop, by widening the inline fixture (src/local.rs:444, two elements against a literal) to the integration one (tests/local_conformance.rs:94, thre… | **Widen the inline fixture and keep one body** | test_local_broadcast_root0_noop converges on the wider integration fixture; nothing is weakened. |
| 102 | TD-035 | defender:test-bloat-29 #2 | R12-nh-test-bloat-29#2 | Because pub trait Communicator: Send + Sync at crates/cobre-comm/src/traits.rs:90 makes assert_communicator::<FerrompiBackend>() at tests/factory_tests.rs:97 already entail the bound its same-file sibling at :90 asserts,… | **Drop it as a strict duplicate under the carve-out** | assert_communicator::<FerrompiBackend>() is entailed by the Send + Sync trait bound; the ratified carve-out governs the count change. |
| 103 | TD-042 | defender:test-bloat-32 #3 | R12-nh-test-bloat-32#3 | The third bullet of the cost-discipline rule states that the one-place property is scoped to the integration-test layer and does not hold workspace-wide for inline cfg(test) literals. Whether the 30 src-side carriers are… | **One-place stays scoped to the integration layer** | The cost-discipline rule's third bullet is read as written; inline cfg(test) copies are consolidated per crate, not workspace-wide. |
| 104 | TD-045 | defender:test-bloat-35 #1 | R12-nh-test-bloat-35#1 | TD-045's fix-shape wording says the local doc comment's two stated reasons are both false at the pin; at the baseline both check out (the named symbol exists at crates/cobre-sddp/src/lp/builder/test_support.rs:65 and an … | **Correct the wording; the claim stands** | TD-045's sentence calling both local doc reasons false is superseded (both hold at the pin); the entry keeps its registered claim and severity. |
| 105 | TD-046 | defender:test-bloat-36 #1 | R12-nh-test-bloat-36#1 | Fix-shape fork this defense surfaced and did not settle: the production rustdoc at noise.rs:211 links the oracle by bracketed intra-doc link, so the owner picks between moving the oracle into mod tests and demoting that … | **Keep the intra-doc link; move the oracle body** | noise.rs:211 keeps its bracketed intra-doc link to the relocated oracle; rustdoc resolves it. |
| 106 | TD-056 | defender:test-bloat-46 #1 | R12-nh-test-bloat-46#1 | TD-056 registers the rank field of the local stub as a dead-field defect; at the pin crates/cobre-sddp/src/simulation/state.rs:409 reads comm.rank() and passes it to assign_scenarios at :412, so rank is a consumed input … | **Correct TD-056: the rank field is read at state.rs:409** | The dead-field premise is false at the pin; TD-056 keeps only its dismissed-delta note. |
| 107 | TD-063 | defender:test-bloat-49 #2 | R12-nh-test-bloat-49#2 | After the repoint, format_report_summary_header_present cannot keep asserting both counts nonzero (no shipped path emits it); whether it is split into the two reachable header cases, which raises the listed count by one,… | **Split into the two reachable halves** | format_report_summary_header_present becomes two tests, each asserting its count on the path that emits it; the assertion set is kept. |
| 108 | TD-064 | defender:test-bloat-50 #2 | R12-nh-test-bloat-50#2 | Whether the re-homed assertions may be strengthened from contains() to a whole-render equality over the returned lines, which is the only shape that would pin the set and order of lines, since that is additive coverage b… | **Strengthen to whole-render equality** | The re-homed render assertions move from contains() to whole-render equality over the returned lines; strictly more assertion, pins the set. |
| 109 | TD-066 | defender:test-bloat-52 #1 | R12-nh-test-bloat-52#1 | Whether an escape-absence assertion may stand as the --color never integration probe at all: cli_color.rs:130-131 records that auto-detection disables colour on a piped stderr, so the absence proves nothing about the fla… | **Keep the escape-absence probe; re-home the wiring assertion onto init** | The --color never probe keeps asserting the absence of escapes with its doc stating the pipe limitation; the flag-to-global wiring assertion moves onto the solve-free init path. |
| 110 | TD-066 | defender:test-bloat-52 #2 | R12-nh-test-bloat-52#2 | Whether the colour suite's flag-to-global wiring assertion may be re-homed onto the solve-free init subcommand path, which changes which subcommand owns the assertion while keeping the predicate identical (carried from t… | **Keep the escape-absence probe; re-home the wiring assertion onto init** | Same assertions, cheaper subcommand for the wiring check; the owning subcommand changes. |
| 111 | TD-067 | defender:test-bloat-53 #2 | R12-nh-test-bloat-53#2 | Whether the accumulator mean and sample-standard-deviation assertions at crates/cobre-cli/src/progress.rs:987-1007 may be re-homed to the crate that declares WelfordAccumulator, since moving a test across a crate boundar… | **Re-home to the declaring crate** | The Welford mean / standard-deviation assertions follow WelfordAccumulator's crate; the test count moves between crates, none is lost. |
| 112 | TD-068 | defender:test-bloat-54 #2 | R12-nh-test-bloat-54#2 | Whether the exit-code-only layer is a wanted coarse-to-fine diagnostic split: an assert_cmd Assert chain stops at the first failing predicate, so a merged test reports the exit-code failure and the message failure one at… | **Keep the exit-code-only layer as the coarse diagnostic** | assert_cmd stops at the first failing predicate, so the coarse-to-fine split is wanted; only exact-duplicate assertions fold under the carve-out. |
| 113 | TD-069 | defender:test-bloat-55 #2 | R12-nh-test-bloat-55#2 | Whether the duplicate count pin at scripts/ci/check-docs-examples.sh:35 (EXPECTED_INPUT_FILES=11) is retired once the unit test derives the set: that script's header at :5-7 documents its constants as deliberate bump-on-… | **Retire the duplicate count pin once the unit test derives the set** | EXPECTED_INPUT_FILES=11 in check-docs-examples.sh:35 goes once the unit test derives the 1dtoy set; the script's header already defers to the unit test. |
| 114 | TD-070 | defender:test-bloat-56 #1 | R12-nh-test-bloat-56#1 | Whether the surviving definition stays a named private helper or the call sites adopt the inline standard-formatter form already used at summary.rs:858: the body is a no-op re-derivation of that formatter, so the choice … | **Adopt the inline standard-formatter form** | The no-op private wrapper goes; every call site uses the form summary.rs:858 already uses. |
| 115 | TD-058 | defender:test-bloat-58 #1 | R12-nh-test-bloat-58#1 | TD-058's other half (test_run.py:38 asserting a subset of test_outputs.py:33-50 on the shared 1dtoy deck) falls outside this candidate's scope; the owner decides whether a sibling candidate carries it or calibration fold… | **Re-state under the ratified carve-out** | TD-058's other half (test_run.py:38 a strict subset of test_outputs.py:33-50) folds into the retained sibling under the ratified carve-out; every distinct assertion kept. |
| 116 | TD-060 | defender:test-bloat-60 #2 | R12-nh-test-bloat-60#2 | The fold replaces four divergent builder docstrings with one contract statement plus per-call-site notes. Each current docstring names a different case-specific reason (D41 is training-only; the anticipated cases ship si… | **One contract statement plus per-call-site notes** | The four divergent builder docstrings collapse to one contract statement; each call site keeps its case-specific note. |
| 117 | TD-065 | defender:test-bloat-51 #3 | R12-nh-test-bloat-51#3 | Whether the deliberate-duplication comment at crates/cobre-cli/tests/cli_color.rs:20-21 is overruled. It states the run-versus-colour fixture duplication is an intentional self-containment choice, so folding that pair re… | **Overrule the comment** | cli_color.rs:20-21's intentional run-versus-colour duplication yields to the shared fixture; the comment is removed with the duplication; no assertion changes. |
| 118 | TD-076 | defender:test-bloat-66 #1 | R12-nh-test-bloat-66#1 | Whether the doc-integrity default of no frozen count without a guard extends to Python docstrings and Rust doc comments is an owner call; it decides whether this fix-shape generalizes to sibling prose or stays a single-c… | **Extend the default to docstrings and doc comments** | A count in any prose needs a guard or becomes an invariant statement; TD-076 generalises to sibling Python docstrings and Rust doc comments. |
| 119 | defended | defender:over-engineering-01 #1 | R12-nh-over-engineering-01#1 | The module doc at crates/cobre-sddp/tests/common/anticipated_structural_assertions.rs:1 calls the helpers shared by the anticipated-thermals integration tests, plural, while at the pin each of its three public helpers ha… | **Keep the aggregator; correct the doc's plural** | The deliberate-and-documented dismissal stands; anticipated_structural_assertions.rs:1's plural is a doc fix with no structural change. |
| 120 | OD-051 | defender:over-engineering-00 #1 | R12-nh-over-engineering-00#1 | Scope call beyond this candidate: eq at crates/cobre-sddp/src/test_support.rs:152-171 has exactly one caller at the pin (crates/cobre-sddp/tests/basis_trajectory_probe.rs:282), so retiring only its zero-caller sibling le… | **Retire eq_with_anticipated; keep eq while it has a caller** | OD-051 as recorded; the positional-constructor question is revisited when eq loses its one caller. |

### 3.7 Presented read-only, no decision taken

- **R13-substrate** — Keep informational: The golden parity_hash_* roster, the mpi_wire.rs determinism modules and the two parity_baselines decks are handed to the roadmap epic as reusable engine-seam substrate; no TD id, no fix-shape, no re-baselining.
- The 11 defender dismissals on a tree or rule basis stay in the section's Cleared list with their citations.

## 4. Handoffs after the gate

- **E9 (alignment)** — every minted row and every folded delta is `neutral` (provisional); nothing held, no override; E9 validates the tags in calibration.json#assigned[].alignmentHint. Directions: fixture-destination (crate-local homes, engine doubles never in cobre-core), sddp-self-dev-dep.
- **E10 (performance)** — nothing: no PD id at this station; the CLP-job bench link and three-job workspace link are recorded as sequencing notes (runner-cadence-targets).
- **E11 (reconciliation / mirror write-back)** — the two prose-drift rows CD-116 (§2.2:76-78) and CD-118 (§5.2:425-431) with the s52-doc-side direction; the three dup-of sharpenings (oracle `close` pin anchors; lp/builder inline giants; the python item's superseded CI-visibility premise); the 10 retired seeds (TD-004, TD-005, TD-007, TD-009, TD-014, TD-017, TD-018, TD-023, TD-024, TD-029) to confirm their resolving commits; the Rule 2 Boundary decision to restate lens-rules; the §5.1 / §5.5 / §5.6 sequencing notes; the genericity-gate inverse (test-bloat-27).
- **Roadmap (Phase 0a)** — gate-substrate.json: the golden parity_hash_* roster, the mpi_wire.rs determinism gates and the two parity_baselines decks as reusable engine-seam machinery; the 9 target gaps as roadmap items.
- **Handoff confirmation** — Confirm: All handoffs recorded as listed in gate.md §4; the RETURNED marker is written and the generalization alignment epic is unblocked.

**Gate: RETURNED 2026-09-19** — baseline `077dbe2c`; accepted 8, downgraded 0, rejected 0, deferred 0, merged 3; seeds: 50 folds ratified, 10 retired, 0 held, 4 fix-shape refusals; 9 target gaps as roadmap items; 120 needs-human answered.
