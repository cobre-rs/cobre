# lp/ kernel boundary — classification at the register pin `077dbe2c`

Universe: 30 `.rs` modules under `crates/cobre-sddp/src/lp` (reconciled against `plans/architecture-debt-audit/measurements/lp-inventory.json`; drift: none). Machine-readable rows: alignment/lp-classification.json; grep evidence: alignment/lp-grep-proof.json; producer: alignment/lp-nontest-slice.py.

## Rubric

- **engine-neutral** — clean word-boundary slice AND a reading showing only entity indexing, block/cursor primitives, plain equipment columns and rows, generic-constraint lowering or FPHA planes; every substring-only hit carries an argued dismissal
- **sddp-geometry** — state-space columns, cost-to-go anchoring, cut projection, anticipated rings, noise patching — stays in the L3 engine; a zero-hit never overrides the reading (builder/patch.rs)
- **mixed** — both, with a function-level split note; neutralHalf = [neutral spans with zero word-boundary hits, neutral spans + vocabulary-free shared spans + a vocabulary-free preamble]
- **test-sibling** — tests.rs / test_support.rs: zero non-test lines by rule

## Rows (tree order)

| module | class | non-test LOC | anchor | grep result | split note |
| --- | --- | --- | --- | --- | --- |
| `crates/cobre-sddp/src/lp/builder/columns.rs` | mixed | 1315 | `crates/cobre-sddp/src/lp/builder/columns.rs::fill_stage_columns` (:25) | 12 word-boundary hits (substring 24) | move the `fill_<equipment>_columns` family and `GroupBoundLookup` (neutral emission that is anchored after the state region today and would be re-parameterized off the θ-anchored layout on extraction (IV.2)); `fill_storage_columns`, `fill_ar_lag_columns`, `fill_transit_bucket_columns`, `fill_anticipated_*_columns`, `fill_theta_column`, `fill_inflow_slack_columns` and `fill_z_inflow_columns` stay; `ColumnBufs` and `fill_stage_columns` are shared — neutral half 815-1053 |
| `crates/cobre-sddp/src/lp/builder/commitment_reconcile.rs` | sddp-geometry | 243 | `crates/cobre-sddp/src/lp/builder/commitment_reconcile.rs::reconcile_commitment` (:71) | 2 word-boundary hits (substring 4) | - |
| `crates/cobre-sddp/src/lp/builder/commitment_reconcile/tests.rs` | test-sibling | 0 | `crates/cobre-sddp/src/lp/builder/commitment_reconcile/tests.rs:1` | n/a (sibling test file) | - |
| `crates/cobre-sddp/src/lp/builder/delivery_ring.rs` | sddp-geometry | 299 | `crates/cobre-sddp/src/lp/builder/delivery_ring.rs::DeliveryRing` (:26) | 19 word-boundary hits (substring 21) | - |
| `crates/cobre-sddp/src/lp/builder/entries.rs` | mixed | 1686 | `crates/cobre-sddp/src/lp/builder/entries.rs::fill_load_balance_entries` (:1046) | 28 word-boundary hits (substring 39) | seam by row family, not by block mode: move `fill_load_balance_entries`, `fill_fpha_entries`, `fill_evaporation_entries`, `fill_generic_constraint_entries`, `fill_ncs_load_balance_entries`, `fill_operational_violation_entries`, `fill_pumping_water_entries`, the filling entries and `assemble_csc`, with the water-balance family (the two block-mode water fills, the arc-release fills, `resolve_chrono_arrival_density`, `fill_prefilling_shortcircuit`) moving only after its transit-bucket deposits are re-parameterized — the parallel-versus-chronological pair is a retracted, deliberate-by-design pair of formulations and is NOT the seam; `anticipated_ring`, `fill_anticipated_*_entries`, `transit_bucket_ring`, `fill_transit_bucket_definition_entries`, `plant_transit_bucket_range` and `fill_z_inflow_entries` stay (the transit-bucket topology is a ratified reserved seam, cited as sanctioned, not a carve candidate); `fill_state_and_water_entries` and `build_stage_matrix_entries` are shared orchestrators — neutral half 768-1335 |
| `crates/cobre-sddp/src/lp/builder/fpha_cursor.rs` | engine-neutral | 100 | `crates/cobre-sddp/src/lp/builder/fpha_cursor.rs::for_each_fpha_plane` (:44) | zero word-boundary hits; 1 substring-only (substring 1 / word 0); DISMISSED: the only substring hit is `load-bearing` in the doc comment on the row-block nesting — line 33: load-bearing -> ring | - |
| `crates/cobre-sddp/src/lp/builder/layout.rs` | mixed | 1962 | `crates/cobre-sddp/src/lp/builder/layout.rs::StageLayout` (:557) | 26 word-boundary hits (substring 46) | move `EquipmentColumns`, `SlackColumns`, `ConstraintRows`, `OperViolationRanges`, `GenericConstraintLayout`, `FillingLayout`, the `identify_*_hydros` / `build_evap_indices` / `allocate_generic_slack_cols` / `enumerate_generic_constraint_rows` builders, `StageProductionRole` and the equipment accessors; `AnticipatedLayout`, `build_transit_bucket_row_pos`, `build_anticipated_*_row_pos` and the state-region accessors stay; `TemplateBuildCtx`, `StageLayout` itself, `StageLayout::new`, `geometry` and `resolver_geom` are shared and would split — neutral half 852-1043 |
| `crates/cobre-sddp/src/lp/builder/layout/tests.rs` | test-sibling | 0 | `crates/cobre-sddp/src/lp/builder/layout/tests.rs:1` | n/a (sibling test file) | - |
| `crates/cobre-sddp/src/lp/builder/mod.rs` | mixed | 155 | `crates/cobre-sddp/src/lp/builder/mod.rs::GenericConstraintRowEntry` (:131) | 1 word-boundary hit (substring 4) | move the shared constants and `GenericConstraintRowEntry` with the neutral substrate; the patch-buffer/state-pinning narrative and the `PatchBuffer`, `BoundRelaxations`, `DeliveryRing` re-exports stay; the `mod` lines and the scaling re-export split with their targets — neutral half 76-91 |
| `crates/cobre-sddp/src/lp/builder/patch.rs` | sddp-geometry | 413 | `crates/cobre-sddp/src/lp/builder/patch.rs::PatchBuffer` (:24) | zero-hit (substring 0 / word 0); DISMISSED: the zero-hit on both passes over 413 non-test lines is not a neutral signal: the snake_case vocabulary cannot see the 8 CamelCase `StateSpace`/`StateDim` lines, so a zero-hit is never on its own a class-1 proof | - |
| `crates/cobre-sddp/src/lp/builder/rows.rs` | mixed | 588 | `crates/cobre-sddp/src/lp/builder/rows.rs::fill_stage_rows` (:13) | zero word-boundary hits; 3 substring-only (substring 3 / word 0) | move `fill_water_balance_rows` / `fill_parallel_water_rows` / `fill_chronological_water_rows`, `fill_filling_target_rows`, `fill_filled_min_storage_floor_rows`, `fill_load_balance_rows`, `fill_fpha_rows`, `fill_evaporation_rows` and `fill_operational_violation_rows`; `fill_anticipated_*_rows`, `fill_transit_bucket_definition_rows` and `fill_z_inflow_rows` stay; `fill_stage_rows` (the orchestrator) and `fill_zero_equality_rows` are shared — neutral half 384-464 |
| `crates/cobre-sddp/src/lp/builder/scaling.rs` | mixed | 265 | `crates/cobre-sddp/src/lp/builder/scaling.rs::compute_col_scale` (:19) | 9 word-boundary hits (substring 11) | move `compute_col_scale`, `compute_row_scale`, `apply_row_scale` and `apply_col_scale` (once its theta coverage assertion is re-parameterized); `apply_bucket_col_scale`, `apply_commitment_hold_col_scale_unscale` and `compute_noise_scale` stay — neutral half 100-166 |
| `crates/cobre-sddp/src/lp/builder/template.rs` | mixed | 1327 | `crates/cobre-sddp/src/lp/builder/template.rs::build_stage_templates` (:723) | 8 word-boundary hits (substring 23) | move `collect_load_bus_indices`, `StageBuildOutput` and `StageGeometry::block_storage_col`; `StageTemplates` (discount factors), `build_single_stage_template`, `models_from_normal`, `load_models_from_normal`, `build_stage_templates`, `build_stage_templates_resolving_layout` and `build_filling_v_target` stay; `StageGeometry`, `build_template_build_ctx` and `assemble_stage_templates_output` are shared and would split — neutral half 63-483 |
| `crates/cobre-sddp/src/lp/builder/template/tests.rs` | test-sibling | 0 | `crates/cobre-sddp/src/lp/builder/template/tests.rs:1` | n/a (sibling test file) | - |
| `crates/cobre-sddp/src/lp/builder/test_support.rs` | test-sibling | 0 | `crates/cobre-sddp/src/lp/builder/test_support.rs:1` | n/a (sibling test file) | - |
| `crates/cobre-sddp/src/lp/generic_constraints.rs` | mixed | 985 | `crates/cobre-sddp/src/lp/generic_constraints.rs::resolve_variable_ref` (:195) | 2 word-boundary hits (substring 8) | move `resolve_variable_ref` and every role-(b) equipment resolver (turbine cells, storage boundary, evaporation, outflow, generation, line exchange, bus deficit, pumping, contracts, block variables); `resolve_hydro_storage`, `resolve_hydro_inflow` and `resolve_anticipated_decision` stay with the engine behind a state-column callback; `GenericResolverGeom` is the shared view that splits into two — neutral half 764-823 |
| `crates/cobre-sddp/src/lp/generic_constraints/tests.rs` | test-sibling | 0 | `crates/cobre-sddp/src/lp/generic_constraints/tests.rs:1` | n/a (sibling test file) | - |
| `crates/cobre-sddp/src/lp/indexer/anticipated_gate.rs` | sddp-geometry | 112 | `crates/cobre-sddp/src/lp/indexer/anticipated_gate.rs::is_anticipated_decision_active_for_delivery` (:90) | zero word-boundary hits; 2 substring-only (substring 2 / word 0) | - |
| `crates/cobre-sddp/src/lp/indexer/block_grid.rs` | engine-neutral | 126 | `crates/cobre-sddp/src/lp/indexer/block_grid.rs::BlockGrid` (:36) | zero word-boundary hits; 1 substring-only (substring 1 / word 0); DISMISSED: the only substring hit is the English participle `mirroring` in the module doc — line 23: mirroring -> ring | - |
| `crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs` | sddp-geometry | 292 | `crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs::CutStateProjection` (:39) | 28 word-boundary hits (substring 33) | - |
| `crates/cobre-sddp/src/lp/indexer/entity_index.rs` | engine-neutral | 293 | `crates/cobre-sddp/src/lp/indexer/entity_index.rs::HydroSys` (:83) | zero-hit (substring 0 / word 0) | - |
| `crates/cobre-sddp/src/lp/indexer/hydro_cell.rs` | engine-neutral | 204 | `crates/cobre-sddp/src/lp/indexer/hydro_cell.rs::HydroCellIndex` (:33) | zero word-boundary hits; 2 substring-only (substring 2 / word 0); DISMISSED: the substring hits are the participles `filtering`, `renumbering` and `reordering` in doc comments — line 15: filtering -> ring; line 63: renumbering -> ring, reordering -> ring | - |
| `crates/cobre-sddp/src/lp/indexer/index.rs` | mixed | 247 | `crates/cobre-sddp/src/lp/indexer/index.rs::Col` (:30) | 8 word-boundary hits (substring 8) | move `Col`, `Row`, `BlockIdx` and `Boundary` (plus `Boundary::from_index`) to the neutral substrate; `StateDim`, `CutSlot`, `InCol` and `OutCol` stay with the engine; the module doc's cut-path paragraphs go with them — neutral half 95-115 |
| `crates/cobre-sddp/src/lp/indexer/layout.rs` | engine-neutral | 40 | `crates/cobre-sddp/src/lp/indexer/layout.rs::EvaporationIndices` (:17) | zero-hit (substring 0 / word 0) | - |
| `crates/cobre-sddp/src/lp/indexer/mod.rs` | mixed | 125 | `crates/cobre-sddp/src/lp/indexer/block_grid.rs::BlockGrid` (:36) | 10 word-boundary hits (substring 13) | the `mod`/`pub use` lines for block_grid, entity_index, hydro_cell, layout, range_cursor, storage_boundary_grid and study_dimensions (and the Col/Row/BlockIdx/Boundary half of the `index` re-export) move with the neutral substrate; the state_space, cut_state_projection and anticipated_gate lines and the state-layout narrative stay — neutral half 18-29 |
| `crates/cobre-sddp/src/lp/indexer/range_cursor.rs` | engine-neutral | 41 | `crates/cobre-sddp/src/lp/indexer/range_cursor.rs::RangeCursor` (:23) | zero word-boundary hits; 1 substring-only (substring 1 / word 0); DISMISSED: the only substring hit is `consecutive` in the module doc, which contains `cut` — line 10: consecutive -> cut | - |
| `crates/cobre-sddp/src/lp/indexer/state_space.rs` | sddp-geometry | 729 | `crates/cobre-sddp/src/lp/indexer/state_space.rs::StateSpace` (:50) | 34 word-boundary hits (substring 40) | - |
| `crates/cobre-sddp/src/lp/indexer/storage_boundary_grid.rs` | engine-neutral | 77 | `crates/cobre-sddp/src/lp/indexer/storage_boundary_grid.rs::StorageBoundaryGrid` (:22) | zero-hit (substring 0 / word 0) | - |
| `crates/cobre-sddp/src/lp/indexer/study_dimensions.rs` | engine-neutral | 50 | `crates/cobre-sddp/src/lp/indexer/study_dimensions.rs::StudyDimensions` (:27) | zero-hit (substring 0 / word 0) | - |
| `crates/cobre-sddp/src/lp/mod.rs` | mixed | 27 | `crates/cobre-sddp/src/lp/generic_constraints.rs::resolve_variable_ref` (:195) | zero word-boundary hits; 1 substring-only (substring 1 / word 0) | the `mod generic_constraints` line and its sentence move with the neutral substrate; the state-pinning and template-factory sentences stay — neutral half 4-8 |

## Totals

**Totals.** corpus 11,701 non-test lines over 25 production modules (30 rows; 5 sibling test files at 0; classes: 8 engine-neutral, 6 sddp-geometry, 11 mixed). engine-neutral: 931 lines; sddp-geometry: 2,088; mixed: 8,682 with neutral half 3,939-5,610. Measured share 4,870-6,541 of 11,701 (42-56%) versus the roadmap's fifth-to-a-quarter band 2,340-2,925 (the ticket's 2,335-2,919 over 11,675): **amended (dated 2026-09-19, baseline `077dbe2c`)** — the fifth-to-a-quarter estimate (2340-2925 of 11701) is superseded by the measured 4870-6541 (42-56%) at 077dbe2c on 2026-09-19; it undercounts the plain equipment column/row/entry fills and the generic-constraint lowering, which alone exceed it; extraction stays priced as a rewrite (IV.2) because every neutral fill is anchored after the θ-anchored state region today.

**Method.** non-test slice = brace-aware exclusion of every `#[cfg(test)]`-attributed item (a statement item ends at its `;`; production lines after an attributed item are kept; the token inside a `//!` doc comment is prose; sibling `tests.rs` / `test_support.rs` count zero), not first-marker truncation; grep run twice over the slice, substring per the rubric and word-boundary to separate false positives; every substring-only hit on an engine-neutral row carries an argued dismissal; a zero-hit never classifies on its own (CamelCase geometry is invisible to the snake_case vocabulary). Extraction priced as a rewrite per Part IV.2.

## Readings

- `crates/cobre-sddp/src/lp/builder/columns.rs` — column fills: turbine, spillage, diversion, thermal, line, deficit, FPHA, evaporation, slack, NCS, pumping, contract and filling columns are plain equipment columns; storage (the state region), AR-lag, transit-bucket, anticipated slot/state, theta, inflow-slack and z_inflow columns are state-space geometry.
- `crates/cobre-sddp/src/lp/builder/commitment_reconcile.rs` — reconciles a pinned anticipated-commitment ring slot against the delivery-stage bounds through StateSpace and StageGeometry.
- `crates/cobre-sddp/src/lp/builder/delivery_ring.rs` — the lagged-delivery ring skeleton (Markov-1 slot per stage) shared by the travel-time bucket ring and the anticipated-thermal ring — state geometry borrowed from StateSpace.
- `crates/cobre-sddp/src/lp/builder/entries.rs` — CSC entry fills: the water-balance, arc-release, filling, pumping, load-balance, FPHA, evaporation, generic-constraint, NCS and operational-violation entries are equipment/physics rows; the anticipated ring, transit-bucket ring and z_inflow entries are state geometry.
- `crates/cobre-sddp/src/lp/builder/fpha_cursor.rs` — the FPHA plane visitor over a stage's hydro cells.
- `crates/cobre-sddp/src/lp/builder/layout.rs` — StageLayout allocates both regions: the equipment column/row chains, slack families, generic-constraint rows and their accessors are neutral; AnticipatedLayout, the anticipated/transit-bucket row-position builders and the state-region accessors (col_theta, col_storage_in_start, col_inflow_lags_start, col_z_inflow_start, col_anticipated_state_start, n_state, anticipated_decision) are geometry.
- `crates/cobre-sddp/src/lp/builder/mod.rs` — module root: the per-solve patch sequence and state-pinning narrative is geometry; the shared physics constants (M3S_TO_HM3, the evaporation column offsets) and GenericConstraintRowEntry are neutral; the `mod` and re-export lines split with their targets.
- `crates/cobre-sddp/src/lp/builder/patch.rs` — PatchBuffer owns the column-bound state region: fill_col_state_patches walks StateSpace::state_to_lp_incoming_column over every state-vector index and fill_forward_patches rewrites the stochastic row bounds before each solve.
- `crates/cobre-sddp/src/lp/builder/rows.rs` — row-bound fills: the water-balance, filling, load-balance, FPHA, evaporation and operational-violation rows are equipment rows; the anticipated fishing/state-out/slot rows, the transit-bucket definition rows and the z_inflow rows are ring and noise geometry; the three substring hits (`mirroring`, `maturing`) are prose.
- `crates/cobre-sddp/src/lp/builder/scaling.rs` — geometric-mean LP prescaling is engine-neutral conditioning; the bucket and commitment-hold unscaling and the noise pre-scaling are ring and noise geometry, and apply_col_scale asserts coverage through theta.
- `crates/cobre-sddp/src/lp/builder/template.rs` — the stage-template factory IV.2 names: discount factors, pre-baked noise (models_from_normal / load_models_from_normal), build_single_stage_template and build_stage_templates are SDDP-shaped; collect_load_bus_indices, StageBuildOutput and block_storage_col are neutral; StageGeometry, build_template_build_ctx and assemble_stage_templates_output are shared.
- `crates/cobre-sddp/src/lp/generic_constraints.rs` — generic-constraint lowering onto the indexed column layout is neutral emission, except the role-(a) state region reached through GenericResolverGeom's StateSpace handle (resolve_hydro_storage, resolve_hydro_inflow, resolve_anticipated_decision); the two word-boundary hits are the doc's cut-path paragraph on that role split.
- `crates/cobre-sddp/src/lp/indexer/anticipated_gate.rs` — anticipated-decision horizon and commissioning-window gating keyed on StateSpace's lead stages and resolution (anticipated ring geometry); the two substring hits (`load-bearing`, `maturing`) are prose and irrelevant to the class.
- `crates/cobre-sddp/src/lp/indexer/block_grid.rs` — the block-major stride primitive shared by every fill path.
- `crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs` — storage-scoped projection of StateSpace onto the enabled cut-state dimensions (cut storage, subgradient extraction, cut rendering).
- `crates/cobre-sddp/src/lp/indexer/entity_index.rs` — typed system/local entity index vocabulary carrying no arithmetic; AnticipatedLocal is a local-list position, not ring geometry.
- `crates/cobre-sddp/src/lp/indexer/hydro_cell.rs` — the study-scope partition of hydro unit groups into bus cells (entity indexing).
- `crates/cobre-sddp/src/lp/indexer/index.rs` — typed address vocabulary: Col, Row, BlockIdx and Boundary are LP-address newtypes any engine needs; StateDim, CutSlot, InCol and OutCol are state-vector and cut-render vocabulary.
- `crates/cobre-sddp/src/lp/indexer/layout.rs` — per-stage evaporation column/row and FPHA row-block satellites; the one CamelCase line is a doc pointer to StateSpace.
- `crates/cobre-sddp/src/lp/indexer/mod.rs` — module root: a column-layout narrative owned by StateSpace plus the `mod` and re-export lines of both the neutral primitives and the geometry types.
- `crates/cobre-sddp/src/lp/indexer/range_cursor.rs` — the running offset allocator; its two CamelCase lines are doc pointers to StateSpace::new as one of its two callers.
- `crates/cobre-sddp/src/lp/indexer/state_space.rs` — the stage-invariant state-vector layout and its incoming/outgoing LP-column resolvers — the SDDP state region itself.
- `crates/cobre-sddp/src/lp/indexer/storage_boundary_grid.rs` — block/cursor primitive for the chronological storage-boundary column formula; it takes StateSpace column bases as inputs (neutral emission that is anchored after the state region today and would be re-parameterized off the θ-anchored layout on extraction (IV.2)).
- `crates/cobre-sddp/src/lp/indexer/study_dimensions.rs` — the study-invariant non-state LP shape (entity counts, presence flags, the anticipated-thermal identity list); its two CamelCase lines are the doc's ownership split against StateSpace, not geometry.
- `crates/cobre-sddp/src/lp/mod.rs` — directory-module root: three `mod` lines and a doc that narrates the state-pinning column-bound contract (geometry) beside the generic-constraint lowering (neutral); the only substring hit is `lowering`.

## Deviations recorded (no spec edit)

- The ticket pins a136840d; the register header pins 077dbe2c, so every row is measured at the register pin; the same file set (30 modules) exists at both pins and the ticket's per-module figures (block_grid 126, anticipated_gate 112, patch.rs 413 with 8 CamelCase lines, naive 11,396, gross 44,374) reproduce exactly at a136840d.
- The ticket's corpus total of 11,675 does not reproduce from its stated slice rule: the rule yields 11685 at a136840d and 11701 at 077dbe2c (columns.rs +2, entries.rs +14 since the scaffold pin). The 10-line gap is consistent with the ticket snippet's handling of `#[cfg(test)] mod tests;`, which scans forward to the next `{` and would swallow builder/mod.rs's re-export block as test code; this slicer ends a statement item at its `;`, as the rule's own words require.
- The sddp station's measurements/lp-inventory.json (non_test_lines 11,655) follows scripts/loc-stats.sh, which also excludes `#[cfg(any(test, feature = "test-support"))]` items; the ticket's rule keeps them (builder/mod.rs 155 vs 145, builder/template.rs 1327 vs 1293, lp/mod.rs 27 vs 25). The inventory is used for the module universe only.
- plans/ is tracked, so the artifacts are committed rather than gitignored.
