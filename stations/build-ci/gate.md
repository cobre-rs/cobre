# Owner gate — build-ci (2026-09, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`)

Run in the main session as AskUserQuestion rounds over this digest. Inputs: `calibration.json` (19 calibrated entries, 4 dismissed + 5 sanctioned cleared, 1 dup-of, 0 held), `verdicts.json` (defender `_needsHuman`), the four `candidates-<lens>.json` envelopes (`_needsHuman`), `gate-census.json` (17 rows), `partI-handoff.json` (item I.3-6, evidence only), `td-queue.json` (empty-with-reason, epic 8), `verification.md` (shared verifier 7/7 + station driver 11/11 + module 47/47 at the pin). This station holds no runtime state: every presented row carries its enforcement strength (Wiring column: blocking | advisory-by-design | unwired | transitively-advisory | n/a for manifest, schema and docs subjects) next to its severity, and a gate-derived row without one is a digest error, not a question. Severity is the HOUSE rating; a calibration downgrade shows `house (reviewer: original)`. The gate executes no fix, rewires no gate, runs no measurement and asserts no figure; CI wall time stays informational (no PD id, no E10 line).

## 1. Presentation

### 1.1 The genericity two-site entry — asked FIRST (round 1)

The station's headline claim and the only entry that binds a hard rule. Covering the future L1 crates cobre-model and cobre-network needs a TWO-SITE edit: the rule prose in CLAUDE.md:39-41 AND SCAN_DIRS in scripts/ci/check-infra-genericity.sh:62-68 together with its CI step (.github/workflows/ci.yml:261-262, job `quality-scripts`, step "Infra genericity gate"). An answer that names one site is incomplete and is re-asked. Alignment advances-1 citing roadmap Part IV.1 ("means amending the rule text and the CI grep's crate list"); the defender proposed advances-0b — recorded in the Alignment note.

| # | ID | Sev | Wiring | Alignment | Claim (narrowed) | Fix-shape (one line) | Sites | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-099 | B | blocking | advances-1 | The infrastructure-crate set is hand-written at sites the gate never pairs: its own header at check-infra-genericity.sh:5-8, SCAN_DIRS at :62-68, and the hard-rule bullet at CLAUDE.md:39-41… | Amend in place, and make the pairing mechanical rather than remembered. The durable shape is to let one of the two sites own the crate list and have the other derive or… | CLAUDE.md:39; scripts/ci/check-infra-genericity.sh:62; .github/workflows/ci.yml:261 | #1 |

### 1.2 The PATTERN vocabulary limitation (round 2; dismissed at ingest)

**architecture-01** — _The infra-genericity gate matches one engine's vocabulary rather than the engine-concept rule it enforces, so a sibling engine's nouns and an underscore-joined identifier both pass unflagged_ — dismissed by the defender `deliberate-and-documented`, citation `scripts/ci/check-infra-genericity.sh:15` (re-raise-of CD-061, sharpen judged and not sustained): Both limbs fail at the pin, and the gate-side delta is the one I judge. The script header does not merely offer three examples at :10-11; line :15 declares the pattern's scope outright as standalone cut / Cut (word boundary), so grep -E treating underscore as a word constituent is the stated semantics rather than an unnoticed hole, and the implementation note at :50-56 pins grep -E as the matching engine precisely because it honours those boundaries. The candidate's own fix shape disproves its load-bearing premise that the boundary protects only execution, cuts_active and cutting_edge: an underscore-separator boundary would make this blocking step at .github/workflows/ci.yml:261-262 fail at…

The ticket asks this immediately after the two-site entry: recorded as its own entry, folded into it, or dismissed with the defender's citation. The pipeline's state is the dismissal; round 2 puts that dismissal to the owner (keep dismissed first, since the citation decides it), with record / fold / defer as the alternatives. The owner questions the dismissed candidate still carries (token scope, the FreezeScratch site) are NH #2.

### 1.3 Census-derived entries (round 3 — the architecture lens, plus the check-comment-bloat.sh census row)

The gate-wiring census classifies **check-comment-bloat.sh** `unwired` in gate-census.json and the section renders it `transitively-advisory`: no `.github/workflows/` and no `scripts/pre-commit` reference (the negative grep), reachable only from the advisory `scripts/ci/quality-report.sh:132` (invoker class advisory-by-design), so the classification is not 'dead'. It carries no id (sanctioned advisory-by-design, `# Exit code: ALWAYS 0` at :39); round 3 ratifies the classification with the four architecture entries. CD-100 is the ci.yml L4-L7 trigger asymmetry (push [main, develop], pull_request [main]).

| # | ID | Sev | Wiring | Alignment | Claim (narrowed) | Fix-shape (one line) | Sites | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-100 | B (reviewer: A) | blocking | neutral | Narrower than the title: ci.yml alone carries the gap — its pull_request filter is [main] at :6-7 against a push filter of [main, develop] at :4-5, so no ci.yml job (the quality-scripts gat… | One sentence, as the probe directs: the trigger list is the owner's call, and the shape that removes the asymmetry is to have the pull_request trigger cover the same bra… | - | #3 |
| 2 | CD-101 | B (reviewer: A) | blocking | neutral | Both invariance-shuffle.yml run lines (:34 and :55) omit cobre-sddp's own test-support feature, so the parity target they name fails to compile at the pin (lib.rs:46-47 gates pub mod test_s… | Amend the two existing run lines so the feature set they pass actually satisfies the target they name: the parity binary needs the crate's own `test-support` feature alo… | - | #4 |
| 3 | CD-102 | B | blocking | neutral | No guard in the corpus compares the canonical examples/1dtoy/ file SET against either hand-maintained enumeration (build.rs TEMPLATE_FILES at :16, templates.rs DTOY1_FILES), so the single e… | Amend the existing blocking gate rather than adding one. The gate already has the canonical directory available at the workspace root and already knows the template name… | - | #5 |
| 4 | CD-103 | B (reviewer: A) | blocking | neutral | Only the `[lints.clippy]` table at `crates/cobre-python/Cargo.toml:50-55`, including `unwrap_used = deny`, is evaluated by no step at the pin, and the gap is a documented-but-uncarried cont… | Amend the job that already exists rather than reopening the exclusion. The workspace exclusion is sanctioned and states its own reason, so the fix is not to make the cra… | - | #6 |
| 5 | census row | — | unwired → transitively-advisory | — | check-comment-bloat.sh: no direct wiring; invoked by quality-report.sh:132 inside an advisory step | none (sanctioned advisory-by-design; classification only) | scripts/ci/check-comment-bloat.sh:39; scripts/ci/quality-report.sh:132 | — |

### 1.4 Over-engineering entries (round 4)

The MPICH Cache/Build/Set triple (CD-112: eight byte-identical copies inside ci.yml plus one near-variant each in mpi-slurm.yml and release-mpi.yml, no .github/actions/ directory at the pin — ten consumers, so the one-consumer objection is waived and the row is neutral, never conflicts); the MPICH version literal (CD-113); the five hand-kept SCAN_DIRS copies (CD-114); the publish list against the five reserved stubs (CD-115, the stubs' existence stays sanctioned); and the two no-op slow-tests declarations (OD-049 cobre-cli, OD-050 cobre-io). DEVIATION: the ticket lists the slow-tests declarations as sanctioned/noRoundFor; the defender brief excluded them from the sanctioned set (no mirror entry, no rule) and both were confirmed and minted, so they are asked here. The entry-free allowlist is sanctioned and gets no question.

| # | ID | Sev | Wiring | Alignment | Claim (narrowed) | Fix-shape (one line) | Sites | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-112 | B | blocking | neutral | Inside ci.yml alone, the Cache/Build/Set MPICH triple at 43-68 recurs byte-identically in seven further jobs (81-106, 124-149, 174-199, 310-335, 371-396, 462-487, 516-541) with no in-file r… | Extract the Cache/Build/Set triple into one composite action under a new .github/actions/ directory and have all ten sites use it. The action takes the MPICH version, an… | - | #7 |
| 2 | CD-113 | B | n/a | neutral | At the pin the base image has two unlinked version decisions, not three: the ARG default at tests/slurm/Dockerfile.base:42 fixes what is compiled and slurm-base-image.yml:14 fixes the tag p… | Give the version one owner and make every other site derive from it. The base-image workflow passes the version to docker/build-push-action as a build-arg so the ARG def… | - | - |
| 3 | CD-114 | B | blocking | neutral | The residue is unguarded duplication plus two false comments, not the two shapes: the four identical ten-entry SCAN_DIRS copies at check-comment-refs.sh:57-68, check-comment-line-refs.sh:49… | Hoist the crate-source directory list into scripts/ci/lib/comment_scan.sh, the shared library these gates already source, as one exported array beside the cfg(test) boun… | - | #8 |
| 4 | CD-115 | B | blocking | neutral | The crates.io release set exists only as eight literal crate names in the publish step body at publish.yml:80-103, with no publish key in the root manifest or any crates/*/Cargo.toml at the… | Make the intent explicit at one site and let the workflow derive from it rather than restate it. Either add publish = false to the five reserved manifests, which puts th… | - | #10 |
| 5 | OD-049 | C | n/a | neutral | The empty slow-tests entry at crates/cobre-cli/Cargo.toml:57 is fully inert at the pin on all three axes I could test - zero cfg consumers inside the crate, no forwarding to any dependency… | Delete the empty declaration and its comment from the manifest. Nothing in the tree needs it: the workspace-wide command in CLAUDE.md:12 and the NON_SOLVER_FEATURES stri… | - | #9 |
| 6 | OD-050 | C | n/a | neutral | cobre-io/Cargo.toml:19 declares slow-tests with an empty body that gates nothing at the pin (no occurrence of the token under the crate's src or tests, no forwarding through the empty body,… | Delete the empty declaration and its comment, and drop the slow-tests attribution from the cobre-io line of the feature census comment at ci.yml:21 in the same change. T… | - | #9 |

### 1.5 Drift entries (round 5)

Eight documentation-drift entries, each naming the tree location that contradicts the doc (the `Drift (doc ↔ tree)` line of the register entry). DEVIATION: the ticket's drift case — the two docs/design/README.md rows (L26, L27) statused 'Implemented (retained pending fold into the live spec)' against the vocabulary at L4 and the convention at L31 — was DISMISSED by the defender (drift-00, premise-false-at-pin: the vocabulary block claims no exhaustiveness and the convention's trigger is the fold, not shipping); it sits in 1.7 with its citation and its owner direction is NH #12. CD-110 is the I.3-6 evidence row (claim-stale; no disposition here).

| # | ID | Sev | Wiring | Alignment | Claim (narrowed) | Fix-shape (one line) | Sites | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-111 | B | n/a | neutral | Confined to the containment parenthetical 'isolated in src/gemm.rs' on the unsafe-code hard rule at CLAUDE.md:19: at the pin cobre-sddp also compiles the unsafe extern "C" qhull binding at… | State the rule in a form that cannot fall behind a crate's manifest: each overriding crate documents its own unsafe surfaces in its [lints] rationale, and the hard rule… | CLAUDE.md:19; Cargo.toml:36; .github/workflows/ci.yml:154 | - |
| 2 | CD-104 | C | n/a | neutral | The eight table rows at docs/design/README.md:20-27 omit exactly one of the nine design docs tracked beside the README at the pin, post-horizon-input-unification.md, whose own :3 status is… | Add the missing row to the table with the status the doc already declares, keeping the row's summary to the shape the other rows use. The durable form of the claim at :4… | - | - |
| 3 | CD-105 | C | n/a | neutral | Exactly one doc in docs/design, anticipated-thermals-and-water-travel-time.md, carries no status marker at any line in the file, which contradicts the universal assertion at docs/design/REA… | Bring each doc's own first lines into agreement with its row: give anticipated-thermals-and-water-travel-time.md the status line every sibling has, and restate backward-… | - | #11 |
| 4 | CD-106 | C | n/a | neutral | The word `five` at ARCHITECTURE.md:95 is false at the pin, contradicted by its own same-sentence parenthetical at :95-96 and by the six `cobre-*` path dependencies declared in crates/cobre-… | Delete the count rather than correcting it, and let the parenthetical carry the fact — the dependency list is a manifest-owned enumeration and a hand-written number in f… | - | - |
| 5 | CD-107 | C | blocking | neutral | Only four rows are wrong: cobre-core, cobre-io, cobre-sddp and cobre-stochastic each declare test-support (crates/cobre-core/Cargo.toml:30, crates/cobre-io/Cargo.toml:33, crates/cobre-sddp/… | The table is a hand-maintained per-crate enumeration of manifest facts with nothing to keep it true, so either complete it and accept that it will rot again, or replace… | - | - |
| 6 | CD-108 | C (reviewer: B) | blocking | neutral | Only the citation half survives: the shipped rustdoc at crates/cobre-comm/tests/local_conformance.rs:4, together with the eight section banners that repeat its SS1.1-SS1.8 coordinates, cite… | Fix the citation at its source: state the contract the test actually verifies, by naming the surfaces under test as the file's first line already does, or point at testi… | - | #13 |
| 7 | CD-109 | C (reviewer: B) | blocking | neutral | Five citation sites across four gate headers name a `.claude/rules` section number that does not resolve at the pin: check-doc-paths.sh:5 and :43 cite doc-integrity.md §6.4 in a file whose… | Re-point the four citations at what they mean and prefer the coordinate that survives renumbering: the directive id where one exists, as check-allow-rationale.sh already… | - | #14 |
| 8 | CD-110 | C | blocking | neutral | Only the parenthetical clause of the mirrored Part-I row at plans/architecture-debt-audit/tools/target-layering-brief.md:91 is contradicted at the pin: no file is exempt from the blocking g… | Strike the exemption parenthetical from the Part-I row and let the row's surviving claim stand on the format itself, which is the part the roadmap actually reasons about… | - | #15 |

### 1.6 Holds (Alignment `conflicts`) — none

No calibrated entry is a `conflicts` row (calibration.json `pullDontPush.conflictsHeld` = 0): no fix-shape proposes a new engine-vocabulary gate framework or a one-consumer composite action; the MPICH composite action has ten consumers (ci.yml 8, mpi-slurm.yml 1, release-mpi.yml 1) and is exempt. The hold round is therefore empty; had one existed it would be asked alone with the amend-the-existing-script alternative and an explicit override option, and an un-overridden hold would stay held with no id promoted.

### 1.7 Cleared and sanctioned — presented read-only, no decision taken

**Part-I item I.3-6 — presented read-only, disposition: epic 9.** cobre-io's policy checkpoint format is literally cut records, with the parenthetical that the genericity gate exempts the four policy files (target-layering-brief.md:91, from beyond-sddp-generalization.md I.3 item 6) Station verdict `claim-stale`; baselineEvidence: `scripts/ci/check-infra-genericity.sh:74` (EXCLUDED_FILES=() — the exclusion array is empty at the baseline); `scripts/ci/check-infra-genericity.sh:70` (comment block :70-73 — Empty: the value-function-artifact format break retired the former output/policy/ exem…); `scripts/ci/check-infra-genericity.sh:40` (header :38-44 — Explicitly excluded files: None. The output/policy/ files formerly sat here pending a value-f…). This gate asserts no disposition and no final Alignment for the row; the two-site block of the handoff is synced from the ratified CD-099 decision only.

**Dismissed by the defender (4), each with the basis and the citation that decides it (no question; the PATTERN dismissal is put to the owner in round 2 because the ticket names it):**

- **architecture-01** — deliberate-and-documented *scripts/ci/check-infra-genericity.sh:15* — The infra-genericity gate matches one engine's vocabulary rather than the engine-concept rule it enforces, so a sibling engine's nouns and an undersc…: Both limbs fail at the pin, and the gate-side delta is the one I judge. The script header does not merely offer three examples at :10-11; line :15 declares the pattern's scope outright as standalone cut / Cut (word boun…
- **drift-00** — premise-false-at-pin *docs/design/README.md:31-36* — docs/design/README.md declares a closed five-value status vocabulary and a delete-on-ship convention that three of its eight rows and two still-prese…: Both load-bearing premises fail against the README text at the pin. On closure, docs/design/README.md:4 says only 'Each doc carries a status at its top; this index is the map. Status vocabulary:' and the bullets at :6-1…
- **drift-05** — premise-false-at-pin *CLAUDE.md:40* — The genericity hard rule names three forbidden tokens while the gate it is enforced by rejects eleven alternatives, including standalone cut and Cut: CLAUDE.md:40 states the contract as an invariant, namely that the five infrastructure crates must contain zero algorithm-specific references, and the token list at :41 sits inside a parenthetical that illustrates that i…
- **performance-04** — premise-false-at-pin *scripts/ci/lib/comment_scan.sh:5 with .github/workflows/ci.yml:262-300* — Ten of the fourteen quality-scripts steps each enumerate the crates tree themselves, and the shared scan library they already source exposes only a p…: Both descriptive halves hold at the pin: the shared library exposes only per-file predicates (cs_emit_production_lines at :21 and cs_emit_comment_lines at :32, each taking one file), and its four callers source it at ch…

**Sanctioned (5; the sanctioned-advisory clearance — offering a question would invite a re-raise):**

- **advisory-by-design exit 0 of check-comment-line-refs.sh, check-comment-banners.sh, check-comment-bloat.sh and quality-report.sh** — `# Exit code: ALWAYS 0` headers at scripts/ci/check-comment-line-refs.sh:35, scripts/ci/check-comment-banners.sh:40, scripts/ci/check-comment-bloat.sh:39, scripts/ci/quality-report.sh:42; the ci.yml ADVISORY step comments :278-279, :282-283, :298-299; termina…
- **the entry-free scripts/ci/allow-rationale-allowlist.txt** — scripts/ci/allow-rationale-allowlist.txt:1 `intentionally EMPTY`, :9-13 the burned-down backlog, :15 `KEEP THIS FILE EMPTY` — 0 active entries at the pin; emptiness is the designed state, not an unwired gate
- **the five reserved stub crates cobre-mcp, cobre-tui, cobre-flow, cobre-uc, cobre-emt (Cargo.toml:12-16)** — CLAUDE.md:10 (reserved stubs), ARCHITECTURE.md:108-125 (`### Reserved crates (not yet implemented)`), plans/generalizing/beyond-sddp-generalization.md:84 (the named algorithm-crate stubs are kept) and :1236 (cobre-flow / cobre-emt deliberately out of scope) —…
- **the cobre-python workspace exclusion** — Cargo.toml:18-23 states the reason (maturin build; keeps `cargo test --workspace` and cargo-dist free of a Python interpreter)
- **EXCLUDED_FILES=() in the genericity gate** — scripts/ci/check-infra-genericity.sh:70-74 and :38-44 — the emptied exemption is the sanctioned state; adding an entry needs owner sign-off

**Dup-of (1, folded, no new number):** performance-01 → performance-00.

**Informational (5 UNMEASURED performance rows — CI wall-time / gate-runtime shape; no PD id, no E10 line):** performance-00 (The schemas job carries the MPICH Cache/Build/Set triple although its only compile is the…); performance-02 (The eight MPICH consumers in ci.yml share one immutable cache key with no dependency edge…); performance-03 (Three job definitions each compile the release cobre binary independently, with no artifa…); performance-05 (Three jobs install their cargo tool from source with cargo install while the same workflo…); performance-06 (Nine of the fourteen job definitions check out every vendored submodule recursively, and…). Their owner directions are NH #16-#20.

### 1.8 Queues handed on (counts only; no figure)

- **alignment → E9:** CD-099 (advances-1, Part IV.1, the two-site edit) and CD-110 (the I.3-6 evidence row, claim-stale, no disposition); every `advances-*` hint is provisional; no conflicts hold.
- **perf-queue → E10:** nothing — the performance lens is informational at this station (no perf-queue.json).
- **td-queue.json → E08:** empty-with-reason envelope naming epic 8 (no TD row).
- **mirror write-back → E11:** the root-level policy.fbs path drift; the stale CI-invisibility premise handed in by cli-python (SN-06); the README index rows once the fold is decided (NH #12); the cobre-stochastic slow-tests twin (NH #9).

### 1.9 Worker needs-human items (20 questions; 26 defender + 16 attacker items merged)

| # | Entry | From | Question | Options (recommended first) |
| -- | -- | -- | -- | -- |
| 1 | CD-099 | defender:architecture-00 | CD-099 — where does the single owner of the infra-crate set sit? CLAUDE.md:39-41 keeps its enumeration pinned by a new divergence check in the gate, or the bullet becomes a shape-only pointer to the gate's list (doc-integrity §3 mode 1 admits either; the choi… | Gate owns the list; CLAUDE.md enumeration pinned by a divergence check · CLAUDE.md becomes a shape-only pointer · Defer to E9 |
| 2 | — | defender:architecture-01 (dismissed); attacker:architecture #5 | PATTERN scope (architecture-01, dismissed): should the gate's token scope ever widen past standalone word-boundary tokens, given an underscore boundary would red this blocking gate on the cobre-io timing / row-selection columns CD-061 ratified as needing no r… | Keep standalone-token scope; no FreezeScratch addition · Widen to underscore boundaries with owner sign-off · Keep scope; add the FreezeScratch site to CD-061's I.3-6 widening |
| 3 | CD-100 | defender:architecture-02; attacker:architecture #1 | CD-100 — the ci.yml trigger list: extend the pull_request filter to [main, develop], accepting a full ci.yml run on every develop pull request, or ratify post-merge detection on develop as the intended trade and record that rationale in the workflow (no text… | Extend pull_request to [main, develop] · Ratify post-merge detection; document the trade in ci.yml · Defer — trigger: the first develop merge that ships a gate regression |
| 4 | CD-101 | defender:architecture-03 (2 items); attacker:architecture #2 | CD-101 — shuffle-matrix reachability: restore the commented-out nightly schedule at invariance-shuffle.yml:5-7, or convert the bare-ignore shuffle tests to the crate's slow-tests cfg_attr form so the automatically triggered suite runs them; and is a compile-o… | cfg_attr slow-tests form; fix the two run lines; no compile-only step · Restore the nightly schedule at :5-7 · Both: cfg_attr form and the nightly schedule |
| 5 | CD-102 | defender:architecture-04 | CD-102 — is examples/1dtoy/ contractually EQUAL to what cobre init materializes, or a superset allowed to carry non-input files as examples/4ree/README.md does? The answer decides whether the missing assertion is set equality or one-way canonical-subset inclu… | One-way canonical-subset inclusion with an explicit non-input allowlist · Set equality · Defer — trigger: the next template added to examples/ |
| 6 | CD-103 | defender:architecture-05 (3 items); attacker:architecture #3; attacker:architecture #4; attacker:drift #3 | CD-103 — the bindings crate's lint coverage: where does the manifest-scoped clippy pass live (beside the existing manifest-scoped test step in the python job, inheriting interpreter and backend on the single 3.12 entry, or a job without an interpreter)? Does… | Clippy beside the python job's test step; doc = false stays sanctioned; add the command to CONTRIBUTING · Clippy in a no-interpreter job; doc = false sanctioned; checklists unchanged · Clippy in the python job; also lift doc = false and add a bindings rustdoc step |
| 7 | CD-112 | defender:over-engineering-00; attacker:over-engineering #3 | CD-112 — is a first .github/actions/ directory acceptable for the MPICH Cache/Build/Set triple (eight intra-ci.yml copies plus mpi-slurm.yml and release-mpi.yml), or should a checked-in setup script invoked by one step per job carry it? GitHub Actions cannot… | Composite action under a new .github/actions/ · Checked-in setup script invoked by one step per job · Keep the eight copies; add an in-file rationale |
| 8 | CD-114 | defender:over-engineering-02; attacker:over-engineering #4 | CD-114 — must the guard over the hoisted crate-source directory list also cover quality-report.sh:53-64, the sixth copy, given comment_scan.sh's holdout array at :46-50 already tracks that script while its header at :11-14 names only two holdouts? (The attack… | Cover quality-report.sh too; fix the comment_scan.sh header when hoisting · Cover the five sourcing gates only · Defer the sixth copy — trigger: quality-report.sh sources comment_scan.sh |
| 9 | OD-049 | defender:over-engineering-03; defender:over-engineering-04; attacker:over-engineering #2 | OD-049 / OD-050 — the three unconsumed slow-tests = [] declarations (cobre-cli:57, cobre-io:19, cobre-stochastic:18): delete them, or keep the convention deliberately and then either restate the :55-56 rationale without the workspace-wide claim or extend the… | Delete the three inert declarations · Keep as a declared convention; restate the rationale and extend to cobre-core, cobre-solver, cobre-comm · Keep as-is; restate the :55-56 rationale without the workspace-wide claim |
| 10 | CD-115 | defender:over-engineering-05; attacker:over-engineering #1 | CD-115 — are the five reserved crate names (cobre-mcp, cobre-tui, cobre-flow, cobre-uc, cobre-emt) meant to be re-published at every release so they keep holding their place on crates.io (their manifests inherit the workspace version and set no publish key),… | publish = false in the five reserved manifests; publish.yml asserts every member is published or publish = false · Re-publish the five stubs every release; add them to the list · Defer — trigger: the first stub gains a real implementation |
| 11 | CD-105 | defender:drift-02 | CD-105 — must a design doc's own status line use one of the five index vocabulary values, or only be present? At the pin enumerated-traversal-distribution.md:3 and backward-warm-start-channels.md:10 carry descriptive status prose instead of a token, and docs/… | A vocabulary token, optionally followed by a qualifier · Presence only; descriptive prose allowed |
| 12 | — | attacker:drift #1; defender:drift-00 (dismissed) | docs/design/README.md:26-27 — the two docs statused 'Implemented (retained pending fold into the live spec)': fold both into their authoritative homes and delete them as README.md:31 prescribes, or give the status vocabulary a sixth value for the retained-pen… | Fold both docs into their homes and delete them per README.md:31; no sixth value · Add a sixth vocabulary value for retained-pending-fold · Defer — trigger: the live spec absorbs both docs |
| 13 | CD-108 | defender:drift-06 (2 items) | CD-108 — the private spec-§ citation family (local_conformance.rs:4 and the eight banners; ferrompi.rs:241/:272/:279 citing backend docs that exist nowhere): re-anchor to in-tree targets or publish the backend docs? And should check-comment-refs.sh's scan set… | Re-anchor to in-tree targets; align check-comment-refs.sh's scan set · Publish the backend-*.md docs into docs/design and keep the citations · Re-anchor only; leave the scan set |
| 14 | CD-109 | defender:drift-07; attacker:drift #2 | CD-109 — are `.claude/rules/*.md` section numbers a citable coordinate at all? Five gate headers cite them and four do not resolve; no gate scans shell-script headers. The alternative is directive ids and heading titles, as check-allow-rationale.sh:5 already… | Not citable: re-point the five citations to directive ids / heading titles and add the section-suffix check to check-comment-refs.sh's .claude/rules token class · Re-point the five citations only; no new check · Keep section numbers citable; add stable anchors to the rule files |
| 15 | CD-110 | defender:drift-08 | CD-110 — the Part-I source plans/generalizing/beyond-sddp-generalization.md is untracked at the pin, so no in-repo guard can see its copy of the exemption parenthetical: is a correction expected in the untracked source as well as in the tracked mirror at targ… | Yes: E9 amends the roadmap row and the tracked mirror together · Mirror only; the untracked roadmap is out of scope |
| 16 | performance-00 | defender:performance-00; attacker:performance #1 | Informational — the MPICH prelude: keep it byte-identical on every ci.yml job for readability even where the job's feature string excludes mpi (schemas, python), or derive it per job from that feature string? No PD id either way. | Derive per job; drop the triple from schemas and python · Keep the prelude uniform for readability |
| 17 | performance-02 | defender:performance-02; attacker:performance #2 | Informational — a populator job with needs: edges for the shared MPICH cache key would collapse the rare cold-key fan-out at the cost of a serialization point on every warm run. Wanted? | No populator; keep parallel restores · Add a populator job with needs: edges |
| 18 | performance-03 | defender:performance-03; attacker:performance #3 | Informational — the release cobre binary is built independently by three jobs: give it one producer plus a publish/consume edge (the first needs: edge; one broken build would block the consumers), or keep per-job builds so each gate surfaces its own failure? | Keep per-job builds; no producer/consumer edge · One producer job publishes the binary as an artifact |
| 19 | performance-05 | defender:performance-05 (2 items); attacker:performance #4 | Informational — cargo-audit (ci.yml:413) and cargo-llvm-cov (coverage job) are installed from source while the workflow already uses a prebuilt-binary install action; whether a prebuilt cargo-about exists for the version pinned at :443, and whether rust-cache… | Move cargo-audit and cargo-llvm-cov to the install action; cargo-about joins only if a prebuilt of the pinned version exists — verify at amend time · Leave all three cargo install sites |
| 20 | performance-06 | defender:performance-06 | Informational — actions/checkout exposes no per-submodule input, so a job-tailored submodule set means hand-listing submodule paths in a following step on each affected job, a second copy of the four-entry .gitmodules registry with no guard. Acceptable? | Keep the single recursive checkout · Tailor the submodule set per job |

## 2. Round plan

Assembled before any question is asked. Options are capped at four per question; the recommended option is listed first; per-entry exceptions inside a batch are named through the free-text option. Round 1 (the two-site entry) precedes everything; round 2 (PATTERN) follows it immediately; the census, over-engineering and drift batches follow in that order; no conflicts hold exists (an existing one would be its own question, never batched); every needs-human item gets its own question; the items in `noRoundFor` get no question and the omission is deliberate.

| Round | Kind | Subject | Options |
| -- | -- | -- | -- |
| R1-two-site | two-site-first | CD-099: The infra-genericity gate's crate list is a hand-kept literal in two unpaired places, so the rule's coverage cannot follow the target layering without a synchr… — two-site edit CLAUDE.md:39-41 + SCAN_DIRS :62-68 + ci.yml:261-262; Alignment advances-1 (Part IV.1) | accept (both sites stand) · amend scope (name the crates/dirs) · downgrade (reviewer's rating preserved) · defer (trigger: the cobre-model carve at Phase 0b) |
| R2-vocabulary | single | PATTERN (check-infra-genericity.sh:79) matches SDDP nouns rather than engine concepts — architecture-01 dismissed deliberate-and-documented at scripts/ci/check-infra-genericity.sh:15 | keep dismissed (citation stands) · record entry (own id; widen PATTERN in place) · fold into CD-099 (second clause, no id) · defer (trigger: cobre-direct first compiles) |
| R3-census | census-batch | architecture (CD-100, CD-101, CD-102, CD-103) + the check-comment-bloat.sh census row (transitively-advisory, no id) | accept all as recorded · downgrade (name) · reject (name) · defer (name + trigger) |
| R4-over-eng | over-engineering-batch | over-engineering (CD-112, CD-113, CD-114, CD-115, OD-049, OD-050) — MPICH 8x + 2 (CD-112), version literal (CD-113), five SCAN_DIRS copies (CD-114), publish list vs stubs (CD-115), no-op slow-tests OD-049 / OD-050 | accept all as recorded · downgrade (name) · reject (name) · defer (name + trigger) |
| R5-drift | drift-batch | drift (CD-111, CD-104, CD-105, CD-106, CD-107, CD-108, CD-109, CD-110) — each names the contradicting tree location | accept all as recorded · downgrade (name) · reject (name) · defer (name + trigger) |
| R6-nh-1 | needs-human | #1 CD-099 / defender:architecture-00 | Gate owns the list; CLAUDE.md enumeration pinned by a divergence check · CLAUDE.md becomes a shape-only pointer · Defer to E9 · other |
| R6-nh-2 | needs-human | #2 — / defender:architecture-01 (dismissed); attacker:architecture #5 | Keep standalone-token scope; no FreezeScratch addition · Widen to underscore boundaries with owner sign-off · Keep scope; add the FreezeScratch site to CD-061's I.3-6 widening · other |
| R6-nh-3 | needs-human | #3 CD-100 / defender:architecture-02; attacker:architecture #1 | Extend pull_request to [main, develop] · Ratify post-merge detection; document the trade in ci.yml · Defer — trigger: the first develop merge that ships a gate regression · other |
| R6-nh-4 | needs-human | #4 CD-101 / defender:architecture-03 (2 items); attacker:architecture #2 | cfg_attr slow-tests form; fix the two run lines; no compile-only step · Restore the nightly schedule at :5-7 · Both: cfg_attr form and the nightly schedule · other |
| R6-nh-5 | needs-human | #5 CD-102 / defender:architecture-04 | One-way canonical-subset inclusion with an explicit non-input allowlist · Set equality · Defer — trigger: the next template added to examples/ · other |
| R6-nh-6 | needs-human | #6 CD-103 / defender:architecture-05 (3 items); attacker:architecture #3; attacker:architecture #4; attacker:drift #3 | Clippy beside the python job's test step; doc = false stays sanctioned; add the command to CONTRIBUTING · Clippy in a no-interpreter job; doc = false sanctioned; checklists unchanged · Clippy in the python job; also lift doc = false and add a bindings rustdoc step · other |
| R6-nh-7 | needs-human | #7 CD-112 / defender:over-engineering-00; attacker:over-engineering #3 | Composite action under a new .github/actions/ · Checked-in setup script invoked by one step per job · Keep the eight copies; add an in-file rationale · other |
| R6-nh-8 | needs-human | #8 CD-114 / defender:over-engineering-02; attacker:over-engineering #4 | Cover quality-report.sh too; fix the comment_scan.sh header when hoisting · Cover the five sourcing gates only · Defer the sixth copy — trigger: quality-report.sh sources comment_scan.sh · other |
| R6-nh-9 | needs-human | #9 OD-049 / defender:over-engineering-03; defender:over-engineering-04; attacker:over-engineering #2 | Delete the three inert declarations · Keep as a declared convention; restate the rationale and extend to cobre-core, cobre-solver, cobre-comm · Keep as-is; restate the :55-56 rationale without the workspace-wide claim · other |
| R6-nh-10 | needs-human | #10 CD-115 / defender:over-engineering-05; attacker:over-engineering #1 | publish = false in the five reserved manifests; publish.yml asserts every member is published or publish = false · Re-publish the five stubs every release; add them to the list · Defer — trigger: the first stub gains a real implementation · other |
| R6-nh-11 | needs-human | #11 CD-105 / defender:drift-02 | A vocabulary token, optionally followed by a qualifier · Presence only; descriptive prose allowed · other |
| R6-nh-12 | needs-human | #12 — / attacker:drift #1; defender:drift-00 (dismissed) | Fold both docs into their homes and delete them per README.md:31; no sixth value · Add a sixth vocabulary value for retained-pending-fold · Defer — trigger: the live spec absorbs both docs · other |
| R6-nh-13 | needs-human | #13 CD-108 / defender:drift-06 (2 items) | Re-anchor to in-tree targets; align check-comment-refs.sh's scan set · Publish the backend-*.md docs into docs/design and keep the citations · Re-anchor only; leave the scan set · other |
| R6-nh-14 | needs-human | #14 CD-109 / defender:drift-07; attacker:drift #2 | Not citable: re-point the five citations to directive ids / heading titles and add the section-suffix check to check-comment-refs.sh's .claude/rules token class · Re-point the five citations only; no new check · Keep section numbers citable; add stable anchors to the rule files · other |
| R6-nh-15 | needs-human | #15 CD-110 / defender:drift-08 | Yes: E9 amends the roadmap row and the tracked mirror together · Mirror only; the untracked roadmap is out of scope · other |
| R6-nh-16 | needs-human | #16 performance-00 / defender:performance-00; attacker:performance #1 | Derive per job; drop the triple from schemas and python · Keep the prelude uniform for readability · other |
| R6-nh-17 | needs-human | #17 performance-02 / defender:performance-02; attacker:performance #2 | No populator; keep parallel restores · Add a populator job with needs: edges · other |
| R6-nh-18 | needs-human | #18 performance-03 / defender:performance-03; attacker:performance #3 | Keep per-job builds; no producer/consumer edge · One producer job publishes the binary as an artifact · other |
| R6-nh-19 | needs-human | #19 performance-05 / defender:performance-05 (2 items); attacker:performance #4 | Move cargo-audit and cargo-llvm-cov to the install action; cargo-about joins only if a prebuilt of the pinned version exists — verify at amend time · Leave all three cargo install sites · other |
| R6-nh-20 | needs-human | #20 performance-06 / defender:performance-06 | Keep the single recursive checkout · Tailor the submodule set per job · other |
| R7-handoffs | handoff-confirm | E9: CD-099 (advances-1) + CD-110 (I.3-6 evidence, disposition epic 9) · E10: nothing · E08: td-queue empty-with-reason · E11: policy.fbs path drift, SN-06, README fold (NH #12), stochastic slow-tests twin (NH #9) | confirm · amend (name) |

**noRoundFor** (deliberate omissions, recorded so they read as decided-not-to-decide):

- partI-handoff.json I.3-6: EXCLUDED_FILES empty at scripts/ci/check-infra-genericity.sh:74 with the header's retirement rationale (:38-44, :70-73) — presented read-only, disposition: epic 9
- cleared[reason=sanctioned]: check-comment-line-refs.sh, check-comment-banners.sh, check-comment-bloat.sh and quality-report.sh exit 0 by design (advisory-by-design headers :35 / :40 / :39 / :42; ADVISORY step comments ci.yml:278-279, :282-283, :298-299)
- cleared[reason=sanctioned]: the five reserved stub members cobre-{mcp,tui,flow,uc,emt} — CLAUDE.md:10, ARCHITECTURE.md:108-125, roadmap IV.1; only their release-path hand-mirror is a finding (CD-115)
- cleared[reason=sanctioned]: the entry-free scripts/ci/allow-rationale-allowlist.txt (:1 intentionally EMPTY, :15 KEEP THIS FILE EMPTY) and the cobre-python workspace exclusion (Cargo.toml:18-23)
- conflicts holds: none at this station (calibration.json pullDontPush.conflictsHeld = 0; the MPICH block has ten consumers and is exempt) — the hold round is empty and the marker guard still refuses any 'conflicts (held)' row
- performance lens: CI wall time is informational only — the five UNMEASURED rows carry no PD id and no E10 queue line is written; their owner directions are needs-human items, not severity questions
- DEVIATION (ticket premise stale at the defender pass): the slow-tests declarations are confirmed entries OD-049 / OD-050, not sanctioned — asked in the over-engineering batch and NH #9; the two docs/design/README.md rows (drift-00) were dismissed premise-false-at-pin — the direction is NH #12, no drift entry

**Invariants:** round 1 precedes round 2 precedes every batch · a two-site answer naming one site is re-asked · no conflicts hold inside a batched question (none exists) · batched questions carry at most four options · a defer needs a concrete trigger · a reject moves the entry to the section's Cleared list as `↩︎ CLEARED` · a downgrade keeps the reviewer's rating in the row · no PD id and no E10 line for CI wall time · an unanswered needs-human item blocks the marker.

## 3. Decision record

**Decision: ratified** — 2026-09-19, main session, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`. Presented 21 (19 calibrated entries + the PATTERN dismissal + the check-comment-bloat.sh census row) over 8 AskUserQuestion calls: round 1 (the two-site entry), round 2 (PATTERN), the census, over-engineering and drift batches, 20 needs-human questions and the handoff confirmation. Every answer was the recommended option unless the Rationale column says otherwise. No conflicts hold existed.

### 3.1 The two-site entry (round 1)

Owner: Accept — Both sites stand as recorded; the fix-shape amends the existing script and rule together; Alignment advances-1 citing Part IV.1 stays provisional for E9.

| ID | Decision | Severity | Wiring | Alignment | Rationale (owner) | Sites |
| -- | -- | -- | -- | -- | -- | -- |
| CD-099 | accept | B | blocking | advances-1 | as recorded (R1-two-site) | sites: CLAUDE.md:39 + scripts/ci/check-infra-genericity.sh:62 + .github/workflows/ci.yml:261 |

### 3.2 The PATTERN vocabulary limitation (round 2)

- **architecture-01** (re-raise-of CD-061) — **keep-dismissed** — Keep dismissed — The citation stands (check-infra-genericity.sh:15 declares the standalone word-boundary scope); the candidate stays in the section's Cleared list with the defender's basis; the owner questions it still carries (token scope, FreezeScratch site) are answered in NH #2. Basis `deliberate-and-documented`, citation `scripts/ci/check-infra-genericity.sh:15`; stays in the section's Cleared list. Directions: -.

### 3.3 Census, over-engineering and drift entries (rounds 3-5)

Census row **check-comment-bloat.sh** — **ratify-classification** (R3-census): gate-census.json class `unwired`, rendered `transitively-advisory`, invoked by `scripts/ci/quality-report.sh:132`; no id. The four entries enter the actionable set at the house severity and wiring class (the three A→B downgrades keep the reviewer's A in the row); the check-comment-bloat.sh classification is ratified as transitively-advisory with its invoker recorded.

**Batches (rounds 3-5).** R3-census — Accept all as recorded: The four entries enter the actionable set at the house severity and wiring class (the three A→B downgrades keep the reviewer's A in the row); the check-comment-bloat.sh classification is ratified as transitively-advisory with its invoker recorded. R4-over-eng — Accept all as recorded: All six enter the actionable set at their house severities; the MPICH duplication is many-consumer and exempt from the one-consumer objection; the fix direction for the slow-tests declarations and the stubs' publish policy are decided in NH #9 and #10. R5-drift — Accept all as recorded: The eight entries enter the actionable set at their house severities (the two B→C downgrades keep the reviewer's B); CD-110 stays evidence-only with no disposition; CD-111's three sites stand.

| ID | Decision | Severity | Wiring | Alignment | Rationale (owner) | Trigger / override / sites | Directions |
| -- | -- | -- | -- | -- | -- | -- | -- |
| CD-100 | accept | B (reviewer: A) | blocking | neutral | as recorded (R3-census) | - | R6-nh-3: Extend pull_request to [main, develop] |
| CD-101 | accept | B (reviewer: A) | blocking | neutral | as recorded (R3-census) | - | R6-nh-4: cfg_attr slow-tests form; fix the two run lines; no compile-only step |
| CD-102 | accept | B | blocking | neutral | as recorded (R3-census) | - | R6-nh-5: One-way canonical-subset inclusion with an explicit non-input allowlist |
| CD-103 | accept | B (reviewer: A) | blocking | neutral | as recorded (R3-census) | - | R6-nh-6: Clippy beside the python job's test step; doc = false stays sanctioned; add the command to CONTRIBUTING |
| CD-112 | accept | B | blocking | neutral | as recorded (R4-over-eng) | - | R6-nh-7: Composite action under a new .github/actions/ |
| CD-113 | accept | B | n/a | neutral | as recorded (R4-over-eng) | - | - |
| CD-114 | accept | B | blocking | neutral | as recorded (R4-over-eng) | - | R6-nh-8: Cover quality-report.sh too; fix the comment_scan.sh header when hoisting |
| CD-115 | accept | B | blocking | neutral | as recorded (R4-over-eng) | - | R6-nh-10: publish = false in the five reserved manifests; publish.yml asserts every member is published or publish = false |
| OD-049 | accept | C | n/a | neutral | as recorded (R4-over-eng) | - | R6-nh-9: Delete the three inert declarations |
| OD-050 | accept | C | n/a | neutral | as recorded (R4-over-eng) | - | R6-nh-9: Delete the three inert declarations |
| CD-111 | accept | B | n/a | neutral | as recorded (R5-drift) | sites: CLAUDE.md:19 + Cargo.toml:36 + .github/workflows/ci.yml:154 | - |
| CD-104 | accept | C | n/a | neutral | as recorded (R5-drift) | - | - |
| CD-105 | accept | C | n/a | neutral | as recorded (R5-drift) | - | R6-nh-11: A vocabulary token, optionally followed by a qualifier |
| CD-106 | accept | C | n/a | neutral | as recorded (R5-drift) | - | - |
| CD-107 | accept | C | blocking | neutral | as recorded (R5-drift) | - | - |
| CD-108 | accept | C (reviewer: B) | blocking | neutral | as recorded (R5-drift) | - | R6-nh-13: Re-anchor to in-tree targets; align check-comment-refs.sh's scan set |
| CD-109 | accept | C (reviewer: B) | blocking | neutral | as recorded (R5-drift) | - | R6-nh-14: Not citable: re-point the five citations to directive ids / heading titles and add the section-suffix check to check-comment-refs.sh's .claude/rules token class |
| CD-110 | accept | C | blocking | neutral | as recorded (R5-drift) | - | R6-nh-15: Yes: E9 amends the roadmap row and the tracked mirror together |

### 3.4 Holds — none

No `conflicts` row was calibrated at this station, so nothing was asked alone and nothing is held or overridden (overridden 0, held 0).

### 3.5 Worker needs-human answers (round 6, 20/20)

| # | Entry | From | Question | Answer | Recorded as |
| -- | -- | -- | -- | -- | -- |
| 1 | CD-099 | defender:architecture-00 | CD-099 — where does the single owner of the infra-crate set sit? CLAUDE.md:39-41 keeps its enumeration pinned by a new divergence check in the gate, or the bullet becomes a shape-only pointer to the… | **Gate owns the list; CLAUDE.md enumeration pinned by a divergence check** | SCAN_DIRS in check-infra-genericity.sh is the owner and the gate verifies CLAUDE.md:39-41 names exactly the same crates, so an amendment edits the script first and the prose fails loudly until it follows (guard-pinned literal per doc-integrity §2). |
| 2 | — | defender:architecture-01 (dismissed); attacker:architecture #5 | PATTERN scope (architecture-01, dismissed): should the gate's token scope ever widen past standalone word-boundary tokens, given an underscore boundary would red this blocking gate on the cobre-io ti… | **Keep standalone-token scope; no FreezeScratch addition** | The dismissal stands (check-infra-genericity.sh:15 declares the word-boundary scope); CD-061's widening is an I.3-6 disposition matter for E9, not a gate change; the FreezeScratch site is outside CD-061's cobre-io scope and is not added. |
| 3 | CD-100 | defender:architecture-02; attacker:architecture #1 | CD-100 — the ci.yml trigger list: extend the pull_request filter to [main, develop], accepting a full ci.yml run on every develop pull request, or ratify post-merge detection on develop as the intend… | **Extend pull_request to [main, develop]** | Every develop pull request runs ci.yml before merge; the filter becomes symmetric with the push trigger at ci.yml:4-5; one edit site. |
| 4 | CD-101 | defender:architecture-03 (2 items); attacker:architecture #2 | CD-101 — shuffle-matrix reachability: restore the commented-out nightly schedule at invariance-shuffle.yml:5-7, or convert the bare-ignore shuffle tests to the crate's slow-tests cfg_attr form so the… | **cfg_attr slow-tests form; fix the two run lines; no compile-only step** | The shuffle tests join the slow-tests suite (CLAUDE.md's slow-tests rule) so the automatic run covers them; invariance-shuffle.yml's two run lines gain test-support and become a convenience; ci.yml:114/:233 already compile the target. |
| 5 | CD-102 | defender:architecture-04 | CD-102 — is examples/1dtoy/ contractually EQUAL to what cobre init materializes, or a superset allowed to carry non-input files as examples/4ree/README.md does? The answer decides whether the missing… | **One-way canonical-subset inclusion with an explicit non-input allowlist** | Every input file under examples/1dtoy/ must be embedded (canonical ⊆ embedded); README-like non-input files are allowlisted by name inside the existing gate; adding an input file without embedding it fails. |
| 6 | CD-103 | defender:architecture-05 (3 items); attacker:architecture #3; attacker:architecture #4; attacker:drift #3 | CD-103 — the bindings crate's lint coverage: where does the manifest-scoped clippy pass live (beside the existing manifest-scoped test step in the python job, inheriting interpreter and backend on th… | **Clippy beside the python job's test step; doc = false stays sanctioned; add the command to CONTRIBUTING** | One step on the 3.12 matrix entry inheriting interpreter and backend; the rustdoc bar does not apply (readers are Python users, doc = false is the seam); CONTRIBUTING.md:386 and :602-604 gain the manifest-scoped clippy command so the stated obligation and CI agree. |
| 7 | CD-112 | defender:over-engineering-00; attacker:over-engineering #3 | CD-112 — is a first .github/actions/ directory acceptable for the MPICH Cache/Build/Set triple (eight intra-ci.yml copies plus mpi-slurm.yml and release-mpi.yml), or should a checked-in setup script… | **Composite action under a new .github/actions/** | The ten sites (8 ci.yml + mpi-slurm.yml + release-mpi.yml) consume one action; the matrix guard becomes an input; the one-consumer objection is waived (10 consumers); the release-critical MPI workflows move with ci.yml in one change. |
| 8 | CD-114 | defender:over-engineering-02; attacker:over-engineering #4 | CD-114 — must the guard over the hoisted crate-source directory list also cover quality-report.sh:53-64, the sixth copy, given comment_scan.sh's holdout array at :46-50 already tracks that script whi… | **Cover quality-report.sh too; fix the comment_scan.sh header when hoisting** | The hoisted list has six consumers; the header :11-14 is corrected to name all three holdouts so it matches the array :46-50; no new id for the header mismatch — it rides CD-114. |
| 9 | OD-049 | defender:over-engineering-03; defender:over-engineering-04; attacker:over-engineering #2 | OD-049 / OD-050 — the three unconsumed slow-tests = [] declarations (cobre-cli:57, cobre-io:19, cobre-stochastic:18): delete them, or keep the convention deliberately and then either restate the :55-… | **Delete the three inert declarations** | No cfg consumer, no forwarding; the workspace command keeps resolving through cobre-sddp; ci.yml:21's feature census comment and cobre-stochastic/README.md:81 are corrected in the same change (the stochastic instance is routed to E11). |
| 10 | CD-115 | defender:over-engineering-05; attacker:over-engineering #1 | CD-115 — are the five reserved crate names (cobre-mcp, cobre-tui, cobre-flow, cobre-uc, cobre-emt) meant to be re-published at every release so they keep holding their place on crates.io (their manif… | **publish = false in the five reserved manifests; publish.yml asserts every member is published or publish = false** | The names were reserved once and need no re-publish per release; the decision sits beside each crate and the workflow derives its list instead of restating it. |
| 11 | CD-105 | defender:drift-02 | CD-105 — must a design doc's own status line use one of the five index vocabulary values, or only be present? At the pin enumerated-traversal-distribution.md:3 and backward-warm-start-channels.md:10… | **A vocabulary token, optionally followed by a qualifier** | The two descriptive-prose docs get a token; README.md:4 is tightened to say the status uses the vocabulary; CD-105's dismissed half becomes part of the fix. |
| 12 | — | attacker:drift #1; defender:drift-00 (dismissed) | docs/design/README.md:26-27 — the two docs statused 'Implemented (retained pending fold into the live spec)': fold both into their authoritative homes and delete them as README.md:31 prescribes, or g… | **Fold both docs into their homes and delete them per README.md:31; no sixth value** | The retained-pending-fold status is transitional; E11 carries the fold and the two rows disappear with the docs. |
| 13 | CD-108 | defender:drift-06 (2 items) | CD-108 — the private spec-§ citation family (local_conformance.rs:4 and the eight banners; ferrompi.rs:241/:272/:279 citing backend docs that exist nowhere): re-anchor to in-tree targets or publish t… | **Re-anchor to in-tree targets; align check-comment-refs.sh's scan set** | The citations name the surfaces under test or testing-architecture.md; the private backend docs are not published; the scan set gains crates/*/tests and crates/*/benches (rides CD-114's hoisted list). |
| 14 | CD-109 | defender:drift-07; attacker:drift #2 | CD-109 — are `.claude/rules/*.md` section numbers a citable coordinate at all? Five gate headers cite them and four do not resolve; no gate scans shell-script headers. The alternative is directive id… | **Not citable: re-point the five citations to directive ids / heading titles and add the section-suffix check to check-comment-refs.sh's .claude/rules token class** | The existing gate already parses the .claude/rules/ token class at :75; a suffix check makes the class un-citable by number; the five headers are re-pointed in the same change. |
| 15 | CD-110 | defender:drift-08 | CD-110 — the Part-I source plans/generalizing/beyond-sddp-generalization.md is untracked at the pin, so no in-repo guard can see its copy of the exemption parenthetical: is a correction expected in t… | **Yes: E9 amends the roadmap row and the tracked mirror together** | This station records the evidence only; the disposition and the Alignment stay epic 9's; both copies move in E9's amendment. |
| 16 | performance-00 | defender:performance-00; attacker:performance #1 | Informational — the MPICH prelude: keep it byte-identical on every ci.yml job for readability even where the job's feature string excludes mpi (schemas, python), or derive it per job from that featur… | **Derive per job; drop the triple from schemas and python** | Direction for whoever amends ci.yml (rides CD-112's composite action); informational, no PD id, no E10 line. |
| 17 | performance-02 | defender:performance-02; attacker:performance #2 | Informational — a populator job with needs: edges for the shared MPICH cache key would collapse the rare cold-key fan-out at the cost of a serialization point on every warm run. Wanted? | **No populator; keep parallel restores** | Every consumer restores in parallel and nothing waits; the cold-key fan-out is rare; informational. |
| 18 | performance-03 | defender:performance-03; attacker:performance #3 | Informational — the release cobre binary is built independently by three jobs: give it one producer plus a publish/consume edge (the first needs: edge; one broken build would block the consumers), or… | **Keep per-job builds; no producer/consumer edge** | Each gate stays independent and surfaces its own failure; informational. |
| 19 | performance-05 | defender:performance-05 (2 items); attacker:performance #4 | Informational — cargo-audit (ci.yml:413) and cargo-llvm-cov (coverage job) are installed from source while the workflow already uses a prebuilt-binary install action; whether a prebuilt cargo-about e… | **Move cargo-audit and cargo-llvm-cov to the install action; cargo-about joins only if a prebuilt of the pinned version exists — verify at amend time** | Both external facts are checked when the amend lands; informational, no PD id. |
| 20 | performance-06 | defender:performance-06 | Informational — actions/checkout exposes no per-submodule input, so a job-tailored submodule set means hand-listing submodule paths in a following step on each affected job, a second copy of the four… | **Keep the single recursive checkout** | A hand-listed submodule set duplicates .gitmodules with no guard; the pointer stays inert; informational. |

### 3.6 Presented, no decision taken

- Part-I item I.3-6 — evidence `scripts/ci/check-infra-genericity.sh:74`, `scripts/ci/check-infra-genericity.sh:70`, `scripts/ci/check-infra-genericity.sh:40`, station verdict `claim-stale`; disposition: epic 9 (no Alignment value set here; the handoff's two-site block is synced from CD-099).
- sanctioned: advisory-by-design exit 0 of check-comment-line-refs.sh, check-comment-banners.sh, check-comment-bloat.sh and quality-report.sh — no question offered.
- sanctioned: the entry-free scripts/ci/allow-rationale-allowlist.txt — no question offered.
- sanctioned: the five reserved stub crates cobre-mcp, cobre-tui, cobre-flow, cobre-uc, cobre-emt (Cargo.toml:12-16) — no question offered.
- sanctioned: the cobre-python workspace exclusion — no question offered.
- sanctioned: EXCLUDED_FILES=() in the genericity gate — no question offered.
- CI wall time — no PD id, no E10 queue line; the five UNMEASURED rows stay under the Performance (informational) block.
- the three remaining defender dismissals (drift-00, drift-05, performance-04) and the performance-01 dup-of fold — Cleared with basis and citation, no question offered.

### 3.7 Informational (no severity, no id)

- R6-nh-2: Keep standalone-token scope; no FreezeScratch addition — The dismissal stands (check-infra-genericity.sh:15 declares the word-boundary scope); CD-061's widening is an I.3-6 disposition matter for E9, not a gate change; the FreezeScratch site is outside CD-061's cobre-io scope and is not added.
- R6-nh-12: Fold both docs into their homes and delete them per README.md:31; no sixth value — The retained-pending-fold status is transitional; E11 carries the fold and the two rows disappear with the docs.
- R6-nh-16: Derive per job; drop the triple from schemas and python — Direction for whoever amends ci.yml (rides CD-112's composite action); informational, no PD id, no E10 line.
- R6-nh-17: No populator; keep parallel restores — Every consumer restores in parallel and nothing waits; the cold-key fan-out is rare; informational.
- R6-nh-18: Keep per-job builds; no producer/consumer edge — Each gate stays independent and surfaces its own failure; informational.
- R6-nh-19: Move cargo-audit and cargo-llvm-cov to the install action; cargo-about joins only if a prebuilt of the pinned version exists — verify at amend time — Both external facts are checked when the amend lands; informational, no PD id.
- R6-nh-20: Keep the single recursive checkout — A hand-listed submodule set duplicates .gitmodules with no guard; the pointer stays inert; informational.

## 4. Handoffs after the gate

- **E9** — CD-099 advances-1 (Part IV.1) with the ratified two-site edit; CD-110 I.3-6 evidence only, disposition epic 9; rows: CD-099, CD-110; directions: R6-nh-1, R6-nh-15.
- **E10** — nothing — informational lens; owner directions on the five UNMEASURED rows are recorded on the rows.
- **E08** — td-queue.json empty-with-reason (epic 8).
- **E11** — policy.fbs path drift; SN-06; README fold (NH #12); cobre-stochastic slow-tests twin (NH #9); the doc/status corrections directed above; directions: R6-nh-9, R6-nh-12.
- **Handoff confirmation** — confirm (R7-handoffs): All four handoffs recorded as listed in gate.md §4 and decisions.json; the RETURNED marker is written and the test-corpus station is unblocked.

**Gate: RETURNED 2026-09-19** — baseline `077dbe2c`; accepted 19, amended 0, downgraded 0, rejected 0, deferred 0, overridden 0, held 0; 1 dismissal ratified (PATTERN, architecture-01), 1 census classification ratified (check-comment-bloat.sh transitively-advisory); 20 needs-human answered; Part-I I.3-6 presented read-only (disposition: epic 9); CI wall time informational (no PD id, no E10 line). Test-corpus station unblocked.
