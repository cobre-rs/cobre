{
  "station": "sddp",
  "baseline": "077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c",
  "calibratedAt": "2026-09-18",
  "idFloorsSeen": {
    "CD": 82,
    "PD": 35,
    "OD": 36,
    "TD": 41
  },
  "order": "(subStation, lens, candidateRef)",
  "sectionTitle": "STATION 5 — cobre-sddp (2026-09)",
  "assigned": [
    {
      "candidateRef": "5a-architecture-02",
      "id": "CD-082",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a caller-patched placeholder standing in for a constructor input, so the two validate paths build templates against an empty parameter table and validate's exit-0 contract is broken for three error variants; a real smell confined to the validate paths and the placeholder protocol — Sev B, CD-004-adjacent (the fix rides the single admission-gate carrier)",
      "category": "bad-abstraction",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V §V.1 Phase 0 (the study-block admission-gate carrier) / Part IV §IV.4",
      "defenderAlignmentHint": "advances-0a",
      "partIRef": "I.3-7",
      "priorId": null,
      "relatedTo": [
        "CD-004",
        "CD-024-successor"
      ],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Whether the fail-loud leg changes the public surface of ResolvedParameters::get (a fallible signature or a panic) or is instead enforced as a construction-time check at the LP-build site, leaving get infallible: get is pub and must_use, so the choice is an owner call on public API shape, not a correctness question."
      ],
      "survivingClaim": "Narrowed to a missed error class, not a corrupted report: because StudySetup::new and new_with_boundary_requirements take no artifact argument, they cannot satisfy the patch protocol StudyParams::scalar_parameters' own rustdoc mandates, so the two validate paths run build_resolved_parameters on an empty table whenever config.policy.boundary is configured, and validate's documented exit-0 contract is therefore broken for exactly the three ResolvedParametersError variants cobre_io::validation::scalar_parameters does not duplicate (MissingSeason, PerStageBlockCoverage, MissingSpecificProductivity), with is_block_varying returning false on a miss collapsing a PerStageBlock row set silently and with no assert. Conceded from the candidate: no boundary-reconciliation fact validate reports reads ResolvedParameters, so nothing validate prints is wrong; the setup is built and discarded; and the validate.rs:234 mirrors-the-run-path comment concerns boundary-requirement ordering, not the parameter table, so it is not falsified.",
      "title": "scalar_parameters is a caller-patched placeholder rather than a constructor input, so both validate paths build stage templates against an empty parameter table and silently resolve every parameterized coefficient to 0.0",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/params.rs",
          "symbol": "StudyParams"
        },
        {
          "path": "crates/cobre-sddp/src/setup/mod.rs",
          "symbol": "new_with_boundary_requirements"
        },
        {
          "path": "crates/cobre-sddp/src/policy/resolved_parameters.rs",
          "symbol": "ResolvedParameters"
        }
      ],
      "fixShape": "Make the table a constructor input instead of a post-construction patch, so the empty case is unrepresentable rather than merely undesirable. Either take the loaded Vec<ScalarParameter> as a parameter of new / new_with_boundary_requirements and drop the field's placeholder role in from_config, or split the type so a StudySetup can only be built from a params value whose scalar table has been supplied (an unpatched value is not accepted by from_broadcast_params). Independently of which, ResolvedParameters::get must stop returning 0.0 on a miss in release: a miss is a build-time defect, not a datum, so it belongs as a construction-time error at the LP-build site rather than a silent sentinel behind a debug_assert -- this is the fail-loud shape already on the open follow-up list. Fixing get alone is not sufficient and fixing the constructors alone is not sufficient: the first without the second turns a silent-wrong validate into an abort, the second without the first leaves the sentinel live for any future caller. Also correct the claim at validate.rs:234 that the call mirrors the run path, or make it true. Byte-neutrality bar: the run paths already patch the table, so crates/cobre-sddp/tests/parity.rs and tests/common/parity_hash.rs goldens must be unchanged and tests/common/permute.rs rank-invariance must be unchanged; validate output on a deck WITH generic parameters is expected to change, and that change is the point, so it needs a new regression case rather than a golden refresh.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/setup/params.rs | sed -n '174,177p;335,341p'; git show 077dbe2c:crates/cobre-sddp/src/setup/mod.rs | sed -n '342,397p'; git show 077dbe2c:crates/cobre-sddp/src/policy/resolved_parameters.rs | grep -n -A8 'pub fn get'; git grep -n 'new_with_boundary_requirements\\|StudySetup::new(' 077dbe2c -- crates",
        "output": "params.rs field + from_config:\n    /// Loaded `constraints/generic_parameters.json` entries; empty out of\n    /// [`Self::from_config`] (they are loaded from disk artifacts, not `Config`)\n    /// and patched by each setup caller before `from_broadcast_params`.\n    pub scalar_parameters: Vec<ScalarParameter>,\n            // Loaded from disk artifacts, not Config; each setup caller patches it.\n            scalar_parameters: Vec::new(),\n\nsetup/mod.rs new_with_boundary_requirements:\n        let mut params = StudyParams::from_config(config)?;\n        params.boundary = boundary;\n        Self::from_broadcast_params(system, &params, stochastic, hydro_models)\n\nresolved_parameters.rs:\n    pub fn get(&self, id: EntityId, stage_idx: usize, block_idx: usize) -> f64 {\n        let Ok(pos) = self.id_to_slot.binary_search_by_key(&id.0, |(k, _)| *k) else {\n            debug_assert!(false, \"ResolvedParameters miss: unknown id={id:?} ...\");\n            return 0.0;\n        };\n\nnon-test constructor call sites:\n077dbe2c:crates/cobre-cli/src/commands/validate.rs:234:    let setup = StudySetup::new_with_boundary_requirements(\n077dbe2c:crates/cobre-python/src/io.rs:306:        let setup = match StudySetup::new_with_boundary_requirements(\n077dbe2c:crates/cobre-sddp/examples/dhat_baseline.rs:128:        StudySetup::new(&system, &config, stochastic, hydro_models)\n077dbe2c:crates/cobre-sddp/examples/probe_k_disaggregated.rs:130:    let mut setup = StudySetup::new(&system, &config, stochastic, hydro_models)",
        "reading": "The scalar-parameter table is a two-phase init with no type-level enforcement of phase 2. from_config writes Vec::new() and its own comment delegates the patch to 'each setup caller'; new_with_boundary_requirements patches params.boundary and NOTHING else before delegating to from_broadcast_params, and new delegates straight to it. So the only two constructors that take (system, config, stochastic, hydro_models) build the whole StudySetup -- including stage templates -- against an EMPTY table. The consequence is not a panic: ResolvedParameters::get returns 0.0 on a miss behind a debug_assert!, which is compiled out in release, and lp/builder/layout.rs::resolve_affine folds those misses into constants (acc + coef * resolved.get(..)). The two non-test callers of new_with_boundary_requirements are exactly the two validate paths -- cobre-cli validate.rs:234 and cobre-python io.rs:306 -- so in a release build `cobre validate` silently zeroes every @-referenced coefficient and affine bound and still reports valid, while the run paths (cobre-cli run/setup.rs and cobre-python run.rs:996) do patch the table before from_broadcast_params. The comment at validate.rs:234 claims the call is there so validate 'mirrors the run path', which is precisely what the empty table breaks. cobre-io's generic-constraint parser already rejects an unknown @name, so the ids reaching get are valid ids against an empty table -- a class the debug_assert message ('unknown id') does not describe."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-architecture-03",
      "id": "CD-083",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one inert `pub` config field with a false rustdoc contract and no production reader; public-API removal or a reserved-seams entry — the delete-or-register disposition (OD-032 precedent) at cosmetic blast radius",
      "category": "bad-abstraction",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Owner must choose between removing `CutManagementConfig::warm_start_cuts` (a breaking change to a re-exported public type) and giving it a real reader plus a reserved-seams entry; the verdict establishes the field is inert and its doc line false, not which of the two lands."
      ],
      "survivingClaim": "`CutManagementConfig::warm_start_cuts` (config.rs:148) is the struct's only field with no production reader -- its four siblings are read at training/backward_pass_state.rs:164-165 and training/session/mod.rs:262/1042/1215 -- and its rustdoc contract 'contributes to cut-pool capacity' (config.rs:147) is false at the baseline because capacity is derived solely from the per-pool counts consumed by `pool_capacity` (cut/fcf.rs:498) and `CutPool::new_with_warm_start` (cut/pool.rs:893-895). Conceded from the title's framing: no wrong-shape defect is established for this field (that argument belongs to the distinct cobre-io manifest symbol `ProducerBlock::warm_start_cuts`), and the residue does not decide delete-versus-give-it-a-reader, because the field is `pub` on a re-exported type so removal is a breaking API change.",
      "title": "CutManagementConfig::warm_start_cuts is a public field with a capacity contract in its doc, hard-coded to 0 at both production construction sites and read by no production consumer",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/config.rs",
          "symbol": "CutManagementConfig"
        },
        {
          "path": "crates/cobre-sddp/src/setup/orchestration.rs",
          "symbol": "train_inner"
        }
      ],
      "fixShape": "Delete CutManagementConfig::warm_start_cuts and let cut-pool capacity keep its single owner, the per-pool warm_start_count argument of cut/fcf.rs::pool_capacity. This is a breaking change to a pub struct, so it lands with the projection step of the sibling CD-005 candidate rather than alone: the new CutManagement params projection simply omits the field, which removes the two production literals and the struct-literal reset inside train_inner at the same time. The many test literals that set it are mechanical deletions with no assertion depending on the value, except config.rs's own accessibility test, which goes away with the field. If instead the owner wants the field kept as a reserved seam, it needs the opposite treatment: give it a real reader and reshape it per-pool to match warm_start_counts, and record it in docs/design/reserved-seams-and-deferred-debt.md so a later sweep does not re-raise it -- an unread scalar whose doc asserts a capacity contribution it does not make is the one state that should not persist. Byte-neutrality bar: the value is 0 at both production sites today, so crates/cobre-sddp/tests/parity.rs and tests/common/parity_hash.rs goldens and the tests/common/permute.rs rank-invariance harness must be bit-identical, and mpiexec -n 1 against -n 2 must reproduce.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/config.rs | sed -n '136,165p'; git show 077dbe2c:crates/cobre-sddp/src/cut/fcf.rs | sed -n '490,500p'; git grep -n 'warm_start_cuts' 077dbe2c -- crates/cobre-sddp/src",
        "output": "config.rs:\n    /// Cuts pre-loaded from a warm-start policy; contributes to cut-pool capacity.\n    pub warm_start_cuts: u32,\n    ... impl Default { warm_start_cuts: 0, }\n\ncut/fcf.rs:\n/// One pool's capacity: `warm_start_count + max_iterations * visit_bound` ...\nfn pool_capacity(warm_start_count: u32, max_iterations: u64, visit_bound: u64) -> usize {\n    (u64::from(warm_start_count) + max_iterations * visit_bound) as usize\n}\n\ngit grep warm_start_cuts -- crates/cobre-sddp/src (production lines only):\n  config.rs:148  pub warm_start_cuts: u32,          (declaration)\n  config.rs:160  warm_start_cuts: 0,                (Default impl)\n  setup/mod.rs:789   warm_start_cuts: 0,            (production literal)\n  setup/orchestration.rs:136  warm_start_cuts: 0,   (production literal)\n  policy/orchestration.rs:322 warm_start_cuts: warm_start_counts.iter().copied().max().unwrap_or(0),   (cobre_io ProducerBlock manifest field, a DIFFERENT symbol)\n  cut/fcf.rs:421 pub fn total_warm_start_cuts(&self)  (counts pool records, does not read the config field)\n  training/training_output.rs:331 total_loaded: fcf.total_warm_start_cuts() as u64,\nEvery other occurrence under crates/cobre-sddp/src is a test literal or a test name (config.rs:291-329, cut/*.rs test names, training/**/tests.rs, training/session/mod.rs:1911 which sits inside the #[cfg(test)] mod tests that opens at line 1508, test_support.rs:825).",
        "reading": "The field states a contract its own crate does not honour. Both production construction sites write the literal 0 -- setup/mod.rs:789 and setup/orchestration.rs:136 -- and no production line anywhere under crates/cobre-sddp/src READS cut_management.warm_start_cuts; the only reads are the assertion in config.rs:304 and the test literals. The real owner of the quantity the doc describes is pool_capacity's warm_start_count parameter in cut/fcf.rs, which is per-pool, and the manifest side is per-pool too: policy/orchestration.rs:322 derives the single scalar it writes as the MAX over warm_start_counts, and cobre-io records.rs documents warm_start_counts as superseding the scalar 'for per-pool accuracy'. So a study-wide u32 is the wrong shape for the fact as well as an unread one. This is not covered by the reserved-config rule: that rule scopes to cobre-io config sections that are loaded, validated and schema-exported, and this is an internal engine struct field with no schema surface. It survives only because the struct is pub, so the external test literals keep it compiling."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-over-engineering-01",
      "id": "OD-036",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a `pub fn *_for_test` shipped ungated on the public surface with one test caller; naming and surface hygiene on one read-only accessor",
      "category": "speculative-generality",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "The defect is confined to public-surface and naming hygiene on one read-only accessor: `StudySetup::ncs_stochastic_dormant_for_test` (setup/accessors.rs:218) is the workspace's only ungated `pub fn *for_test`, has no production caller, and has a single caller at tests/deterministic.rs:2014. The sibling-parity framing over-reaches and is withdrawn: the four gated hooks in the same file guard config mutation or private-field-layout exposure, which a `&self` method returning owned booleans cannot breach. The candidate's flagged trade-off is also narrower than filed: gating is CI-coverage-neutral because `test-support` is already in NON_SOLVER_FEATURES on both test jobs, so the only coverage that moves is a bare default-feature local `cargo test`, and the D18 rustdoc sentence promising an unconditional default-profile run would have to be corrected with it.",
      "title": "ncs_stochastic_dormant_for_test ships in the default public API while its four sibling test hooks in the same file are test-support-gated",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/accessors.rs",
          "symbol": "ncs_stochastic_dormant_for_test"
        }
      ],
      "fixShape": "Either move the method under the same '#[cfg(any(test, feature = \"test-support\"))]' as its four siblings and declare test-support as a required feature of the one consuming test, or, if the published surface is intended, drop the _for_test suffix and document it as a supported query. Prefer the gate. Note the real trade-off the gate carries: the D18 dormancy test runs today under a plain 'cargo test --workspace', so gating it moves that coverage behind a feature flag and the station should say so rather than let it silently drop. No production caller exists either way, so the solve paths are untouched; parity goldens and the rank-invariance harness bound the change.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/setup/accessors.rs (read in full, 370 lines) ; git grep -n -w ncs_stochastic_dormant_for_test 077dbe2c -- crates ; git show 077dbe2c:crates/cobre-sddp/Cargo.toml",
        "output": "In accessors.rs, 'set_risk_measures', 'set_scheduler', 'set_hardest_first_claim_order' and 'training_ctx' each sit under '#[cfg(any(test, feature = \"test-support\"))]'. 'ncs_stochastic_dormant_for_test' is declared plain: '#[must_use] pub fn ncs_stochastic_dormant_for_test(&self) -> Vec<Vec<bool>>', documented as 'reconstructed for the out-of-crate tests/ harness'. Its only caller anywhere in the workspace at the pin is crates/cobre-sddp/tests/deterministic.rs:2014, inside the D18 per-stage NCS commissioning dormancy test whose doc block runs :1971-1980; that test is not feature-gated. Cargo.toml declares 'default = [\"highs\"]' and 'test-support = [\"cobre-core/test-support\"]', so test-support is off by default.",
        "reading": "The method allocates a Vec<Vec<bool>> reconstruction of dormancy state that no production path reads, and its own name carries the _for_test suffix, so it is a test seam published to every downstream consumer of the crate. The same file already applies the correct gate four times, which is what makes this a miss rather than a design choice: the pattern exists and one member of the family does not follow it."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-performance-00",
      "id": "PD-035",
      "class": "PD",
      "lens": "performance",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "the surviving residue is the bit-exact symmetric-matrix half only, on a once-per-study opening-order chain builder (setup-time by project rule); the tour-cost half moves goldens and is demoted to an owner call — structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Backward opening order is warm-start-only"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Owner call: whether the goldens-moving delta reformulation of the 2-opt accept test is worth a parity re-baseline at all, given that the confirmed free win is only the symmetric matrix fill; if not, the perf sweep should carry the symmetric-fill item alone."
      ],
      "survivingClaim": "Only the symmetric-matrix half of the title survives as a bit-exact free win: `l2_distance_matrix` (noise_key.rs:223-243) evaluates every off-diagonal pair's L2 twice and computes the zero diagonal, and mirroring plus a skipped diagonal leaves every entry bit-identical. The whole-tour `tour_cost` recomputation in `two_opt_improve` (174-191) is real but is demoted from removable redundancy to a semantics-changing perf-sweep item, because the full sum is what keeps the accept test at 182 and the cross-start winner at 212 functions of the tours and the matrix alone, and because a path reversal changes only ONE edge at `j == n_o - 1`. The per-start `nearest_neighbor_tour` buffer allocations (141-142) do not survive at all: they are once-per-study-setup work outside the hot-path allocation ban.",
      "title": "The opening-order chain builder recomputes a whole-tour cost sum for every candidate 2-opt reversal, and fills a symmetric distance matrix twice",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "two_opt_improve"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "tour_cost"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "shortest_chain_path"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "l2_distance_matrix"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "nearest_neighbor_tour"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise_key.rs",
          "symbol": "apply_chain_order"
        }
      ],
      "fixShape": "Two halves with different risk, and they should be queued separately. The byte-exact half: fill the distance matrix symmetrically by writing the mirrored entry instead of recomputing it and skip the zero diagonal, since `(a - b) * (a - b)` equals `(b - a) * (b - a)` exactly in IEEE arithmetic and the per-pair summation order is unchanged, so the matrix is bit-identical; and hoist the nearest-neighbour tour and visited buffers out of the per-start loop, reusing them by clear-and-refill, which changes no value at all. Both leave the winning permutation identical, so the parity goldens in `tests/parity.rs` and `tests/common/parity_hash.rs`, the rank-invariance harness in `tests/common/permute.rs`, and `mpiexec` at one rank versus two ranks all reproduce unchanged and no golden moves. The half that does move goldens: replacing the whole-tour recomputation with the two-edge delta a path reversal actually implies changes the floating-point basis of the accept/reject comparison, can flip a marginal reversal, and so can change the winning permutation. Per `.claude/rules/sddp.md` section Backward opening order is warm-start-only, outcomes are written and aggregated by canonical omega, so a changed permutation cannot break declaration-order invariance or run-to-run reproducibility; but that same section states that changing the order changes the warm-start chain each opening solves from and therefore re-checks the golden parity baselines instead of assuming byte-identical outputs. So the delta half is proposed only with an explicit golden re-check plus the `opening_order_determinism` gate in `tests/mpi_wire.rs` and the rank-invariance comparison. Keep `total_cmp` throughout: a delta formulation must not reintroduce `partial_cmp` or bare `<` ordering, which the same section names as the wrong-but-compiling alternative.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/stochastic/noise_key.rs | sed -n '132,146p;174,222p;223,266p'; git grep -n 'build_noise_key_table' 077dbe2c -- crates/cobre-sddp/src | grep -v 'noise_key.rs:5'",
        "output": "132:fn tour_cost(tour: &[usize], distances: &[f64], n_o: usize) -> f64 {\n133:    tour.windows(2).map(|w| distances[w[0] * n_o + w[1]]).sum()\n134:}\n140:fn nearest_neighbor_tour(distances: &[f64], n_o: usize, start: usize) -> Vec<usize> {\n141:    let mut tour = Vec::with_capacity(n_o);\n142:    let mut visited = vec![false; n_o];\n...\n174:fn two_opt_improve(tour: &mut [usize], cost: &mut f64, distances: &[f64], n_o: usize) {\n175:    let mut improved = true;\n176:    while improved {\n177:        improved = false;\n178:        for i in 1..n_o.saturating_sub(1) {\n179:            for j in (i + 1)..n_o {\n180:                tour[i..=j].reverse();\n181:                let candidate_cost = tour_cost(tour, distances, n_o);\n182:                if candidate_cost.total_cmp(cost).is_lt() {\n183:                    *cost = candidate_cost;\n184:                    improved = true;\n185:                } else {\n186:                    tour[i..=j].reverse();\n187:                }\n...\n198:fn shortest_chain_path(distances: &[f64], n_o: usize) -> Vec<usize> {\n...\n208:    for start in 0..n_o {\n209:        let mut tour = nearest_neighbor_tour(distances, n_o, start);\n210:        let mut cost = tour_cost(&tour, distances, n_o);\n211:        two_opt_improve(&mut tour, &mut cost, distances, n_o);\n...\n223:fn l2_distance_matrix(\n...\n230:    let mut distances = vec![0.0_f64; n_o * n_o];\n231:    for i in 0..n_o {\n232:        let opening_i = &tree.opening(stage, i)[..n_hydros];\n233:        for j in 0..n_o {\n234:            let opening_j = &tree.opening(stage, j)[..n_hydros];\n235:            distances[i * n_o + j] = opening_i\n236:                .iter()\n237:                .zip(opening_j)\n238:                .map(|(a, b)| (a - b) * (a - b))\n239:                .sum::<f64>()\n240:                .sqrt();\n...\n254:fn apply_chain_order(keys: &mut [Vec<f64>], tree: &OpeningTreeView<'_>, n_hydros: usize) {\n255:    for (stage, stage_keys) in keys.iter_mut().enumerate() {\n256:        let n_o = stage_keys.len();\n257:        if n_o < 3 {\n258:            continue;\n259:        }\n260:        let distances = l2_distance_matrix(tree, stage, n_o, n_hydros);\n261:        let path = shortest_chain_path(&distances, n_o);\n\n077dbe2c:crates/cobre-sddp/src/setup/mod.rs:53:use crate::stochastic::noise_key::build_noise_key_table;\n077dbe2c:crates/cobre-sddp/src/setup/mod.rs:467:        let solve_order_keys = build_noise_key_table(system, &stochastic)?;",
        "reading": "`two_opt_improve` calls `tour_cost`, a pass over the whole tour, once per candidate segment reversal, and the reversal loop is the quadratic (i, j) sweep repeated under `while improved`. `shortest_chain_path` runs that whole improvement for each of n_o nearest-neighbour starts, and `apply_chain_order` calls it once per study stage carrying at least three openings. A path 2-opt reversal changes exactly two edges, so the accept/reject test needs two matrix reads rather than a whole-tour sum. `l2_distance_matrix` writes every entry of an `n_o` by `n_o` symmetric matrix, so each off-diagonal pair is computed twice and the zero diagonal is computed too. `nearest_neighbor_tour` allocates a fresh `Vec<usize>` plus a fresh `vec![false; n_o]` on every start. Honest reach: the grep finds exactly one production call site, `setup/mod.rs` in `from_broadcast_params`, so this is setup-time work executed once per study and replicated on every rank; it has no per-iteration and no per-solve reach, which is why the severity proposed is local rather than structural."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/stochastic/noise_key.rs::l2_distance_matrix",
      "exercisingCallSites": [
        "stochastic/noise_key.rs · shortest_chain_path → nearest_neighbor_tour / two_opt_improve (once per study at setup)"
      ],
      "mechanism": "redundant pass — a whole-tour cost sum recomputed per candidate reversal where only two edges change, a symmetric distance matrix computed twice per pair, and a per-start buffer allocation",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-performance-01",
      "id": "PD-036",
      "class": "PD",
      "lens": "performance",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a study-invariant level partition re-derived by whole-graph predicate scans and freshly allocated on every backward pass at two per-iteration callers — the never-allocate-on-hot-paths rule with a one-field precomputation fix; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals; NodeGraph is the engine's traversal structure)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "mirror:module-map",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        },
        {
          "ref": "mirror:graph-shape-predicate-grep",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Per-level exchange in the backward pass"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "Placement is an owner call the architecture rules do not disambiguate: a derived study-invariant consumed only by the backward drivers fits decision-tree rule 2 (a `NodeGraph` field beside `n_pools`/`pool_stage`, the candidate's shape) and rule 5 (a `BackwardPassState` scratch field) equally; the training owner should also decide whether this lands together with the register's open one-owner residue on the interior-cut-node accessor."
      ],
      "survivingClaim": "At `NodeGraph::backward_cut_levels` and its two per-iteration callers only (`run_sampled_backward`, `run_enumerated_backward`), the graph-invariant level partition is re-derived by evaluating the cut-generating predicate once per (stage, node) pair instead of in one bucket pass, and re-collected into a fresh outer `Vec` plus one fresh `Vec` per non-empty level, on every backward pass; `build_node_graph` is a fix site rather than a defect site, and the buffer is distinct in SUBJECT (not in named driver) from the `stage_stats` telemetry pack that the register's deferred backward-scratch item already covers for BOTH drivers.",
      "title": "The reverse-topological cut level decomposition is a study invariant but is rescanned and reallocated on every backward pass",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "backward_cut_levels"
        },
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "NodeGraph"
        },
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "build_node_graph"
        }
      ],
      "fixShape": "Resolve the level decomposition once where the graph is constructed, in `build_node_graph`, and store it as a `NodeGraph` field the drivers borrow, so each backward pass reads a slice of slices instead of rescanning and reallocating. The crate already has this precedent: the enumerated plan and the nested upper-bound topology are documented as resolved once at construction rather than every training iteration, and `NodeGraph` is exactly the study-level read-only home the architecture rules name for this data. Byte-neutrality: the stored decomposition must be built by a single stable bucket pass that reproduces both orders the current code produces, the outer list descending by stage and each inner list ascending by canonical node position, because the within-level node order is the order the risk aggregation consumes and the index-order tie-break that `assemble_outcome_weights` documents for the nested risk measure depends on it. With both orders preserved the levels are element-for-element what they are today, the level driver's one state exchange and one batched cut exchange per level stay in the same sequence per `.claude/rules/sddp.md` section Per-level exchange in the backward pass, and the parity goldens in `tests/parity.rs` with `tests/common/parity_hash.rs`, the rank-invariance harness in `tests/common/permute.rs`, and `mpiexec` at one rank versus two ranks all reproduce bit-for-bit. No golden moves.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/setup/node_graph.rs | sed -n '774,798p'; git grep -n 'backward_cut_levels()' 077dbe2c -- crates/cobre-sddp/src/training",
        "output": "774:    pub(crate) fn backward_cut_levels(&self) -> Vec<Vec<NodePos>> {\n775:        let cut_generating = |pos: NodePos| !self.successors[pos].is_empty();\n776:        let Some(max_stage) = self\n777:            .nodes\n778:            .iter_indexed()\n779:            .filter(|&(pos, _)| cut_generating(pos))\n780:            .map(|(_, n)| n.stage)\n781:            .max()\n...\n786:        let mut levels: Vec<Vec<NodePos>> = Vec::new();\n787:        for stage in (0..=max_stage.0).rev().map(StageIdx) {\n788:            let level: Vec<NodePos> = self\n789:                .nodes\n790:                .iter_indexed()\n791:                .filter(|&(pos, n)| n.stage == stage && cut_generating(pos))\n792:                .map(|(pos, _)| pos)\n793:                .collect();\n794:            if !level.is_empty() {\n795:                levels.push(level);\n796:            }\n797:        }\n798:        levels\n\n077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs:645:        let levels = training_ctx.node_graph.backward_cut_levels();\n077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs:756:        let levels = node_graph.backward_cut_levels();",
        "reading": "The body walks the whole `nodes` array once to find the maximum cut-generating stage, then walks it again in full for every stage from that maximum down to zero, allocating one `Vec<NodePos>` per non-empty level plus the outer `Vec`. Both production call sites are per-iteration backward drivers: `run_sampled_backward` (the enclosing function at the first hit) and `run_enumerated_backward` (the enclosing function at the second). `NodeGraph` is immutable study-level data reached through `training_ctx.node_graph`, the placement the architecture rules pin in the F7 note, so the returned decomposition is identical on every iteration and the whole computation plus its allocations repeat for no new information. This is the crate hard rule against allocating on hot paths, applied to a driver prelude. Distinct from the deferred per-run allocation item anchored at `run_enumerated_backward`: the subject here is the callee's study-invariant result, and it is reached first from `run_sampled_backward`, which that item does not name."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/setup/node_graph.rs::backward_cut_levels",
      "exercisingCallSites": [
        "training/backward_pass_state.rs · run_sampled_backward (per iteration)",
        "training/backward_pass_state.rs · run_enumerated_backward (per iteration)"
      ],
      "mechanism": "redundant pass plus allocation per call — a study-invariant level decomposition recomputed by repeated whole-graph scans and freshly allocated on every backward pass",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-performance-02",
      "id": "PD-037",
      "class": "PD",
      "lens": "performance",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "the residue is one scan per stage per enumerated sweep on declared multi-node graphs only; the frontier_node / any_stage_node family has no per-stage amplification and the chain case is a singleton — bounded, not hot",
      "upgradeNote": null,
      "calibrationBasis": "narrowed to the two enumerated per-stage driver loops on a declared multi-node graph: one whole-node-array pass per stage per sweep, amortised and bounded — not a per-solve cost; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "mirror:module-map",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        },
        {
          "ref": "mirror:graph-shape-predicate-grep",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Backward opening order is warm-start-only"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Only the two enumerated per-stage driver loops (training/forward/enumerated.rs:536, simulation/enumerated.rs:302) pay a whole-node-array pass per stage, and only on a declared multi-node graph; the frontier_node / any_stage_node family is a bounded per-pass root-or-terminal lookup with no per-stage amplification (the solve_simulation_stage site is short-circuited behind is_terminal, so it fires once per scenario), the synthesized chain reduces every frontier to a singleton over one node per stage, the pass is amortized against at least one LP solve per stage into a reused stage_units buffer, and any membership index must stay a total function yielding empty for a stage the graph declares no node at, because that emptiness feeds the carries-no-alive-node validation errors.",
      "title": "Stage frontier resolution scans the entire node array per stage, so an enumerated sweep pays a whole-graph scan for every stage it visits",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "stage_frontier"
        },
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "frontier_node"
        },
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "any_stage_node"
        },
        {
          "path": "crates/cobre-sddp/src/setup/node_graph.rs",
          "symbol": "build_declared_node_graph"
        }
      ],
      "fixShape": "Give `NodeGraph` a per-stage membership index built once in `build_node_graph`: an offsets array plus a positions array, the same compressed shape the graph already uses for out-edges, so `stage_frontier` returns an iterator over one contiguous slice and `frontier_node` and `any_stage_node` read that slice's head. The per-stage loop in each enumerated driver then visits only the nodes at that stage. Byte-neutrality: because canonical position is ascending declared node id and not stage-grouped, a stage's members are not contiguous in position space, so the index must be filled by a stable pass over ascending canonical position; each stage's slice then yields exactly the sequence the current filter yields. Same nodes in the same order means the per-stage unit list each driver builds is filled identically, and the claim cursor over it therefore hands the same units to the same workers, so the parity goldens in `tests/parity.rs` with `tests/common/parity_hash.rs`, the rank-invariance harness in `tests/common/permute.rs`, and `mpiexec` at one rank versus two ranks all reproduce bit-for-bit and no golden moves. This touches no pinned contract: the frontier is structural, it selects no opening and no basis, so section Backward opening order is warm-start-only and section A stored basis warm-starts only at its own node are not engaged. The two driver call sites are cited as context only; they belong to sibling sub-stations and the subject of this candidate is the accessor.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/setup/node_graph.rs | sed -n '1120,1125p;1133,1135p;1149,1151p'; git grep -n 'stage_frontier(' 077dbe2c -- crates/cobre-sddp/src | grep -v 'node_graph.rs'; git show 077dbe2c:crates/cobre-sddp/src/training/forward/enumerated.rs | sed -n '533,540p'",
        "output": "1120:    pub(crate) fn stage_frontier(&self, stage: StageIdx) -> impl Iterator<Item = NodePos> + '_ {\n1121:        self.nodes\n1122:            .iter_indexed()\n1123:            .filter(move |(_, n)| n.stage == stage)\n1124:            .map(|(pos, _)| pos)\n1125:    }\n1134:        let mut frontier = self.stage_frontier(stage);\n1150:        self.stage_frontier(stage).next()\n\n077dbe2c:crates/cobre-sddp/src/simulation/enumerated.rs:302:        for node in node_graph.stage_frontier(t) {\n077dbe2c:crates/cobre-sddp/src/training/forward/enumerated.rs:536:        for node in node_graph.stage_frontier(t) {\n077dbe2c:crates/cobre-sddp/src/training/forward_pass_state.rs:2434:        let leaves: Vec<NodePos> = node_graph.stage_frontier(StageIdx(1)).collect();\n\n533:    for t in (0..num_stages).map(StageIdx) {\n534:        // Units: the nodes at stage `t` on this rank's paths, canonical order.\n535:        scratch.stage_units.clear();\n536:        for node in node_graph.stage_frontier(t) {\n537:            if scratch.on_my_paths[node] {\n538:                scratch.stage_units.push(node);\n539:            }\n540:        }\n",
        "reading": "`stage_frontier` filters the whole `nodes` array on every call, so resolving one stage's frontier costs a pass over every node in the graph. Both enumerated drivers call it inside a loop over every stage, so one sweep costs a number of node visits equal to stages times nodes, and the enumerated forward sweep runs once per training iteration while the simulation sweep runs per simulation pass. `frontier_node` and `any_stage_node` are built on the same scan and are the resolution route used by the simulation pipeline, the simulation state driver, the sampled forward pass, the lower-bound root evaluation and the training session, so the scan is the single shared frontier lookup rather than an isolated one. The remaining hit in `forward_pass_state.rs` is inside that file's test module. Nothing about the scan is required by the graph's shape: `build_declared_node_graph` sorts nodes into canonical position by ascending declared node id, not grouped by stage, which is why a linear filter was the only route available at construction time."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "enumerated",
      "profiledSymbol": "crates/cobre-sddp/src/setup/node_graph.rs::stage_frontier",
      "exercisingCallSites": [
        "training/forward/enumerated.rs · run_enumerated_forward (per stage per sweep)",
        "simulation/enumerated.rs · run_sweep (per stage per sweep)"
      ],
      "mechanism": "redundant pass — a whole-graph node scan per stage to select that stage's nodes, driven once per stage per sweep",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-01",
      "id": "TD-041",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "two helpers × three copies inside setup/, fix is one pair on test_support.rs (effort S); the crate-wide penalties ripple is the sibling entry",
      "upgradeNote": null,
      "calibrationBasis": "two small fixture helpers declared three times inside one directory; one `pub(crate)` pair on the existing test_support surface removes six declarations — TD-035-scale, not the TD-037 ripple",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Two of the three named helpers are a genuine triplication, not all three: the hydro(id, downstream_id, travel_time_hours) constructor and the 16-field all-zero HydroPenalties builder have bodies identical up to a binding name and a callee prefix at bucket_topology.rs:417/442, setup/mod.rs:2981/3002 and setup/tests.rs:9957/9982, and the crate's shared surface carries no reusable equivalent because geometry_hydro pins different bounds, so one pub(crate) pair added to test_support.rs beside ymd removes six declarations, not nine. The date() leg does not survive as duplication: mod.rs:2977 uses unwrap_or_else with unreachable! against .unwrap() at the other two sites, so the trio is not byte-identical, and test_support::ymd already exists, making that leg three call-site rewrites onto an existing helper.",
      "title": "The transit-seed fixture family is declared three times byte-identically inside one directory: hydro(), zero_penalties() and date() each exist in setup/bucket_topology.rs, setup/mod.rs and setup/tests.rs",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/bucket_topology.rs",
          "symbol": "hydro"
        },
        {
          "path": "crates/cobre-sddp/src/setup/mod.rs",
          "symbol": "hydro"
        },
        {
          "path": "crates/cobre-sddp/src/setup/tests.rs",
          "symbol": "bucket_seed_hydro"
        },
        {
          "path": "crates/cobre-sddp/src/setup/bucket_topology.rs",
          "symbol": "zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/src/setup/mod.rs",
          "symbol": "zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/src/setup/tests.rs",
          "symbol": "bucket_seed_zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/src/setup/mod.rs",
          "symbol": "date"
        },
        {
          "path": "crates/cobre-sddp/src/setup/tests.rs",
          "symbol": "bucket_seed_date"
        }
      ],
      "fixShape": "Hoist one transit-seed fixture trio onto the existing shared surface: add a pub neutral-penalties constructor and a pub travel-time hydro constructor to crates/cobre-sddp/src/test_support.rs beside the ymd date helper already there, under the module's existing cfg(any(test, feature = test-support)) gate, then delete the nine local declarations and call the shared ones. This is L3-internal, adds no crate and no dependency, and per the directive introduces no new fixture crate. Byte-neutral by construction: the shared constructors return the same field values the three local bodies already return, so every fixture that feeds a solve resolves to an identical System, leaving the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map unchanged with no golden moved. Touches no pinned contract in .claude/rules/sddp.md.",
      "evidence": {
        "command": "git grep -n 'fn zero_penalties|fn hydro\\(|fn date\\(' 077dbe2c -- crates/cobre-sddp/src/setup/; for spec in 'setup/bucket_topology.rs 442' 'setup/mod.rs 3002' 'setup/tests.rs 9982'; do set -- $spec; git show 077dbe2c:crates/cobre-sddp/src/$1 | sed -n \"$2,+24p\" | sed 's/^ *//' | md5sum; done",
        "output": "src/setup/bucket_topology.rs:417:    fn zero_penalties() -> HydroPenalties {\nsrc/setup/bucket_topology.rs:438:    fn date(y: i32, m: u32, d: u32) -> NaiveDate {\nsrc/setup/bucket_topology.rs:442:    fn hydro(id: i32, downstream_id: Option<i32>, travel_time_hours: Option<f64>) -> Hydro {\nsrc/setup/mod.rs:2977:    fn date(y: i32, m: u32, d: u32) -> NaiveDate {\nsrc/setup/mod.rs:2981:    fn zero_penalties() -> HydroPenalties {\nsrc/setup/mod.rs:3002:    fn hydro(id: i32, downstream_id: Option<i32>, travel_time_hours: Option<f64>) -> Hydro {\nsrc/setup/tests.rs:9957:fn bucket_seed_zero_penalties() -> HydroPenalties {\nsrc/setup/tests.rs:9978:fn bucket_seed_date(y: i32, m: u32, d: u32) -> NaiveDate {\nsrc/setup/tests.rs:9982:fn bucket_seed_hydro(id: i32, downstream_id: Option<i32>, travel_time_hours: Option<f64>) -> Hydro {\n\n(hydro bodies, leading whitespace stripped) identical apart from the local binding name and which sibling zero-penalties helper is called",
        "reading": "`fn hydro(id, downstream_id, travel_time_hours) -> Hydro` has exactly three declarations in the entire crate and all three are inside this manifest, two of them inside the same file pair (bucket_topology.rs:442, setup/mod.rs:3002, setup/tests.rs:9982). `zero_penalties() -> HydroPenalties` (17 fields, every one 0.0) has 9 declarations crate-wide, 3 of them here at bucket_topology.rs:417, setup/mod.rs:2981 and setup/tests.rs:9957. `date(y,m,d) -> NaiveDate` is a 3-line wrapper with 5 declarations crate-wide, 3 of them here at bucket_topology.rs:438, setup/mod.rs:2977 and setup/tests.rs:9978, while test_support.rs:770 already exposes `pub fn ymd(year, month, day)` as the same function on the crate's shared surface. The third copy of all three exists to serve the single test fn in setup/mod.rs::transit_seed_round_trip_tests, and setup/tests.rs already has `use crate::test_support;` at L9, so the shared surface is in scope at every one of the three sites."
      },
      "yardstickSection": "§5.2 uniform test-support feature convention (shared fixtures live on the crate's own test-support surface, never a new fixture crate)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-02",
      "id": "TD-042",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "56 zero-argument `-> HydroPenalties` helpers under 11 names re-implement the spread base cobre-core already exports behind the enabled `test-support` feature, and no literal in the crate carries a `..` base — every new penalty field costs O(copies) edits, the ripple the yardstick's shared-fixture rule exists to prevent (TD-037 precedent, Sev B); folds 5c-test-bloat-05",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": [
        "5c-test-bloat-05"
      ],
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Whether the corpus should keep at least one literal that still spells all sixteen penalty fields as a canary: spreading `..HydroPenalties::uniform(v)` everywhere means a newly added penalty field silently defaults in every fixture instead of breaking the build, and while `uniform`'s rustdoc endorses the spread and builders.rs places its strictness on the make_<entity> literal, no artifact rules on the crate-wide loss of that compile-time signal."
      ],
      "survivingClaim": "cobre-sddp's fixtures never adopt the spread base cobre-core already exports to them: 56 zero-argument `-> HydroPenalties` helpers under 11 names re-implement `HydroPenalties::uniform`, nine of them byte-identical `default_hydro_penalties` copies inside src/setup/tests.rs alone, and no HydroPenalties literal in the crate carries a `..` base, even though `uniform` is pub behind the `cobre-core/test-support` feature the crate's dev-dependencies already enable and whose sibling `declare_mirror_unit_group` those same 35 files already call. The residue is call-site non-adoption of an existing L1 constructor, not a missing one-place constructor, not the absent Default derive, and not the tests/common/builders.rs invariant being defeated; the fix spreads `..HydroPenalties::uniform(v)` at the call sites and adds no constructor to cobre-sddp's test_support.",
      "title": "HydroPenalties is hand-enumerated field by field 46 times in setup/tests.rs and 276 times across the crate with zero struct-update spreads, defeating the one-place field-addition invariant tests/common/builders.rs documents",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/setup/tests.rs",
          "symbol": "minimal_system"
        },
        {
          "path": "crates/cobre-sddp/src/setup/tests.rs",
          "symbol": "bucket_seed_zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/src/setup/mod.rs",
          "symbol": "zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/src/setup/bucket_topology.rs",
          "symbol": "zero_penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/common/builders.rs",
          "symbol": "neutral_hydro_penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/common/builders.rs",
          "symbol": "make_hydro"
        },
        {
          "path": "crates/cobre-sddp/tests/common/builders.rs",
          "symbol": "HydroSpec"
        }
      ],
      "fixShape": "Promote the neutral penalties constructor from tests/common/builders.rs to a pub fn on crates/cobre-sddp/src/test_support.rs under the module's existing cfg(any(test, feature = test-support)) gate, so both the src-side tests and the integration binaries reach one declaration, then rewrite the literals as struct-update expressions that name only the fields a given test actually varies. Deliberately do NOT add a Default derive to the cobre-core HydroPenalties entity: a Default there would let an omitted field default silently to zero in production construction and deserialization paths, turning a test-ergonomics cleanup into a correctness regression, so the neutral constructor stays behind the test gate. No new fixture crate, per the directive. Byte-neutral by construction: each rewritten site resolves to the identical 17 field values it spells out today, so the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map are unaffected and no golden moves.",
      "evidence": {
        "command": "git grep -c 'HydroPenalties {' 077dbe2c -- crates/cobre-sddp | sort -t: -k3 -nr | head -6; git grep -c '\\.\\.Default::default\\(\\)' 077dbe2c -- crates/cobre-sddp/src/setup/tests.rs; git show 077dbe2c:crates/cobre-core/src/entities/hydro.rs | sed -n '54,58p'; git show 077dbe2c:crates/cobre-sddp/tests/common/builders.rs | sed -n '1,20p'",
        "output": "276 `HydroPenalties {` literal sites across 64 files in crates/cobre-sddp; densest:\n  src/setup/tests.rs                46\n  tests/anticipated_core.rs         27\n  tests/template_integration.rs     19\n  tests/deterministic.rs            17\n  tests/anticipated_scenarios.rs    13\n  src/lp/builder/columns.rs         13\n\n`git grep -c '..Default::default()' -- src/setup/tests.rs` -> no match (0)\n\ncrates/cobre-core/src/entities/hydro.rs:56: #[derive(Debug, Clone, Copy, PartialEq)]\n\ntests/common/builders.rs module doc: 'The one-place field-addition invariant: a new required field on a `cobre-core` entity is absorbed by editing only this module - add the field to the matching `<Entity>Spec` (with a neutral `Default`) and map it in `make_<entity>`.'",
        "reading": "276 HydroPenalties literal sites across 64 files inside cobre-sddp at the pin. HydroPenalties derives no Default (crates/cobre-core/src/entities/hydro.rs:56 derives only Debug, Clone, Copy, PartialEq), and setup/tests.rs contains zero `..Default::default()` spreads, so each of its 46 sites hand-writes all 17 fields; that is roughly 780 lines of field enumeration in one file. tests/common/builders.rs:190 neutral_hydro_penalties() already is the one place the module doc promises, but it is private and reachable only through the four <Entity>Spec types, so no src-side test and no integration binary that builds a Hydro outside make_hydro can use it. The documented invariant therefore holds only for the small share of the corpus that goes through HydroSpec: adding a required penalty field today edits 276 sites, not one."
      },
      "yardstickSection": "§5.2 uniform test-support feature convention (one fixture owner per type; struct-update spreads over an exported base)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-03",
      "id": "TD-043",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "md5-identical `penalties()` across four right_boundary_* binaries, `bounds()` identical in three, and five ring-prelude functions code-identical between pricing and validation — four binaries drifting in lockstep on one fixture prelude (TD-037 precedent, Sev B); folds 5d-test-bloat-02",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": [
        "5d-test-bloat-02"
      ],
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "penalties() is md5-identical across all four right_boundary_* binaries and bounds() is md5-identical in three with validation's a strict one-parameter superset, and pricing/validation share five ring-prelude functions that are code-identical modulo path qualification and one BETA-const-vs-beta-parameter split. Narrowed on four counts: study_start() is stripped as a 3-line per-fixture calendar anchor whose four calendars genuinely diverge downstream and for which test_support::ymd already exists; the destination narrows from tests/common/builders.rs to the cfg-gated src/test_support.rs that testing-architecture.md section 5.2's Adoption stamp designates, since tests/common/ is itself slated to collapse into that surface; any hoist must carry the union of pricing's three prelude rationale clauses that validation lacks; and the merged twin's premise that right_boundary_validation.rs is a parity-hash golden is false, so the twin's rank-invariance and MPI-reproduction verification burden falls away, though its fixture_priced_date x3 and leaf_positions x2 residue is verified byte-identical and rides along.",
      "title": "The four right_boundary_* binaries each re-declare the same ResolvedPenalties, ResolvedBounds and study_start prelude, and right_boundary_validation.rs carries a ~125-line copy of right_boundary_pricing.rs's ring-injection prelude",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/right_boundary_cost_semantics.rs",
          "symbol": "penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_output.rs",
          "symbol": "penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_pricing.rs",
          "symbol": "penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_validation.rs",
          "symbol": "penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_cost_semantics.rs",
          "symbol": "bounds"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_output.rs",
          "symbol": "bounds"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_cost_semantics.rs",
          "symbol": "study_start"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_pricing.rs",
          "symbol": "freeze_terminal_template"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_validation.rs",
          "symbol": "freeze_terminal_template"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_pricing.rs",
          "symbol": "inject_ring_boundary"
        },
        {
          "path": "crates/cobre-sddp/tests/right_boundary_validation.rs",
          "symbol": "inject_ring_boundary"
        }
      ],
      "fixShape": "Extend tests/common/builders.rs with the two resolved-layer constructors it is missing, a neutral ResolvedPenalties and a ResolvedBounds taking the one dimension the validation variant parameterizes, and move the shared ring-injection prelude into a tests/common submodule so pricing and validation call one copy. The four right_boundary_* binaries already declare mod common, so this adds no link unit and no crate, and per the directive introduces no new fixture crate. Byte-neutral by construction: the shared constructors return the same resolved field values the identical local bodies return today and the parameterized bounds variant keeps its argument, so every affected test builds the same LP, leaving the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map unchanged with no golden moved. The boundary-ring pinning that these files exercise is a contract in .claude/rules/sddp.md, so the moved prelude must keep the ring slot and terminal-theta pinning assertions verbatim.",
      "evidence": {
        "command": "git grep -n 'fn penalties\\(\\)|fn bounds\\(|fn study_start\\(\\)|fn freeze_terminal_template|fn inject_ring_boundary|fn post_study_ring_slot' 077dbe2c -- crates/cobre-sddp/tests/right_boundary_*.rs; then per site: git show 077dbe2c:<file> | sed -n '<decl>,<end>p' | sed 's/^ *//' | md5sum",
        "output": "fn penalties() -> cobre_core::resolved::ResolvedPenalties   41 lines  md5 4b5df6d0ec99  x4 IDENTICAL\n  right_boundary_cost_semantics.rs:164  right_boundary_output.rs:167  right_boundary_pricing.rs:191  right_boundary_validation.rs:200\nfn bounds() -> ResolvedBounds                                40 lines  md5 2da9a556461d  x3 IDENTICAL\n  right_boundary_cost_semantics.rs:123  right_boundary_output.rs:126  right_boundary_pricing.rs:150\n  (right_boundary_validation.rs:159 is `fn bounds(thermal_block_max_mw: f64)`, the parameterized variant)\nfn study_start() -> NaiveDate                                 3 lines  md5 98f5b845a8f0  x4 IDENTICAL\n  rbc:60  rbo:66  rbp:84  rbv:83\nfn stages()                                                  33 lines  md5 472917407e0b  x2 IDENTICAL (rbc:68, rbo:73)\nring prelude right_boundary_validation.rs:353-470 vs right_boundary_pricing.rs:353-477\n  post_study_ring_slot / inject_ring_boundary / freeze_terminal_template / terminal_theta / ring_pin\n  diff shows only path qualification (cobre_sddp::StudySetup -> StudySetup), doc wording, and one `beta: f64` parameter",
        "reading": "Four separate integration binaries, four separate Cargo link units, and the same fixture prelude in each. The 41-line ResolvedPenalties constructor is byte-identical in all four (md5 4b5df6d0ec99); the 40-line ResolvedBounds constructor is byte-identical in three of the four, with the fourth differing only by taking one f64 parameter; study_start is byte-identical in all four. Beyond the prelude, right_boundary_validation.rs:353-470 is a ~125-line clone of right_boundary_pricing.rs:353-477 covering five fns, differing only in path qualification, doc wording and one added beta parameter. All four already declare `mod common;`, so tests/common is linked into each of them, but tests/common/builders.rs covers cobre-core entity structs only and offers no ResolvedBounds or ResolvedPenalties helper, which is why each binary hand-rolls ResolvedBounds::new and ResolvedPenalties::new positionally."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (tests/common owns shared fixture builders; integration binaries do not re-declare them)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-04",
      "id": "TD-044",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "byte-identical fixture classes re-declared six and four-plus-two times inside one 8.8k-line binary, with one class repeated across a sibling binary — a per-class hoist to file scope removes every copy; the `build_system` family is conceded (13 pairwise-distinct bodies); folds 5d-test-bloat-03",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": [
        "5d-test-bloat-03"
      ],
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Scope call for the owner: the default_hydro_penalties body is one md5 across anticipated_core.rs and anticipated_scenarios.rs, and scenarios build_config at 2954 equals core build_config at 3831, so the owner must decide whether the hoist stays per-binary at file scope or lifts the shared class into tests/common (the test-corpus station owns the cross-binary convention)."
      ],
      "survivingClaim": "Only the byte-identical fixture classes duplicate: in anticipated_core.rs the six default_hydro_penalties bodies (one md5), the 4-plus-2 default_hydro_bounds and 4-plus-2 default_hydro_block_bounds classes, and two build_config classes (1631/2619/6235 and 8322/8674), each collapsible to one file-scope declaration per class in the binary that owns it, with anticipated_scenarios.rs repeating that same penalties body three times and hydro_sim.rs only study_start; build_system's 13 pairwise-distinct bodies, the other nine build_config variants, and any routing through tests/common make_hydro or HydroSpec are not part of the defect.",
      "title": "anticipated_core.rs declares build_config 14 times, build_system 13 times and default_hydro_penalties 6 times inside a single 8,828-line binary that already links mod common",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/anticipated_core.rs",
          "symbol": "default_hydro_penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_core.rs",
          "symbol": "build_config"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_core.rs",
          "symbol": "build_system"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_core.rs",
          "symbol": "default_hydro_bounds"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_scenarios.rs",
          "symbol": "default_hydro_penalties"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_scenarios.rs",
          "symbol": "build_config"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_scenarios.rs",
          "symbol": "build_system"
        },
        {
          "path": "crates/cobre-sddp/tests/common/builders.rs",
          "symbol": "make_hydro"
        }
      ],
      "fixShape": "Collapse each family to one declaration at file scope in the binary that owns it, hoisting the byte-identical default_hydro_penalties, default_hydro_bounds and default_hydro_block_bounds out of the sibling inline modules, and fold the build_config variants into one file-level builder whose parameters are exactly the fields the variants differ in. Route the hydro construction through tests/common/builders.rs make_hydro and HydroSpec, which both binaries already link via mod common, so nothing new is introduced and per the directive no fixture crate is added. Byte-neutral by construction: a hoisted declaration returns the value its six identical copies return, and a parameterized build_config called with each variant's current arguments produces the same Config, so every anticipated test builds the same study and the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map are all unchanged with no golden moved. The anticipated hold-family and ring semantics these tests pin are contracts in .claude/rules/sddp.md, so the parameterization must not merge two variants that differ in a commitment or lead-stage value.",
      "evidence": {
        "command": "git grep -n 'fn build_config|fn build_system|fn default_hydro_penalties|fn default_hydro_bounds|fn default_hydro_block_bounds' 077dbe2c -- crates/cobre-sddp/tests/anticipated_core.rs crates/cobre-sddp/tests/anticipated_scenarios.rs; then per site: git show 077dbe2c:<file> | sed -n '<decl>,<end>p' | sed 's/^ *//' | md5sum",
        "output": "anticipated_core.rs (8,828 lines, 15 inline `mod` blocks, `mod common;` at L25)\n  fn build_config             x14  at 386 1091 1631 2619 3206 3831 4202 4705 5269 6235 6912 7303 8322 8674\n     build_config(iterations) 35 lines md5 aab2fdb27577 IDENTICAL at 1631 / 2619 / 6235\n     build_config()           27 lines md5 1608c8a51088 IDENTICAL at 8322 / 8674\n     remaining 8 are 27-34 line variants -> ~432 lines of config fixture in one binary\n  fn build_system             x13  at 152 848 1319 2316 2916 3610 3988 4387 4956 5505 6012 6693 8112 8484\n  fn default_hydro_penalties  x6   at 1493 2490 3091 4561 5132 5678\n     20 lines md5 7d6690cdf19c IDENTICAL at ALL SIX sites\n  fn default_hydro_bounds     x6   8 lines md5 0b911343eceb at 1476/2473/4544/5115; md5 5beb28d56431 at 3074/5661\n  fn default_hydro_block_bounds x6 at 1485 2482 3083 4553 5124 5670\nanticipated_scenarios.rs (3,498 lines, 9 inline `mod` blocks, `mod common;` at L24)\n  fn build_config x6, fn build_system x5, fn default_hydro_penalties x3 (296, 771, 1265), default_hydro_bounds x4, default_hydro_block_bounds x4",
        "reading": "This is intra-binary duplication, not cross-binary: the six default_hydro_penalties declarations are byte-identical (md5 7d6690cdf19c) and live in six sibling inline modules of the same file, so the compiler already sees all six in one translation unit and one file-level declaration would serve them. Three of the fourteen build_config declarations are byte-identical (md5 aab2fdb27577, 35 lines each) and two more are byte-identical to each other, leaving nine near-variants of the same 27-34 line config fixture in one file. Both binaries already declare `mod common;` (anticipated_core.rs:25, anticipated_scenarios.rs:24), so tests/common is linked and in scope at every one of these sites, and tests/common/builders.rs:212 make_hydro plus HydroSpec::default().penalties already cover the entity these local helpers rebuild by hand."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (one declaration per fixture class per binary; whole-system builders parameterised, not copied)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-05",
      "id": "TD-045",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "seven integration binaries hand-copy `state_layout_for` (plus `study_dims` / `all_enabled_cut_state_layouts`) under a doc rationale false at the pin while the same files already resolve other `cobre_sddp::test_support` symbols ungated — the exported surface is the owner and the copies are the ripple (TD-037 precedent); folds 5d-test-bloat-00; carries I.3-8",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-1",
      "alignmentCites": "Part IV §IV.5 (I.3-8 shed line) / Part V §V.2 (Phase 1 purification)",
      "defenderAlignmentHint": "advances-1",
      "partIRef": "I.3-8",
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": [
        "5d-test-bloat-00"
      ],
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus",
        "alignment"
      ],
      "needsHuman": [
        "Owner call outside this candidate: invariance-shuffle.yml builds cargo nextest -p cobre-sddp --test parity without the test-support feature, while parity.rs reaches cobre_sddp::test_support ungated at :894 and :1303, so either that workflow cannot build at the pin or the clones never protected a working default-feature configuration; deciding which fixes whether the consolidation must also gate these modules on the feature or the workflow gains it."
      ],
      "survivingClaim": "For state_layout_for the surviving residue is narrower than the title in two ways: the seven bodies are identical only after dedent and are semantically the same StateSpace::new call as test_support::state_layout, the unreachability half of the rationale is false in all seven because each file already resolves other cobre_sddp::test_support symbols ungated, but the stale-symbol half is false in six of seven (inflow_nonnegativity.rs:69-71 names no symbol), and the duplicated symbol is imported by NONE of the seven - it is imported by name in three other binaries of the same corpus (mpi_wire.rs:1344, lp_builder.rs:1532, basis_trajectory_probe.rs:282), which is what proves reachability. From the merged 5d twin: the five all_enabled_cut_state_layouts copies survive in full, and there the cited symbol does exist (test_support.rs:657) so only the reachability claim is false, with simulation_pipeline_integration.rs:47 importing it by name while four siblings hand-copy it; the four study_dims copies survive as exact matches of test_support::study_dims (:681). The two study_dims_for locals do NOT survive as duplication: they are a five-scalar helper pinning max_deficit_segments to 1, not a copy of the &GeometryDims-taking test_support::study_dims_for (:689), so only their false doc rationale survives.",
      "title": "state_layout_for is cloned byte-identically into seven integration binaries under a doc comment whose stated rationale is false at the pin, while the shared test_support symbol it duplicates is already imported by those same files",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/integration.rs",
          "symbol": "state_layout_for"
        },
        {
          "path": "crates/cobre-sddp/tests/load_integration.rs",
          "symbol": "state_layout_for"
        },
        {
          "path": "crates/cobre-sddp/tests/conformance.rs",
          "symbol": "state_layout_for"
        },
        {
          "path": "crates/cobre-sddp/tests/integration.rs",
          "symbol": "study_dims"
        },
        {
          "path": "crates/cobre-sddp/tests/load_integration.rs",
          "symbol": "study_dims"
        }
      ],
      "fixShape": "Delete the seven state_layout_for clones and the four study_dims clones and call cobre_sddp::test_support::state_layout and cobre_sddp::test_support::study_dims, which are already pub, already gated on the test-support feature these binaries enable, and already imported by the same files. Delete the doc comment with them rather than repairing it, since its two stated reasons are both false at the pin and a corrected copy would only re-justify the duplication. Byte-neutral by construction: the shared state_layout bottoms out in StateSpace::new with the identical eight arguments the clones pass, including the dense per-hydro effective lag vector, so patch-column resolution is unchanged and the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map all hold with no golden moved. Because parity.rs is one of the seven, verify the parity hash is unmoved rather than assuming it.",
      "evidence": {
        "command": "git grep -n 'fn state_layout_for|fn study_dims' 077dbe2c -- crates/cobre-sddp/tests crates/cobre-sddp/src/test_support.rs; for f in conformance inflow_nonnegativity integration load_integration parity simulation_integration simulation_pipeline_integration; do git show 077dbe2c:crates/cobre-sddp/tests/$f.rs | sed -n '<decl>,+11p' | sed 's/^ *//' | md5sum; done; git grep -c 'test_support::' 077dbe2c -- crates/cobre-sddp/tests/integration.rs crates/cobre-sddp/tests/load_integration.rs crates/cobre-sddp/tests/conformance.rs",
        "output": "fn state_layout_for(hydro_count: usize, max_par_order: usize) -> StateSpace  12 lines  md5 27537dfbfaa4  x7 ALL IDENTICAL\n  conformance.rs:797  inflow_nonnegativity.rs:74  integration.rs:66  load_integration.rs:61\n  parity.rs:816  simulation_integration.rs:90  simulation_pipeline_integration.rs:60\n\nbody: StateSpace::new(hydro_count, max_par_order, 0, Vec::new(), 0, 0, vec![], &vec![max_par_order; hydro_count])\n\ndoc comment above all seven: 'Mirrors the gated `test_support::state_layout_for` via the public\n [`StateSpace::new`], so this external test crate (which cannot see the parent\n crate's `#[cfg(test)]` surface) resolves byte-identical patch columns.'\n\nsrc/test_support.rs:572  pub fn state_layout(hydro_count, max_par_order)\n  -> state_layout_full(hc, mpo, 0, 0, Vec::new())\n  -> state_layout_with_transit_buckets: `let effective_lag_count = vec![max_par_order; hydro_count];`\n     StateSpace::new(hydro_count, max_par_order, 0, Vec::new(), 0, 0, Vec::new(), &effective_lag_count)\nsrc/lib.rs:46  #[cfg(any(test, feature = \"test-support\"))] pub mod test_support;\nonly other `state_layout_for` in src: lp/builder/test_support.rs:65 `pub(super) fn state_layout_for(ctx: &TemplateBuildCtx)` inside the private `mod test_support;` declared at lp/builder/mod.rs:55\n\nfn study_dims() -> StudyDimensions { StudyDimensions::default() }\n  test_support.rs:681 (pub, shared)  integration.rs:79  load_integration.rs:74  parity.rs:829  simulation_pipeline_integration.rs:73\n\ntest_support:: use counts: conformance.rs 5, integration.rs 15, load_integration.rs 6",
        "reading": "Seven integration binaries declare the same 12-line fn with md5 27537dfbfaa4, and the shared test_support::state_layout reduces to StateSpace::new with exactly those eight arguments, so all seven clones are literally replaceable by one existing pub symbol. Both halves of the doc comment justifying the duplication are false at the pin. First, no `test_support::state_layout_for` with that signature exists: the crate-level shared surface exposes state_layout(hydro_count, max_par_order) at test_support.rs:572, and the only state_layout_for in src is lp/builder/test_support.rs:65, which takes a &TemplateBuildCtx and sits in a private module. Second, the shared surface is not invisible to an external test crate: lib.rs:46 gates it on cfg(any(test, feature = test-support)), the test-support feature exists for exactly this reason, and these same files already call through it 15, 6 and 5 times respectively. The same pattern repeats for study_dims, a 3-line StudyDimensions::default() wrapper cloned into four binaries against the shared test_support.rs:681. Cited for context and owned outside this manifest: parity.rs, simulation_integration.rs, simulation_pipeline_integration.rs and inflow_nonnegativity.rs carry the other four state_layout_for clones, and src/cut/row.rs:369 and src/training/backward/duals_extraction.rs:105 each declare a further inline state_layout helper."
      },
      "yardstickSection": "§5.2 uniform test-support feature convention (the exported surface already owns these helpers)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5a-test-bloat-06",
      "id": "TD-046",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5a",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one `#[cfg(test)] pub(crate)` oracle declared in a file's production region with all six callers inside that file's own `mod tests`; scope and visibility exceed every caller — cosmetic homing",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Scope call for this item: whether relocating the oracle should also re-word the three production doc comments that use `shift_lag_state` as the canonical lag-remap vocabulary (the bracketed reference at `noise.rs:211` plus the code spans at `lp/indexer/state_space.rs:443` and `lp/indexer/cut_state_projection.rs:269`), or leave them naming a test-module-private symbol."
      ],
      "survivingClaim": "Within `crates/cobre-sddp/src/stochastic/noise.rs` the `#[cfg(test)] pub(crate) fn shift_lag_state` (176-197) is the file's only test-only item declared in its 560-line production region, and both its module scope and its `pub(crate)` visibility exceed every caller: all six call sites lie inside this same file's `mod tests` (561-EOF) and no other module in the workspace calls it. The hot-path-module framing does not survive, since `stochastic/noise.rs` is not one of the files the hot-path rules enumerate; nor does rather than beside the tests, since the oracle is in the same file 384 lines above its callers.",
      "title": "A test-only reference oracle for the lag-shift kernel is homed in the hot-path production module stochastic/noise.rs rather than beside the tests that are its only callers",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/stochastic/noise.rs",
          "symbol": "shift_lag_state"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise.rs",
          "symbol": "accumulate_and_shift_lag_state"
        },
        {
          "path": "crates/cobre-sddp/src/stochastic/noise.rs",
          "symbol": "tests"
        }
      ],
      "fixShape": "Move the oracle inside the `mod tests` block that already holds its only six call sites, dropping the cfg(test) attribute and the pub(crate) visibility that module scope forced on it, so the hot-path production file contains only production code above its test module. Keep the oracle itself and every equivalence assertion verbatim, since the equivalence between the oracle and the production kernel is what pins the lag-shift behavior. Byte-neutral by construction: relocating a cfg(test) fn into the test module that already calls it changes no production symbol, so the reproducibility and order-invariance harness, the parity goldens and the rank-invariance map are untouched and no golden moves.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/stochastic/noise.rs | sed -n '174,200p'; git show 077dbe2c:crates/cobre-sddp/src/stochastic/noise.rs | grep -n 'shift_lag_state\\|^mod tests\\|#\\[cfg(test)\\]'",
        "output": "176:#[cfg(test)]\n177:pub(crate) fn shift_lag_state(\n...\n232:pub(crate) fn accumulate_and_shift_lag_state(   <- the production kernel\n561:mod tests {\ncall sites of shift_lag_state: 1347 1365 1379 1397 1412 1572  (all inside `mod tests` at 561)\ntest fns: shift_lag_state_par0_is_noop 1341, _par1_single_hydro 1356, _par3_single_hydro 1370, _par1_two_hydros 1387, _preserves_storage 1403",
        "reading": "shift_lag_state at noise.rs:177 is annotated #[cfg(test)], so it cannot be reached from any production path, and all six of its call sites (1347, 1365, 1379, 1397, 1412, 1572) sit inside the `mod tests` block that starts at line 561. It is a reference oracle used to pin the production accumulate_and_shift_lag_state at line 232 by equivalence. The oracle itself is a legitimate and valuable pattern; the observation is only that it is declared at module scope in a hot-path production file, 384 lines above the only module that can call it, so a reader of the production kernel encounters test scaffolding inline in the file the architecture rules flag as hot path."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (test-only items live with their tests)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-architecture-00",
      "id": "CD-084",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5b",
      "severity": "B (A-risk)",
      "reviewerRating": "A",
      "downgradeReason": "latent at the pin (no shipped deck satisfies the two-part precondition), a single fill site, and the three sibling residue owners are correct — the blast radius is one function, not a spreading structure",
      "upgradeNote": null,
      "calibrationBasis": "a LATENT single-site contract divergence: `fill_anticipated_columns` is the only one of four ring-residue owners keying the deposit slot off the raw delivery axis instead of `PointResolution::ring_index`, contradicting its own rustdoc and the pinned deposit clause; the residues diverge only for a plant whose fixed post-horizon width is not a multiple of k_max AND which carries a class-3 in-study decision above that window — a bounded, wrong-but-compiling divergence that would write a wrong column bound silently, hence the A-risk marker (CD-074 class: pinned state coupled to a bounded column reconciled ad hoc per family)",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals; the anticipated ring is SDDP geometry)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction — asserted at the pin (no shipped deck reaches the precondition); a deck that does moves a golden legitimately — re-baseline decision at the owner gate for that case",
      "contractCited": [
        "In-LP anticipated ring: definition-row sign, hold carry & asymmetric masking",
        "State pinning uses column bounds, not equality rows",
        "The ring axis: the delivery axis with the fixed post-horizon window excised"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrowed to a LATENT single-site contract violation with a named precondition: fill_anticipated_columns (columns.rs:618) is the only one of four ring-residue owners that keys the deposit slot off the raw delivery axis instead of PointResolution::ring_index, contradicting both its own rustdoc at columns.rs:573-575 and the pinned deposit clause; the two residues diverge only for a plant whose fixed post-horizon width g is not a multiple of k_max AND which also holds a class-3 carried in-study decision above that window, in which case the latched slot stays frozen from fill_anticipated_slot_columns and the commitment is forced to zero (infeasible once min_generation_mw exceeds zero). Such a deck is accepted by cobre-io (thermal.rs:112/:117/:149 exempt a horizon-exceeding lead that reaches post-study), but NO shipped deck declares it - d55 is the only post-study example and resolves g equal to zero - so the defect is latent at the pin, no parity golden is wrong today, and the residue is the one-site routing change plus the missing excised-window column test, not the new shared walker the fixShape proposes.",
      "title": "fill_anticipated_columns re-opens the latched commitment slot at the RAW delivery-axis residue that its two sibling residue owners' own rustdoc calls the forbidden, wrong-but-compiling alternative",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/builder/columns.rs",
          "symbol": "fill_anticipated_columns"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
          "symbol": "fill_anticipated_state_out_def_entries"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/layout.rs",
          "symbol": "build_anticipated_slot_row_pos"
        }
      ],
      "fixShape": "Give the anticipated ring ONE residue owner in the fpha_cursor.rs style: hoist the delivery-axis to ring-axis conversion (point.ring_index(delivery_stage) with the same excised-window debug_assert entries.rs already carries) into a single walker in lp/builder that the row fill, the column fill and build_anticipated_slot_row_pos all drive off, so a one-sided edit that lands the deposit row and the re-opened column on different slots cannot compile; the change touches only which state column receives (-INF, +INF) rather than [0, 0] and so must cite .claude/rules/sddp.md section 'State pinning uses column bounds, not equality rows' (the re-open is a column-bound write, not a new equality row, and incoming pinning stays with fill_col_state_patches through state_to_lp_incoming_column); byte-neutrality bar is that no deck without a fixed post-horizon window changes a single column bound (ring_index is the identity on a contiguous ring, so parity goldens in tests/parity.rs and tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction are all bit-identical), and the only decks whose bytes move are the excised-window decks the current code renders wrong - a moved golden that must be regenerated with the defect named in the commit message.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/lp/builder/columns.rs | sed -n '604,624p'; git show 077dbe2c:crates/cobre-sddp/src/lp/builder/entries.rs | sed -n '155,169p'; git show 077dbe2c:crates/cobre-sddp/src/lp/builder/layout.rs | sed -n '694,724p'",
        "output": "columns.rs (fill_anticipated_columns): let point = anticipated_resolution_for(layout.state, AnticipatedLocal::new(local_idx), n_stages); let Some(delivery_stage) = point.genuine_decisions_at(stage_idx).next() else { continue; }; ... let slot = delivery_stage % layout.k_max; let state_out_col = ring.out_col(slot, local_idx);  ||  entries.rs (fill_anticipated_state_out_def_entries), same `point`, same `genuine_decisions_at`, same debug_assert: let r = point.ring_index(delivery_stage); debug_assert!(r.is_some(), \"...inside the excised fixed post-horizon window - never a ring member...\"); let slot = r.unwrap_or(delivery_stage) % layout.k_max; ring.emit_deposit(slot, local_idx, row, col_decision, col_entries);  and its rustdoc: \"`slot` is the RING-AXIS residue of the decision's own delivery target ([`PointResolution::ring_index`] owns the delivery-axis -> ring-axis map), never a distance-derived boundary. `delivery_stage % k_max` - keying directly off the raw delivery axis - is the forbidden alternative: it collides the first post-study deposit with the last in-study seed's slot whenever a plant declares a fixed post-horizon window.\"  ||  layout.rs (build_anticipated_slot_row_pos) walks the ring axis: for depth in 0..k_max { let r = stage_idx + depth + 1; let slot = r % k_max; for (plant, point) in points.iter().enumerate() { let m = point.physical_target(r); ... let is_deposit = point.decider.get(m).copied().flatten() == Some(stage_idx); let is_interior = !is_deposit && point.is_ready_at(m, stage_idx); if is_interior { row_pos[slot * n_anticipated + plant] = Some(n_reachable); n_reachable += 1; } } }  and its rustdoc: \"Walking the RAW delivery axis instead (a shared `m = stage_idx + depth + 1` read directly, pre-migration) is the wrong-but-compiling alternative once any plant's fixed post-horizon window excises part of the ring: `m mod k_max` is injective only over a contiguous run, which the excised window breaks.\"",
        "reading": "Three modules own the anticipated deposit-slot residue and only two agree. entries.rs::fill_anticipated_state_out_def_entries and layout.rs::build_anticipated_slot_row_pos both key off the RING axis (`point.ring_index(delivery_stage)` and the `r = stage_idx + depth + 1` ring walk); columns.rs::fill_anticipated_columns keys off the RAW delivery axis (`delivery_stage % layout.k_max`) eleven lines after the identical `point` is already in scope, i.e. the conversion it needs is available and unused. The two agreeing owners each name the raw form in their own rustdoc as the forbidden / wrong-but-compiling alternative, and both name the same precondition: a plant declaring a fixed post-horizon window, which excises part of the ring so the two axes stop agreeing. The consequence is not cosmetic. build_anticipated_slot_row_pos deliberately leaves a plant's OWN deposit slot masked (`is_interior = !is_deposit && ...`), and fill_anticipated_slot_columns then freezes every masked slot to [0, 0] via DeliveryRing::freeze_masked_columns, so the latched column depends on fill_anticipated_columns to re-open it. Under the in-tree fixture plant0_excised_window_g3_resolution (entries.rs, k_max = 4, g = 3, ring_index(7) == Some(4)) the deposit row lands on slot 4 % 4 == 0 while the column re-open lands on 7 % 4 == 3: the deposited column stays frozen at [0, 0] and an unrelated slot is opened. That forces the carried commitment to zero, or makes the stage LP infeasible once min_generation_mw > 0. entries.rs carries a regression test for exactly this axis choice (anticipated_deposit_targets_the_ring_axis_residue_across_an_excised_window, asserting slot 0 and NOT slot 3); columns.rs has no excised-window test, which is why the divergence compiles and ships."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-architecture-02",
      "id": "CD-085",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5b",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "the commitment-hold family is the only one of five state families without a typed `InCol`/`OutCol` resolver reachable from outside lp/, so the two extraction-side reads recompose the LP column by hand — CD-035 precedent (missing typed-role seam), Sev B; NOT a one-consumer seam: both consumers exist at the pin and the accessors complete an existing five-member family (owner question carried)",
      "category": "missing-seam",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV §IV.2 (lp/ indexer is engine-neutral emission logic the 0b carve lifts)",
      "defenderAlignmentHint": "advances-0b",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "State pinning uses column bounds, not equality rows"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Alignment-epic owner call: whether conflicts trigger 4 (one-consumer abstraction) reaches a purpose-named single-consumer StateSpace accessor that completes an existing five-member family, or is bounded to seams, traits and crates as its text states."
      ],
      "survivingClaim": "Only the two commitment-hold reads in simulation/extraction.rs (:268 incoming, :316 outgoing) are genuine untyped LP-column recompositions: the same file already resolves the storage, lag and bucket families' primal reads through the typed InCol/OutCol accessors, while the commitment family has no StateSpace-side accessor reachable from outside lp/ because its purpose-named owner DeliveryRing::out_col/in_col is only constructible through the pub(super) anticipated_ring helper. The three state-vector sites (commitment_reconcile.rs:193 with :208, setup/mod.rs:2651, :2667) are StateDim indices and not columns, and the DeliveryRing out_col versus slot_target transposition hazard is unreachable at every production call site.",
      "title": "Commitment hold is the only state family with no typed InCol/OutCol resolver, so four production sites recompose its column by hand as a bare usize - across two different index axes",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/indexer/state_space.rs",
          "symbol": "commitment_hold_in_study_offset"
        },
        {
          "path": "crates/cobre-sddp/src/lp/indexer/state_space.rs",
          "symbol": "bucket_incoming_col"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/delivery_ring.rs",
          "symbol": "out_col"
        }
      ],
      "fixShape": "Add the missing typed resolvers next to their siblings in state_space.rs - a commitment_hold_incoming_col returning InCol and a commitment_hold_outgoing_col returning OutCol, both routing through state_to_lp_incoming_column / state_to_lp_column exactly as bucket_incoming_col and storage_incoming_col already do - then convert the LP-column call sites (commitment_reconcile.rs, simulation/extraction.rs both sides) to them, leaving setup/mod.rs on an explicitly state-dimension-named accessor so the two axes stop sharing one expression; keep commitment_hold_in_study_offset as the shared slot arithmetic both resolvers call. Cites .claude/rules/sddp.md section 'State pinning uses column bounds, not equality rows' because the incoming resolver is precisely what that section requires every pin and dual read to resolve through, and no equality row is added. Byte-neutrality bar: each new resolver returns the same integer the arithmetic it replaces already produced, proven by an equivalence test asserting resolver equals hand-computed offset over every plant and slot, so parity goldens in tests/parity.rs and tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction are bit-identical with no golden moved. Optionally give DeliveryRing lane and slot newtypes so out_col and slot_target cannot be called with transposed operands, a types-only change with the same neutrality argument.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/lp/indexer/state_space.rs | sed -n '565,640p'; git grep -n 'commit_out\\.start\\|commit_in\\.start' 077dbe2c -- crates/cobre-sddp/src | grep -v tests; git show 077dbe2c:crates/cobre-sddp/src/lp/builder/delivery_ring.rs | grep -n 'pub.*fn \\(out_col\\|in_col\\|slot_target\\)'",
        "output": "state_space.rs typed resolvers: storage_incoming_col(h) -> InCol, storage_outgoing_col(h) -> OutCol, lag_incoming_col(lag, h) -> InCol, bucket_incoming_col(b) -> InCol, bucket_outgoing_col(b) -> OutCol - each routing through state_to_lp_incoming_column / state_to_lp_column. The commitment family's only accessor is `pub(crate) fn commitment_hold_in_study_offset(&self, plant: usize, m: usize) -> usize` (line 571), a bare offset with no resolver sibling.  ||  Non-test production sites recomposing the column by hand: commitment_reconcile.rs:193 `let commit_out_start = pins.state_layout.commit_out.start;` then :208 `.commitment_hold_in_study_offset(local, pins.stage_idx)`; simulation/extraction.rs:268 `let col = spec.state.commit_in.start + slot_offset;`; simulation/extraction.rs:316 `state.commit_out.start + state.commitment_hold_in_study_offset(local, m)`; setup/mod.rs:2651 and :2667 `let off = layout.commit_out.start + layout.commitment_hold_in_study_offset(local_idx, slot);` then `state[off] = history.value_mw;`.  ||  delivery_ring.rs addressing API: `pub fn out_col(&self, slot: usize, lane: usize) -> usize`, `pub fn in_col(&self, slot: usize, lane: usize) -> usize`, `pub(crate) fn slot_target(&self, lane: usize, lag: usize) -> usize`.  ||  state_dim_commitment_hold_range() = [N*(1+L)+B, N*(1+L)+B+A*k_max), numerically equal to the commit_out LP-column start in the StateSpace column diagram.",
        "reading": "Every other state family carries a typed resolver returning InCol or OutCol, and entity_index.rs pins six cross-family swaps with compile_fail doctests, so the crate's stated discipline is that a resolved column is a newtype and not a usize. The commitment-hold family is the exception, and the exception has a concrete cost visible at the call sites. First, setup/mod.rs:2651 indexes a STATE VECTOR with `commit_out.start + commitment_hold_in_study_offset(..)` while simulation/extraction.rs:316 uses the identical expression as an LP COLUMN; both are correct today only because the outgoing state region maps to LP columns by identity, so the two axes coincide numerically - nothing in the types records which axis a given `off` belongs to, and the coincidence is what makes a mis-axis edit compile. Second, simulation/extraction.rs:268 builds an INCOMING commitment column arithmetically (`commit_in.start + slot_offset`) instead of routing through state_to_lp_incoming_column, which is the resolver the pinning contract names as the single owner of incoming-column resolution, and which the sibling families' bucket_incoming_col / storage_incoming_col / lag_incoming_col all use. Third, the ring's own addressing API inverts operand order between out_col(slot, lane) and slot_target(lane, lag) with identical bare usize parameters, so an in-range transposition silently addresses another lane's slot; the debug asserts catch only out-of-range arguments. This is the same swap class entity_index.rs already decided was worth a compile_fail doctest for the entity families."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-over-engineering-00",
      "id": "OD-037",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5b",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "zero consumers, three in-file tests and two rustdoc links are the whole blast radius; OD-032 (zero-consumer public predicate) calibrated C",
      "upgradeNote": null,
      "calibrationBasis": "two `pub` newtypes with no non-test, non-doc consumer at the pin (three round-trip tests and a re-export are their whole footprint); the delete-or-register disposition is one edit — OD-032 precedent, Sev C; the semver-major removal is a scheduling call (CD-019 batching precedent), not severity",
      "category": "speculative-generality",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV §IV.2 (lp/ indexer moves with the cobre-model carve-out)",
      "defenderAlignmentHint": "advances-0b",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Scheduling only: whether the public-API removal of Col and Row ships as its own semver-major break or waits for the 0b cobre-model carve-out of lp/, as the CD-019 deferral precedent suggests — the defect itself is settled."
      ],
      "survivingClaim": "Among the types declared in lp/indexer/index.rs, only Col and Row have no non-test, non-doc production consumer at the pin — their sole references are the mod.rs:119 re-export, three in-file round-trip tests and two rustdoc links — and Col's own doc names set_col_bounds as its seam although that seam is usize-only at cobre-solver/src/trait_def.rs:90; Boundary is excluded from the residue because block_storage_col consumes it, and because lib.rs:69 makes both names public items the removal is a semver-major deletion that batches onto the 0b carve-out of lp/, not a standalone break.",
      "title": "The generic Col and Row LP-index newtypes have zero consumers: the typed-address vocabulary their module doc describes is carried entirely by StateDim, CutSlot, BlockIdx, InCol and OutCol",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/indexer/index.rs",
          "symbol": "Col"
        },
        {
          "path": "crates/cobre-sddp/src/lp/indexer/index.rs",
          "symbol": "Row"
        }
      ],
      "fixShape": "Delete the Col and Row declarations and their two impl blocks from lp/indexer/index.rs, delete the three tests that exist only to exercise them (col_is_zero_cost_and_round_trips, row_is_zero_cost_and_round_trips, col_equality_is_value_based_not_identity), drop the two names from the pub use in lp/indexer/mod.rs, and rewrite the index.rs module-doc sentence so the typed vocabulary it introduces lists only the types that exist: StateDim, CutSlot, BlockIdx, the InCol/OutCol role split and Boundary. Byte-neutral by construction, because the census proves zero production call sites, so no LP column, row, coefficient or bound moves; still assert the parity goldens in tests/parity.rs through tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction, and move no golden. The blast radius is the public API: lib.rs re-exports the module (pub use lp::indexer), so both names are public items of cobre-sddp and the deletion is a public-API removal with no in-repo consumer; the natural batching is the next public-API break, which the 0b carve-out of cobre-model already forces on this very module.",
      "evidence": {
        "command": "git grep -n -E '\\b(Col|Row)::new\\b' 077dbe2c -- crates/ ; git grep -n -E '\\b(Col|Row)::new\\b|:\\s*(Col|Row)\\b|->\\s*(Col|Row)\\b' 077dbe2c -- crates/ | grep -v 'lp/indexer/index.rs' ; git grep -n -w Col 077dbe2c -- crates/ | grep -v 'lp/indexer/index.rs' | grep -vE '//[!/]' ; for s in StateDim CutSlot BlockIdx InCol OutCol Col; do echo -n \"$s: \"; git grep -l -w \"$s\" 077dbe2c -- crates/cobre-sddp/src | grep -v 'lp/indexer/index.rs' | grep -cv 'lp/indexer/mod.rs'; done",
        "output": "$ git grep -n -E '\\b(Col|Row)::new\\b' 077dbe2c -- crates/\n077dbe2c:crates/cobre-sddp/src/lp/indexer/index.rs:265:        assert_eq!(Col::new(42).get(), 42);\n077dbe2c:crates/cobre-sddp/src/lp/indexer/index.rs:286:        assert_eq!(Row::new(7).get(), 7);\n077dbe2c:crates/cobre-sddp/src/lp/indexer/index.rs:312:        assert_eq!(Col::new(3), Col::new(3));\n077dbe2c:crates/cobre-sddp/src/lp/indexer/index.rs:313:        assert_ne!(Col::new(3), Col::new(4));\n(4 hits, all inside index.rs's own #[cfg(test)] mod tests)\n\n$ (typed uses outside index.rs, repo-wide)\n077dbe2c:crates/cobre-io/src/config/simulation.rs:149:        assert_eq!(solver.price, Some(PriceStrategy::Row));\n077dbe2c:crates/cobre-io/src/config/training.rs:701:        assert_eq!(backward.price, Some(PriceStrategy::Row));\n077dbe2c:crates/cobre-sddp/src/solve/solver_phase.rs:506:        PriceStrategy::Row => 1,\n077dbe2c:crates/cobre-sddp/src/solve/solver_phase.rs:1285:                price: Some(PriceStrategy::Row),\n077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs:4123:                price: Some(cobre_io::config::PriceStrategy::Row),\n077dbe2c:crates/cobre-sddp/tests/mpi_wire.rs:482:            price: Some(PriceStrategy::Row),\n(every hit is the unrelated cobre_io::config::PriceStrategy::Row variant)\n\n$ (word Col outside index.rs, comments stripped)\n077dbe2c:crates/cobre-sddp/src/lp/indexer/mod.rs:119:pub use index::{BlockIdx, Boundary, Col, CutSlot, InCol, OutCol, Row, StateDim};\n\n$ (consumer-file count per newtype, excluding index.rs and the indexer/mod.rs re-export)\nStateDim: 8\nCutSlot: 5\nBlockIdx: 11\nInCol: 2\nOutCol: 3\nCol: 1   <- the single hit is two doc-comment prose words (\"Col capacity\") in lp/builder/patch.rs:101,127, not the type",
        "reading": "Nothing in the repository constructs or names Col or Row as a type outside index.rs itself. The only non-declaration mention of Col in the crate is the re-export at lp/indexer/mod.rs:119; the only typed Row matches repo-wide are the unrelated cobre_io PriceStrategy::Row variant; the one remaining Col hit is prose inside a patch.rs doctest comment. Three tests in index.rs exist solely to give the two unused types a test: col_is_zero_cost_and_round_trips, row_is_zero_cost_and_round_trips and col_equality_is_value_based_not_identity. The five sibling newtypes declared in the same file are genuinely earned by 8, 5, 11, 2 and 3 consumer files respectively, so the finding is scoped to Col and Row alone. Neither carries #[allow(dead_code)] (both are pub and re-exported, so no lint fires to mark them reserved), neither appears in the mirror's Reserved-seam (Voice 4) class, and neither is on the prior-register roster, so this is not a sanctioned reserved seam. The module doc's own closing sentence concedes the seam these two were introduced for stays untyped: every type extracts its raw usize via get() for the solver-FFI seam, which stays usize-only per the infra-genericity rule.",
        "line": null
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-over-engineering-01",
      "id": "OD-038",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5b",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "a single zero-consumer type and one false doc sentence; the formula contradiction the reviewer weighed did not survive the defender",
      "upgradeNote": null,
      "calibrationBasis": "one `pub` type with no construction or read site outside its own smoke test, plus a module-doc carrier sentence that is false for it; the row-formula contradiction is conceded — OD-032 precedent, Sev C; public-API removal batching per CD-019 is the owner's call",
      "category": "speculative-generality",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV §IV.2 (lp/ indexer moves with the cobre-model carve-out)",
      "defenderAlignmentHint": "advances-0b",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "FPHA uses average storage",
        "Hydro-cell aggregation assumes one production map per cell"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Owner call carried over from the attacker: deleting FphaRowRange is a public-API removal (reachable as cobre_sddp::indexer::FphaRowRange via lib.rs:69) with no in-repo consumer; decide whether it lands now as ordinary cleanup or is batched with the Phase 0b cobre-model carve-out that relocates lp/indexer anyway, per the CD-019 deferral precedent.",
        "Scope call: the fix as filed leaves the same per-plant framing live in BlockGrid::advance_fpha_base's rustdoc (block_grid.rs:106-107), so the reader hazard is only half retired; decide whether that sibling doc correction joins this item or gets its own."
      ],
      "survivingClaim": "FphaRowRange (lp/indexer/layout.rs:34-40) has no construction or read site anywhere under crates/ outside its own cfg(test) Debug/Copy smoke test, yet is public API through lib.rs:69, and the satellite-types carrier sentence at layout.rs:5-7 is false for it because StageGeometry exposes FPHA rows only as the flat fpha: row_fpha_start()..fpha_rows_end range (builder/layout.rs:1905), never as a Vec<FphaRowRange>. Narrowed from the title on two counts: the documented row formula does NOT contradict the walker's stride, since start + k * planes_per_block + p reproduces BlockGrid::fpha_plane (block_grid.rs:95-104) exactly and only the granularity of start diverges, per-plant in the doc against the per-CELL re-base in for_each_fpha_plane (fpha_cursor.rs:80-97); and that divergence is latent rather than a live mis-address, because the hydro-cell partition is the identity for every shipping study (hydro_cell.rs:5-10, builder/layout.rs:1282-1284), so no in-repo deck can be mis-addressed by it. The stale per-plant framing is also not unique to the dead type and survives the fix as filed in BlockGrid::advance_fpha_base's rustdoc (block_grid.rs:106-107).",
      "title": "FphaRowRange is a zero-consumer public type whose module doc claims a production carrier that does not exist and whose row formula contradicts the live FPHA walker",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/indexer/layout.rs",
          "symbol": "FphaRowRange"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/fpha_cursor.rs",
          "symbol": "for_each_fpha_plane"
        }
      ],
      "fixShape": "Delete the FphaRowRange declaration and the fpha_row_range_debug_clone_copy smoke test from lp/indexer/layout.rs, drop the name from the pub use in lp/indexer/mod.rs, narrow both module docs (the lp/indexer/layout.rs header and the satellite-types bullet in lp/indexer/mod.rs) to describe only EvaporationIndices, and repoint the sentence in lp/builder/fpha_cursor.rs that says it matches FphaRowRange::start at StageLayout::row_fpha_start, the value for_each_fpha_plane actually seeds its cursor from. Contract-first: the change touches no FPHA row emission, so neither the FPHA uses average storage section nor the Hydro-cell aggregation assumes one production map per cell section of .claude/rules/sddp.md is weakened; the cell-major nesting those sections rest on stays owned by for_each_fpha_plane, which is exactly why the dead type's per-plant formula must not survive as documentation of it. Byte-neutral by construction, since the type has zero construction sites and only prose changes accompany the deletion; assert the parity goldens in tests/parity.rs through tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction, and move no golden. Blast radius is the same public surface as the sibling finding, since lib.rs re-exports the indexer module.",
      "evidence": {
        "command": "git grep -n '\\bFphaRowRange\\b' 077dbe2c -- crates/ ; git grep -c 'FphaRowRange' 077dbe2c -- crates/cobre-sddp/src/lp/builder/template.rs crates/cobre-sddp/src/lp/builder/layout.rs ; git grep -c 'EvaporationIndices' 077dbe2c -- crates/cobre-sddp/src ; git show 077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs | sed -n '1,9p;28,39p' ; git show 077dbe2c:crates/cobre-sddp/src/lp/builder/fpha_cursor.rs | sed -n '32,39p;54p'",
        "output": "$ git grep -n '\\bFphaRowRange\\b' 077dbe2c -- crates/\n077dbe2c:crates/cobre-sddp/src/lp/builder/fpha_cursor.rs:39:/// row-sortedness assertion. Matches `FphaRowRange::start`.\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:2://! [`FphaRowRange`].\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:34:pub struct FphaRowRange {\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:43:    use super::{EvaporationIndices, FphaRowRange};\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:62:        let r = FphaRowRange {\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:70:        assert!(debug_str.contains(\"FphaRowRange\"));\n077dbe2c:crates/cobre-sddp/src/lp/indexer/mod.rs:51://!   and [`FphaRowRange`] (locating one hydro's evaporation columns/row and FPHA\n077dbe2c:crates/cobre-sddp/src/lp/indexer/mod.rs:120:pub use layout::{EvaporationIndices, FphaRowRange};\n(8 hits total: the declaration, its own #[cfg(test)] smoke test at :43/:62/:70, two module-doc lines, the pub use, and one prose cross-reference)\n\n$ git grep -c 'FphaRowRange' 077dbe2c -- .../builder/template.rs .../builder/layout.rs\n(no output: zero hits in the file that declares StageGeometry and zero in the file that declares StageLayout)\n\n$ git grep -c 'EvaporationIndices' 077dbe2c -- crates/cobre-sddp/src\n077dbe2c:crates/cobre-sddp/src/lp/builder/layout.rs:4\n077dbe2c:crates/cobre-sddp/src/lp/builder/mod.rs:1\n077dbe2c:crates/cobre-sddp/src/lp/builder/template.rs:2\n077dbe2c:crates/cobre-sddp/src/lp/generic_constraints.rs:2\n077dbe2c:crates/cobre-sddp/src/lp/indexer/layout.rs:6\n077dbe2c:crates/cobre-sddp/src/simulation/extraction/tests.rs:3\n\n$ lp/indexer/layout.rs 1-9 and 28-39\n//! Per-stage geometry satellite types: [`EvaporationIndices`] and\n//! [`FphaRowRange`].\n//!\n//! Each locates a single hydro's evaporation columns/row or FPHA row block\n//! within one stage LP. They are produced by the per-stage\n//! [`StageLayout`](crate::lp_builder) and carried on its\n//! [`StageGeometry`](crate::lp_builder::StageGeometry) snapshot; ...\n/// Locates the block of FPHA hyperplane rows assigned to a single FPHA hydro\n/// within a stage LP. Rows for hydro `i` at block `k` and plane `p` are at:\n/// `start + k * planes_per_block + p`.\npub struct FphaRowRange {\n    pub start: usize,\n    pub planes_per_block: usize,\n}\n\n$ lp/builder/fpha_cursor.rs 32-39 and 54\n/// The row-block base advances once per CELL, not once per plant: each cell owns\n/// its own contiguous `n_blks * n_planes` row block. ... Matches `FphaRowRange::start`.\n    let mut fpha_block_start = layout.row_fpha_start();",
        "reading": "All eight repo-wide mentions of FphaRowRange are the declaration, its own Debug/Clone smoke test, two module-doc lines, the re-export at lp/indexer/mod.rs:120 and one prose cross-reference. No code anywhere constructs or reads the type. Two separate claims in the doc are false. First, the carrier claim: the module doc says these satellite types are produced by StageLayout and carried on its StageGeometry snapshot, which holds for the sibling EvaporationIndices (four production consumer files: builder/layout.rs, builder/mod.rs, builder/template.rs, lp/generic_constraints.rs) but not for FphaRowRange, which does not appear at all in builder/template.rs where StageGeometry is declared or in builder/layout.rs where StageLayout is declared. Second, the addressing claim: the documented formula start + k * planes_per_block + p is per-plant, while the live walker for_each_fpha_plane seeds fpha_block_start from layout.row_fpha_start() and its own doc states that the row-block base advances once per CELL, not once per plant, with that nesting called load-bearing for CSC row-sortedness. A reader who addressed FPHA rows through the documented formula would compute wrong rows for any multi-cell hydro. The type carries no #[allow(dead_code)] and no Reserved-seam rationale, and it is absent from the mirror's Reserved-seam (Voice 4) class and from the prior-register roster, so it is not a sanctioned seam.",
        "line": null
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-performance-00",
      "id": "PD-038",
      "class": "PD",
      "lens": "performance",
      "subStation": "5b",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one per-solve loop re-runs the four-region `classify` membership test per state element for a map fixed at `StateSpace::new`, while the two other production consumers read a precomputed table — an uncached twin on the per-solve pin path; structural calibration only, UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "State pinning uses column bounds, not equality rows"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "Narrowed to one loop: `fill_col_state_patches` (patch.rs:257-260) re-runs `classify`'s four-way region membership test and region select once per state element on every prepared solve, for a state-dim-to-incoming-column map that is fixed at `StateSpace::new` and that both other production consumers read from a precomputed table. Stripped from the claim: (i) the twin-cache symmetry as evidence, since `state_to_lp_column_map` amortizes the outgoing lag arm's `offset % n` / `offset / n` (state_space.rs:478-479) that the incoming resolver does not contain; (ii) the filed per-element cost model of a Range reconstruction per probe plus two further boundary derivations, since those are loop-invariant through `#[inline]` accessors on non-aliasing `&StateSpace` fields; (iii) the extraction wrappers `storage_incoming_col` / `lag_incoming_col` / `bucket_incoming_col` at simulation/extraction.rs:376,920,924,2065, which run per extracted stage rather than per solve.",
      "title": "The incoming state-to-LP-column resolver is uncached on the per-solve pin path while its outgoing twin and the cut-extraction path both cache it",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/builder/patch.rs",
          "symbol": "fill_col_state_patches"
        },
        {
          "path": "crates/cobre-sddp/src/lp/indexer/state_space.rs",
          "symbol": "state_to_lp_incoming_column"
        },
        {
          "path": "crates/cobre-sddp/src/lp/indexer/state_space.rs",
          "symbol": "classify"
        }
      ],
      "fixShape": "Give the incoming direction the cache the outgoing direction already has: an incoming twin of state_to_lp_column_map on StateSpace, filled by an incoming twin of finalize_state_column_map that calls state_to_lp_incoming_column for every j in [0, n_state) -- a pure cache of the resolver, never a second copy of its arithmetic, which is the discipline finalize_state_column_map's own rustdoc already states -- and let fill_col_state_patches read it, with storage_incoming_col / lag_incoming_col / bucket_incoming_col following the same route so the simulation extraction path benefits without a second cache. The debug_assert on the finalized length that lp_column_for_state carries transfers unchanged. .claude/rules/sddp.md Sec. State pinning uses column bounds, not equality rows is untouched: the same incoming columns are pinned with the same column bounds, and the resolver remains the single authority for the arithmetic. Byte-neutral by construction -- every offset is a pure function of (N, L, B, A, k_max) as the state_space.rs module doc states, so a table lookup returns the identical column for every j and fill_col_state_patches emits identical col_indices / col_lower / col_upper; the parity goldens (tests/parity.rs, tests/common/parity_hash.rs), the rank-invariance harness (tests/common/permute.rs) and mpiexec -n 1 vs -n 2 all see byte-identical solver submissions and no golden moves. This station executes no measurement; the perf epic profiles it on its deck.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/lp/builder/patch.rs | sed -n '243,262p'; git show 077dbe2c:crates/cobre-sddp/src/lp/indexer/state_space.rs | sed -n '429,436p;489,499p;508,515p;540,543p'; git grep -n 'state_to_lp_incoming_column(' 077dbe2c -- crates/cobre-sddp/src/lp/builder/patch.rs crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs; git grep -n 'storage_incoming_col(\\|lag_incoming_col(\\|bucket_incoming_col(' 077dbe2c -- crates/cobre-sddp/src/simulation/extraction.rs",
        "output": "    pub fn fill_col_state_patches(\n        &mut self,\n        state_layout: &StateSpace,\n        state: &[f64],\n        col_scale: &[f64],\n    ) {\n...\n        for (j, &sv) in state.iter().enumerate() {\n            let col = state_layout\n                .state_to_lp_incoming_column(StateDim::new(j))\n                .get();\n...\n    pub(crate) fn classify(&self, j: StateDim) -> (StateRegion, usize) {\n        let j = j.get();\n        let region = REGION_ORDER\n            .into_iter()\n            .find(|&region| self.state_dim_range(region).contains(&j))\n            .unwrap_or(StateRegion::CommitmentHold);\n        (region, j - self.state_dim_range(region).start)\n    }\n...\n    /// Fill [`Self::state_to_lp_column_map`] by calling\n    /// [`Self::state_to_lp_column`] for every `j` in `[0, n_state)` -- a pure cache\n    /// of the resolver, never a reimplementation of its arithmetic.\n    pub fn finalize_state_column_map(&mut self) {\n...\n    pub fn lp_column_for_state(&self, j: StateDim) -> OutCol {\n...\n        self.state_to_lp_column_map[j]\n...\n    pub fn state_to_lp_incoming_column(&self, j: StateDim) -> InCol {\n        let (region, offset) = self.classify(j);\n        InCol::new(self.incoming_block_start(region) + offset)\n    }\n...\ncrates/cobre-sddp/src/lp/builder/patch.rs:259:                .state_to_lp_incoming_column(StateDim::new(j))\ncrates/cobre-sddp/src/lp/indexer/cut_state_projection.rs:91:            incoming_columns.push(global.state_to_lp_incoming_column(g));\n...\ncrates/cobre-sddp/src/simulation/extraction.rs:376:            view.primal[state.bucket_incoming_col(b).get()]\ncrates/cobre-sddp/src/simulation/extraction.rs:920:        let storage_initial = view.primal[state.storage_incoming_col(h).get()];\ncrates/cobre-sddp/src/simulation/extraction.rs:924:            view.primal[state.lag_incoming_col(0, h).get()]\ncrates/cobre-sddp/src/simulation/extraction.rs:2065:                    inflow_m3s: view.primal[state.lag_incoming_col(l, h).get()],\n",
        "reading": "patch.rs:259 calls state_to_lp_incoming_column once per state element inside fill_col_state_patches, and StageSolvePrep::run executes fill_col_state_patches on every solve on all four hot paths (forward solve/stage_solve.rs, backward training/backward/lp_setup.rs::patch_opening_bounds, simulation/pipeline.rs, training/lower_bound.rs). Each call runs classify(), a linear find over the four REGION_ORDER regions that reconstructs a Range<usize> per probe, then derives the offset with a second state_dim_range(region).start call; state_to_lp_incoming_column adds a third four-arm match (incoming_block_start). The outgoing twin does none of this at call time: state_to_lp_column_map is filled once by finalize_state_column_map and lp_column_for_state is a plain indexed read. The incoming direction ALREADY has this cache for the extraction path -- cut_state_projection.rs:91 fills the incoming_columns field once in CutStateProjection::new, whose field doc reads 'LP incoming column per cut slot (extraction hot path)'. So the pinning path is the only production consumer that re-resolves. Two further production consumers reach the same uncached resolver through the three StateSpace wrappers: simulation/extraction.rs at 376, 920, 924 and 2065 resolve a bucket, storage and lag column per entity on every extracted stage (all four are above that file's first #[cfg(test)] at 2156; extraction.rs is 5d and is cited here for frequency only, never anchored). The only other state_to_lp_incoming_column call sites in the crate are inside #[cfg(test)] regions (entries.rs:8892 with the file's first #[cfg(test)] at 1680, cut_state_projection.rs:359/683/928 above its 292 non-test lines, state_space.rs:967+ above its 729, duals_extraction.rs:151/173, test_support.rs)."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/lp/builder/patch.rs::fill_col_state_patches",
      "exercisingCallSites": [
        "training/stage_solve_prep.rs · StageSolvePrep::run (every prepared solve)",
        "training/backward/* · patch_opening_bounds (per opening)"
      ],
      "mechanism": "redundant per-element index recomputation on every solve: a four-region linear scan with a Range reconstruction per probe plus two further region-boundary derivations per state element, where a precomputed table already exists for the twin direction and for the extraction path",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-performance-01",
      "id": "PD-039",
      "class": "PD",
      "lens": "performance",
      "subStation": "5b",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "for a child run with more than one opening, the opening-invariant column-bound pin is re-filled and re-submitted per opening (one fill over n_state plus one `set_col_bounds` the CLP backend expands to a full-length FFI push), and the commitment relaxation likewise when anticipated geometry is present — redundant per-opening work on the backward hot path; the three governing contracts hold under the hoist (same columns, same bounds, NCS patch still per opening, opening order untouched); structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction — asserted (bit-identical bounds already in force are simply not re-pushed); the owner gate decides whether dropping an idempotent re-push counts as a warm-start-chain change that must clear the `opening_order_determinism` gate before it lands",
      "contractCited": [
        "Backward opening order is warm-start-only",
        "Lower-bound evaluation must patch NCS",
        "State pinning uses column bounds, not equality rows"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "Owner call: whether removing a bit-identical repeat submission of already-in-force column bounds counts as a warm-start-chain change that must clear the parity goldens, the rank-invariance harness and the opening_order_determinism gate before the hoist lands, given that the CLP backend's set_row_bounds rustdoc asserts factorization/basis preservation across a bound patch but nothing in this repo pins that property for Clp_chgColumn*."
      ],
      "survivingClaim": "For a Generated child run carrying more than one opening, the per-opening backward prep redundantly re-fills and re-submits the opening-invariant column-bound state pin from the unchanged x_hat and col_scale, costing one fill over n_state plus one set_col_bounds per opening that the CLP backend expands into a full-length col_lower and col_upper FFI push; the opening-invariant commitment relaxation is additionally re-filled per opening only when n_anticipated is nonzero and the stage has geometry, and re-submitted only when the relaxation set is nonempty. The claim is redundant call frequency alone: no pinned contract is violated, patch.rs's 'once per stage-visit' rustdoc is a value-semantics contract the current code already satisfies, and the External single-opening branch carries no redundancy.",
      "title": "The backward per-opening prep recomputes and re-submits an opening-invariant column-bound state pin and an opening-invariant commitment relaxation once per opening",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/builder/patch.rs",
          "symbol": "fill_col_state_patches"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/commitment_reconcile.rs",
          "symbol": "fill_bound_relaxations"
        }
      ],
      "fixShape": "Hoist the opening-invariant work to per-child: fill and submit the column-bound state pin (fill_col_state_patches plus its set_col_bounds) and fill the commitment relaxation once alongside load_backward_lp, and narrow the per-opening entry point to what actually varies -- the row patches from the transformed inflow/load noise and the NCS availability column patch. All three backward drivers reach this through the single helper patch_opening_bounds, so the blast radius is that helper plus a parameterization of StageSolvePrep::run (a per-child prep and a per-opening prep), with no driver-by-driver edit. Three pinned contracts govern it. .claude/rules/sddp.md Sec. State pinning uses column bounds, not equality rows: the pin stays a column-bound pin at the same incoming columns and merely recovers the once-per-stage-visit identity patch.rs's rustdoc already declares. Sec. Lower-bound evaluation must patch NCS: the NCS patch stays per opening and must not be hoisted with it. And the ordering rule reconcile_commitments carries in its rustdoc -- it runs last because the template reload and the bound patches reset the columns it relaxes -- so the hoist is valid only if the hoisted relaxation still runs after every column submission that touches the anticipated columns it relaxes; at the pin the sole remaining per-opening column submission is the NCS patch, which addresses a different column family, and the fix must verify that disjointness rather than assume it. Byte-neutrality is asserted on the data and validated, not assumed on the solver: every bound value and index in force before each solve is unchanged, but the NUMBER of set_col_bounds calls the solver sees per opening changes, which is the same class of perturbation Sec. Backward opening order is warm-start-only describes for the warm-start chain (a differently-warmed solve may settle on a different-but-equally-valid vertex at a degenerate optimum, the divergence the Cobre determinism contract permits). The fix is therefore gated on the parity goldens (tests/parity.rs, tests/common/parity_hash.rs), the rank-invariance harness (tests/common/permute.rs) and mpiexec -n 1 vs -n 2, plus the opening_order_determinism gate in tests/mpi_wire.rs; if a golden moves, the baseline is the arbiter and the hoist is reconsidered, not rebaselined.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/training/backward/replicated.rs | sed -n '141,144p'; git show 077dbe2c:crates/cobre-sddp/src/training/stage_solve_prep.rs | sed -n '127,131p;155,161p;230,242p'; git grep -n 'load_backward_lp(\\|patch_opening_bounds(' 077dbe2c -- crates/cobre-sddp/src/training/backward",
        "output": "        for local_omega in local_start..local_end {\n            let raw_noise = tree_view.opening(s.0, local_omega);\n            patch_opening_bounds(ws, ctx, training_ctx, raw_noise, x_hat, s)?;\n...\n        patch_buf.fill_col_state_patches(\n            training_ctx.state,\n            pinned_state,\n            &ctx.template(stage).col_scale,\n        );\n...\n        let cp = patch_buf.state_col_patch_count();\n        solver.set_col_bounds(\n            &patch_buf.col_indices[..cp],\n            &patch_buf.col_lower[..cp],\n            &patch_buf.col_upper[..cp],\n        );\n...\n        fill_bound_relaxations(\n            &DeliveryPins {\n                state_layout: training_ctx.state,\n                pinned_state,\n                template: ctx.template(stage),\n                geometry,\n                anticipated_thermal_indices: &training_ctx.study_dims.anticipated_thermal_indices,\n                n_blks: ctx.block_count(stage),\n                stage_idx: stage.0,\n                n_stages: training_ctx.horizon.num_stages(),\n            },\n            &mut patch_buf.commitment_relax,\n        )?;\n...\ncrates/cobre-sddp/src/training/backward/by_node.rs:245:                    load_backward_lp(ws, &child);\ncrates/cobre-sddp/src/training/backward/by_node.rs:284:                        patch_opening_bounds(ws, ctx, training_ctx, raw_noise, x_hat, s)?;\ncrates/cobre-sddp/src/training/backward/by_scenario.rs:85:                load_backward_lp(ws, child);\ncrates/cobre-sddp/src/training/backward/by_scenario.rs:187:        patch_opening_bounds(ws, ctx, training_ctx, raw_noise, x_hat, s)?;\ncrates/cobre-sddp/src/training/backward/by_scenario.rs:305:        patch_opening_bounds(ws, ctx, training_ctx, raw_noise, x_hat, s)?;\ncrates/cobre-sddp/src/training/backward/replicated.rs:101:    super::lp_setup::load_backward_lp(ws, child);\ncrates/cobre-sddp/src/training/backward/replicated.rs:143:            patch_opening_bounds(ws, ctx, training_ctx, raw_noise, x_hat, s)?;\n",
        "reading": "load_backward_lp runs once per child ('The LP structure is identical across a child's openings, so only bound patching runs per opening') and patch_opening_bounds runs once per opening with the SAME x_hat -- x_hat is loop-invariant in replicated.rs's opening loop and in the by_node.rs / by_scenario.rs loops, which is the whole per-opening entry point (load once at by_node.rs:245, by_scenario.rs:85, replicated.rs:101; patch at by_node.rs:284, by_scenario.rs:187 and :305, replicated.rs:143). patch_opening_bounds delegates to StageSolvePrep::run, which unconditionally re-runs fill_col_state_patches over pinned_state (= x_hat) and re-submits the result through solver.set_col_bounds, then re-runs fill_bound_relaxations, every one of whose eight DeliveryPins inputs is opening-invariant (state_layout, pinned_state, template, geometry, anticipated_thermal_indices, n_blks, stage_idx, n_stages) and whose result is re-submitted through a second set_col_bounds. The only genuinely per-opening work in that sequence is the transformed inflow/load noise driving the ROW patches and the NCS availability COLUMN patch. patch.rs's own rustdoc on fill_col_state_patches states the intended frequency in the opposite direction -- 'pin this once per stage-visit (the trial point / decision-driven state), never per opening ... (contrast with NCS availability, which patches per opening)' -- so the code re-pins per opening what its contract calls a per-stage-visit pin. This is a call-frequency claim about opening-invariant inputs; it is not CD-074's drift-classification claim about drift_margin's constants, and it proposes no change to that classification."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/lp/builder/patch.rs::fill_col_state_patches",
      "exercisingCallSites": [
        "training/backward/by_scenario.rs · patch_opening_bounds (:187, :305)",
        "training/backward/by_node.rs · patch_opening_bounds (:284)",
        "training/backward/replicated.rs · patch_opening_bounds (:143)"
      ],
      "mechanism": "redundant per-opening recomputation plus a repeated FFI bound-submission crossing for an opening-invariant column-bound pin and an opening-invariant commitment relaxation",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-test-bloat-02",
      "id": "TD-047",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5b",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one inline test module of 6.5k lines and 77 tests named for its least-exercised subject with no internal partition; the split shape is the CD-007 convention call — inline-test giants calibrate C (CD-007 precedent)",
      "category": "god-module",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (the 0b carve lifts lp/builder emission logic; its tests move with it)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-007"
      ],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Target shape for the split is a convention call CD-007 owns: one extracted tests.rs matching commitment_reconcile/layout/template, or N subject-named files matching columns.rs's 12 inline modules. The unratified section 5.1 threshold cannot settle it."
      ],
      "survivingClaim": "Scoped to crates/cobre-sddp/src/lp/builder/entries.rs alone: its inline module pumping_water_tests (3518-10093) is named for the least-exercised subject it contains and carries no internal partition, evidenced at the pin by 3 call sites of fill_pumping_water_entries against 25 of build_stage_matrix_entries, 36 helper fns declared from module offset 46 to 6440, and one CSC accessor duplicated about 2000 lines apart (coeff_at at 2431, csc_at at 4395), while sibling columns.rs in the same directory partitions 72 tests across 12 subject-named modules. Not conceded: the 'crate's catch-all' framing, and the fixShape's 'pure test relocation' premise, because the 72-reference module-private PumpFixtures harness must gain sibling visibility and the read_dir-based lp_builder_never_references_dual_extraction gate's scan surface depends on where the split files land.",
      "title": "entries.rs's `pumping_water_tests` has become the crate's catch-all test dump: 6576 lines and 77 test fns under a name that describes one entry family",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
          "symbol": "pumping_water_tests"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
          "symbol": "csc_byte_identical_under_permuted_declaration_order"
        },
        {
          "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
          "symbol": "zero_cost_tests"
        }
      ],
      "fixShape": "When candidate two converts entries.rs to a directory module, do not carry pumping_water_tests across as one file. Split it along the subject boundaries its own fn names already mark, one sibling file per subject (contracts, split-plant cells, FPHA crediting, transit buckets, chronological water, filling-phase rows, generic constraints, declaration-order determinism), and lift the shared fixture constructors it declares into a single sibling shared by them. Each resulting file gets a name that predicts its contents so a new test has one obvious home. The determinism gate csc_byte_identical_under_permuted_declaration_order moves verbatim and stays a gate. Pure test relocation: no non-test line of entries.rs changes, the library stays byte-identical, and the parity goldens, the rank-invariance harness and the mpiexec -n 1 versus -n 2 reproduction are unaffected. Moves no golden.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/lp/builder/entries.rs | grep -n '^mod '; git show 077dbe2c:crates/cobre-sddp/src/lp/builder/entries.rs | sed -n '3518,10093p' | grep -oP '^\\s{4}fn \\K\\w+'",
        "output": "Four top-level inline modules: assemble_csc_tests L1682-1726 (45 lines, 2 fns); parameter_resolution_tests L1727-2248 (522, 3); zero_cost_tests L2249-3517 (1269, 17); pumping_water_tests L3518-10093 (6576, 77). Sampled fn names inside pumping_water_tests: fixture_hydro, fixture_bus, fixture_thermal, fixture_line, station, station_full, build_sorted_csc, coeff_at, split_plant_fixture, min_floor_fixture, contract_import_enters_bus_row_with_plus_one, csc_byte_identical_under_permuted_declaration_order, test_fpha_cell_generation_is_credited_to_its_own_bus, fill_transit_bucket_definition_entries_matches_pre_migration_formula_across_ragged_plants, k1_chronological_with_travel_time_is_byte_identical_to_parallel, sigma_fill_row_columns_coefficients_and_rhs_at_last_filling_stage, b6b_generic_constraint_resolves_pumping_columns_in_lp, lp_builder_never_references_dual_extraction.",
        "reading": "pumping_water_tests is a single brace block with no nested submodules holding 65 percent of entries.rs and 78 percent of the file's 99 test fns. Its contents span at least eight subjects that have nothing to do with pumping water entries: contract import and export rows, split-plant cell water balance, FPHA per-bus generation crediting, transit-bucket definition rows, chronological travel time, filling-phase sigma_fill rows, generic-constraint column resolution, and the CSC declaration-order byte-identity gate. It also holds the module's local fixture constructors (fixture_hydro, fixture_bus, fixture_thermal, fixture_line, station, build_sorted_csc, coeff_at). The name no longer predicts the contents, so it is the default landing zone for any new entries.rs test and keeps accreting; the three sibling modules stay small because the catch-all absorbs everything."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (subject-named test homes; the section 5.1 threshold is a proposal the test-corpus station ratifies)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-test-bloat-03",
      "id": "TD-048",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5b",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "one clone pair, one binary, one-parameter delta — TD-035-scale blast radius, not the TD-037 cross-binary ripple",
      "upgradeNote": null,
      "calibrationBasis": "one 229-line verbatim clone pair inside one binary whose only delta is one field; parameterising in place is a local S fix with drift confined to the raw spec literals",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Whether this binary's classical arm should be parameterized in place or instead folded onto the existing two_hydro_par_system helper when template_integration and par_a_lag12_lp_coefficient are grouped under the testing-architecture section 5.1 layout - a test-corpus station call, since that homing layout is still a proposal at the pin."
      ],
      "survivingClaim": "build_classical_fixture (par_a_lag12_lp_coefficient.rs:347-580, 234 lines) repeats 229 lines of build_par_a_fixture verbatim to vary only the annual field at its two InflowModel sites, and the structural sameness that the control test classical_par_has_no_lag_11_column depends on is carried solely by the doc comment at 344-346; the drift exposure is confined to the raw InflowModel, LoadModel, HydroPenalties and bounds/penalties spec literals that bypass tests/common/builders.rs, since Stage, Hydro and Bus field additions are already one-place changes there, and any consolidation must additionally preserve the two per-variant builder-internal .expect labels that the fixShape mistook for test-body messages.",
      "title": "tests/par_a_lag12_lp_coefficient.rs's `build_classical_fixture` is a 243-line clone of `build_par_a_fixture` whose entire semantic delta is one field set to None",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/par_a_lag12_lp_coefficient.rs",
          "symbol": "build_classical_fixture"
        },
        {
          "path": "crates/cobre-sddp/tests/par_a_lag12_lp_coefficient.rs",
          "symbol": "build_par_a_fixture"
        },
        {
          "path": "crates/cobre-sddp/tests/par_a_lag12_lp_coefficient.rs",
          "symbol": "classical_par_has_no_lag_11_column"
        }
      ],
      "fixShape": "Collapse the pair into one private builder taking the annual component as a parameter, for example an `Option<AnnualComponent>` argument, with the two existing names kept as two-line wrappers that pass Some(...) and None so the three test bodies and their assertion messages are untouched. This is test-binary-local: it changes no library code, so the parity goldens under tests/parity.rs with tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and the mpiexec -n 1 versus -n 2 reproduction are all unaffected, and the three tests in this binary assert the same LP coefficients as before. Moves no golden. Keep the builders in this binary rather than promoting them to tests/common, since no other binary consumes the PAR-A lag-12 shape.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/tests/par_a_lag12_lp_coefficient.rs > /tmp/par.rs; sed -n '101,346p' /tmp/par.rs > /tmp/p1; sed -n '347,589p' /tmp/par.rs > /tmp/p2; diff /tmp/p1 /tmp/p2 | grep -c '^[<>]'",
        "output": "29 differing lines across both sides of a 246-line versus 243-line pair. The differences are: the fn name; the six-line `let annual_component = AnnualComponent { coefficient: PSI, mean_m3s: 1000.0, std_m3s: SIGMA_A };` binding; two occurrences of `annual: Some(annual_component.clone())` becoming `annual: None`; two `.expect(...)` message strings; two blank lines. The doc comment on the second reads `Build the same system shape as [build_par_a_fixture] but with annual: None on every model.`",
        "reading": "Roughly 230 of 243 lines are byte-identical between the two fixture builders, and the file's own doc comment declares the duplication. The only behavioural difference is whether the two inflow models carry an AnnualComponent. Both builders already draw their entities from tests/common/builders.rs via make_bus, make_hydro and make_stage, so the duplication is not a missing shared builder; it is a copy taken instead of adding one parameter. The cost is paid on every edit: any change to the shared 230 lines, for instance a new Stage or InflowModel field, has to be made twice in one file or the two fixtures silently diverge, and the sole consumer of the clone is one test, classical_par_has_no_lag_11_column."
      },
      "yardstickSection": "§3.2 sustainability (a fixture earns its second copy only by a delta the copy cannot express as a parameter)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-test-bloat-04",
      "id": "TD-049",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5b",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two verified copy-paste families among nineteen whole-system builders in one binary (a trio 127-130/144 lines identical, a pair 111/117), each one list-parameterised builder away; the remaining fan-out through the raw spec literals is the owner's scope call — TD-037 precedent (ripple on every field change), Sev B",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Scope call: whether the ticket is the family collapse as filed or the higher-leverage extension of the O(1) Spec layer to InflowModel, LoadModel and the four bounds/penalties sizing structs, which owns the 20-copy fan-out the collapse leaves at fifteen."
      ],
      "survivingClaim": "Narrowed to two verified copy-paste families and a corrected cost basis: the anticipated-thermal trio (one_anticipated_thermal_system, two_thermal_one_anticipated_system, two_anticipated_thermal_system, 127 and 130 of 144 lines identical, differing only in the thermal list and the n_thermals and k_max derived from it) and the one_bus_system_n_blks / one_bus_system_n_blks_with_generic pair (111 of 117 identical, the second being the first plus two builder calls) are genuine sibling copies that one list-parameterized builder would subsume. The title's stated cost is wrong and does not survive: StageSpec plus the Default spread at all 20 sites keeps a new Stage field O(1) per the invariant documented at tests/common/builders.rs:1-9, so the per-copy fan-out is confined to the non-Spec literals, namely the four bounds and penalties sizing structs (20 copies, 55 of 80 sites without a Default spread) and the InflowModel and LoadModel literals (32 sites, none with one), which the proposed family collapse leaves at fifteen copies. Also excluded: the anticipated-hydro pair, whose members differ on state_config.inflow_lags and block layout and therefore encode two separately documented column geometries, and the one_bus_system / one_bus_system_n_blks pairing at 79 of 121 identical, which is not an 85 percent twin.",
      "title": "tests/template_integration.rs grows nineteen whole-system fixture builders in copy-paste-then-parameterize families, two of which are roughly 85 percent textually identical to their sibling",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/template_integration.rs",
          "symbol": "build_hydro_one_ant_system"
        },
        {
          "path": "crates/cobre-sddp/tests/template_integration.rs",
          "symbol": "one_hydro_one_ant_system"
        },
        {
          "path": "crates/cobre-sddp/tests/template_integration.rs",
          "symbol": "two_anticipated_thermal_system"
        },
        {
          "path": "crates/cobre-sddp/tests/template_integration.rs",
          "symbol": "one_anticipated_thermal_system"
        },
        {
          "path": "crates/cobre-sddp/tests/template_integration.rs",
          "symbol": "one_bus_system_n_blks_with_generic"
        }
      ],
      "fixShape": "Collapse each family into one parameterized builder in the parent module and keep the existing names as thin wrappers so the eleven submodules that reach these through `use super::*` need no edits. The three families the diffs identify are the anticipated-hydro pair, the anticipated-thermal trio and the one-bus trio; parameterize on the values the copies already differ by, namely lead stages, discount rate, block count and the optional generic constraint. Do this inside tests/template_integration.rs rather than moving the builders to tests/common/builders.rs, since only this binary consumes whole-system shapes at this granularity and common/builders.rs owns the per-entity layer below them. No library code changes, so the parity goldens under tests/parity.rs with tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs and the mpiexec -n 1 versus -n 2 reproduction are unaffected; the wrappers must reproduce their current field values exactly so every assertion in the eleven submodules keeps its present expected numbers. Moves no golden.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/tests/template_integration.rs > /tmp/ti.rs; awk '/^fn [a-z_]/{n=$0;sub(/^fn /,\"\",n);sub(/\\(.*/,\"\",n);s=NR} /^}$/{if(s){print n, NR-s+1; s=0}}' /tmp/ti.rs; diff <(sed -n RANGE1 /tmp/ti.rs) <(sed -n RANGE2 /tmp/ti.rs) | grep -c '^[<>]'",
        "output": "Nineteen builders returning a whole cobre_core::System, sizes in lines: one_bus_system 121, one_hydro_system 185, fpha_system_with_turbined_cost 226, two_bus_system_with_stochastic_load 218, one_fpha_hydro_system 195, four_hydro_mixed_system 293, evap_hydro_system_with_violation_cost 204, multi_segment_system 103, one_hydro_system_with_withdrawal 227, one_bus_system_n_blks 117, one_bus_system_n_blks_with_generic 123, one_bus_one_thermal_system 136, one_hydro_active_violations 221, two_hydro_par_system 211, one_anticipated_thermal_system 144, two_thermal_one_anticipated_system 154, two_anticipated_thermal_system 155, one_hydro_one_ant_system 207, build_hydro_one_ant_system 228. Pair diffs: one_hydro_one_ant_system versus build_hydro_one_ant_system 59 differing lines over 207 plus 228; one_anticipated_thermal_system versus two_anticipated_thermal_system 45 over 144 plus 155; one_bus_system versus one_bus_system_n_blks 80 over 121 plus 117.",
        "reading": "These nineteen builders are about 3400 of the file's 4421 lines, so the parent of the eleven #[path] submodules is roughly three quarters fixture scaffolding. They are not duplicating tests/common/builders.rs: every one of them calls make_bus, make_hydro, make_stage or make_thermal (two to six calls each) and constructs zero Stage, Hydro, Bus or Thermal literals, so the shared builder layer is being used as intended. The duplication is one level up, between siblings. The build_hydro_one_ant_system body is the one_hydro_one_ant_system body with `lead_stages` and `annual_discount_rate` promoted to parameters, two blocks instead of one, and AR(0) instead of AR(1); roughly 176 of its 207 shared lines are identical. The one_anticipated_thermal_system versus two_anticipated_thermal_system pair is roughly 122 of 144 identical. Each pair was created by copying a builder and parameterizing the one value the new test needed, rather than adding the parameter to the existing builder, so every future field addition to Stage or InflowModel must be applied across the whole family."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (parameterised builders, one per shape family)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5b-test-bloat-07",
      "id": "TD-050",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5b",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one base-path construction spelled three times inside one binary; the tests/common half is conceded (already exported) and the corpus-wide spread belongs to the test-corpus station",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": "corpus-wide `case_dir` spread belongs to the test-corpus station (E08); only the in-file copies are anchored here",
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Only the single-file half survives, and it is the base-path construction rather than a shared corpus helper: tests/lp_builder.rs repeats Path::new(env!(\"CARGO_MANIFEST_DIR\")).join(\"../../examples/deterministic\") three times in one binary, as the two parameter-name-only variants of case_dir at lines 65-69 and 573-577 and as deterministic_root at line 478, all of which one file-scope base-path helper would collapse. The tests/common/parity_hash.rs site is conceded and excluded: its case_dir is already exported (pub fn inside pub mod parity_hash at common/mod.rs:27, linked from parity.rs:48) and is a closed-set golden-label-to-directory mapper with a panic! guard, not a copy of the suffix-join helper.",
      "title": "The four-line `case_dir` helper is declared twice inside tests/lp_builder.rs alone, and tests/common/ hosts a third copy it does not export",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/lp_builder.rs",
          "symbol": "case_dir"
        },
        {
          "path": "crates/cobre-sddp/tests/common/parity_hash.rs",
          "symbol": "case_dir"
        },
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "build_setup_for_case"
        }
      ],
      "fixShape": "Export one case-directory helper from tests/common/mod.rs next to the case helpers that already take that path as an argument, and have lp_builder.rs's two inline modules and common/parity_hash.rs call it instead of declaring their own. Keep it in tests/common rather than creating a fixture crate. The wider corpus-level spread across the other binaries belongs to the test-corpus station; the two in-file copies here are the part anchored in this sub-station. Pure test-harness change resolving to the same directory, so the parity goldens under tests/parity.rs with tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs and the mpiexec -n 1 versus -n 2 reproduction all read the same cases and stay byte-neutral. Moves no golden.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/tests/lp_builder.rs | sed -n '65,70p;573,578p'; git grep -c 'fn case_dir' 077dbe2c -- crates/cobre-sddp/tests",
        "output": "lp_builder.rs L65 in mod extraction_nonuniform_block_bases and L573 in mod policy_entity_manifest are the same four lines, differing only in the parameter name (suffix versus name): `Path::new(env!(\"CARGO_MANIFEST_DIR\")).join(\"../../examples/deterministic\").join(...)`. Counts across the tests corpus: lp_builder.rs 2, deterministic.rs 2, anticipated_scenarios.rs 1, basis_trajectory_probe.rs 1, common/parity_hash.rs 1, filling_commissioning.rs 1, parity.rs 1.",
        "reading": "tests/common/mod.rs is the fixture root and every one of its case helpers takes a case directory as `&Path` (boundary_requirements, build_setup_for_case, fresh_setup_with), yet it exports no way to name one, so each consumer re-derives the examples/deterministic path itself. tests/common/parity_hash.rs, inside the shared directory, holds its own private copy rather than a shared one. The clearest instance is entirely within one file in scope: lp_builder.rs declares the identical helper twice, once per inline module, because the two modules do not share a scope. The path is a repository layout fact, so the duplication is also a hard-coded relative path repeated at every copy."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (one file-scope helper per binary; corpus-wide helpers live in tests/common)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-architecture-00",
      "id": "CD-086",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5c",
      "severity": "B (A-risk)",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": "house rating above the reviewer's — see calibrationBasis",
      "calibrationBasis": "an instance of the owner-elevated retrofitted-variant asymmetry class (live instances CD-013, CD-015, CD-027): the enumerated traversal is the Nth variant on the traversal axis while the sampled original keeps the cut-binding metadata channel bespoke, so under `Traversal::Enumerated` `CutPool::record_binding` never fires and the DCS resident-set seed and the budget eviction key both degrade SILENTLY on a pairing no admission gate rejects — Sev B by the class, A-risk carried by the silence of the divergence (CD-004 marker rule); reviewer proposed B",
      "category": "asymmetry",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (cut selection and the backward decomposition are SDDP geometry that stays in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-022"
      ],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "PD-004",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        },
        {
          "ref": "mirror:performance-debt-follow-ups-first-performance-pass-2026-08-18",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "By-node scheduler is warm-start-only",
        "Cut pool is append-only; basis matches by slot identity"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Carried over from the attacker and still decisive: is enumerated traversal combined with dynamic cut selection a SUPPORTED configuration? Road (a) rejects the pairing beside the existing enumerated preconditions in `setup/mod.rs`; road (b) wires the binding contribution plus a per-stage metadata reduction into the enumerated driver. Both are byte-neutral on today's goldens, so only owner intent decides which road.",
        "Scope and severity call: `CutPool::enforce_budget`'s `(last_active_iter, active_count)` eviction key is a second production reader that degrades the same way under enumerated traversal, and unlike the DCS seed it changes which cuts are deactivated and therefore the bound. The owner must decide whether that reader belongs inside this item's scope and whether it lifts the severity above B; I did not mint a separate item for it."
      ],
      "survivingClaim": "Under `Traversal::Enumerated`, `run_enumerated_backward` neither folds a `slot_increments` contribution nor calls `sync_stage_metadata`, so `CutPool::record_binding` never fires and every populated slot keeps `last_active_iter == iteration_generated` with `active_count == 0`; the enumerated forward's `build_initial_resident_set` seed therefore reduces to active-and-generated-within-k2, on a pairing no validation rejects. Strictly narrower than the title on two counts: the per-opening bump is NOT a hand-copied per-driver duty (both sampled schedulers call the single shared `accumulate_opening_outcome`; only the `slot_increments`-to-`metadata_sync_contribution` fold and the `sync_stage_metadata` call are driver-local), and the DCS consequence is confined to inner iterations and solve-count telemetry — never a wrong cut, bound or objective, because the lazy loop is exact and the seed is only a warm-start hint.",
      "title": "The cut-binding metadata channel is a sampled-traversal-only side duty: the enumerated backward commits cuts but records no binding activity, so DCS resident-set seeding silently degrades to a generation-age filter on a combination no admission gate rejects",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "run_enumerated_backward"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward/replicated.rs",
          "symbol": "run_backward_node_replicated"
        },
        {
          "path": "crates/cobre-sddp/src/cut/dcs.rs",
          "symbol": "build_initial_resident_set"
        }
      ],
      "fixShape": "Two roads, and the pick is the owner's because it turns on whether enumerated-plus-DCS is a supported combination. Road (a), make the exclusion explicit: reject the combination in the study admission gate exactly as the by-node scheduler's DCS exclusion is already handled, and pin it with a named test plus a rules entry, so an unsupported combination is a rejection rather than a silent degradation. Road (b), make the channel driver-independent: fold the slot_increments contribution into the shared outcome-aggregation step every backward driver already calls, so the replicated driver cannot omit it, and give the enumerated backward the per-stage allreduce that sync_stage_metadata performs on the sampled path. Road (a) is the smaller and the reversible one and matches the existing precedent; road (b) is the durable one if enumerated-plus-DCS is meant to work. Byte-neutrality bar: road (a) is byte-neutral by construction on every currently-passing configuration, since it only converts a silently-degraded run into a rejected one. Road (b) is NOT byte-neutral in solver statistics, because a differently-seeded DCS resident set changes the LP-solve count; it must remain byte-neutral in cuts, bounds and reported objective, which the parity goldens plus the rank-invariance harness and mpiexec -n 1 versus -n 2 verify, and which holds because the DCS lazy loop is exact. Both roads touch pinned contracts and must cite them: .claude/rules/sddp.md section 'By-node scheduler is warm-start-only' is the precedent for an explicitly pinned traversal-by-DCS exclusion, and the append-only cut pool and slot-identity basis section owns active_count and last_active_iter, which only record_binding may advance.",
      "evidence": {
        "command": "git grep -n 'record_binding' 077dbe2c -- crates/ ; git grep -n -i 'dcs' 077dbe2c -- crates/cobre-sddp/src/setup/mod.rs ; git show 077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs | grep -n 'Traversal::Enumerated\\|Traversal::Sampled\\|sync_stage_metadata(\\|PoolRegion {\\|fcf.add_cut' ; git grep -n 'slot_increments' 077dbe2c -- crates/cobre-sddp/src/training/backward/replicated.rs ; git show 077dbe2c:crates/cobre-sddp/src/cut/dcs.rs | sed -n '758,768p'",
        "output": "record_binding: cut/pool.rs:475 (the declaration) and backward_pass_state.rs:1063 'fcf.pools[pool].record_binding(slot, inc, iteration);' -- one production caller, no other.\nsetup/mod.rs: zero matches for 'dcs' (case-insensitive).\nbackward_pass_state.rs:508 'Traversal::Enumerated(plan) => self.run_enumerated_backward(inputs, plan),' / :509 'Traversal::Sampled { .. } => self.run_sampled_backward(inputs),'; :880 'inputs.fcf.add_cut(' (inside run_enumerated_backward, decl :721); :1673 'state.sync_stage_metadata(' (inside run_one_backward_level, decl :1534, reached only from run_sampled_backward :647); :1810 'state.level_pool_regions_scratch.push(PoolRegion {' (inside compute_one_backward_node, decl :1727); :1981 'inputs.fcf.add_cut('.\nreplicated.rs: zero matches for 'slot_increments'.\ndcs.rs:762-766 'let meta = pool.metadata(s); let within_window = current_iteration.saturating_sub(meta.last_active_iter) <= window; let is_current_iter = meta.iteration_generated == current_iteration; if within_window || is_current_iter { out.push(s as u32); }'",
        "reading": "CutPool::record_binding has exactly one production caller (backward_pass_state.rs:1063, inside sync_stage_metadata), and sync_stage_metadata has exactly one production call site (:1673, inside run_one_backward_level), which is reached only from run_sampled_backward (:647). BackwardPassState::run routes Traversal::Enumerated to run_enumerated_backward (:508); that function commits cuts (inputs.fcf.add_cut at :880) but never pushes a PoolRegion -- the sole push is :1810 inside compute_one_backward_node, on the sampled path -- and never calls sync_stage_metadata. Its per-node worker, replicated.rs::run_backward_node_replicated, never names slot_increments at all. So on an enumerated traversal no binding increment is ever produced, allreduced, or recorded. Meanwhile DCS is fully wired on the enumerated FORWARD: forward/enumerated.rs:327-354 calls build_initial_resident_set then lazy_solve_preloaded, :288 and :546 gate fusion and basis capture on params.dcs, and :1018 passes training_ctx.dcs.filter(|p| p.is_active(iteration)) into the stage worker; setup/mod.rs contains zero occurrences of 'dcs', so the admission gate neither rejects nor mentions the enumerated-plus-DCS combination. CutPool::add_cut initialises active_count: 0 and last_active_iter: iteration (the generation iteration) and record_binding is the only mutator of either field, so on an enumerated run last_active_iter == iteration_generated for every cut and build_initial_resident_set's 'within_window || is_current_iter' test collapses to a pure generation-age window: the resident set is seeded by how recently a cut was CREATED, never by whether it recently BOUND, which is the signal the field exists to carry. This is not the mirror's 'Enumerated backward observability gap' entry (that one is the empty per-stage worker-statistics vector feeding the solver-statistics log, and states cuts and bounds are unaffected) and not PD-004 (per-run allocation in the same function, queued to the perf epic): the subject here is the binding-metadata channel and its DCS consumer. The architectural defect is that contributing binding metadata is an undeclared per-driver duty -- hand-copied into two drivers and simply absent from the third -- rather than a step of the shared successor-outcome contract, so a driver omits it silently and no gate notices."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-over-engineering-01",
      "id": "OD-039",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5c",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a parameter that shadows a total the struct already stores, letting the per-rank view be built from a different total than the one its siblings were derived from; the deletion half is dropped because the mirror declares the method the owner of the partition arithmetic",
      "category": "over-parameterization",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "OD-009"
      ],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "The method must not exist while `actual_per_rank` accepts `total_forward_passes` as a parameter (rank_distribution.rs:60) that shadows the `num_total_forward_passes` the same struct already stored in `new` (:53): the vector view can be built from a total other than the one `my_actual_fwd` / `my_fwd_offset` / `max_local_fwd` were derived from (:37-41), with no check rejecting the disagreement. The residue is the parameter, not the method - the deletion half of the title is dropped, since the mirror at :933 declares this method the SDDP-side owner and prescribes routing cut_sync.rs:182 and stats_aggregation.rs:118 through `RankDistribution` rather than the reverse.",
      "title": "RankDistribution::actual_per_rank forwards to cobre_comm::per_rank_counts without deciding anything, for one caller, while re-taking a total the struct already stores",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/session/rank_distribution.rs",
          "symbol": "actual_per_rank"
        },
        {
          "path": "crates/cobre-sddp/src/training/session/rank_distribution.rs",
          "symbol": "RankDistribution"
        }
      ],
      "fixShape": "Delete the method and let session/mod.rs:245 call cobre_comm::per_rank_counts directly, matching the two sibling sites that already do. If a named owner on the struct is preferred, keep the method but drop the parameter and read self.num_total_forward_passes, so the vector and the per-rank constants cannot disagree by construction. Do not promote the split arithmetic into a new trait or type - cobre_comm::per_rank_counts is already the single owner, and a seam over one consumer would trip conflicts-trigger 4. Byte-neutrality: both variants evaluate the same base/remainder split over the same two inputs, because the sole call site already passes the value that new stored, so the returned Vec<usize> is element-wise identical and the ExchangeBuffers::with_actual_counts sizing it feeds is unchanged. This is rank-distribution arithmetic, so the bar is the one that binds it: mpiexec -n 1 vs -n 2 vs -n 4 must reproduce bit-for-bit, which tests/parity.rs already asserts by comparing 1-rank against 2-rank and 4-rank hashes, plus tests/common/parity_hash.rs and the tests/common/permute.rs rank-invariance harness; no golden moves. No pinned contract in .claude/rules/sddp.md governs rank distribution - the change touches none of the cut pool, the Benders cut sign, column-bound state pinning, FPHA average storage, risk aggregation, the node-tag warm-start rule or the policy-load path.",
      "evidence": {
        "command": "git grep -n 'actual_per_rank\\|per_rank_counts' 077dbe2c -- crates/cobre-sddp/src crates/cobre-comm/src | grep -v 'state_exchange.rs'; git grep -n 'num_total_forward_passes' 077dbe2c -- crates/cobre-sddp/src",
        "output": "077dbe2c:crates/cobre-comm/src/lib.rs:83:pub fn per_rank_counts(total: usize, num_ranks: usize) -> Vec<usize> {\n077dbe2c:crates/cobre-sddp/src/cut/cut_sync.rs:182:        let per_rank_cuts = per_rank_counts(total_forward_passes, num_ranks);\n077dbe2c:crates/cobre-sddp/src/training/forward/stats_aggregation.rs:118:    let path_counts = per_rank_counts(total_forward_passes, num_ranks);\n077dbe2c:crates/cobre-sddp/src/training/session/mod.rs:245:        let actual_per_rank = ranks.actual_per_rank(total_forward_passes);\n077dbe2c:crates/cobre-sddp/src/training/session/rank_distribution.rs:60:    pub(crate) fn actual_per_rank(&self, total_forward_passes: usize) -> Vec<usize> {\n077dbe2c:crates/cobre-sddp/src/training/session/rank_distribution.rs:61:        cobre_comm::per_rank_counts(total_forward_passes, self.num_ranks)\n077dbe2c:crates/cobre-sddp/src/training/session/rank_distribution.rs:205:    fn rank_distribution_actual_per_rank_is_consistent_with_my_actual_fwd() {\n077dbe2c:crates/cobre-sddp/src/training/session/rank_distribution.rs:210:            let per_rank = rd.actual_per_rank(8);\n077dbe2c:crates/cobre-sddp/src/training/forward_pass_state.rs:116:            total_forward_passes: ranks.num_total_forward_passes,\n077dbe2c:crates/cobre-sddp/src/training/session/mod.rs:877:        let global_n = self.ranks.num_total_forward_passes;\n077dbe2c:crates/cobre-sddp/src/training/session/rank_distribution.rs:53:            num_total_forward_passes: total_forward_passes,",
        "reading": "RankDistribution::actual_per_rank is a two-line method whose whole body forwards to cobre_comm::per_rank_counts with one field. It decides nothing: it neither validates, adapts, reorders nor reshapes the result. It has exactly one production caller (session/mod.rs:245) plus one unit test (:210), while the two sibling sites in this same crate that need the identical vector call the free function directly (cut/cut_sync.rs:182, training/forward/stats_aggregation.rs:118), so the codebase already prefers the unwrapped form 2 to 1. The wrapper also re-takes as a parameter a value the struct already stores: rank_distribution.rs:53 saves the same argument as num_total_forward_passes, a field read at forward_pass_state.rs:116 and session/mod.rs:877, so the method can silently be handed a total that disagrees with the my_actual_fwd / my_fwd_offset / max_local_fwd constants derived from it in new. Today the single caller passes the same total it passed to RankDistribution::new at session/mod.rs:171, so no live defect exists. Counter-signals checked and absent: no second implementation, no test-double seam (the unit test drives the real method), no layer boundary crossed, and no performance or reproducibility fence. Disjoint from OD-009: that row's claim is the #[allow(dead_code)] on the my_rank field of this same struct, and actual_per_rank is not in its reraiseKey symbol list."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-performance-00",
      "id": "PD-040",
      "class": "PD",
      "lens": "performance",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two removable allocations per call inside the per-node cut-selection value sweep (`m_block_starts`, `eligible`) on a path whose caller documents a no-allocation contract; the other four allocations and the zero-fill are conceded — never-allocate-on-hot-paths rule, structural calibration only, UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (cut selection is SDDP geometry that stays in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-018",
        "CD-012"
      ],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "mirror:performance-debt-follow-ups-first-performance-pass-2026-08-18",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Cut pool is append-only; basis matches by slot identity"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "Owner call: whether the inaccurate rustdoc sentence at training/session/mod.rs:1026-1027 ('no heap allocation when the cut pools have not grown') gets its own doc-accuracy item — it is false because of run_cut_management's own per_stage / deactivations / record_by_pool allocations, independent of this candidate's mechanism claim, so it is stripped here rather than fixed by it."
      ],
      "survivingClaim": "Only two of the sweep's allocations are removable by the mechanism as filed: `m_block_starts` (cut_selection.rs:359) materialises a stride range that the following .par_iter() could take as a parallel range with no Vec at all, and `eligible` (:344) is call-local and dead at return so it can ride the TrainingSession::cut_selection_state_scratch field the caller already reuses across the same per-node loop; the Vec::new() deactivation/reactivation pair, the two per-fold-task identity buffers, the zero-fill and the run_cut_management no-allocation-contract framing do not survive.",
      "title": "The cut-selection value sweep heap-allocates six times per call and twice per rayon fold task, contradicting run_cut_management's stated no-allocation contract",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/cut/cut_selection.rs",
          "symbol": "select_for_stage"
        },
        {
          "path": "crates/cobre-sddp/src/training/session/mod.rs",
          "symbol": "run_cut_management"
        },
        {
          "path": "crates/cobre-sddp/src/cut/cut_selection.rs",
          "symbol": "M_BLOCK"
        }
      ],
      "fixShape": "Move the four per-call vectors and the two per-fold-task buffers onto the per-worker scratch the crate already owns for exactly this purpose, reusing them through clear/resize in the State Struct shape that .claude/architecture-rules.md prescribes for hot-path drivers, and borrow the fold identity from that scratch rather than materialising a fresh vec per task. Drop the zero-fill of the value panel, because gemm_block writes it with beta zero before any read. Byte-neutrality: the rayon fold-and-reduce keeps the same commutative associative OR-merge over the same bitmaps, so the selected slot set, the deactivation list and its order are unchanged, and cut activity therefore stays identical against the parity goldens in tests/parity.rs and tests/common/parity_hash.rs, against the rank-invariance harness in tests/common/permute.rs, and between mpiexec -n 1 and -n 2 because selection is rank-local. .claude/rules/sddp.md section 'Cut pool is append-only; basis matches by slot identity' governs: the fix must not renumber or compact slots, only reuse the scratch that scores them.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/cut/cut_selection.rs | sed -n '344,372p;405,421p' && git show 077dbe2c:crates/cobre-sddp/src/training/session/mod.rs | sed -n '1024,1027p'",
        "output": "cut_selection.rs:344  let eligible: Vec<bool> = (0..populated)\ncut_selection.rs:359  let m_block_starts: Vec<usize> = (0..n_blocks).map(|i| i * M_BLOCK).collect();\ncut_selection.rs:364  let is_selected: Vec<bool> = m_block_starts\ncut_selection.rs:367      || (vec![0.0_f64; populated * M_BLOCK], vec![false; populated]),\ncut_selection.rs:410      || vec![false; populated],\ncut_selection.rs:419  let mut deactivations: Vec<u32> = Vec::new();\ncut_selection.rs:420  let mut reactivations: Vec<u32> = Vec::new();\nsession/mod.rs:1026  /// All operations are O(active cuts) and perform no heap allocation when the\nsession/mod.rs:1027  /// cut pools have not grown since the previous iteration.",
        "reading": "select_for_stage allocates eligible, m_block_starts, deactivations and reactivations once per call, plus a populated*M_BLOCK value panel and a populated-long bitmap per rayon fold task and one more bitmap per reduce identity. None of them is workspace-owned, so every one is a fresh heap allocation on each invocation. run_cut_management calls select_for_stage once per interior node per cut-management iteration while its own rustdoc promises no heap allocation once the pools have stopped growing, so the caller's stated contract and the callee's shape disagree. The panel is zero-initialised by vec! although gemm_block writes it with beta zero and overwrites every element it reads back."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/cut/cut_selection.rs::select_for_stage",
      "exercisingCallSites": [
        "training/session/mod.rs · run_cut_management (per node per cut-management pass)"
      ],
      "mechanism": "allocation per call and per rayon fold task inside the per-node cut-selection value sweep, plus a redundant zero-fill of a panel the gemm kernel fully overwrites",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-performance-02",
      "id": "PD-041",
      "class": "PD",
      "lens": "performance",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "the single gather in `run_cut_management` pushes one scalar at a time through the slot-index table although the archive is flat and packed at one global stride, so an identity pool's trial row is already the wanted contiguous run; the mandatory-predicate premise is conceded — structural calibration only, UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (cut management is SDDP geometry in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-012"
      ],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "mirror:performance-debt-follow-ups-first-performance-pass-2026-08-18",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "The cut intercept dots the trial state through the projection, never positionally"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "Only the absent contiguous-copy path at the single gather in `training/session/mod.rs` L1112-1121 survives, on the ground that the archive is flat and packed at one global stride so an identity pool's trial row is already the wanted contiguous run. Stripped: the fixShape premise that a dedicated projection identity predicate is mandatory because a reduced pool could match the state dimension in slot count, which cannot arise at the pin since `REGION_ORDER`'s four ranges partition [0, n_state) contiguously and `CutStateProjection::new` walks them ascending with only Storage and Lag gated, making `n_slots == n_global` already equivalent to the identity index vector and strictly better than the rustdoc `storage && inflow_lags` test, which misses an identity pool with an empty lag range. Also stripped: Sev-B, because the gather reads each archive element once while its sole consumer `select_for_stage` reads the same buffer once per populated cut inside `gemm_block`, making the residue a lower-order term inside its own pass and a minor perf-sweep item at `4t`. The claim does not reach `dot_trial_state`, which fuses the gather into a reduction and has no contiguous destination to copy into.",
      "title": "The cut-management projection gather pushes one scalar at a time through the slot-index table with no bulk-copy path for the identity projection",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/session/mod.rs",
          "symbol": "run_cut_management"
        }
      ],
      "fixShape": "Give the gather a guarded fast path: when the projection is the identity, extend scratch from the contiguous trial row of global_states in one extend_from_slice, and otherwise keep the indexed loop exactly as it stands. The guard must test the projection's own identity predicate, not the slot count alone, so a reduced pool that happens to have as many slots as the state dimension cannot take the fast path. .claude/rules/sddp.md section 'The cut intercept dots the trial state through the projection, never positionally' is why the guard is mandatory rather than optional: a positional prefix is wrong for a reduced pool, and that section is the pinned reason the slow path must remain the default. Byte-neutrality: on the fast path the same f64 values land in the same buffer positions in the same order, so the value sweep, the selected set and the deactivations are bit-identical; parity goldens, the rank-invariance harness and mpiexec -n 1 versus -n 2 are unaffected.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/training/session/mod.rs | sed -n '1104,1124p'",
        "output": "let n_slots = proj.n_slots();\nlet n_trials = archive_ref.map_or(0, |a| a.count(node_pos));\nlet global_states = archive_ref.map_or(&[] as &[f64], |a| a.states_for_node(node_pos));\n// Project the StateDim-packed archive into the pool's projected\n// slot space through its own CutStateProjection (identity when\n// n_slots == n_global). A positional prefix is wrong -- a reduced\n// pool's slots are not the first n_slots StateDims.\nscratch.clear();\nscratch.reserve(n_trials * n_slots);\nfor m in 0..n_trials {\n    let base = m * n_global;\n    for s in 0..n_slots {\n        scratch.push(\n            global_states[base + proj.global_state_index(CutSlot::new(s)).get()],\n        );\n    }\n}",
        "reading": "The gather runs one push per (trial, slot) pair for every interior node, each push routed through global_state_index, an inline lookup into the projection's slot-index table. The comment on the loop names the identity case itself: the projection is the identity when n_slots equals n_global, which is the all-enabled pool, and the projection's own rustdoc in the sibling sub-station states s equals global_state_index(s) in that case. In that case the trial row in global_states is already exactly the wanted contiguous run, yet the loop still walks it scalar by scalar. This is not CD-038: that live item is the loop-shape duplication between this open-coded gather and the fused dot_trial_state; the claim here is the absent bulk-copy fast path."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/training/session/mod.rs::run_cut_management",
      "exercisingCallSites": [
        "training/session/mod.rs · run_cut_management L1112-1121 (per node, trial, slot)"
      ],
      "mechanism": "scalar-at-a-time gather through an indirection table per (node, trial, slot), with no contiguous copy path for the identity projection the loop's own comment names",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-performance-03",
      "id": "PD-042",
      "class": "PD",
      "lens": "performance",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "on the sampled level-driver path with an archive (cut selection enabled or export_states) and a declared multi-node level, every sibling node holds its own copy of one identical level block — dense replication removable by one shared per-level block viewed per node; the value-sweep half is conceded; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (the backward decomposition stays in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Cut pool is append-only; basis matches by slot identity",
        "Per-level exchange in the backward pass"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "Owner call, needed only to scope the LARGER change and not the claim confirmed here: is the level-wide over-inclusion a required correctness margin, or is narrowing each node's archive to its own routed states licensed as a golden-moving change? Only that narrowing reduces the per-node value sweep; the shared-block fix confirmed above does not depend on the answer."
      ],
      "survivingClaim": "Only the MEMORY half survives, and only on the sampled level-driver path when the archive exists (cut selection enabled or export_states) and a declared nodes[] graph puts more than one cut-generating node at a stage: each sibling's NodeStates.data holds its own extend_from_slice copy of an identical level block, removable byte-neutrally by one shared per-level block viewed per node. The value-sweep half does not survive: each interior node owns its own pool and projection, so the per-sibling walk evaluates different cuts rather than recomputing the same values, and the proposed fix leaves n_trials and the per-node projection gather unchanged.",
      "title": "The backward level archives the whole level's gathered state block into every sibling node's bucket, so archive footprint and the downstream value sweep both scale with nodes per level",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "run_one_backward_level"
        },
        {
          "path": "crates/cobre-sddp/src/training/visited_states.rs",
          "symbol": "archive_gathered_states"
        },
        {
          "path": "crates/cobre-sddp/src/training/visited_states.rs",
          "symbol": "append"
        }
      ],
      "fixShape": "Store the level's gathered block once and give every node in that level a view of it, so states_for_node returns the shared block instead of a per-node copy: the values every reader sees are unchanged, only the duplication disappears. The per-node retention window must be preserved, since trim_to_window currently trims each node's own vector, so a shared block needs a level-keyed trim with the same window arithmetic. .claude/rules/sddp.md section 'Per-level exchange in the backward pass' governs the level's gather and must keep owning it, and section 'Cut pool is append-only; basis matches by slot identity' is untouched because no slot is renumbered. Byte-neutrality: the trial states read back per node are identical in value and order, so cut selection, cut generation and the reported bounds are bit-identical against the parity goldens, the rank-invariance harness and mpiexec -n 1 versus -n 2. Narrowing the archive to each node's OWN routed states is a separate and larger change: it would alter which trial states each pool is scored against and therefore which cuts survive selection, so it would move the goldens and is not proposed here.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs | sed -n '1560,1574p' && git show 077dbe2c:crates/cobre-sddp/src/training/visited_states.rs | sed -n '53,63p;157,168p'",
        "output": "backward_pass_state.rs:1560  if let Some(ref mut archive) = inputs.visited_archive {\n        inputs.exchange.pack_real_states_into(&mut state.real_states_buf);\n        // ... every node in a multi-node level receives the level's states\n        // -- a conservative over-inclusion that never drops a binding cut.\n        for &node_pos in level {\n            archive.archive_gathered_states(node_pos, &state.real_states_buf, total_fwd);\n        }\n    }\nvisited_states.rs:59  pub fn append(&mut self, gathered: &[f64], total_fwd: usize) {\n        self.data.extend_from_slice(gathered);\n        self.count += total_fwd;\nvisited_states.rs:157  pub fn archive_gathered_states(&mut self, node: NodePos, gathered: &[f64], total_fwd: usize) {\n        self.nodes[node.0].append(gathered, total_fwd);",
        "reading": "archive_gathered_states delegates to NodeStates::append, which extend_from_slice copies the entire real_states_buf into that node's own data vector. The loop runs that copy once per node in the level with the same buffer, so on a level with sibling nodes the identical state block is duplicated into every node's bucket every iteration. The code's own comment calls this a conservative over-inclusion. The cost is twofold: archive bytes grow as nodes-per-level times the level's real forward-pass count times the state dimension, and run_cut_management then reads count(node_pos) trial states back per node, so the cut-selection value sweep re-evaluates the same trial states once per sibling node. The sibling archive_one_state rustdoc states the contrast explicitly: the enumerated path never replicates the state total_forward_passes times the way archive_gathered_states does."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/training/backward_pass_state.rs::run_one_backward_level",
      "exercisingCallSites": [
        "training/backward_pass_state.rs · run_one_backward_level → training/visited_states.rs · archive_gathered_states (per level per iteration)"
      ],
      "mechanism": "dense replication: the level's state block is copied into every sibling node's archive bucket, so both archive memory and the per-node cut-selection value sweep scale with nodes per level",
      "measurabilityNote": "exercised only on a declared multi-node graph under SAMPLED traversal; neither protocol deck (chain sampled 4t; branching enumerated 2t) has that shape — stays UNMEASURED until the sweep adds such a deck"
    },
    {
      "candidateRef": "5c-performance-04",
      "id": "PD-043",
      "class": "PD",
      "lens": "performance",
      "subStation": "5c",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a fixed-length clear over the run-lifetime pool capacity on every warm-started training solve where only the reconcilable slots are written; proportionally largest early and shrinking as the pool fills — bounded; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (basis reconstruction is SDDP geometry in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-023"
      ],
      "mergedFrom": null,
      "retiredOverlaps": [
        {
          "ref": "mirror:organizational-quality-follow-ups-low-priority",
          "kind": "anchor-overlap",
          "justification": "distinct claim anchored in a file a file-scoped retired item also cites; NOT a re-raise"
        }
      ],
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "A stored basis warm-starts only at its own node (node-tag)",
        "Cut pool is append-only; basis matches by slot identity"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrowed to the training path: `build_slot_lookup`'s unconditional clear is a FIXED-length pass over the run-lifetime pool capacity (`warm_start_count + max_iterations * visit_bound`, set once by `ScratchBuffers::new` from `max_pool_capacity`), not over the current pool length, so the redundancy is proportionally largest in the FIRST iterations and shrinks as the pool fills rather than diverging monotonically; the simulation path, whose buffer the `run_stage_solve` resize sizes to `populated()` (the LP's own cut-row count), carries no residue, and the clear is the only capacity-keyed pass in a call that is otherwise already several LP-width passes.",
      "title": "build_slot_lookup clears the whole pool-length slot table on every warm-started solve although only the reconcilable slots are ever written",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/cut/basis_reconstruct.rs",
          "symbol": "build_slot_lookup"
        },
        {
          "path": "crates/cobre-sddp/src/solve/stage_solve.rs",
          "symbol": "run_stage_solve"
        }
      ],
      "fixShape": "Replace the sentinel clear with a generation stamp: store a solve epoch alongside each position and treat an entry whose epoch is stale as absent, so no bulk reset is needed; the alternative is to record the slots written by the previous call and reset only those. Either way every lookup reconstruct_basis performs returns the same answer it does today. .claude/rules/sddp.md section 'Cut pool is append-only; basis matches by slot identity' governs and is preserved exactly, because the slot-to-position mapping is unchanged and no slot is compacted; section 'A stored basis warm-starts only at its own node (node-tag)' is untouched because the node-tag filter runs before reconstruction. Byte-neutrality: the reconstructed basis bytes are identical, so the parity goldens, the rank-invariance harness and mpiexec -n 1 versus -n 2 all reproduce. The debug assertion that the caller pre-sized the buffer stays; the workspace construction test that asserts every entry is None would assert a zero epoch instead.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/cut/basis_reconstruct.rs | sed -n '240,260p' && git show 077dbe2c:crates/cobre-sddp/src/solve/stage_solve.rs | sed -n '60,66p'",
        "output": "basis_reconstruct.rs:240  fn build_slot_lookup(reconcilable_slots: &[u32], slot_lookup: &mut Vec<Option<u32>>) {\n...\nbasis_reconstruct.rs:254      slot_lookup.fill(None);\n    for (position, &slot) in reconcilable_slots.iter().enumerate() {\n        slot_lookup[slot as usize] = Some(position as u32);\n    }\nstage_solve.rs:60  // `pool.populated()` only grows, so this resize is a no-op after the\n    // first few iterations.\n    if ws.scratch.recon_slot_lookup.len() < inputs.pool.populated() {\n        ws.scratch.recon_slot_lookup.resize(inputs.pool.populated(), None);\n    }",
        "reading": "fill(None) writes every entry of slot_lookup, and run_stage_solve resizes that buffer up to inputs.pool.populated() before each warm-started solve while noting that the populated count only grows. The scatter that follows writes reconcilable_slots.len() entries. The clear therefore costs the full append-only pool length on every warm-started LP solve while the useful work stays proportional to the reconcilable slot count, and the two diverge monotonically as the pool grows over the training run."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/cut/basis_reconstruct.rs::build_slot_lookup",
      "exercisingCallSites": [
        "solve/stage_solve.rs · run_stage_solve (per warm-started solve)"
      ],
      "mechanism": "full-length clear per warm-started LP solve, over a buffer whose length tracks the monotonically growing append-only pool, where the useful scatter touches only the reconcilable slots",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-performance-06",
      "id": "PD-044",
      "class": "PD",
      "lens": "performance",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "an instance of the retrofitted-variant asymmetry class (CD-013/015/027): the by-node arm already returns per-worker counts read back through `canonical_scatter`, while the original by-scenario arm drains the reused workspace buffer into a fresh owned `Vec<StagedCut>` per (backward node, worker, iteration) — Sev B by the class; structural calibration only, UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.2 (the backward decomposition stays in the engine)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-016"
      ],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Cut pool is append-only; basis matches by slot identity"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "On the default ByScenario branch, and only for a worker owning at least one routed trial point, process_stage_backward pays one payload-proportional heap allocation plus a copy in, a copy out and a free per (backward node, worker, iteration) for the owned Vec<StagedCut> it drains into at its return, which the counts-plus-canonical_scatter read-back already used by process_stage_backward_by_node removes; the workspace buffer's own capacity reuse is NOT defeated (drain leaves capacity intact and within-stage pushes stay allocation-free), and the constant-size per-node outer result vector is excluded because the sibling scheduler allocates the same shape plus its own counts vector.",
      "title": "process_stage_backward allocates a fresh owned staged-cut vector per worker per node at its return boundary, defeating the reused workspace buffer it drains",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "process_stage_backward"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward/by_node.rs",
          "symbol": "process_stage_backward_by_node"
        }
      ],
      "fixShape": "Adopt the sibling scheduler's return shape: have process_stage_backward return per-worker counts and leave each staged cut in the producing worker's own staged_cuts_buf, then let the caller read them back in ascending worker-then-item order through the shared canonical_scatter primitive and extend state.staged_cuts_buf from borrowed slices, so no per-node vector is allocated. Error propagation is preserved because the by-node path already returns a Result per worker and propagates the first failure by value. Byte-neutrality: the merge already sorts by cut.trial_state_idx, the sole globally unique key, so the order in which cuts are added to the pool is unchanged and remains independent of worker count; .claude/rules/sddp.md section 'Cut pool is append-only; basis matches by slot identity' governs that add order and section 'Joint risk is applied once over the flattened successor x opening vector' is untouched because no aggregation moves. Parity goldens, the rank-invariance harness and mpiexec -n 1 versus -n 2 stay byte-identical.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/training/backward_pass_state.rs | sed -n '2036,2038p;2164,2170p;1950,1958p' && git show 077dbe2c:crates/cobre-sddp/src/training/backward/by_node.rs | sed -n '144,145p'",
        "output": "backward_pass_state.rs:2036  ) -> Vec<Result<(usize, Vec<StagedCut>), SddpError>> {\nbackward_pass_state.rs:2166  // `drain(..)` leaves capacity intact for the next stage; `w` rides\n            // alongside so the merge resolves each cut's `coefficients_range`\n            // against that worker's arena.\n            Ok((w, ws.backward_accum.staged_cuts_buf.drain(..).collect()))\n        })\n        .collect()\nbackward_pass_state.rs:1950  state.staged_cuts_buf.clear();\n        let mut worker_failure: Option<SddpError> = None;\n        for worker_result in worker_staged {\n            match worker_result {\n                Ok((w, cuts)) => state.staged_cuts_buf.extend(cuts.into_iter().map(|cut| (w, cut))),\nby_node.rs:144  ) -> Vec<Result<(usize, usize), SddpError>> {",
        "reading": "The per-worker closure pushes each staged cut into ws.backward_accum.staged_cuts_buf, a SolverWorkspace-owned buffer that exists to be reused, then returns drain(..).collect(), which allocates a brand new owned Vec<StagedCut> while leaving the reused buffer empty. Rayon then collects those into a fresh Vec<Result<..>>. The caller immediately re-extends its own reused state.staged_cuts_buf from the temporaries and drops them, so both allocations are pure intermediates on a path called once per backward node per rayon worker, in a crate whose hard rule forbids allocation on hot paths. The sibling scheduler already avoids it: process_stage_backward_by_node returns per-worker counts, not owned cut vectors. This is distinct from CD-016, which is the duplicated buffer pre-allocation prologue shared by the same two functions."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/training/backward_pass_state.rs::process_stage_backward",
      "exercisingCallSites": [
        "training/backward_pass_state.rs · compute_one_backward_node (ByScenario arm, per node per iteration)"
      ],
      "mechanism": "allocation per (backward node, rayon worker) at a return boundary: a reused workspace buffer is drained into a fresh owned vector that the caller immediately copies into another reused buffer and drops",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-test-bloat-00",
      "id": "TD-051",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5c",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "a single binary with one duplicated assertion; TD-039 (redundant clp-only binary) calibrated C; the sequencing against the superseded sync_cuts family is the owner's call",
      "upgradeNote": null,
      "calibrationBasis": "one redundant integration binary whose only assertion is the weakest of three copies of the same count-mismatch check (the strongest sits in the unit test with `unreachable!` collective bodies); deleting it is coverage-neutral and removes one solver link — TD-039 precedent (retire a redundant binary), Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [
        "Superseded cut-sync public methods"
      ],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Sequencing call for the cut-pool/training owner: delete the redundant binary now, or let it retire together with the superseded sync_cuts family at the next licensed public-API break, since all three copies of this assertion die with the method."
      ],
      "survivingClaim": "Narrowed to one deletion and stripped of its two supporting claims: at the pin sync_cuts_rejects_mismatched_local_cut_count is the weakest of THREE copies of the count-mismatch assertion, not one of two, because the unit test sync_cuts_invariant_rejects_local_mismatch (src/cut/cut_sync.rs:1672) already holds the strongest copy with unreachable! collective bodies pinning that none fires; the byte-identity is confined to the struct plus six-method impl Communicator bodies (22-68 versus 25-71), the enclosing rustdoc differing; the defensible residue is deleting the single solver-linking binary test_mpi_allgatherv_nonuniform_workers.rs, whose remaining test is strictly weaker than both other copies and whose titular nonuniform-workers assertion lives at src/training/backward/tests.rs:5443. Rejected as over-reach: the section 6 count-parity gate cannot certify a deletion that drops the nextest count by one, and the fallback hoist into test_support.rs is unwarranted because tests/common/mod.rs:86 Rank0Of2 is the existing shared rank-0-of-2 helper. The survivor binary itself must stay: it hosts the real-MPI k_fan rank-invariance gate.",
      "title": "The sync_cuts count-mismatch rejection is asserted twice, in two separate integration binaries, each carrying its own byte-identical 2-rank Communicator stub",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/test_mpi_allgatherv_nonuniform_workers.rs",
          "symbol": "sync_cuts_rejects_mismatched_local_cut_count"
        },
        {
          "path": "crates/cobre-sddp/tests/test_mpi_allgatherv_nonuniform_workers.rs",
          "symbol": "StubComm2Rank"
        },
        {
          "path": "crates/cobre-sddp/tests/test_mpi_sync_cuts_invariant.rs",
          "symbol": "sync_cuts_invariant_rejected_when_cut_count_mismatches"
        },
        {
          "path": "crates/cobre-sddp/tests/test_mpi_sync_cuts_invariant.rs",
          "symbol": "StubComm2Rank"
        }
      ],
      "fixShape": "Delete the test_mpi_allgatherv_nonuniform_workers.rs binary and keep the surviving sync_cuts count-mismatch assertion in test_mpi_sync_cuts_invariant.rs, which is a strict superset of it. This is coverage-neutral, not a coverage reduction: the deleted file's titular nonuniform-workers assertion already lives in the handshake_rejects_nonuniform_workers unit test by its own module doc, and its only remaining test is the byte-for-byte same assertion as the survivor's. It removes exactly one integration binary and one static solver link, which is the per-binary cost testing-architecture.md section 7 names. No CI edit is needed: neither file is enumerated by name under .github or scripts, so the binary roster is discovered by cargo. If the owner instead wants both file names retained, then the alternative is to hoist one strict 2-rank Communicator stub into the existing cobre_sddp::test_support surface behind its current cfg(any(test, feature = 'test-support')) gate per testing-architecture.md section 5.2 and have both binaries consume it; a dedicated fixture crate is explicitly out of bounds. Byte-neutrality bar: the change is confined to crates/cobre-sddp/tests, touches no src path and no LP construction, so the parity goldens in tests/parity.rs and tests/common/parity_hash.rs are untouched, the rank-invariance harness in tests/common/permute.rs is untouched, and mpiexec -n 1 versus -n 2 is unaffected because the deleted test uses a hand-rolled stub rather than the real communicator while the survivor keeps the real-MPI K-fan gate under its test-support feature gate. Gate the change on test-count parity per testing-architecture.md section 6 so the deletion is shown to drop only the duplicated assertion.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/tests/test_mpi_allgatherv_nonuniform_workers.rs > /tmp/a.rs; git show 077dbe2c:crates/cobre-sddp/tests/test_mpi_sync_cuts_invariant.rs > /tmp/b.rs; diff /tmp/a.rs /tmp/b.rs; git grep -c 'mod common;' 077dbe2c -- crates/cobre-sddp/tests/test_mpi_allgatherv_nonuniform_workers.rs crates/cobre-sddp/tests/test_mpi_sync_cuts_invariant.rs; git grep -rn 'allgatherv_nonuniform|test_mpi_sync_cuts_invariant' 077dbe2c -- .github scripts",
        "output": "nonuniform binary = 113 lines, 1 #[test]; sync_cuts_invariant binary = 216 lines, 2 #[test]. Both declare `struct StubComm2Rank` with the same 6-method `impl Communicator` body over 47 lines (nonuniform 22-71, invariant 25-71); the only textual difference is doc-comment wording. Both tests build `CutSyncBuffers::with_distribution(2, 3, 2, 6)` plus `FutureCostFunction::new(1, 2, 3, 10, &[0; 1])`, submit 2 local cuts against per_rank_cuts[0]==3, and assert the same Err(SddpError::Validation) with the same two substrings ('sync_cuts invariant violated', 'rank 0 produced 2 cuts, expected 3'). `mod common;` count = 0 in both files. Neither file name appears anywhere under .github/ or scripts/ at the baseline (0 hits).",
        "reading": "Two integration binaries, two solver links, one assertion. The nonuniform binary's own module doc states the assertion it is named for has moved out: 'The separate `n_workers_local` uniformity handshake is covered by the unit test `handshake_rejects_nonuniform_workers`.' What remains in it is therefore only the duplicate of the sibling's first test. The sibling additionally carries the real-communicator K-fan rank-invariance gate, which the nonuniform binary does not, so the sibling is the strict superset. Per testing-architecture.md section 7 the suite cost is per-binary and per-feature-combo, never per-test, so a wholly redundant binary is the one thing that is cheap to remove without touching coverage."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (integration binaries are expensive; one owner per assertion)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-test-bloat-01",
      "id": "TD-052",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5c",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "three constant helpers inside one binary; the two divergent setup builders are conceded as genuine variants",
      "upgradeNote": null,
      "calibrationBasis": "three constant-valued helpers declared two and three times inside one binary, one pair already drifted unforced (`.unwrap()` vs `.expect(...)`); a file-scope declaration each removes them — TD-035 scale",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Only the three constant-valued helpers duplicate without justification: d01_case_dir at 41, 917 and 1580, d03_case_dir at 908 and 1884, and ascending_stage_end_dates at 54, 1593 and 2745 are fixed deck paths and one pure function of n_pools with no per-group tuning surface, and the d01 copies have already drifted unforced (`.unwrap()` at 41 and 1580 versus `.expect(...)` at 917, with both lint styles allowed file-wide at lines 8-9); each should be declared once at cut_basis.rs file scope. write_test_checkpoint and build_setup are excluded: the former is the assertion-feeding fixture whose baked metadata constants are per-site pinning covered by the module doc at lines 3-5 and the Layer-1 fixtures-untouched invariant, and the latter genuinely diverges in return type at 113 versus 1652. The claim that the wired `mod common` seam is the unused sharing route does not survive, since that seam is used at seven sites and its build_setup_for_case drives from_broadcast_params with patched scalar_parameters rather than StudySetup::new.",
      "title": "cut_basis.rs re-declares five fixture helpers two and three times inside a single binary that already wires mod common",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/cut_basis.rs",
          "symbol": "write_test_checkpoint"
        },
        {
          "path": "crates/cobre-sddp/tests/cut_basis.rs",
          "symbol": "d01_case_dir"
        },
        {
          "path": "crates/cobre-sddp/tests/cut_basis.rs",
          "symbol": "d03_case_dir"
        },
        {
          "path": "crates/cobre-sddp/tests/cut_basis.rs",
          "symbol": "ascending_stage_end_dates"
        },
        {
          "path": "crates/cobre-sddp/tests/cut_basis.rs",
          "symbol": "build_setup"
        },
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "build_setup_for_case"
        }
      ],
      "fixShape": "Declare each of the five helpers once at cut_basis.rs file scope, above the nine inline modules that consume them, since all copies already share one compilation unit and no cfg gate separates them. Keep the two genuinely divergent variants distinguishable rather than forcing one signature: the tuple-returning setup builder and the bare-StudySetup one differ in return type and both have callers, so give the second a distinct name or have it delegate to the first and discard the System. Do not collapse the multi-pool checkpoint writer into test_support::write_synthetic_boundary, which writes a single-cut boundary checkpoint and is a different fixture. Only if a second binary later needs these helpers does ownership move to tests/common; a new fixture crate is out of bounds per testing-architecture.md section 5.2. Byte-neutrality bar: the change is confined to one file under crates/cobre-sddp/tests, so the parity goldens in tests/parity.rs and tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 are all untouched. The relocation is behavior-neutral by construction because the surviving body is the byte-identical text of the copies it replaces; gate it on test-count parity per testing-architecture.md section 6 to prove no assertion was dropped, and note that this file exercises the stored-basis path so the module docs of crates/cobre-sddp/src/cut/basis_reconstruct.rs govern which reconstruct entry point each retained test may call.",
      "evidence": {
        "command": "git grep -nE '^    fn (write_test_checkpoint|d01_case_dir|d03_case_dir|ascending_stage_end_dates|build_setup)' 077dbe2c -- crates/cobre-sddp/tests/cut_basis.rs; then per-pair: git show 077dbe2c:crates/cobre-sddp/tests/cut_basis.rs | sed -n '<range>p' > /tmp/x; diff /tmp/x /tmp/y",
        "output": "cut_basis.rs = 3463 lines, 9 inline modules, `mod common;` declared at line 23. write_test_checkpoint declared 3x (61, 1600, 2754); the 61 and 1600 copies are byte-identical across 52 lines and the 2754 copy differs only by a hoisted `use cobre_sddp::policy_export` block. d01_case_dir declared 3x (41, 917, 1580); 41 and 1580 byte-identical, 917 differs only in .unwrap() versus .expect(). ascending_stage_end_dates declared 3x (54, 1593, 2745), all three byte-identical across 7 lines. d03_case_dir declared 2x (908, 1884), byte-identical. build_setup declared 2x (113, 1652) with the same 5-statement body (load_case, prepare_stochastic with seed 42, prepare_hydro_models, StudySetup::new) differing only in return type, (StudySetup, System) versus StudySetup. Duplicated total is approximately 154 lines inside one compilation unit.",
        "reading": "Every copy is in the same compilation unit as every other, so this duplication buys nothing at all; a single file-scope declaration is visible to all nine inline modules. The binary already wires `mod common;`, so the sharing seam is present and unused for these five helpers. Two of the five are genuinely divergent in signature and must stay distinguishable: build_setup returns a tuple at one site and a bare StudySetup at the other. write_test_checkpoint is NOT the same fixture as test_support::write_synthetic_boundary, which writes a single-cut boundary checkpoint rather than a multi-pool one, so the consolidation must not collapse those two."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (one declaration per helper per binary)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-test-bloat-02",
      "id": "TD-053",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three token-identical `StubComm` / `Rank0Of2` re-declarations in src unit tests that cannot reach the tests/common owner because the crate's EXPORTED test-support surface holds no Communicator double — a fixture with no shared owner (TD-037 precedent), Sev B; the destination (cobre-comm test-support per §5.2 vs the §6 phase-3 fold) is the test-corpus station's E08-2 call",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals; the double is an L0 communicator contract)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": "E08-2 needs-human (StubComm/Rank0Of2 duplication: tests/common/mod.rs + private copies) and E04 TD-037 (tests/common vs test-support gate) — the src-side half is this station's; tests/common is the test-corpus station's",
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Destination owner call for the shared StubComm/Rank0Of2 pair: testing-architecture.md section 5.2 assigns them to cobre-comm, which has no test-support feature at the pin, while section 6 phase 3 collapses tests/common into cobre-sddp's test-support surface; and Rank0Of2's load-bearing forward_passes == 1 caveat names cobre-sddp's RankDistribution, which the infrastructure-genericity hard rule would keep out of cobre-comm."
      ],
      "survivingClaim": "Narrowed to three bodies and no destination: session/mod.rs:1654 StubComm, training/training/tests.rs:173 StubComm and session/mod.rs:2767 Rank0Of2 are token-identical re-declarations of tests/common/mod.rs:32 and :86 that no src unit test can reach because cobre-sddp's EXPORTED test-support surface holds no Communicator double, so one shared pair is warranted. Excluded from the claim: the 'zero impl Communicator in test_support.rs' premise (a seventh token-identical copy sits at src/test_support.rs:3555-3598, private to mod trunk_fan_tests); the fix-shape's destination (testing-architecture.md section 5.2 assigns StubComm/Rank0Of2 to cobre-comm, not cobre-sddp/src/test_support.rs, so the home is an open owner call); the other three src-side bodies, which are strictness variants to preserve rather than fold (backward/tests.rs:233's unreachable broadcast AND the full-slice allreduce equal-length assertion that backward_pass_state.rs:2216 and :2258 drop); and all four cut/cut_sync.rs duplicates, which sit exclusively inside tests of the superseded sync_cuts, pack_local_records and sync_packed_records methods and dissolve pairwise at that ratified seam's trigger.",
      "title": "No Communicator test double lives in the shared test_support surface, so src-side unit tests re-declare StubComm four times and Rank0Of2 twice while tests/common already exports both",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "Rank0Of2"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward/tests.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/src/training/session/mod.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/src/training/training/tests.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "Rank0Of2"
        },
        {
          "path": "crates/cobre-sddp/src/training/session/mod.rs",
          "symbol": "Rank0Of2"
        },
        {
          "path": "crates/cobre-sddp/src/cut/cut_sync.rs",
          "symbol": "ThreeRankComm"
        },
        {
          "path": "crates/cobre-sddp/src/cut/cut_sync.rs",
          "symbol": "TwoRankStubComm"
        }
      ],
      "fixShape": "Move one strict single-rank Communicator double and one rank-0-of-2 double into crates/cobre-sddp/src/test_support.rs behind the cfg(any(test, feature = 'test-support')) gate it already carries, then have the src-side unit tests and tests/common/mod.rs consume that one pair instead of re-declaring it; collapse the two verbatim same-file stub pairs in cut/cut_sync.rs to one declaration each. The consolidation must preserve per-site strictness rather than average it: at least one existing copy implements broadcast as unreachable with the message that broadcast is not used in backward pass tests, and rewriting that to Ok(()) would silently weaken a live assertion, so the shared double keeps the strict body and any site that genuinely needs a permissive broadcast declares that permissiveness explicitly at the call site. Leave FailingComm, FailingBcastComm, NonUniformStubComm, Rank1Of2, StubCommN, LbReconcileStub, ReconcileStub, Rank0Of2Outcome, Rank0Of2Preserve and DualRankStubComm alone; each encodes a different failure or outcome shape and is not a duplicate. Do not introduce a dedicated fixture crate, which testing-architecture.md section 5.2 rules out. Byte-neutrality bar: the moved items are test-only and gated, so no production build changes and the parity goldens in tests/parity.rs and tests/common/parity_hash.rs are unaffected; the rank-invariance harness in tests/common/permute.rs is unaffected because no permutation input changes; mpiexec -n 1 versus -n 2 is unaffected because these are hand-rolled stubs, never the real communicator, and the real-MPI gates keep their own communicator construction. Gate on test-count parity per testing-architecture.md section 6.",
      "evidence": {
        "command": "git grep -nE '^\\s*(pub )?struct (StubComm|Rank0Of2|ThreeRankComm|TwoRankStubComm)\\b' 077dbe2c -- crates/cobre-sddp/src crates/cobre-sddp/tests; git grep -nE 'impl Communicator' 077dbe2c -- crates/cobre-sddp/src/test_support.rs",
        "output": "Inside the 5c manifest: `struct StubComm;` declared 4x (training/backward/tests.rs:233, training/backward_pass_state.rs:2216, training/session/mod.rs:1654, training/training/tests.rs:173) and `struct Rank0Of2;` 2x (training/backward_pass_state.rs:2258, training/session/mod.rs:2767). tests/common/mod.rs already exports `pub struct StubComm` (impl at 34) and `pub struct Rank0Of2` (impl at 88). crates/cobre-sddp/src/test_support.rs is 3652 lines and contains 0 occurrences of `impl Communicator`. Within cut/cut_sync.rs the same file declares `struct ThreeRankComm` twice (1173 and 1301, the two 49-line stub blocks verbatim identical) and `struct TwoRankStubComm` twice (1675 and 1751, verbatim identical over 50 lines).",
        "reading": "tests/common is only reachable from crates/cobre-sddp/tests, whereas crates/cobre-sddp/src/test_support.rs is declared at src/lib.rs under cfg(any(test, feature = 'test-support')) and is therefore reachable from BOTH src unit tests and integration binaries. It is the sanctioned shared-fixture home named by testing-architecture.md section 5.2, yet it carries no Communicator double at all, which is exactly why the src side re-rolls one. The two verbatim same-file duplicates inside cut/cut_sync.rs are duplication with no reachability excuse whatsoever. Genuinely distinct doubles that must NOT be folded in: FailingComm, FailingBcastComm, NonUniformStubComm, Rank1Of2, StubCommN, LbReconcileStub, ReconcileStub, Rank0Of2Outcome, Rank0Of2Preserve, DualRankStubComm."
      },
      "yardstickSection": "§5.2 uniform test-support feature convention / §5.8 (StubComm and Rank0Of2 are the kept harness doubles)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5c-test-bloat-03",
      "id": "TD-054",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5c",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "eight same-named `MockSolver` doubles restate the inert subset of the eleven required `SolverInterface` methods identically, so a new required trait method is an eight-site edit — the TD-037 ripple, Sev B; the strict per-site behaviours are conceded as genuine and must be preserved by any consolidation",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Owner call on the shape of the inert-only consolidation: a cfg-gated declarative macro in test_support.rs versus the candidate's field-configured shared struct, since a single struct cannot express the seven-to-one get_basis split and the two strict sites without adding modes no test reads."
      ],
      "survivingClaim": "Only the inert subset of the eleven required methods is genuine duplication across the eight sites — the ActiveProfile associated type, the empty apply_profile, the constant solver_name_version returning the literal MockSolver 0.0.0 and the no-op set_row_bounds (identical at all eight, differing only in ignored parameter names), plus the SolverStatistics::default() statistics/statistics_into pair at six of the eight — and it is that inert restatement alone that makes a new required trait method an eight-site edit here. The behavioral bodies are not duplication and do not reduce to one or two knobs: solve is four structurally distinct modes, get_basis splits seven-to-one at backward_pass_state.rs:2363, statistics is three-way, and two sites carry live strictness a shared default would erase (the copy_from_slice length check at forward_pass_state.rs:1138 and the set_col_bounds_calls == 2 * n_openings NCS per-opening assertion at lower_bound.rs:2093). The 'no solver double in test_support' premise is conceded as false: test_support.rs:1859 already impls SolverInterface for TemplateCaptureSolver and test_support.rs:140 fill_consistent_basis is already the shared basis body seven of the eight call.",
      "title": "Eight same-named MockSolver doubles re-implement the SolverInterface required methods across the manifest, with no shared base in test_support",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/training/backward/tests.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/forward/tests.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/forward_pass_state.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/lower_bound.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/session/mod.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/training/training/tests.rs",
          "symbol": "MockSolver"
        },
        {
          "path": "crates/cobre-sddp/src/workspace/workspace.rs",
          "symbol": "MockSolver"
        }
      ],
      "fixShape": "Put one configurable solver double in crates/cobre-sddp/src/test_support.rs behind the cfg(any(test, feature = 'test-support')) gate it already carries, implementing the 11 required SolverInterface methods once, and let each of the eight sites configure the one or two behaviors it actually varies instead of restating the whole surface. Keep it a plain struct with fields, not a trait-object or closure-per-method design, so the crate-wide ban on Box<dyn Trait> is respected and the double stays enum-or-field dispatched. Preserve each site's current strictness exactly: where a body panics or asserts on an unexpected call today, the shared double must keep a mode that still panics there rather than returning a default, because turning a panic into a silent success removes a live assertion. Leave the genuinely distinct recording and probing doubles alone, specifically the two-phase mock in cut/dcs.rs, the recording mock in the row builder, the recording solver in training/stage_solve_prep/tests.rs and the per-child probe solver in training/backward/tests.rs. Byte-neutrality bar: the change is test-only and feature-gated so no production path and no LP construction changes, leaving the parity goldens in tests/parity.rs and tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 all unaffected. Gate on test-count parity per testing-architecture.md section 6, and note that the lower-bound and backward-pass sites are covered by pinned contracts in .claude/rules/sddp.md, so no assertion about Benders cut sign, column-bound state pinning or NCS availability factors may be weakened while the doubles are consolidated.",
      "evidence": {
        "command": "git grep -nE '^\\s*(pub )?struct MockSolver' 077dbe2c -- crates/cobre-sddp/src/cut crates/cobre-sddp/src/training crates/cobre-sddp/src/solve crates/cobre-sddp/src/workspace crates/cobre-sddp/src/convergence; then per file: git show 077dbe2c:crates/cobre-sddp/src/<f> | awk '/impl SolverInterface for MockSolver/{f=1} f{c++; brace-depth} f&&d==0&&c>1{print c; exit}'",
        "output": "8 declarations of the identically-named `MockSolver` inside the 5c manifest: training/backward/tests.rs:283, training/backward_pass_state.rs:2298, training/forward/tests.rs:57, training/forward_pass_state.rs:1101, training/lower_bound.rs:873, training/session/mod.rs:1593, training/training/tests.rs:96, workspace/workspace.rs:1155. Their `impl SolverInterface for MockSolver` block sizes are 64, 48, 45, 43, 52, 46, 52 and 31 lines, total 381 lines. The trait they satisfy, SolverInterface in crates/cobre-solver/src/trait_def.rs, declares 13 methods of which 11 are required, so each of the eight sites pays the whole required surface to vary one or two behaviors.",
        "reading": "One name, eight bodies, 381 lines of boilerplate whose only purpose is to satisfy the 11 required methods so a test can vary a single return. Adding a required method to SolverInterface is therefore an eight-site edit inside this crate alone. The shared home already exists and is reachable from src unit tests: crates/cobre-sddp/src/test_support.rs is gated cfg(any(test, feature = 'test-support')), but it holds no solver double. Genuinely distinct doubles that are not part of this claim, because each records or fails in a way a shared base would have to expose anyway: TwoPhaseMock in cut/dcs.rs, RecordingMockSolver in the row builder, RecordingSolver in training/stage_solve_prep/tests.rs, and PerChildProbeSolver in training/backward/tests.rs."
      },
      "yardstickSection": "§5.2 uniform test-support feature convention (one configurable double on the crate's test-support surface; no `Box<dyn Trait>`)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-architecture-00",
      "id": "CD-087",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "import-path spelling only: no type, behaviour or dependency changes, and the public re-export surface is untouched — the CD-021 organisational precedent is C",
      "upgradeNote": null,
      "calibrationBasis": "the crate-root alias block gives each cluster submodule a second, cluster-blind in-crate path and internal code mixes both spellings inside single `use crate` trees — an import-path organisational asymmetry with no type or behaviour change; CD-021 precedent (module-shape asymmetry against the directory-module convention), Sev C; the public half of the block is the owner's call",
      "category": "asymmetry",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV §IV.2 (the lp/ aliases are exactly the surface the cobre-model carve-out moves)",
      "defenderAlignmentHint": "advances-0b",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Whether the public half of the alias block is also retired is an owner call: lib.rs:23-26 declares the pub mod namespaces non-semver-stable and the nested paths already resolve, but removing pub use policy::orchestration would touch cobre-cli, cobre-python, the integration corpus and the literal match in scripts/ci/check_python_parity.py."
      ],
      "survivingClaim": "Narrowed to the internal-import half of the alias block at lib.rs:56-99: the crate-root aliases give each cluster submodule a second, cluster-blind in-crate path, and internal code mixes both spellings inside a single use crate tree (workspace/ as context plus workspace at simulation/enumerated.rs:27 and :43; solve/ as crate::solver_phase at simulation/state.rs:33 plus solve::partition at :50; cut/ as cut plus cut_sync at training/backward_pass_state.rs:33-34), including inside one cluster where production/hydro_models/production.rs pairs super::types at 27 with crate::energy_conversion at 32 and crate::fpha_fitting at 35 and types.rs:27 repeats it. Conceded and stripped: no alias is dead, so the five-zero-use finding and the delete-outright half of the fix do not survive (four are reached from braced use crate trees, and orchestration is load-bearing for cobre-cli, cobre-python, the integration corpus and the literal string the python-parity gate matches); and the cluster-blindness is confined to the aliased names, not the import surface generally.",
      "title": "A crate-root flat alias namespace shadows the directory-module tree, so the same file reaches the same cluster by two different paths and an import states nothing about which cluster it crosses into",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/lib.rs",
          "symbol": "lp"
        },
        {
          "path": "crates/cobre-sddp/src/lib.rs",
          "symbol": "production"
        },
        {
          "path": "crates/cobre-sddp/src/lib.rs",
          "symbol": "workspace"
        },
        {
          "path": "crates/cobre-sddp/src/lib.rs",
          "symbol": "solve"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/pipeline.rs",
          "symbol": "solve_simulation_stage"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/enumerated.rs",
          "symbol": "run_enumerated_simulation"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_stage_result"
        },
        {
          "path": "crates/cobre-sddp/src/production/hydro_models/production.rs",
          "symbol": "resolve_production_models_from_artifacts"
        },
        {
          "path": "crates/cobre-sddp/src/production/hydro_models/mod.rs",
          "symbol": "prepare_hydro_models"
        }
      ],
      "fixShape": "Separate the two roles the alias layer currently conflates. Keep the curated public re-export surface in lib.rs, which is what downstream and the published API depend on, and retire the crate-root module aliases as an INTERNAL import path, rewriting internal call sites to name the owning cluster, so an import reads production::energy_conversion, lp::indexer, lp::builder, stochastic::noise, solve::stage_solve and workspace::context. Delete outright the five aliases with zero internal uses. Do the lp aliases first and in isolation, because lp::builder and lp::indexer carry the largest internal reference counts and are exactly the surface the cobre-model carve-out has to move, so every remaining crate::lp_builder or crate::indexer import is a site that carve-out must touch blind. Byte-neutrality bar: import-path-only change with no item moved, so cargo fmt --all --check plus the parity goldens in crates/cobre-sddp/tests/parity.rs and the rank-invariance harness in tests/common/permute.rs are sufficient; no .claude/rules/sddp.md contract is touched.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/lib.rs | sed -n '52,99p' ; for f in simulation/enumerated.rs simulation/state.rs simulation/pipeline.rs; do git show 077dbe2c:crates/cobre-sddp/src/$f | grep -n -E '^\\s*(context|stage_solve|dcs|indexer|lp_builder|noise|energy_conversion)::|^\\s*(workspace|solve|lp|stochastic|cut|production)(::|\\{)'; done ; git grep -c -E '^\\s{4}(context|stage_solve|indexer|lp_builder|hydro_models|cut_selection|resolved_parameters|risk_measure|stopping_rule|energy_conversion|training_session|dcs|noise|fpha_fitting|generic_constraints|policy_export|trajectory|visited_states|basis_reconstruct|inflow_method)::' 077dbe2c -- crates/cobre-sddp/src | awk -F: '{s+=$NF; n++} END{print n\" files\", s\" braced-use lines\"}'",
        "output": "lib.rs declares 32 crate-root aliases over the cluster tree, among them 'pub use workspace::context;', 'pub use solve::solver_phase;', 'pub(crate) use solve::stage_solve;', 'pub use lp::builder as lp_builder;', 'pub use lp::indexer;', 'pub(crate) use production::fpha_fitting;', 'pub use production::{energy_conversion, hydro_models};', 'pub(crate) use stochastic::{noise, stochastic_summary};', 'pub use cut::{basis_reconstruct, cut_selection, cut_sync, dcs};'. In simulation/enumerated.rs ONE use crate::{...} tree carries 'context::{StageContext, TrainingContext}' at line 27 and 'workspace::SolverWorkspace' at line 43. simulation/state.rs carries 'context::{...}' at 36 and 'workspace::{CapturedBasis, SolverWorkspace}' at 52, plus 'crate::solver_phase::Phase::Simulation' at 18 against 'solve::partition' at 50. simulation/pipeline.rs carries 'context::{...}' at 34, 'dcs::{...}' at 35, 'indexer::{...}' at 36 and 'workspace::{...}' at 53, and above the tree five separate 'use crate::stage_solve::' lines plus 'use crate::energy_conversion::', 'use crate::indexer::', 'use crate::lp_builder::' and three 'use crate::noise::' lines. The alias count sweep returns '33 files 109 braced-use lines'; the per-alias internal reference counts include indexer 71, hydro_models 48, lp_builder 26, cut_selection 25, resolved_parameters 22, and five aliases at zero internal uses (cut_sync, orchestration, backward_pass_state, rank_reconcile, state_exchange).",
        "reading": "The crate has two path namespaces for the same modules and internal code standardizes on the flat one, so a cluster boundary the directory tree was created to express is invisible at the import site. The sharpest form is one file reaching one cluster twice by different names: simulation/enumerated.rs names workspace::context as 'context' at line 27 and workspace::workspace as 'workspace' at line 43 in the SAME use tree, and simulation/state.rs does the same for solve, naming solve::solver_phase as 'solver_phase' at line 18 and solve::partition by its real path at line 50. Nothing in the file records that 'crate::energy_conversion' is production/, that 'crate::indexer' and 'crate::lp_builder' are lp/, that 'crate::noise' is stochastic/, or that 'crate::stage_solve' is solve/. The cost has already propagated into a cluster's own contract: production/hydro_models/mod.rs states 'Every public symbol is re-exported here so both the curated flat surface in lib.rs and the cobre_sddp::hydro_models::Symbol module path resolve to the same item', and the leak reaches inside the cluster too, since production/hydro_models/production.rs:32 and types.rs:27 import their own sibling as 'crate::energy_conversion' rather than by a within-cluster path. Five aliases have zero internal uses, so the layer is not even uniformly load-bearing. This is not the sanctioned #[allow] census and not a mirror-registered seam: git grep over docs/design/reserved-seams-and-deferred-debt.md for shim, re-export, flat, alias and crate-root returns no matching entry."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-architecture-01",
      "id": "CD-088",
      "class": "CD",
      "lens": "architecture",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a documentation-and-organisation misfit: one engine-to-cobre_io projection homed in the production cluster whose module doc does not cover it, while every sibling projection sits beside its source types; the destination (intra-engine move vs the Phase-0a shared output orchestration) decides the hint",
      "category": "leaky-boundary",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V §V.1 Phase 0 (shared output orchestration in cobre-io) / Part IV §IV.1",
      "defenderAlignmentHint": "advances-0a",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [
        "Terminal boundary FCF is booked in the reported total cost"
      ],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Destination is an owner call and decides the hint: re-home inside the engine beside simulation/types.rs (a pure local cleanup, hint degrades to neutral) or leave it until Phase 0a's shared output orchestration in cobre-io absorbs the engine-to-io projection wholesale (target-layering-brief.md section 2, 0a bullet), which keeps advances-0a but makes the intra-engine move wasted work."
      ],
      "survivingClaim": "Narrowed to a documentation-and-organization misfit: production/mod.rs:3-6 states a cluster purpose that does not cover production/conversion.rs, whose sole source domain is simulation/types.rs, making it the one engine-to-cobre_io projection in cobre-sddp homed outside the domain that owns its source types (unlike training/training_output.rs, policy/policy_export.rs, production/hydro_models/export.rs, fixed_delivery_echo.rs and generic_constraint_echo.rs). Conceded and dropped from the claim: the file breaks no layering or dependency rule (production/ imports cobre_io elsewhere), costs nothing at any call site or to Python parity (both front ends reach it by From coherence without naming the module), and the doc sentence is judged as written rather than as an amendment.",
      "title": "The simulation-result to cobre-io write-payload conversion is homed in the production-modeling cluster, whose module doc had to be amended to describe it",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/production/conversion.rs",
          "symbol": "IntoWriteRecord"
        },
        {
          "path": "crates/cobre-sddp/src/production/conversion.rs",
          "symbol": "with_node"
        },
        {
          "path": "crates/cobre-sddp/src/production/mod.rs",
          "symbol": "conversion"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/types.rs",
          "symbol": "SimulationStageResult"
        }
      ],
      "fixShape": "Re-home the file beside the types it converts, as a simulation-owned write-payload projection module, so the production cluster doc states one purpose again and the cluster boundary is not carrying an unrelated member; the private IntoWriteRecord trait and with_node helper move with it unchanged. Governing contract: .claude/rules/sddp.md section Terminal boundary FCF is booked in the reported total cost governs the cost fields these records carry, so the move must stay field-for-field with no re-derivation of any cost component, and the Python-parity hard rule means the CLI and cobre-python write paths must continue to reach the same conversion. Byte-neutrality bar: a pure move of trait impls with no signature change, verified by the parity goldens in crates/cobre-sddp/tests/parity.rs with tests/common/parity_hash.rs, which hash the written simulation output. Defer the decision on whether the destination is the engine or the Phase 0a cobre-io output orchestration to the owner, since one target makes the other move redundant.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/production/mod.rs ; git show 077dbe2c:crates/cobre-sddp/src/production/conversion.rs | grep -n -E '^use |^impl |^trait |^fn |crate::' ; git grep -n -E 'production::conversion|conversion::' 077dbe2c -- crates/cobre-sddp/src crates/cobre-cli/src crates/cobre-python/src",
        "output": "production/mod.rs doc: 'Production-modeling cluster ... groups the preprocessing that turns reservoir geometry and plant data into the per-(hydro, stage) production representations the LP consumes — FPHA hyperplane fitting, resolved production/evaporation models, energy-conversion scalars, and the simulation write-payload conversions.' conversion.rs module doc: 'Field-for-field conversions from Simulation*Result to write-payload types.' Its only non-test imports are 'use cobre_io::output::simulation_writer::{...}' at line 3 and 'use crate::simulation::types::{...}' at line 12; the sole crate::setup::NodeId import at line 11 is #[cfg(test)]. It declares 13 'impl IntoWriteRecord for Simulation*Result' blocks, 'trait IntoWriteRecord' at 298, 'fn with_node' at 304, and the two public conversions 'impl From<SimulationStageResult> for StageWritePayload' and 'impl From<SimulationScenarioResult> for ScenarioWritePayload'. The production::conversion grep returns zero hits naming the module path anywhere in cobre-sddp, cobre-cli or cobre-python; every other hit is energy_conversion.",
        "reading": "The file has no production-modeling content and no reference to any sibling in its own cluster: it reads simulation/types.rs and writes cobre_io::output::simulation_writer, nothing else. Because its entire surface is trait impls, callers reach it by type inference and no call site ever names production::conversion, so its home is invisible and therefore arbitrary. The evidence that the placement is wrong is written in production/mod.rs itself: the cluster doc states a single coherent purpose, reservoir geometry and plant data into per-(hydro, stage) production representations, then appends 'and the simulation write-payload conversions' because one member does not fit the sentence. This is the engine-side half of the output write path, which is the surface Phase 0a moves into shared orchestration in cobre-io, and locating it under production/ hides it from that carve-out. Checked and absent from the prior corpus: the register mentions production/conversion.rs only at prior-register.md:173 as the surviving location of the anticipated delivery_date field, a reserved-seam note, not a placement claim, and the mirror has no IntoWriteRecord or production/conversion entry."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-over-engineering-00",
      "id": "OD-040",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5d",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "of 42 bare `#[allow]` openers only the four `cast_sign_loss` sites in `resolve_fitting_bounds` survive, and not as a comment gap: no non-negativity guarantee for the four discretisation counts exists in cobre-io's raw struct, `validate_model_fields` or the schema, and the `< 2` / `< 1` guards test the already-cast `usize`, so a declared negative count wraps past every guard — a real input-boundary hazard confined to one fn, Sev B; the residue is a cobre-io validation gap routed to reconciliation (E11) for re-filing against the owning crate",
      "category": "missing-seam",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L1 cobre-io owns input validation; the L3 consumer trusts it)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "reconciliation (re-file the cobre-io validation gap)"
      ],
      "needsHuman": [
        "Doc-owner call: the mirror sentence at reserved-seams-and-deferred-debt.md:1278-1284 asserts every numeric-cast allow on production code carries a `// Rationale:`, which comments.md D4 and the E4 gate's IN_SCOPE_LINTS both contradict; decide whether that sentence is corrected down to the D4 scope or D4 is widened to cover cast_* lints.",
        "Routing call: the surviving residue is a missing input-boundary validation owned by cobre-io, not an over-engineering item in cobre-sddp; decide whether it stays on this station's over-engineering ledger or is re-filed against the owning crate."
      ],
      "survivingClaim": "Only 4 of the 42 bare openers survive, and not as a comment gap: at the four `#[allow(clippy::cast_sign_loss)]` sites in `resolve_fitting_bounds` (`geometry.rs:157-164`) the suppressed sign loss is unmitigated, because no non-negativity guarantee for `volume_discretization_points` and its three siblings exists in cobre-io's raw struct, in `validate_model_fields`, or in the exported schema, and the `< 2` / `< 1` guards test the already-cast `usize`, so a declared negative count wraps past every guard into `build_grid` instead of raising `InsufficientDiscretization`. The 42-site rationale sweep and the census-premise refutation do not survive: comments.md D4 and the E4 gate's `IN_SCOPE_LINTS` both exclude numeric-cast lints, so the mirror sentence is a prose overclaim, not 42 sites of code debt.",
      "title": "42 of 69 production-region `#[allow(...)]` openers in the 5d manifest carry no rationale, refuting the premise the census clearing rests on",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/geometry.rs",
          "symbol": "resolve_fitting_bounds"
        },
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/grid.rs",
          "symbol": "build_grid"
        },
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/secant.rs",
          "symbol": "fit_gamma_s"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/aggregation.rs",
          "symbol": "aggregate_simulation"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_hydro_per_block"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/state.rs",
          "symbol": "run_worker_scenarios"
        }
      ],
      "fixShape": "Sharpen, do not sweep. For each of the 42 bare openers either narrow the attribute to the expression it guards or add the single clause that records the fact living outside the file, which for the fpha_fitting cluster is the non-negativity validated on the cobre-io config fields. Never delete an allow that CI's zero-warning bar still needs. Under the comment-discipline Deletion Test the clause qualifies precisely because the justifying fact is cross-crate. The work is comment-and-attribute-only and must be byte-neutral: assert the parity goldens in tests/parity.rs through tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction, and move no golden. Blast radius is 11 files under production/fpha_fitting/, production/hydro_models/ and simulation/, with no signature or control-flow change. Touches no pinned contract in .claude/rules/sddp.md, so no section citation applies.",
      "evidence": {
        "command": "python3 - <<'EOF'\nimport re,subprocess\nfiles=subprocess.run(['git','ls-tree','-r','--name-only','077dbe2c','crates/cobre-sddp/src/'],capture_output=True,text=True).stdout.split()\n# 5d manifest = the 41 non-test files of error.rs, lib.rs, hull/, lead_time/, production/, simulation/\nrat=re.compile(r'rationale|safety:|precision|deliberate|by design|reserved',re.I)\n# for each file: keep only the region before the first top-level #[cfg(test)];\n# an opener is 'rationale' if the contiguous comment/attr block above it matches rat,\n# 'other-comment' if that block or the same line carries any //, else 'bare'.\nEOF",
        "output": "files 41 openers 69 rationale 15 other-comment 12 bare 42\nbare-by-file [('simulation/extraction.rs', 11), ('simulation/state.rs', 6), ('simulation/aggregation.rs', 5), ('production/fpha_fitting/geometry.rs', 4), ('production/fpha_fitting/grid.rs', 4), ('production/fpha_fitting/secant.rs', 4), ('simulation/pipeline.rs', 3), ('simulation/enumerated.rs', 2), ('production/fpha_fitting/rng.rs', 1), ('production/hydro_models/export.rs', 1), ('production/hydro_models/production.rs', 1)]\nbare-lints [('cast_possible_truncation', 30), ('cast_sign_loss', 6), ('cast_precision_loss', 6)]\n\n$ git show 077dbe2c:crates/cobre-sddp/src/production/fpha_fitting/geometry.rs | sed -n '155,164p'\n    #[allow(clippy::cast_sign_loss)]\n    let requested_volume_points = config.volume_discretization_points.unwrap_or(5) as usize;\n    #[allow(clippy::cast_sign_loss)]\n    let n_flow_points = config.turbine_discretization_points.unwrap_or(5) as usize;\n    #[allow(clippy::cast_sign_loss)]\n    let n_spillage_points = config.spillage_discretization_points.unwrap_or(5) as usize;\n    #[allow(clippy::cast_sign_loss)]\n    let max_planes = config.max_planes_per_hydro.unwrap_or(10) as usize;\n\n$ git show 077dbe2c:crates/cobre-io/src/extensions/production_models.rs | sed -n '171p'\n    pub volume_discretization_points: Option<i32>,\n\n$ git show 077dbe2c:docs/design/reserved-seams-and-deferred-debt.md | sed -n '1278,1284p'\n**Load-bearing.** Numeric-cast lints (`cast_possible_truncation`,\n`cast_precision_loss`, `cast_sign_loss`, `cast_possible_wrap`) and\nrefactor-decision lints (`too_many_arguments`, `too_many_lines`,\n`type_complexity`, `struct_field_names`, `implicit_hasher`,\n`needless_pass_by_value`, and similar) on production code, **each carrying a\n`// Rationale:` comment naming the non-obvious choice the lint would otherwise\nflag** — the majority of the census.",
        "reading": "The mirror's census clearing rests on a stated premise: every numeric-cast allow on production code carries a `// Rationale:` naming the non-obvious choice. Over the 5d manifest that premise does not hold for 42 of 69 production-region openers, which carry no comment of any kind. All 42 are numeric-cast lints (cast_possible_truncation 30, cast_sign_loss 6, cast_precision_loss 6) spread over 11 files. This is not the settled \"this allow exists\" claim the do-not-touch entry clears, and it is disjoint from OD-009: zero of the 42 are `dead_code`, and OD-009's three anchors live under training/. The geometry.rs cluster shows why the missing clause is load-bearing rather than cosmetic: the four casts are sound only because `volume_discretization_points` and its three siblings are validated non-negative, and that fact lives in a different crate (cobre-io production_models.rs, typed `Option<i32>`), so a reader of geometry.rs alone cannot reconstruct it. Anchors name six enclosing declared fns; `enumerated_sim_stage_worker` also encloses two bare openers but is deliberately not anchored because PD-001 names that symbol and is refuted."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-over-engineering-01",
      "id": "OD-041",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three tailrace types carry `pub(crate)` that no signature forces and that contradicts the module's declared crate surface; marker-level cleanup narrowed to one unit (the family's `segments` field types the segments type); the two other items are conceded as forced",
      "category": "speculative-generality",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Scope call: whether crate-internal single-file items should be narrowed to module-private at all, or whether pub(crate) is the ratified uniform internal marker for this workspace; if narrowing is wanted it should be one crate-wide pass over the 25 sites with a per-site forcing check, not this five-anchor slice."
      ],
      "survivingClaim": "Narrowed to the tailrace trio only, and to a marker-level cleanup rather than a defect: QuarticSegment (tailrace.rs:60), TailraceSegments (:86) and TailraceFamily (:189) hold pub(crate) that no signature forces and that contradicts the module's own declared crate surface at fpha_fitting/mod.rs:66, and they must be narrowed as one unit because TailraceFamily's pub segments field (:194) types TailraceSegments. SimWorkerParams drops out (mirror of ForwardWorkerParams, whose pub(crate) is forced by pub(crate) fn run_forward_worker) and DEFAULT_REFERENCE_VOLUME_FRACTION drops out (its 'sole owner' doc is about the 0.65 literal, held in production code, with the cross-file protocol at types.rs:441-443 routed through resolve_reference_volume_hm3). The trio is 3 of 25 single-file pub(crate) declarations out of 397 in the crate, so it belongs to a crate-wide visibility pass with per-site forcing checks, not to a standalone three-file edit.",
      "title": "Five `pub(crate)` items in production/ and simulation/ have zero consumers outside their defining file and nothing forces the wider visibility",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/tailrace.rs",
          "symbol": "QuarticSegment"
        },
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/tailrace.rs",
          "symbol": "TailraceSegments"
        },
        {
          "path": "crates/cobre-sddp/src/production/fpha_fitting/tailrace.rs",
          "symbol": "TailraceFamily"
        },
        {
          "path": "crates/cobre-sddp/src/production/hydro_models/production.rs",
          "symbol": "DEFAULT_REFERENCE_VOLUME_FRACTION"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/state.rs",
          "symbol": "SimWorkerParams"
        }
      ],
      "fixShape": "Narrow each of the five to the visibility its single defining file actually needs, which is private for QuarticSegment, TailraceSegments, TailraceFamily, DEFAULT_REFERENCE_VOLUME_FRACTION and SimWorkerParams, keeping TailraceFamilies and build_tailrace_families_map at pub(crate) because mod.rs re-exports them. Compilation is the verifier: if private_interfaces fires on any of the five, that item is structurally forced and stays pub(crate) with the reason recorded. A visibility narrowing emits no code, but still assert byte-neutrality through the parity goldens in tests/parity.rs via tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2, and move no golden. Blast radius is three files. No pinned contract in .claude/rules/sddp.md is touched.",
      "evidence": {
        "command": "for s in QuarticSegment TailraceSegments TailraceFamily DEFAULT_REFERENCE_VOLUME_FRACTION SimWorkerParams; do echo \"== $s\"; git grep -n -w \"$s\" 077dbe2c -- 'crates/**/*.rs' | sed 's/^077dbe2c://'; done; git show 077dbe2c:crates/cobre-sddp/src/production/fpha_fitting/mod.rs | sed -n '66p'",
        "output": "== QuarticSegment\ncrates/cobre-sddp/src/production/fpha_fitting/tailrace.rs (all 6 hits: the pub(crate) struct, its impl, and Vec<QuarticSegment> inside this file)\n== TailraceSegments\ncrates/cobre-sddp/src/production/fpha_fitting/tailrace.rs (all 5 hits inside this file)\n== TailraceFamily\ncrates/cobre-sddp/src/production/fpha_fitting/tailrace.rs (all 7 hits inside this file; TailraceFamilies is a distinct word and is exported)\n== DEFAULT_REFERENCE_VOLUME_FRACTION\ncrates/cobre-sddp/src/production/hydro_models/production.rs (2 hits: the pub(crate) const and its one use in resolve_reference_volume_hm3)\n== SimWorkerParams\ncrates/cobre-sddp/src/simulation/state.rs (3 hits: the pub(crate) struct, its construction, and the &SimWorkerParams<'_> parameter of the private fn run_worker_scenarios)\n\n$ git show 077dbe2c:crates/cobre-sddp/src/production/fpha_fitting/mod.rs | sed -n '66p'\npub(crate) use tailrace::{TailraceFamilies, build_tailrace_families_map};\n\n$ git show 077dbe2c:crates/cobre-sddp/src/production/hydro_models/production.rs | sed -n '655,660p'\n/// The sole owner of this literal.\npub(crate) const DEFAULT_REFERENCE_VOLUME_FRACTION: f64 = 0.65;",
        "reading": "Five items are declared `pub(crate)` and have zero mentions outside their defining file, so the wider visibility publishes a crate-wide surface no crate-wide consumer wants. The counter-signal that would justify it is absent in each case: Rust's private_interfaces lint forces `pub(crate)` only when a type appears in a wider-visibility signature, and here `TailraceSegments.segments`, `TailraceFamilies.families` and every field of `SimWorkerParams` are private while `run_worker_scenarios` is a private fn, so nothing forces it. None carries a reserved-seam rationale or `#[allow(dead_code)]`, so none is in the mirror's reserved class. The intended crate surface is stated explicitly at fpha_fitting/mod.rs line 66, which re-exports only TailraceFamilies and build_tailrace_families_map. The const contradicts itself: its own doc calls it the sole owner of the literal while its visibility offers it to the whole crate, and it never appears in a signature."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-over-engineering-02",
      "id": "OD-042",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a one-item impl block (`SimulationInputs::new`, its `too_many_arguments` allow and rationale) whose struct-literal replacement is proven by the test twin `run_simulate` already constructing the bundle at the same call shape",
      "category": "redundant-wrapper",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "The removable residue is exactly the one-item impl block at simulation/state.rs:87-118 — `SimulationInputs::new`, its `#[allow(clippy::too_many_arguments)]` and its RATIONALE comment — justified not by a duplicated suppression pair but by `run_simulate` at simulation/pipeline/tests.rs:38-66 being a signature-for-signature twin of `simulate` that already constructs the bundle with the struct literal at lines 54 and 92, proving the literal compiles at that exact call shape; the suppression on `simulate` itself is independently earned by its own public ten-parameter signature and survives.",
      "title": "`SimulationInputs::new` is a ten-parameter pass-through constructor whose only production caller already carries the suppression it duplicates",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/simulation/state.rs",
          "symbol": "SimulationInputs"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/pipeline.rs",
          "symbol": "simulate"
        }
      ],
      "fixShape": "Delete `SimulationInputs::new` and have `simulate` build the value with the struct literal the sibling tests at simulation/pipeline/tests.rs already use, which removes the state.rs suppression and its duplicated rationale. The suppression on `simulate` itself stays, because its own ten parameters are the public signature and are not in scope here. No field visibility changes, since all ten fields are already pub. The change is call-shape only and must be byte-neutral: assert the parity goldens in tests/parity.rs through tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs, and mpiexec -n 1 versus -n 2 reproduction, and move no golden. Blast radius is two files plus the two sibling test sites that already compile against the literal form. Touches no pinned contract in .claude/rules/sddp.md.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/simulation/state.rs | sed -n '52,118p'; git show 077dbe2c:crates/cobre-sddp/src/simulation/pipeline.rs | sed -n '1040,1085p'; git grep -n -w 'SimulationInputs' 077dbe2c -- 'crates/**/*.rs'",
        "output": "$ sed -n '52,118p' simulation/state.rs\npub(crate) struct SimulationInputs<'a, S: SolverInterface + Send, C> {\n    pub workspaces: &'a mut [SolverWorkspace<S>],\n    pub ctx: &'a StageContext<'a>,\n    pub fcf: &'a FutureCostFunction,\n    pub training_ctx: &'a TrainingContext<'a>,\n    pub config: &'a SimulationConfig,\n    pub output: SimulationOutputSpec<'a>,\n    pub frozen_templates: Option<&'a [StageTemplate]>,\n    pub node_bases: &'a [Option<CapturedBasis>],\n    pub comm: &'a C,\n    pub traversal: &'a Traversal,\n}\n    /// Construct a `SimulationInputs` bundle from positional arguments.\n    // RATIONALE: a field-for-field bundle constructor; splitting would only\n    // relocate the parameter list, not reduce it — the struct already bundles\n    // what can be bundled.\n    #[allow(clippy::too_many_arguments)]\n    pub(crate) fn new(workspaces, ctx, fcf, training_ctx, config, output,\n                      frozen_templates, node_bases, comm, traversal) -> Self {\n        Self { workspaces, ctx, fcf, training_ctx, config, output,\n               frozen_templates, node_bases, comm, traversal }\n    }\n\n$ sed -n '1040,1085p' simulation/pipeline.rs\n// RATIONALE: simulate() is a thin argument-forwarding shim to\n// SimulationState::run; splitting would only relocate the parameter list, not\n// reduce it — SimulationInputs already bundles what can be bundled.\n#[allow(clippy::too_many_arguments)]\npub fn simulate<S, C: Communicator>(...ten params...) -> Result<SimulationRunResult, SimulationError> {\n    let mut state = SimulationState::new(training_ctx.horizon.num_stages());\n    state.set_profile(config.profile);\n    state.run(&mut SimulationInputs::new(workspaces, ctx, fcf, training_ctx, config,\n        output, frozen_templates, node_bases, comm, traversal))\n}\n\n$ git grep -n -w SimulationInputs 077dbe2c -- 'crates/**/*.rs'\nsimulation/state.rs (declaration, impl, and the &mut SimulationInputs param of run)\nsimulation/pipeline.rs:1084 (the single ::new call site)\nsimulation/pipeline/tests.rs:54  let mut inputs = SimulationInputs {\nsimulation/pipeline/tests.rs:92  (second struct-literal construction)",
        "reading": "`SimulationInputs::new` takes ten positional parameters and forwards all ten unchanged into a struct literal, deciding nothing: no defaulting, no validation, no derived field. Every one of the ten fields is `pub`, so the struct literal is available to every caller, and the crate's own sibling tests already use it twice at simulation/pipeline/tests.rs lines 54 and 92 rather than calling `new`. There is exactly one production call site, inside `simulate`, which itself already carries the identical suppression. The constructor therefore exists only to require a second copy of `#[allow(clippy::too_many_arguments)]` and a near-duplicate rationale comment. The written rationale does not answer this finding: it defends against splitting the parameter list, whereas the alternative here is the struct literal the tests already use, which needs no parameter list at all. This is both named 5d shapes at once, a wrapper that forwards without deciding and a struct whose only inherent method is a constructor."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-performance-00",
      "id": "PD-045",
      "class": "PD",
      "lens": "performance",
      "subStation": "5d",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "an element-wise divide over the entire per-stage `row_lower` template repeated on every (scenario, stage) simulation solve although both operands are per-stage constants and only the load-balance span is ever read — the residue is a per-stage cache of that span; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "Owner picks where the per-stage cache lives (shared `StageContext` vs per-worker simulation scratch); the span-only residue shrinks the memory side of that trade from a full row vector to the load-balance rows."
      ],
      "survivingClaim": "Narrowed to the `row_scale` non-empty branch (every production template after `postprocess_templates`): the removable per-(scenario, stage) work in `build_row_lower_unscaled` is the element-wise division and its length/zero guard, not the buffer fill (the candidate's copy keeps a full-length pass over a capacity-retaining workspace buffer), and only the load-balance span of the result is ever read (simulation/extraction.rs:1420), so the defensible residue is a per-stage cache of that span's unscaled values under absolute row indexing, not of the whole row vector.",
      "title": "Per-(scenario, stage) element-wise unscale of a per-stage constant row_lower template in the simulation hot path",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/simulation/pipeline.rs",
          "symbol": "build_row_lower_unscaled"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/pipeline.rs",
          "symbol": "extract_sim_stage_result"
        }
      ],
      "fixShape": "Compute the unscaled row_lower once per stage and, on each call, copy the cached vector into the scratch buffer and overwrite only the load-balance span that actually varies per scenario. The cache is per-stage read-only data, so it belongs on StageContext, or on SimulationState scratch if built lazily per worker, per the context-struct decision tree in .claude/architecture-rules.md; keep build_row_lower_unscaled within its recorded argument budget. Byte-neutral because each element's value is the same quotient val / scale computed from the same operands, division is deterministic, and the row order is untouched: the parity goldens in tests/parity.rs with tests/common/parity_hash.rs, the rank-invariance harness in tests/common/permute.rs (the cache is keyed by stage index, never by entity order), and mpiexec -n 1 against -n 2 all see identical bytes since the cache derives from already-broadcast templates.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/simulation/pipeline.rs | sed -n '223,247p;664,673p'",
        "output": "fn build_row_lower_unscaled<'a>(\n    template_row_lower: &[f64],\n    row_scale: &[f64],\n    load_rhs_buf: &[f64],\n    scratch_buf: &'a mut Vec<f64>,\n    ...\n) -> &'a [f64] {\n    scratch_buf.clear();\n    scratch_buf.reserve(template_row_lower.len());\n    if row_scale.is_empty() {\n        scratch_buf.extend_from_slice(template_row_lower);\n    } else {\n        for (i, &val) in template_row_lower.iter().enumerate() {\n            let scale = if i < row_scale.len() && row_scale[i] != 0.0 { row_scale[i] } else { 1.0 };\n            scratch_buf.push(val / scale);\n        }\n    }\n--\n    let row_lower_ref = build_row_lower_unscaled(\n        &ctx.template(t).row_lower,\n        &ctx.template(t).row_scale,\n        load_rhs_buf,\n        row_lower_buf,\n        ctx.n_load_buses,\n        load_row_start,\n        load_n_blks,\n        ctx.load_bus_indices,\n    );",
        "reading": "build_row_lower_unscaled divides every element of template_row_lower by the matching row_scale entry. Both scaling inputs at the only production call site are ctx.template(t).row_lower and ctx.template(t).row_scale, per-stage constants read off StageContext. The one scenario-dependent input is load_rhs_buf, which overwrites the n_load_buses * n_blks load-balance span after the pass. The call sits inside extract_sim_stage_result, which solve_simulation_stage invokes once per (scenario, stage), so the whole row vector is re-divided once per solve while producing identical values for every scenario at that stage."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/simulation/pipeline.rs::build_row_lower_unscaled",
      "exercisingCallSites": [
        "simulation/pipeline.rs · extract_sim_stage_result ← solve_simulation_stage (per scenario per stage)"
      ],
      "mechanism": "Redundant pass: an element-wise divide over the entire per-stage row_lower template is repeated on every (scenario, stage) solve, although both scaling operands are per-stage constants and only the load-balance rows differ per scenario.",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-performance-01",
      "id": "PD-046",
      "class": "PD",
      "lens": "performance",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "the residue is O(log n) reallocations per vector per (scenario, stage), a reservation asymmetry rather than an unbounded allocation pattern; bounded to one file",
      "upgradeNote": null,
      "calibrationBasis": "a reservation asymmetry with a logarithmic residue: three per-block extraction vectors (four when PAR lags are present) grow by amortised doubling from a first-group seed while six siblings pre-reserve the same product — a bounded number of reallocations per (scenario, stage); structural calibration only, UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrowed to a reservation asymmetry with a logarithmic residue: the three per-block branches (extract_hydros' turbine branch, extract_exchanges, extract_buses) and, only when state.max_par_order is positive, extract_stub_collections' inflow_lags grow their result vector by amortized doubling from a capacity seeded at one entity's block group, because Vec's nested from_iter reads the FlatMap size_hint after pulling the first element and the live frontiter then reports that group; the cost is one reallocation per doubling with a wide-record prefix copy, not the zero-capacity cold start the title's 'no reservation' implies, and the fix removes only the redundant regrowth, so the item is an asymmetry against the five sibling functions that already reserve the exact product, not a breach of the no-allocation-on-hot-paths rule that Vec::with_capacity per call would equally violate.",
      "title": "Four per-(scenario, stage) extraction vectors collect from flat_map with no reservation while six siblings in the same file pre-reserve the same product",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_hydros"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_exchanges"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_buses"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/extraction.rs",
          "symbol": "extract_stub_collections"
        }
      ],
      "fixShape": "Build each of the four vectors with Vec::with_capacity over the same product its pre-reserving sibling already reserves and extend from the flat_map, or drive the nested loop directly in the shape extract_thermals uses. Byte-neutral by construction: capacity is not observable in the output, push order is unchanged, so the emitted row order and every parity golden hash are identical, the rank-invariance harness in tests/common/permute.rs is unaffected, and MPI reproduction is unaffected because each worker extracts only its own scenarios. This also brings the four sites into line with the crate hard rule against allocating on hot paths.",
      "evidence": {
        "command": "git grep -n \"flat_map\" 077dbe2c -- crates/cobre-sddp/src/simulation/extraction.rs; git grep -n \"Vec::with_capacity\" 077dbe2c -- crates/cobre-sddp/src/simulation/extraction.rs",
        "output": "extraction.rs:1187:            .flat_map(|(h, &hydro_id)| {\nextraction.rs:1340:            .flat_map(move |(l, &line_id)| {\nextraction.rs:1394:            .flat_map(move |(bus_idx, &bus_id)| {\nextraction.rs:2058:        .flat_map(|(h, &hydro_id)| {\n--\nextraction.rs:1207:        let mut results = Vec::with_capacity(n_cells);\nextraction.rs:1224:        let mut results = Vec::with_capacity(n_cells * n_blks);\nextraction.rs:1285:        let mut results = Vec::with_capacity(spec.entity_counts.thermal_ids.len() * n_blks);\nextraction.rs:1865:        let mut results = Vec::with_capacity(n_ncs * n_blks);\nextraction.rs:1933:        let mut results = Vec::with_capacity(n_pumping * n_blks);\nextraction.rs:2004:        let mut results = Vec::with_capacity(n_contracts * n_blks);",
        "reading": "The four flat_map collect sites fall inside extract_hydros (declared at 1167, per-block branch), extract_exchanges (1314), extract_buses (1367) and extract_stub_collections (2043). Their output lengths are known before the walk: hydro_ids.len() * n_blks, line_ids.len() * n_blks, bus_ids.len() * n_blks, and hydro_ids.len() * state.max_par_order. A FlatMap reports a zero size_hint lower bound before consumption and is not TrustedLen, so collect starts at zero capacity and grows by repeated reallocation. The pre-reserving siblings in the same file cover exactly the same count shapes: extract_hydro_bus_generation, extract_thermals, extract_non_controllables, extract_pumping_stations, extract_contracts. All of these run inside extract_stage_result_with_lookups, once per (scenario, stage)."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/simulation/extraction.rs::extract_stage_result_with_lookups",
      "exercisingCallSites": [
        "simulation/extraction.rs · extract_hydros / extract_exchanges / extract_buses / extract_stub_collections (per scenario per stage)"
      ],
      "mechanism": "Allocation per call: collect over a FlatMap begins at zero capacity and grows by reallocation, so each of these per-(scenario, stage) result vectors performs a sequence of reallocations and copies where one exact allocation would do.",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-performance-02",
      "id": "PD-047",
      "class": "PD",
      "lens": "performance",
      "subStation": "5d",
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one deep clone of a thirteen-vector stage result per owned distinct arena node in the enumerated re-expansion (not per leaf path as titled), with the final-stage clone the bookkeeping-free subset; dense reification on the enumerated simulation path; structural calibration only, UNMEASURED",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals; enumerated orchestration)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "The avoidable copy in re_expand is one deep clone per owned distinct arena node, not one per visiting leaf path: dispatch_scenario_result takes the stage-result Vec by value and moves it into the channel, so each path must own its materialized rows and only a node's terminal visit can become a move. The bookkeeping-free subset is the final-stage clone at enumerated.rs:401, where paths.leaf is injective over the single-predecessor tree so a path's own leaf result has exactly one reader. The title's immutable-borrow causation does not survive, because the caller already holds the scratch mutably at enumerated.rs:494-499.",
      "title": "re_expand deep-clones each captured node result once per visiting leaf path because its scratch is borrowed immutably",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/simulation/enumerated.rs",
          "symbol": "re_expand"
        },
        {
          "path": "crates/cobre-sddp/src/simulation/types.rs",
          "symbol": "SimulationStageResult"
        }
      ],
      "fixShape": "Take the scratch as &mut and move each node's captured result out on its last visiting path, cloning only for the earlier visitors; under the canonical enumeration the leaf paths beneath a node are contiguous, so the last visitor is identifiable from the walk itself without extra bookkeeping. Do not convert the field to a shared handle: an Arc inside SimulationStageResult would drag the serde rc feature into the output types. Byte-neutral because the value pushed is identical whether cloned or moved and the push order into stage_results is untouched, so parity goldens and the rank-invariance harness observe the same bytes; MPI reproduction is unaffected since re_expand runs per worker over its own scenario range.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/simulation/enumerated.rs | sed -n '367,374p;398,402p'; git show 077dbe2c:crates/cobre-sddp/src/simulation/types.rs | sed -n '471,515p' | grep -n 'pub '",
        "output": "fn re_expand(\n    plan: &EnumeratedPlan,\n    scratch: &EnumeratedSimScratch,\n    scenario_range: Range<u32>,\n    num_stages: usize,\n    ctx: &StageContext<'_>,\n    output: &SimulationOutputSpec<'_>,\n) -> Result<(WorkerCosts, WorkerStats), SimulationError> {\n--\n            let cum_d = ctx.cumulative_discount_factor(StageIdx(t));\n            total_cost += cum_d * visit.immediate_cost;\n            SolverStatsDelta::accumulate_into(&mut leaf_stats, &visit.stats);\n            stage_results.push(visit.result.clone());\n--\npub struct SimulationStageResult { stage_id, node_id,\n  pub costs: Vec<SimulationCostResult>,\n  pub hydros: Vec<SimulationHydroResult>,\n  pub hydro_bus_generation: Vec<SimulationHydroBusResult>,\n  pub thermals: Vec<SimulationThermalResult>,\n  pub exchanges: Vec<SimulationExchangeResult>,\n  pub buses: Vec<SimulationBusResult>,\n  pub pumping_stations: Vec<SimulationPumpingResult>,\n  pub contracts: Vec<SimulationContractResult>,\n  pub non_controllables: Vec<SimulationNonControllableResult>,\n  pub inflow_lags: Vec<SimulationInflowLagResult>,\n  pub transit_buckets: Vec<SimulationTransitBucketResult>,\n  pub generic_violations: ..., pub anticipated_lanes: ... }",
        "reading": "re_expand receives scratch as an immutable &EnumeratedSimScratch, so no visitor can move a captured result out; its only consumer of a captured node is stage_results.push(visit.result.clone()). SimulationStageResult declares thirteen owned Vec fields (costs, hydros, hydro_bus_generation, thermals, exchanges, buses, pumping_stations, contracts, non_controllables, inflow_lags, transit_buckets, generic_violations, anticipated_lanes), so each clone is a thirteen-vector deep copy of a result the census sweep already solved and captured exactly once. A node shared by many leaf paths is therefore re-materialized once per path. This is a different site and a different mechanism from the refuted per-node capture allocation in enumerated_sim_stage_worker: that one allocates once per solved node, this one copies an already-captured result once per visiting path."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "enumerated",
      "profiledSymbol": "crates/cobre-sddp/src/simulation/enumerated.rs::re_expand",
      "exercisingCallSites": [
        "simulation/enumerated.rs · run_enumerated_simulation → re_expand (per owned arena node per sweep)"
      ],
      "mechanism": "Dense reification: an already-captured per-node result carrying thirteen owned entity vectors is deep-copied once per leaf path that visits the node, instead of being moved out on its final visit.",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-performance-05",
      "id": "PD-048",
      "class": "PD",
      "lens": "performance",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "once per run at study setup, never per solve or per iteration; the project rule accepts setup-time recomputation — the rule-11 scope question (superlinear setup cost) goes to the owner gate",
      "upgradeNote": null,
      "calibrationBasis": "a study-setup cost whose SIZE scales as computed-FPHA plant count × total history rows (each per-plant scan reads the whole shared table) but which runs once per run and only for plants on the `ComputedFromGeometry` branch — setup-time recomputation is acceptable by project rule, recorded for the size scaling only; structural calibration, UNMEASURED and not queued",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Rule-11 scope call the owner may want to settle once for all similar candidates: does the setup-time per-plant carve-out cover a per-plant recomputation whose cost is sized by a whole shared table (superlinear in plant count), or only one sized by the plant's own data? I read it as the latter and narrowed accordingly."
      ],
      "survivingClaim": "Narrowed to a one-time study-setup cost whose SIZE, not its once-per-plant recurrence, is the defect: each `long_term_mean_inflow` call is sized by the whole shared inflow-history table while it retains only one plant's rows, so setup scan work grows as computed-FPHA plant count times total history rows instead of total history rows once. Only plants reaching the `ComputedFromGeometry` branch at production.rs:289 pay it, so a precomputed-FPHA or constant-productivity case scans nothing, and no per-solve, per-iteration or per-stage path is affected.",
      "title": "long_term_mean_inflow rescans the entire inflow-history table once per hydro inside the parallel production fit",
      "anchors": [
        {
          "path": "crates/cobre-sddp/src/production/hydro_models/production.rs",
          "symbol": "long_term_mean_inflow"
        },
        {
          "path": "crates/cobre-sddp/src/production/hydro_models/production.rs",
          "symbol": "fit_one_hydro"
        }
      ],
      "fixShape": "Accumulate per-hydro sums and counts in one pass over inflow_history() before the parallel map, into a container indexed by the hydro's canonical declaration position, and pass the resolved mean into fit_one_hydro the way the other pre-grouped maps are already passed. This preserves the determinism note on the function: a single sequential pass in stored canonical order adds each hydro's rows in exactly the order the current per-hydro scan does, no add is reordered and no partitioned reduction is introduced, so every mean is bit-identical. Declaration-order invariance holds because the accumulator is indexed by declaration position rather than by first appearance in the history table. Guarded by the parity goldens over the exported FPHA planes plus the rank-invariance harness.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-sddp/src/production/hydro_models/production.rs | sed -n '390,398p;286,290p;161,166p'",
        "output": "fn long_term_mean_inflow(system: &System, hydro_id: EntityId) -> f64 {\n    let mut sum = 0.0_f64;\n    let mut count = 0_u64;\n    for row in system.inflow_history() {\n        if row.hydro_id == hydro_id {\n            sum += row.value_m3s;\n            count += 1;\n        }\n    }\n--\n            let long_term_mean_inflow_m3s = long_term_mean_inflow(system, hydro.id);\n--\n    let fits: Vec<PerHydroFit> = system\n        .hydros()\n        .par_iter()\n        .map(|hydro| {\n            fit_one_hydro(",
        "reading": "long_term_mean_inflow iterates system.inflow_history() in full and keeps only the rows matching one hydro_id. Its call site is inside fit_one_hydro on the ComputedFromGeometry branch, and fit_one_hydro is the body of system.hydros().par_iter().map(...), so the whole history table is scanned once per hydro that computes planes. Every other per-hydro row table that fit_one_hydro consumes is pre-grouped exactly once before the parallel map and passed in as a map: config_map, geometry_map built by build_geometry_map, families_map built by build_tailrace_families_map, and hyperplane_map. The inflow history is the one table left as a repeated filtered scan."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-sddp/src/production/hydro_models/production.rs::long_term_mean_inflow",
      "exercisingCallSites": [
        "production/hydro_models/production.rs · fit_one_hydro (once per computed-FPHA hydro at setup)"
      ],
      "mechanism": "Redundant pass: a full linear scan of the inflow-history table filtered to a single hydro_id, repeated once per computed-FPHA hydro, where one grouped pass before the parallel map would serve all of them.",
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-test-bloat-01",
      "id": "TD-055",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "two tautological tests in one binary; the precedent for a test asserting its own literal (TD-038, TD-040) is C — the false-confidence guard is the finding, the fix is S",
      "upgradeNote": null,
      "calibrationBasis": "two of six tests in one probe binary assert string literals against a const declared in the same file and passed green through the rename that rotted them — tautological tests (TD-038 / TD-040 precedents: a test asserting its own transcription), Sev C; the rot is the evidence, not the blast radius",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Owner call on whether the audited k_max partition should survive in any executable form now that the shift-to-hold switchover has landed and there are zero k_max >= 2 tier-1 goldens: retire the checklist to prose, or re-home it as a script-backed gate that resolves each name against a nextest listing, since Rust cannot reflect over another integration binary's tests (the probe's own module doc, lines 25-28)."
      ],
      "survivingClaim": "Narrowed from the whole file to two of its six tests. lead_ge2_rebaseline_contains_the_named_k2_k3_fixtures (hold_k1_byte_stability_probe.rs:217-229) requires three string literals to appear in a const declared 45 lines above it in the same file, and it therefore passed green when commit 19ecadbd renamed simulation_ring_buffer_shifts_anticipated_state_k2 to simulation_commitment_hold_carries_anticipated_state_k2 in the same commit that also edited this probe, leaving lines 172 and 222 as the only two occurrences of a name that resolves to no test fn at the pin. k1_byte_stability_verdict (lines 231-250) has zero assert sites and five println!, so it has no failing path. Explicitly conceded and NOT part of the claim: the three DeliveryRing tests at lines 32-94 exercise production code and must stay; k1_survivors_and_lead_ge2_rebaseline_are_disjoint (lines 197-215) enforces a real cross-list partition invariant over all 46 entries and is not confirmed for deletion; and the k2 coverage was renamed rather than deleted, so the false confidence is in the probe's stale index, not in lost k2 coverage.",
      "title": "hold_k1_byte_stability_probe.rs asserts over its own string literals and one required fixture name no longer exists, so the guard passes green while verifying nothing",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "lead_ge2_rebaseline_contains_the_named_k2_k3_fixtures"
        },
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "LEAD_GE2_REBASELINE"
        },
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "K1_SURVIVORS"
        },
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "k1_survivors_and_lead_ge2_rebaseline_are_disjoint"
        },
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "k1_byte_stability_verdict"
        },
        {
          "path": "crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs",
          "symbol": "AUDITED_FIXTURE_FILES"
        },
        {
          "path": "crates/cobre-sddp/tests/anticipated_scenarios.rs",
          "symbol": "simulation_ring_buffer_shifts_anticipated_state_k1"
        }
      ],
      "fixShape": "Keep the three DeliveryRing primitive tests, which are the only part of this binary that touches production code. Delete k1_byte_stability_verdict, whose five println! lines carry no assertion and are invisible under a normal cargo test run, and delete the two list-shape assertions that compare in-file literals to each other. Relocate the audited k_max partition and its rationale to prose that no green test can misrepresent, either the module doc that already holds the argument or the sddp rules file, and fix or drop the rotted simulation_ring_buffer_shifts_anticipated_state_k2 entry while doing so. If the partition must stay executable, make the assertion resolve each name against the test tree rather than against a sibling literal in the same file, so a renamed or deleted fixture fails the guard. Byte-neutrality: the probe writes no output and reads no golden, and the three retained tests construct DeliveryRing directly, so nothing in the parity or rank-invariance sets is touched; the reproduction pair still runs to confirm.",
      "evidence": {
        "command": "git grep -n 'simulation_ring_buffer_shifts_anticipated_state' 077dbe2c ; for each of the 46 quoted names in K1_SURVIVORS/LEAD_GE2_REBASELINE/EXCLUDED_NOT_RING_GEOMETRY_SENSITIVE: git grep -q \"fn <name>(\" 077dbe2c -- crates/cobre-sddp/tests/ ; git show 077dbe2c:crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs | grep -c -e '#\\[test\\]' -e assert -e 'println!' ; git show 077dbe2c:crates/cobre-sddp/tests/hold_k1_byte_stability_probe.rs | grep -n '^use'",
        "output": "46 distinct fixture names are quoted across the three const lists; 45 resolve to a 'fn <name>(' declaration under crates/cobre-sddp/tests/, 1 does not: simulation_ring_buffer_shifts_anticipated_state_k2. Repo-wide at the pin that string occurs only at hold_k1_byte_stability_probe.rs:172 (inside LEAD_GE2_REBASELINE) and hold_k1_byte_stability_probe.rs:222 (inside the required-names array of lead_ge2_rebaseline_contains_the_named_k2_k3_fixtures); the sibling _k1 name does resolve, at anticipated_scenarios.rs:1430. File census: 250 lines, 9 #[test] attributes, 17 assert sites, 5 println! sites, exactly 1 production import (use cobre_sddp::lp_builder::DeliveryRing at line 30). k1_byte_stability_verdict (line 232) contains 5 println! calls and 0 asserts.",
        "reading": "Three of the nine tests exercise production code through the single DeliveryRing import and are legitimate. The other structure is self-referential: lead_ge2_rebaseline_contains_the_named_k2_k3_fixtures asserts that a hand-written &[&str] in the same file contains three string literals the same file writes, so the assertion can only fail if someone edits both sites inconsistently; it cannot detect that simulation_ring_buffer_shifts_anticipated_state_k2 has no corresponding test fn anywhere in the repo, which is the state at the pin. k1_survivors_and_lead_ge2_rebaseline_are_disjoint likewise compares three in-file literals to each other. k1_byte_stability_verdict has zero assertions and its five println! lines are swallowed by cargo test without --nocapture, so it can never fail and reports nothing in a normal run. The file's own module doc states the partition 'is an audited, re-runnable manual enumeration, not a mechanically-provable one' - which is exactly why the two list-shape tests are not a guard for it. One name has already rotted, which makes this a demonstrated false-confidence guard, not a hypothetical one."
      },
      "yardstickSection": "§3.2 sustainability (a test that restates its own literals is invisible and therefore dead)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    },
    {
      "candidateRef": "5d-test-bloat-04",
      "id": "TD-056",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "5d",
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one local double reuses the harness's `StubComm` name for a type with incompatible collective semantics inside a file that compiles `mod common;`, with a stale Single-rank rustdoc and a dead `rank` field — naming, stale-doc and dead-field only; the three-way consolidation is conceded and the harness pair stays per §5.8",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (L3 engine internals)",
      "defenderAlignmentHint": "neutral",
      "partIRef": null,
      "priorId": null,
      "relatedTo": [],
      "mergedFrom": null,
      "retiredOverlaps": null,
      "crossStation": "E08-2 needs-human (communicator doubles in tests/common) — kept distinct from 5c-test-bloat-02 (harness shape vs src-side re-declaration)",
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only or doc-only change; no rendered LP byte can move (bounded by the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction as a formality)",
      "contractCited": [],
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Owner call: whether testing-architecture.md section 5.8 (keep StubComm/Rank0Of2) plus the section 5.2 move into cobre-comm's test-support surface is the final disposition for the harness pair, or whether the test-corpus station's E08-2 needs-human item may still merge them - my dismissal of the merge half rests on section 5.8 being ratified.",
        "Owner call: whether conformance.rs:40 LocalComm's no-copy collectives (Ok(()) without writing recv, unlike common::StubComm) are deliberate; I read the divergence as semantics-by-design, but no rustdoc states why."
      ],
      "survivingClaim": "Confined to tests/simulation_pipeline_integration.rs:78 - the local double reuses the harness's StubComm identifier for a type whose collective semantics are incompatible with it, inside a file that compiles `mod common;` at line 51; its rustdoc at line 77 says Single-rank while line 1173 instantiates it with `size: 2`; and its `rank` field is 0 at all 20 sites. A naming, stale-rustdoc and dead-field defect only. The three-way consolidation, the premise that the parameterized type subsumes the two harness unit structs, and the anchors common/mod.rs::StubComm, common/mod.rs::Rank0Of2, conformance.rs::LocalComm and integration.rs::ShutdownComm do not survive.",
      "title": "The shared harness carries two hardcoded single-shape communicators while one binary carries the parameterized one that subsumes both, so the rank shape a test needs decides which of three types it reaches for",
      "anchors": [
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/tests/common/mod.rs",
          "symbol": "Rank0Of2"
        },
        {
          "path": "crates/cobre-sddp/tests/simulation_pipeline_integration.rs",
          "symbol": "StubComm"
        },
        {
          "path": "crates/cobre-sddp/tests/integration.rs",
          "symbol": "ShutdownComm"
        },
        {
          "path": "crates/cobre-sddp/tests/conformance.rs",
          "symbol": "LocalComm"
        }
      ],
      "fixShape": "Replace the three shapes with one parameterized stub communicator in the shared harness that takes rank and size and carries an explicit mode for the collectives a given path must not touch, so the pipeline tests keep their assert-unused semantics as a stated mode rather than as a separate type. The two existing unit structs become constructor calls at their 20 and 4 call sites and the local copy in simulation_pipeline_integration.rs disappears along with the name shadowing. This is not a blind delete: the unreachable! bodies are load-bearing for the pipeline tests that assert no collective fires on the frozen path, and the displs-offset copy in the 2-rank shape must survive verbatim. Byte-neutrality: communicator behaviour on every currently exercised path stays identical, so no golden moves; verify against the tier-1 parity goldens, the rank-invariance permutation harness, and a single-rank versus two-rank MPI reproduction, which is the direct check for this candidate.",
      "evidence": {
        "command": "git grep -n -E '^ *(pub )?struct StubComm' 077dbe2c -- crates/cobre-sddp/tests/ ; git grep -l 'common::StubComm' 077dbe2c -- crates/cobre-sddp/tests/ | wc -l ; git grep -l 'Rank0Of2' 077dbe2c -- crates/cobre-sddp/tests/ ; git show 077dbe2c:crates/cobre-sddp/tests/simulation_pipeline_integration.rs | grep -nE 'mod common|struct StubComm|StubComm \\{ rank'",
        "output": "tests/common/mod.rs:32 declares 'pub struct StubComm;' as a unit struct whose rank() returns 0 and size() returns 1; 20 integration binaries use common::StubComm. tests/common/mod.rs also declares 'pub struct Rank0Of2;', a second unit struct whose rank() returns 0 and size() returns 2, used by 4 files (common/mod.rs, mpi_wire.rs, node_native_backward_gate.rs, simulation_integration.rs). tests/simulation_pipeline_integration.rs:51 declares 'mod common;' and imports common::builders at line 52, then at line 78 declares its own 'struct StubComm { rank: usize, size: usize }' and instantiates it at 15 sites, 14 with size: 1 and one with size: 2 at line 1173. integration.rs:86 declares ShutdownComm and conformance.rs:40 declares LocalComm as two further local Communicator impls.",
        "reading": "The same concept is spelled three ways in one crate's test surface: a fixed 1-rank unit struct and a fixed 2-rank unit struct in the shared harness, plus a parameterized rank/size struct local to one binary that covers both shapes and shadows the shared name inside a file that imports the same common module. The collective bodies are not interchangeable as written, so this is fragmentation rather than a redundant copy: the local one panics via unreachable! on allgatherv, allreduce and broadcast, common::StubComm copies send into recv, and common::Rank0Of2 copies at the displs[0] offset. That difference is the reason a consolidation has to be deliberate rather than a delete, and it is also the reason a test author picks by rank shape instead of by semantics today."
      },
      "yardstickSection": "§5.8 (StubComm / Rank0Of2 are the kept harness doubles) / §5.1",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null
    }
  ],
  "reusedIds": [
    {
      "id": "CD-001",
      "registerId": "CD-001",
      "registerLine": 152,
      "wave": 4,
      "disposition": "retire",
      "resolvingCommit": "b051c410",
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/stochastic_pipeline.rs",
        "symbol": "build_stochastic_context_for_study"
      },
      "supersededClaim": null,
      "survivingClaim": null,
      "registerSeverity": "A",
      "severity": "A",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (the target layering: the stochastic-context rebuild is L3 engine-internal work, owned by cobre-sddp)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "n/a — retired",
      "actionable": true
    },
    {
      "id": "CD-003-construction-hop",
      "registerId": "CD-003",
      "registerLine": 176,
      "wave": 4,
      "disposition": "retire",
      "resolvingCommit": "4075c4e8",
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/params.rs",
        "symbol": "from_config"
      },
      "supersededClaim": "the Broadcast→Construction hop: a 21-field ConstructionConfig literal assembled by into_construction_config from StudyParams, the third near-isomorphic carrier",
      "survivingClaim": "Config → BroadcastConfig (cobre-cli commands/broadcast.rs) → StudyParams::from_config (setup/params.rs) is a two-carrier residue: the MPI arm still hand-projects Config into BroadcastConfig, then every rank runs StudyParams::from_config — this residue is CD-004's Wave-4 business, not a re-open of the hop",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V.1 Phase 0 (D14 MPI semantics: rank 0 executes, non-roots skip setup and idle to a final barrier; engine-tagged setup stages); Part IV.4 (engine-selection seam without a dependency cycle)",
      "phase0aRestatement": {
        "artifact": "rank-0-executes MPI shape",
        "shape": "the CD-002/CD-003 residue (a positional load tuple plus a hand-projected wire config that every rank re-derives) is restated as the resolved D14 MPI semantics: rank 0 executes setup and — for a direct study — the whole study serially, non-roots skip setup and idle to a final barrier, ranks_participated records 1; the SDDP engine keeps its collective setup behind the same seam, so the wire carrier becomes the one projection CD-004 names rather than a per-rank re-derivation",
        "guardrail": "the projection/stage types name no engine and no paradigm; the Engine enum stays at L4 (cobre-cli / cobre-python, IV.4); cobre-sddp gains no dependency on a sibling engine; no seam is stood up for a single hypothetical consumer (pull, don't push, V.0)",
        "byteNeutralityBar": "parity goldens (crates/cobre-sddp/tests/parity.rs + tests/common/parity_hash.rs) + the rank-invariance harness (tests/common/permute.rs shuffle matrix) + mpiexec -n 1 / -n 2 reproduction; SDDP output bit-for-bit unchanged is the Phase-0a gate itself (V.1 Gate)",
        "anchorOwner": "cli-python (the BroadcastConfig / broadcast_and_build_setup anchors are CD-002's, owned by E06); recorded here as the restated residue of the retired hop"
      },
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "n/a — retired",
      "actionable": true
    },
    {
      "id": "CD-006",
      "registerId": "CD-006",
      "registerLine": 227,
      "wave": 4,
      "disposition": "retire",
      "resolvingCommit": "3f4c3db3",
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/node_graph.rs",
        "symbol": "frontier_node"
      },
      "supersededClaim": null,
      "survivingClaim": null,
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (NodeGraph is the engine's traversal structure; free-fn → method is intra-L3)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "n/a — retired",
      "actionable": true
    },
    {
      "id": "CD-004",
      "registerId": "CD-004",
      "registerLine": 192,
      "wave": 4,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/params.rs",
        "symbol": "from_config"
      },
      "supersededClaim": "three ~20-field near-isomorphic structs (StudyParams / ConstructionConfig / BroadcastConfig) and the params → ConstructionConfig hop — deleted by 4075c4e8",
      "survivingClaim": "the wire projection is untouched: Config → BroadcastConfig (crates/cobre-cli/src/commands/broadcast.rs, from_config at :143) and Config → StudyParams (setup/params.rs from_config at :186) are still two hand-kept field-for-field twins (both project export_states from config.exports.states); one config knob still costs edits on both sides and a silent MPI-vs-local divergence is still the failure mode",
      "registerSeverity": "B (A-risk)",
      "severity": "B (A-risk)",
      "calibrationBasis": "CD-004 precedent: the in-process and wire projections must agree field-for-field or MPI diverges SILENTLY from local; the A-risk marker is carried by the silence of the divergence, not by blast radius",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V.1 Phase 0 (the study/config schema, the typed admission gate and per-engine solver-profile scoping land first and alone, byte-identical against the existing SDDP engine); Part IV.4 (the engine-selection seam without a dependency cycle)",
      "phase0aRestatement": {
        "artifact": "study-block admission-gate carrier",
        "shape": "ONE postcard-serializable projection of Config, itself broadcast on the wire and consumed by both the in-process and the MPI path, replacing the StudyParams/BroadcastConfig twin. The serde tagged-enum breakage that owner-confirmed forced the split is handled once, at that projection's boundary. Per-engine section admissibility and per-engine solver-profile scoping are decided at the same gate, so a phase-keyed block naming a phase the chosen engine lacks is a rejection, not a silent ignore.",
        "guardrail": "the projection/stage types name no engine and no paradigm; the Engine enum stays at L4 (cobre-cli / cobre-python, IV.4); cobre-sddp gains no dependency on a sibling engine; no seam is stood up for a single hypothetical consumer (pull, don't push, V.0)",
        "byteNeutralityBar": "parity goldens (crates/cobre-sddp/tests/parity.rs + tests/common/parity_hash.rs) + the rank-invariance harness (tests/common/permute.rs shuffle matrix) + mpiexec -n 1 / -n 2 reproduction; SDDP output bit-for-bit unchanged is the Phase-0a gate itself (V.1 Gate)",
        "anchorOwner": "sddp (the BroadcastConfig half is anchored in cli-python via CD-002)"
      },
      "e05_5Delta": {
        "candidateRef": "5a-architecture-01",
        "verdict": "confirmed",
        "survivingClaim": "Verified delta over CD-004: the two projections are NOT independent Config readers -- BroadcastConfig::from_config calls StudyParams::from_config at broadcast.rs:144 and every wire field except training_enabled, policy_mode and the two scenario sources traces to that single params value -- so CD-004's silent MPI-vs-local value-divergence surface reduces to exactly ONE site, export_states at broadcast.rs:214, which re-reads config.exports.states although params.rs:339 already resolved it onto the params still in scope, and which cobre-python/src/run.rs:997 reads off the carrier instead. The title's structural half does not survive: training_enabled and policy_mode are front-end orchestration facts that reach no StudyParams and no StudySetup on any path, and scalar_parameters is not a Config projection, so there is no two-way symmetric difference in the Config projection itself.",
        "argument": null,
        "needsHuman": []
      },
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-005",
      "registerId": "CD-005",
      "registerLine": 206,
      "wave": 4,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/mod.rs",
        "symbol": "StudySetup"
      },
      "supersededClaim": "a local struct split: 'half-finished NCS sub-struct extraction' plus a field-count census (35 fields, 2026-08-22) framed as god-struct cleanup",
      "survivingClaim": "StudySetup conflates precomputed model state (stage_data, stochastic, fcf, hydro_models, scenario_libraries, node_graph, the ncs_* patch vectors) with resolved run-config (loop_params, simulation_config, policy_path, …); the setter wall in setup/accessors.rs (set_start_iteration, set_warm_start_basis_cache, set_export_states, set_budget, set_risk_measures, set_scheduler, set_hardest_first_claim_order) exists only because run-config lives in the data struct",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V.1 Phase 0 (engine-tagged setup stages skipping stochastic reconstruction for engines that do not consume it); Part IV.4 (the seam refactors the StudySetup-typed run pipeline)",
      "phase0aRestatement": {
        "artifact": "engine-tagged setup stages",
        "shape": "the StudySetup split into precomputed-model state versus resolved run params IS the seam that lets a non-SDDP engine skip stochastic reconstruction: setup stages become engine-tagged so an engine that does not consume the stochastic context never builds it, while the SDDP engine's stages stay byte-identical",
        "guardrail": "the projection/stage types name no engine and no paradigm; the Engine enum stays at L4 (cobre-cli / cobre-python, IV.4); cobre-sddp gains no dependency on a sibling engine; no seam is stood up for a single hypothetical consumer (pull, don't push, V.0)",
        "byteNeutralityBar": "parity goldens (crates/cobre-sddp/tests/parity.rs + tests/common/parity_hash.rs) + the rank-invariance harness (tests/common/permute.rs shuffle matrix) + mpiexec -n 1 / -n 2 reproduction; SDDP output bit-for-bit unchanged is the Phase-0a gate itself (V.1 Gate). NCS caveat: the ncs_stochastic_dense_col / ncs_stochastic_windows / ncs_max_gen / ncs_allow_curtailment fields feed hot-path StageContext borrows (workspace/context.rs ncs_col_starts, n_ncs) and carry the D15 patch-identity contract (.claude/rules/sddp.md § 'Lower-bound evaluation must patch NCS'; register L215/L563) — the split must move them as one unit or not at all",
        "anchorOwner": "sddp"
      },
      "e05_5Delta": {
        "candidateRef": "5a-architecture-00",
        "verdict": "confirmed",
        "survivingClaim": "Delta over CD-005: of the seven setter-wall members, `StudySetup::set_budget` (setup/accessors.rs:62-65) is the only one with zero call sites anywhere in the repository at the pin AND the only one that is `pub` and ungated without a caller justifying the ungating (its three test-only siblings at accessors.rs:69-90 are `#[cfg(any(test, feature = \"test-support\"))]`-gated; `set_start_iteration` and `set_warm_start_basis_cache` are ungated because the CLI/Python resume paths call them); it writes `cut_management.budget` behind both the `StudyParams::from_config` budget-below-forward-passes validation (setup/params.rs:287-295) and the broadcast that carries `budget` to every rank (cobre-cli/src/commands/broadcast.rs:113, :215), so any use is unvalidated and rank-local. The structural halves of the title - two incompatible storage conventions, and `train_inner`'s field-by-field re-inflation - do not survive.",
        "argument": null,
        "needsHuman": [
          "Owner call: `set_budget` is enumerated in the declared accessor surface at `.claude/architecture-rules.md:117`, so the disposition of a caller-free ungated mutator is either remove it and edit that rules line, or keep it as a deliberate public-library knob and route it through the same validation `StudyParams::from_config` applies - the station cannot pick without the owner."
        ]
      },
      "dupOf": [],
      "byteNeutrality": "re-baseline decision at the owner gate — the NCS split feeds hot-path StageContext borrows and the patch-identity contract",
      "actionable": true
    },
    {
      "id": "CD-024-successor",
      "registerId": "CD-024",
      "registerLine": 958,
      "wave": 4,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/policy/resolved_parameters.rs",
        "symbol": "build_resolved_parameters"
      },
      "supersededClaim": "the resolved-parameters broadcast pair CD-024 registered (serialize_/deserialize_resolved_parameters, RESOLVED_PARAMETERS_WIRE_VERSION) — deleted as dead code; CD-024 itself RESOLVED 2026-08-18",
      "survivingClaim": "ResolvedParameters is still built per rank (policy/resolved_parameters.rs build_resolved_parameters; setup/mod.rs 'Per-rank, never broadcast'); the successor question is whether a rank-0-built resolved run config is broadcast — the seam activates against the CD-004 projection or dies",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "conflicts",
      "alignmentCites": "Part V.0 (pull, don't push — the second consumer precedes the abstraction); Part V.1 Phase 0; Part IV.4",
      "phase0aRestatement": {
        "artifact": "study-block admission-gate carrier",
        "shape": "activate-or-die rider on CD-004: the per-rank ResolvedParameters derivation either rides the single Config projection (its inputs are already on the wire, so no second broadcast seam is needed) or becomes permanent as a per-rank derivation; a dedicated resolved-parameters broadcast seam is NOT proposed",
        "guardrail": "the projection/stage types name no engine and no paradigm; the Engine enum stays at L4 (cobre-cli / cobre-python, IV.4); cobre-sddp gains no dependency on a sibling engine; no seam is stood up for a single hypothetical consumer (pull, don't push, V.0)",
        "byteNeutralityBar": "parity goldens (crates/cobre-sddp/tests/parity.rs + tests/common/parity_hash.rs) + the rank-invariance harness (tests/common/permute.rs shuffle matrix) + mpiexec -n 1 / -n 2 reproduction; SDDP output bit-for-bit unchanged is the Phase-0a gate itself (V.1 Gate)",
        "anchorOwner": "sddp"
      },
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": false,
      "conflicts": {
        "condition": "one-consumer seam kept alive against a single hypothetical consumer ('pull, don't push', Part V §V.0)",
        "alternative": "die: keep the per-rank derivation; its inputs ride the single Config projection of CD-004, so the seam is subsumed rather than activated — no OD/CD id promoted for the broadcast variant",
        "status": "HELD for owner override at the E05-8 gate",
        "guardrailBreach": "one-consumer abstraction (target-layering brief §3 trigger 4): a seam justified by a single hypothetical consumer"
      }
    },
    {
      "id": "CD-015",
      "registerId": "CD-015",
      "registerLine": 537,
      "wave": 6,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
        "symbol": "compute_one_backward_node"
      },
      "supersededClaim": "extract a standalone commit_by_scenario_cuts to mirror by_node_finish (a symmetry fix on its own)",
      "survivingClaim": "compute_one_backward_node (backward_pass_state.rs :1727) still inlines by_scenario's post-dispatch aggregation while by_node's is extracted (training/backward/by_node.rs by_node_finish :416); the asymmetry is real and is resolved by the SAME extraction as CD-022 — one convergent change, disposed together",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "owner-elevated retrofitted-variant asymmetry class (live instances CD-013, CD-015, CD-027)",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (the backward-pass decomposition is SDDP geometry that 'stays put' in the engine under the kernel carve)",
      "phase0aRestatement": null,
      "e05_5Delta": {
        "candidateRef": "5c-architecture-01",
        "verdict": "confirmed",
        "survivingClaim": "Delta over CD-015: the live entry mislabels the inline residue. By-scenario's risk aggregation is NOT in compute_one_backward_node — aggregate_cut_into runs inside process_by_scenario_backward (by_scenario.rs:525) and the fn returns a finished StagedCut. The asymmetry that survives is narrower: exactly the cross-worker merge-and-commit tail at backward_pass_state.rs:1950-1991 (first-error fold, sole-key trial_state_idx sort, length assert, arena-resolved fcf.add_cut loop) is inline where the by-node arm reaches the same role through one call to by_node_finish (by_node.rs:416). Not surviving: the absent name commit_by_scenario_cuts is the open entry's premise, not evidence, and the extraction leaves BOTH arms multi-statement rather than one call per arm and does not clear the fn's too_many_lines allow.",
        "argument": null,
        "needsHuman": []
      },
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-022",
      "registerId": "CD-022",
      "registerLine": 799,
      "wave": 6,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
        "symbol": "run_enumerated_backward"
      },
      "supersededClaim": "a ~66-line duplicated span framed as its own dedup",
      "survivingClaim": "the successor-outcome reification (successors loop → build_delta_cut_row_batch_into in training/forward/delta_cut_batch.rs → SuccessorEntry → SuccessorOutcomes in training/backward/mod.rs) is still inlined twice — compute_one_backward_node (:1727) and run_enumerated_backward (:721); extracting it once de-god-fns compute_one_backward_node (CD-014-remnant) and absorbs CD-015",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (SDDP decomposition stays in the engine)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-018",
      "registerId": "CD-018",
      "registerLine": 634,
      "wave": 6,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/cut/cut_selection.rs",
        "symbol": "CutSelectionStrategy"
      },
      "supersededClaim": null,
      "survivingClaim": "`CutSelectionStrategy` conflates two selection paradigms — periodic value-based (Level1/Lml1/Dominated share `select_for_stage`) and lazy-solve DCS (`Dynamic`), whose variant early-returns empty and whose real logic lives in `dcs.rs::lazy_solve_preloaded`",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "paradigm conflation inside one enum",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (cut selection is SDDP geometry that stays in the engine)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-035",
      "registerId": "CD-035",
      "registerLine": 1471,
      "wave": 6,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward/outcome_aggregation.rs",
        "symbol": "write_opening_outcome"
      },
      "supersededClaim": null,
      "survivingClaim": "`write_opening_outcome`/`accumulate_opening_outcome` take a bare `&CutStateProjection` where two role-distinct projections are live (`child_cut_layout` vs the cut-generating parent's `succ_spec.cut_state`) — passing the wrong one compiles",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "missing typed-role seam in backward aggregation",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (cut projection is SDDP geometry; the role newtype is engine-internal)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-034",
      "registerId": "CD-034",
      "registerLine": 1462,
      "wave": 6,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
        "symbol": "fill_operational_violation_entries"
      },
      "supersededClaim": null,
      "survivingClaim": "The min/max outflow entry blocks in `fill_operational_violation_entries` are two hand-mirrored `for blk` loops while the sibling rows file emits the same row pair from a two-element descriptor",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "mirrored min/max outflow blocks",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV.2 (plain equipment rows/entries are the method-neutral emission logic the cobre-model carve reuses; the descriptor-driven row pair in rows.rs is that shape); Part V.1 Phase 0b",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-023",
      "registerId": "CD-023",
      "registerLine": 855,
      "wave": 6,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/stage_solve_prep.rs",
        "symbol": "StageSolvePrep"
      },
      "supersededClaim": "'re-home StageSolvePrep out of training/' as an unstarted move",
      "survivingClaim": "the carve-out is half done: the neutral solve/ module exists (mod.rs, partition.rs, solver_phase.rs, stage_solve.rs) and owns the solve ENTRY (run_stage_solve), but training/stage_solve_prep.rs and training/stage_solve_prep/tests.rs still live under training/ and simulation/pipeline.rs:50 still imports training::stage_solve_prep — the declared-undesirable simulation→training inversion survives; never retire on the strength of solve/ merely existing",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (intra-engine module layering; the inversion is inside L3)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-021",
      "registerId": "CD-021",
      "registerLine": 722,
      "wave": 7,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/workspace/workspace.rs",
        "symbol": "SolverWorkspace"
      },
      "supersededClaim": "'a flat workspace.rs mega-file asymmetric with the directory-module convention' — the organizational half is discharged: workspace/ is a directory module (mod.rs 32, context.rs 271, workspace.rs 2,671 lines)",
      "survivingClaim": "all nine structs (CapturedBasis, WorkspaceSizing, BackwardAccumulators, ByNodeScratch, ScratchBuffers, SolverWorkspace, WorkspacePool, BasisStore, BasisStoreSliceMut) still share workspace/workspace.rs; the split-by-concern direction is preserved against the new path",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "flat mega-module against the directory-module convention",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (per-worker arenas are L3 engine internals)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-007",
      "registerId": "CD-007",
      "registerLine": 264,
      "wave": 7,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/lp/builder/entries.rs",
        "symbol": "fill_load_balance_entries"
      },
      "supersededClaim": "sizes as recorded 2026-08-22: entries.rs 10,079 and columns.rs 9,026 lines (the register already says GREW)",
      "survivingClaim": "re-measured at the pin: entries.rs 10,093 lines (first #[cfg(test)] at L1680) and columns.rs 9,319 (L1305) — inline test modules dominate both while sibling builder submodules use extracted tests.rs (template/tests.rs 5,356, layout/tests.rs 3,565); two conventions in one module dir, and the inline giants are the sole reason these read as god-files",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "inline-test giants",
      "alignmentHint": "advances-0b",
      "alignmentCites": "Part IV.2 (the 0b carve lifts the engine-neutral emission logic out of lp/builder; entries.rs and columns.rs are where that logic lives)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [
        "5a-test-bloat-00",
        "5b-test-bloat-00",
        "5b-test-bloat-01",
        "5c-test-bloat-04",
        "5d-test-bloat-05"
      ],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-012",
      "registerId": "CD-012",
      "registerLine": 385,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/session/mod.rs",
        "symbol": "run_cut_management"
      },
      "supersededClaim": null,
      "survivingClaim": "`run_cut_management` (~250 lines, `too_many_lines`-allowed) inlines cut-selection + budget enforcement; its rationale defends against FREE-fn extraction, a category error since the natural refactor is `&mut self` methods",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (cut management is SDDP geometry in the engine)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-014-remnant",
      "registerId": "CD-014",
      "registerLine": 531,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
        "symbol": "compute_one_backward_node"
      },
      "supersededClaim": null,
      "survivingClaim": "`compute_one_backward_node` (~290 lines, biggest fn in the crate) — the god-fn residue that remains after the shared successor-reification extraction (CD-022) and the by_scenario commit extraction (CD-015) relieve it",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (backward-pass decomposition stays in the engine)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-016",
      "registerId": "CD-016",
      "registerLine": 542,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
        "symbol": "process_stage_backward"
      },
      "supersededClaim": null,
      "survivingClaim": "Both backward scheduler workers duplicate ~25 lines of per-worker `backward_accum` buffer pre-allocation (`process_stage_backward` vs `process_stage_backward_by_node`)",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "duplicated pre-allocation block between the two backward scheduler workers",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (per-worker buffer pre-allocation is L3-internal)",
      "phase0aRestatement": null,
      "e05_5Delta": {
        "candidateRef": "5c-architecture-02",
        "verdict": "confirmed",
        "survivingClaim": "Beyond the pre-allocation duplication CD-016 records, the same two backward drivers carry a second copied block -- the slot_increments into metadata_sync_contribution fold at by_scenario.rs:559-566 and again at by_node.rs:386-392 -- and the two copies have ALREADY diverged in their fold bound: by-scenario folds the whole grown slot_increments.len() capacity once per trial point while by-node folds exactly this stage's outcomes.total_metadata_len() once per worker per stage, so one copy writes tail slots the other never touches (latent only because sync_stage_metadata reads a populated_count-bounded prefix).",
        "argument": null,
        "needsHuman": []
      },
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-028",
      "registerId": "CD-028",
      "registerLine": 1232,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/forward/enumerated.rs",
        "symbol": "run_enumerated_forward"
      },
      "supersededClaim": null,
      "survivingClaim": "Enumerated forward and enumerated simulation reimplement the mid-level claim/scatter orchestration skeleton independently (`run_sweep`/`enumerated_sim_stage_worker` vs `run_enumerated_forward`/`enumerated_stage_worker`); only the low-level primitives are shared",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1 (enumerated orchestration is L3-internal)",
      "phase0aRestatement": null,
      "e05_5Delta": {
        "candidateRef": "5c-architecture-04",
        "verdict": "confirmed",
        "survivingClaim": "Delta over CD-028: the shared owner claim_scatter.rs already exists but its module doc's consumer list (:1-4) names only by_node and the enumerated forward while simulation/enumerated.rs:26 is a third importer of both primitives, so the boundary statement a future reader trusts is false at the pin; and the single fragment outside that doc's caller-side reservation that is genuinely identical modulo the error type is the worker-result fold (forward:586-589 vs simulation:335-338, collect plus the counts-to-lp_solves sum). The title's marking-loop half and its three-consumer framing are conceded: by_node has no path marking, and the two marking loops differ in element type (usize local index carrying the BasisStore row vs u32 global scenario id documented as basis-role-free), in offset arithmetic, and in whether the per-run reset of on_my_paths/solved is fused in -- so CD-030 does not fold in here as an identical-primitive extraction.",
        "argument": null,
        "needsHuman": [
          "Owner call: whether the confirmed residue should produce any code change at all, or only the module-doc consumer-list correction -- the identical fragment is a four-line composition of stdlib collect plus an integer sum, and hoisting it behind a generic-over-E helper trades two transparent ? sites for one opaque one."
        ]
      },
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-030",
      "registerId": "CD-030",
      "registerLine": 1261,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/simulation/enumerated.rs",
        "symbol": "mark_own_paths"
      },
      "supersededClaim": null,
      "survivingClaim": "`mark_own_paths` (simulation/enumerated.rs) reimplements the ~10-line path-marking loop that is inlined in `training/forward/enumerated.rs`; folds with the CD-028 dedup",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-037",
      "registerId": "CD-037",
      "registerLine": 1487,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/setup/mod.rs",
        "symbol": "admission_gate"
      },
      "supersededClaim": null,
      "survivingClaim": "The gap-rule rejecters each re-evaluate `rule_is_gap` + `training_enumerated`, and which rejection message a sampled-forwards study sees depends on call order inside `admission_gate` (documented as deliberate)",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "CD-038",
      "registerId": "CD-038",
      "registerLine": 1493,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs",
        "symbol": "dot_trial_state"
      },
      "supersededClaim": null,
      "survivingClaim": "`CutStateProjection` exposes only the fused `dot_trial_state`; the cut-selection value sweep in `run_cut_management` open-codes the gather-only loop (via `global_state_index`) — loop-shape duplication, not a hazard",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.2 (cut projection is SDDP geometry that stays in the engine)",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    },
    {
      "id": "OD-009",
      "registerId": "OD-009",
      "registerLine": 1853,
      "wave": 7,
      "disposition": "keep",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/session/runtime.rs",
        "symbol": "RuntimeHandles"
      },
      "supersededClaim": null,
      "survivingClaim": "Production items ship as `#[allow(dead_code)]` 'for symmetry, exercised only by tests' (`RuntimeHandles.export_states`, `RankDistribution.my_rank`, `ExchangeBuffers::new`) — the sanctioned Symmetry-or-test-retention class; `#[cfg(test)]`-gating is the cleaner form — a policy refinement of the sanctioned Symmetry-or-test-retention #[allow] class (mirror `#[allow(...)]` census), not a violation",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; no precedent rule re-rates this row",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.1",
      "phase0aRestatement": null,
      "e05_5Delta": null,
      "dupOf": [
        "5c-over-engineering-00"
      ],
      "byteNeutrality": "asserted against the parity goldens (`parity_hash_highs` / `parity_hash_clp` in the sddp parity binary), the rank-invariance harness (tests/common/permute.rs) and `mpiexec -n 1/2` reproduction for the restated shape",
      "actionable": true
    }
  ],
  "existenceOnly": [
    {
      "id": "PD-004",
      "exists": true,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/training/backward_pass_state.rs",
        "symbol": "run_enumerated_backward"
      },
      "evidence": [
        "crates/cobre-sddp/src/training/backward_pass_state.rs:721:    fn run_enumerated_backward<S, C: Communicator>("
      ],
      "status": "do-not-touch: DEFERRED pending a profile (register L1316; do-not-touch list L1942)",
      "queuedTo": "perf-sweep (E10-2 measures PD-004 on the enumerated deck; UNMEASURED until then)",
      "disposition": null,
      "note": "existence verdict only — the disposition enum is never applied to a do-not-touch item; no fix-shape, no severity, no id change",
      "registerLine": 1316
    }
  ],
  "cleared": [
    {
      "candidateRef": "5b-architecture-01",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": "Delivered commitments reconcile against solver drift; exactness is unreachable",
      "reason": "The delta this candidate adds over CD-074 is that the commitment family's two exactness mechanisms are mutually unaware and their coupling unstated; both halves are false at the pin. The reconciler's module doc (crates/cobre-sddp/src/lp/builder/commitment_reconcile.rs:8-12) names `apply_commitment_…",
      "needsHuman": []
    },
    {
      "candidateRef": "5b-architecture-03",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": "State pinning uses column bounds, not equality rows",
      "reason": "The factual half holds (nothing in columns.rs writes state.transit_buckets_in) but the reading does not, on three counts. (1) The asymmetry is a stated rule, not an unexplained omission. The buffer default is (0.0, +INF) at columns.rs:31-32, so a family is written exactly when its open domain diffe…",
      "needsHuman": []
    },
    {
      "candidateRef": "5b-architecture-04",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The candidate's load-bearing premise fails at the pin: in a release build the gate family has two members, not one. anticipated_gate.rs:123-142 declares anticipated_resolution_for with NO cfg gate, re-exported for production use at indexer/mod.rs:109-111, taking state as its first parameter and rea…",
      "needsHuman": []
    },
    {
      "candidateRef": "5c-architecture-03",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": "The cut intercept dots the trial state through the projection, never positionally",
      "reason": "The named fix target does not do what the candidate says it does. solve_replicated_outcome_slice (replicated.rs:83-166) never touches a BackwardOutcome: at :137-138 and :160-161 it pushes the raw objective followed by state_duals[..n_state] into the flat out Vec<f64> whose per-outcome width is outc…",
      "needsHuman": []
    },
    {
      "candidateRef": "5b-performance-02",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The mechanism is real but it is setup-time only, and the allocation policy on this exact path is deliberate and documented in present tense. Frequency: resolve_variable_ref has one production caller, fill_generic_constraint_entries at lp/builder/entries.rs:1363; that function is reached only from l…",
      "needsHuman": []
    },
    {
      "candidateRef": "5c-performance-01",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The mechanism as filed ('redundant strided pass', 'full-panel stride') does not hold at the pin, on five grounds read from the code. (1) Nothing is redundant and the interchange removes no load. The per-column nest at crates/cobre-sddp/src/cut/cut_selection.rs:392-403 issues one max-loop load per p…",
      "needsHuman": []
    },
    {
      "candidateRef": "5c-performance-05",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The mechanism rests on the premise that the reconstruction already knew the BASIC counts; at the pin it does not. reconstruct_col_statuses (basis_reconstruct.rs:209-211) is clear + extend_from_slice + resize, and reconstruct_template_row_statuses (:224-232) is the same pair; BasisStatus is a fieldl…",
      "needsHuman": []
    },
    {
      "candidateRef": "5d-performance-03",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "Setup scope is verified at the pin, and it decides this candidate. The only non-test entry into the fitting pipeline is `fit_fpha_planes` (production/fpha_fitting/mod.rs:137), reached through `fit_planes_for_hydro` (production/hydro_models/production.rs:414) and `fit_computed_planes_per_stage` (:50…",
      "needsHuman": [
        "Process call: the 4t sweep skips setup-time-only items, so the owner must say whether a setup-budget track exists to record the two conceded residues (per-plane grid rebuild in the secant; duplicate deviation walk under the opt-in flag) or whether they are dropped."
      ]
    },
    {
      "candidateRef": "5d-performance-04",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The mechanism the candidate names is really present: at secant.rs:74 every call to representative_operating_point re-allocates both axis Vecs through build_grid, and at secant.rs:80-88 the min-over-planes fold and pf.evaluate depend only on the node and the immutable snapshot, so both are recompute…",
      "needsHuman": []
    },
    {
      "candidateRef": "5d-performance-06",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The linear membership scan is real as read, but it sits entirely in study setup and its operand set is bounded to a handful of keys, so the named mechanism has no measurable region to act on. Reachability: fit_hull_planes has exactly one non-test caller, production/fpha_fitting/mod.rs:165 inside fi…",
      "needsHuman": []
    },
    {
      "candidateRef": "5d-performance-07",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "This is a study-setup path, not a solve path. The only production call sites of prepare_hydro_models_from_artifacts are crates/cobre-cli/src/commands/run/setup.rs:129 and crates/cobre-python/src/run.rs:981, plus the two validate-only entries crates/cobre-cli/src/commands/validate.rs:419 and crates/…",
      "needsHuman": []
    },
    {
      "candidateRef": "5a-over-engineering-00",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": "Backward opening order is warm-start-only",
      "reason": "Dismissed on the code plus the project's own pinned scope for rationale-on-suppression. comments.md directive D4 (.claude/rules/comments.md:365-371) mandates a rationale for a CLOSED list of refactor-decision lints (too_many_arguments, too_many_lines, type_complexity, dead_code, unused_*) plus borr…",
      "needsHuman": [
        "Owner call (prose, not code): reword the allow-census Load-bearing class at docs/design/reserved-seams-and-deferred-debt.md:1282-1288 and the scripts/README.md:36 one-liner so neither reads as an absolute every-site rationale claim, since D4 and the E4 gate never require one of a cast_* allow."
      ]
    },
    {
      "candidateRef": "5a-over-engineering-02",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The filed fix does not compile, and that is why the literals exist. accessors.rs:233 and :274 declare stage_ctx(&self) -> StageContext and training_ctx(&self) -> TrainingContext with elided output lifetimes, so each returned value holds a borrow of all of *self for as long as it lives. train_inner…",
      "needsHuman": []
    },
    {
      "candidateRef": "5a-over-engineering-03",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "Both load-bearing premises fail against the code and the pinned rules at the baseline. (1) The impl-less-public-module shape is the documented layout, not an accident: .claude/architecture-rules.md section 'StudySetup Sub-Structs' (line 80 onward) tabulates ScenarioLibraries and PhaseLibraries by n…",
      "needsHuman": []
    },
    {
      "candidateRef": "5b-test-bloat-05",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "The shadow is real but nothing about it is silent, which is the load-bearing word in the title. The parent declaration at tests/template_integration.rs:251 takes two usize parameters (n_stages, lag_order); the local declaration at tests/template_integration/generic_constraints.rs:1433 takes four (n…",
      "needsHuman": []
    },
    {
      "candidateRef": "5b-test-bloat-06",
      "verdict": "dismissed",
      "sanctionedBy": null,
      "contractCited": null,
      "reason": "Dismissed on four independent grounds, all read at the pin. (1) The count is wrong: crate::test_support exposes no Stage constructor at all. geometry_stage is a private fn at crates/cobre-sddp/src/test_support.rs:409 with exactly one caller, :554, which feeds StageLayout::new(...).geometry(BlockMod…",
      "needsHuman": []
    }
  ],
  "dupOf": [
    {
      "candidateRef": "5c-over-engineering-00",
      "dupOf": "OD-009",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    },
    {
      "candidateRef": "5a-test-bloat-00",
      "dupOf": "CD-007",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    },
    {
      "candidateRef": "5b-test-bloat-00",
      "dupOf": "CD-007",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    },
    {
      "candidateRef": "5b-test-bloat-01",
      "dupOf": "CD-007",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    },
    {
      "candidateRef": "5c-test-bloat-04",
      "dupOf": "CD-007",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    },
    {
      "candidateRef": "5c-test-bloat-05",
      "dupOf": "5a-test-bloat-02",
      "priorRelation": "intra-station",
      "assignedId": null,
      "survivorId": "TD-042"
    },
    {
      "candidateRef": "5d-test-bloat-00",
      "dupOf": "5a-test-bloat-05",
      "priorRelation": "intra-station",
      "assignedId": null,
      "survivorId": "TD-045"
    },
    {
      "candidateRef": "5d-test-bloat-02",
      "dupOf": "5a-test-bloat-03",
      "priorRelation": "intra-station",
      "assignedId": null,
      "survivorId": "TD-043"
    },
    {
      "candidateRef": "5d-test-bloat-03",
      "dupOf": "5a-test-bloat-04",
      "priorRelation": "intra-station",
      "assignedId": null,
      "survivorId": "TD-044"
    },
    {
      "candidateRef": "5d-test-bloat-05",
      "dupOf": "CD-007",
      "priorRelation": "restates",
      "assignedId": null,
      "survivorId": null
    }
  ],
  "conflicts": [
    {
      "id": "CD-024-successor",
      "condition": "one-consumer seam kept alive against a single hypothetical consumer ('pull, don't push', Part V §V.0)",
      "alternative": "die: keep the per-rank derivation; its inputs ride the single Config projection of CD-004, so the seam is subsumed rather than activated — no OD/CD id promoted for the broadcast variant",
      "status": "HELD for owner override at the E05-8 gate",
      "guardrailBreach": "one-consumer abstraction (target-layering brief §3 trigger 4): a seam justified by a single hypothetical consumer"
    }
  ],
  "counts": {
    "received": 75,
    "confirmed": 49,
    "minted": 44,
    "sharpenMerged": 5,
    "dismissed": 16,
    "dupOf": 10,
    "downgrades": 12,
    "upgrades": 1,
    "bySeverity": {
      "B": 19,
      "C": 23,
      "B (A-risk)": 2
    }
  }
}
