# test-corpus attacker dispatch log

Baseline: `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c` (register pin; the ticket's `a136840d` is the superseded scaffold pin — every figure the workers were handed was re-measured at the pin by E08-1 (`inventory.json`) and every prior anchor re-verified by E08-2 (`prior-register.md`); see `attacker-prompt.md` § Frozen figures for the ticket→pin deltas). Every finding below is a snapshot of that tree, not a standing claim. Dispatched: 2026-09-19. Agent type: `adversarial-attacker` (Opus, read-only — no Write/Edit tools; each worker wrote its envelope to `/tmp/test-corpus-attackers/out/<worker>.json` with one heredoc and replied `WRITTEN <bytes> <path>`). Dispatcher: the main session, sole writer of every artifact under `stations/test-corpus/`. Raw envelopes live only under `/tmp`; the four `candidates-*.json` files are the merged, gated record.

## Prompt and validator (before dispatch)

One shared contract, `attacker-prompt.md` (54465 bytes at merge time; generated by the dispatcher from `inventory.json`, `seeds.json`, `prior-register.md`, `claim-classes.json` and `lens-rules.md`, adapting the frozen core-io template): 13 numbered rules (read-only + scratch-file hand-over; one bare JSON object; anchors resolve at the pin with a command actually run; prose fix shapes; never re-raise — the three mirror items travel as `dupOf`, the 72 handed-over TD ids only as `seedRef`; reserved-seams-first; COVERAGE-NEUTRAL FIX-SHAPES ONLY; CLASSIFY, NEVER ADJUDICATE; counts only from `inventory.json` keys with `measurementDefinition`; performance informational and UNMEASURED; anchors inside the worker's scope; empty-is-legitimate; every routed seed gets a disposition), the alignment vocabulary, the frozen-figure key table plus the ticket→pin delta table, the do-not-raise set, the claim-class summary, the seven test-bloat probes with pin anchors, the three secondary lens walls, the frozen envelope, the nine-row scope table (prefixes; `validate-tb.py partition` expands them over the 480-file corpus and printed `PARTITION OK` before dispatch), the seeds-per-worker table and the dispatch mechanics. Each rendered worker copy appended § Your scope (the expanded file list) and § Your seeds (the routed register rows with anchors, claim and upstream fix-shape).

The shape gate is `stations/test-corpus/validate-tb.py envelope <file> --worker <name>`: the shared `tools/validate-envelope.py` (imported as a library, untouched — the ticket's own step 2 forbids editing it, so its `--profile` literal in AC 3 is realised by this station-local wrapper) plus the test-bloat profile (`claimKind`, `measurementDefinition`, `measuredValue` key AND value against `inventory.json`, `yardstickRef` form, no `targetNotDefect`, `dupOf` ∈ the three mirror items, `seedRef` form and membership, per-lens extras, anchors inside the worker's scope, a complete seed ledger). Self-tested before dispatch on one well-formed and four malformed envelopes (claimKind outside the set → `$.candidates[0].claimKind`; a count with no measurementDefinition → `$.candidates[0].measurementDefinition`; prose around the JSON → `$ not parseable JSON`; a ticket figure 37 against the pin's 40 → `$.candidates[0].measuredValue.value`).

## Dispatches

| worker | lens | scope (corpus files) | envelope | re-dispatch | candidates | positives | seeds routed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| tb-cobre-core | test-bloat | 33 | valid | 0 | 5 | 4 | 5 |
| tb-cobre-io | test-bloat | 114 | valid | 1 | 8 | 6 | 18 |
| tb-cobre-solver | test-bloat | 18 | valid | 0 | 6 | 8 | 4 |
| tb-cobre-stochastic | test-bloat | 48 | valid | 0 | 9 | 6 | 10 |
| tb-cobre-comm | test-bloat | 8 | valid | 0 | 3 | 6 | 2 |
| tb-cobre-sddp | test-bloat | 188 | valid | 0 | 16 | 7 | 16 |
| tb-cobre-cli | test-bloat | 26 | valid | 0 | 11 | 7 | 10 |
| tb-cobre-python | test-bloat | 42 | valid | 0 | 9 | 7 | 7 |
| tb-workspace-topology | test-bloat | 3 | valid | 0 | 6 | 8 | 0 |
| sec-architecture | architecture | 480 | valid | 0 | 3 | 5 | 0 |
| sec-over-engineering | over-engineering | 480 | valid | 0 | 2 | 15 | 0 |
| sec-performance | performance | 480 | valid | 0 | 3 | 7 | 0 |

## Gaps

No worker was excluded: every envelope validated (first or second attempt) and every worker merged.

Re-dispatched once (first-attempt diagnostics, then valid):

- `tb-cobre-io`: $.positives[0].why: required; $.positives[1].why: required; $.positives[2].why: required; $.positives[3].why: required; $.positives[4].why: required; $.positives[5].why: required

## Seed ledger

seedsIn = 72, seedsAccounted = 72 (confirmed 30, dropped 14, sharpened 28). Every row of `seeds.json` carries its disposition, reason and (for confirmed / sharpened) the candidate title carrying its `seedRef`; the dropped rows are also listed in `attacker-prompt.md` § Seed ledger.

| dropped seed | worker | reason |
| --- | --- | --- |
| TD-004 (`stations/core-io/td-queue.json#queue[3]`) | tb-cobre-core | Premise fails at the pin. The seed claims ad-hoc bounds comparators where the yardstick calls for one shared comparator, with the risk that a newly added bounds field is silently ignored. At the pin all six comparators in crates/cobre-core/src/model/resolved/bounds.rs (hydro_stage_bounds_bits_eq:1908, hydro_block_bounds_bits_eq:1927, thermal_block_bounds_bits_eq:1964, line_bounds_bits_eq:1977, pumping_bounds_bits_eq:1989, contract_bounds_bits_eq:2002) destructure both operands with no rest pattern, so a new field on any of those structs fails to compile rather than being dropped, and all six already delegate their float comparison to the shared crate::test_support helpers imported at bounds.rs:1205. Command: git show 077dbe2c:crates/cobre-core/src/model/resolved/bounds.rs | sed -n '1905,2020p' ; git grep -n 'use crate::test_support' 077dbe2c -- crates/cobre-core/src/model/resolved/bounds.rs |
| TD-005 (`stations/core-io/td-queue.json#queue[4]`) | tb-cobre-core | Premise fails at the pin: the consolidation the seed asks for has already happened. One of the five test sites the seed names, crates/cobre-core/src/topology/network.rs, does not exist at the pin (the topology module declares only the cascade submodule), and each of make_bus, make_line, make_hydro, make_thermal and make_ncs exists exactly once, as a public function in crates/cobre-core/src/test_support.rs, consumed through its spec structs by system/mod.rs, system/builder.rs, topology/cascade.rs and tests/integration.rs. Command: git grep -c 'fn make_bus\|fn make_line\|fn make_hydro\|fn make_thermal\|fn make_ncs' 077dbe2c -- crates/cobre-core/src ; git show 077dbe2c:crates/cobre-core/src/topology/mod.rs ; git log --oneline --diff-filter=D -- crates/cobre-core/src/topology/network.rs |
| TD-007 (`stations/core-io/td-queue.json#queue[6]`) | tb-cobre-io | Premise resolved between the seed's mint pin and the register pin. A crate-level test_support.rs was added holding single definitions of write_parquet, write_json, make_global, make_minimal_case, write_file, penalties_all, base_parsed_data and make_unit_group, and validation/semantic/test_support.rs was deleted. The claim that the input path has no shared test-support module while the validation path does is false at the pin; the surviving fixture copies are narrower and are raised separately as the test-support restatement candidate. |
| TD-009 (`stations/core-io/td-queue.json#queue[8]`) | tb-cobre-io | Resolved at the pin. The triplicated batch builder and the five common non-determinism cases are now single definitions in the crate test-support module, consumed by all three statistics parsers, and no local make_batch remains. The three per-parser declaration-order tests the seed flagged as load-bearing all survive and each carries an ascending and a descending fixture, so the consolidation preserved the determinism pins. Recorded under positives. |
| TD-011 (`stations/core-io/td-queue.json#queue[10]`) | tb-cobre-io | The fact holds at the pin: the bus ordering test's second bus name assertion is the only surface absent from the full-case test, and that test's whole-System comparison implies it. But the only fix shape is removing a test, which this station's cost-discipline rule forbids, and this is one of the ratified removal-direction entries the rule's boundary paragraph names explicitly. Recorded under positives and left for the owner decision on the duplicate-test carve-out rather than re-litigated here. |
| TD-012 (`stations/core-io/td-queue.json#queue[11]`) | tb-cobre-io | The fact holds at the pin with shifted lines: test_load_error_io_display sits at 133 and test_load_error_io_helper at 220, both reach the same display facts, and test_load_error_schema_display at 162 re-asserts the bus_id containment the module doctest at line 27 already covers. The only shape that resolves it is removing a test, which the cost-discipline rule forbids, so it is recorded under positives with the other three deletion-only seeds rather than raised as a candidate. |
| TD-014 (`stations/core-io/td-queue.json#queue[13]`) | tb-cobre-io | Resolved at the pin. The minimal-case corpus and write_file are now single definitions in the crate test-support module, and tests/helpers/mod.rs re-exports make_minimal_case and write_file from there instead of restating them, so the barrier-forced copy the seed named as its narrowest residue is gone. What remains behind the tests barrier are the two richer case builders, which are raised as their own candidate. |
| TD-015 (`stations/core-io/td-queue.json#queue[14]`) | tb-cobre-io | The fact holds at the pin with shifted lines: test_filling_guard_no_exit_no_error is at 1808, test_filling_guard_entry_below_horizon_no_error at 1705, and make_filling_hydro at 1518 sets only entry_stage_id and filling, never exit_stage_id, so the no-exit test cannot exercise the condition it names. The guard's rejection path is covered separately at 1784. The only fix shape is removal, which the cost-discipline rule forbids and which this ratified entry already prescribes, so it is recorded under positives and left to the owner decision. |
| TD-017 (`stations/core-io/td-queue.json#queue[16]`) | tb-cobre-io | Every anchor is absent at the pin. thermal.rs is 3784 lines with a single cfg(test) module at 1361 and no nested boundary_tests module, and none of override_at_t_minus_1_acceptance_boundary, test_thermal_bounds_override_stage_within_horizon_accepted, test_thermal_bounds_override_stage_equals_n_rejected or test_thermal_bounds_override_multiple_offending_rows appears anywhere in the file. The unbacked do-not-delete comment the seed relied on has zero matches. The override naming was replaced by a committed_value family, so the premise is dead rather than merely relocated and no anchor is edited into something that resolves. |
| TD-018 (`stations/core-io/td-queue.json#queue[17]`) | tb-cobre-io | Premise dead at the pin. output/convergence_reader.rs does not exist, so the byte-identical pair the seed measured cannot be compared. make_config, make_system and make_output_context are now single definitions in the output submodule of the crate test-support module, which is the destination the seed's fix shape implied. |
| TD-019 (`stations/core-io/td-queue.json#queue[18]`) | tb-cobre-io | The fact holds at the pin with shifted lines: the exhaustive sweep is at 1336 and the two per-schema description sweeps at 3162 and 3175, with the units test that subsumes the third at 3114 and the descriptions test at 3129. Resolving a strict-subset duplication requires removing a test, which the cost-discipline rule forbids as a fix shape, so it joins the three other deletion-only seeds under positives and the open owner question. |
| TD-023 (`stations/core-io/td-queue.json#queue[22]`) | tb-cobre-io | Premise false at the pin. parquet_helpers.rs is 486 lines, not 166, and carries a cfg(test) module at 125 with 33 tests that pin the missing-column and wrong-type message contract directly for all six extractors plus three tests for open_record_batch_reader, so the owner-level coverage the seed said was absent exists. Recorded under positives. |
| TD-024 (`stations/stochastic/td-queue.json#queue[0]`) | tb-cobre-stochastic | Premise false at the pin. `git grep -n 'fn make_bus|fn make_hydro' 077dbe2c -- crates/cobre-stochastic/tests` returns nothing: no integration binary declares its own bus or hydro builder any more, they all reach `deficit_bus` at `tests/common/mod.rs:92` and `sized_hydro` at :106. The only per-binary fixture code left in `tests/` is the seven stage wrappers, which the queue[6] candidate raises with its own anchors, so re-raising here would double-claim them. |
| TD-029 (`stations/stochastic/td-queue.json#queue[5]`) | tb-cobre-stochastic | Premise false at the pin. `tests/saa_golden_value.rs` no longer declares a local `identity_correlation`; :16 is `use common::{StageSpec, identity_correlation};`, so the copy the seed names has already been replaced by the shared export at `tests/common/mod.rs:74`. Its remaining local declaration is the blockless stage wrapper at :22, raised under the queue[6] candidate. |

## Gate: anchors demoted or candidates rejected (rule 11)

- `test-bloat` / `tb-cobre-io`: anchor `crates/cobre-io/src/output/policy/checkpoint.rs`:1 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The policy directory module is almost entirely test code for submodules it only re-exports, and checkpoint.rs "
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:163 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "Every SS1.1 fixture builder in cobre-solver is the same body modulo indentation, so the seed's in-src versus i"
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:61 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The profile and retry composition binary gates its whole test module on the non-default test-support feature w"
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:64 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The profile and retry composition binary gates its whole test module on the non-default test-support feature w"
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:75 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The profile and retry composition binary gates its whole test module on the non-default test-support feature w"
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:77 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The profile and retry composition binary gates its whole test module on the non-default test-support feature w"
- `test-bloat` / `tb-cobre-solver`: anchor `crates/cobre-solver/src/lib.rs`:163 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The profile and retry composition binary gates its whole test module on the non-default test-support feature w"
- `test-bloat` / `tb-cobre-stochastic`: anchor `crates/cobre-stochastic/src/test_support.rs`:167 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The five-function undated fixture prelude is re-declared in three inline `#[cfg(test)]` modules while `tests/c"
- `test-bloat` / `tb-cobre-stochastic`: anchor `crates/cobre-stochastic/src/test_support.rs`:23 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The dated `make_hydro` preset is byte-identical across four inline test modules and `dated_stage` across two, "
- `architecture` / `sec-architecture`: anchor `crates/cobre-solver/src/lib.rs`:163 demoted to `citedContext` (out-of-scope-anchor: outside the enumerated test surface) on "The test-support surface has two activation regimes: cobre-core, cobre-io and cobre-stochastic pin the feature"
- `over-engineering` / `sec-over-engineering`: anchor `crates/cobre-sddp/tests/anticipated_scenarios.rs` demoted to `citedContext` (anchor-form: symbol is not a bare identifier) on "The `cobre-sddp` `tests/common/` aggregator declares every submodule to every consumer binary, so a single-con"

## Merge

- `candidates-test-bloat.json`: 73 candidates, 59 positives, 28 needs-human; no cross-worker duplicate.
- `candidates-architecture.json`: 3 candidates, 5 positives, 4 needs-human; no cross-worker duplicate.
- `candidates-over-engineering.json`: 2 candidates, 15 positives, 1 needs-human; no cross-worker duplicate.
- `candidates-performance.json`: 3 candidates, 7 positives, 3 needs-human; no cross-worker duplicate.

test-bloat claimKind split: target-gap 11, tree-fact 62; measurementDefinition split: binary 15, file 27, n/a 31; dupOf: Oracle test-harness duplication 1, Mega-file / inline-test-giant asymmetry 1, Python-binding Rust tests invisible to CI 1.

## Probe P1/P6 — the §5.8 StubComm / Rank0Of2 sentence

The drift probe landed as complementary candidates from two workers rather than one: the cobre-comm worker anchors the §5.8 sentence (`docs/design/testing-architecture.md:524`, restated at :415) against the manifest that declares no `test-support` feature, and the cobre-sddp worker anchors the canonical home (`crates/cobre-sddp/tests/common/mod.rs:32` / `:86`) with the private copies it found in the src test modules. Neither sets `targetNotDefect`; the defender pass adjudicates target-vs-drift. The complete copy census at the pin (12 `struct` hits plus the two `cut_sync.rs` variants) is in § Self-check below and in `attacker-prompt.md` § Frozen figures — the ingest ticket joins the two candidates against it.

- `tb-cobre-comm` (test-bloat, claimKind `tree-fact`, ta-5.8): cobre-comm declares no test-support feature and mentions the comm harness pair nowhere, while the yardstick states in the present tense that StubComm 
- `tb-cobre-sddp` (test-bloat, claimKind `tree-fact`, ta-5.8): The shared tests/common stub communicator and its rank-zero variant are byte-identical to copies inside two src test modules

## Self-check (baseline `077dbe2c`)

Commands run by the dispatcher at merge time against the pin, with their output, so a reader sees the facts the probes were built on rather than trusting the prompt.

### nextest config + runner split

```
$ git cat-file -e 077dbe2c:.config/nextest.toml && echo present || echo '.config/nextest.toml: absent'; git show 077dbe2c:.github/workflows/ci.yml | grep -n 'cargo nextest run\|cargo test --workspace'
.config/nextest.toml: absent
114:      - run: cargo test --workspace --features "${{ env.NON_SOLVER_FEATURES }}"
233:        run: cargo nextest run --workspace --no-default-features --features "clp ${{ env.NON_SOLVER_FEATURES }}" --no-fail-fast
fatal: path '.config/nextest.toml' does not exist in '077dbe2c'
```

### shuffle cadence

```
$ git show 077dbe2c:.github/workflows/invariance-shuffle.yml | grep -n 'workflow_dispatch\|cron\|schedule'
4:  workflow_dispatch:
6:  # schedule:
7:  #   - cron: "0 3 * * *"
```

### slow-tests in the PR feature set

```
$ git show 077dbe2c:.github/workflows/ci.yml | grep -n 'NON_SOLVER_FEATURES:'
32:  NON_SOLVER_FEATURES: "mpi numa shared-memory serde schema slow-tests flatc-conformance test-support"
```

### binary vs file definitions (must differ for cobre-sddp, cobre-io, cobre-stochastic)

```
$ for c in cobre-sddp cobre-io cobre-stochastic cobre-cli cobre-solver; do printf '%s binaries=%s files=%s\n' $c "$(git ls-tree --name-only 077dbe2c crates/$c/tests/ | grep -c '\.rs$')" "$(git ls-tree -r --name-only 077dbe2c crates/$c/tests/ | grep -c '\.rs$')"; done
cobre-sddp binaries=40 files=56
cobre-io binaries=12 files=13
cobre-stochastic binaries=9 files=10
cobre-cli binaries=14 files=14
cobre-solver binaries=8 files=8
```

### test-support declarers/consumers (cobre-comm and cobre-python absent)

```
$ git grep -l 'test-support' 077dbe2c -- 'crates/*/Cargo.toml' | sed 's#^077dbe2c:##'
crates/cobre-cli/Cargo.toml
crates/cobre-core/Cargo.toml
crates/cobre-io/Cargo.toml
crates/cobre-sddp/Cargo.toml
crates/cobre-solver/Cargo.toml
crates/cobre-stochastic/Cargo.toml
```

### StubComm / Rank0Of2 copy census (canonical home tests/common/mod.rs:32/:86)

```
$ git grep -n 'struct \(StubComm\|Rank0Of2\)\b' 077dbe2c -- 'crates/**/*.rs' | sed 's#^077dbe2c:##'
crates/cobre-sddp/examples/dhat_baseline.rs:44:struct StubComm;
crates/cobre-sddp/src/simulation/pipeline/tests.rs:109:struct StubComm {
crates/cobre-sddp/src/test_support.rs:3555:    struct StubComm;
crates/cobre-sddp/src/training/backward/tests.rs:233:struct StubComm;
crates/cobre-sddp/src/training/backward_pass_state.rs:2216:    struct StubComm;
crates/cobre-sddp/src/training/backward_pass_state.rs:2258:    struct Rank0Of2;
crates/cobre-sddp/src/training/session/mod.rs:1654:    struct StubComm;
crates/cobre-sddp/src/training/session/mod.rs:2767:    struct Rank0Of2;
crates/cobre-sddp/src/training/training/tests.rs:173:struct StubComm;
crates/cobre-sddp/tests/common/mod.rs:32:pub struct StubComm;
crates/cobre-sddp/tests/common/mod.rs:86:pub struct Rank0Of2;
crates/cobre-sddp/tests/simulation_pipeline_integration.rs:78:struct StubComm {
```

### fn close carriers

```
$ git grep -n 'fn close' 077dbe2c -- crates/cobre-sddp/tests | sed 's#^077dbe2c:##'
crates/cobre-sddp/tests/branching_value_oracle.rs:93:fn close(a: f64, b: f64) -> bool {
crates/cobre-sddp/tests/extensive_form_oracle.rs:77:fn close(a: f64, b: f64) -> bool {
crates/cobre-sddp/tests/mpi_wire.rs:2816:    fn close(a: f64, b: f64) -> bool {
crates/cobre-sddp/tests/node_native_backward_gate.rs:76:fn close(a: f64, b: f64) -> bool {
```

### homing asymmetry (sibling tests.rs count)

```
$ git ls-tree -r --name-only 077dbe2c crates | grep -c '/src/.*tests\.rs$'
19
```

### read-only assertion

```
$ git status --porcelain --untracked-files=no || echo clean; echo '--- untracked:'; git status --porcelain | grep '^??' || echo '(none)'
 M plans/architecture-debt-audit/stations/test-corpus/tests/test_station.py
--- untracked:
?? plans/architecture-debt-audit/stations/test-corpus/attacker-log.md
?? plans/architecture-debt-audit/stations/test-corpus/attacker-prompt.md
?? plans/architecture-debt-audit/stations/test-corpus/candidates-architecture.json
?? plans/architecture-debt-audit/stations/test-corpus/candidates-over-engineering.json
?? plans/architecture-debt-audit/stations/test-corpus/candidates-performance.json
?? plans/architecture-debt-audit/stations/test-corpus/candidates-test-bloat.json
?? plans/architecture-debt-audit/stations/test-corpus/seeds.json
?? plans/architecture-debt-audit/stations/test-corpus/validate-tb.py
```
