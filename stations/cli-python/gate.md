# Owner gate — cli-python (2026-09, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`)

Run in the main session as AskUserQuestion rounds over this digest. Inputs: `wave-dispositions.json` (4 owned prior ids), `partI-handoff.json` (8 I.5 sub-claims + the I.3-7 fragment), `calibration.json` (40 calibrated entries, 20 dismissed + 2 sanctioned cleared, 7 dup-of, 0 held ids), `verdicts.json` (defender `_needsHuman`), the four `candidates-<lens>.json` envelopes (`_needsHuman`), `perf-queue.json`, `td-queue.json`, `verification.md` (shared verifier 7/7 + station verifier 10/10 + module 68/68 at the pin). Severity is the HOUSE rating; a downgraded row shows `house (reviewer: original)`. The gate executes no fix, runs no measurement and asserts no figure. A parity-gap row is rendered only with its three enforcement layers named (2 such rows: S6a-architecture-06, S6a-over-engineering-03, both CD-025 deltas — the CD-025 row below carries the layers); a parity-script or workflow decision is recorded with queue=build-ci, never as actionable here.

## 1. Presentation

### 1.1 Wave-5 dispositions — asked FIRST (round 0a, 4 owned ids)

Every id is a reused prior id (never re-minted); all four are `sharpen`, so each row carries the superseded and the surviving claim, the baseline-resolved anchor and the restated fix-shape owner. CD-025's historical 'cobre-sddp or cobre-io' destination wording is presented as SUPERSEDED (its own column); its two conflicts variants and CD-029's two are held in 1.4 and asked ALONE in R4, never in a batch. CD-002 carries the calibration's proposed B → C downgrade (register B recorded for the `downgrade` verb) and CD-029 its severity question; both are asked as their own round-0a questions.

| # | ID | Wave | Disposition | Anchor | Superseded → surviving | Fix-shape owner | Superseded wording | Sev (house / register) | Alignment | Enforcement layers |
| -- | -- | -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-025 | 5 | sharpen | `crates/cobre-cli/src/commands/run/outputs.rs::write_training_outputs` | the 2026-08 framing of a THREE-helper `_if_any` family, of a Python side with several entry points, and of the direction wording naming cobre-sddp as… → the cross-crate hand-mirror is intact at 077dbe2c: write_training_outputs (outputs.rs:58) re-inlines the fpha/evaporation/deviation/echo/fixed-delive… | cobre-io (L2) | hoist the shared "which outputs + guards" helpers to a crate BOTH already depend on (cobre-sddp or cobre-io); the `_if_… | B / B | advances-0a | scripts/ci/check_python_parity.py; crates/cobre-python/tests/test_cli_python_file_set_parity.py; crates/cobre-cli/tests/python_parity_check.rs |
| 2 | CD-029 | 5 | sharpen | `crates/cobre-sddp/src/validate_phases.rs::PrepPhase` | the Python-parity half ('zero equivalent in cobre-python/src/io.rs', ticket AC: grep -c boundary returns 0) — fixed by fc81427a, the binding validato… → PrepPhase's prose (validate_phases.rs:20) still claims four preparation steps over a three-row table and a three-variant enum, and the fourth step —… | cobre-sddp PrepPhase (L3) — cobre-io tagged conflicts | - | B / B | advances-0a | - |
| 3 | CD-002 | 4 | sharpen | `crates/cobre-cli/src/commands/run/setup.rs::broadcast_and_build_setup` | 'Shrinks once CD-001 lands' — CD-001 landed (b051c410, retired at the sddp gate 2026-09-18) and the tuple did NOT shrink: the non-root reconstruction… → broadcast_and_build_setup (setup.rs:246) destructures a positional 10-tuple (:253-263: raw_system, raw_bcast_config, root_config, root_stochastic, ro… | cobre-cli-local (constructor input struct) | - | C / B | neutral | - |
| 4 | CD-009 | 7 | sharpen | `crates/cobre-cli/src/commands/run/policy.rs::load_policy_for_simulation` | only the line anchors — the register cites the 2026-08 lines and the ticket the a136840d lines; the shape is unchanged → the resolve + `!exists()` guard is repeated exactly three times at policy.rs:172/201/334 (WarmStart :171, Resume :200, load_policy_for_simulation :32… | cobre-cli-local (path helper) | - | C / C | neutral | - |

**CD-025 restated shape.** shared output-orchestration entry point in cobre-io (L2): ONE call-site list — which outputs, under which config/system guards — consumed by both the CLI and the Python bindings, taking the per-engine results value plus the config/system context it needs; CLI and Python both wire through it, the five `_if_any` helpers and the three CLI orchestrators collapse into it The superseded wording's reason: the cobre-sddp half of the disjunction would home front-end output policy in an engine (L3); Part IV.1 places shared output orchestration at L2 (cobre-io), and Part III.7 / V.1 name that entry point as the Phase-0a deliverable — the wording is superseded, not replaced

**CD-029 destination and severity.** fold the boundary check into PrepPhase in cobre-sddp (L3) as its fourth phase — ONE engine-owned reconcile function that both front ends call and that prep_phase_metadata classifies — and correct the prose to match the variant count; the two front-end copies collapse into that single owner The disposition record holds two owner questions: (a) {'question': "The ticket (description + step 3) restates BOTH Wave-5 fix-shapes with destination = the shared output-orchestration entry point in cobre-io. CD-029's measured shape is a validation phase over cobre_sddp::StudySetup whose helpers all live in cobre-sddp, and the register's own fix is 'fold into PrepPhase' (a cobre-sddp enum). This record names cobre-sddp PrepPhase (L3) as the fold destination and tags a cobre-io home `conflicts`. Confirm, or override to cobre-io with a dated Owner-override line.", 'recommended': 'confirm cobre-sddp PrepPhase (L3) — output orchestration (L2 rule) does not cover a validation phase', 'forGate': 'E06-8'} (b) {'question': 'Severity: the parity half is fixed, but the bypass half now has two hand-mirrored copies (validate.rs::reconcile_boundary and run.rs::reconcile_boundary_policy). B is kept here; downgrade to C only if the gate treats the duplicated engine logic as local cleanup.', 'recommended': 'keep B', 'forGate': 'E06-8'} Question (a) is the CD-029 cobre-io hold in 1.4 (R4); question (b) is asked in round 0a.

**CD-002 proposed downgrade.** the register header rates B (bad-abstraction); the register's own 2026-08-19 roadmap (BACKLOG L1616 'Local cleanups (Sev C, batchable): CD-002/…') and the E06 tickets treat it as C-tier local cleanup; the output side was already a named struct (LoadBroadcastResult, ff6d3313) at audit time, the fix is an S-effort named input struct with no wire change — proposed C, original B recorded for the gate's `downgrade` verb Asked in round 0a as `keep B` / `downgrade to C`.

### 1.2 Part-I I.5 dispositions (round 0b, 8 sub-claims + the I.3-7 fragment)

Every row names the command that re-measured it (refused otherwise); two rows the ticket names as sharpen corrections (I.5-4 from_config — one non-test caller inside cobre-sddp, five across the workspace; the BroadcastConfig + StudySetup::new_with_boundary_requirements coupling path, carried by the I.3-7 fragment) are presented for ratification. Destination: the generalization-alignment epic (E9); every `advances-*` tag is provisional.

| # | Row | Sub-claim | Disposition | Old figure | Baseline figure | Command |
| -- | -- | -- | -- | -- | -- | -- |
| 1 | I.5-1a | the run subcommand's help text is 'train an SDDP policy' | retire | "train an SDDP policy" (v0.12 snapshot) | clap doc string at main.rs:70: "Load a case directory, train a policy, and run simulation." — main.rs carries zero occurrences of 'sddp' in any case | `git show 077dbe2c:crates/cobre-cli/src/main.rs / grep -n -B1 'Run(RunArgs)'; git show 077dbe2c:crates/cobre-cli/src/mai…` |
| 2 | I.5-1b | the whole lifecycle is the SDDP train → simulate two-phase shape gated on training_enabled and n_scenarios | keep | two-phase shape gated on training_enabled / n_scenarios | run/mod.rs:166 `if training_enabled {` and :224/227 `setup.simulation_config.n_scenarios > 0` | `git show 077dbe2c:crates/cobre-cli/src/commands/run/mod.rs / grep -n 'training_enabled\/n_scenarios'` |
| 3 | I.5-2 | the run is typed on the concrete cobre_sddp::StudySetup; training is setup.train(...), simulation setup.simulate(...) —… | keep | typed on StudySetup; inherent train/simulate | training.rs:11 `use cobre_sddp::StudySetup;`, :88 `setup.train(`; simulation.rs:21 `use cobre_sddp::StudySetup;`, :98 `.simulate(`; both are inherent `impl StudySetup` m… | `git show 077dbe2c:crates/cobre-cli/src/commands/run/training.rs / grep -n 'setup.train('; git show 077dbe2c:crates/cobr…` |
| 4 | I.5-3 | the CLI imports ~70 symbols from cobre_sddp | sharpen | ~70 symbols (v0.12 estimate); ticket @a136840d: 95 refs / 11 files / 68 use lines | 97 `cobre_sddp::` occurrences over 10 files, 70 `use cobre_sddp` lines, 88 distinct imported symbols (leaf names across every `use cobre_sddp::…;`, re-exports included) | `git grep -o 'cobre_sddp::' 077dbe2c -- 'crates/cobre-cli/src/*.rs' 'crates/cobre-cli/src/**/*.rs' / wc -l; git grep -l…` |
| 5 | I.5-4 | config conversion goes through cobre_sddp::StudyParams::from_config | sharpen | the CLI calls StudyParams::from_config | the run path never calls StudyParams::from_config directly: setup.rs:112 `BroadcastConfig::from_config(&config)?` (inside load_case_and_config :78) → broadcast.rs:143 `p… | `git grep -n 'from_config' 077dbe2c -- 'crates/cobre-cli/src/*.rs' 'crates/cobre-cli/src/**/*.rs' / grep -v tests` |
| 6 | I.5-5 | the output tree is training/ + simulation/ + hydro_models/ with a cut-based policy checkpoint; the summary vocabulary i… | sharpen | training/ + simulation/ + hydro_models/; a `summary` subcommand rendering LB/UB/gap | the tree holds: `training` (cobre-io training_writer.rs), `simulation` (outputs.rs:200, cobre-io simulation_writer.rs), `hydro_models` (outputs.rs:93/105/117), plus `gen… | `git show 077dbe2c:crates/cobre-cli/src/commands/run/outputs.rs / grep -n '"hydro_models"\/"generic_constraints"\/"simul…` |
| 7 | I.5-6 | only schema and version are near-generic commands; init ships a single hydrothermal template | sharpen | seven subcommands (init, run, validate, report, summary, schema, version); init: one template | five variants (main.rs:67: Init, Run, Validate, Schema, Version); schema and version stay near-generic; init scaffolds from embedded templates (1 named: 1dtoy), all hydr… | `git show 077dbe2c:crates/cobre-cli/src/main.rs / grep -n -A 12 'enum Command'; git show 077dbe2c:crates/cobre-cli/src/t…` |
| 8 | I.5-7 | validate, report and summary are SDDP-shaped: validate runs the same StudyParams::from_config conversion, report/summar… | sharpen | three SDDP-shaped subcommands (2026-07-23 addendum); the ticket's premise: commands/report.rs has zero cobre_… | report and summary no longer exist (797ba443 2026-09-16 feat: remove the report and summary subcommands and their Python and cobre-io mirrors); validate is SDDP-shaped e… | `git ls-tree --name-only 077dbe2c -- crates/cobre-cli/src/commands; git show 077dbe2c:crates/cobre-cli/src/commands/vali…` |
| 9 | I.3-7 fragment | the config type is SDDP-shaped: Config.training + the StudyParams::from_config conversion; BroadcastConfig is a hand-ma… | sharpen (handed to E9) | Part-I I.3-7 (BroadcastConfig as a hand-maintained SDDP-parameter subset) | crates/cobre-cli/src/commands/run/setup.rs:112 BroadcastConfig::from_config(&config)? (run path, inside load_case_and_config :78) → crates/cobre-cli/src/commands/broadca…; second path crates/cobre-cli/src/commands/validate.rs:384 StudyParams::from_config(&config) (direct) → crates/cobre-cli/src/command… | `crates/cobre-sddp/src/setup/mod.rs:378` (sites: 5) |

### 1.3 New entries by house severity (rounds 1-2; 40 entries, no Sev A)

#### Sev B — 13 entries

| # | ID | Sev | Alignment | Part-I | Claim (narrowed) | Fix-shape (one line) | Queue | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-089 | B | advances-0a | I.5 | The run lifecycle is hand-mirrored across both L4 entry points with no Engine seam: two three-branch phase plans, each reading enablement from a different source | Give the phase plan one owner at L4 and one query below it: the engine answers which phases a study runs as a small owned value (trained-then-simulated, simulate-from-st… | alignment | #1 |
| 2 | CD-090 | B | neutral | I.5 | Training solver-stats aggregation is a third hand-mirror outside CD-025's anchor list: two otherwise identical folds differing only by the rank filter, with a bit-for-bi… | One fold next to its input types in cobre-sddp `src/solver_stats.rs`, over `&[SolverStatsLogEntry]`, with the rank filter as an explicit argument (this rank, or all rank… | alignment | - |
| 3 | CD-091 | B | advances-0a | I.5 | The validate --json error contract and cobre.io.validate's error dict disagree on kind and message for the same input, and six CLI failure paths emit no JSON at all | Give LoadError one kind map owned by the crate that owns the type — cobre-io (L2), the crate both front ends already depend on — and have the CLI and the bindings both c… | alignment | #5, #6 |
| 4 | CD-095 | B | advances-0a | - | results.rs re-enumerates the simulation entity-family list as a ten-name constant against the fourteen families cobre-io writes, so the documented load-everything path s… | The family-name set has exactly one owner already, and it is not the bindings. cobre-io should expose the family names it writes as public data next to SIMULATION_FAMILI… | alignment | - |
| 5 | CD-096 | B | neutral | - | load_convergence hand-lists fourteen of the fifteen columns cobre-io declares for convergence.parquet, and its sibling load_convergence_arrow documents the same fourteen… | A reader that must name columns should derive them from the schema that declares them rather than from a second list. For the dict reader that means iterating the file's… | - | - |
| 6 | PD-049 | B | neutral | - | load_convergence and load_policy hold the GIL across their entire Rust-side decode, while three sibling readers in the same file release it with py.detach | No crate move: the fix stays in cobre-python (L4). Split each of the two readers into the two halves load_stochastic already separates, a detached decode that returns ow… | performance-sweep | - |
| 7 | PD-050 | B | neutral | - | read_parquet_partition_into re-runs the Arrow data-type match and the array downcast once per cell, while the sibling load_convergence hoists the identical downcast to o… | Stays in cobre-python (L4). Resolve the typed array view once per column per batch before the row loop, the shape get_column_by_name already provides for the convergence… | performance-sweep | #14 |
| 8 | PD-051 | B | neutral | - | Bulk f64 arrays reach NumPy through a per-element Python list: reshape_f64 and Policy::cut_matrix materialise every coefficient as a Python float that numpy then re-pars… | Stays in cobre-python (L4) and keeps numpy a soft, lazily imported dependency, which is the documented contract of these accessors. Hand numpy the bytes instead of a lis… | performance-sweep | - |
| 9 | TD-057 | B | neutral | - | Seven of the nine run-path test binaries hold exactly one #[test], and cobre-cli has no tests/common/: fn cobre() is redefined in 13 of 14 binaries, the repo-root case-d… | Introduce crates/cobre-cli/tests/common/ as the single harness home (a mod common declared from the consolidated binary, which is the layout section Cost discipline alre… | test-corpus | #22, #23 |
| 10 | TD-063 | B | neutral | - | The seven inline tests of the validate subcommand assert a test-local reimplementation of the report renderer, not the shipped one, and the copy has already drifted | Extract the rendering that execute inlines at 436-448 into a private fn that returns the lines (the same shape summary.rs already uses for training_summary_lines and sim… | test-corpus | - |
| 11 | TD-064 | B | neutral | - | Four cfg(test)-gated format_*_string twins in summary.rs let the shipped print_* renderers drift while the tests stay green; the shipped path is asserted only for absenc… | Apply the pattern the module header already declares and the file already uses three times: convert each of the four print_* twins to build its lines through a private *… | test-corpus | #35 |
| 12 | TD-065 | B | neutral | - | cobre-cli has no tests/common at all: the binary invocation helper is redefined in 13 of 14 integration binaries and the valid-case fixture in three, against the verbati… | Add crates/cobre-cli/tests/common/mod.rs holding the single fn cobre() built on assert_cmd cargo_bin, the write_file helper, and one parameterised valid-case builder who… | test-corpus | #37 |
| 13 | TD-066 | B | neutral | - | Ten full cobre run trainings execute inside the validate binary and three more inside the color binary, to assert claims the unit tier already covers | For the color suite, keep one test that exercises the flag-to-global wiring through cobre init (init calls print_banner and performs no solve) and drop the two remaining… | test-corpus | - |

#### Sev C — 27 entries

| # | ID | Sev | Alignment | Part-I | Claim (narrowed) | Fix-shape (one line) | Queue | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-092 | C | advances-0a | I.5 | The pre-solver phase driver is written twice over one engine-owned PrepPhase: the CLI extracts the (kind, message) pair into a helper, the bindings re-inline the identic… | Hoist the two-line body of describe_prep_error next to prep_phase_metadata in cobre-sddp's validate_phases.rs — the same destination CD-029's restated fix shape already… | alignment | - |
| 2 | CD-093 | C (reviewer: B) | neutral | - | Seven doc examples across four CLI modules import crate paths that cannot resolve, and the crate has no lib target so no doctest ever compiles them | Make each of the seven examples honest at its current fence. Rewrite the three `cobre::` imports to the module-local form the code actually uses, and for the four `cobre… | - | #8 |
| 3 | CD-094 | C | neutral | I.5 | summary.rs imports three cobre_sddp provenance types into production module scope, with pub and allow(unused_imports), only so its own test block's use super resolves | Split the declaration: keep a plain `use cobre_sddp::{BoundaryReconciliationReport, HydroModelSummary, ModelProvenanceReport};` for the three types the renderers name in… | alignment | - |
| 4 | CD-097 | C | neutral | - | The Python exception class of a failed run is recovered by string-prefix matching on an error message that twenty-five hand-written literals in the run path mint, and fi… | The classifier and the mint sites should agree by construction rather than by convention: one set of named constants for the prefixes, referenced at both ends, so a rena… | - | - |
| 5 | CD-098 | C | neutral | - | The schema-export write loop is duplicated verbatim between the bindings and the CLI, down to three identical message strings, and only the CLI copy is covered by the sc… | The loop that turns the generated map into files belongs beside generate_schemas in cobre-io, returning the count and a typed error each front end maps to its own type;… | - | - |
| 6 | OD-043 | C | neutral | - | commands/broadcast.rs is a 1,088-line postcard wire-type module filed under commands/, whose own parent doc says every module there is a clap subcommand | Placement only; the Config-to-BroadcastConfig projection content is CD-004's and is not touched here. Minimal shape: relocate the module out of commands/ to the director… | - | #17, #34 |
| 7 | OD-044 | C | neutral | - | WriteTrainingArgs carries hydro_models next to setup, and at its one construction site the field is literally &setup.hydro_models | Drop the hydro_models field and read args.setup.hydro_models at the four sites in write_training_outputs; the change stays inside cobre-cli (L4) and crosses no crate bou… | - | - |
| 8 | OD-045 | C | neutral | - | Thirteen production #[allow] suppressions in the cell carry no written rationale, including a verbatim-duplicated cast in the two phase files and one whose identical twi… | Two shapes, both inside the crates that own the sites. For the duplicated cast, compute parallelism once in a small function beside build_distribution_info in crates/cob… | - | #19 |
| 9 | OD-046 | C | neutral | - | Three subcommand entry points take their clap args by value and suppress `needless_pass_by_value` without a rationale, while the same `match` binds the run args by refer… | Take `&SchemaArgs` and `&ValidateArgs` at the two entry points that never move a field, bind those two arms as `ref args` in main's `match` to match the run arm, and del… | - | - |
| 10 | OD-047 | C | neutral | - | Three cobre.model entity twins are self-labelled minimal stubs: full pyclass scaffolding per class surfaces only id and name, and nothing outside a hasattr presence chec… | Either complete the three twins so each exposes the field set its core entity actually carries, matching the shape PyBus, PyLine, PyThermal, PyHydro and PySystem already… | - | #21 |
| 11 | OD-048 | C | neutral | - | Allow-attribute proliferation in the bindings crate: the errors test module re-declares the exact crate-level test allow lib.rs already sets, and 14 of the 17 needless_p… | Delete the allow attribute on the errors test module outright; the crate-level cfg_attr in lib.rs already covers all three lints for every test module in the crate, as t… | - | - |
| 12 | PD-052 | C | neutral | - | The seven PySystem entity-list getters deep-clone the whole entity vector on every attribute access, in a file whose from_arc constructor already establishes the share-b… | Stays in cobre-python (L4). Two shapes, both compatible with the module doc: hold the shared Arc<System> plus an index inside each entity wrapper so a getter becomes a r… | - | #16 |
| 13 | PD-053 | C | neutral | - | build_stage_cuts_data clones every cut coefficient vector of every stage even on the branch that needs no reservation, and does it under the GIL before the detached write | Stays in cobre-python (L4). Make the owned coefficient buffer conditional on the reservation, for example a borrowed-or-owned representation whose borrowed arm points at… | - | - |
| 14 | PD-054 | C | neutral | - | Stochastic::opening_tree scans every opening row of every stage to locate one stage's block, although the reader already rejects unsorted rows and the code comments that… | Stays in cobre-python (L4). Locate the block with the sort invariant the reader already enforces, by partition point on the first and last index of the stage rather than… | - | - |
| 15 | PD-055 | C | neutral | - | cobre.io.validate and cobre.io.load_case hold the GIL across the whole case load, stochastic preparation and hydro-model preparation, while Study in the same crate detac… | Stays in cobre-python (L4). Convert the overrides as today, then run the phase pipeline inside py.detach and return a plain Rust outcome value that carries either the fi… | - | - |
| 16 | TD-058 | C (reviewer: B) | neutral | - | test_study.py and test_run.py declare no fixtures and re-train or re-run examples/1dtoy per test function, including for pure argument-validation claims, while test_outp… | Add a module-scoped trained-policy fixture to crates/cobre-python/tests/conftest.py (the file that already owns the session-scoped cli_binary fixture, so no new file and… | test-corpus | #24 |
| 17 | TD-059 | C (reviewer: B) | neutral | - | The bindings' inline test module runs seven full run_via_study studies and hand-pins the CLI's 1dtoy numbers, and cli_e2e_run_end_block.rs pins the same mean cost again… | Give the CLI/Python 1dtoy agreement one owner. Keep the live derivation in the pytest parity suite as the authority, and reduce the inline bindings test to what only it… | test-corpus | #36 |
| 18 | TD-060 | C | neutral | - | The pytest parity files each rebuild the same harness: _make_case_with_simulation is declared four times with a byte-identical body, plus a fifth copy inside _seed_case… | Hoist the shared harness into the seam that already exists: put one case-with-simulation builder, one Hive-partitioned parquet collector parameterised by entity director… | test-corpus | #25 |
| 19 | TD-061 | C | neutral | - | test_run_via_study_emits_contract_output asserts a strict subset of test_cli_python_contract_row_parity on the same Python surface and the same derived d41 deck, costing… | Drop test_run_via_study_emits_contract_output and let test_cli_python_contract_row_parity carry the Python-side schema and non-emptiness assertions it already makes, or,… | test-corpus | - |
| 20 | TD-062 | C | neutral | I.5 | run/mod.rs's inline test module is the wrong home for its unit tests: it tests resolve_thread_count, declared in setup.rs which has its own test module, and pins three l… | Move the two thread-count tests into the existing #[cfg(test)] module of setup.rs, next to the function they exercise, which also removes the cross-module import from th… | test-corpus, alignment | #26 |
| 21 | TD-067 | C | neutral | - | Fifteen of the twenty-two inline progress tests spawn a real renderer thread to re-assert one channel round-trip contract, five of them in cases the widest test already… | Collapse the fifteen spawn-and-inspect tests into one table-driven test over (event sequence, expected variant order) plus the empty-channel edge and one Log-mode case,… | test-corpus | #27 |
| 22 | TD-068 | C | neutral | - | One assertion per subprocess: several integration tests re-run the same invocation to check a second substring of the same output | Merge each split family into a single test that builds the fixture once, spawns once, and chains the exit-code and substring predicates on one Assert. Byte-neutral: the… | test-corpus | - |
| 23 | TD-069 | C | neutral | - | Six of the seven template tests assert compile-time literals or restate an inert doc example; the one real guard sits next to a frozen file count the sibling schema suit… | Delete test_1dtoy_files_have_descriptions and test_1dtoy_files_have_relative_paths (static literals, nothing can change them at runtime), and replace the frozen assertio… | test-corpus | - |
| 24 | TD-070 | C | neutral | - | fmt_sci is defined byte-identically in two S6b modules and neither copy has a direct test, so a divergence between the two renderers is unasserted | Keep one fmt_sci, delete the other, and give the surviving copy one table-driven unit test over the exponent cases the two call sites exercise (positive and negative exp… | test-corpus | - |
| 25 | TD-071 | C (reviewer: B) | neutral | - | Seven py_to_json_value unit claims are routed through the eleven-phase cobre.io.validate on a premise the crate's own Rust tests falsify | Give convert.rs its own #[cfg(test)] mod tests following the pattern errors.rs already proves under --no-default-features: Python::initialize() then Python::attach, buil… | test-corpus | #28 |
| 26 | TD-072 | C | neutral | - | conftest.py owns one fixture, so copy-the-case and find-the-case are each re-implemented per module in two incompatible conventions and borrowed across modules by a priv… | Move the shared harness surface into conftest.py beside cli_binary: a session-scoped constant for the repo root that the modules import instead of re-deriving parents[3]… | test-corpus | #29 |
| 27 | TD-073 | C (reviewer: B) | neutral | - | Four redundant full solver runs pay for preconditions a module-scoped fixture already establishes in the same directory | In test_boundary_load.py add a module-scoped fixture that performs the source run once with tmp_path_factory and yields the source policy directory, then have the three… | test-corpus | #30 |

### 1.4 Holds (Alignment `conflicts` variants) — each asked ALONE (round 4)

No calibrated entry is a `conflicts` row (calibration.json `conflicts: []`). The four holds are the conflicts-tagged fix-shape VARIANTS on the two Wave-5 disposition records; each is asked as its own question beside the record's restated alternative, and an override is a guardrail exception recorded with a dated rationale. An un-overridden hold stays held and no id is promoted.

| # | Prior id | Variant (held) | Rule it conflicts with | Roadmap-consistent alternative | Status |
| -- | -- | -- | -- | -- | -- |
| 1 | CD-025 | cobre-sddp home (orchestration at L3) | Part IV.1 — L2 cobre-io owns shared output orchestration; an engine owning the front ends' output policy means the second engine (cobre-direct, V.1) would route its outputs through a sibling engine — trigger 2 of the la… | shared output-orchestration entry point in cobre-io (L2): ONE call-site list — which outputs, under which config/system guards — consumed by both the CLI and the Python bindings, taking the per-engin… | HELD for the owner at this gate |
| 2 | CD-025 | cobre-cli-local helper (keeps the double-mirror) | Part IV.1 — L2 cobre-io owns 'shared output orchestration'; forbidden at L2: 'an engine-specific output tree hard-wired in the CLI instead of orchestrated here'; a helper in cobre-cli is unreachable from cobre-python, s… | shared output-orchestration entry point in cobre-io (L2): ONE call-site list — which outputs, under which config/system guards — consumed by both the CLI and the Python bindings, taking the per-engin… | HELD for the owner at this gate |
| 3 | CD-029 | cobre-io home for the boundary reconciliation | Part IV.1 — L2 forbids engine dispatch; the reconciliation is typed on cobre_sddp::StudySetup and calls cobre-sddp's policy loader, so an L2 home inverts the layering (cobre-io depending on an engine crate) | fold the boundary check into PrepPhase in cobre-sddp (L3) as its fourth phase — ONE engine-owned reconcile function that both front ends call and that prep_phase_metadata classifies — and correct the… | HELD for the owner at this gate |
| 4 | CD-029 | keep two front-end copies (cli-local fold into run_boundary_check, Python keeps reconcile… | Part IV.1 / III.7 — the hand-mirrored call-site list is the duplication mechanism the roadmap names for outputs; the same shape on the validation path keeps two owners of engine logic in L4 | fold the boundary check into PrepPhase in cobre-sddp (L3) as its fourth phase — ONE engine-owned reconcile function that both front ends call and that prep_phase_metadata classifies — and correct the… | HELD for the owner at this gate |

### 1.5 Cleared and dup-of — presented read-only, no decision taken

**Dismissed by a defender (20), each with the basis and the citation that decides it:**

- **S6a-architecture-04** — sanctioned-seam *docs/design/reserved-seams-and-deferred-debt.md:563* — The CLI run path reaches the engine's parameter projection only through the MPI wire type, unconditionally, and both fr…: The mirror entry the candidate itself cites reserves exactly this caller shape. At docs/design/reserved-seams-and-deferred-debt.md:563 the open half is stated as 'the local StudyParams and the wire BroadcastConfig proje…
- **S6b-architecture-06** — deliberate-and-documented *crates/cobre-python/src/errors.rs:13-17* — The two L4 classifications of cobre_sddp::SddpError are not jointly owned and only the CLI's is compiler-enforced, so a…: The asymmetry is deliberate, documented at crates/cobre-python/src/errors.rs:13-17, and its load-bearing premise holds at the pin. ErrorSource::Sddp is constructed at exactly four production sites (run.rs:362, run.rs:42…
- **S6c-architecture-00** — premise-false-at-pin *crates/cobre-python/src/run.rs:1395-1444* — The bindings carry no Engine seam: Study and Policy are typed directly on six cobre_sddp types, and two separately regi…: Judging only the station delta the ingest note names (two separately registered public entry points over one engine), not the I.5 seam claim. The delta's load-bearing premise is that cobre.run.run and cobre.Study are tw…
- **S6c-architecture-03** — deliberate-and-documented *crates/cobre-python/src/results.rs:117-135 (load_results returns the metadata files' contents as a dict; Valu…* — Both front ends bypass cobre-io's two typed metadata readers in production and read the same files untyped, leaving the…: The two reads are not the same read, so there is no bypassed reuse. load_results is documented at crates/cobre-python/src/results.rs:117 and :123 to hand Python the CONTENTS of training/metadata.json and simulation/meta…
- **S6c-architecture-06** — premise-false-at-pin *crates/cobre-sddp/src/policy/policy_load.rs:55-61* — write_policy_checkpoint in the bindings re-declares the engine's cost-scale default as a bare literal while cobre-sddp…: The load-bearing blast-radius claim does not hold at the pin. cobre-sddp declares TWO public constants at this value with opposite lifetimes: DEFAULT_COST_SCALE_FACTOR (crates/cobre-sddp/src/setup/params.rs:43), the mov…
- **S6a-performance-00** — cost-accepted-by-rule *crates/cobre-cli/src/commands/run/setup.rs:319-380 (the once-per-run broadcast region, timed into SetupTiming…* — broadcast_value postcard-encodes and discards every payload on rank 0 even when the world has a single rank: The candidate's facts hold at the pin. In broadcast.rs, the is_root arm at lines 417-421 calls postcard::to_allocvec with no comm.size() guard, the encoded buffer is broadcast at lines 445-449, and the root arm at lines…
- **S6a-performance-01** — cost-accepted-by-rule *crates/cobre-cli/src/commands/run/simulation.rs:155-158 — both gathers' only call sites, post-simulate teardo…* — Both simulation gathers use allgatherv although only rank 0 consumes the gathered rows: Every factual premise holds at the pin. Both helpers allgatherv their payload (simulation.rs:363, :408) after exchange_gather_plan allgathers the per-rank length vector (:324), and the sole consumer of global_path_rows…
- **S6a-performance-02** — cost-accepted-by-rule *CLAUDE.md § Architecture Guides — hot-path list (training/forward, training/backward, training/training.rs, s…* — The cobre-io path-row writer takes ownership, so both front ends clone the full simulation path-row set at the write ca…: The premise is factually present at the pin and the dismissal rests on cost, not on premise: write_paths takes rows by value and sorts them in place (crates/cobre-io/src/output/simulation_writer.rs:1168-1172), while bot…
- **S6a-performance-03** — cost-accepted-by-rule *CLAUDE.md § Hard Rules — "Never allocate on hot paths" (hot-path list in CLAUDE.md § Architecture Guides); th…* — Both front ends allocate a per-scenario vector only to reorder two tuple fields before the scenario-summary write: The named mechanism is one allocation per run, not per scenario, per stage or per call in a loop. The CLI reshape at outputs.rs:214-220 is reachable only through write_sim_outputs_on_root under the is_root guard (simula…
- **S6a-performance-04** — cost-accepted-by-rule *crates/cobre-cli/src/commands/run/simulation.rs:156* — Every per-scenario solver-stats delta is cloned, heap histogram included, only to drop one tuple field before packing: The mechanism is factually real at the pin and I am not disputing it. simulation.rs:397-402 builds local_stats_stripped by cloning every local (u32, i32, SolverStatsDelta) entry into a two-tuple purely to shed the openi…
- **S6a-performance-05** — cost-accepted-by-rule *crates/cobre-cli/src/commands/run/setup.rs:352-363 (sole production call of prepare_hydro_models, setup phase…* — Non-root ranks re-read the case directory and redo the hydro-model fit rank 0 already holds: The mechanism is real but it is entirely setup-phase work, which the station's perf rule accepts by name (case load, hydro-model fit, validation). prepare_hydro_models has exactly one production caller at crates/cobre-c…
- **S6a-performance-06** — cost-accepted-by-rule *crates/cobre-cli/src/commands/run/mod.rs:228* — In simulation-only mode every rank reads and rebuilds the whole policy checkpoint, while the sibling boundary-cut path…: load_policy_for_simulation has exactly one call site (crates/cobre-cli/src/commands/run/mod.rs:228), reached once per run in the non-training branch of execute_inner immediately before run_simulation_phase, so the direc…
- **S6a-performance-07** — deliberate-and-documented *crates/cobre-cli/src/commands/run/mod.rs:172-173* — The serial rank-0 training-output write chain sits on every rank's critical path with nothing overlapping it: Dismissed on the in-code rationale at crates/cobre-cli/src/commands/run/mod.rs:172-173 ('Write training outputs before simulation so they persist even if simulation fails'), which the crate's failure model makes conting…
- **S6b-performance-01** — deliberate-and-documented *crates/cobre-cli/src/progress.rs:7-9 (module doc: both strategies collect every received event verbatim and r…* — The progress thread retains every received training event in a vector that the simulation caller discards: The receiver is taken by value at progress.rs:156 and moved into the spawned closure at :161, so once a progress thread exists the returned Vec is the only path by which any caller can observe the event stream; training…
- **S6c-performance-03** — cost-accepted-by-rule *CLAUDE.md Hard Rules - 'Never allocate on hot paths' (hot-path scope list under CLAUDE.md Architecture Guides…* — load_results converts the same training metadata JSON tree into Python twice, once for the manifest key and once for th…: Premise is true at the pin: crates/cobre-python/src/results.rs:184-185 call json_value_to_py on the same &metadata_val read at :160, so the recursive per-node conversion really does run twice. The mechanism is nonethele…
- **S6a-over-engineering-02** — contract *clippy.toml:1 (too-many-lines-threshold = 150), with Cargo.toml:65-67 and Cargo.toml:71* — write_sim_outputs_on_root is a decision-free pass-through layer, and the root guard its name asserts lives at the call…: The candidate's load-bearing step — inline the seven-parameter wrapper into its single call site — is unavailable at the pin, because the project's own configured complexity bar is what produces the extraction. clippy.t…
- **S6a-over-engineering-05** — premise-false-at-pin *crates/cobre-python/src/run.rs:1412* — RunSummary is declared pub(crate) but has no consumer outside its own file, unlike the SimSummary declared 14 lines bel…: The grep in the candidate is accurate, but its load-bearing inference is not: the consumer that asks for crate visibility here is a signature, not a call site. RunSummary is the success arm of the return type of run_via…
- **S6b-over-engineering-04** — contract *.claude/rules/comments.md:365* — Nine production cast suppressions in the summary module carry no `// Rationale:` line, against the mirror's own Load-be…: The factual half of the candidate holds at the pin: the nine production cast suppressions exist as filed (summary.rs:602, 648, 713, 757, 777, 821, 851, 866, 870) and none is preceded by a Rationale line, while the test-…
- **S6c-test-bloat-01** — premise-false-at-pin *crates/cobre-python/src/schema.rs:69* — Schema export is asserted twice in two languages, with one test name byte-identical across the Rust and pytest suites: The load-bearing premise (the Rust test is a strict superset of the pytest pair) does not hold at the pin. Both Rust tests call the plain Rust export with an already-built PathBuf (schema.rs:69, schema.rs:92), so they e…
- **S6c-test-bloat-04** — premise-false-at-pin *docs/design/testing-architecture.md:430 (section 5.2, Adoption 2026-09-15: the bindings manifest does not yet…* — testing-architecture section 5.11 still asks for the bindings crate's Rust tests to be wired into CI and section 5.2 st…: Both conjuncts of the title fail at the pin. The section 5.2 conjunct is refuted by the doc itself: the dated Adoption paragraph at docs/design/testing-architecture.md:425-431 states that the bindings crate's dev-depend…

**Sanctioned before any id was minted (2), with the owner question each carries to round 5:**

- **The facade crate `cobre`** — sanctioned by ARCHITECTURE.md:102-106 at the pin; no mirror row; keep-or-retire is needs-human #32.
- **cobre-python Rust `#[test]` population 'invisible to CI'** — registered deferred-debt entry (mirror :347), premise SUPERSEDED at the pin (SN-06); the E11 write-back scope is needs-human #31.

**Dup-of (7, folded, no new number):** S6b-performance-00 → CD-029 (ingest fold); S6b-over-engineering-01 → TD-064 (ingest fold); S6b-over-engineering-03 → CD-093 (ingest fold); S6b-over-engineering-05 → CD-094 (ingest fold); S6b-test-bloat-04 → CD-093 (ingest fold); S6b-test-bloat-08 → CD-094 (ingest fold); S6a-test-bloat-00 → TD-057 (calibration fold)

**Reserved-seam filters (ratified seams, never a live finding):** the section's Cleared list carries the `BroadcastConfig` projection content (CD-004's twin, mirror :563), the `#[allow(...)]` census classes (mirror :1272) and the `Unwired config is reserved, not dead` hard rule; each cited against docs/design/reserved-seams-and-deferred-debt.md or CLAUDE.md. No question is offered: a round would invite a re-raise.

### 1.6 Queues handed on (counts only; no figure)

- **perf-queue.json → E10:** 3 claim rows, all UNMEASURED, all single-process `4t` cobre-python READER claims (Python-side harness noted in the layout contract): PD-049 (`4t`, requires none), PD-050 (`4t`, requires none), PD-051 (`4t`, requires none). The four Sev-C PD rows (PD-052, PD-053, PD-054, PD-055) are section-only and not queued. A rejected or C-downgraded PD row is struck from the queue by this gate.
- **td-queue.json → E08:** 17 TD rows: TD-057, TD-058, TD-059, TD-060, TD-061, TD-062, TD-063, TD-064, TD-065, TD-066, TD-067, TD-068, TD-069, TD-070, TD-071, TD-072, TD-073; each paired with its testing-architecture.md § yardstick (§5 is a proposal — the yardstick, never settled); one calibration fold (S6a-test-bloat-00 → TD-057).
- **alignment → E9:** 7 non-neutral / I.5-tagged new rows: CD-089 (advances-0a · I.5), CD-090 (neutral · I.5), CD-091 (advances-0a · I.5), CD-092 (advances-0a · I.5), CD-094 (neutral · I.5), CD-095 (advances-0a), TD-062 (neutral · I.5); plus the reused CD-025, CD-029 (advances-0a) and the I.3-7 BroadcastConfig fragment (CD-004's twin, OD-043 for placement). Written to `alignment-queue.json` after the gate if the owner approves the sddp-shaped extras (R7).
- **build/CI + docs → E07** and **reconciliation → E11:** doc corrections only, no id here — the stale CI-invisibility premise (mirror :347, testing-architecture.md §5.11 / :17 / :101-102), the allow-census clause vs comments.md D4 (mirror :1324-1330), the facade crate's missing mirror row, the CD-025 destination-wording supersession, the shared-filesystem assumption (if ratified), BroadcastNodeGraph's row (if kept). Routed decisions are recorded with queue=build-ci / reconciliation, never as actionable rows of this station.

### 1.7 Handoffs the alignment epic consumes (round 6 confirmation)

- **partI-handoff.json** — 8 I.5 rows (dispositions {'retire': 1, 'keep': 2, 'sharpen': 5}) with their re-measure commands; the I.3-7 fragment (BroadcastConfig path: crates/cobre-cli/src/commands/run/setup.rs:112 BroadcastConfig::from_config(&config)? (run path, inside load_case_and_config :78) → crates/cobre-cli/src/comman…); destination generalization alignment epic (E9) — the I.5 rows and the item-7 fragment are consumed there; every advances-* tag is provisional until the E06-8 gate.
- **The three parity enforcement layers** named on every CD-025 delta and on every PD fix-shape: scripts/ci/check_python_parity.py; crates/cobre-python/tests/test_cli_python_file_set_parity.py; crates/cobre-cli/tests/python_parity_check.rs — the parity-script and workflow surface itself belongs to the build/CI/docs station.
- **perf-queue.json rows → E10; td-queue.json rows → E08** as listed in 1.6.

### 1.8 Worker needs-human items (37 questions; 39 defender + 30 attacker + 3 disposition items merged)

Defender items come from `verdicts.json` `_needsHuman` (39 on 37 candidates); the four attacker envelopes carry 30 envelope-level items, 29 of which are merged onto the question they restate (shown as members) and 1 (E04, the CD-029 destination) rides the R4 hold; the 3 disposition-record items are asked in round 0a / R4. Every question lists its recommended option first.

| # | Entry | Members | Question | Recommended |
| -- | -- | -- | -- | -- |
| 1 | CD-089 | S6a-architecture-00 (confirmed); attacker/architecture S6a E01 | Under the single phase-plan owner (CD-089), do the no-op arm's two behaviours converge — the CLI writes 'Training disabled, simulation disabled' to stderr, the bindings return a zeroed RunSummary (converged false, iterations 0, lower_bound 0.0)? Both are public surface and neither writes a file, so the file-set parity test cann… | Keep per-front-end presentation |
| 2 | CD-025 | S6a-architecture-01 (confirmed); attacker/architecture S6a E02; S6a-architecture-02 (confirmed); S6a-architecture-05 (confirmed) | Where do the cobre-sddp-typed folds live once the writes move to cobre-io (CD-025)? delta_to_stats_row / SolverStatsDelta (outputs.rs:29, :35), the five training row folds (outputs.rs:32-38) and the solver-stats totals fold (CD-090) take engine-declared inputs, so an L2 home would make cobre-io depend on an engine crate. Confir… | cobre-sddp home for the folds; writes + guards to L2 |
| 3 | — | S6a-architecture-04 (dismissed); attacker/architecture S6a E03 | StudySetup::from_broadcast_params (cobre-sddp's sole construction core, entered by both front ends) carries a transport-flavoured name. File a rename against the station that owns cobre-sddp's public API, or leave it to the setup-layer redesign the mirror :563 entry already triggers? | Leave it to the setup-layer redesign (CD-004) |
| 4 | CD-029 | S6b-architecture-00 (confirmed) | Which vocabulary supplies the boundary error object's user-visible `phase` string — the binding's literal BoundaryReconciliationError (cobre-python io.rs:305-328) or a fourth prep_phase_metadata row produced by the folded engine-owned phase? The second depends on CD-029's PrepPhase destination (R4). | Fourth prep_phase_metadata row (engine-owned phase) |
| 5 | CD-091 | S6b-architecture-01 (confirmed); S6b-architecture-02 (confirmed); attacker/architecture S6b E06 | May the CLI's --json kind/phase string CaseValidationError (validate.rs:90, :361) be retired in favour of the cobre-io/Python vocabulary (ConstraintError etc.)? No test, script or binding matches on it, but it is a field in a documented machine-readable contract. | Retire it; one cobre-io vocabulary, noted in the CHANGELOG |
| 6 | CD-091 | S6b-architecture-02 (confirmed); attacker/architecture S6b E07 | Where does the shared LoadError → stable-kind classification live? cobre-io (L2) owns LoadError and both front ends depend on it, but the station's L2 destination rule is written for output orchestration. Confirm that L2 also binds shared front-end contract data (an error-classification map), or name another owner. | cobre-io (L2) owns the kind map |
| 7 | CD-029 | S6b-architecture-03 (confirmed); attacker/performance S6b E21 | Sequencing for the duplicate boundary-requirements resolve in validate.rs: patch it now as a cobre-cli-local parameter thread, or hold it and let CD-029's fold of the boundary check into PrepPhase absorb it (the requirements become a phase input resolved once)? | Hold; CD-029's fold absorbs it |
| 8 | CD-093 | S6b-architecture-04 (confirmed); attacker/architecture S6b E05; attacker/over-engineering S6b E15; attacker/test-bloat S6b E27 | Should cobre-cli gain a [lib] target (plus a cargo test --doc step) so its seven doc examples compile, or should the examples stop presenting themselves as importable code? A library publishes the CLI's renderers, error enum and progress types as a maintained surface with no current consumer. | No [lib]; make the seven examples honest at their fences |
| 9 | — | S6c-architecture-00 (dismissed) | Coverage residue behind the dismissed S6c-architecture-00: study.rs:583-585 / :694-696 admit that whole-tree byte-identity is unasserted for the cobre.Study methods, while the whole-tree goldens reach the same native calls only through run_via_study. Does that warrant its own test-corpus coverage item, or is it covered in subst… | Covered in substance; no new item |
| 10 | — | S6c-architecture-06 (dismissed) | Should the terminal fallback at cobre-python policy.rs:510 spell the exported cobre_sddp::LEGACY_COST_SCALE_FACTOR (no new dependency), and should write_policy_checkpoint's doc state the both-markers-absent case (the field doc at policy.rs:104-107 points at a doc that is silent on it)? | Yes to both, as an E11 reconciliation nit |
| 11 | — | S6a-performance-01 (dismissed); attacker/performance S6a E18 | Rooted gather at L0: closing the non-root replication in the two simulation gathers needs a gatherv the cobre-comm Communicator trait does not expose (traits.rs:64 has allgatherv, allreduce, broadcast, barrier). Admit the primitive (grows the L0 FFI surface across ferrompi, LocalBackend, the factory enum and the precondition ta… | Keep allgatherv; cost accepted |
| 12 | — | S6a-performance-05 (dismissed); S6a-performance-06 (dismissed); attacker/performance S6a E19 | Is the shared-filesystem re-read on non-root ranks (case directory, hydro-model refit, and the per-rank policy-checkpoint read in simulation-only mode) a ratified deployment assumption? Two module docs state the behaviour (setup.rs:1-8, hydro_models/mod.rs:66-68) and no docs/design entry records it. | Ratify it; E11 records the assumption |
| 13 | CD-025 | S6a-performance-07 (dismissed); attacker/performance S6a E20 | Overlapping the rank-0 training-artefact write with rank 0 entering simulation trades the documented 'training outputs persist even if simulation fails' guarantee (mod.rs:172-173, contingent on completion because any rank's error reaches comm.abort) plus the stderr progress ordering. Accept that trade (the item re-enters the pe… | Keep the serial shape |
| 14 | PD-050 | S6c-performance-01 (confirmed); attacker/performance S6c E22 | Is the dict-shaped cobre.results.load_simulation a supported bulk reader, or is load_simulation_arrow the intended path for large result trees? load_simulation's rustdoc (results.rs:1081-1126) never points at the Arrow reader; the answer decides whether PD-050's per-column hoist is worth doing. | Supported bulk reader; PD-050 proceeds, doc cross-links Arrow |
| 15 | — | S6c-performance-03 (dismissed); attacker/performance S6c E23 | Is object identity between result['training']['manifest'] and result['training']['metadata'] part of the published cobre.results contract? Binding one converted dict to both keys would alias them on mutation; the dismissal (setup-time double conversion) stands unless overridden. | Identity unspecified; dismissal stands |
| 16 | PD-052 | S6c-performance-04 (confirmed) | Is per-read element identity part of the published cobre.model contract? Today system.buses[0] is system.buses[0] is False (every read builds new objects); PD-052's per-collection memo would make identity stable across reads. No test pins either. | Identity unspecified; the memo shape is acceptable |
| 17 | OD-043 | S6a-over-engineering-00 (confirmed); attacker/over-engineering S6a E10 | BroadcastNodeGraph (commands/broadcast.rs:256): keep as a documented reserved seam or retire? Its only references workspace-wide are its two From impls and ~110 lines of inline round-trip tests; the mirror's opposite precedent (docs/design/reserved-seams-and-deferred-debt.md:683-697) deleted an identically shaped zero-caller po… | Retire it (mirror :683-697 precedent) |
| 18 | CD-025 | S6a-over-engineering-03 (confirmed) | Sequencing for the two uncovered emit conditions (evaporation_models, fpha_deviation_points — no enforcement layer asserts them): close them now with CLI-vs-Python pytest coverage on an evaporation case plus a deviation-points case, or wait for CD-025's shared cobre-io emit list to remove the duplicated condition and gate it th… | Wait for CD-025's shared emit list |
| 19 | OD-045 | S6a-over-engineering-04 (confirmed); S6b-over-engineering-02 (confirmed); S6b-over-engineering-04 (dismissed) | The mirror's allow census (docs/design/reserved-seams-and-deferred-debt.md:1272, :1324-1330) asserts every Load-bearing numeric-cast and needless_pass_by_value allow carries a // Rationale: comment, which comments.md D4's closed list does not require (twelve cast sites and three needless_pass_by_value sites in this station carr… | Correct the mirror prose down to D4 (E11) |
| 20 | CD-029 | S6b-over-engineering-00 (confirmed); attacker/over-engineering S6b E12 | Should `cobre validate --json` emit an error object when boundary reconciliation (the fourth phase the module doc lists) fails? Today stdout is empty and stderr carries a hint the human path forbids; no test pins either shape, so adding the object extends the --json contract on an untested path. | Yes: emit the object and add a --json reject test |
| 21 | OD-047 | S6c-over-engineering-00 (confirmed); attacker/over-engineering S6c E17 | The three cobre.model stub twins (EnergyContract, PumpingStation, NonControllableSource) expose only id and name. Complete them to the house pattern the other four twins follow, or withdraw them and have the PySystem getters return untyped mappings until a caller needs typed objects? | Complete the three twins |
| 22 | TD-057 | S6a-test-bloat-00 (confirmed); S6a-test-bloat-01 (confirmed); attacker/test-bloat S6a E25 | Is the cobre-cli run-path binary consolidation (tests/common/ + folding the single-#[test] binaries, TD-057) sanctioned now under the standing Cost-discipline rule, or does it wait for the proposed §5.1 Layer-1 migration in docs/design/testing-architecture.md so the move is made once? | Consolidate now under the standing rule |
| 23 | TD-057 | S6a-test-bloat-01 (confirmed) | Helper home for the cobre-cli integration corpus: rule 14 names tests/common/ while testing-architecture §6 phase 3 proposes collapsing tests/common/ into cobre-sddp's test-support feature. Pick one so the helper home is not built twice. | tests/common/ in cobre-cli (rule 14) |
| 24 | TD-058 | S6a-test-bloat-02 (confirmed) | Is the per-test-isolation cost acceptance recorded in the two pytest module docstrings (test_study.py:12-14, test_run.py:9-11) standing policy for the pytest corpus? If it is, the shared read-only-Policy fixture (TD-058) is unwanted even for the five named tests. | Not standing policy; the shared fixture is sanctioned |
| 25 | TD-060 | S6a-test-bloat-04 (confirmed); S6a-test-bloat-05 (confirmed); attacker/test-bloat S6a E24 | Does testing-architecture §5.11's 'keep the pytest output-parity suite unchanged' freeze the eleven parity modules against fixture hygiene (harness hoisting, removing a strictly subsumed test), or only exempt them from the §5.1 Layer-1 binary migration? The answer decides whether TD-060 / TD-061 / TD-062 are actionable. | It freezes the comparisons, not the files |
| 26 | TD-062 | S6a-test-bloat-06 (confirmed) | The one unique stats-row assertion (the simulation shape) in run/mod.rs's misplaced test module: rehome it next to cobre-sddp's existing solver_stats_log_to_rows test, or reshape it into a front-end assertion covering both outputs.rs:197 and cobre-python run.rs:824 under the Python-parity hard rule? | Front-end assertion covering both call sites |
| 27 | TD-067 | S6b-test-bloat-05 (confirmed) | The progress inline test module (22 fns, 608 lines) exceeds the unratified ~500 test-LOC inline homing threshold of testing-architecture §5.1 while staying under the ~40-fn threshold. Does TD-067's consolidation land inline or in an extracted src/progress/tests.rs sibling? | E08's ratification call; default inline |
| 28 | TD-071 | S6c-test-bloat-00 (confirmed); attacker/test-bloat S6c E30 | convert.rs:104-106 and test_convert.py:5-9 assert that a #[cfg(test)] module cannot link an interpreter, contradicted by errors.rs and the feature-off CI invocation. Land TD-071 as a rationale correction only, as an added cfg(test) module (keeping the seven pytest merge-landing assertions), or both in one change? | Both in one change |
| 29 | TD-072 | S6c-test-bloat-02 (confirmed) | Case-path convention for the pytest corpus: pyproject.toml:52 sets testpaths = ["tests"], which makes a bare pytest run from the crate directory a configured invocation the nine cwd-relative modules cannot resolve, while every docstring and ci.yml name the repo-root invocation. Normalize all nine onto a __file__-resolved case p… | Normalize onto __file__-resolved paths |
| 30 | TD-073 | S6c-test-bloat-03 (confirmed); attacker/test-bloat S6c E29 | Anchor vocabulary for pytest files: five of TD-073's six anchors are pytest def names. tools/check-anchors.py grew a def/class branch for .py paths at the E06-3 shared-tool extension (aa95a722), so these anchors already resolve. Confirm that the pytest anchors stay first-class anchors (not demoted to evidence). | Confirm: pytest anchors stay first-class (already resolving) |
| 31 | — | S6c-test-bloat-04 (dismissed) | Confirm the E11 write-back's scope covers every design-doc trace of the superseded CI-invisibility premise: reserved-seams-and-deferred-debt.md:347, testing-architecture.md:17 and :101-102 (the unit-test half is false at the pin; the doctest half still holds because the lib target sets doc = false), and §5.11. | Yes: E11 corrects every trace |
| 32 | — | attacker/architecture S6c E08; attacker/over-engineering S6b E14; attacker/over-engineering S6c E16 | The facade crate crates/cobre (two doc lines, no [dependencies], keywords and categories with no publish = false): keep it as a documented reserved seam — adding the missing mirror row, listing it among CLAUDE.md's reserved stubs and noting the crates.io name reservation in ARCHITECTURE.md — or retire it from the workspace unti… | Keep as a reserved seam; E11 adds the row and the two listings |
| 33 | — | attacker/architecture S6c E09 | Under the Phase-0a Engine seam, does cobre.Study become generic over the engine (its setup field moving behind the enum), or stay the SDDP study with a dispatching constructor above it? The choice decides whether cobre.Study is a Python API break and where the typed admission gate for an unsupported engine-plus-option combinati… | Dispatching constructor above it; no Python API break (E9 default) |
| 34 | OD-043 | attacker/over-engineering S6a E11 | Should the CLI's Config wire mirror move beside crates/cobre-io/src/broadcast.rs (which owns the identical externally-tagged mirror for ScalarParameter), instead of OD-043's minimal in-crate relocation? CD-004's ratified single Config projection shares this mirror, so the two should not be designed twice. | Defer OD-043 to CD-004's projection work |
| 35 | TD-064 | attacker/over-engineering S6b E13 | May the four #[cfg(test)] pub fn format_*_string renderers in summary.rs be deleted in favour of private *_lines helpers (TD-064's fix)? The 61 inline tests that call them are repointed in the same change, sequencing the over-engineering and test-bloat halves together. | Yes, one change with TD-064 |
| 36 | TD-059 | attacker/test-bloat S6a E26 | Should the hand-pinned 1dtoy numbers (bindings inline tests, cli_e2e_run_end_block.rs) be retired in favour of the live CLI/Python parity derivation (TD-059), or kept deliberately as an offline regression guard for environments with no built wheel? | Retire them; the live derivation is the authority |
| 37 | TD-065 | attacker/test-bloat S6b E28 | The three copies of the cobre-cli valid-case fixture differ in forward passes, iteration limit, stage count and openings. Is the divergence deliberate per suite (the shared builder exposes named presets) or incidental (keyword knobs defaulting to the common case)? | Incidental; keyword knobs with defaults |

## 2. Round plan

Assembled before any question is asked. Options are capped at four per question; the recommended option is listed first; per-entry exceptions inside a batch are named through the free-text option. Rounds 0a and 0b precede every severity batch; no conflicts hold appears inside a batched question (each of the four variants is its own question); the Cleared, sanctioned and dup-of items (1.5) get no round; a parity-script or workflow decision is recorded with queue=build-ci.

| Round | Kind | Subject | Options |
| -- | -- | -- | -- |
| R0a-wave5 | prior-disposition | sharpen (4): CD-025, CD-029, CD-002, CD-009 — CD-025 destination cobre-io (L2) with the 'cobre-sddp or cobre-io' wording superseded; CD-029 fold into cobre-sddp PrepPhase (its cobre-io variant held, R4); CD-002 / CD-009 cobre-cli-local | ratify-as-presented · re-dispose (name) · defer (name + trigger) · reject-to-Cleared (name) |
| R0a-sev-CD-029 | prior-severity | CD-029 severity — B kept by the calibration (two hand-mirrored copies of the reconciler); C only if the duplicated engine logic is local cleanup | keep B · downgrade to C |
| R0a-sev-CD-002 | prior-severity | CD-002 severity — calibration proposes B → C (register B; the register's own 2026-08-19 tiering lists it as a local cleanup; S-effort named input struct, no wire change) | downgrade to C (proposed) · keep B |
| R0b-i5 | prior-disposition | I.5 (8 rows: I.5-1a retire, I.5-1b keep, I.5-2 keep, I.5-3 sharpen, I.5-4 sharpen, I.5-5 sharpen, I.5-6 sharpen, I.5-7 sharpen) + the I.3-7 BroadcastConfig fragment; two sharpen corrections ratified here (I.5-4 from_config five callers; the BroadcastConfig +… | ratify-as-presented · re-dispose (name) · defer (name + trigger) · reject-to-Cleared (name) |
| R1-sevB | severity-batch | Sev B (13): CD-089, CD-090, CD-091, CD-095, CD-096, PD-049, PD-050, PD-051, TD-057, TD-063, TD-064, TD-065, TD-066 | accept all · downgrade (name) · reject (name) · defer (name + trigger) |
| R2-sevC | severity-batch | Sev C (27): CD-092, CD-093, CD-094, CD-097, CD-098, OD-043, OD-044, OD-045, OD-046, OD-047, OD-048, PD-052, PD-053, PD-054, PD-055, TD-058, TD-059, TD-060, TD-061, TD-062, TD-067, TD-068, TD-069, TD-070, TD-071, TD-072, TD-073 | accept all · downgrade (name) · reject (name) · defer (name + trigger) |
| R4-hold-CD-025-cobre-sddp-home | conflicts-hold | CD-025 — cobre-sddp home (orchestration at L3) — Part IV.1 — L2 cobre-io owns shared output orchestration; an engine owning the front ends' output policy means the second engine (cobre-direct, V.1) would rout… | take-the-alternative · override-with-rationale · reject · defer |
| R4-hold-CD-025-cli-local-helper | conflicts-hold | CD-025 — cobre-cli-local helper (keeps the double-mirror) — Part IV.1 — L2 cobre-io owns 'shared output orchestration'; forbidden at L2: 'an engine-specific output tree hard-wired in the CLI instead of orchestrated here… | take-the-alternative · override-with-rationale · reject · defer |
| R4-hold-CD-029-cobre-io-home | conflicts-hold | CD-029 — cobre-io home for the boundary reconciliation — Part IV.1 — L2 forbids engine dispatch; the reconciliation is typed on cobre_sddp::StudySetup and calls cobre-sddp's policy loader, so an L2 home inverts the l… | take-the-alternative · override-with-rationale · reject · defer |
| R4-hold-CD-029-two-front-end-copies | conflicts-hold | CD-029 — keep two front-end copies (cli-local fold into run_boundary_check, Python keeps reconcile_boundary_policy) — Part IV.1 / III.7 — the hand-mirrored call-site list is the duplication mechanism the roadmap names for outputs; the same shape on the valida… | take-the-alternative · override-with-rationale · reject · defer |
| R5-nh-1 | needs-human | #1 CD-089 / S6a-architecture-00 (confirmed); attacker/architecture S6a E01 | recommended · alternative(s) · defer · other |
| R5-nh-2 | needs-human | #2 CD-025 / S6a-architecture-01 (confirmed); attacker/architecture S6a E02; S6a-architecture-02 (confirmed); S6a-architecture-05 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-3 | needs-human | #3 — / S6a-architecture-04 (dismissed); attacker/architecture S6a E03 | recommended · alternative(s) · defer · other |
| R5-nh-4 | needs-human | #4 CD-029 / S6b-architecture-00 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-5 | needs-human | #5 CD-091 / S6b-architecture-01 (confirmed); S6b-architecture-02 (confirmed); attacker/architecture S6b E06 | recommended · alternative(s) · defer · other |
| R5-nh-6 | needs-human | #6 CD-091 / S6b-architecture-02 (confirmed); attacker/architecture S6b E07 | recommended · alternative(s) · defer · other |
| R5-nh-7 | needs-human | #7 CD-029 / S6b-architecture-03 (confirmed); attacker/performance S6b E21 | recommended · alternative(s) · defer · other |
| R5-nh-8 | needs-human | #8 CD-093 / S6b-architecture-04 (confirmed); attacker/architecture S6b E05; attacker/over-engineering S6b E15; attacker/test-bloat S6b E27 | recommended · alternative(s) · defer · other |
| R5-nh-9 | needs-human | #9 — / S6c-architecture-00 (dismissed) | recommended · alternative(s) · defer · other |
| R5-nh-10 | needs-human | #10 — / S6c-architecture-06 (dismissed) | recommended · alternative(s) · defer · other |
| R5-nh-11 | needs-human | #11 — / S6a-performance-01 (dismissed); attacker/performance S6a E18 | recommended · alternative(s) · defer · other |
| R5-nh-12 | needs-human | #12 — / S6a-performance-05 (dismissed); S6a-performance-06 (dismissed); attacker/performance S6a E19 | recommended · alternative(s) · defer · other |
| R5-nh-13 | needs-human | #13 CD-025 / S6a-performance-07 (dismissed); attacker/performance S6a E20 | recommended · alternative(s) · defer · other |
| R5-nh-14 | needs-human | #14 PD-050 / S6c-performance-01 (confirmed); attacker/performance S6c E22 | recommended · alternative(s) · defer · other |
| R5-nh-15 | needs-human | #15 — / S6c-performance-03 (dismissed); attacker/performance S6c E23 | recommended · alternative(s) · defer · other |
| R5-nh-16 | needs-human | #16 PD-052 / S6c-performance-04 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-17 | needs-human | #17 OD-043 / S6a-over-engineering-00 (confirmed); attacker/over-engineering S6a E10 | recommended · alternative(s) · defer · other |
| R5-nh-18 | needs-human | #18 CD-025 / S6a-over-engineering-03 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-19 | needs-human | #19 OD-045 / S6a-over-engineering-04 (confirmed); S6b-over-engineering-02 (confirmed); S6b-over-engineering-04 (dismissed) | recommended · alternative(s) · defer · other |
| R5-nh-20 | needs-human | #20 CD-029 / S6b-over-engineering-00 (confirmed); attacker/over-engineering S6b E12 | recommended · alternative(s) · defer · other |
| R5-nh-21 | needs-human | #21 OD-047 / S6c-over-engineering-00 (confirmed); attacker/over-engineering S6c E17 | recommended · alternative(s) · defer · other |
| R5-nh-22 | needs-human | #22 TD-057 / S6a-test-bloat-00 (confirmed); S6a-test-bloat-01 (confirmed); attacker/test-bloat S6a E25 | recommended · alternative(s) · defer · other |
| R5-nh-23 | needs-human | #23 TD-057 / S6a-test-bloat-01 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-24 | needs-human | #24 TD-058 / S6a-test-bloat-02 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-25 | needs-human | #25 TD-060 / S6a-test-bloat-04 (confirmed); S6a-test-bloat-05 (confirmed); attacker/test-bloat S6a E24 | recommended · alternative(s) · defer · other |
| R5-nh-26 | needs-human | #26 TD-062 / S6a-test-bloat-06 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-27 | needs-human | #27 TD-067 / S6b-test-bloat-05 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-28 | needs-human | #28 TD-071 / S6c-test-bloat-00 (confirmed); attacker/test-bloat S6c E30 | recommended · alternative(s) · defer · other |
| R5-nh-29 | needs-human | #29 TD-072 / S6c-test-bloat-02 (confirmed) | recommended · alternative(s) · defer · other |
| R5-nh-30 | needs-human | #30 TD-073 / S6c-test-bloat-03 (confirmed); attacker/test-bloat S6c E29 | recommended · alternative(s) · defer · other |
| R5-nh-31 | needs-human | #31 — / S6c-test-bloat-04 (dismissed) | recommended · alternative(s) · defer · other |
| R5-nh-32 | needs-human | #32 — / attacker/architecture S6c E08; attacker/over-engineering S6b E14; attacker/over-engineering S6c E16 | recommended · alternative(s) · defer · other |
| R5-nh-33 | needs-human | #33 — / attacker/architecture S6c E09 | recommended · alternative(s) · defer · other |
| R5-nh-34 | needs-human | #34 OD-043 / attacker/over-engineering S6a E11 | recommended · alternative(s) · defer · other |
| R5-nh-35 | needs-human | #35 TD-064 / attacker/over-engineering S6b E13 | recommended · alternative(s) · defer · other |
| R5-nh-36 | needs-human | #36 TD-059 / attacker/test-bloat S6a E26 | recommended · alternative(s) · defer · other |
| R5-nh-37 | needs-human | #37 TD-065 / attacker/test-bloat S6b E28 | recommended · alternative(s) · defer · other |
| R6-handoffs | handoff-confirm | partI-handoff.json (8 I.5 rows + I.3-7 fragment) → E9 · perf-queue.json rows → E10 · td-queue.json rows → E08 · parity-script and workflow rows → build/CI/docs (E07) · doc corrections → E11 | confirm · amend (name) |
| R7-extras | record-shape | machine-readable record beyond gate.md: decisions.json + alignment-queue.json + the gate stamp on td-queue.json (the sddp gate's shape; undeclared on this ticket, so each is a justified extra) | all three (sddp shape) · decisions.json only · none — gate.md is the record |

**Invariants:** rounds 0a/0b precede every severity batch · no conflicts hold inside a batched question · batched questions carry at most four options · a retire row without a commit SHA is refused (none presented) · a defer needs a concrete trigger · an un-overridden hold stays held with no id promoted · a parity-gap row is presented only with its three enforcement layers · a rejected or C-downgraded PD row is struck from the perf queue · a downgrade keeps the reviewer's original rating beside the new one.

## 3. Decision record

**Decision: ratified** — 2026-09-18, main session, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`. Presented 53 (4 prior ids + 9 I.5 rows + 40 new entries) over 13 AskUserQuestion calls: round 0a (the four Wave-5 restatements, the CD-029 and CD-002 severity questions), round 0b (the I.5 rows), two severity batches, the four holds each on its own question, 37 needs-human questions four to a call, the handoff confirmation and the record-shape question. Prior ratified 4/4 (all sharpen; CD-002 downgraded B → C on the calibration's proposal, CD-029 kept at B); I.5 ratified 9/9; new accepted 39, amended 0, downgraded 0, rejected 0, deferred 1 (OD-043, trigger CD-004); holds closed by their alternatives 4, overridden 0; needs-human answered 37/37; perf rows struck 0, TD rows struck 0. Every answer was the recommended option. No fix executed, no measurement run, no figure asserted.

### 3.1 Wave-5 dispositions (round 0a)

| ID | Wave | Presented | Owner decision | Severity | Fix-shape owner | Rationale | Directions |
| -- | -- | -- | -- | -- | -- | -- | -- |
| CD-025 | 5 | sharpen | ratified (R0a-wave5) | B | cobre-io (L2) | All four stay sharpen with both claims recorded; CD-025's L2 destination and the superseded wording stand; CD-029's L3 fold is the record (its cobre-io variant is asked alone in R4). | R5-nh-2: cobre-sddp home for the folds; writes + guards to L2; R5-nh-13: Keep the serial shape; R5-nh-18: Wait for CD-025's shared emit list |
| CD-029 | 5 | sharpen | ratified (R0a-wave5) | B | cobre-sddp PrepPhase (L3) | All four stay sharpen with both claims recorded; CD-025's L2 destination and the superseded wording stand; CD-029's L3 fold is the record (its cobre-io variant is asked alone in R4).; R0a-sev-CD-029: Keep B (Recommended) — Duplicated engine logic across two f… | R5-nh-4: Fourth prep_phase_metadata row (engine-owned phase); R5-nh-7: Hold; CD-029's fold absorbs it; R5-nh-20: Yes: emit the object and add a --json reject test |
| CD-002 | 4 | sharpen | ratified (R0a-wave5) | C (reviewer: B) | cobre-cli-local | All four stay sharpen with both claims recorded; CD-025's L2 destination and the superseded wording stand; CD-029's L3 fold is the record (its cobre-io variant is asked alone in R4).; R0a-sev-CD-002: Downgrade to C (Recommended) — Recorded as C (reviewer: B);… | - |
| CD-009 | 7 | sharpen | ratified (R0a-wave5) | C | cobre-cli-local | All four stay sharpen with both claims recorded; CD-025's L2 destination and the superseded wording stand; CD-029's L3 fold is the record (its cobre-io variant is asked alone in R4). | - |

### 3.2 Part-I I.5 dispositions (round 0b)

| Row | Presented | Owner decision | Old → baseline | Command | Queue |
| -- | -- | -- | -- | -- | -- |
| I.5-1a | retire | ratified (R0b-i5) | "train an SDDP policy" (v0.12 snapshot) → clap doc string at main.rs:70: "Load a case directory, train a policy, and run simulation." — main.rs carries zero occurrences of 'sddp' in any case | `git show 077dbe2c:crates/cobre-cli/src/main.rs / grep -n -B1 'Run(RunArgs)'; git show 077dbe2c:crates/cobre-c…` | alignment (released at R6) |
| I.5-1b | keep | ratified (R0b-i5) | two-phase shape gated on training_enabled / n_scenarios → run/mod.rs:166 `if training_enabled {` and :224/227 `setup.simulation_config.n_scenarios > 0` | `git show 077dbe2c:crates/cobre-cli/src/commands/run/mod.rs / grep -n 'training_enabled\/n_scenarios'` | alignment (released at R6) |
| I.5-2 | keep | ratified (R0b-i5) | typed on StudySetup; inherent train/simulate → training.rs:11 `use cobre_sddp::StudySetup;`, :88 `setup.train(`; simulation.rs:21 `use cobre_sddp::StudySetup;`, :98 `.simulate(`; both are inherent… | `git show 077dbe2c:crates/cobre-cli/src/commands/run/training.rs / grep -n 'setup.train('; git show 077dbe2c:c…` | alignment (released at R6) |
| I.5-3 | sharpen | ratified (R0b-i5) | ~70 symbols (v0.12 estimate); ticket @a136840d: 95 refs / 11 files / 68 use lines → 97 `cobre_sddp::` occurrences over 10 files, 70 `use cobre_sddp` lines, 88 distinct imported symbols (leaf names across every `use cobre_sddp::…;`, r… | `git grep -o 'cobre_sddp::' 077dbe2c -- 'crates/cobre-cli/src/*.rs' 'crates/cobre-cli/src/**/*.rs' / wc -l; gi…` | alignment (released at R6) |
| I.5-4 | sharpen | ratified (R0b-i5) | the CLI calls StudyParams::from_config → the run path never calls StudyParams::from_config directly: setup.rs:112 `BroadcastConfig::from_config(&config)?` (inside load_case_and_config :78) →… | `git grep -n 'from_config' 077dbe2c -- 'crates/cobre-cli/src/*.rs' 'crates/cobre-cli/src/**/*.rs' / grep -v te…` | alignment (released at R6) |
| I.5-5 | sharpen | ratified (R0b-i5) | training/ + simulation/ + hydro_models/; a `summary` subcommand rendering LB/UB/gap → the tree holds: `training` (cobre-io training_writer.rs), `simulation` (outputs.rs:200, cobre-io simulation_writer.rs), `hydro_models` (outputs.rs:93… | `git show 077dbe2c:crates/cobre-cli/src/commands/run/outputs.rs / grep -n '"hydro_models"\/"generic_constraint…` | alignment (released at R6) |
| I.5-6 | sharpen | ratified (R0b-i5) | seven subcommands (init, run, validate, report, summary, schema, version); init: one temp… → five variants (main.rs:67: Init, Run, Validate, Schema, Version); schema and version stay near-generic; init scaffolds from embedded templates (1 nam… | `git show 077dbe2c:crates/cobre-cli/src/main.rs / grep -n -A 12 'enum Command'; git show 077dbe2c:crates/cobre…` | alignment (released at R6) |
| I.5-7 | sharpen | ratified (R0b-i5) | three SDDP-shaped subcommands (2026-07-23 addendum); the ticket's premise: commands/repor… → report and summary no longer exist (797ba443 2026-09-16 feat: remove the report and summary subcommands and their Python and cobre-io mirrors); valid… | `git ls-tree --name-only 077dbe2c -- crates/cobre-cli/src/commands; git show 077dbe2c:crates/cobre-cli/src/com…` | alignment (released at R6) |
| I.3-7 fragment | sharpen | ratified (R0b-i5) | BroadcastConfig as a hand-maintained SDDP-parameter subset (Part-I I.3-7) → crates/cobre-cli/src/commands/run/setup.rs:112 BroadcastConfig::from_config(&config)? (run path, inside load_case_and_config :78) → crates/cobre-cli/… | `crates/cobre-sddp/src/setup/mod.rs:378` | alignment (released at R6) |

### 3.3 New entries (rounds 1-2)

| ID | Round | Decision | Severity | Alignment | Rationale (owner) | Queue | Directions |
| -- | -- | -- | -- | -- | -- | -- | -- |
| CD-089 | R1 | accept | B | advances-0a · I.5 | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | alignment | R5-nh-1: Keep per-front-end presentation |
| CD-090 | R1 | accept | B | neutral · I.5 | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | alignment | - |
| CD-091 | R1 | accept | B | advances-0a · I.5 | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | alignment | R5-nh-5: Retire it; one cobre-io vocabulary, noted in the CHANGELOG; R5-nh-6: cobre-io (L2) owns the kind map |
| CD-092 | R2 | accept | C | advances-0a · I.5 | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | alignment | - |
| CD-093 | R2 | accept | C (reviewer: B) | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | R5-nh-8: No [lib]; make the seven examples honest at their fences |
| CD-094 | R2 | accept | C | neutral · I.5 | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | alignment | - |
| CD-095 | R1 | accept | B | advances-0a | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | alignment | - |
| CD-096 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | none | - |
| CD-097 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| CD-098 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| OD-043 | R2 | defer | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row…trigger: CD-004's single Config projection lands (sddp station, ratified 2026-09-18) and relocates the Config wire mirror once | none | R5-nh-17: Retire it (mirror :683-697 precedent); R5-nh-34: Defer OD-043 to CD-004's projection work |
| OD-044 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| OD-045 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | R5-nh-19: Correct the mirror prose down to D4 (E11) |
| OD-046 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| OD-047 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | R5-nh-21: Complete the three twins |
| OD-048 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| PD-049 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | perf | - |
| PD-050 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | perf | R5-nh-14: Supported bulk reader; PD-050 proceeds, doc cross-links Arrow |
| PD-051 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | perf | - |
| PD-052 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | R5-nh-16: Identity unspecified; the memo shape is acceptable |
| PD-053 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| PD-054 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| PD-055 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | none | - |
| TD-057 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | test-debt | R5-nh-22: Consolidate now under the standing rule; R5-nh-23: tests/common/ in cobre-cli (rule 14) |
| TD-058 | R2 | accept | C (reviewer: B) | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-24: Not standing policy; the shared fixture is sanctioned |
| TD-059 | R2 | accept | C (reviewer: B) | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-36: Retire them; the live derivation is the authority |
| TD-060 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-25: It freezes the comparisons, not the files |
| TD-061 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | - |
| TD-062 | R2 | accept | C | neutral · I.5 | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt, alignment | R5-nh-26: Front-end assertion covering both call sites |
| TD-063 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | test-debt | - |
| TD-064 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | test-debt | R5-nh-35: Yes, one change with TD-064 |
| TD-065 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | test-debt | R5-nh-37: Incidental; keyword knobs with defaults |
| TD-066 | R1 | accept | B | neutral | All 13 stay live at Sev B with the calibrated fix-shapes; the three PD rows stay queued UNMEASURED 4t; the TD rows go t… | test-debt | - |
| TD-067 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-27: E08's ratification call; default inline |
| TD-068 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | - |
| TD-069 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | - |
| TD-070 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | - |
| TD-071 | R2 | accept | C (reviewer: B) | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-28: Both in one change |
| TD-072 | R2 | accept | C | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-29: Normalize onto __file__-resolved paths |
| TD-073 | R2 | accept | C (reviewer: B) | neutral | All 27 stay live at Sev C as calibrated (the five reviewer-B rows keep their downgrade recorded); the four Sev-C PD row… | test-debt | R5-nh-30: Confirm: pytest anchors stay first-class (already resolving) |

### 3.4 Holds (round 4, each asked alone)

- **CD-025 — cobre-sddp home (orchestration at L3)** (R4-hold-CD-025-cobre-sddp-home) — **take-the-alternative** — Take the cobre-io alternative (Recommended) — The hold closes: no L3 home for output orchestration; CD-025's L2 entry point is the direction; no id promoted for the variant. Condition: Part IV.1 — L2 cobre-io owns shared output orchestration; an engine owning the front ends' output policy means the second engine (cobre-direct, V.1) would route its outputs through a sibling engine —… No id promoted.
- **CD-025 — cobre-cli-local helper (keeps the double-mirror)** (R4-hold-CD-025-cli-local-helper) — **take-the-alternative** — Take the cobre-io alternative (Recommended) — The hold closes: no cli-local orchestration helper; the hand-mirror is removed by the L2 entry point; no id promoted. Condition: Part IV.1 — L2 cobre-io owns 'shared output orchestration'; forbidden at L2: 'an engine-specific output tree hard-wired in the CLI instead of orchestrated here'; a helper in cobre-cli is unreachable… No id promoted.
- **CD-029 — cobre-io home for the boundary reconciliation** (R4-hold-CD-029-cobre-io-home) — **take-the-alternative** — Take the PrepPhase alternative (Recommended) — Confirms cobre-sddp PrepPhase (L3) as the fold destination; the cobre-io variant stays closed; the ticket's 'cobre-io owner for both' premise is recorded as superseded. Condition: Part IV.1 — L2 forbids engine dispatch; the reconciliation is typed on cobre_sddp::StudySetup and calls cobre-sddp's policy loader, so an L2 home inverts the layering (cobre-io depending on an engine… No id promoted.
- **CD-029 — keep two front-end copies (cli-local fold into run_boundary_check, Python keeps reconcile_boundary_policy)** (R4-hold-CD-029-two-front-end-copies) — **take-the-alternative** — Take the PrepPhase alternative (Recommended) — The hold closes: the two front-end copies collapse into the engine-owned phase; no cli-local fold; no id promoted. Condition: Part IV.1 / III.7 — the hand-mirrored call-site list is the duplication mechanism the roadmap names for outputs; the same shape on the validation path keeps two owners of engine logic in L4 No id promoted.

### 3.5 Worker needs-human answers (round 5)

| # | Entry | From | Question | Answer | Recorded as |
| -- | -- | -- | -- | -- | -- |
| 1 | CD-089 | S6a-architecture-00 (confirmed); attacker/architecture S6a E01 | Under the single phase-plan owner (CD-089), do the no-op arm's two behaviours converge — the CLI writes 'Training disabled, simulation disabled' to stderr, the… | **Keep per-front-end presentation** | the shared owner returns the phase-plan value only; each front end keeps its own no-op rendering (stderr line / zeroed RunSummary); CD-089's surviving claim stays as calibrated; no API change |
| 2 | CD-025 | S6a-architecture-01 (confirmed); attacker/architecture S6a E02; S6a-architecture-02 (confirmed); S6a-architecture-05 (c… | Where do the cobre-sddp-typed folds live once the writes move to cobre-io (CD-025)? delta_to_stats_row / SolverStatsDelta (outputs.rs:29, :35), the five traini… | **cobre-sddp home for the folds; writes + guards to L2** | the engine-typed folds (delta_to_stats_row / SolverStatsDelta, the five row folds, the CD-090 totals fold) live in cobre-sddp beside their input types; CD-025's L2 entry point owns only the writes and their guards; CD-090's cobre-sddp home is confirmed outsid… |
| 3 | — | S6a-architecture-04 (dismissed); attacker/architecture S6a E03 | StudySetup::from_broadcast_params (cobre-sddp's sole construction core, entered by both front ends) carries a transport-flavoured name. File a rename against t… | **Leave it to the setup-layer redesign (CD-004)** | no rename item; the constructor is renamed when CD-004's single Config projection re-shapes the setup entry; informational for E9 |
| 4 | CD-029 | S6b-architecture-00 (confirmed) | Which vocabulary supplies the boundary error object's user-visible `phase` string — the binding's literal BoundaryReconciliationError (cobre-python io.rs:305-3… | **Fourth prep_phase_metadata row (engine-owned phase)** | the folded PrepPhase fourth phase supplies the kind/phase string through prep_phase_metadata; the binding literal is retired when the fold lands; both front ends read the same vocabulary |
| 5 | CD-091 | S6b-architecture-01 (confirmed); S6b-architecture-02 (confirmed); attacker/architecture S6b E06 | May the CLI's --json kind/phase string CaseValidationError (validate.rs:90, :361) be retired in favour of the cobre-io/Python vocabulary (ConstraintError etc.)… | **Retire it; one cobre-io vocabulary, noted in the CHANGELOG** | CaseValidationError is retired when CD-091's shared kind map lands; the change is called out in the CHANGELOG as a --json contract change; no alias kept |
| 6 | CD-091 | S6b-architecture-02 (confirmed); attacker/architecture S6b E07 | Where does the shared LoadError → stable-kind classification live? cobre-io (L2) owns LoadError and both front ends depend on it, but the station's L2 destinat… | **cobre-io (L2) owns the kind map** | the L2 destination rule generalizes to shared front-end contract data owned by the type's crate: the LoadError → kind map lives in cobre-io beside LoadError; CD-091's fix shape stands |
| 7 | CD-029 | S6b-architecture-03 (confirmed); attacker/performance S6b E21 | Sequencing for the duplicate boundary-requirements resolve in validate.rs: patch it now as a cobre-cli-local parameter thread, or hold it and let CD-029's fold… | **Hold; CD-029's fold absorbs it** | no standalone patch; the call chain is touched once when the boundary check folds into PrepPhase and resolves the requirements as a phase input |
| 8 | CD-093 | S6b-architecture-04 (confirmed); attacker/architecture S6b E05; attacker/over-engineering S6b E15; attacker/test-bloat… | Should cobre-cli gain a [lib] target (plus a cargo test --doc step) so its seven doc examples compile, or should the examples stop presenting themselves as imp… | **No [lib]; make the seven examples honest at their fences** | CD-093's fix as filed: the three cobre:: imports are rewritten to the module-local form and the four cobre_cli:: examples are de-annotated or de-fenced; no published library surface |
| 9 | — | S6c-architecture-00 (dismissed) | Coverage residue behind the dismissed S6c-architecture-00: study.rs:583-585 / :694-696 admit that whole-tree byte-identity is unasserted for the cobre.Study me… | **Covered in substance; no new item** | the single execution path (run_via_study) is the shared owner; no coverage item; informational note for E08 |
| 10 | — | S6c-architecture-06 (dismissed) | Should the terminal fallback at cobre-python policy.rs:510 spell the exported cobre_sddp::LEGACY_COST_SCALE_FACTOR (no new dependency), and should write_policy… | **Yes to both, as an E11 reconciliation nit** | the literal is replaced by the exported constant and the doc gains the both-markers-absent clause; byte-neutral; no id, routed to E11 |
| 11 | — | S6a-performance-01 (dismissed); attacker/performance S6a E18 | Rooted gather at L0: closing the non-root replication in the two simulation gathers needs a gatherv the cobre-comm Communicator trait does not expose (traits.r… | **Keep allgatherv; cost accepted** | no new L0 collective for two call sites in one CLI file; the dismissal stands; informational for the solver+comm owner |
| 12 | — | S6a-performance-05 (dismissed); S6a-performance-06 (dismissed); attacker/performance S6a E19 | Is the shared-filesystem re-read on non-root ranks (case directory, hydro-model refit, and the per-rank policy-checkpoint read in simulation-only mode) a ratif… | **Ratify it; E11 records the assumption** | shared-filesystem visibility on every rank is a ratified deployment assumption; the E11 mirror write-back adds the docs/design entry and the module docs point at it; the per-rank reads stay |
| 13 | CD-025 | S6a-performance-07 (dismissed); attacker/performance S6a E20 | Overlapping the rank-0 training-artefact write with rank 0 entering simulation trades the documented 'training outputs persist even if simulation fails' guaran… | **Keep the serial shape** | the guarantee and the stderr ordering stand; the dismissal stands; no 2x2 perf row |
| 14 | PD-050 | S6c-performance-01 (confirmed); attacker/performance S6c E22 | Is the dict-shaped cobre.results.load_simulation a supported bulk reader, or is load_simulation_arrow the intended path for large result trees? load_simulation… | **Supported bulk reader; PD-050 proceeds, doc cross-links Arrow** | load_simulation stays a supported reader; PD-050's per-column typed-view hoist stays queued; its rustdoc gains a pointer to load_simulation_arrow for large trees |
| 15 | — | S6c-performance-03 (dismissed); attacker/performance S6c E23 | Is object identity between result['training']['manifest'] and result['training']['metadata'] part of the published cobre.results contract? Binding one converte… | **Identity unspecified; dismissal stands** | identity is not part of the contract and nothing changes: the two conversions stay; no id |
| 16 | PD-052 | S6c-performance-04 (confirmed) | Is per-read element identity part of the published cobre.model contract? Today system.buses[0] is system.buses[0] is False (every read builds new objects); PD-… | **Identity unspecified; the memo shape is acceptable** | element identity across reads is not part of the contract; PD-052's memo (or index-into-shared-Arc) shape is accepted as calibrated; the module doc states identity is unspecified |
| 17 | OD-043 | S6a-over-engineering-00 (confirmed); attacker/over-engineering S6a E10 | BroadcastNodeGraph (commands/broadcast.rs:256): keep as a documented reserved seam or retire? Its only references workspace-wide are its two From impls and ~11… | **Retire it (mirror :683-697 precedent)** | BroadcastNodeGraph and its round-trip tests are deleted under the no-dead-code directive and recorded as removed so no audit resurrects it; OD-043's relocation drops those lines |
| 18 | CD-025 | S6a-over-engineering-03 (confirmed) | Sequencing for the two uncovered emit conditions (evaporation_models, fpha_deviation_points — no enforcement layer asserts them): close them now with CLI-vs-Py… | **Wait for CD-025's shared emit list** | no interim pytest additions; the shared cobre-io call-site list removes the duplicated condition and the parity golden gates it there |
| 19 | OD-045 | S6a-over-engineering-04 (confirmed); S6b-over-engineering-02 (confirmed); S6b-over-engineering-04 (dismissed) | The mirror's allow census (docs/design/reserved-seams-and-deferred-debt.md:1272, :1324-1330) asserts every Load-bearing numeric-cast and needless_pass_by_value… | **Correct the mirror prose down to D4 (E11)** | the same ruling as the sddp gate (NH29): the census clause states the comments.md D4 lint scope; the cast and needless_pass_by_value sites are not a rule gap; OD-045 / OD-046 stand as the duplicated-cast and by-value residues only |
| 20 | CD-029 | S6b-over-engineering-00 (confirmed); attacker/over-engineering S6b E12 | Should `cobre validate --json` emit an error object when boundary reconciliation (the fourth phase the module doc lists) fails? Today stdout is empty and stder… | **Yes: emit the object and add a --json reject test** | the boundary reject emits the documented single error object (phase string per the vocabulary answer above) and a new reject test in --json mode pins it; lands with CD-029's fold |
| 21 | OD-047 | S6c-over-engineering-00 (confirmed); attacker/over-engineering S6c E17 | The three cobre.model stub twins (EnergyContract, PumpingStation, NonControllableSource) expose only id and name. Complete them to the house pattern the other… | **Complete the three twins** | OD-047 is a completion ticket: each twin exposes the field set its core entity carries, matching PyBus / PyLine / PyThermal / PyHydro; no mirror row needed once complete |
| 22 | TD-057 | S6a-test-bloat-00 (confirmed); S6a-test-bloat-01 (confirmed); attacker/test-bloat S6a E25 | Is the cobre-cli run-path binary consolidation (tests/common/ + folding the single-#[test] binaries, TD-057) sanctioned now under the standing Cost-discipline… | **Consolidate now under the standing rule** | TD-057 lands now: tests/common/ plus the fold of the single-#[test] binaries under the Cost-discipline contract; the Layer-1 migration later moves the consolidated shape once |
| 23 | TD-057 | S6a-test-bloat-01 (confirmed) | Helper home for the cobre-cli integration corpus: rule 14 names tests/common/ while testing-architecture §6 phase 3 proposes collapsing tests/common/ into cobr… | **tests/common/ in cobre-cli (rule 14)** | the process-spawning CLI helpers (fn cobre(), write_file, the valid-case builder) live in crates/cobre-cli/tests/common/; they are not engine test-support |
| 24 | TD-058 | S6a-test-bloat-02 (confirmed) | Is the per-test-isolation cost acceptance recorded in the two pytest module docstrings (test_study.py:12-14, test_run.py:9-11) standing policy for the pytest c… | **Not standing policy; the shared fixture is sanctioned** | TD-058 stands: a module-scoped trained-policy fixture in conftest.py serves the five pure argument-validation tests; the two docstrings are rewritten |
| 25 | TD-060 | S6a-test-bloat-04 (confirmed); S6a-test-bloat-05 (confirmed); attacker/test-bloat S6a E24 | Does testing-architecture §5.11's 'keep the pytest output-parity suite unchanged' freeze the eleven parity modules against fixture hygiene (harness hoisting, r… | **It freezes the comparisons, not the files** | §5.11 exempts the parity suite from the binary migration only; assertion-preserving fixture hygiene is in scope; TD-060 / TD-061 / TD-062 stand as calibrated |
| 26 | TD-062 | S6a-test-bloat-06 (confirmed) | The one unique stats-row assertion (the simulation shape) in run/mod.rs's misplaced test module: rehome it next to cobre-sddp's existing solver_stats_log_to_ro… | **Front-end assertion covering both call sites** | the assertion becomes a CLI-vs-Python parity assertion over the two delta_to_stats_row call sites; TD-062 is a move plus one new assertion |
| 27 | TD-067 | S6b-test-bloat-05 (confirmed) | The progress inline test module (22 fns, 608 lines) exceeds the unratified ~500 test-LOC inline homing threshold of testing-architecture §5.1 while staying und… | **E08's ratification call; default inline** | TD-067 collapses the fifteen spawn tests inline; whether the module is extracted follows E08's §5.1 threshold ratification (routed, not decided here) |
| 28 | TD-071 | S6c-test-bloat-00 (confirmed); attacker/test-bloat S6c E30 | convert.rs:104-106 and test_convert.py:5-9 assert that a #[cfg(test)] module cannot link an interpreter, contradicted by errors.rs and the feature-off CI invoc… | **Both in one change** | the two false rationales are corrected and convert.rs gains its own cfg(test) module on the errors.rs pattern; the seven pytest tests stay |
| 29 | TD-072 | S6c-test-bloat-02 (confirmed) | Case-path convention for the pytest corpus: pyproject.toml:52 sets testpaths = ["tests"], which makes a bare pytest run from the crate directory a configured i… | **Normalize onto __file__-resolved paths** | TD-072 as filed: conftest.py owns the repo-root constant and the case-path helpers; the nine modules stop deriving parents[3] or cwd |
| 30 | TD-073 | S6c-test-bloat-03 (confirmed); attacker/test-bloat S6c E29 | Anchor vocabulary for pytest files: five of TD-073's six anchors are pytest def names. tools/check-anchors.py grew a def/class branch for .py paths at the E06-… | **Confirm: pytest anchors stay first-class (already resolving)** | the E06-3 extension is the ruling; check-anchors.py resolves def / class names for .py paths; no further action |
| 31 | — | S6c-test-bloat-04 (dismissed) | Confirm the E11 write-back's scope covers every design-doc trace of the superseded CI-invisibility premise: reserved-seams-and-deferred-debt.md:347, testing-ar… | **Yes: E11 corrects every trace** | the mirror :347 entry, testing-architecture.md :17 / :101-102 / §5.11 are all corrected to the measured state (ci.yml runs the crate's Rust tests; doctests still absent); no id |
| 32 | — | attacker/architecture S6c E08; attacker/over-engineering S6b E14; attacker/over-engineering S6c E16 | The facade crate crates/cobre (two doc lines, no [dependencies], keywords and categories with no publish = false): keep it as a documented reserved seam — addi… | **Keep as a reserved seam; E11 adds the row and the two listings** | the facade stays (crates.io name reservation, ARCHITECTURE.md:102-106); E11 adds the reserved-seam mirror row, lists cobre among CLAUDE.md's reserved stubs and notes the name reservation; the two `use cobre::` doc examples are rewritten under CD-093 |
| 33 | — | attacker/architecture S6c E09 | Under the Phase-0a Engine seam, does cobre.Study become generic over the engine (its setup field moving behind the enum), or stay the SDDP study with a dispatc… | **Dispatching constructor above it; no Python API break (E9 default)** | cobre.Study stays the SDDP study; an engine-dispatching constructor sits above it and owns the admission gate; recorded as the default the alignment epic designs against |
| 34 | OD-043 | attacker/over-engineering S6a E11 | Should the CLI's Config wire mirror move beside crates/cobre-io/src/broadcast.rs (which owns the identical externally-tagged mirror for ScalarParameter), inste… | **Defer OD-043 to CD-004's projection work** | the in-crate move is skipped; OD-043 is deferred with trigger CD-004's single Config projection, which relocates the mirror once |
| 35 | TD-064 | attacker/over-engineering S6b E13 | May the four #[cfg(test)] pub fn format_*_string renderers in summary.rs be deleted in favour of private *_lines helpers (TD-064's fix)? The 61 inline tests th… | **Yes, one change with TD-064** | the four cfg(test) twins are deleted, the print_* renderers build through private *_lines helpers and the 61 tests are repointed in one change |
| 36 | TD-059 | attacker/test-bloat S6a E26 | Should the hand-pinned 1dtoy numbers (bindings inline tests, cli_e2e_run_end_block.rs) be retired in favour of the live CLI/Python parity derivation (TD-059),… | **Retire them; the live derivation is the authority** | TD-059 as filed: the pytest parity suite owns the 1dtoy agreement; the inline bindings test keeps only what it alone can assert |
| 37 | TD-065 | attacker/test-bloat S6b E28 | The three copies of the cobre-cli valid-case fixture differ in forward passes, iteration limit, stage count and openings. Is the divergence deliberate per suit… | **Incidental; keyword knobs with defaults** | TD-065's builder exposes the four knobs with defaults and each call site keeps today's values |

### 3.6 Presented, no decision taken

- 20 defender dismissals (Cleared with basis and citation) — no question offered.
- 7 dup-of folds onto minted or prior ids — no question offered.
- the sanctioned-seam filters (BroadcastConfig projection content, the #[allow] census classes, `Unwired config is reserved, not dead`) — no question offered; the two sanctioned Cleared rows' owner questions were asked as NH #31 and #32.

### 3.7 Informational (no severity, no id)

- R5-nh-3: Leave it to the setup-layer redesign (CD-004) — no rename item; the constructor is renamed when CD-004's single Config projection re-shapes the setup entry; informational for E9
- R5-nh-9: Covered in substance; no new item — the single execution path (run_via_study) is the shared owner; no coverage item; informational note for E08
- R5-nh-10: Yes to both, as an E11 reconciliation nit — the literal is replaced by the exported constant and the doc gains the both-markers-absent clause; byte-neutral; no id, routed to E11
- R5-nh-11: Keep allgatherv; cost accepted — no new L0 collective for two call sites in one CLI file; the dismissal stands; informational for the solver+comm owner
- R5-nh-12: Ratify it; E11 records the assumption — shared-filesystem visibility on every rank is a ratified deployment assumption; the E11 mirror write-back adds the docs/design entry and the module docs point at it; the per-rank reads stay
- R5-nh-15: Identity unspecified; dismissal stands — identity is not part of the contract and nothing changes: the two conversions stay; no id
- R5-nh-31: Yes: E11 corrects every trace — the mirror :347 entry, testing-architecture.md :17 / :101-102 / §5.11 are all corrected to the measured state (ci.yml runs the crate's Rust tests; doctests still absent); no id
- R5-nh-32: Keep as a reserved seam; E11 adds the row and the two listings — the facade stays (crates.io name reservation, ARCHITECTURE.md:102-106); E11 adds the reserved-seam mirror row, lists cobre among CLAUDE.md's reserved stubs and notes the name reservation; the two `use cobre::` doc examples are rewritten under CD-093
- R5-nh-33: Dispatching constructor above it; no Python API break (E9 default) — cobre.Study stays the SDDP study; an engine-dispatching constructor sits above it and owns the admission gate; recorded as the default the alignment epic designs against

## 4. Handoffs after the gate

- **E9** — alignment-queue.json written: 7 new rows (CD-089 advances-0a, CD-090 neutral, CD-091 advances-0a, CD-092 advances-0a, CD-094 neutral, CD-095 advances-0a, TD-062 neutral) plus the reused CD-025 / CD-029 (advances-0a) and the I.3-7 BroadcastConfig fragment; the four holds CLOSED by their alternatives (no id promoted); 9 I.5 rows released (R6-handoffs); NH1 (per-front-end no-op rendering), NH2 (cobre-sddp folds, L2 writes), NH6 (cobre-io owns the LoadError kind map), NH33 (dispatching constructor, no Python API break) and NH3 (from_broadcast_params rename rides CD-004) carried as E9 inputs.
- **E10** — perf-queue.json stamped: 3 rows kept (PD-049, PD-050, PD-051), all UNMEASURED `4t`, none struck; directions: PD-050 stays a supported bulk reader (NH14, its rustdoc cross-links load_simulation_arrow); PD-052 memo shape acceptable, element identity unspecified (NH16, section-only); the training-write overlap stays out of the sweep (NH13) and the rooted gather stays out of L0 (NH11).
- **E08** — td-queue.json stamped: 17 rows kept, none struck; directions: TD-057 consolidate now, tests/common/ in cobre-cli (NH22, NH23); TD-058 shared fixture sanctioned (NH24); TD-060/061/062 in scope — §5.11 freezes the comparisons, not the files (NH25); TD-062 front-end assertion over both delta_to_stats_row sites (NH26); TD-067 inline by default, extraction is E08's §5.1 call (NH27); TD-071 both rationale and cfg(test) module (NH28); TD-072 __file__-resolved case paths (NH29); TD-073 pytest anchors first-class (NH30); TD-064 one change with the format_*_string deletion (NH35); TD-059 live derivation is the authority (NH36); TD-065 knobs with defaults (NH37); the cobre.Study whole-tree coverage is covered in substance (NH9).
- **E07** — the parity-script and workflow surface (scripts/ci/check_python_parity.py, ci.yml's python job) is owned there; no cobre-cli [lib] target and no doc-test step (NH8); the CI-invisibility premise is superseded at the pin (SN-06) — corrections routed with E11.
- **E11** — mirror write-back: the facade crate's reserved-seam row plus the CLAUDE.md reserved-stub listing and the ARCHITECTURE.md crates.io name-reservation note (NH32); the shared-filesystem deployment assumption entry (NH12); every CI-invisibility trace (mirror :347, testing-architecture.md :17 / :101-102 / §5.11) corrected to the measured state (NH31); the allow-census clause corrected down to comments.md D4 (NH19); the CD-025 'cobre-sddp or cobre-io' wording supersession; the policy.rs:510 LEGACY_COST_SCALE_FACTOR spelling and the both-markers-absent doc clause (NH10); BroadcastNodeGraph recorded as removed (NH17) once OD-043's deferred relocation or an earlier dead-code removal lands.
- **sddp station** — the from_broadcast_params rename rides CD-004's setup-layer redesign (NH3); CD-025's engine-typed folds home in cobre-sddp beside their input types (NH2).

**Gate: RETURNED 2026-09-18** — baseline `077dbe2c`; accepted 39, amended 0, downgraded 0, rejected 0, deferred 1, overridden 0; 4 prior ids ratified (0 retire, 0 keep, 4 sharpen; CD-002 severity B → C); 9 I.5 rows ratified and released to E9; 4 holds closed by their alternatives; 37 needs-human answered; perf rows struck 0, TD rows struck 0.
