# Owner gate — cobre-sddp (2026-09, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`)

Run in the main session as AskUserQuestion rounds over this digest. Inputs: `wave-dispositions.json` (22 owned prior ids), `calibration.json` (44 calibrated entries, 16 cleared, 10 dup-of, 1 hold), `verdicts.json` (worker needs-human items), `partI-handoff.json`, `measurements/lp-inventory.json`, `perf-queue.json`, `td-queue.json`, `verification.md` (shared verifier 7/7 + station verifier 5/5 + module 72/72 at the pin), the sixteen `candidates.<lens>.<sub>.json` envelopes (`_needsHuman`). Severity is the HOUSE rating; a downgraded row shows `house (reviewer: original)`. The gate executes no fix, runs no measurement and asserts no figure.

## 1. Presentation

### 1.1 Prior Wave 4/6/7 dispositions — asked FIRST (round 0, 22 ids in three groups)

Every id is a reused prior id (never re-minted). Retire rows carry the resolving commit; sharpen rows carry the superseded and the surviving claim; the four Wave-4 rows restated in Phase-0a terms show their restatement artifact. CD-024-successor is presented here as a sharpen row AND held in 1.3 — its `conflicts` question is asked alone in R4, never in a batch.

| # | ID | Wave | Group | Anchor | Closing commit | Superseded → surviving | Phase-0a restatement | Sev (house / register) | Alignment |
| -- | -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-001 | 4 | retire-with-commit | `crates/cobre-sddp/src/setup/stochastic_pipeline.rs::build_stochastic_context_for_study` | b051c410 | - | - | A / A | neutral |
| 2 | CD-003-construction-hop | 4 | retire-with-commit | `crates/cobre-sddp/src/setup/params.rs::from_config` | 4075c4e8 | - | rank-0-executes MPI shape | B / B | advances-0a |
| 3 | CD-006 | 4 | retire-with-commit | `crates/cobre-sddp/src/setup/node_graph.rs::frontier_node` | 3f4c3db3 | - | - | C / C | neutral |
| 4 | CD-018 | 6 | keep | `crates/cobre-sddp/src/cut/cut_selection.rs::CutSelectionStrategy` | - | `CutSelectionStrategy` conflates two selection paradigms — periodic value-based (Level1/Lml1/Dominated share `select_for_stage`) and lazy-solve DCS (… | - | C / C | neutral |
| 5 | CD-034 | 6 | keep | `crates/cobre-sddp/src/lp/builder/entries.rs::fill_operational_violation_entries` | - | The min/max outflow entry blocks in `fill_operational_violation_entries` are two hand-mirrored `for blk` loops while the sibling rows file emits the… | - | B / B | advances-0b |
| 6 | CD-035 | 6 | keep | `crates/cobre-sddp/src/training/backward/outcome_aggregation.rs::write_opening_outcome` | - | `write_opening_outcome`/`accumulate_opening_outcome` take a bare `&CutStateProjection` where two role-distinct projections are live (`child_cut_layou… | - | B / B | neutral |
| 7 | CD-012 | 7 | keep | `crates/cobre-sddp/src/training/session/mod.rs::run_cut_management` | - | `run_cut_management` (~250 lines, `too_many_lines`-allowed) inlines cut-selection + budget enforcement; its rationale defends against FREE-fn extract… | - | C / C | neutral |
| 8 | CD-014-remnant | 7 | keep | `crates/cobre-sddp/src/training/backward_pass_state.rs::compute_one_backward_node` | - | `compute_one_backward_node` (~290 lines, biggest fn in the crate) — the god-fn residue that remains after the shared successor-reification extraction… | - | C / C | neutral |
| 9 | CD-016 | 7 | keep | `crates/cobre-sddp/src/training/backward_pass_state.rs::process_stage_backward` | - | Both backward scheduler workers duplicate ~25 lines of per-worker `backward_accum` buffer pre-allocation (`process_stage_backward` vs `process_stage_… | - | C / C | neutral |
| 10 | CD-028 | 7 | keep | `crates/cobre-sddp/src/training/forward/enumerated.rs::run_enumerated_forward` | - | Enumerated forward and enumerated simulation reimplement the mid-level claim/scatter orchestration skeleton independently (`run_sweep`/`enumerated_si… | - | B / B | neutral |
| 11 | CD-030 | 7 | keep | `crates/cobre-sddp/src/simulation/enumerated.rs::mark_own_paths` | - | `mark_own_paths` (simulation/enumerated.rs) reimplements the ~10-line path-marking loop that is inlined in `training/forward/enumerated.rs`; folds wi… | - | C / C | neutral |
| 12 | CD-037 | 7 | keep | `crates/cobre-sddp/src/setup/mod.rs::admission_gate` | - | The gap-rule rejecters each re-evaluate `rule_is_gap` + `training_enumerated`, and which rejection message a sampled-forwards study sees depends on c… | - | C / C | neutral |
| 13 | CD-038 | 7 | keep | `crates/cobre-sddp/src/lp/indexer/cut_state_projection.rs::dot_trial_state` | - | `CutStateProjection` exposes only the fused `dot_trial_state`; the cut-selection value sweep in `run_cut_management` open-codes the gather-only loop… | - | C / C | neutral |
| 14 | OD-009 | 7 | keep | `crates/cobre-sddp/src/training/session/runtime.rs::RuntimeHandles` | - | Production items ship as `#[allow(dead_code)]` 'for symmetry, exercised only by tests' (`RuntimeHandles.export_states`, `RankDistribution.my_rank`, `… | - | C / C | neutral |
| 15 | CD-004 | 4 | sharpen | `crates/cobre-sddp/src/setup/params.rs::from_config` | - | three ~20-field near-isomorphic structs (StudyParams / ConstructionConfig / BroadcastConfig) and the params →… → the wire projection is untouched: Config → BroadcastConfig (crates/cobre-cli/src/commands/broadcast.rs, from_config at :143) and Config → StudyParams… | study-block admission-gate carrier | B (A-risk) / B (A-risk) | advances-0a |
| 16 | CD-005 | 4 | sharpen | `crates/cobre-sddp/src/setup/mod.rs::StudySetup` | - | a local struct split: 'half-finished NCS sub-struct extraction' plus a field-count census (35 fields, 2026-08… → StudySetup conflates precomputed model state (stage_data, stochastic, fcf, hydro_models, scenario_libraries, node_graph, the ncs_* patch vectors) wit… | engine-tagged setup stages | B / B | advances-0a |
| 17 | CD-024-successor | 4 | sharpen | `crates/cobre-sddp/src/policy/resolved_parameters.rs::build_resolved_parameters` | - | the resolved-parameters broadcast pair CD-024 registered (serialize_/deserialize_resolved_parameters, RESOLVE… → ResolvedParameters is still built per rank (policy/resolved_parameters.rs build_resolved_parameters; setup/mod.rs 'Per-rank, never broadcast'); the s… | study-block admission-gate carrier | C / C | conflicts |
| 18 | CD-015 | 6 | sharpen | `crates/cobre-sddp/src/training/backward_pass_state.rs::compute_one_backward_node` | - | extract a standalone commit_by_scenario_cuts to mirror by_node_finish (a symmetry fix on its own) → compute_one_backward_node (backward_pass_state.rs :1727) still inlines by_scenario's post-dispatch aggregation while by_node's is extracted (training… | - | B / B | neutral |
| 19 | CD-022 | 6 | sharpen | `crates/cobre-sddp/src/training/backward_pass_state.rs::run_enumerated_backward` | - | a ~66-line duplicated span framed as its own dedup → the successor-outcome reification (successors loop → build_delta_cut_row_batch_into in training/forward/delta_cut_batch.rs → SuccessorEntry → Success… | - | B / B | neutral |
| 20 | CD-023 | 6 | sharpen | `crates/cobre-sddp/src/training/stage_solve_prep.rs::StageSolvePrep` | - | 're-home StageSolvePrep out of training/' as an unstarted move → the carve-out is half done: the neutral solve/ module exists (mod.rs, partition.rs, solver_phase.rs, stage_solve.rs) and owns the solve ENTRY (run_st… | - | C / C | neutral |
| 21 | CD-007 | 7 | sharpen | `crates/cobre-sddp/src/lp/builder/entries.rs::fill_load_balance_entries` | - | sizes as recorded 2026-08-22: entries.rs 10,079 and columns.rs 9,026 lines (the register already says GREW) → re-measured at the pin: entries.rs 10,093 lines (first #[cfg(test)] at L1680) and columns.rs 9,319 (L1305) — inline test modules dominate both while… | - | C / C | advances-0b |
| 22 | CD-021 | 7 | sharpen | `crates/cobre-sddp/src/workspace/workspace.rs::SolverWorkspace` | - | 'a flat workspace.rs mega-file asymmetric with the directory-module convention' — the organizational half is… → all nine structs (CapturedBasis, WorkspaceSizing, BackwardAccumulators, ByNodeScratch, ScratchBuffers, SolverWorkspace, WorkspacePool, BasisStore, Ba… | - | C / C | neutral |

**PD-004 (existence-and-queue only, no fix option):** exists at `crates/cobre-sddp/src/training/backward_pass_state.rs::run_enumerated_backward`; do-not-touch: DEFERRED pending a profile (register L1316; do-not-touch list L1942); queued to perf-sweep (E10-2 measures PD-004 on the enumerated deck; UNMEASURED until then). No disposition verb applies; no round offers a fix.

### 1.2 New entries by house severity (rounds 1-3)

#### Sev B (A-risk) — 2 entries

| # | ID | Sev | Alignment | Part-I | Claim (narrowed) | Fix-shape (one line) | Queue | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-084 | B (A-risk) (reviewer: A) | neutral | - | Narrowed to a LATENT single-site contract violation with a named precondition: fill_anticipated_columns (columns.rs:618) is the only one of four ring-residue owners that… | Give the anticipated ring ONE residue owner in the fpha_cursor.rs style: hoist the delivery-axis to ring-axis conversion (point.ring_index(delivery_s… | - | #38 |
| 2 | CD-086 | B (A-risk) (⬆ from B) | neutral | - | Under `Traversal::Enumerated`, `run_enumerated_backward` neither folds a `slot_increments` contribution nor calls `sync_stage_metadata`, so `CutPool::record_binding` nev… | Two roads, and the pick is the owner's because it turns on whether enumerated-plus-DCS is a supported combination. Road (a), make the exclusion expli… | - | #19, #20 |

#### Sev B — 19 entries

| # | ID | Sev | Alignment | Part-I | Claim (narrowed) | Fix-shape (one line) | Queue | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-082 | B | advances-0a | I.3-7 | Narrowed to a missed error class, not a corrupted report: because StudySetup::new and new_with_boundary_requirements take no artifact argument, they cannot satisfy the p… | Make the table a constructor input instead of a post-construction patch, so the empty case is unrepresentable rather than merely undesirable. Either… | ['alignment'] | #2 |
| 2 | CD-085 | B | advances-0b | - | Only the two commitment-hold reads in simulation/extraction.rs (:268 incoming, :316 outgoing) are genuine untyped LP-column recompositions: the same file already resolve… | Add the missing typed resolvers next to their siblings in state_space.rs - a commitment_hold_incoming_col returning InCol and a commitment_hold_outgo… | ['alignment'] | #11 |
| 3 | OD-040 | B | neutral | - | Only 4 of the 42 bare openers survive, and not as a comment gap: at the four `#[allow(clippy::cast_sign_loss)]` sites in `resolve_fitting_bounds` (`geometry.rs:157-164`)… | Sharpen, do not sweep. For each of the 42 bare openers either narrow the attribute to the expression it guards or add the single clause that records… | ['reconciliation (re-file the cobre-io… | #29, #30 |
| 4 | PD-036 | B | neutral | - | At `NodeGraph::backward_cut_levels` and its two per-iteration callers only (`run_sampled_backward`, `run_enumerated_backward`), the graph-invariant level partition is re… | Resolve the level decomposition once where the graph is constructed, in `build_node_graph`, and store it as a `NodeGraph` field the drivers borrow, s… | ['performance-sweep'] | #6 |
| 5 | PD-038 | B | neutral | - | Narrowed to one loop: `fill_col_state_patches` (patch.rs:257-260) re-runs `classify`'s four-way region membership test and region select once per state element on every… | Give the incoming direction the cache the outgoing direction already has: an incoming twin of state_to_lp_column_map on StateSpace, filled by an inco… | ['performance-sweep'] | - |
| 6 | PD-039 | B | neutral | - | For a Generated child run carrying more than one opening, the per-opening backward prep redundantly re-fills and re-submits the opening-invariant column-bound state pin… | Hoist the opening-invariant work to per-child: fill and submit the column-bound state pin (fill_col_state_patches plus its set_col_bounds) and fill t… | ['performance-sweep'] | #15 |
| 7 | PD-040 | B | neutral | - | Only two of the sweep's allocations are removable by the mechanism as filed: `m_block_starts` (cut_selection.rs:359) materialises a stride range that the following .par_… | Move the four per-call vectors and the two per-fold-task buffers onto the per-worker scratch the crate already owns for exactly this purpose, reusing… | ['performance-sweep'] | #22 |
| 8 | PD-041 | B | neutral | - | Only the absent contiguous-copy path at the single gather in `training/session/mod.rs` L1112-1121 survives, on the ground that the archive is flat and packed at one glob… | Give the gather a guarded fast path: when the projection is the identity, extend scratch from the contiguous trial row of global_states in one extend… | ['performance-sweep'] | - |
| 9 | PD-042 | B | neutral | - | Only the MEMORY half survives, and only on the sampled level-driver path when the archive exists (cut selection enabled or export_states) and a declared nodes[] graph pu… | Store the level's gathered block once and give every node in that level a view of it, so states_for_node returns the shared block instead of a per-no… | ['performance-sweep'] | #23 |
| 10 | PD-044 | B | neutral | - | On the default ByScenario branch, and only for a worker owning at least one routed trial point, process_stage_backward pays one payload-proportional heap allocation plus… | Adopt the sibling scheduler's return shape: have process_stage_backward return per-worker counts and leave each staged cut in the producing worker's… | ['performance-sweep'] | - |
| 11 | PD-045 | B | neutral | - | Narrowed to the `row_scale` non-empty branch (every production template after `postprocess_templates`): the removable per-(scenario, stage) work in `build_row_lower_unsc… | Compute the unscaled row_lower once per stage and, on each call, copy the cached vector into the scratch buffer and overwrite only the load-balance s… | ['performance-sweep'] | #32 |
| 12 | PD-047 | B | neutral | - | The avoidable copy in re_expand is one deep clone per owned distinct arena node, not one per visiting leaf path: dispatch_scenario_result takes the stage-result Vec by v… | Take the scratch as &mut and move each node's captured result out on its last visiting path, cloning only for the earlier visitors; under the canonic… | ['performance-sweep'] | - |
| 13 | TD-042 | B | neutral | - | cobre-sddp's fixtures never adopt the spread base cobre-core already exports to them: 56 zero-argument `-> HydroPenalties` helpers under 11 names re-implement `HydroPena… | Promote the neutral penalties constructor from tests/common/builders.rs to a pub fn on crates/cobre-sddp/src/test_support.rs under the module's exist… | ['test-corpus'] | #7 |
| 14 | TD-043 | B | neutral | - | penalties() is md5-identical across all four right_boundary_* binaries and bounds() is md5-identical in three with validation's a strict one-parameter superset, and pric… | Extend tests/common/builders.rs with the two resolved-layer constructors it is missing, a neutral ResolvedPenalties and a ResolvedBounds taking the o… | ['test-corpus'] | - |
| 15 | TD-044 | B | neutral | - | Only the byte-identical fixture classes duplicate: in anticipated_core.rs the six default_hydro_penalties bodies (one md5), the 4-plus-2 default_hydro_bounds and 4-plus-… | Collapse each family to one declaration at file scope in the binary that owns it, hoisting the byte-identical default_hydro_penalties, default_hydro_… | ['test-corpus'] | #8 |
| 16 | TD-045 | B | advances-1 | I.3-8 | For state_layout_for the surviving residue is narrower than the title in two ways: the seven bodies are identical only after dedent and are semantically the same StateSp… | Delete the seven state_layout_for clones and the four study_dims clones and call cobre_sddp::test_support::state_layout and cobre_sddp::test_support:… | ['test-corpus', 'alignment'] | #9 |
| 17 | TD-049 | B | neutral | - | Narrowed to two verified copy-paste families and a corrected cost basis: the anticipated-thermal trio (one_anticipated_thermal_system, two_thermal_one_anticipated_system… | Collapse each family into one parameterized builder in the parent module and keep the existing names as thin wrappers so the eleven submodules that r… | ['test-corpus'] | #18 |
| 18 | TD-053 | B | neutral | - | Narrowed to three bodies and no destination: session/mod.rs:1654 StubComm, training/training/tests.rs:173 StubComm and session/mod.rs:2767 Rank0Of2 are token-identical r… | Move one strict single-rank Communicator double and one rank-0-of-2 double into crates/cobre-sddp/src/test_support.rs behind the cfg(any(test, featur… | ['test-corpus'] | #25 |
| 19 | TD-054 | B | neutral | - | Only the inert subset of the eleven required methods is genuine duplication across the eight sites — the ActiveProfile associated type, the empty apply_profile, the cons… | Put one configurable solver double in crates/cobre-sddp/src/test_support.rs behind the cfg(any(test, feature = 'test-support')) gate it already carri… | ['test-corpus'] | #26 |

#### Sev C — 23 entries

| # | ID | Sev | Alignment | Part-I | Claim (narrowed) | Fix-shape (one line) | Queue | Needs-human |
| -- | -- | -- | -- | -- | -- | -- | -- | -- |
| 1 | CD-083 | C | neutral | - | `CutManagementConfig::warm_start_cuts` (config.rs:148) is the struct's only field with no production reader -- its four siblings are read at training/backward_pass_state… | Delete CutManagementConfig::warm_start_cuts and let cut-pool capacity keep its single owner, the per-pool warm_start_count argument of cut/fcf.rs::po… | - | #3 |
| 2 | CD-087 | C (reviewer: B) | advances-0b | - | Narrowed to the internal-import half of the alias block at lib.rs:56-99: the crate-root aliases give each cluster submodule a second, cluster-blind in-crate path, and in… | Separate the two roles the alias layer currently conflates. Keep the curated public re-export surface in lib.rs, which is what downstream and the pub… | ['alignment'] | #27 |
| 3 | CD-088 | C | advances-0a | - | Narrowed to a documentation-and-organization misfit: production/mod.rs:3-6 states a cluster purpose that does not cover production/conversion.rs, whose sole source domai… | Re-home the file beside the types it converts, as a simulation-owned write-payload projection module, so the production cluster doc states one purpos… | ['alignment'] | #28 |
| 4 | OD-036 | C | neutral | - | The defect is confined to public-surface and naming hygiene on one read-only accessor: `StudySetup::ncs_stochastic_dormant_for_test` (setup/accessors.rs:218) is the work… | Either move the method under the same '#[cfg(any(test, feature = "test-support"))]' as its four siblings and declare test-support as a required featu… | - | - |
| 5 | OD-037 | C (reviewer: B) | advances-0b | - | Among the types declared in lp/indexer/index.rs, only Col and Row have no non-test, non-doc production consumer at the pin — their sole references are the mod.rs:119 re-… | Delete the Col and Row declarations and their two impl blocks from lp/indexer/index.rs, delete the three tests that exist only to exercise them (col_… | ['alignment'] | #12 |
| 6 | OD-038 | C (reviewer: B) | advances-0b | - | FphaRowRange (lp/indexer/layout.rs:34-40) has no construction or read site anywhere under crates/ outside its own cfg(test) Debug/Copy smoke test, yet is public API thro… | Delete the FphaRowRange declaration and the fpha_row_range_debug_clone_copy smoke test from lp/indexer/layout.rs, drop the name from the pub use in l… | ['alignment'] | #13, #14 |
| 7 | OD-039 | C | neutral | - | The method must not exist while `actual_per_rank` accepts `total_forward_passes` as a parameter (rank_distribution.rs:60) that shadows the `num_total_forward_passes` the… | Delete the method and let session/mod.rs:245 call cobre_comm::per_rank_counts directly, matching the two sibling sites that already do. If a named ow… | - | #41 |
| 8 | OD-041 | C | neutral | - | Narrowed to the tailrace trio only, and to a marker-level cleanup rather than a defect: QuarticSegment (tailrace.rs:60), TailraceSegments (:86) and TailraceFamily (:189)… | Narrow each of the five to the visibility its single defining file actually needs, which is private for QuarticSegment, TailraceSegments, TailraceFam… | - | #31 |
| 9 | OD-042 | C | neutral | - | The removable residue is exactly the one-item impl block at simulation/state.rs:87-118 — `SimulationInputs::new`, its `#[allow(clippy::too_many_arguments)]` and its RATI… | Delete `SimulationInputs::new` and have `simulate` build the value with the struct literal the sibling tests at simulation/pipeline/tests.rs already… | - | - |
| 10 | PD-035 | C | neutral | - | Only the symmetric-matrix half of the title survives as a bit-exact free win: `l2_distance_matrix` (noise_key.rs:223-243) evaluates every off-diagonal pair's L2 twice an… | Two halves with different risk, and they should be queued separately. The byte-exact half: fill the distance matrix symmetrically by writing the mirr… | - | #5 |
| 11 | PD-037 | C (reviewer: B) | neutral | - | Only the two enumerated per-stage driver loops (training/forward/enumerated.rs:536, simulation/enumerated.rs:302) pay a whole-node-array pass per stage, and only on a de… | Give `NodeGraph` a per-stage membership index built once in `build_node_graph`: an offsets array plus a positions array, the same compressed shape th… | - | - |
| 12 | PD-043 | C | neutral | - | Narrowed to the training path: `build_slot_lookup`'s unconditional clear is a FIXED-length pass over the run-lifetime pool capacity (`warm_start_count + max_iterations *… | Replace the sentinel clear with a generation stamp: store a solve epoch alongside each position and treat an entry whose epoch is stale as absent, so… | - | #42 |
| 13 | PD-046 | C (reviewer: B) | neutral | - | Narrowed to a reservation asymmetry with a logarithmic residue: the three per-block branches (extract_hydros' turbine branch, extract_exchanges, extract_buses) and, only… | Build each of the four vectors with Vec::with_capacity over the same product its pre-reserving sibling already reserves and extend from the flat_map,… | - | - |
| 14 | PD-048 | C (reviewer: B) | neutral | - | Narrowed to a one-time study-setup cost whose SIZE, not its once-per-plant recurrence, is the defect: each `long_term_mean_inflow` call is sized by the whole shared infl… | Accumulate per-hydro sums and counts in one pass over inflow_history() before the parallel map, into a container indexed by the hydro's canonical dec… | - | #34 |
| 15 | TD-041 | C (reviewer: B) | neutral | - | Two of the three named helpers are a genuine triplication, not all three: the hydro(id, downstream_id, travel_time_hours) constructor and the 16-field all-zero HydroPena… | Hoist one transit-seed fixture trio onto the existing shared surface: add a pub neutral-penalties constructor and a pub travel-time hydro constructor… | ['test-corpus'] | - |
| 16 | TD-046 | C | neutral | - | Within `crates/cobre-sddp/src/stochastic/noise.rs` the `#[cfg(test)] pub(crate) fn shift_lag_state` (176-197) is the file's only test-only item declared in its 560-line… | Move the oracle inside the `mod tests` block that already holds its only six call sites, dropping the cfg(test) attribute and the pub(crate) visibili… | ['test-corpus'] | #10 |
| 17 | TD-047 | C | neutral | - | Scoped to crates/cobre-sddp/src/lp/builder/entries.rs alone: its inline module pumping_water_tests (3518-10093) is named for the least-exercised subject it contains and… | When candidate two converts entries.rs to a directory module, do not carry pumping_water_tests across as one file. Split it along the subject boundar… | ['test-corpus'] | #16 |
| 18 | TD-048 | C (reviewer: B) | neutral | - | build_classical_fixture (par_a_lag12_lp_coefficient.rs:347-580, 234 lines) repeats 229 lines of build_par_a_fixture verbatim to vary only the annual field at its two Inf… | Collapse the pair into one private builder taking the annual component as a parameter, for example an `Option<AnnualComponent>` argument, with the tw… | ['test-corpus'] | #17 |
| 19 | TD-050 | C | neutral | - | Only the single-file half survives, and it is the base-path construction rather than a shared corpus helper: tests/lp_builder.rs repeats Path::new(env!("CARGO_MANIFEST_D… | Export one case-directory helper from tests/common/mod.rs next to the case helpers that already take that path as an argument, and have lp_builder.rs… | ['test-corpus'] | - |
| 20 | TD-051 | C (reviewer: B) | neutral | - | Narrowed to one deletion and stripped of its two supporting claims: at the pin sync_cuts_rejects_mismatched_local_cut_count is the weakest of THREE copies of the count-m… | Delete the test_mpi_allgatherv_nonuniform_workers.rs binary and keep the surviving sync_cuts count-mismatch assertion in test_mpi_sync_cuts_invariant… | ['test-corpus'] | #24 |
| 21 | TD-052 | C (reviewer: B) | neutral | - | Only the three constant-valued helpers duplicate without justification: d01_case_dir at 41, 917 and 1580, d03_case_dir at 908 and 1884, and ascending_stage_end_dates at… | Declare each of the five helpers once at cut_basis.rs file scope, above the nine inline modules that consume them, since all copies already share one… | ['test-corpus'] | #43 |
| 22 | TD-055 | C (reviewer: B) | neutral | - | Narrowed from the whole file to two of its six tests. lead_ge2_rebaseline_contains_the_named_k2_k3_fixtures (hold_k1_byte_stability_probe.rs:217-229) requires three stri… | Keep the three DeliveryRing primitive tests, which are the only part of this binary that touches production code. Delete k1_byte_stability_verdict, w… | ['test-corpus'] | #35 |
| 23 | TD-056 | C | neutral | - | Confined to tests/simulation_pipeline_integration.rs:78 - the local double reuses the harness's StubComm identifier for a type whose collective semantics are incompatibl… | Replace the three shapes with one parameterized stub communicator in the shared harness that takes rank and size and carries an explicit mode for the… | ['test-corpus'] | #36, #37 |

### 1.3 Holds (Alignment `conflicts`) — each asked ALONE (round 4)

| ID | Kind | Condition | Roadmap-consistent alternative | Status |
| -- | -- | -- | -- | -- |
| CD-024-successor | reused prior id (Wave-4 sharpen) | one-consumer seam kept alive against a single hypothetical consumer ('pull, don't push', Part V §V.0) | die: keep the per-rank derivation; its inputs ride the single Config projection of CD-004, so the seam is subsumed rather than activated — no OD/CD id promoted for the broadcast variant | HELD for owner override at the E05-8 gate |

### 1.4 Re-baseline flags — each asked ALONE (round 5)

New entries carrying `byteNeutral: needs-rebaseline`: 0 (calibration byteNeutral census: {'asserted': 30, 'n/a': 14}). Reused rows whose byte-neutrality is a gate decision:

| ID | Disposition | Byte-neutrality note | Bar it must meet |
| -- | -- | -- | -- |
| CD-005 | sharpen | re-baseline decision at the owner gate — the NCS split feeds hot-path StageContext borrows and the patch-identity contract | parity goldens (`parity_hash_highs` / `parity_hash_clp`) + rank-invariance harness (tests/common/permute.rs) + `mpiexec -n 1/2` reproduction |

### 1.5 Cleared and dup-of — presented read-only, no decision taken

**Dismissed by a defender (16), each with the citation that decides it:**

- **5b-architecture-01** — contract *Delivered commitments reconcile against solver drift; exactness is unreachable* (.claude/rules/sddp.md) — The delta this candidate adds over CD-074 is that the commitment family's two exactness mechanisms are mutually unaware and their coupling unstated; both halves are false at the pin. The reconciler's module doc (crates/…
- **5b-architecture-03** — contract *State pinning uses column bounds, not equality rows* (.claude/rules/sddp.md) — The factual half holds (nothing in columns.rs writes state.transit_buckets_in) but the reading does not, on three counts. (1) The asymmetry is a stated rule, not an unexplained omission. The buffer default is (0.0, +INF…
- **5b-architecture-04** — argument — The candidate's load-bearing premise fails at the pin: in a release build the gate family has two members, not one. anticipated_gate.rs:123-142 declares anticipated_resolution_for with NO cfg gate, re-exported for produ…
- **5c-architecture-03** — contract *The cut intercept dots the trial state through the projection, never positionally* (.claude/rules/sddp.md) — The named fix target does not do what the candidate says it does. solve_replicated_outcome_slice (replicated.rs:83-166) never touches a BackwardOutcome: at :137-138 and :160-161 it pushes the raw objective followed by s…
- **5b-performance-02** — argument — The mechanism is real but it is setup-time only, and the allocation policy on this exact path is deliberate and documented in present tense. Frequency: resolve_variable_ref has one production caller, fill_generic_constr…
- **5c-performance-01** — argument — The mechanism as filed ('redundant strided pass', 'full-panel stride') does not hold at the pin, on five grounds read from the code. (1) Nothing is redundant and the interchange removes no load. The per-column nest at c…
- **5c-performance-05** — argument — The mechanism rests on the premise that the reconstruction already knew the BASIC counts; at the pin it does not. reconstruct_col_statuses (basis_reconstruct.rs:209-211) is clear + extend_from_slice + resize, and recons…
- **5d-performance-03** — argument — Setup scope is verified at the pin, and it decides this candidate. The only non-test entry into the fitting pipeline is `fit_fpha_planes` (production/fpha_fitting/mod.rs:137), reached through `fit_planes_for_hydro` (pro…
- **5d-performance-04** — argument — The mechanism the candidate names is really present: at secant.rs:74 every call to representative_operating_point re-allocates both axis Vecs through build_grid, and at secant.rs:80-88 the min-over-planes fold and pf.ev…
- **5d-performance-06** — argument — The linear membership scan is real as read, but it sits entirely in study setup and its operand set is bounded to a handful of keys, so the named mechanism has no measurable region to act on. Reachability: fit_hull_plan…
- **5d-performance-07** — argument — This is a study-setup path, not a solve path. The only production call sites of prepare_hydro_models_from_artifacts are crates/cobre-cli/src/commands/run/setup.rs:129 and crates/cobre-python/src/run.rs:981, plus the two…
- **5a-over-engineering-00** — contract *Backward opening order is warm-start-only* (.claude/rules/sddp.md) — Dismissed on the code plus the project's own pinned scope for rationale-on-suppression. comments.md directive D4 (.claude/rules/comments.md:365-371) mandates a rationale for a CLOSED list of refactor-decision lints (too…
- **5a-over-engineering-02** — argument — The filed fix does not compile, and that is why the literals exist. accessors.rs:233 and :274 declare stage_ctx(&self) -> StageContext and training_ctx(&self) -> TrainingContext with elided output lifetimes, so each ret…
- **5a-over-engineering-03** — argument — Both load-bearing premises fail against the code and the pinned rules at the baseline. (1) The impl-less-public-module shape is the documented layout, not an accident: .claude/architecture-rules.md section 'StudySetup S…
- **5b-test-bloat-05** — argument — The shadow is real but nothing about it is silent, which is the load-bearing word in the title. The parent declaration at tests/template_integration.rs:251 takes two usize parameters (n_stages, lag_order); the local dec…
- **5b-test-bloat-06** — argument — Dismissed on four independent grounds, all read at the pin. (1) The count is wrong: crate::test_support exposes no Stage constructor at all. geometry_stage is a private fn at crates/cobre-sddp/src/test_support.rs:409 wi…

**Dup-of a prior id (10, folded, no new number):** 5c-over-engineering-00 → OD-009 (restates); 5a-test-bloat-00 → CD-007 (restates); 5b-test-bloat-00 → CD-007 (restates); 5b-test-bloat-01 → CD-007 (restates); 5c-test-bloat-04 → CD-007 (restates); 5c-test-bloat-05 → 5a-test-bloat-02 (intra-station); 5d-test-bloat-00 → 5a-test-bloat-05 (intra-station); 5d-test-bloat-02 → 5a-test-bloat-03 (intra-station); 5d-test-bloat-03 → 5a-test-bloat-04 (intra-station); 5d-test-bloat-05 → CD-007 (restates)

**Reserved-seam filters (ratified seams, never a live finding):** the section's Cleared list carries the five sanctioned-by-construction filters — `LipschitzConfig.mode` / `UpperBoundEvaluationConfig`, the per-family boundary coupling channels, the Legacy (`None`) cost-scale branch, the `#[allow(...)]` census Reserved-seam class, and the superseded cut-sync public methods — each cited against `docs/design/reserved-seams-and-deferred-debt.md`. No question is offered: a round would invite a re-raise.

### 1.6 Queues handed on (counts only; no figure)

- **perf-queue.json → E10:** 9 claim rows, all UNMEASURED: PD-036 (`4t`, requires none), PD-038 (`4t`, requires none), PD-039 (`4t`, requires none), PD-040 (`4t`, requires none), PD-041 (`4t`, requires none), PD-042 (`4t`, requires none), PD-044 (`4t`, requires none), PD-045 (`4t`, requires none), PD-047 (`4t`, requires enumerated); plus the PD-004 existence row (`2t` enumerated deck marker, fixShape null). A rejected or C-downgraded PD row is struck from the queue by this gate.
- **td-queue.json → E08:** 16 TD rows: TD-041, TD-042, TD-043, TD-044, TD-045, TD-046, TD-047, TD-048, TD-049, TD-050, TD-051, TD-052, TD-053, TD-054, TD-055, TD-056; 5 CD-007 homing candidates folded.
- **alignment → E9:** 13 non-neutral rows: CD-082 (advances-0a · I.3-7), TD-045 (advances-1 · I.3-8), CD-085 (advances-0b), OD-037 (advances-0b), OD-038 (advances-0b), CD-087 (advances-0b), CD-088 (advances-0a), CD-003-construction-hop (advances-0a), CD-004 (advances-0a), CD-005 (advances-0a), CD-024-successor (conflicts), CD-034 (advances-0b), CD-007 (advances-0b). Written to `alignment-queue.json` after the gate.

### 1.7 Handoffs the alignment epic consumes (round 7 confirmation)

- **measurements/lp-inventory.json** — 30 lp/ modules at `077dbe2c`, reconciled by verify-sddp.py (30/30 both directions, top_symbols non-empty, non_test_lines <= total_lines == wc -l); the E9 0b-carve input.
- **Part-I item I.3-7** — disposition sharpen, proposed phase 0a, alignment {'value': 'advances-0a', 'citation': 'Part V.1 Phase 0 (study/config schema + admission gate + per-engine solver-profile scoping); Part IV.4'}; anchors `crates/cobre-sddp/src/setup/params.rs::StudyParams`, `crates/cobre-sddp/src/setup/params.rs::from_config`, `crates/cobre-io/src/config/mod.rs::Config`, `crates/cobre-cli/src/commands/broadcast.rs::BroadcastConfig`, `crates/cobre-sddp/src/solve/solver_phase.rs::Phase`; cross-ref CD-004 in stations/sddp/wave-dispositions.json (study-block admission-gate carrier).
- **Part-I item I.3-8** — reaches this station through TD-045 (state_layout_for clones); owned by the solver+comm station.

### 1.8 Worker needs-human items (43; all must be answered before the marker)

Defender items come from `verdicts.json` `_needsHuman` (37 on 33 candidates); the sixteen attacker envelopes carry 18 envelope-level items, 12 of which restate a defender question and are merged onto it (shown as *also asked by*), 6 are attacker-only and get their own question.

| # | Entry | Candidate | From | Question |
| -- | -- | -- | -- | -- |
| 1 | CD-005 | 5a-architecture-00 (defended) | defender | Owner call: `set_budget` is enumerated in the declared accessor surface at `.claude/architecture-rules.md:117`, so the disposition of a caller-free ungated mutator is either remove it and edit that rules line, or keep it as a deliberate public-library knob and route it through the same validation `StudyParams::from_config` appl… |
| 2 | CD-082 | 5a-architecture-02 (defended) | defender | Whether the fail-loud leg changes the public surface of ResolvedParameters::get (a fallible signature or a panic) or is instead enforced as a construction-time check at the LP-build site, leaving get infallible: get is pub and must_use, so the choice is an owner call on public API shape, not a correctness question. |
| 3 | CD-083 | 5a-architecture-03 (defended) | defender | Owner must choose between removing `CutManagementConfig::warm_start_cuts` (a breaking change to a re-exported public type) and giving it a real reader plus a reserved-seams entry; the verdict establishes the field is inert and its doc line false, not which of the two lands. |
| 4 | — | 5a-over-engineering-00 (dismissed) | defender | Owner call (prose, not code): reword the allow-census Load-bearing class at docs/design/reserved-seams-and-deferred-debt.md:1282-1288 and the scripts/README.md:36 one-liner so neither reads as an absolute every-site rationale claim, since D4 and the E4 gate never require one of a cast_* allow. *(also asked by attacker/over-engineering 5a)* |
| 5 | PD-035 | 5a-performance-00 (defended) | defender | Owner call: whether the goldens-moving delta reformulation of the 2-opt accept test is worth a parity re-baseline at all, given that the confirmed free win is only the symmetric matrix fill; if not, the perf sweep should carry the symmetric-fill item alone. *(also asked by attacker/performance 5a)* |
| 6 | PD-036 | 5a-performance-01 (defended) | defender | Placement is an owner call the architecture rules do not disambiguate: a derived study-invariant consumed only by the backward drivers fits decision-tree rule 2 (a `NodeGraph` field beside `n_pools`/`pool_stage`, the candidate's shape) and rule 5 (a `BackwardPassState` scratch field) equally; the training owner should also deci… *(also asked by attacker/performance 5a)* |
| 7 | TD-042 | 5a-test-bloat-02 (defended) | defender | Whether the corpus should keep at least one literal that still spells all sixteen penalty fields as a canary: spreading `..HydroPenalties::uniform(v)` everywhere means a newly added penalty field silently defaults in every fixture instead of breaking the build, and while `uniform`'s rustdoc endorses the spread and builders.rs p… *(also asked by attacker/test-bloat 5c)* |
| 8 | TD-044 | 5a-test-bloat-04 (defended) | defender | Scope call for the owner: the default_hydro_penalties body is one md5 across anticipated_core.rs and anticipated_scenarios.rs, and scenarios build_config at 2954 equals core build_config at 3831, so the owner must decide whether the hoist stays per-binary at file scope or lifts the shared class into tests/common (the test-corpu… |
| 9 | TD-045 | 5a-test-bloat-05 (defended) | defender | Owner call outside this candidate: invariance-shuffle.yml builds cargo nextest -p cobre-sddp --test parity without the test-support feature, while parity.rs reaches cobre_sddp::test_support ungated at :894 and :1303, so either that workflow cannot build at the pin or the clones never protected a working default-feature configur… |
| 10 | TD-046 | 5a-test-bloat-06 (defended) | defender | Scope call for this item: whether relocating the oracle should also re-word the three production doc comments that use `shift_lag_state` as the canonical lag-remap vocabulary (the bracketed reference at `noise.rs:211` plus the code spans at `lp/indexer/state_space.rs:443` and `lp/indexer/cut_state_projection.rs:269`), or leave… |
| 11 | CD-085 | 5b-architecture-02 (defended) | defender | Alignment-epic owner call: whether conflicts trigger 4 (one-consumer abstraction) reaches a purpose-named single-consumer StateSpace accessor that completes an existing five-member family, or is bounded to seams, traits and crates as its text states. |
| 12 | OD-037 | 5b-over-engineering-00 (defended) | defender | Scheduling only: whether the public-API removal of Col and Row ships as its own semver-major break or waits for the 0b cobre-model carve-out of lp/, as the CD-019 deferral precedent suggests — the defect itself is settled. *(also asked by attacker/over-engineering 5b)* |
| 13 | OD-038 | 5b-over-engineering-01 (defended) | defender | Owner call carried over from the attacker: deleting FphaRowRange is a public-API removal (reachable as cobre_sddp::indexer::FphaRowRange via lib.rs:69) with no in-repo consumer; decide whether it lands now as ordinary cleanup or is batched with the Phase 0b cobre-model carve-out that relocates lp/indexer anyway, per the CD-019… |
| 14 | OD-038 | 5b-over-engineering-01 (defended) | defender | Scope call: the fix as filed leaves the same per-plant framing live in BlockGrid::advance_fpha_base's rustdoc (block_grid.rs:106-107), so the reader hazard is only half retired; decide whether that sibling doc correction joins this item or gets its own. |
| 15 | PD-039 | 5b-performance-01 (defended) | defender | Owner call: whether removing a bit-identical repeat submission of already-in-force column bounds counts as a warm-start-chain change that must clear the parity goldens, the rank-invariance harness and the opening_order_determinism gate before the hoist lands, given that the CLP backend's set_row_bounds rustdoc asserts factoriza… *(also asked by attacker/performance 5b)* |
| 16 | TD-047 | 5b-test-bloat-02 (defended) | defender | Target shape for the split is a convention call CD-007 owns: one extracted tests.rs matching commitment_reconcile/layout/template, or N subject-named files matching columns.rs's 12 inline modules. The unratified section 5.1 threshold cannot settle it. |
| 17 | TD-048 | 5b-test-bloat-03 (defended) | defender | Whether this binary's classical arm should be parameterized in place or instead folded onto the existing two_hydro_par_system helper when template_integration and par_a_lag12_lp_coefficient are grouped under the testing-architecture section 5.1 layout - a test-corpus station call, since that homing layout is still a proposal at… |
| 18 | TD-049 | 5b-test-bloat-04 (defended) | defender | Scope call: whether the ticket is the family collapse as filed or the higher-leverage extension of the O(1) Spec layer to InflowModel, LoadModel and the four bounds/penalties sizing structs, which owns the 20-copy fan-out the collapse leaves at fifteen. |
| 19 | CD-086 | 5c-architecture-00 (defended) | defender | Carried over from the attacker and still decisive: is enumerated traversal combined with dynamic cut selection a SUPPORTED configuration? Road (a) rejects the pairing beside the existing enumerated preconditions in `setup/mod.rs`; road (b) wires the binding contribution plus a per-stage metadata reduction into the enumerated dr… *(also asked by attacker/architecture 5c)* |
| 20 | CD-086 | 5c-architecture-00 (defended) | defender | Scope and severity call: `CutPool::enforce_budget`'s `(last_active_iter, active_count)` eviction key is a second production reader that degrades the same way under enumerated traversal, and unlike the DCS seed it changes which cuts are deactivated and therefore the bound. The owner must decide whether that reader belongs inside… |
| 21 | CD-028 | 5c-architecture-04 (defended) | defender | Owner call: whether the confirmed residue should produce any code change at all, or only the module-doc consumer-list correction -- the identical fragment is a four-line composition of stdlib collect plus an integer sum, and hoisting it behind a generic-over-E helper trades two transparent ? sites for one opaque one. |
| 22 | PD-040 | 5c-performance-00 (defended) | defender | Owner call: whether the inaccurate rustdoc sentence at training/session/mod.rs:1026-1027 ('no heap allocation when the cut pools have not grown') gets its own doc-accuracy item — it is false because of run_cut_management's own per_stage / deactivations / record_by_pool allocations, independent of this candidate's mechanism clai… |
| 23 | PD-042 | 5c-performance-03 (defended) | defender | Owner call, needed only to scope the LARGER change and not the claim confirmed here: is the level-wide over-inclusion a required correctness margin, or is narrowing each node's archive to its own routed states licensed as a golden-moving change? Only that narrowing reduces the per-node value sweep; the shared-block fix confirme… *(also asked by attacker/performance 5c)* |
| 24 | TD-051 | 5c-test-bloat-00 (defended) | defender | Sequencing call for the cut-pool/training owner: delete the redundant binary now, or let it retire together with the superseded sync_cuts family at the next licensed public-API break, since all three copies of this assertion die with the method. |
| 25 | TD-053 | 5c-test-bloat-02 (defended) | defender | Destination owner call for the shared StubComm/Rank0Of2 pair: testing-architecture.md section 5.2 assigns them to cobre-comm, which has no test-support feature at the pin, while section 6 phase 3 collapses tests/common into cobre-sddp's test-support surface; and Rank0Of2's load-bearing forward_passes == 1 caveat names cobre-sdd… |
| 26 | TD-054 | 5c-test-bloat-03 (defended) | defender | Owner call on the shape of the inert-only consolidation: a cfg-gated declarative macro in test_support.rs versus the candidate's field-configured shared struct, since a single struct cannot express the seven-to-one get_basis split and the two strict sites without adding modes no test reads. |
| 27 | CD-087 | 5d-architecture-00 (defended) | defender | Whether the public half of the alias block is also retired is an owner call: lib.rs:23-26 declares the pub mod namespaces non-semver-stable and the nested paths already resolve, but removing pub use policy::orchestration would touch cobre-cli, cobre-python, the integration corpus and the literal match in scripts/ci/check_python… |
| 28 | CD-088 | 5d-architecture-01 (defended) | defender | Destination is an owner call and decides the hint: re-home inside the engine beside simulation/types.rs (a pure local cleanup, hint degrades to neutral) or leave it until Phase 0a's shared output orchestration in cobre-io absorbs the engine-to-io projection wholesale (target-layering-brief.md section 2, 0a bullet), which keeps… *(also asked by attacker/architecture 5d)* |
| 29 | OD-040 | 5d-over-engineering-00 (defended) | defender | Doc-owner call: the mirror sentence at reserved-seams-and-deferred-debt.md:1278-1284 asserts every numeric-cast allow on production code carries a `// Rationale:`, which comments.md D4 and the E4 gate's IN_SCOPE_LINTS both contradict; decide whether that sentence is corrected down to the D4 scope or D4 is widened to cover cast_… *(also asked by attacker/over-engineering 5d)* |
| 30 | OD-040 | 5d-over-engineering-00 (defended) | defender | Routing call: the surviving residue is a missing input-boundary validation owned by cobre-io, not an over-engineering item in cobre-sddp; decide whether it stays on this station's over-engineering ledger or is re-filed against the owning crate. |
| 31 | OD-041 | 5d-over-engineering-01 (defended) | defender | Scope call: whether crate-internal single-file items should be narrowed to module-private at all, or whether pub(crate) is the ratified uniform internal marker for this workspace; if narrowing is wanted it should be one crate-wide pass over the 25 sites with a per-site forcing check, not this five-anchor slice. |
| 32 | PD-045 | 5d-performance-00 (defended) | defender | Owner picks where the per-stage cache lives (shared `StageContext` vs per-worker simulation scratch); the span-only residue shrinks the memory side of that trade from a full row vector to the load-balance rows. *(also asked by attacker/performance 5d)* |
| 33 | — | 5d-performance-03 (dismissed) | defender | Process call: the 4t sweep skips setup-time-only items, so the owner must say whether a setup-budget track exists to record the two conceded residues (per-plane grid rebuild in the secant; duplicate deviation walk under the opt-in flag) or whether they are dropped. *(also asked by attacker/performance 5d)* |
| 34 | PD-048 | 5d-performance-05 (defended) | defender | Rule-11 scope call the owner may want to settle once for all similar candidates: does the setup-time per-plant carve-out cover a per-plant recomputation whose cost is sized by a whole shared table (superlinear in plant count), or only one sized by the plant's own data? I read it as the latter and narrowed accordingly. |
| 35 | TD-055 | 5d-test-bloat-01 (defended) | defender | Owner call on whether the audited k_max partition should survive in any executable form now that the shift-to-hold switchover has landed and there are zero k_max >= 2 tier-1 goldens: retire the checklist to prose, or re-home it as a script-backed gate that resolves each name against a nextest listing, since Rust cannot reflect… |
| 36 | TD-056 | 5d-test-bloat-04 (defended) | defender | Owner call: whether testing-architecture.md section 5.8 (keep StubComm/Rank0Of2) plus the section 5.2 move into cobre-comm's test-support surface is the final disposition for the harness pair, or whether the test-corpus station's E08-2 needs-human item may still merge them - my dismissal of the merge half rests on section 5.8 b… |
| 37 | TD-056 | 5d-test-bloat-04 (defended) | defender | Owner call: whether conformance.rs:40 LocalComm's no-copy collectives (Ok(()) without writing recv, unlike common::StubComm) are deliberate; I read the divergence as semantics-by-design, but no rustdoc states why. |
| 38 | CD-084 | 5b-architecture-00 (defended) | attacker/architecture 5b | Can a shipped deck declare an anticipated plant with a fixed post-horizon window AND a genuine in-study decision whose delivery target sits above that window? If yes, the Sev A raw-axis residue in columns.rs::fill_anticipated_columns is reachable in production and the fix moves a parity golden; if the combination is rejected up… |
| 39 | CD-074 | 5b-architecture-01 (dismissed) | attacker/architecture 5b | Are the commitment drift tolerances COMMITMENT_DRIFT_REL = 1e-7 and COMMITMENT_DRIFT_ABS = 1e-5 in commitment_reconcile.rs chosen under the assumption that commitment-hold col_scale is exactly 1.0? The two mechanisms never cross-reference, so whether the tolerance budget already absorbs a non-unit scale factor or depends on the… |
| 40 | — | 5d-architecture-02 (defended) | attacker/architecture 5d | CLAUDE.md's Hard Rules enumerate the cobre-sddp unsafe_code = "allow" override as existing for the matrixmultiply dgemm call 'isolated in src/gemm.rs', but at this baseline hull/ is a second, unrelated unsafe island: git grep for 'unsafe ' over crates/cobre-sddp/src returns gemm.rs:93 plus hull/ffi.rs:27 and hull/mod.rs:82, :15… |
| 41 | OD-039 | 5c-over-engineering-01 (defended) | attacker/over-engineering 5c | Out-of-lens routing question, found while checking CutSelectionStrategy variant reachability and left as a candidate for no lens of mine: are the field doc comments on CutSelectionStrategy::Dynamic (cut/cut_selection.rs) deliberately the reverse of DcsParams, or is that drift the docs lens should own? The enum doc calls k2 'Max… |
| 42 | PD-043 | 5c-performance-04 (defended) | attacker/performance 5c | Would the owner accept demoting the BASIC recount in enforce_basic_count_invariant to a debug assertion, with the release path trusting counts carried out of the two reconstruction entry points? The recount is currently the only unconditional runtime check that a reconstructed basis satisfies the HiGHS shape requirement before… |
| 43 | TD-052 | 5c-test-bloat-01 (defended) | attacker/test-bloat 5c | Deleting test_mpi_allgatherv_nonuniform_workers.rs is coverage-neutral by the evidence, since its titular nonuniform-workers assertion moved to the handshake_rejects_nonuniform_workers unit test by its own module doc and its one remaining test duplicates the sibling's, and no CI workflow or script names the file. The open quest… |

## 2. Round plan

Assembled before any question is asked. Options are capped at four per question; the recommended option is listed first; per-entry exceptions inside a batch are named through the free-text option. Round 0 precedes every severity batch; no `conflicts` hold and no re-baseline flag appears inside a batched question; PD-004 is presented existence-and-queue only, with no fix option; the Cleared and dup-of items (1.5) get no round.

| Round | Kind | Subject | Options |
| -- | -- | -- | -- |
| R0-retire | prior-disposition | retire-with-commit (3): CD-001, CD-003-construction-hop, CD-006 | ratify-as-presented · re-dispose (name) · defer (name + trigger) · reject-to-Cleared (name) |
| R0-keep | prior-disposition | keep (11): CD-018, CD-034, CD-035, CD-012, CD-014-remnant, CD-016, CD-028, CD-030, CD-037, CD-038, OD-009 | ratify-as-presented · re-dispose (name) · defer (name + trigger) · reject-to-Cleared (name) |
| R0-sharpen | prior-disposition | sharpen (8): CD-004, CD-005, CD-024-successor, CD-015, CD-022, CD-023, CD-007, CD-021 | ratify-as-presented · re-dispose (name) · defer (name + trigger) · reject-to-Cleared (name) |
| R1-sevB | severity-batch | Sev B (A-risk) (2): CD-084, CD-086 | accept all · downgrade (name) · reject (name) · defer (name + trigger) |
| R2-sevB | severity-batch | Sev B (19): CD-082, CD-085, OD-040, PD-036, PD-038, PD-039, PD-040, PD-041, PD-042, PD-044, PD-045, PD-047, TD-042, TD-043, TD-044, TD-045, TD-049, TD-053, TD-054 | accept all · downgrade (name) · reject (name) · defer (name + trigger) |
| R3-sevC | severity-batch | Sev C (23): CD-083, CD-087, CD-088, OD-036, OD-037, OD-038, OD-039, OD-041, OD-042, PD-035, PD-037, PD-043, PD-046, PD-048, TD-041, TD-046, TD-047, TD-048, TD-050, TD-051, TD-052, TD-055, TD-056 | accept all · downgrade (name) · reject (name) · defer (name + trigger) |
| R4-hold-CD-024-successor | conflicts-hold | CD-024-successor — one-consumer seam kept alive against a single hypothetical consumer ('pull, don't push', Part V §V.0) | take-the-alternative · override-with-rationale · reject · defer |
| R5-rebaseline-CD-005 | rebaseline | CD-005 — must name the bar: parity goldens, rank-invariance harness, mpiexec -n 1/2 repro | accept-with-rebaseline · restate-byte-neutral · defer · reject |
| R6-nh-1 | needs-human | #1 CD-005 / 5a-architecture-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-2 | needs-human | #2 CD-082 / 5a-architecture-02 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-3 | needs-human | #3 CD-083 / 5a-architecture-03 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-4 | needs-human | #4 — / 5a-over-engineering-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-5 | needs-human | #5 PD-035 / 5a-performance-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-6 | needs-human | #6 PD-036 / 5a-performance-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-7 | needs-human | #7 TD-042 / 5a-test-bloat-02 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-8 | needs-human | #8 TD-044 / 5a-test-bloat-04 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-9 | needs-human | #9 TD-045 / 5a-test-bloat-05 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-10 | needs-human | #10 TD-046 / 5a-test-bloat-06 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-11 | needs-human | #11 CD-085 / 5b-architecture-02 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-12 | needs-human | #12 OD-037 / 5b-over-engineering-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-13 | needs-human | #13 OD-038 / 5b-over-engineering-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-14 | needs-human | #14 OD-038 / 5b-over-engineering-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-15 | needs-human | #15 PD-039 / 5b-performance-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-16 | needs-human | #16 TD-047 / 5b-test-bloat-02 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-17 | needs-human | #17 TD-048 / 5b-test-bloat-03 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-18 | needs-human | #18 TD-049 / 5b-test-bloat-04 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-19 | needs-human | #19 CD-086 / 5c-architecture-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-20 | needs-human | #20 CD-086 / 5c-architecture-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-21 | needs-human | #21 CD-028 / 5c-architecture-04 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-22 | needs-human | #22 PD-040 / 5c-performance-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-23 | needs-human | #23 PD-042 / 5c-performance-03 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-24 | needs-human | #24 TD-051 / 5c-test-bloat-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-25 | needs-human | #25 TD-053 / 5c-test-bloat-02 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-26 | needs-human | #26 TD-054 / 5c-test-bloat-03 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-27 | needs-human | #27 CD-087 / 5d-architecture-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-28 | needs-human | #28 CD-088 / 5d-architecture-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-29 | needs-human | #29 OD-040 / 5d-over-engineering-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-30 | needs-human | #30 OD-040 / 5d-over-engineering-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-31 | needs-human | #31 OD-041 / 5d-over-engineering-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-32 | needs-human | #32 PD-045 / 5d-performance-00 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-33 | needs-human | #33 — / 5d-performance-03 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-34 | needs-human | #34 PD-048 / 5d-performance-05 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-35 | needs-human | #35 TD-055 / 5d-test-bloat-01 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-36 | needs-human | #36 TD-056 / 5d-test-bloat-04 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-37 | needs-human | #37 TD-056 / 5d-test-bloat-04 (defender) | recommended · alternative(s) · defer · other |
| R6-nh-38 | needs-human | #38 CD-084 / 5b-architecture-00 (attacker/architecture 5b) | recommended · alternative(s) · defer · other |
| R6-nh-39 | needs-human | #39 CD-074 / 5b-architecture-01 (attacker/architecture 5b) | recommended · alternative(s) · defer · other |
| R6-nh-40 | needs-human | #40 — / 5d-architecture-02 (attacker/architecture 5d) | recommended · alternative(s) · defer · other |
| R6-nh-41 | needs-human | #41 OD-039 / 5c-over-engineering-01 (attacker/over-engineering 5c) | recommended · alternative(s) · defer · other |
| R6-nh-42 | needs-human | #42 PD-043 / 5c-performance-04 (attacker/performance 5c) | recommended · alternative(s) · defer · other |
| R6-nh-43 | needs-human | #43 TD-052 / 5c-test-bloat-01 (attacker/test-bloat 5c) | recommended · alternative(s) · defer · other |
| R7-handoffs | handoff-confirm | measurements/lp-inventory.json (30 lp/ modules) · Part-I item I.3-7 cross-reference · alignment-queue.json | confirm · amend (name) |

**Invariants:** round 0 precedes every severity batch · no conflicts hold or rebaseline flag inside a batched question · batched questions carry at most four options · PD-004 is presented existence-and-queue only, with no fix option · a retire row without a commit SHA is refused · a defer needs a concrete trigger · an un-overridden hold stays held with no id promoted.

## 3. Decision record

**Decision: ratified** — 2026-09-18, main session, baseline `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`. Presented 66 (22 prior ids + 44 new entries): prior ratified 22 (3 retire → Cleared, 11 keep, 8 sharpen); new accepted 44, amended 0, downgraded 0, rejected 0, deferred 0; holds overridden 0, closed by their alternative 1; re-baseline accepted 1; needs-human answered 43/43; perf rows struck 0, TD rows struck 0. Every answer was the recommended option. No fix executed, no measurement run, no figure asserted.

### 3.1 Prior Wave 4/6/7 dispositions (round 0)

| ID | Wave | Presented | Owner decision | Closing commit | Rationale | Directions |
| -- | -- | -- | -- | -- | -- | -- |
| CD-001 | 4 | retire | ratified → Cleared (R0-retire) | b051c410 | Ratify as presented (Recommended) — all three retire with their closing commits and move to Cleared | - |
| CD-003-construction-hop | 4 | retire | ratified → Cleared (R0-retire) | 4075c4e8 | Ratify as presented (Recommended) — all three retire with their closing commits and move to Cleared | - |
| CD-006 | 4 | retire | ratified → Cleared (R0-retire) | 3f4c3db3 | Ratify as presented (Recommended) — all three retire with their closing commits and move to Cleared | - |
| CD-004 | 4 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-005 | 4 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | re-baseline accepted (R5): Accept with re-baseline (Recommended) — CD-005 stays accepted; the fix lands only behind a parity re-baseline that names all three bars, with the NCS fields moved as one unit · R6-nh-1: Remove it and edit the rules line — StudySetup… |
| CD-024-successor | 4 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | hold closed by its alternative (R4): Take the alternative (Recommended) — die: the per-rank derivation stays; CD-024-successor rides CD-004's single Config projection; no broadcast seam, no id promoted; the hold closes as subsumed |
| CD-015 | 6 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-022 | 6 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-018 | 6 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-035 | 6 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-034 | 6 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-023 | 6 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-021 | 7 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-007 | 7 | sharpen | ratified (R0-sharpen) | - | Ratify as presented (Recommended) — all eight sharpen with both claims recorded; the three Wave-4 rows keep their Phase… | - |
| CD-012 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-014-remnant | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-016 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-028 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | R6-nh-21: Module-doc correction only; no code hoist — claim_scatter.rs's module-doc consumer list is corrected to name simulation/enumerated.rs; the four-line worker-result fold stays inline at both sites; CD-028's skeleton claim stays open as calibrated |
| CD-030 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-037 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| CD-038 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |
| OD-009 | 7 | keep | ratified (R0-keep) | - | Ratify as presented (Recommended) — all eleven stay live at their register severities with the surviving claims as writ… | - |

### 3.2 New entries (rounds 1-3)

| ID | Round | Decision | Severity | Alignment | Rationale (owner) | Queue | Directions |
| -- | -- | -- | -- | -- | -- | -- | -- |
| CD-084 | R1 | accept | B (A-risk) (reviewer: A) | neutral | Accept both (Recommended) — both stay live at B (A-risk) with the calibrated fix-shapes;… | none | R6-nh-38: Rejected at validation; latent, fix byte-neutral |
| CD-086 | R1 | accept | B (A-risk) (⬆ from B) | neutral | Accept both (Recommended) — both stay live at B (A-risk) with the calibrated fix-shapes;… | none | R6-nh-19: Road (a): reject the pairing at admission · R6-nh-20: In scope; severity stays B (A-risk) |
| CD-082 | R2 | accept | B | advances-0a · I.3-7 | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | alignment | R6-nh-2: Construction-time check, get stays infallible |
| CD-085 | R2 | accept | B | advances-0b | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | alignment | R6-nh-11: Bounded to seams/traits/crates; the accessor is fine |
| OD-040 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | none | R6-nh-29: Correct the mirror sentence down to D4 · R6-nh-30: Re-file the residue against cobre-io at E11 |
| PD-036 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | R6-nh-6: NodeGraph field, land with the accessor residue |
| PD-038 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | - |
| PD-039 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | R6-nh-15: Treat as a warm-start-chain change; clear all three bars in E10 |
| PD-040 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | R6-nh-22: Correct the sentence inside PD-040's fix |
| PD-041 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | - |
| PD-042 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | R6-nh-23: Keep the over-inclusion; shared-block fix only |
| PD-044 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | - |
| PD-045 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | R6-nh-32: StageContext, span-only |
| PD-047 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | perf | - |
| TD-042 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-7: Uniform spread + one canary literal; no Default derive |
| TD-043 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | - |
| TD-044 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-8: Per-binary hoist now; cross-binary lift is E08's call |
| TD-045 | R2 | accept | B | advances-1 · I.3-8 | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-9: Consolidate behind test-support; E07 fixes the workflow |
| TD-049 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-18: Spec-layer extension, with the collapse as its first step |
| TD-053 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-25: cobre-sddp test-support surface |
| TD-054 | R2 | accept | B | neutral | Accept all (Recommended) — all 19 stay live at Sev B as calibrated; the nine PD rows stay… | test-debt | R6-nh-26: cfg-gated declarative macro in test_support.rs |
| CD-083 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-3: Remove, batched with the public-API break |
| CD-087 | R3 | accept | C (reviewer: B) | advances-0b | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | alignment | R6-nh-27: Retire the private half now; public half in the API-break batch |
| CD-088 | R3 | accept | C | advances-0a | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | alignment | R6-nh-28: Land with 0a's shared output orchestration |
| OD-036 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | - |
| OD-037 | R3 | accept | C (reviewer: B) | advances-0b | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | alignment | R6-nh-12: Batch with the next licensed public-API break / 0b carve |
| OD-038 | R3 | accept | C (reviewer: B) | advances-0b | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | alignment | R6-nh-13: Batch with the next licensed public-API break / 0b carve · R6-nh-14: Joins OD-038 |
| OD-039 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-41: Route the two-comment correction to E07 |
| OD-041 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-31: Crate-wide pass over the 25 sites with a forcing check |
| OD-042 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | - |
| PD-035 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-5: Symmetric fill only, no re-baseline |
| PD-037 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | - |
| PD-043 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-42: Keep the unconditional recount; PD-043 is the slot-table clear only |
| PD-046 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | - |
| PD-048 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | none | R6-nh-34: Only own-data recomputations are carved out; PD-048 stands as narrowed |
| TD-041 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | - |
| TD-046 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-10: Relocate and re-word the three doc comments |
| TD-047 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-16: One extracted tests.rs per module, matching the sibling convention |
| TD-048 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-17: Parameterize in place now; the fold is E08's grouping call |
| TD-050 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | - |
| TD-051 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-24: Delete the redundant binary now |
| TD-052 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-43: Delete; provenance lives in git history |
| TD-055 | R3 | accept | C (reviewer: B) | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-35: Retire the checklist to prose |
| TD-056 | R3 | accept | C | neutral | Accept all (Recommended) — all 23 stay live at Sev C as calibrated (the reviewer-B rows k… | test-debt | R6-nh-36: Final: keep the pair; destination per NH25 · R6-nh-37: Deliberate; state the why in one doc clause |

### 3.3 Holds and re-baseline decisions (rounds 4-5, each asked alone)

- **CD-024-successor** (R4) — **take-the-alternative** — Take the alternative (Recommended) — die: the per-rank derivation stays; CD-024-successor rides CD-004's single Config projection; no broadcast seam, no id promoted; the hold closes as subsumed. Condition: one-consumer seam kept alive against a single hypothetical consumer ('pull, don't push', Part V §V.0). No id promoted.
- **CD-005** (R5) — **accept-with-rebaseline** — Accept with re-baseline (Recommended) — CD-005 stays accepted; the fix lands only behind a parity re-baseline that names all three bars, with the NCS fields moved as one unit. Bar: parity goldens (parity_hash_highs / parity_hash_clp); rank-invariance harness (tests/common/permute.rs); mpiexec -n 1/2 reproduction.

### 3.4 Worker needs-human answers (round 6)

| # | Entry | From | Question | Answer | Recorded as |
| -- | -- | -- | -- | -- | -- |
| 1 | CD-005 / 5a-architecture-00 | defender | Owner call: `set_budget` is enumerated in the declared accessor surface at `.claude/architecture-rules.md:117`, so the disposition of a caller-free ungated mut… | **Remove it and edit the rules line** | StudySetup::set_budget is deleted and dropped from the .claude/architecture-rules.md accessor list; folds into the CD-005 split ticket (no public knob is kept) |
| 2 | CD-082 / 5a-architecture-02 | defender | Whether the fail-loud leg changes the public surface of ResolvedParameters::get (a fallible signature or a panic) or is instead enforced as a construction-time… | **Construction-time check, get stays infallible** | the fail-loud leg is enforced at the LP-build / admission-gate site riding the CD-004 single-projection carrier; ResolvedParameters::get keeps its infallible pub must_use signature |
| 3 | CD-083 / 5a-architecture-03 | defender | Owner must choose between removing `CutManagementConfig::warm_start_cuts` (a breaking change to a re-exported public type) and giving it a real reader plus a r… | **Remove, batched with the public-API break** | CutManagementConfig::warm_start_cuts is deleted in the same licensed public-API break as OD-037 / OD-038 (CD-019 batching precedent); the false doc line is corrected now |
| 4 | — / 5a-over-engineering-00 | defender (+ attacker/over-engineering 5a) | Owner call (prose, not code): reword the allow-census Load-bearing class at docs/design/reserved-seams-and-deferred-debt.md:1282-1288 and the scripts/README.md… | **Reword both down to the D4 scope** | routed to E11's mirror write-back: the mirror :1282-1288 Load-bearing prose and scripts/README.md:36 stop claiming an every-site rationale and state the comments.md D4 lint scope instead; prose only |
| 5 | PD-035 / 5a-performance-00 | defender (+ attacker/performance 5a) | Owner call: whether the goldens-moving delta reformulation of the 2-opt accept test is worth a parity re-baseline at all, given that the confirmed free win is… | **Symmetric fill only, no re-baseline** | PD-035's fix-shape narrows to the byte-exact half (mirrored distance matrix, hoisted per-start buffers); the tour-cost delta reformulation is dropped, no parity re-baseline; setup-time, stays Sev C, not queued |
| 6 | PD-036 / 5a-performance-01 | defender (+ attacker/performance 5a) | Placement is an owner call the architecture rules do not disambiguate: a derived study-invariant consumed only by the backward drivers fits decision-tree rule… | **NodeGraph field, land with the accessor residue** | decision-tree rule 2: the cut-level decomposition becomes a NodeGraph field beside n_pools / pool_stage, reaching the backward drivers through TrainingContext; landed in the same change as the interior-cut-node accessor one-owner residue |
| 7 | TD-042 / 5a-test-bloat-02 | defender (+ attacker/test-bloat 5c) | Whether the corpus should keep at least one literal that still spells all sixteen penalty fields as a canary: spreading `..HydroPenalties::uniform(v)` everywhe… | **Uniform spread + one canary literal; no Default derive** | the fix stays a test-support constructor (..HydroPenalties::uniform(v) spread) with one full sixteen-field literal kept per crate as the compile-time canary; no Default derive in cobre-core |
| 8 | TD-044 / 5a-test-bloat-04 | defender | Scope call for the owner: the default_hydro_penalties body is one md5 across anticipated_core.rs and anticipated_scenarios.rs, and scenarios build_config at 29… | **Per-binary hoist now; cross-binary lift is E08's call** | the in-file duplicates collapse at file scope in each binary; whether the shared class lifts into tests/common follows the E08 test-corpus convention decision |
| 9 | TD-045 / 5a-test-bloat-05 | defender | Owner call outside this candidate: invariance-shuffle.yml builds cargo nextest -p cobre-sddp --test parity without the test-support feature, while parity.rs re… | **Consolidate behind test-support; E07 fixes the workflow** | the shared state_layout_for lives in the test-support surface; invariance-shuffle.yml's missing --features test-support on the parity binary is handed to the build-ci station (E07) as a CI drift item |
| 10 | TD-046 / 5a-test-bloat-06 | defender | Scope call for this item: whether relocating the oracle should also re-word the three production doc comments that use `shift_lag_state` as the canonical lag-r… | **Relocate and re-word the three doc comments** | the lag-shift oracle leaves stochastic/noise.rs and the three production doc comments (noise.rs:211, state_space.rs:443, cut_state_projection.rs:269) describe the kernel by behaviour instead of naming the test oracle; no behaviour change |
| 11 | CD-085 / 5b-architecture-02 | defender | Alignment-epic owner call: whether conflicts trigger 4 (one-consumer abstraction) reaches a purpose-named single-consumer StateSpace accessor that completes an… | **Bounded to seams/traits/crates; the accessor is fine** | conflicts trigger 4 does not reach a family-completing StateSpace accessor; CD-085 stays accepted at B, advances-0b, no hold; recorded on the E9 alignment queue |
| 12 | OD-037 / 5b-over-engineering-00 | defender (+ attacker/over-engineering 5b) | Scheduling only: whether the public-API removal of Col and Row ships as its own semver-major break or waits for the 0b cobre-model carve-out of lp/, as the CD-… | **Batch with the next licensed public-API break / 0b carve** | OD-037's Col/Row removal ships in the same licensed public-API break as OD-038 and CD-083 (CD-019 batching precedent); the defect is settled, only the scheduling is batched |
| 13 | OD-038 / 5b-over-engineering-01 | defender | Owner call carried over from the attacker: deleting FphaRowRange is a public-API removal (reachable as cobre_sddp::indexer::FphaRowRange via lib.rs:69) with no… | **Batch with the next licensed public-API break / 0b carve** | OD-038's FphaRowRange removal ships in the same licensed public-API break as OD-037 and CD-083; the false module doc is corrected now |
| 14 | OD-038 / 5b-over-engineering-01 | defender | Scope call: the fix as filed leaves the same per-plant framing live in BlockGrid::advance_fpha_base's rustdoc (block_grid.rs:106-107), so the reader hazard is… | **Joins OD-038** | one doc-correction step covers the FphaRowRange module doc and BlockGrid::advance_fpha_base's rustdoc (block_grid.rs:106-107); lands now, independent of the removal's batching |
| 15 | PD-039 / 5b-performance-01 | defender (+ attacker/performance 5b) | Owner call: whether removing a bit-identical repeat submission of already-in-force column bounds counts as a warm-start-chain change that must clear the parity… | **Treat as a warm-start-chain change; clear all three bars in E10** | PD-039's hoist is measured by the perf sweep at 4t and lands only with the parity goldens, the rank-invariance harness and the opening_order_determinism gate green; nothing asserted at the gate |
| 16 | TD-047 / 5b-test-bloat-02 | defender | Target shape for the split is a convention call CD-007 owns: one extracted tests.rs matching commitment_reconcile/layout/template, or N subject-named files mat… | **One extracted tests.rs per module, matching the sibling convention** | TD-047's split follows the three extracted-tests siblings in lp/builder (commitment_reconcile / layout / template); recorded as CD-007's convention decision |
| 17 | TD-048 / 5b-test-bloat-03 | defender | Whether this binary's classical arm should be parameterized in place or instead folded onto the existing two_hydro_par_system helper when template_integration… | **Parameterize in place now; the fold is E08's grouping call** | build_classical_fixture collapses onto build_par_a_fixture inside the binary now; folding onto two_hydro_par_system follows the E08 section 5.1 grouping decision |
| 18 | TD-049 / 5b-test-bloat-04 | defender | Scope call: whether the ticket is the family collapse as filed or the higher-leverage extension of the O(1) Spec layer to InflowModel, LoadModel and the four b… | **Spec-layer extension, with the collapse as its first step** | TD-049's ticket targets the O(1) Spec-layer extension to InflowModel, LoadModel and the four sizing structs, which owns the remaining fan-out; the nineteen-builder collapse lands as its first step |
| 19 | CD-086 / 5c-architecture-00 | defender (+ attacker/architecture 5c) | Carried over from the attacker and still decisive: is enumerated traversal combined with dynamic cut selection a SUPPORTED configuration? Road (a) rejects the… | **Road (a): reject the pairing at admission** | enumerated traversal + dynamic cut selection is NOT a supported configuration: it becomes a typed admission-gate rejection beside the existing enumerated preconditions in setup/mod.rs, never a silent degrade; no new collective |
| 20 | CD-086 / 5c-architecture-00 | defender | Scope and severity call: `CutPool::enforce_budget`'s `(last_active_iter, active_count)` eviction key is a second production reader that degrades the same way u… | **In scope; severity stays B (A-risk)** | CutPool::enforce_budget's eviction-key reader is inside CD-086's scope; road (a) covers both readers; severity stays B (A-risk), the marker already carrying the bound hazard; no separate item |
| 21 | CD-028 / 5c-architecture-04 | defender | Owner call: whether the confirmed residue should produce any code change at all, or only the module-doc consumer-list correction -- the identical fragment is a… | **Module-doc correction only; no code hoist** | claim_scatter.rs's module-doc consumer list is corrected to name simulation/enumerated.rs; the four-line worker-result fold stays inline at both sites; CD-028's skeleton claim stays open as calibrated |
| 22 | PD-040 / 5c-performance-00 | defender | Owner call: whether the inaccurate rustdoc sentence at training/session/mod.rs:1026-1027 ('no heap allocation when the cut pools have not grown') gets its own… | **Correct the sentence inside PD-040's fix** | the false 'no heap allocation' rustdoc at training/session/mod.rs:1026-1027 is made true in the same change that removes or documents run_cut_management's allocations; no extra id |
| 23 | PD-042 / 5c-performance-03 | defender (+ attacker/performance 5c) | Owner call, needed only to scope the LARGER change and not the claim confirmed here: is the level-wide over-inclusion a required correctness margin, or is narr… | **Keep the over-inclusion; shared-block fix only** | the level-wide over-inclusion is a required correctness margin; PD-042 is exactly the byte-neutral one-block-per-level fix; narrowing each node's archive to its routed states is NOT licensed |
| 24 | TD-051 / 5c-test-bloat-00 | defender | Sequencing call for the cut-pool/training owner: delete the redundant binary now, or let it retire together with the superseded sync_cuts family at the next li… | **Delete the redundant binary now** | the duplicate sync_cuts count-mismatch binary is deleted now (coverage-neutral, independent of the API break); the surviving copy retires with the superseded method |
| 25 | TD-053 / 5c-test-bloat-02 | defender | Destination owner call for the shared StubComm/Rank0Of2 pair: testing-architecture.md section 5.2 assigns them to cobre-comm, which has no test-support feature… | **cobre-sddp test-support surface** | the genericity hard rule decides: Rank0Of2 names cobre-sddp's RankDistribution, so the StubComm / Rank0Of2 pair lives in cobre-sddp's test_support; a later StubComm move to a cobre-comm test-support feature is E08-2's call |
| 26 | TD-054 / 5c-test-bloat-03 | defender | Owner call on the shape of the inert-only consolidation: a cfg-gated declarative macro in test_support.rs versus the candidate's field-configured shared struct… | **cfg-gated declarative macro in test_support.rs** | one cfg-gated macro stamps the inert MockSolver body; the seven-to-one get_basis site and the two strict sites keep their own doubles |
| 27 | CD-087 / 5d-architecture-00 | defender | Whether the public half of the alias block is also retired is an owner call: lib.rs:23-26 declares the pub mod namespaces non-semver-stable and the nested path… | **Retire the private half now; public half in the API-break batch** | the intra-crate aliases go now; pub use policy::orchestration is removed in the same licensed public-API break as OD-037 / OD-038 / CD-083, updating cobre-cli, cobre-python, the corpus and scripts/ci/check_python_parity.py then |
| 28 | CD-088 / 5d-architecture-01 | defender (+ attacker/architecture 5d) | Destination is an owner call and decides the hint: re-home inside the engine beside simulation/types.rs (a pure local cleanup, hint degrades to neutral) or lea… | **Land with 0a's shared output orchestration** | CD-088 stays accepted at C, advances-0a; the write-payload conversion moves when Phase 0a's shared output orchestration in cobre-io absorbs the engine-to-io projection, not as an intra-engine move 0a would undo |
| 29 | OD-040 / 5d-over-engineering-00 | defender (+ attacker/over-engineering 5d) | Doc-owner call: the mirror sentence at reserved-seams-and-deferred-debt.md:1278-1284 asserts every numeric-cast allow on production code carries a `// Rational… | **Correct the mirror sentence down to D4** | the mirror :1278-1284 sentence stops asserting an every-site Rationale on numeric-cast allows and states the comments.md D4 lint scope; no new obligation on the 42 cast_* openers; routed to E11's mirror write-back with NH4 |
| 30 | OD-040 / 5d-over-engineering-00 | defender | Routing call: the surviving residue is a missing input-boundary validation owned by cobre-io, not an over-engineering item in cobre-sddp; decide whether it sta… | **Re-file the residue against cobre-io at E11** | OD-040 stays accepted here as the record; the four cast_sign_loss sites are a missing cobre-io input-boundary validation and are re-filed against cobre-io at the reconciliation epic |
| 31 | OD-041 / 5d-over-engineering-01 | defender | Scope call: whether crate-internal single-file items should be narrowed to module-private at all, or whether pub(crate) is the ratified uniform internal marker… | **Crate-wide pass over the 25 sites with a forcing check** | OD-041 widens to one crate-wide visibility pass over the 25 single-file pub(crate) items, each narrowed only where a per-site forcing check proves no other consumer; stays Sev C |
| 32 | PD-045 / 5d-performance-00 | defender (+ attacker/performance 5d) | Owner picks where the per-stage cache lives (shared `StageContext` vs per-worker simulation scratch); the span-only residue shrinks the memory side of that tra… | **StageContext, span-only** | PD-045's per-stage unscaled cache lives on StageContext as a load-balance-rows span (shared immutable per-stage data, the context-struct pattern); not per-worker scratch |
| 33 | — / 5d-performance-03 | defender (+ attacker/performance 5d) | Process call: the 4t sweep skips setup-time-only items, so the owner must say whether a setup-budget track exists to record the two conceded residues (per-plan… | **Informational note only; no track, no id** | the 5d-performance-03 dismissal stands; the two conceded setup-time FPHA-fitting residues (per-plane grid rebuild in the secant; duplicate deviation walk under the opt-in flag) are listed once as informational for E10; no setup-time track, no id, no queue row |
| 34 | PD-048 / 5d-performance-05 | defender | Rule-11 scope call the owner may want to settle once for all similar candidates: does the setup-time per-plant carve-out cover a per-plant recomputation whose… | **Only own-data recomputations are carved out; PD-048 stands as narrowed** | rule-11 interpretation recorded: the setup-time per-plant carve-out covers recomputations sized by the plant's own data only; a per-plant rescan of a whole shared table is superlinear and stays a finding; PD-048 stands as narrowed at Sev C |
| 35 | TD-055 / 5d-test-bloat-01 | defender | Owner call on whether the audited k_max partition should survive in any executable form now that the shift-to-hold switchover has landed and there are zero k_m… | **Retire the checklist to prose** | hold_k1_byte_stability_probe.rs is retired; the audited k_max partition becomes a short prose note in the testing-architecture doc; no executable gate |
| 36 | TD-056 / 5d-test-bloat-04 | defender | Owner call: whether testing-architecture.md section 5.8 (keep StubComm/Rank0Of2) plus the section 5.2 move into cobre-comm's test-support surface is the final… | **Final: keep the pair; destination per NH25** | testing-architecture section 5.8 is ratified for the StubComm / Rank0Of2 pair; the merge half stays dismissed; the destination is cobre-sddp's test_support (NH25), not cobre-comm |
| 37 | TD-056 / 5d-test-bloat-04 | defender | Owner call: whether conformance.rs:40 LocalComm's no-copy collectives (Ok(()) without writing recv, unlike common::StubComm) are deliberate; I read the diverge… | **Deliberate; state the why in one doc clause** | conformance.rs:40 LocalComm's no-copy collectives are semantics by design; its rustdoc gains the single clause explaining the divergence from tests/common StubComm; no behaviour change |
| 38 | CD-084 / 5b-architecture-00 | attacker/architecture 5b | Can a shipped deck declare an anticipated plant with a fixed post-horizon window AND a genuine in-study decision whose delivery target sits above that window?… | **Rejected at validation; latent, fix byte-neutral** | an anticipated plant with a fixed post-horizon window AND an in-study decision above it is not a valid deck (rejected at validation), so CD-084's raw-axis residue is latent; CD-084 stays B (A-risk) as calibrated and the one-site ring-axis correction lands byt… |
| 39 | CD-074 / 5b-architecture-01 | attacker/architecture 5b | Are the commitment drift tolerances COMMITMENT_DRIFT_REL = 1e-7 and COMMITMENT_DRIFT_ABS = 1e-5 in commitment_reconcile.rs chosen under the assumption that com… | **Chosen at col_scale 1.0; add the cross-reference + a scale assertion** | COMMITMENT_DRIFT_REL / COMMITMENT_DRIFT_ABS assume commitment-hold col_scale == 1.0; the coupling is documented in both modules and the unit scale asserted where the tolerances apply; no constant changes; rides E11's reconciliation (5b-architecture-01 stays d… |
| 40 | — / 5d-architecture-02 | attacker/architecture 5d | CLAUDE.md's Hard Rules enumerate the cobre-sddp unsafe_code = "allow" override as existing for the matrixmultiply dgemm call 'isolated in src/gemm.rs', but at… | **Module granularity: crate-root forbid + per-module allow** | cobre-sddp moves to #![forbid(unsafe_code)] at the crate root with #[allow(unsafe_code)] on gemm.rs and hull::ffi only, so a third island needs an explicit exemption; CLAUDE.md's hard-rule enumeration is amended to name both islands; informational at this sta… |
| 41 | OD-039 / 5c-over-engineering-01 | attacker/over-engineering 5c | Out-of-lens routing question, found while checking CutSelectionStrategy variant reachability and left as a candidate for no lens of mine: are the field doc com… | **Route the two-comment correction to E07** | the reversed field docs on CutSelectionStrategy::Dynamic (k2 / nadic) are a rustdoc drift item for the build-ci/docs station; no id at this station; the config mapping is correct |
| 42 | PD-043 / 5c-performance-04 | attacker/performance 5c | Would the owner accept demoting the BASIC recount in enforce_basic_count_invariant to a debug assertion, with the release path trusting counts carried out of t… | **Keep the unconditional recount; PD-043 is the slot-table clear only** | enforce_basic_count_invariant's BASIC recount stays an unconditional release check; PD-043's fix-shape is the reconstructed-slots-only clear of the slot table; no debug_assert demotion |
| 43 | TD-052 / 5c-test-bloat-01 | attacker/test-bloat 5c | Deleting test_mpi_allgatherv_nonuniform_workers.rs is coverage-neutral by the evidence, since its titular nonuniform-workers assertion moved to the handshake_r… | **Delete; provenance lives in git history** | tests/test_mpi_allgatherv_nonuniform_workers.rs is deleted (coverage-neutral); the historical nonuniform-workers bug stays traceable through git history and the surviving handshake_rejects_nonuniform_workers unit test |

### 3.5 Presented, no decision taken

- 16 defender dismissals (Cleared with citation) — no question offered.
- 10 dup-of folds onto prior ids — no question offered.
- five reserved-seam filters (LipschitzConfig.mode / UpperBoundEvaluationConfig, per-family boundary coupling channels, Legacy cost-scale branch, #[allow] census Reserved-seam class, superseded cut-sync public methods) — no question offered.

### 3.6 Informational (no severity, no id)

- R6-nh-33: two setup-time FPHA-fitting residues (per-plane grid rebuild in the secant; duplicate deviation walk under the opt-in flag) — informational for E10, no track, no id
- R6-nh-40: hull/ is a second unsafe island beside gemm.rs; move to crate-root forbid + per-module allow and amend CLAUDE.md — E07/E11, no id
- R6-nh-41: CutSelectionStrategy::Dynamic k2 / nadic field docs reversed against dcs.rs — E07, no id
- R6-nh-9: invariance-shuffle.yml builds the parity binary without the test-support feature — E07, no id

## 4. Handoffs after the gate

- **E9** — alignment-queue.json written: 8 non-neutral rows (CD-082 advances-0a I.3-7, CD-088 advances-0a, CD-085 / CD-087 / OD-037 / OD-038 advances-0b, TD-045 advances-1 I.3-8, CD-034 advances-0b) plus the reused Wave-4 rows CD-004 / CD-005 / CD-003 hop (advances-0a); CD-024-successor hold CLOSED by its alternative (no id promoted); NH11 (trigger 4 bounded to seams/traits/crates), NH28 (CD-088 lands with 0a's output orchestration); lp-inventory.json (30 lp/ modules) and Part-I item I.3-7 released
- **E10** — perf-queue.json unchanged: 9 claim rows kept, all UNMEASURED 4t, plus the PD-004 existence row; directions attached: PD-036 NodeGraph field with the accessor residue, PD-039 warm-start-chain bars (parity goldens + rank-invariance + opening_order_determinism), PD-040 doc sentence corrected inside the fix, PD-042 over-inclusion kept (shared block only), PD-045 StageContext span; informational: two setup-time FPHA-fitting residues (NH33), no track
- **E08** — td-queue.json unchanged: 16 TD rows kept; directions: TD-042 uniform spread + one canary literal, TD-044 per-binary hoist, TD-045 behind test-support, TD-047 one extracted tests.rs per module, TD-048 parameterize in place, TD-049 Spec-layer extension, TD-051 delete the redundant binary now, TD-052 delete, TD-053 / TD-056 pair in cobre-sddp test_support (section 5.8 ratified), TD-054 cfg-gated macro, TD-055 prose
- **E07** — invariance-shuffle.yml builds the parity binary without --features test-support (NH9); CutSelectionStrategy::Dynamic k2 / nadic field docs reversed (NH41); CLAUDE.md unsafe enumeration + module-granularity forbid/allow on gemm.rs and hull::ffi (NH40)
- **E11** — mirror write-back: allow-census Load-bearing prose down to the D4 scope (NH4, NH29); OD-040 residue re-filed against cobre-io (NH30); COMMITMENT_DRIFT_* cross-reference + scale assertion (NH39); the Legacy cost-scale seam has no mirror entry at the pin; public-API removal batch = OD-037, OD-038, CD-083, CD-087 public half (CD-019 precedent)
- **E06** — the CD-004 wire twin (BroadcastConfig) is the cli-python station's half; CD-082's validate.rs reconcile_boundary context cited

**Gate: RETURNED 2026-09-18** — baseline `077dbe2c`; accepted 44, amended 0, downgraded 0, rejected 0, deferred 0, overridden 0; 22 prior ids ratified (3 retire → Cleared, 11 keep, 8 sharpen); 1 hold closed by its alternative; 1 re-baseline accepted; 43 needs-human answered. The next station is unblocked.
