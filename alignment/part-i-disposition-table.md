## GENERALIZATION ALIGNMENT

Part-I re-verification — the nine roadmap claims (I.3 items 1-8 and I.5) consolidated from the five owning stations' handoffs and dispositioned at the register pin `077dbe2c` (envelope: alignment/part-i-dispositions.json beside this fragment). Fresh ids start at CD-119; no fix is executed and no tracked source is written.

| # | Claim (v0.12) | v0.12 anchor | Baseline anchor | Disposition | Phase | Register |
|---|---------------|--------------|-----------------|-------------|-------|----------|
| 1 | System stores the stochastic input model as first-class fields | crates/cobre-core/src/system/mod.rs:107 | `crates/cobre-core/src/system/mod.rs::System` | sharpen | 1 | CD-119 |
| 2 | Stage bakes in stochastic and risk configuration | crates/cobre-core/src/model/temporal.rs:281 | `crates/cobre-core/src/model/temporal.rs::Stage` | keep | 1 | CD-120 |
| 3 | PolicyGraph is framed around forward/backward traversal, discount rates and cyclic convergence | crates/cobre-core/src/model/horizon.rs `PolicyGraph` | `crates/cobre-core/src/model/horizon.rs::HorizonGraph` | sharpen | 1 | CD-121 |
| 4 | InitialConditions is SDDP-warm-start-shaped | crates/cobre-core/src/constraints/initial_conditions.rs:183 | `crates/cobre-core/src/constraints/initial_conditions.rs::InitialConditions` | sharpen | 1 | CD-122 |
| 5 | training_event.rs lives in cobre-core | crates/cobre-core/src/constraints/training_event.rs:125 | `crates/cobre-core/src/constraints/training_event.rs::TrainingEvent` | sharpen | 1 | CD-123 |
| 6 | cobre-io's policy checkpoint format is literally cut-records | crates/cobre-io/src/output/policy | `crates/cobre-io/src/output/policy/records.rs::PolicyCutRecord` | sharpen | 1 | CD-124 |
| 7 | The config type is SDDP-shaped | crates/cobre-io/src/config/mod.rs | `crates/cobre-io/src/config/mod.rs:77` | sharpen | 0a | CD-125 |
| 8 | The leakage reaches one layer below cobre-core: StageTemplate | crates/cobre-solver/src/types.rs | `crates/cobre-solver/src/types.rs::StageTemplate` | keep | 1 | CD-126 |
| I.5 | The CLI and orchestration coupling | crates/cobre-cli/src | `crates/cobre-cli/src/commands/run/outputs.rs::write_training_outputs` | sharpen | 0a | CD-127 |

### Entries (keep and sharpen rows)

**CD-119 · Sev B · paradigm-leakage · effort L · confidence high**
`System` carries eight stochastic-input fields, not four: the claimed `inflow_models`, `load_models`, `ncs_models`, `correlation` plus `inflow_history`, `external_scenarios`, `external_load_scenarios`, `external_ncs_scenarios`, which the v0.12 text folded into 'plus external/historical scenario tables' without naming; a deterministic dispatch or power-flow study reads none of them, and the whole `model/scenario.rs` pipeline (1175 lines) still sits in the generic crate.

- **Station:** alignment (Part-I re-verification; handoffs from core-io)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-core/src/system/mod.rs::System`, `crates/cobre-core/src/system/mod.rs:115`, `crates/cobre-core/src/system/mod.rs:117`, `crates/cobre-core/src/system/mod.rs:119`, `crates/cobre-core/src/system/mod.rs:121`, `crates/cobre-core/src/system/mod.rs:129`, `crates/cobre-core/src/system/mod.rs:131`, `crates/cobre-core/src/system/mod.rs:133`, `crates/cobre-core/src/system/mod.rs:135`, `crates/cobre-core/src/model/scenario.rs:1`
- **Evidence:** (1) `crates/cobre-core/src/system/mod.rs::System` — container (:55 at the ticket's a136840d) (2) `crates/cobre-core/src/system/mod.rs:115` — claimed field inflow_models (:111 at the ticket's a136840d) (3) `crates/cobre-core/src/system/mod.rs:117` — claimed field load_models (:113 at the ticket's a136840d) (4) `crates/cobre-core/src/system/mod.rs:119` — claimed field ncs_models (:115 at the ticket's a136840d) (5) `crates/cobre-core/src/system/mod.rs:121` — claimed field correlation (:117 at the ticket's a136840d) (6) `crates/cobre-core/src/system/mod.rs:129` — widening field inflow_history (:125 at the ticket's a136840d) (7) `crates/cobre-core/src/system/mod.rs:131` — widening field external_scenarios (:127 at the ticket's a136840d) (8) `crates/cobre-core/src/system/mod.rs:133` — widening field external_load_scenarios (:129 at the ticket's a136840d) (9) `crates/cobre-core/src/system/mod.rs:135` — widening field external_ncs_scenarios (:131 at the ticket's a136840d) (10) `crates/cobre-core/src/model/scenario.rs:1` — the stochastic-inflow pipeline module (11) Changed since v0.12: The four claimed fields still exist (anchor drift only); the surface is wider than recorded; `System::policy_graph` is item 3's residue, not this item's.
- **Fix-shape:** Phase 1 purification: the eight stochastic-input fields and the scenario pipeline leave `System` for the `cobre-stochastic` uncertainty store (`Switchable<T>`, by-reference); prose only, no fix executed here.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1462 — IV.5 (`InflowModel`, `LoadModel`, `NcsModel`, `CorrelationModel` and the external-scenario tables leave `System`); provisional until the alignment adjudication)
- **Part-I:** I.3-1 — disposition sharpen, phase 1; station verdicts: core-io: sharpen; stochastic: sharpen (CD-065 / CD-070 / CD-071, the L1 store-vs-config seam).
- **Related register ids:** CD-045, OD-013, OD-016, CD-065, CD-070, CD-071 (station findings that cross-reference this item; none is this claim, so none is a dup-of)

**CD-120 · Sev B · paradigm-leakage · effort M · confidence high**
Stage bakes in stochastic and risk configuration — the v0.12 claim holds verbatim at the pin. Anchor drift only: `Stage` moved from :281 (v0.12) to the pin's line; `state_config`, `risk_config` and `scenario_config` are all still declared on it.

- **Station:** alignment (Part-I re-verification; handoffs from core-io)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-core/src/model/temporal.rs::Stage`, `crates/cobre-core/src/model/temporal.rs:314`, `crates/cobre-core/src/model/temporal.rs:317`, `crates/cobre-core/src/model/temporal.rs:320`
- **Evidence:** (1) `crates/cobre-core/src/model/temporal.rs::Stage` — container (:282 at the ticket's a136840d) (2) `crates/cobre-core/src/model/temporal.rs:314` — claimed field state_config (:309 at the ticket's a136840d) (3) `crates/cobre-core/src/model/temporal.rs:317` — claimed field risk_config (:312 at the ticket's a136840d) (4) `crates/cobre-core/src/model/temporal.rs:320` — claimed field scenario_config (:315 at the ticket's a136840d) (5) Changed since v0.12: Anchor drift only: `Stage` moved from :281 (v0.12) to the pin's line; `state_config`, `risk_config` and `scenario_config` are all still declared on it.
- **Fix-shape:** Phase 1 purification: `StageRiskConfig` (CVaR), `ScenarioSourceConfig` and `inflow_lags` leave `Stage` for an SDDP per-node config; prose only.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1465 — IV.5 (`StageRiskConfig`, `ScenarioSourceConfig` and `inflow_lags` leave `Stage`); provisional until the alignment adjudication)
- **Part-I:** I.3-2 — disposition keep, phase 1; station verdicts: core-io: keep.
- **Related register ids:** CD-044, CD-050 (station findings that cross-reference this item; none is this claim, so none is a dup-of)

**CD-121 · Sev C · paradigm-leakage · effort S · confidence high**
`HorizonGraph` (the type `PolicyGraph` became in bd2cd4c0) still frames the horizon as discount-rate plus cyclic convergence (`graph_type: PolicyGraphType`, `annual_discount_rate`), and the residual `System::policy_graph` field name and the `PolicyGraphType` enum keep the policy vocabulary; the forward/backward-traversal framing is gone (zero forward|backward hits in horizon.rs).

- **Station:** alignment (Part-I re-verification; handoffs from core-io)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-core/src/model/horizon.rs::HorizonGraph`, `crates/cobre-core/src/model/horizon.rs:24`, `crates/cobre-core/src/model/horizon.rs:29`, `crates/cobre-core/src/model/temporal.rs::PolicyGraphType`, `crates/cobre-core/src/model/temporal.rs::Node`, `crates/cobre-core/src/system/mod.rs:96`
- **Evidence:** (1) `crates/cobre-core/src/model/horizon.rs::HorizonGraph` — the surviving type (renamed from PolicyGraph in bd2cd4c0) (2) `crates/cobre-core/src/model/horizon.rs:24` — graph_type field (3) `crates/cobre-core/src/model/horizon.rs:29` — discount-rate framing (4) `crates/cobre-core/src/model/temporal.rs::PolicyGraphType` — residual policy vocabulary (enum) (5) `crates/cobre-core/src/model/temporal.rs::Node` — the node type (:559 at the ticket's a136840d) (6) `crates/cobre-core/src/system/mod.rs:96` — residual System::policy_graph field name (:92 at the ticket's a136840d) (7) `PolicyGraph` resolves nowhere at the pin; renamed to `HorizonGraph` in bd2cd4c0 (rename evidence, not anchor-missing) (8) Changed since v0.12: The v0.12 type name resolves nowhere at the pin (only cobre-io's `RawPolicyGraph` loader shape carries the word); dispositioned on the rename evidence, never as anchor-missing.
- **Fix-shape:** Phase 1 purification settles the residual naming (`PolicyGraphType`, `System::policy_graph`) with the horizon model; `HorizonGraph` itself stays in the purified core; prose only.
- **Alignment:** neutral (plans/generalizing/beyond-sddp-generalization.md:1371 — IV.1 keeps `HorizonGraph` in the purified `cobre-core`; no roadmap row names the residual-vocabulary rename, so the row neither advances nor conflicts; provisional until the alignment adjudication)
- **Part-I:** I.3-3 — disposition sharpen, phase 1; station verdicts: core-io: sharpen.

**CD-122 · Sev C · paradigm-leakage · effort M · confidence high**
Two of the three v0.12 warm-start fields remain on `InitialConditions` — `past_anticipated_commitments` and `past_defluences`; `past_inflows` is gone (closed by the windowed-inflow work, c1a360ad, released in v0.13.0), and `recent_observations` was added as paradigm-neutral realized data.

- **Station:** alignment (Part-I re-verification; handoffs from core-io)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-core/src/constraints/initial_conditions.rs::InitialConditions`, `crates/cobre-core/src/constraints/initial_conditions.rs:193`, `crates/cobre-core/src/constraints/initial_conditions.rs:206`, `crates/cobre-core/src/constraints/initial_conditions.rs:198`
- **Evidence:** (1) `crates/cobre-core/src/constraints/initial_conditions.rs::InitialConditions` — container (2) `crates/cobre-core/src/constraints/initial_conditions.rs:193` — surviving field past_anticipated_commitments (3) `crates/cobre-core/src/constraints/initial_conditions.rs:206` — surviving field past_defluences (4) `crates/cobre-core/src/constraints/initial_conditions.rs:198` — field added since v0.12 (5) Changed since v0.12: `past_inflows` has zero production references at the pin (it survives only as the rejection test `test_legacy_past_inflows_field_is_rejected` in cobre-io); the container moved from :183 to the pin's line.
- **Fix-shape:** Phase 1 purification: the anticipated-commitment and defluence seeds follow the commitment and routing state respectively; prose only.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1467 — IV.5 (`InitialConditions`' PAR-lag/defluence seeds follow the uncertainty and routing state); provisional until the alignment adjudication)
- **Part-I:** I.3-4 — disposition sharpen, phase 1; station verdicts: core-io: sharpen.
- **Field dispositions:** `past_inflows` retire (closed by c1a360ad, v0.13.0 (CHANGELOG.md: BREAKING — `past_inflows` is gone from `initial_conditions.json`)); `past_anticipated_commitments` keep; `past_defluences` keep

**CD-123 · Sev C · paradigm-leakage · effort M · confidence high**
`training_event.rs` (938 lines) still lives in `cobre-core` with `TrainingEvent`'s `ForwardPassComplete` / `BackwardPassComplete` / `ConvergenceUpdate` variants and allreduce fields, and its placement rationale — interface crates consume events without depending on the algorithm crate — is defeated at the pin: the real consumers are `cobre-cli` and `cobre-python`, both of which already depend on `cobre-sddp`, while the named `cobre-tui` and `cobre-mcp` consumers are 1-line and 6-line stubs. The lone lexical near-miss ('Active cuts after budget enforcement' on `active_after_budget`) stands; the gate's word-boundary pattern cannot see the plural.

- **Station:** alignment (Part-I re-verification; no owning station — dispositioned directly from the baseline tree)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-core/src/constraints/training_event.rs::TrainingEvent`, `crates/cobre-core/src/constraints/training_event.rs::StageRowSelectionRecord`, `crates/cobre-core/src/constraints/training_event.rs:125`, `crates/cobre-core/src/constraints/training_event.rs:126`, `crates/cobre-core/src/constraints/training_event.rs:21`, `crates/cobre-core/src/lib.rs:67`, `crates/cobre-cli/Cargo.toml:23`, `crates/cobre-python/Cargo.toml:24`, `crates/cobre-tui/src/lib.rs:1`, `crates/cobre-mcp/src/main.rs:1`
- **Evidence:** (1) `crates/cobre-core/src/constraints/training_event.rs::TrainingEvent` — the event enum (2) `crates/cobre-core/src/constraints/training_event.rs::StageRowSelectionRecord` — the struct carrying the near-miss field (3) `crates/cobre-core/src/constraints/training_event.rs:125` — the lone lexical near-miss doc comment (4) `crates/cobre-core/src/constraints/training_event.rs:126` — field active_after_budget (5) `crates/cobre-core/src/constraints/training_event.rs:21` — the placement rationale in the module doc (6) `crates/cobre-core/src/lib.rs:67` — the crate-root re-export (:65 at the ticket's a136840d) (7) `crates/cobre-cli/Cargo.toml:23` — cobre-cli depends on the algorithm crate (8) `crates/cobre-python/Cargo.toml:24` — cobre-python depends on the algorithm crate (9) `crates/cobre-tui/src/lib.rs:1` — the named cobre-tui consumer (stub) (10) `crates/cobre-mcp/src/main.rs:1` — the named cobre-mcp consumer (stub) (11) Changed since v0.12: Line count and the :125 near-miss unchanged; the consumer set the module doc names never materialised beyond stubs.
- **Fix-shape:** Phase 1: `training_event.rs` moves to a telemetry module the verticals emit into (not core); the :125 doc comment is a rename rider on that move; prose only.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1466 — IV.5 (`training_event.rs` moves to a telemetry module the verticals emit into); provisional until the alignment adjudication)
- **Part-I:** I.3-5 — disposition sharpen, phase 1; station verdicts: none (no owning station).
- **Measurements:** 109 = TrainingEvent occurrences in crates/cobre-cli (`git grep -o TrainingEvent 077dbe2c -- crates/cobre-cli | wc -l`); 14 = TrainingEvent occurrences in crates/cobre-python (`git grep -o TrainingEvent 077dbe2c -- crates/cobre-python | wc -l`); 0 = TrainingEvent occurrences in crates/cobre-tui (`git grep -o TrainingEvent 077dbe2c -- crates/cobre-tui | wc -l`); 0 = TrainingEvent occurrences in crates/cobre-mcp (`git grep -o TrainingEvent 077dbe2c -- crates/cobre-mcp | wc -l`); 1 = lines in crates/cobre-tui/src/lib.rs (`git show 077dbe2c:crates/cobre-tui/src/lib.rs | wc -l`); 6 = lines in crates/cobre-mcp/src/main.rs (`git show 077dbe2c:crates/cobre-mcp/src/main.rs | wc -l`); 938 = lines in crates/cobre-core/src/constraints/training_event.rs (`git show 077dbe2c:crates/cobre-core/src/constraints/training_event.rs | wc -l`)

**CD-124 · Sev B · paradigm-leakage · effort L · confidence high**
The gate exemption the v0.12 claim leaned on has been withdrawn (`EXCLUDED_FILES=()`; the four policy files are scanned like any other infra source and the gate exits 0), yet the structural coupling survives the lexical retirement: `PolicyCutRecord` and `StageCutsPayload` evade the gate's word-boundary pattern, the `StageCuts` FlatBuffers root table lives in a schema the gate never reads (it scans only `*.rs` under five src/ trees), and the on-disk artifact is still a `cuts/` directory. The persisted value-function format is still cut-shaped.

- **Station:** alignment (Part-I re-verification; handoffs from core-io, build-ci)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-io/src/output/policy/records.rs::PolicyCutRecord`, `crates/cobre-io/src/output/policy/records.rs::StageCutsPayload`, `crates/cobre-io/schemas/policy.fbs:163`, `crates/cobre-io/src/output/policy/checkpoint.rs:323`, `scripts/ci/check-infra-genericity.sh:74`
- **Evidence:** (1) `crates/cobre-io/src/output/policy/records.rs::PolicyCutRecord` — surviving coupling (:98 at the ticket's a136840d) (2) `crates/cobre-io/src/output/policy/records.rs::StageCutsPayload` — surviving coupling (:176 at the ticket's a136840d) (3) `crates/cobre-io/schemas/policy.fbs:163` — the StageCuts root table the gate never reads (:140 at the ticket's a136840d) (4) `crates/cobre-io/src/output/policy/checkpoint.rs:323` — the on-disk cuts/ directory (:212 at the ticket's a136840d) (5) `scripts/ci/check-infra-genericity.sh:74` — the withdrawn exemption (6) Changed since v0.12: Exemption retired by the script itself (header and the comment block above the empty array); the FlatBuffers vocabulary moved to `AffinePiece` and the provenance carrier to a `CheckpointManifest` table — exit 0 is not proof the claim retired.
- **Fix-shape:** Generalize the persisted artifact behind an engine-neutral policy artifact (the checkpoint format row of the target crate table); prose only, no fix executed here.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1376 — IV.1 `cobre-io` row (checkpoint format generalized from SDDP cut-records to an engine-neutral policy artifact); provisional until the alignment adjudication)
- **Re-raise-of:** the 2026-07-23 sanctioned genericity-gate exemption for `output/policy/{mod,records,codec,checkpoint}.rs` (roadmap I.3 parenthetical; retired in the gate's own header, the emptied `EXCLUDED_FILES` sanctioned as the enforced state at the build-ci station 2026-09-19). Live again because the lexical sanction was retired while the structural coupling it covered was not.
- **Part-I:** I.3-6 — disposition sharpen, phase 1; station verdicts: core-io: sharpen; build-ci: claim-stale (exemption parenthetical only; no disposition set).
- **Measurements:** 0 = genericity gate exit code (worktree) (`bash scripts/ci/check-infra-genericity.sh; echo $?`); 0 = .fbs files under the scanned crates/cobre-io/src tree (`git grep -l . 077dbe2c -- 'crates/cobre-io/src/**/*.fbs' | wc -l`)
- **Related register ids:** CD-061, PD-015, OD-024, TD-021, CD-099 (station findings that cross-reference this item; none is this claim, so none is a dup-of)

**CD-125 · Sev B · paradigm-leakage · effort M · confidence high**
The config leak moved layers rather than closing: `cobre_io::Config.training` is still SDDP-shaped (tree_seed, stopping_rules, cut_selection, the forward-pass mode, per-phase backward/forward solver profiles, the backward scheduler selector), the real config-to-domain conversion is still `StudyParams::from_config` in `cobre-sddp`, and the MPI-broadcastable subset `BroadcastConfig` is now a `pub(crate)` type inside `cobre-cli` (grown by backward_scheduler, cost_scale_factor, three per-phase solver profiles and the boundary requirements) while `cobre-io` exposes only the generic `BroadcastScalarParameter`. The front ends reach the conversion through two paths (run via `BroadcastConfig::from_config`, validate directly and via `StudySetup::new_with_boundary_requirements`).

- **Station:** alignment (Part-I re-verification; handoffs from core-io, sddp, cli-python)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-io/src/config/mod.rs:77`, `crates/cobre-sddp/src/setup/params.rs:186`, `crates/cobre-cli/src/commands/broadcast.rs:87`, `crates/cobre-io/src/broadcast.rs::BroadcastScalarParameter`, `crates/cobre-sddp/src/setup/params.rs::StudyParams`, `crates/cobre-io/src/config/mod.rs::Config`
- **Evidence:** (1) `crates/cobre-io/src/config/mod.rs:77` — Config.training (core-io fragment) (2) `crates/cobre-sddp/src/setup/params.rs:186` — StudyParams::from_config (sddp fragment) (3) `crates/cobre-cli/src/commands/broadcast.rs:87` — BroadcastConfig, now pub(crate) in cobre-cli (cli-python fragment) (4) `crates/cobre-io/src/broadcast.rs::BroadcastScalarParameter` — the only broadcast type cobre-io exposes (:48 at the ticket's a136840d) (5) `crates/cobre-sddp/src/setup/params.rs::StudyParams` — the conversion's owner (6) `crates/cobre-io/src/config/mod.rs::Config` — the config type (7) Changed since v0.12: `ConstructionConfig` and `into_construction_config` were deleted (4075c4e8); `StudyParams::from_config` now also captures export_states; the `Phase` enum stayed in `cobre-sddp` and `cobre-solver` stayed phase-agnostic, so the containment the item notes still holds.
- **Fix-shape:** Phase 0a: one config projection behind the study admission gate; CLI and Python both wire through it instead of hand-mirroring a broadcast subset; prose only.
- **Alignment:** advances-0a (plans/generalizing/beyond-sddp-generalization.md:1597 — V.1 Phase 0 (the `study`/config schema, the typed admission gate and per-engine solver-profile scoping land first and alone); provisional until the alignment adjudication)
- **Part-I:** I.3-7 — disposition sharpen, phase 0a; station verdicts: core-io: sharpen (Config.training half); sddp: sharpen (params.rs half; CD-004 carrier); cli-python: sharpen (front-end half).
- **Merged from:** core-io — Config.training (`crates/cobre-io/src/config/mod.rs::training`); sddp — StudyParams::from_config (`crates/cobre-sddp/src/setup/params.rs::from_config`); cli-python — BroadcastConfig (`crates/cobre-cli/src/commands/broadcast.rs::BroadcastConfig`)
- **Related register ids:** CD-004, CD-082, CD-051, OD-020, OD-043 (station findings that cross-reference this item; none is this claim, so none is a dup-of)

**CD-126 · Sev B · paradigm-leakage · effort M · confidence high**
The leakage reaches one layer below cobre-core: StageTemplate — the v0.12 claim holds verbatim at the pin. Nothing structural: the five fields and their doc vocabulary are unchanged; `n_dual_relevant`'s doc comment still claims it equals n_state while its only writer hard-codes 0 (documented drift rider).

- **Station:** alignment (Part-I re-verification; handoffs from solver-comm)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-solver/src/types.rs::StageTemplate`, `crates/cobre-solver/src/types.rs:270`, `crates/cobre-solver/src/types.rs:278`, `crates/cobre-solver/src/types.rs:287`, `crates/cobre-solver/src/types.rs:290`, `crates/cobre-solver/src/types.rs:297`, `crates/cobre-solver/src/freeze.rs:158`
- **Evidence:** (1) `crates/cobre-solver/src/types.rs::StageTemplate` — the L0 container (2) `crates/cobre-solver/src/types.rs:270` — field n_state (3) `crates/cobre-solver/src/types.rs:278` — field n_transfer (4) `crates/cobre-solver/src/types.rs:287` — field n_dual_relevant (5) `crates/cobre-solver/src/types.rs:290` — field n_hydro (6) `crates/cobre-solver/src/types.rs:297` — field max_par_order (7) `crates/cobre-solver/src/freeze.rs:158` — the one production propagation site (8) Changed since v0.12: Nothing structural: the five fields and their doc vocabulary are unchanged; `n_dual_relevant`'s doc comment still claims it equals n_state while its only writer hard-codes 0 (documented drift rider). (9) Merge rule: Item-level keep: all five fields are still declared on `StageTemplate`, so the v0.12 claim holds verbatim; the solver-comm station's sharper evidence (write-only fields, one production propagation site, multistage vocabulary in L0 doc prose) narrows nothing and is recorded as evidence, and its per-field `retire` verdicts describe what the Phase-1 fix-shape does to each field, not a closure at the pin — a field-level disposition inside a surviving claim is subordinate to the claim and is never promoted to an item-level retire.
- **Fix-shape:** Phase 1: `StageTemplate` sheds the five fields to the layer that owns the multistage layout (`StateSpace`, `StageRowLayout` on the kernel/engine side), leaving the L0 container pure CSC; prose only.
- **Alignment:** advances-1 (plans/generalizing/beyond-sddp-generalization.md:1471 — IV.5 (`StageTemplate` sheds n_state/n_transfer/n_dual_relevant/n_hydro/max_par_order) and the IV.1 `cobre-solver` purified-plus-capabilities row; provisional until the alignment adjudication)
- **Part-I:** I.3-8 — disposition keep, phase 1; station verdicts: solver-comm: sharpen (claim holds and is sharper: doc-prose vocabulary, one propagation site, all five fields write-only).
- **Field dispositions (what the Phase-1 fix-shape does to the field (the template sheds it); not a closure at the pin):** `n_state` retire; `n_transfer` retire; `n_dual_relevant` retire; `n_hydro` retire; `max_par_order` retire
- **Related register ids:** CD-079, TD-045 (station findings that cross-reference this item; none is this claim, so none is a dup-of)

**CD-127 · Sev B · paradigm-leakage · effort L · confidence high**
The run subcommand is still SDDP-hydrothermal with no algorithm-selection seam: the lifecycle is the train-then-simulate two-phase shape gated on training_enabled and n_scenarios, the run is typed on the concrete `cobre_sddp::StudySetup` with inherent train/simulate methods, and config conversion reaches `StudyParams::from_config` through `BroadcastConfig::from_config`. The import figure is measured, not estimated: 79 `use cobre_sddp` lines / 95 `cobre_sddp::` references / 46 distinct symbols / 11 files at the ticket's scaffold pin and 81 / 97 / 48 / 10 at the register pin (each figure with its command below; the cli-python station's 70 / 97 / 88 / 10 use different commands and are recorded, not discarded). The help text no longer names SDDP (retired against main.rs), `report` and `summary` no longer exist (797ba443), `validate` is SDDP-shaped end to end, and the output tree gained `generic_constraints`. The CLI and Python output paths invoke different writer sets (the writer-mirror measurement below).

- **Station:** alignment (Part-I re-verification; handoffs from cli-python)
- **Baseline:** `077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c`
- **Anchors:** `crates/cobre-cli/src/commands/run/outputs.rs::write_training_outputs`, `crates/cobre-cli/src/commands/run/outputs.rs::write_simulation_outputs`, `crates/cobre-python/src/run.rs:1406`, `crates/cobre-cli/src/commands/broadcast.rs:143`, `crates/cobre-cli/src/commands/run/mod.rs:166`, `crates/cobre-cli/src/main.rs::Command`
- **Evidence:** (1) `crates/cobre-cli/src/commands/run/outputs.rs::write_training_outputs` — the CLI output-writer layer (2) `crates/cobre-cli/src/commands/run/outputs.rs::write_simulation_outputs` — the CLI output-writer layer (3) `crates/cobre-python/src/run.rs:1406` — the Python output path (:1362 at the ticket's a136840d) (4) `crates/cobre-cli/src/commands/broadcast.rs:143` — the run path's conversion call (5) `crates/cobre-cli/src/commands/run/mod.rs:166` — the two-phase lifecycle gate (6) `crates/cobre-cli/src/main.rs::Command` — the command surface (:87 at the ticket's a136840d) (7) Changed since v0.12: One sub-claim retired (the 'train an SDDP policy' help text), two hold verbatim, five sharpened by the cli-python station; report/summary removed in 797ba443.
- **Fix-shape:** Phase 0a: the `Engine` enum and dispatch in `cobre-cli` / `cobre-python` (the seam covers the full command surface) plus the shared output-orchestration entry point in `cobre-io`; prose only.
- **Alignment:** advances-0a (plans/generalizing/beyond-sddp-generalization.md:1577 — V.1 Phase 0 (introduce the `Engine` enum + dispatch in `cobre-cli`/`cobre-python`; the shared output-orchestration entry point); provisional until the alignment adjudication)
- **Part-I:** I.5 — disposition sharpen, phase 0a; station verdicts: cli-python: 8 sub-claims: I.5-1a retire, I.5-1b keep, I.5-2 keep, I.5-3..7 sharpen.
- **Measurements:** 79 = `use cobre_sddp` lines (ticket scaffold pin a136840d) (`git grep -n 'use cobre_sddp' a136840d -- crates/cobre-cli/src | wc -l`); 95 = `cobre_sddp::` references (ticket scaffold pin a136840d) (`git grep -o 'cobre_sddp::' a136840d -- crates/cobre-cli/src | wc -l`); 46 = distinct `cobre_sddp::<symbol>` (ticket scaffold pin a136840d) (`git grep -hoE 'cobre_sddp::[A-Za-z_][A-Za-z0-9_]*' a136840d -- crates/cobre-cli/src | sort -u | wc -l`); 11 = files naming cobre_sddp (ticket scaffold pin a136840d) (`git grep -l cobre_sddp a136840d -- crates/cobre-cli/src | wc -l`); 81 = `use cobre_sddp` lines (register pin 077dbe2c) (`git grep -n 'use cobre_sddp' 077dbe2c -- crates/cobre-cli/src | wc -l`); 97 = `cobre_sddp::` references (register pin 077dbe2c) (`git grep -o 'cobre_sddp::' 077dbe2c -- crates/cobre-cli/src | wc -l`); 48 = distinct `cobre_sddp::<symbol>` (register pin 077dbe2c) (`git grep -hoE 'cobre_sddp::[A-Za-z_][A-Za-z0-9_]*' 077dbe2c -- crates/cobre-cli/src | sort -u | wc -l`); 10 = files naming cobre_sddp (register pin 077dbe2c) (`git grep -l cobre_sddp 077dbe2c -- crates/cobre-cli/src | wc -l`); 70 = `^use cobre_sddp` lines (cli-python station, register pin) (`cli-python partI-handoff.json I.5-3 survivingClaim (station command)`); 97 = `cobre_sddp::` occurrences over 10 files (cli-python station, register pin) (`cli-python partI-handoff.json I.5-3 survivingClaim (station command)`); 88 = distinct imported leaf symbols, re-exports included (cli-python station, register pin) (`cli-python partI-handoff.json I.5-3 survivingClaim (station command)`); 16 vs 24 = writer names invoked in crates/cobre-cli/src/commands/run/outputs.rs vs crates/cobre-python/src/run.rs (register pin) (`git show 077dbe2c:<file> | grep -oE '\b[a-z_]*write[a-z_]*\(' | sort -u`)
- **Writer mirror:** CLI-only `write_line`, `write_simulation_outputs`, `write_training_outputs`; Python-only `write`, `write_evaporation_models_if_any`, `write_fixed_delivery_if_any`, `write_fpha_deviation_points_if_any`, `write_fpha_hyperplanes_if_any`, `write_generic_constraint_echo_if_any`, `write_hydro_model_summary`, `write_provenance_report`, `write_scaling_report`, `write_scenario`, `write_training_artifacts`; shared 13.
