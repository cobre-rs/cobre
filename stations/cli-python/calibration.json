{
  "station": "cli-python",
  "baseline": "077dbe2c287b92c2d0c6a12d5f67c2c0cb83c39c",
  "calibratedAt": "2026-09-18",
  "idFloorsSeen": {
    "CD": 89,
    "PD": 49,
    "OD": 43,
    "TD": 57
  },
  "order": "(subStation, lens, candidateRef)",
  "sectionTitle": "★ STATION 6 — cobre-cli / cobre-python / facade (2026-09, baseline)",
  "scaffoldHeading": "## ★ QUALITY EVALUATION (2026-09, baseline a136840d) — cli-python",
  "l2Rule": {
    "scope": "every fix-shape + survivingClaim of every minted row (not only the risky ones); writerScope marks rows touching the writer double-mirror",
    "conditions": [
      "homes the shared output helpers in cobre-sddp (L3)",
      "hoists them into a cobre-cli-local module (cobre-python cannot reach it, so the double mirror survives)",
      "pushes the Engine enum or its dispatch below L4",
      "makes cobre-cli or cobre-python depend on a second engine",
      "introduces a one-consumer seam"
    ],
    "hits": 0
  },
  "assigned": [
    {
      "candidateRef": "S6a-architecture-00",
      "id": "CD-089",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "the simulate-arm enablement gate is written twice in non-equivalent form across the two L4 entry points and agrees only through the cobre-sddp normalization, with no cross-boundary test outside the 1dtoy golden — a cross-front-end hand-mirror with a bounded, golden-guarded blast radius: Sev B on the CD-025 precedent (the parity goldens bound the drift; nothing structurally prevents it)",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V §V.1 Phase 0 (the L4 Engine seam owns the run-phase plan once; both front ends consume it) / Part IV §IV.4 (the Engine enum and its dispatch live only at L4)",
      "defenderAlignmentHint": "advances-0a",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": "I.5",
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "C1 (kept, narrowed): this verdict dismisses the no-op arm as per-front-end presentation, so if the owner holds that the CLI stderr line and the bindings' zeroed RunSummary are meant to be one behaviour, the surviving claim widens to cover that arm; both are public surface and neither writes a file, so the file-set parity test cannot decide it."
      ],
      "survivingClaim": "Only the simulate-arm gate is genuinely written twice in non-equivalent form: execute_inner gates on the normalized setup.simulation_config.n_scenarios > 0 alone (run/mod.rs:224 and :227) while Study::simulation_enabled re-conjoins config.simulation.enabled (cobre-python study.rs:255), so the two arm selections agree only through the cobre-sddp setup/params.rs:253-261 normalization and no cross-boundary test asserts that agreement outside the train-then-simulate arm the examples/1dtoy golden exercises. The title's claim that each side reads enablement from a different source does NOT survive for the training gate (broadcast.rs:212 projects config.training.enabled verbatim, pinned by broadcast_config_propagates_training_enabled at broadcast.rs:737-757), and the no-op arm's stderr line versus zeroed RunSummary is per-front-end presentation, not a divergent phase plan.",
      "title": "The run lifecycle is hand-mirrored across both L4 entry points with no Engine seam: two three-branch phase plans, each reading enablement from a different source",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/run/mod.rs",
          "symbol": "execute_inner"
        },
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "run_via_study"
        }
      ],
      "fixShape": "Give the phase plan one owner at L4 and one query below it: the engine answers which phases a study runs as a small owned value (trained-then-simulated, simulate-from-stored-policy, nothing), and both entry points consume that answer instead of re-deriving it from `training_enabled` plus `n_scenarios` on one side and `config.training.enabled` plus `config.simulation.enabled` on the other. The `Engine` enum and the dispatch stay in cobre-cli and cobre-python (rule 13, Part IV.4); nothing engine-selecting moves down, and the query must be a value the engine already knows rather than a new trait with one implementation (rule 13, V.0). The no-op arm keeps both observable behaviours as presentation, not as plan: the CLI's stderr line and the bindings' zeroed `RunSummary` are public surface. Byte-neutrality: the query only reclassifies predicates already evaluated, so `crates/cobre-python/tests/test_cli_python_determinism_parity.py` (examples/1dtoy, whole tree value-for-value) and `tests/test_cli_python_file_set_parity.py` must stay green unchanged; `scripts/ci/check_python_parity.py --min-shared 18` is untouched because no writer name moves, and the `cobre validate --json` single-object stdout contract is outside this seam.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/run/mod.rs | grep -n 'if training_enabled\\|n_scenarios > 0\\|nothing to do\\|is_root && !ctx.quiet'; git show 077dbe2c:crates/cobre-python/src/run.rs | sed -n '1419,1423p;1462,1470p'",
        "output": "166:    if training_enabled {\n224:        if setup.simulation_config.n_scenarios > 0 {\n227:    } else if setup.simulation_config.n_scenarios > 0 {\n230:    } else if ctx.is_root && !ctx.quiet {\n233:            .write_line(\"Training disabled, simulation disabled — nothing to do.\");\n---\n    if study.training_enabled() {\n        let policy = study.train_native(on_iteration)?;\n        let simulation = if should_simulate {\n            Some(study.simulate_native(&policy, None)?)\n    } else if should_simulate {\n        let policy = study.load_policy_native(None)?;\n        let simulation = Some(study.simulate_native(&policy, None)?);\n    } else {\n        Ok(RunSummary {\n            converged: false,\n            iterations: 0,\n            lower_bound: 0.0,",
        "reading": "Both entry points hard-code the same three-branch phase plan (train-then-maybe-simulate / simulate-from-stored-policy / neither) and neither owns it. The CLI branches on two different objects: `training_enabled`, lifted off the wire type at run/setup.rs:365, and `setup.simulation_config.n_scenarios > 0` at :224 and :227. `run_via_study` branches on `study.training_enabled()` and `study.simulation_enabled()`, which are `config.training.enabled` and `config.simulation.enabled && n_scenarios > 0`. The two predicate pairs agree only because cobre-sddp `setup/params.rs:253-262` forces `n_scenarios = 0` whenever `!config.simulation.enabled`; the agreement rests on an invariant three crates away, not on a shared decision, and nothing fails if one side is edited. The third arm already diverges observably: the CLI writes a stderr line, the bindings return a `RunSummary` with `converged: false, iterations: 0, lower_bound: 0.0`. A second engine means editing two phase plans, and the SDDP vocabulary sits in the CLI's own prose (run/mod.rs:1-2 module doc, clap `about` at :59), which is legal at L4 but is exactly the I.5 claim."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-architecture-05",
      "id": "CD-090",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a pure log-to-totals fold duplicated line for line in both front ends, its bit-for-bit caveat living as prose in one copy, while the owning crate already exports a sibling fold over the same type — a cross-crate hand-mirror with no shared owner: Sev B on the CD-025 precedent (bounded: two call sites over one shared type; the emitted totals are compared by the value golden)",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (a pure fold over cobre-sddp's SolverStatsLogEntry is L3-owned data shaping, not output orchestration — the L2 destination rule does not apply; the fix homes beside solver_stats.rs's existing fold) / Part IV §IV.4",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": [
          {
            "condition": "homes the shared output helpers in cobre-sddp (L3)",
            "why": "pattern present but the row is outside the writer-mirror scope — not output orchestration, so the L2 destination rule does not apply"
          }
        ]
      },
      "partIRef": "I.5",
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "Owner confirmation only, narrowed from the attacker's C6: confirm that this pure fold sits outside CD-025's writer set-plus-guards scope (mirror :587), so a cobre-sddp home next to its input types is not the held-for-owner L3 writer case. Evidence already on the record: solver_stats_log_to_rows (cobre-sddp solver_stats.rs:316) is shared by both front ends over the same type, and the cobre-sddp to cobre-io dependency direction rules out an L2 home."
      ],
      "survivingClaim": "The pure log-to-totals fold over a SolverStatsLogEntry slice is duplicated line for line at cobre-cli training.rs:262-294 and cobre-python run.rs:256-282, differing only by the CLI rank guard at training.rs:272-274, with no shared owner even though cobre-sddp solver_stats.rs:316 already exports a sibling pure fold over the same type that both front ends share; and the bit-for-bit total_lp_solves divergence caveat exists as prose only in the cobre-python copy (run.rs:252-255), while the dropped lower-bound solve time is documented only in the CLI copy (training.rs:276-277). The MetadataTrainingSolveStats assemblies are excluded from the claim: training.rs:240-248 and run.rs:318-326 legitimately differ in value source and parallelism semantics.",
      "title": "Training solver-stats aggregation is a third hand-mirror outside CD-025's anchor list: two otherwise identical folds differing only by the rank filter, with a bit-for-bit contract living as prose in one copy",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/run/training.rs",
          "symbol": "aggregate_solver_stats"
        },
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "aggregate_training_solve_stats"
        }
      ],
      "fixShape": "One fold next to its input types in cobre-sddp `src/solver_stats.rs`, over `&[SolverStatsLogEntry]`, with the rank filter as an explicit argument (this rank, or all ranks) so the MPI caller and the single-process caller differ by a parameter instead of by a copy. The `total_lp_solves` warning moves to the doc of that single fold and gets a named regression test, instead of surviving as prose in one of two copies. Rule 5's cobre-io (L2) destination is checked and does not bind here: it governs the writer hand-mirror, and an L2 home for this fold would force cobre-io to depend on an engine crate for `SolverStatsLogEntry`, which rule 13 forbids and Part IV.1 places out of bounds. Byte-neutrality: the fold must return the same five values for the same log, so `crates/cobre-python/tests/test_cli_python_determinism_parity.py` (examples/1dtoy, whole tree value-for-value) covers the single-rank case through the metadata it writes, the all-ranks argument must be the identity on a one-rank run, and `tests/test_cli_python_file_set_parity.py` plus `scripts/ci/check_python_parity.py --min-shared 18` are unaffected because no writer name changes.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/run/training.rs | sed -n '257,278p;286,293p'; git show 077dbe2c:crates/cobre-python/src/run.rs | sed -n '249,256p;264,270p;276,281p'",
        "output": "cli training.rs:\n/// Backward entries are allgatherv-replicated across ranks, so filtering by\n/// originating rank is required for the per-rank totals that `allreduce(Sum)`\n/// then sums correctly.\nfn aggregate_solver_stats(\n    stats_log: &[cobre_sddp::SolverStatsLogEntry],\n    my_rank: i32,\n) -> (u64, u64, u64, f64, f64) {\n...\n    for entry in stats_log {\n        if entry.rank != my_rank {\n            continue;\n        }\n...\n        forward_solve_ms / 1000.0,\n        backward_solve_ms / 1000.0,\npython run.rs:\n/// Fold the per-phase training solver-stats log into category totals, mirroring\n/// the CLI's `aggregate_solver_stats` shape. Solve times are ms→s.\n///\n/// `total_lp_solves` is intentionally NOT derived here: the CLI sources it from\n/// the per-iteration convergence records (`IterationRecord.lp_solves`), and the\n/// two sums can diverge for multi-stage cases — the caller must compute it from\n/// the convergence records to stay bit-for-bit identical to the CLI.\nfn aggregate_training_solve_stats(\n    stats_log: &[cobre_sddp::SolverStatsLogEntry],\n) -> (u64, u64, u64, f64, f64) {\n...\n    for entry in stats_log {\n        let delta = &entry.delta;\n...\n        forward_solve_ms / 1000.0,\n        backward_solve_ms / 1000.0,",
        "reading": "Same return tuple, same five accumulators, same `saturating_sub` for the retry count, same `match entry.phase` arms, same `/ 1000.0` conversion on both returns. The single behavioural difference is the CLI's `if entry.rank != my_rank { continue; }` at training.rs:272-274, required because backward entries are allgatherv-replicated (doc :258-261). The bindings' doc at run.rs:250 states outright that it mirrors \"the CLI's `aggregate_solver_stats` shape\", and its next paragraph is a bit-for-bit correctness contract kept alive as prose in the copy that does not implement it: `total_lp_solves` must be summed from the convergence records by the caller because the two sums diverge for multi-stage cases. Both sides also re-derive the same `MetadataTrainingSolveStats` assembly (training.rs:240-248 and run.rs:311-326). This is NOT the writer boundary CD-025 owns, and the destination differs accordingly: both input types are declared in cobre-sddp, `SolverStatsLogEntry` at src/solver_stats.rs:227 and `SolverStatsDelta` at :15. Call sites confirm both folds are live: training.rs:140 and run.rs:317 plus :2032."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-over-engineering-00",
      "id": "OD-043",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a postcard wire-type module filed under a namespace whose own doc says every child is a clap subcommand, with pub(crate) wider than its three consumers — placement and visibility only: Sev C; the projection CONTENT is CD-004's sanctioned seam and the cobre-io destination the attacker preferred does not survive (Infrastructure crate genericity)",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 owns the wire type; where it sits inside the crate is L4-internal)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [
        "CD-002"
      ],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": "CLAUDE.md § Hard Rules — Infrastructure crate genericity",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "BroadcastNodeGraph (crates/cobre-cli/src/commands/broadcast.rs:256): keep as a documented reserved seam or retire? Verified at the pin that its only references workspace-wide are its own two From impls (:273, :319) and the inline tests at :583-710 — no production caller — and the mirror's opposite precedent for an identically shaped zero-caller postcard pair sits at docs/design/reserved-seams-and-deferred-debt.md:683-697; the answer sets whether the relocation carries or drops roughly 110 lines of round-trip tests, and if kept, the E11 write-back owes it a reserved-seam entry it does not have today."
      ],
      "survivingClaim": "Placement only, and only inside cobre-cli: broadcast.rs is the single child of commands/ that declares neither a clap Args nor an execute while all three of its consumers sit under commands/run/, so the defensible residue is relocating it out of commands/ and narrowing pub(crate) to its actual consumer scope. The 1,088-line size framing does not survive (roughly 627 of those lines are the cfg(test) module, leaving about 460 production lines), and neither does the cobre-io destination the fixShape prefers.",
      "title": "commands/broadcast.rs is a 1,088-line postcard wire-type module filed under commands/, whose own parent doc says every module there is a clap subcommand",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/broadcast.rs",
          "symbol": "BroadcastConfig"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/setup.rs",
          "symbol": "broadcast_and_build_setup"
        }
      ],
      "fixShape": "Placement only; the Config-to-BroadcastConfig projection content is CD-004's and is not touched here. Minimal shape: relocate the module out of commands/ to the directory that owns its only consumers, crates/cobre-cli/src/commands/run/broadcast.rs, so commands/ holds subcommands and nothing else, and the pub(crate) visibility narrows to pub(super). Larger shape, and the one to prefer if CD-004 relocates the projection anyway: move the wire mirror beside the existing one in cobre-io (L2), which already owns the identical externally-tagged mirror for ScalarParameter and is depended on by both front ends; that variant is held until CD-004 lands because both mirrors read the same Config. Byte-neutrality: a module relocation and a visibility narrowing change no postcard field order, no From impl body and no writer call order, so the CLI-vs-Python determinism golden, the file-set parity test, the import-resolving parity gate at 18 shared names and the cobre validate --json single-object stdout contract are all untouched.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/mod.rs; git grep -ln 'broadcast::' 077dbe2c -- crates/cobre-cli/src; git show 077dbe2c:crates/cobre-io/src/broadcast.rs | sed -n '1,12p'",
        "output": "//! Subcommand implementations for the `cobre` binary.\n//!\n//! Each module pairs a clap-derived `Args` struct with an `execute` function.\n\npub(crate) mod broadcast;\npub mod init;\npub mod run;\npub mod schema;\npub mod validate;\npub mod version;\n--\ncrates/cobre-cli/src/commands/run/mod.rs\ncrates/cobre-cli/src/commands/run/policy.rs\ncrates/cobre-cli/src/commands/run/setup.rs\n--\n//! Postcard-safe mirror types for [`ScalarParameter`], used to broadcast\n//! scalar parameters over MPI.\n... [`BroadcastScalarParameter`],\n//! [`BroadcastParameterKind`] and [`BroadcastComputedParameter`] mirror them\n//! with externally-tagged encoding instead, convertible via `From` in both\n//! directions. This is the same pattern as `BroadcastConfig` in `cobre-cli`,\n//! used for [`crate::Config`].",
        "reading": "commands/mod.rs states the module contract for that directory: each module pairs a clap-derived Args struct with an execute function. broadcast is the only pub(crate) module of the six and satisfies neither half: its declaration census is 3 enums (BroadcastStoppingRule 4 variants, BroadcastStoppingMode 2, BroadcastBackwardScheduler 2), 3 structs (BroadcastConfig, BroadcastOpeningTree, BroadcastNodeGraph), 4 From impls, 2 free functions (stopping_rules_from_broadcast, broadcast_value) and 19 inline tests, with no Args type and no execute. Its only three consumers are commands/run/mod.rs, commands/run/policy.rs and commands/run/setup.rs, i.e. every consumer sits one directory down. The same wire-mirror pattern already has an L2 home: cobre-io/src/broadcast.rs holds the postcard mirror for ScalarParameter and its module doc names the cobre-cli copy as the same pattern, so the module is misfiled rather than novel.",
        "reading_note": null
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-over-engineering-01",
      "id": "OD-044",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one field of a ten-field args struct duplicates a sub-borrow of another field at its single construction site, read at four cobre-cli-internal sites — Sev C",
      "category": "over-parameterization",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4-internal plumbing)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": true,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Exactly one field of WriteTrainingArgs is redundant at outputs.rs:52 — a sub-borrow of the pub StudySetup field the struct already carries, which the cobre-python mirror already reads through setup at run.rs:598/:602/:619/:683 — and the residue is a cobre-cli-internal pub(super) redundancy across four reader sites with no cross-crate surface, no emitted byte effect and no hypothetical-second-consumer seam; the ten-field struct itself and its other nine fields are conceded justified, since none of them duplicates another.",
      "title": "WriteTrainingArgs carries hydro_models next to setup, and at its one construction site the field is literally &setup.hydro_models",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/run/outputs.rs",
          "symbol": "WriteTrainingArgs"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/mod.rs",
          "symbol": "execute_inner"
        }
      ],
      "fixShape": "Drop the hydro_models field and read args.setup.hydro_models at the four sites in write_training_outputs; the change stays inside cobre-cli (L4) and crosses no crate boundary. Ordering note: CD-025 will move the shared output entry point to cobre-io (L2), and this argument struct is the thing that moves, so removing the redundant field first reduces the surface that has to be designed at the L2 boundary rather than carrying the duplication across it. Byte-neutrality: the same PrepareHydroModelsResult is borrowed from the same StudySetup, so every row set, every guard condition and the writer call order are unchanged; the CLI-vs-Python determinism golden, the file-set parity test, the parity gate at 18 shared names and the cobre validate --json contract see no difference.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/run/outputs.rs | sed -n '44,55p'; git show 077dbe2c:crates/cobre-cli/src/commands/run/mod.rs | sed -n '190,200p'; git grep -n 'hydro_models' 077dbe2c -- crates/cobre-cli/src/commands/run",
        "output": "pub(super) struct WriteTrainingArgs<'a> {\n    pub(super) output_dir: &'a Path,\n    pub(super) system: &'a System,\n    pub(super) config: &'a Config,\n    pub(super) training_output: &'a TrainingOutput,\n    pub(super) setup: &'a StudySetup,\n    pub(super) training_result: &'a TrainingResult,\n    pub(super) output_ctx: &'a OutputContext,\n    pub(super) hydro_models: &'a PrepareHydroModelsResult,\n    pub(super) quiet: bool,\n    pub(super) stderr: &'a Term,\n}\n--\n            write_training_outputs(&WriteTrainingArgs {\n                ...\n                setup: &setup,\n                training_result: &training.result,\n                output_ctx: &training_ctx,\n                hydro_models: &setup.hydro_models,\n--\nrun/outputs.rs:52:    pub(super) hydro_models: &'a PrepareHydroModelsResult,\nrun/outputs.rs:90:    if !args.hydro_models.fpha_export_rows.is_empty() {\nrun/outputs.rs:95:        write_fpha_hyperplanes(&fpha_path, &args.hydro_models.fpha_export_rows)\nrun/outputs.rs:101:    let evaporation_rows = build_evaporation_model_rows(args.hydro_models, args.system);\nrun/outputs.rs:113:        let deviation_point_rows = args.hydro_models.fpha_deviation_point_rows.as_slice();\nrun/setup.rs:535:    let hydro_summary = build_hydro_model_summary(&setup.hydro_models, system);",
        "reading": "The 10-field argument struct carries both setup and hydro_models, and the sole construction site (run/mod.rs, inside execute_inner) fills them as setup: &setup and hydro_models: &setup.hydro_models, so one field is a second borrow of a field of the other. The four readers are outputs.rs:90, :95, :101 and :113; each could read args.setup.hydro_models, which run/setup.rs:535 already does in the same crate. Grep over the whole crate shows no other construction site and no test constructs WriteTrainingArgs, so the extra field has exactly one producer and one shape."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-over-engineering-04",
      "id": "OD-045",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two copied two-lint cast suppressions where one lint cannot fire on usize operands and the other disappears under the checked idiom the parent module already uses — avoidable suppressions at two sites: Sev C (OD-009 class: a retained wrapper/suppression under the census)",
      "category": "redundant-wrapper",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4-internal)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": ".claude/rules/comments.md D4 - Rationale above suppression",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "The committed mirror's allow census (docs/design/reserved-seams-and-deferred-debt.md:1324-1330) asserts that every Load-bearing numeric-cast and needless_pass_by_value allow carries a // Rationale: comment; twelve cast sites in this cell carry none, so the owner must decide whether the E11 write-back corrects that census prose to match D4's narrower closed list, or whether D4 is extended to cast lints (which would make the twelve a real rule gap rather than a style inconsistency)."
      ],
      "survivingClaim": "At exactly two sites, training.rs:191 and simulation.rs:173, the copied two-lint cast suppression is over-broad and avoidable rather than merely unannotated: both operands are usize (RunContext.n_threads at run/mod.rs:94, Communicator::size at cobre-comm traits.rs:225), so cast_sign_loss cannot fire there and is earned only at the f64-to-u64 site training.rs:166, while the truncation half disappears under the checked idiom the parent module already uses for the same n_threads at run/mod.rs:297. The 13-site count and the run.rs:1405 site do not survive.",
      "title": "Thirteen production #[allow] suppressions in the cell carry no written rationale, including a verbatim-duplicated cast in the two phase files and one whose identical twin in the same file is documented",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/run/training.rs",
          "symbol": "run_training_phase"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/simulation.rs",
          "symbol": "run_simulation_phase"
        },
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "run_via_study"
        }
      ],
      "fixShape": "Two shapes, both inside the crates that own the sites. For the duplicated cast, compute parallelism once in a small function beside build_distribution_info in crates/cobre-cli/src/commands/run/mod.rs, keeping the existing saturating arithmetic exactly as written so the value is bit-identical, which leaves one suppression site instead of two and gives it one rationale line. For the rest, either add the one-clause reason above each remaining suppression or remove the suppression by narrowing the conversion, and in particular give run_via_study the reason its twin already states or drop the attribute if the borrow is in fact available there. Byte-neutrality: parallelism reaches only the metadata field of the same name, the arithmetic is unchanged, and no writer, guard or row set moves, so the CLI-vs-Python determinism golden, the file-set parity test, the parity gate at 18 shared names and the cobre validate --json contract are unaffected.",
      "evidence": {
        "command": "for f in crates/cobre-cli/src/commands/broadcast.rs crates/cobre-cli/src/commands/run/mod.rs crates/cobre-cli/src/commands/run/setup.rs crates/cobre-cli/src/commands/run/training.rs crates/cobre-cli/src/commands/run/policy.rs crates/cobre-cli/src/commands/run/simulation.rs crates/cobre-cli/src/commands/run/outputs.rs crates/cobre-python/src/run.rs; do git show 077dbe2c:\"$f\" | grep -n -B1 '#\\[allow('; done; git show 077dbe2c:crates/cobre-cli/src/commands/run/training.rs | sed -n '191,192p'; git show 077dbe2c:crates/cobre-cli/src/commands/run/simulation.rs | sed -n '173,174p'; git show 077dbe2c:crates/cobre-python/src/run.rs | sed -n '1405,1406p;1665,1667p'",
        "output": "25 #[allow(...) sites over the 8 manifest files: broadcast.rs 3, run/mod.rs 1, setup.rs 2, training.rs 5, policy.rs 2, simulation.rs 3, outputs.rs 1, cobre-python run.rs 8.\n9 carry a written rationale on the preceding line (broadcast.rs:86, policy.rs:73, policy.rs:262, setup.rs:245, setup.rs:422, simulation.rs:313, training.rs:58, run.rs:1202, run.rs:1666).\n3 are #[cfg(test)]-scoped (broadcast.rs:463, run/mod.rs:324, run.rs:1759).\n13 bare: broadcast.rs:429, outputs.rs:196, simulation.rs:138, simulation.rs:173, training.rs:150, training.rs:166, training.rs:191, training.rs:194, run.rs:757, run.rs:823, run.rs:1096, run.rs:1098, run.rs:1405.\n--\ntraining.rs:191:    #[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]\ntraining.rs:192:    let parallelism = (ctx.n_threads as u32).saturating_mul(ctx.comm.size() as u32);\nsimulation.rs:173:    #[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]\nsimulation.rs:174:    let parallelism = (ctx.n_threads as u32).saturating_mul(ctx.comm.size() as u32);\n--\nrun.rs:1405:#[allow(clippy::needless_pass_by_value)]\nrun.rs:1406:pub(crate) fn run_via_study(\nrun.rs:1665:// needless_pass_by_value: PyO3's from-Python extraction hands over owned values,\nrun.rs:1666:#[allow(clippy::needless_pass_by_value)]",
        "reading": "Census of the named target. Of 25 suppressions, 9 follow the project's own practice of a rationale line above the attribute, 3 are inside #[cfg(test)] modules where unwrap_used is explicitly permitted, and 13 in production code state nothing. Honest scoping: .claude/rules/comments.md D4 enumerates the lints that MUST carry a rationale (too_many_arguments, too_many_lines, type_complexity, dead_code, unused_*), and 12 of the 13 are cast lints outside that list, so those are a consistency gap rather than a rule violation. Two sharp instances remain. First, training.rs:191-192 and simulation.rs:173-174 are byte-identical two-line blocks, the same suppression around the same parallelism expression duplicated across the two phase files, while the Python side computes the same metadata field at run.rs:802 with u32::try_from(n_threads).unwrap_or(u32::MAX) and needs no suppression at all. Second, run.rs:1405 suppresses needless_pass_by_value with nothing written, while run.rs:1666 suppresses the same lint 261 lines later with the exact rationale (PyO3's from-Python extraction hands over owned values); the documented one is the #[pyfunction] boundary where the reason holds, and the bare one is a plain Rust function where it does not, so the missing line is also the place where the justification is weakest."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-01",
      "id": "TD-057",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "six single-#[test] run-path binaries that are plain cobre-run feature lanes (CONTRIBUTING.md:206-209 assigns them to cli_run.rs) plus three helper bodies duplicated verbatim across 13, 5 and 2 files with no shared home in the crate — the Cost-discipline link-count and one-place residues at crate scale: Sev B; absorbs S6a-test-bloat-00 (the two metadata binaries are two of the six lanes)",
      "category": "duplication",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": [
        "S6a-test-bloat-00"
      ],
      "calibrationFolds": [
        {
          "candidateRef": "S6a-test-bloat-00",
          "why": "the two single-#[test] solver-linked binaries (output_metadata_active_backend, setup_timings_metadata) and their byte-identical d01_case_dir() are two of the six run-path lanes S6a-test-bloat-01 already folds into cli_run.rs and one of the three helper bodies it names; the only residue TB-00 adds is the destination question (a metadata-domain binary per testing-architecture §5.1 versus cli_run.rs), carried as the survivor's needs-human — same defect, one id (the CD-025 'not a new ID — broader scope' precedent applied to a test-bloat pair)"
        }
      ],
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Sequencing: is a cobre-cli-local consolidation sanctioned now, or does it wait for the proposed Layer-1 migration in docs/design/testing-architecture.md §5.1, whose §6 phase 1 orders that consolidation first workspace-wide?",
        "Destination: rule 14 names tests/common/ as the cobre-cli helper home, while docs/design/testing-architecture.md §6 phase 3 proposes collapsing tests/common/ into cobre-sddp's test-support feature; an owner must pick one so the helper home is not built twice.",
        "Sequencing only: docs/design/testing-architecture.md already names cli_metadata as the cobre-cli domain binary that would absorb both files under its #[path] submodule mechanics, but that section is a Proposal, so the owner must decide whether to fold this pair now under the standing Cost-discipline rule or defer it to the Layer-1 grouping so the move is made once rather than twice."
      ],
      "survivingClaim": "Narrowed to the first Cost-discipline bullet plus literal duplication: six of the seven single-test run-path binaries (cli_run_anticipated, cli_run_anticipated_k2, cli_run_evaporation, cli_e2e_run_end_block, output_metadata_active_backend, setup_timings_metadata) are plain cobre-run feature lanes that CONTRIBUTING.md:206-209's by-subcommand organization already assigns to cli_run.rs, and three helper bodies are duplicated verbatim with no shared home in the crate: fn cobre() in 13 of 14 files, the two-parents repo-root case-dir resolver at 5 sites under 3 names (d01_case_dir byte-identical between setup_timings_metadata.rs:18 and output_metadata_active_backend.rs:22), and the 102-line write_file/ThermalRows/read_thermals_parquet block byte-identical between cli_run_anticipated.rs:125-226 and cli_run_anticipated_k2.rs:135-236. Stripped from the title: the 'seven full solver links' cost framing (only cli_run_evaporation.rs and output_metadata_active_backend.rs name a solver-linking crate; the other five import no workspace crate, and cobre-cli has no lib target, so the residue is link-step count and duplicate-helper maintenance) and the reading of the rule's second bullet as a pinned tests/common/ mandate for this crate (it enumerates cobre-sddp's harness helpers and scopes its one-place property to that layer). python_parity_check.rs survives only as a dev-local vacuous pass on a machine without python3, not a CI gap (ci.yml:294 and scripts/pre-commit:14 run the same script with the same --max 0), and its third-enforcement-layer role must survive any merge.",
      "title": "Seven of the nine run-path test binaries hold exactly one #[test], and cobre-cli has no tests/common/: fn cobre() is redefined in 13 of 14 binaries, the repo-root case-dir body in 5 corpus files under 3 names, and a 102-line parquet-reader block is byte-identical across the two anticipated binaries",
      "anchors": [
        {
          "path": "crates/cobre-cli/tests/cli_run_anticipated.rs",
          "symbol": "read_thermals_parquet"
        },
        {
          "path": "crates/cobre-cli/tests/cli_run_anticipated_k2.rs",
          "symbol": "read_thermals_parquet"
        },
        {
          "path": "crates/cobre-cli/tests/cli_run_evaporation.rs",
          "symbol": "d08_case_dir"
        },
        {
          "path": "crates/cobre-cli/tests/cli_run_generic_echo.rs",
          "symbol": "case_dir"
        },
        {
          "path": "crates/cobre-cli/tests/python_parity_check.rs",
          "symbol": "python_parity_script_passes"
        },
        {
          "path": "crates/cobre-cli/tests/cli_run.rs",
          "symbol": "cobre"
        },
        {
          "path": "crates/cobre-cli/tests/output_metadata_active_backend.rs",
          "symbol": "training_metadata_solver_matches_active_backend"
        },
        {
          "path": "crates/cobre-cli/tests/setup_timings_metadata.rs",
          "symbol": "training_metadata_carries_well_formed_setup_timings"
        },
        {
          "path": "crates/cobre-cli/tests/setup_timings_metadata.rs",
          "symbol": "d01_case_dir"
        }
      ],
      "fixShape": "Introduce crates/cobre-cli/tests/common/ as the single harness home (a mod common declared from the consolidated binary, which is the layout section Cost discipline already names) holding fn cobre(), one case_dir(name) resolver that subsumes the d01 and d08 variants and the 1dtoy form, fn write_file, struct ThermalRows and fn read_thermals_parquet, then move the single-test run-path binaries into the existing cli_run.rs domain binary as mod groups, one per feature lane (anticipated, anticipated_k2, evaporation, generic_echo, end_block, run_metadata). No new test binary and no new fixture crate: the helper home is a module of an existing binary, not a crate. Keep python_parity_check.rs shelling to the one canonical parity script rather than reimplementing parity in Rust, but move it into the consolidated binary too and turn its python3-absent early return into an explicit skip signal so an absent interpreter cannot read as a pass. Byte-neutrality (rule 8): helper bodies move verbatim, every assertion and every deck stays as written, and no golden, baseline or committed .sha256 is touched.",
      "evidence": {
        "command": "git ls-tree -r --name-only 077dbe2c -- crates/cobre-cli/tests; git grep -l 'fn cobre() -> Command' 077dbe2c -- crates/cobre-cli/tests | wc -l; diff <(git show 077dbe2c:crates/cobre-cli/tests/cli_run_anticipated.rs | sed -n '125,226p') <(git show 077dbe2c:crates/cobre-cli/tests/cli_run_anticipated_k2.rs | sed -n '135,236p'); echo exit=$?",
        "output": "14 flat .rs paths, no tests/common/ entry anywhere in the tree\\n13\\nexit=0   (no diff output: the 102 lines are byte-identical)",
        "reading": "The crate has no shared harness directory at all, so every binary rebuilds the harness. fn cobre() -> Command, whose body is a single Command::new(assert_cmd::cargo::cargo_bin!(\\\"cobre\\\")), is declared in 13 of the 14 binaries, 8 of them in this corpus. The repo-root case-dir resolution body (manifest dir, two parents, join examples/deterministic/<case>) appears 5 times in this corpus under 3 different names, d08_case_dir, case_dir(name), d01_case_dir, plus inline in python_parity_check.rs, with a sixth variant form in cli_e2e_run_end_block.rs. The 102-line block carrying fn write_file, struct ThermalRows and fn read_thermals_parquet is byte-identical between cli_run_anticipated.rs and cli_run_anticipated_k2.rs, and each of those binaries holds exactly one #[test]. Against the station's frozen per-binary census, 7 of the 9 corpus binaries hold a single test function (all but cli_run.rs at 12 and cli_run_generic_echo.rs at 2), so the crate pays 7 full solver links for 7 test functions. .claude/rules/testing.md section Cost discipline states both halves of the violation directly: add a test function to an existing domain binary rather than a new file, and the shared harness helpers live once in tests/common/, never re-defined per file. python_parity_check.rs is the sharpest instance: a whole solver-linked binary whose single test never invokes the cobre binary at all, it only shells python3 against scripts/ci/check_python_parity.py and returns early, a silent pass, when python3 is absent."
      },
      "yardstickSection": "§5.1 canonical per-crate layout (by-subcommand integration binaries; helpers live once) / §3.2 sustainability",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-02",
      "id": "TD-058",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "five redundant 1dtoy trainings in two pytest modules, fix is one conftest fixture (effort S) and both modules document per-test isolation as policy — cosmetic ROI, the owner question is whether that policy stands",
      "upgradeNote": null,
      "calibrationBasis": "four read-only Policy tests and one run re-train the default deck that a module-scoped fixture in the existing conftest would serve with every assertion unchanged — per-test isolation is the two modules' documented policy, so the residue is one fixture hoist: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Is the per-test-isolation cost acceptance recorded in the two module docstrings (test_study.py:12-14, test_run.py:9-11) standing policy for the pytest corpus? It is the only stated rationale, and if the owner keeps it as policy the shared read-only-Policy fixture is unwanted even for the five named tests."
      ],
      "survivingClaim": "The four read-only Policy tests in test_study.py (:363 cut-matrix-max, :391 shapes/dtype, :416 stage-out-of-range, :433 bad-state-length) each pay a dedicated 1dtoy training for an object they only read through an &self accessor, and test_run_1dtoy_creates_output (test_run.py:38) re-runs the default deck for an artifact-tree assertion that test_outputs.py:33-50 already makes as a strict superset on its module-scoped run_output fixture; one module-scoped fixture in the existing conftest.py would serve all five with every assertion text unchanged. The other trainings and runs in both files are the claim under test and are not redundant.",
      "title": "test_study.py and test_run.py declare no fixtures and re-train or re-run examples/1dtoy per test function, including for pure argument-validation claims, while test_outputs.py in the same directory runs each deck once behind a module-scoped fixture",
      "anchors": [
        {
          "path": "crates/cobre-python/tests/test_study.py",
          "symbol": "test_policy_evaluate_stage_out_of_range_raises_indexerror"
        },
        {
          "path": "crates/cobre-python/tests/test_study.py",
          "symbol": "test_policy_evaluate_bad_state_length_raises_valueerror"
        },
        {
          "path": "crates/cobre-python/tests/test_run.py",
          "symbol": "test_run_1dtoy_succeeds"
        },
        {
          "path": "crates/cobre-python/tests/test_run.py",
          "symbol": "test_run_1dtoy_creates_output"
        },
        {
          "path": "crates/cobre-python/tests/test_outputs.py",
          "symbol": "run_output"
        },
        {
          "path": "crates/cobre-python/tests/conftest.py",
          "symbol": "cli_binary"
        }
      ],
      "fixShape": "Add a module-scoped trained-policy fixture to crates/cobre-python/tests/conftest.py (the file that already owns the session-scoped cli_binary fixture, so no new file and no new fixture crate) that trains examples/1dtoy once into a tmp_path_factory directory and yields the Study and its Policy, then have the tests whose claim is about an already-built Policy consume it: the two policy_evaluate validation tests and the other read-only Policy assertions in test_study.py. Tests that assert on the output tree, on config overrides, or on per-run metadata keep their own tmp_path and their own run, since their claim is about the run itself. In test_run.py, fold the result-dict and output-tree assertions on the shared 1dtoy deck onto one module-scoped run the same way test_outputs.py already does. Every assertion text and every deck stays exactly as written, so byte-neutrality (rule 8) holds and no parity golden or committed .sha256 is involved.",
      "evidence": {
        "command": "for f in test_study.py test_run.py test_outputs.py test_callback.py; do echo \\\"== $f\\\"; git show 077dbe2c:crates/cobre-python/tests/$f | grep -c '@pytest.fixture'; git show 077dbe2c:crates/cobre-python/tests/$f | grep -c '^def test_'; git show 077dbe2c:crates/cobre-python/tests/$f | grep -c '\\\\.train()'; git show 077dbe2c:crates/cobre-python/tests/$f | grep -c 'run\\\\.run('; done; git show 077dbe2c:crates/cobre-python/tests/test_outputs.py | grep -n '@pytest.fixture(scope=' -A 1",
        "output": "== test_study.py  fixtures 0  tests 20  train() 15  run.run 4\\n== test_run.py  fixtures 0  tests 13  run.run 14\\n== test_outputs.py  fixtures 3  tests 9  run.run 3\\n== test_callback.py  fixtures 0  tests 3  run.run 4\\ntest_outputs.py: @pytest.fixture(scope=\\\"module\\\") def run_output(...)  /  def d20_output(...)  /  def d28_output(...)",
        "reading": "test_study.py drives 20 test functions against a single deck, VALID_CASE = examples/1dtoy, with zero fixtures: 20 Study constructions and 15 trainings, plus 4 tests that additionally run cobre.run.run to compare the two paths. Two of those trainings buy nothing: test_policy_evaluate_stage_out_of_range_raises_indexerror and test_policy_evaluate_bad_state_length_raises_valueerror each train a full policy and then assert only that an out-of-range stage index raises IndexError and that a wrong-length state vector raises ValueError, claims about argument validation on an already-built Policy. test_run.py repeats the pattern: 13 tests, 14 run.run calls, no fixture, with test_run_1dtoy_succeeds and test_run_1dtoy_creates_output performing two separate full runs of the same deck to assert the result dict and then the output tree. test_outputs.py, in the same directory, is the counter-example that proves the seam exists and is idiomatic here: three module-scoped fixtures, run_output, d20_output and d28_output, cover 9 tests with 3 runs. docs/design/testing-architecture.md section 5.3 asks each claim to sit at the cheapest sufficient tier; an argument-validation claim on a Policy object does not need its own training. test_callback.py is excluded from this finding on purpose: its callbacks must observe distinct runs, so its 4 runs for 3 tests are correct."
      },
      "yardstickSection": "§5.3 tier taxonomy (the cheapest tier that catches the regression) / §5.1 fixtures",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-03",
      "id": "TD-059",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "one redundant study of four (effort S); the goldens are not a redundant second source, so the title's coverage claim does not survive",
      "upgradeNote": null,
      "calibrationBasis": "one of the four CI-executed 1dtoy studies in the bindings' inline module re-solves the identical default deck — a single redundant study; the hand-pinned goldens the title attacked are load-bearing and stay: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Tiers — use the cheapest tier that catches the regression",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Exactly one of the inline module's full 1dtoy studies is avoidable duplication: `reconstruct_policy_from_checkpoint_roundtrips_for_1dtoy` (crates/cobre-python/src/run.rs:2326) re-solves the identical default deck that `python_run_1dtoy_metadata_matches_cli_golden_values` (2094) already solves, with no once-initialised shared output dir in the module, so a lazily-shared train-then-simulate fixture would drop one of the four studies CI executes while preserving every assertion. The hand-pinned goldens are NOT a redundant second source (the pytest parity suite pins no absolute value and cannot subsume them), the two mean-cost literals cannot rot silently, and the study count is six call sites / four CI-executed studies, not seven.",
      "title": "The bindings' inline test module runs seven full run_via_study studies and hand-pins the CLI's 1dtoy numbers, and cli_e2e_run_end_block.rs pins the same mean cost again at different precision in a second crate, while the pytest parity suite derives the CLI/Python agreement live",
      "anchors": [
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "python_run_1dtoy_metadata_matches_cli_golden_values"
        },
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "copy_dir_all"
        },
        {
          "path": "crates/cobre-cli/tests/cli_e2e_run_end_block.rs",
          "symbol": "EXPECTED_MEAN_COST"
        },
        {
          "path": "crates/cobre-python/tests/test_cli_python_determinism_parity.py",
          "symbol": "test_cli_python_json_files_match"
        }
      ],
      "fixShape": "Give the CLI/Python 1dtoy agreement one owner. Keep the live derivation in the pytest parity suite as the authority, and reduce the inline bindings test to what only it can assert, that run_via_study returns a well-formed metadata structure, dropping the hand-copied CLI numbers rather than maintaining a second pinned copy. If an offline guard is wanted for environments with no built wheel, pin the 1dtoy numbers in exactly one place and have the other site read them, and say in that one place which command regenerates them. Separately, collapse the four tests that each drive a fresh full 1dtoy study onto one shared setup within the module so the deck is trained once for the metadata, override, checkpoint-roundtrip and simulation-only claims, and reuse one directory-copy helper instead of the module-local copy_dir_all. Byte-neutrality (rule 8): no parity golden, no committed .sha256 and no output artifact changes; only where the numbers live and how many times the same deck is solved.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/run.rs | grep -n 'run_via_study(\\\\|golden_\\\\|fn python_run_1dtoy'; git show 077dbe2c:crates/cobre-cli/tests/cli_e2e_run_end_block.rs | grep -n 'EXPECTED_MEAN_COST'; git ls-tree -r --name-only 077dbe2c -- crates/cobre-sddp/tests/fixtures | grep 'sha256$'",
        "output": "run.rs: run_via_study( called 7 times across 4 tests; golden_lower_bound = 15_595_518.381_798_638; golden_upper_bound = 579_592.198_622_440_7; golden_mean_cost = 9_679_385.922_404_84\\ncli_e2e_run_end_block.rs: const EXPECTED_MEAN_COST: f64 = 9_679_385.922_404_844;\\nfixtures: parity_baselines and parity_baselines_clp .sha256 files for D06 D15 D30 D34 D41 only, no 1dtoy baseline",
        "reading": "The inline module of the bindings crate executes run_via_study seven times across four tests, all on examples/1dtoy, each a full train-then-simulate into a process-id-named temp dir, and one of them, python_run_1dtoy_metadata_matches_cli_golden_values, hand-pins the CLI's lower bound, upper bound, mean cost and two counts as Rust literals; its own doc says the golden values are the CLI's actual output for examples/1dtoy. That is a CLI/Python agreement claim, the exact claim test_cli_python_determinism_parity.py derives live by running both front ends and comparing their artifacts, so the number-pinning copy adds a second, hand-maintained source for it. The same mean cost is pinned a third time in a different crate as EXPECTED_MEAN_COST in cli_e2e_run_end_block.rs, at 9_679_385.922_404_844 against the bindings' 9_679_385.922_404_84, the same quantity at two precisions with no generator tying them. Scope caveat stated deliberately: the section Contracts rule in .claude/rules/testing.md, parity baselines have ONE source of truth, the committed .sha256, do not add a second pinned copy, does not literally cover these constants, because as the fixture listing shows 1dtoy has no committed baseline at all; it is cited here as the nearest standing norm, not as a rule these lines breach. This finding is about duplicated hand-pinned goldens and repeated full studies inside a unit-tier module; it is NOT the superseded claim that the bindings' Rust tests are invisible to CI, which ci.yml runs at the pin."
      },
      "yardstickSection": "§5.3 tier taxonomy / §5.1 (inline module tests)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-04",
      "id": "TD-060",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three byte-identical helper families across the pytest parity modules lack a home in the seam that already exists (conftest.py / _cobre_cli.py) — hoistable fixtures with every assertion unchanged; the six parquet collectors carry distinct contracts and stay: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Does section 5.11's keep the pytest output-parity suite unchanged clause freeze the four output-parity modules against fixture hoisting, or does it only exempt them from the Layer-1 Rust binary migration while leaving harness hygiene in scope? The confirmed residue touches exactly the files that clause names."
      ],
      "survivingClaim": "Only the three byte-identical helper families lack a shared home in the seam that already exists: the four _make_case_with_simulation copies (plus the same copy-and-enable prologue inside _seed_case), the two _relative_files one-liners, and the two float comparators, all hoistable into conftest.py or _cobre_cli.py. The six parquet collectors and the Rust copy_dir_all are struck from the claim: the collectors carry three distinct contracts (empty-tolerant, assert-present, concatenating table) and the empty-tolerant return IS the assertion at test_anticipated_lanes_output_parity.py:236 and :243, so unifying them would weaken a golden negative test, and copy_dir_all is an inline cfg(test) Rust helper no pytest fixture can absorb.",
      "title": "The pytest parity files each rebuild the same harness: _make_case_with_simulation is declared four times with a byte-identical body, plus a fifth copy inside _seed_case and a sixth in Rust, alongside six one-off parquet collectors, two identical comparators and two _relative_files, while conftest.py and _cobre_cli.py are the existing shared seam",
      "anchors": [
        {
          "path": "crates/cobre-python/tests/test_contract_output_parity.py",
          "symbol": "_make_case_with_simulation"
        },
        {
          "path": "crates/cobre-python/tests/test_anticipated_lanes_output_parity.py",
          "symbol": "_make_case_with_simulation"
        },
        {
          "path": "crates/cobre-python/tests/test_anticipated_output_parity.py",
          "symbol": "_make_case_with_simulation"
        },
        {
          "path": "crates/cobre-python/tests/test_parity_hydros.py",
          "symbol": "_make_case_with_simulation"
        },
        {
          "path": "crates/cobre-python/tests/test_parity_scalar_parameters.py",
          "symbol": "_seed_case"
        },
        {
          "path": "crates/cobre-python/tests/test_cli_python_file_set_parity.py",
          "symbol": "_relative_files"
        },
        {
          "path": "crates/cobre-python/tests/test_cli_python_determinism_parity.py",
          "symbol": "_relative_files"
        },
        {
          "path": "crates/cobre-python/tests/conftest.py",
          "symbol": "cli_binary"
        }
      ],
      "fixShape": "Hoist the shared harness into the seam that already exists: put one case-with-simulation builder, one Hive-partitioned parquet collector parameterised by entity directory, and one float comparator into conftest.py as fixtures (or beside run_cli in the existing _cobre_cli.py helper module, whichever keeps the import graph flatter), have the four parity modules and _seed_case consume them, and keep the scenario_id partition-key note once at that single home instead of six times. Give the two cli_python parity modules one shared _relative_files. No new fixture crate and no new test binary: both target files already exist and are already imported by the suite. Every test keeps its own deck, its own tolerance and its own assertions, the two file-set tests keep their distinct decks, so byte-neutrality (rule 8) holds and no parity golden or committed .sha256 is touched, which is what section 5.11 of docs/design/testing-architecture.md asks for.",
      "evidence": {
        "command": "git grep -l 'def _make_case_with_simulation' 077dbe2c -- crates/cobre-python/tests; for f in test_contract_output_parity.py test_anticipated_lanes_output_parity.py test_anticipated_output_parity.py test_parity_hydros.py; do git show 077dbe2c:crates/cobre-python/tests/$f | python3 -c 'import re,sys; s=sys.stdin.read(); b=s.split(\\\"def _make_case_with_simulation\\\")[1].split(\\\"\\\\ndef \\\")[0]; b=re.sub(r\\\"\\\\\\\"\\\\\\\"\\\\\\\".*?\\\\\\\"\\\\\\\"\\\\\\\"\\\", \\\"\\\", b, flags=re.S); import hashlib; print(hashlib.md5(b.encode()).hexdigest())'; done; git grep -c 'scenario_id is both' 077dbe2c -- crates/cobre-python/tests | wc -l",
        "output": "4 files declare _make_case_with_simulation\\nc86c32161ad55be6132368cd860919c9\\nc86c32161ad55be6132368cd860919c9\\nc86c32161ad55be6132368cd860919c9\\nc86c32161ad55be6132368cd860919c9\\n6 files carry the same scenario_id partition-key note",
        "reading": "With docstrings stripped, all four _make_case_with_simulation bodies hash identically: copy the source case tree item by item, load config.json, set simulation.enabled true, write it back. A fifth copy of that logic is embedded in _seed_case in test_parity_scalar_parameters.py and a sixth exists in Rust as copy_dir_all in the bindings' inline test module. The same directory also carries six separate partitioned-parquet collectors that each walk a Hive-partitioned output directory and concatenate, _lanes_parquets, _thermals_parquets, _contract_parquets, _thermals_table, _collect_hydros_parquet and _collect_buses_parquet; two byte-identical float comparators, _lane_values_equal and _anticipated_values_equal; two _relative_files declarations, one per cli_python parity file; and the same warning note about scenario_id being both the partition key and an in-file column repeated across six files, which is the signature of a shared gotcha with no shared home. .claude/rules/testing.md section Cost discipline states the standard for the Rust side, shared harness helpers live once and are never re-defined per file, and the pytest suite has the seam ready to hold them: conftest.py already registers the --require-cli-binary option and the session-scoped cli_binary fixture, and _cobre_cli.py already owns resolve_cli_binary and run_cli for the 12 modules that consume them. The duplication is in the case-building and output-reading half that was never hoisted."
      },
      "yardstickSection": "§5.2 uniform test-support convention (shared fixtures live on the existing seam, never a new fixture crate) / §5.11 cobre-python parity",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-05",
      "id": "TD-061",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one redundant full train-and-simulate inside one parity module, removable by a module-scoped derived-case fixture; the test function itself is load-bearing because the superset test skips without the CLI binary: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Owner call: does docs/design/testing-architecture.md section 5.11's 'keep the pytest output-parity suite unchanged' freeze the eleven parity modules against fixture hygiene, or only exempt them from the section 5.1 Layer-1 binary migration? That answer decides whether this assertion-preserving shared-fixture residue is actionable at all."
      ],
      "survivingClaim": "Inside test_contract_output_parity.py the derived D41 deck is solved through cobre.run.run twice (:224 and :264) with no shared fixture, so exactly one full train-and-simulate is redundant work removable by a module-scoped derived-case-and-run fixture; the test function itself is NOT deletable, because the superset test is gated on the skipping cli_binary fixture and therefore does not run in the module's default local invocation.",
      "title": "test_run_via_study_emits_contract_output asserts a strict subset of test_cli_python_contract_row_parity on the same Python surface and the same derived d41 deck, costing one extra full train-and-simulate",
      "anchors": [
        {
          "path": "crates/cobre-python/tests/test_contract_output_parity.py",
          "symbol": "test_run_via_study_emits_contract_output"
        },
        {
          "path": "crates/cobre-python/tests/test_contract_output_parity.py",
          "symbol": "test_cli_python_contract_row_parity"
        },
        {
          "path": "crates/cobre-python/tests/test_contract_output_parity.py",
          "symbol": "CONTRACT_SCHEMA_FIELDS"
        }
      ],
      "fixShape": "Drop test_run_via_study_emits_contract_output and let test_cli_python_contract_row_parity carry the Python-side schema and non-emptiness assertions it already makes, or, if a Python-only claim is wanted for runs where no CLI binary is available, keep the thinner test but have it consume the same module-scoped derived-case-and-run fixture instead of materialising and solving the deck again. In either direction the contract field list and the non-emptiness guard stay asserted, so no coverage is lost. Byte-neutrality (rule 8): no output bytes, no parity golden and no committed .sha256 are affected, and the row-for-row comparison that section 5.11 of docs/design/testing-architecture.md protects is the test that survives.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/tests/test_contract_output_parity.py | grep -n '^def test_\\\\|_make_case_with_simulation(\\\\|cobre.run.run(\\\\|run_cli(\\\\|CONTRACT_SCHEMA_FIELDS\\\\|@pytest.fixture'",
        "output": "no @pytest.fixture in the module; the derived case is materialised in 4 tests; test_run_via_study_emits_contract_output asserts len(parquets) > 0 and schema_names == CONTRACT_SCHEMA_FIELDS after cobre.run.run; test_cli_python_contract_row_parity builds the same derived case, calls run_cli and the same cobre.run.run, and asserts len(py_parquets) > 0, py_schema == CONTRACT_SCHEMA_FIELDS, and row-for-row CLI/Python equality",
        "reading": "Both tests build the same derived d41 case through the same helper and exercise the same Python surface, cobre.run.run. The parity test asserts everything the other one asserts, non-empty parquet set and the exact contract schema field list, and then adds the CLI comparison and the row-for-row equality. So the earlier test is a strict subset on an identical surface and deck, and the module pays one extra full d41 train-and-simulate for a claim already covered. This is distinguished from the neighbouring modules on purpose: test_in_transit_output_parity.py and test_pumping_output_parity.py look the same in shape but their paired tests exercise different Python surfaces, Study.simulate versus cobre.run.run, so their pairs are not subsets and are not raised."
      },
      "yardstickSection": "§5.3 tier taxonomy / §5.11 cobre-python parity",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6a-test-bloat-06",
      "id": "TD-062",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6a",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two thread-count tests homed away from their subject's own test module plus one orphan literal pass-through assertion on an engine helper — test homing inside one crate: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus; the delta_to_stats_row coupling is the I.5 cross-reference)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": true,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": "I.5",
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus",
        "alignment"
      ],
      "needsHuman": [
        "Owner call: whether the one unique stats-row assertion (the simulation shape) should be rehomed next to the owner's existing solver_stats_log_to_rows test in cobre-sddp, or reshaped into a front-end assertion that covers both outputs.rs:197 and cobre-python run.rs:824 under the Python parity hard rule; the answer decides whether the residue is a pure move or a new assertion."
      ],
      "survivingClaim": "Narrower than the title on both halves. (1) Confirmed as filed only for the two thread-count tests: their subject and its single production caller both sit in setup.rs (:58 declaration, :184 caller), which already has a cfg(test) mod tests at :601, and run/mod.rs:331's cross-module import exists solely to reach them. (2) For delta_to_stats_row the claim narrows to duplication plus one orphan, not absent owner coverage: the backward (mod.rs:398) and forward (mod.rs:419) cases restate assertions cobre-sddp already makes indirectly at solver_stats.rs:1038 via solver_stats_log_to_rows, so 'no unit test in the crate that declares it' holds only as a test-name search; the simulation case (mod.rs:440) is the sole unique assertion and it pins a literal pass-through of the shape both front ends hard-code at outputs.rs:197 and run.rs:824 without pinning either site. (3) The title's whole-module claim does NOT survive: host_layouts and check_stats_overflow are declared in mod.rs and rightly tested there.",
      "title": "run/mod.rs's inline test module is the wrong home for its unit tests: it tests resolve_thread_count, declared in setup.rs which has its own test module, and pins three literal-argument cases of the engine's delta_to_stats_row, which has no unit test in the crate that declares it",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/run/mod.rs",
          "symbol": "test_resolve_thread_count_cli_value"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/mod.rs",
          "symbol": "test_resolve_thread_count_default"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/mod.rs",
          "symbol": "test_delta_to_stats_row_backward_carries_opening_rank_worker"
        },
        {
          "path": "crates/cobre-cli/src/commands/run/setup.rs",
          "symbol": "resolve_thread_count"
        }
      ],
      "fixShape": "Move the two thread-count tests into the existing #[cfg(test)] module of setup.rs, next to the function they exercise, which also removes the cross-module import from the run module's test module. Move the three stats-row tests to the engine module that declares the function so its owner holds the unit coverage for its own field-propagation rules, and let the CLI keep only what is genuinely the CLI's claim, that the run path calls it once per phase with the right phase argument, if that is asserted anywhere. Test bodies and assertions move verbatim, so no claim changes and byte-neutrality (rule 8) is unaffected: these are unit tests that write no output and read no golden or committed .sha256.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/run/mod.rs | grep -n 'cfg(test)\\\\|mod tests\\\\|use super::\\\\|use cobre_sddp::\\\\|fn test_'; git show 077dbe2c:crates/cobre-cli/src/commands/run/setup.rs | grep -n 'fn resolve_thread_count\\\\|cfg(test)'; git grep -n 'fn delta_to_stats_row' 077dbe2c -- crates/cobre-sddp/src",
        "output": "run/mod.rs: #[cfg(test)] mod tests, use super::setup::resolve_thread_count;, use cobre_sddp::{SolverStatsDelta, delta_to_stats_row};, then test_resolve_thread_count_cli_value, test_resolve_thread_count_default, and three delta_to_stats_row tests\\nsetup.rs: pub(super) fn resolve_thread_count declared here, and setup.rs has its own #[cfg(test)] mod tests\\ncobre-sddp: delta_to_stats_row declared in solver_stats.rs, no #[test] naming it in that crate",
        "reading": "The thread-count tests import the function across a module boundary, use super::setup::resolve_thread_count, to test a function declared in setup.rs, which already carries its own #[cfg(test)] module, so the test sits one module away from its subject for no reason. The three delta_to_stats_row tests are pure literal-argument unit tests of an engine function the CLI merely calls: they construct a SolverStatsDelta and assert which of opening, rank and worker id survive on the backward, forward and simulation paths. That is a cobre-sddp behaviour, and the declaring crate has no unit test naming the function, so the front end holds coverage for engine logic while the owner holds none. Under docs/design/testing-architecture.md section 5.3 the cheapest sufficient tier for both claims is a unit test next to the declaration."
      },
      "yardstickSection": "§5.1 canonical layout (a unit test lives beside the item it tests)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-architecture-01",
      "id": "CD-091",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "an un-owned LoadError → kind vocabulary re-derived on each side of the boundary, plus two documented cobre-io load failures that exit 4 with no --json object while the bindings return them as data — the validate --json contract oversells its coverage while a path escapes the shared discipline: Sev B on the CD-029 precedent",
      "category": "missing-seam",
      "effort": "M",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part IV §IV.1 (L2 cobre-io owns the LoadError → kind classification both front ends re-derive) / Part V §V.1 Phase 0",
      "defenderAlignmentHint": "advances-0a",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": "I.5",
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "needs-rebaseline",
      "byteNeutrality": "NOT byte-neutral: needs-rebaseline — the emitted `cobre validate --json` object or an emitted metadata field legitimately moves; held for an owner decision at the gate rather than recorded as byte-neutral",
      "contractCited": "cobre validate module contract (crates/cobre-cli/src/commands/validate.rs module doc: --json stdout is exactly one object)",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "May the CLI's --json phase value CaseValidationError be retired in favour of the cobre-io/Python vocabulary? It is referenced nowhere outside its own declaration and doc (validate.rs:90 and :361), so the change costs nothing mechanically, but it is a user-visible field in a documented machine-readable contract and an unknown downstream consumer may match on it."
      ],
      "survivingClaim": "Narrowed to the un-owned LoadError kind map plus the non-boundary uncovered --json returns, and the malformed-config instance removed: (1) the two front ends' stable kind vocabularies diverge ONLY at the pre-PrepPhase cobre-io layer, where validate.rs:361 hard-codes CaseValidationError into its error.phase field and cobre-python io.rs:50-58 keeps a private total load_error_kind over the five LoadError variants, with no shared owner in cobre-io even though prep_phase_metadata already establishes exactly that pattern for the three PrepPhase kinds; and (2) validate.rs:370 maps LoadError::ParseError and LoadError::SchemaError, both documented returns of run_pipeline_with_artifacts at cobre-io pipeline.rs:38-42, to CliError::Internal at exit 4 with a bug-report hint and no --json object at all, while cobre.io.validate returns the same failures as data with kind ParseError or SchemaError. Explicitly NOT in the residue: a malformed config.json, which validation/schema.rs:231-235 collects inside the schema layer so it surfaces as ConstraintError and does emit an object (making validate.rs:381 effectively unreachable for malformed content), and the boundary-path silence at validate.rs:298 and :401, whose destination belongs to CD-029's open abstraction half.",
      "title": "The validate --json error contract and cobre.io.validate's error dict disagree on kind and message for the same input, and six CLI failure paths emit no JSON at all",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "execute"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "ValidateErrorOutput"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "ValidateBoundaryOutput"
        }
      ],
      "fixShape": "Give LoadError one kind map owned by the crate that owns the type — cobre-io (L2), the crate both front ends already depend on — and have the CLI and the bindings both call it, replacing validate.rs's 3-arm match and io.rs's local load_error_kind. Then route every CLI early-return through emit_validate_json when args.json is set, so the --json object exists for the existence check, the config parse, the scenario-source resolve and the residual LoadError variants. Byte-neutrality bar: the direction of the naming unification is forced by what is pinned — test_validate.py:222 asserts the Python kinds, while \"CaseValidationError\" is asserted nowhere (git grep finds only its own declaration and doc), so the CLI adopts the cobre-io vocabulary and no assertion moves; cli_validate.rs:163-197 only requires that stdout parse as one object and that error.message name the offending file, both preserved; the register's accept/reject parity is unchanged because exit codes and the reject decision are untouched.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/validate.rs | sed -n '330,405p' ; git show 077dbe2c:crates/cobre-python/src/io.rs | sed -n '50,60p;227,252p' ; git grep -n 'CaseValidationError' 077dbe2c -- crates scripts",
        "output": "validate.rs paths that return WITHOUT emit_validate_json even when args.json is true:\n  :334 !case_dir.exists()            -> CliError::Io (exit 2)\n  :348 LoadError::IoError            -> CliError::Io (exit 2)\n  :370 Err(other)  [ParseError | SchemaError | PolicyIncompatible] -> CliError::Internal (exit 4)\n  :381 cobre_io::parse_config(..).map_err(CliError::from)?\n  :397 config.training_scenario_source(..).map_err(CliError::from)?\n  :401 resolve_boundary_state_requirements(..)?\nThe only CLI paths that DO emit JSON: :354-364 (kind \"CaseValidationError\", message = description) and the three run_prep_phase calls via prep_error_to_cli_error.\n---\nio.rs:236-241  match validate_case_with_artifacts(&path) { .. Err(ref err) => return_error!(load_error_kind(err), err.to_string()) }\nio.rs:50 fn load_error_kind(err: &LoadError) -> &'static str  -- total over all five variants -> \"IoError\"|\"ParseError\"|\"SchemaError\"|\"ConstraintError\"|\"PolicyIncompatible\"\nio.rs:227 existence check -> return_error!(\"IoError\", ..)\nio.rs:247 config load  -> return_error!(load_error_kind(err), ..)\nio.rs:274 training_scenario_source -> return_error!(load_error_kind(err), ..)\nio.rs:264 resolve_boundary_state_requirements -> return_error!(\"BoundaryReconciliationError\", ..)\nEvery cobre.io.validate path returns a dict; test_validate.py:222 asserts err[\"kind\"] in (\"ConstraintError\",\"IoError\",\"ParseError\",\"SchemaError\").\n---\ngit grep CaseValidationError: only validate.rs:90 (its own doc) and validate.rs:361 (the emit). No test, script or Python site references the string.",
        "reading": "Both front ends publish a programmatic error vocabulary over the same cobre-io LoadError, and the two vocabularies are not the same function. On a duplicate-bus-id deck the CLI reports kind \"CaseValidationError\" with `description`; cobre.io.validate reports \"ConstraintError\" with `err.to_string()`. The CLI's classifier is a 3-arm match with a catch-all to CliError::Internal (exit 4, no JSON); the Python classifier is the total 5-variant load_error_kind at io.rs:50. Both surfaces are documented as stable programmatic contracts — validate.rs:89-93 calls the field 'prep_phase_metadata's stable kind string (or CaseValidationError ...)', and errors.rs:19-20 says 'the cobre.io.validate data-report kind field is ... a stable data contract decoupled from these class names' — so this is two stable contracts for one fact, with no shared owner. The coverage half is worse than the naming half: six CLI paths honour --json by emitting nothing, including a malformed config.json and a missing case directory, both of which cobre.io.validate answers with a kind. This is not CD-029 (no boundary phase involved) and not the CD-004 Config-projection twin; it is the error-classification half of the validate mirror that rule 14 assigns to whichever cell sees it.",
        "citedNotAnchored": [
          "crates/cobre-python/src/io.rs::load_error_kind (:50)",
          "crates/cobre-python/src/io.rs::validate (:227, :236, :247, :264, :274)",
          "crates/cobre-python/src/errors.rs (:19-20)",
          "crates/cobre-python/tests/test_validate.py:222"
        ]
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-architecture-02",
      "id": "CD-092",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6b",
      "subSurfaces": [
        "S6b",
        "S6c"
      ],
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "four copies of a one-line (file_label, message) join with no owner and two small observable divergences (opposite phase order, one of five kind strings) over an engine-owned PrepPhase whose driver bodies are conceded equivalent — minor asymmetry: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part IV §IV.1 (the rendered join belongs beside prep_phase_metadata's owner) / Part V §V.1 Phase 0",
      "defenderAlignmentHint": "advances-0a",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": "I.5",
      "waveRef": "CD-029",
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "needs-rebaseline",
      "byteNeutrality": "NOT byte-neutral: needs-rebaseline — the emitted `cobre validate --json` object or an emitted metadata field legitimately moves; held for an owner decision at the gate rather than recorded as byte-neutral",
      "contractCited": "CLAUDE.md § Hard Rules — Python parity",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [
        "May the CLI --json kind string CaseValidationError be retired in favour of the cobre-io vocabulary the bindings already use? It occurs only at validate.rs:90 and :361 and no test, script or binding matches on it, but it is a field in a documented machine-readable contract, so retiring it is an owner call, not a refactor detail.",
        "Where should a shared LoadError-to-kind classification live? cobre-io (L2) owns LoadError and both front ends depend on it, but the station's committed destination rule is written for output orchestration only; confirm that the L2 destination binds an error-classification map or name a different owner."
      ],
      "survivingClaim": "Only the rendered message, not the phase driver, is unowned: the expression joining prep_phase_metadata's file_label to the error text exists in four copies (cobre-cli validate.rs:147 plus cobre-python io.rs:258, :292, :301) with no single owner anywhere, and two observable divergences survive from the folded S6c text - the opposite relative order of training_scenario_source and resolve_boundary_state_requirements (validate.rs:396 then :401 against io.rs:264 then :274) changes which error a deck failing both reports first, and the CLI --json surface classifies one of five LoadError classes under a string the bindings never use against the bindings' five of five. The driver bodies, the phase-versus-kind key names and the short-circuit doc claim do not survive.",
      "title": "The pre-solver phase driver is written twice over one engine-owned PrepPhase: the CLI extracts the (kind, message) pair into a helper, the bindings re-inline the identical two lines three times",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "describe_prep_error"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "run_prep_phase"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "validate"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "load_error_kind"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "load_validate_config"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "convert_load_error"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "build_warnings_list"
        }
      ],
      "fixShape": "Hoist the two-line body of describe_prep_error next to prep_phase_metadata in cobre-sddp's validate_phases.rs — the same destination CD-029's restated fix shape already names for the boundary phase — so one engine-owned function returns (kind, \"file_label: message\") and both front ends call it. The CLI keeps run_prep_phase for its dual-sink rendering and the bindings keep return_error! for the dict shape; only the string computation moves. Byte-neutrality bar: the rendered message is character-identical on both sides today, so cli_validate.rs's `error.message` contains-assertions and the pytest kind assertions in test_validate.py all keep passing unchanged, and the accept/reject parity between cobre validate and cobre.io.validate is untouched.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/validate.rs | sed -n '141,200p' ; git show 077dbe2c:crates/cobre-python/src/io.rs | sed -n '254,302p'",
        "output": "validate.rs:145 fn describe_prep_error(phase: PrepPhase, err: &SddpError) -> (&'static str, String) {\n:146     let (kind, file_label) = prep_phase_metadata(phase, err);\n:147     (kind, format!(\"{file_label}: {err}\"))\n:148 }\n:182 fn run_prep_phase<T>(result: Result<T, SddpError>, stdout_sink: Option<&Term>, json: bool, phase: PrepPhase, case_dir: &Path) -> Result<T, CliError>\n  -- called three times: :383 PrepPhase::Config, :402 PrepPhase::Stochastic, :418 PrepPhase::HydroModels\n---\nio.rs:257-258   let (kind, file_label) = prep_phase_metadata(PrepPhase::Config, err);       return_error!(kind, format!(\"{file_label}: {err}\"));\nio.rs:291-292   let (kind, file_label) = prep_phase_metadata(PrepPhase::Stochastic, err);   return_error!(kind, format!(\"{file_label}: {err}\"));\nio.rs:300-301   let (kind, file_label) = prep_phase_metadata(PrepPhase::HydroModels, err);  return_error!(kind, format!(\"{file_label}: {err}\"));\n(io.rs:214-225 defines the return_error! macro that fills {valid:false, errors:[{kind,message}], warnings:[]})",
        "reading": "cobre-sddp already owns the phase vocabulary: PrepPhase and prep_phase_metadata give a (kind, file_label) pair. What is not owned anywhere is the one line that turns that pair into a report string, `format!(\"{file_label}: {err}\")`. The CLI recognised it as shared and extracted describe_prep_error; the bindings copied the same two statements into three sites. The message format is therefore a four-copy contract across two crates with a single-copy owner nowhere, and its stated purpose in validate.rs:141-144 is anti-drift — a purpose the Python copies silently opt out of. The two phase drivers around it (run_prep_phase vs the return_error! macro) differ in sink handling and are legitimately front-end-specific; the (kind, message) computation is not. This is the phase-driver half of the validate mirror, distinct from CD-029, whose claim is the PrepPhase enum's doc/variant count and the boundary check running outside the enum.",
        "citedNotAnchored": [
          "crates/cobre-python/src/io.rs::validate (:257-258, :291-292, :300-301)",
          "crates/cobre-sddp/src/validate_phases.rs::PrepPhase"
        ]
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-architecture-04",
      "id": "CD-093",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "seven mis-annotated fences in four modules; the fix is the fence annotation (ignore / text) summary.rs already uses — cosmetic tier; the absent lib target is the correct L4 shape, not a defect",
      "upgradeNote": null,
      "calibrationBasis": "seven doc fences annotated as compilable Rust over use lines that resolve nowhere in a bin-only crate whose own summary.rs already applies the honest ignore/text treatment — a documentation-annotation defect with no code path or contract behind it: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (an L4 entry point is a bin-only crate by design)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [
        "S6b-over-engineering-03",
        "S6b-test-bloat-04"
      ],
      "mergedFrom": [
        "S6b-over-engineering-03",
        "S6b-test-bloat-04"
      ],
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": "CLAUDE.md § Hard Rules — Comment discipline — default-off",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Should cobre-cli gain a [lib] target? Carried from the attacker because it still decides the residue's shape: with a library the four cobre_cli:: examples become compiler-verified, without one they must be de-annotated or de-fenced. Publishing the CLI's renderers, error enum and progress types as a maintained surface with no current consumer is the cost, so the choice is the owner's, not the station's."
      ],
      "survivingClaim": "Seven doc fences in banner.rs, error.rs, progress.rs and templates.rs are annotated as compilable Rust (rust, rust,no_run, or a bare fence) while carrying a use line that resolves nowhere at the pin, and the three cobre:: ones can never resolve because the named items are pub(crate) in a bin-only package the facade neither depends on nor re-exports: the false fence annotation is the defect, not the absent lib target, which is the correct shape for an L4 entry point, and the same crate's summary.rs already applies the honest ignore/text treatment at lines 49, 91 and 100.",
      "title": "Seven doc examples across four CLI modules import crate paths that cannot resolve, and the crate has no lib target so no doctest ever compiles them",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/templates.rs",
          "symbol": "find_template"
        },
        {
          "path": "crates/cobre-cli/src/banner.rs",
          "symbol": "render_banner_string"
        },
        {
          "path": "crates/cobre-cli/src/error.rs",
          "symbol": "CliError"
        },
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "run_progress_thread"
        }
      ],
      "fixShape": "Make each of the seven examples honest at its current fence. Rewrite the three `cobre::` imports to the module-local form the code actually uses, and for the four `cobre_cli::` ones either re-point the path at a crate that exists or de-annotate the fence so the block is no longer claimed as compilable Rust, using a plain-text fence kind where the snippet is purely illustrative and an ignore-annotated fence where it must stay Rust-shaped but cannot compile, which is the same treatment `summary.rs` already applies to its own example at line 49. Do NOT make the facade crate re-export these: the register's reserved-seams pre-check records no entry for `crates/cobre` and routes any facade change to the owner gate. Whether cobre-cli should instead gain a library target, which would make the `pub` surface and all four `cobre_cli::` examples compiler-verified at the cost of publishing the CLI's internals as a contract, is an owner question raised in `_needsHuman` rather than pre-judged here. Byte-neutrality bar: doc-comment-only edits, no runtime code and no output bytes change, so both parity determinism tests and the validate --json contract are untouched.",
      "evidence": {
        "command": "git grep -n '^\\s*//[/!] use cobre' 077dbe2c -- 'crates/cobre-cli/src/*.rs' 'crates/cobre-cli/src/**/*.rs' ; git grep -n '//[/!] ```' 077dbe2c -- 'crates/cobre-cli/src/*.rs' ; git grep -n '\\[lib\\]' 077dbe2c -- crates/cobre-cli/Cargo.toml ; git show 077dbe2c:crates/cobre/src/lib.rs",
        "output": "banner.rs:10   //! use cobre::banner;                                    fence banner.rs:8  ```rust,no_run\nerror.rs:43    /// use cobre_cli::error::CliError;                        fence error.rs:42  ``` (bare -> compiled)\nerror.rs:99    /// use cobre_cli::error::CliError;                        fence error.rs:98  ```\nerror.rs:133   /// use cobre_cli::error::CliError;                        fence error.rs:132 ```\nprogress.rs:16 //! use cobre_cli::progress::{run_progress_thread, RenderMode};  fence progress.rs:13 ```rust,no_run\ntemplates.rs:18  //! use cobre::templates;                                fence templates.rs:17  ```rust\ntemplates.rs:145 /// use cobre::templates;                                fence templates.rs:144 ```rust\n(progress.rs:15 `use cobre_core::TrainingEvent;` is the one line that would resolve)\n---\ngit grep '[lib]' crates/cobre-cli/Cargo.toml -> no match. The manifest declares only:\n  [[bin]]\n  name = \"cobre\"\n  path = \"src/main.rs\"\n  doc = false\n---\ncrates/cobre/src/lib.rs (the whole file):\n  //! Umbrella crate for the Cobre power-system ecosystem; re-exports nothing yet.\n  //! Depend on the individual `cobre-*` crates directly.\n  (and crates/cobre/Cargo.toml has no [dependencies] section at all)",
        "reading": "Seven of the eight doc-comment import lines in this crate name a path that does not exist. Four say `cobre_cli::`, which is not an importable path because the crate declares no [lib] target; three say `cobre::`, the umbrella crate whose lib.rs re-exports nothing and whose manifest has no dependencies, so it could not supply `banner` or `templates` in any configuration. Every one of the seven sits in a fence kind that compiles — `rust`, `rust,no_run` (no_run skips execution, not compilation) or a bare fence, which defaults to Rust — so each would be a hard doctest failure if it were ever collected. None is collected: rustdoc only runs doctests for library targets, and `doc = false` suppresses the bin from rustdoc as well. The result is four modules documented as if they were a published library, with the compiler backstop that makes Rust examples trustworthy absent by construction, and with three of them pointing readers at a crate the register already records as empty. This is the architectural half of the problem rather than a typo count: the crate's entire `pub` surface (`pub fn print_banner`, `pub fn run_progress_thread`, `pub enum RenderMode`, the `pub` renderers in summary.rs) asserts an external contract that no target publishes.",
        "citedNotAnchored": [
          "crates/cobre-cli/Cargo.toml:15-18",
          "crates/cobre/src/lib.rs",
          "crates/cobre/Cargo.toml",
          "docs/design/reserved-seams-and-deferred-debt.md:30"
        ]
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-architecture-05",
      "id": "CD-094",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three engine-type re-exports with no production consumer plus a visibility no-op over all six, under an allow whose rationale is wrong on one point — dead imports and a stale rationale in one module: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 front-end plumbing; the re-export is the I.5-cited surface)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": "I.5",
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [
        "S6b-over-engineering-05"
      ],
      "mergedFrom": [
        "S6b-over-engineering-05",
        "S6b-test-bloat-08"
      ],
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": ".claude/rules/comments.md D4 — Rationale above suppression",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [],
      "survivingClaim": "Three of the six names in the summary.rs re-export (HydroProductionProvenance, InflowProvenance, ProvenanceSource) have no production consumer anywhere in cobre-cli, so their module-scope presence and the #[allow(unused_imports)] covering them are created by the module's own test fixtures alone; the pub is a separate unredeemed visibility no-op over all six names (private mod summary at main.rs:14, one [[bin]] with doc = false, no import through crate::summary), not a test-driven requirement, and the rationale at :18-21 is wrong only on the single point that the pub is what makes the use super at :1968-1969 resolve.",
      "title": "summary.rs imports three cobre_sddp provenance types into production module scope, with pub and allow(unused_imports), only so its own test block's use super resolves",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "print_hydro_model_summary"
        }
      ],
      "fixShape": "Split the declaration: keep a plain `use cobre_sddp::{BoundaryReconciliationReport, HydroModelSummary, ModelProvenanceReport};` for the three types the renderers name in their signatures, and let the #[cfg(test)] block import HydroProductionProvenance, InflowProvenance and ProvenanceSource from cobre_sddp directly instead of through `use super`. Both `pub` and #[allow(unused_imports)] then come off, and the crate stops carrying three engine types in production scope for test-only reasons. Byte-neutrality bar: imports and visibility only, no rendered line and no output file changes, so the CLI/Python determinism and file-set parity tests and the validate --json contract are all untouched; the same test assertions run against the same renderers.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/summary.rs | sed -n '18,27p' ; git show 077dbe2c:crates/cobre-cli/src/summary.rs | grep -n 'HydroProductionProvenance\\|InflowProvenance\\|ProvenanceSource' | head ; git show 077dbe2c:crates/cobre-cli/src/summary.rs | grep -n '#\\[cfg(test)\\]' | tail -1",
        "output": ":18 // Rationale: the `#[cfg(test)]` blocks in this module import\n:19 // `HydroProductionProvenance`, `InflowProvenance`, and `ProvenanceSource` via\n:20 // `use super::{...}`; this `pub use` is the declaration that puts those names on\n:21 // the module surface so those test imports resolve. Without it they would not.\n:22 #[allow(unused_imports)]\n:23 pub use cobre_sddp::{\n:24     BoundaryReconciliationReport, HydroModelSummary, HydroProductionProvenance, InflowProvenance,\n:25     ModelProvenanceReport, ProvenanceSource,\n:26 };\n---\nHydroProductionProvenance / InflowProvenance / ProvenanceSource occur at :19 (the comment), :24-25 (the re-export), then only from :1969 onward -- and the module's test block opens at :911. Zero production occurrences.\nThe other three names ARE production-used: HydroModelSummary :29/:198/:215/:224, ModelProvenanceReport :295/:303/:330, BoundaryReconciliationReport :364/:375/:395.",
        "reading": "Three of the six re-exported engine types have no production consumer in this module; they exist in production scope purely so the test block can write `use super::{...}`, and #[allow(unused_imports)] suppresses the warning that would otherwise say so. The `pub` does the same job for the other three: a bin-only crate has no external consumer, so `pub` here is not an API decision, it is a visibility workaround for an intra-crate test import. The author documented the mechanism honestly at :18-21, which makes this avoidable rather than hidden. Under Part-I item I.5 the CLI's engine coupling is expected at L4 and is not a genericity violation; what is a flaw is that three units of that coupling are paid for by the test code alone, in the crate's production surface, with a lint suppression attached. The register's I.5 sharpening note separately treats the report/summary subcommand doc claim as an E9/E11 correction; that claim is not re-raised here — this is the code-level re-export mechanism, not the doc claim.",
        "citedNotAnchored": [
          "crates/cobre-cli/src/summary.rs:1969 (test-module use super import)",
          "plans/architecture-debt-audit/stations/cli-python/prior-register.md:49"
        ]
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-over-engineering-02",
      "id": "OD-046",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two by-value clap-args signatures whose only effect is the lint they suppress — removable suppressions, not a missing rationale (needless_pass_by_value is outside comments.md D4's closed list): Sev C",
      "category": "redundant-wrapper",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4-internal)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": ".claude/rules/comments.md D4 — Rationale above suppression: Every `#[allow(...)]` for a refactor-decision lint (`clippy::too_many_arguments`, `too_many_lines`, `type_complexity`, `dead_code`, `unused_*`) and every borrow-checker workaround carries a rationale",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "The mirror's allow census (:1272, Load-bearing class) asserts that every refactor-decision allow including `needless_pass_by_value` carries a `// Rationale:`, which is false at schema.rs:49, validate.rs:329 and init.rs:53, while comments.md D4's closed list omits that lint: the owner must decide whether the census prose is corrected to match D4 or D4's list is extended to cover `needless_pass_by_value`, since that choice decides whether the remaining undocumented suppression at init.rs:53 is debt at all."
      ],
      "survivingClaim": "Two of the three named entry points are unjustified, not three, and the residue is a removable suppression rather than a missing rationale: `schema::execute` (schema.rs:49) and `validate::execute` (validate.rs:329) take their clap args by value although every use is a borrow or a `Copy` read, so each `#[allow(clippy::needless_pass_by_value)]` exists only to silence a lint its own signature causes and both vanish under `&SchemaArgs` / `&ValidateArgs` with those arms bound `ref args` like the run arm (run/mod.rs:125). `init::execute` drops out of the claim because init.rs:63-69 consumes `args.template` and `args.directory`, and the absent `// Rationale:` is not a violation because comments.md D4's closed lint list excludes `needless_pass_by_value`.",
      "title": "Three subcommand entry points take their clap args by value and suppress `needless_pass_by_value` without a rationale, while the same `match` binds the run args by reference",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/schema.rs",
          "symbol": "execute"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "execute"
        },
        {
          "path": "crates/cobre-cli/src/commands/init.rs",
          "symbol": "execute"
        },
        {
          "path": "crates/cobre-cli/src/main.rs",
          "symbol": "Command"
        }
      ],
      "fixShape": "Take `&SchemaArgs` and `&ValidateArgs` at the two entry points that never move a field, bind those two arms as `ref args` in main's `match` to match the run arm, and delete the two suppressions - the convention then has one shape across the whole dispatch table. Leave `init::execute` by value because it consumes two fields, and either give its suppression a `// Rationale:` line naming why the lint still fires or delete the suppression if it no longer does; which of the two applies is a clippy question this station does not run, so the fix states both branches rather than guessing. cobre-cli local, no crate move. Byte-neutrality: signature and binding changes only, with no touched format literal, no stdout or stderr text change, no `cobre validate --json` shape change and no written output file, so both parity goldens and check_python_parity.py --min-shared 18 are unaffected.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/main.rs | sed -n '95,101p' && git show 077dbe2c:crates/cobre-cli/src/commands/schema.rs | sed -n '49,55p' && git grep -c 'args\\.' 077dbe2c -- crates/cobre-cli/src/commands/validate.rs",
        "output": "let result = match cli.command {\n    Command::Init(args) => init::execute(args),\n    Command::Run(ref args) => run::execute(args),\n    Command::Validate(args) => validate::execute(args),\n    Command::Schema(args) => schema::execute(args),\n    Command::Version => version::execute(),\n};\n---\n#[allow(clippy::needless_pass_by_value)]\npub fn execute(args: SchemaArgs) -> Result<(), CliError> {\n    match args.command {\n        SchemaCommand::Export(ref export_args) => execute_export(export_args),\n    }\n}\n---\n077dbe2c:crates/cobre-cli/src/commands/validate.rs:10",
        "reading": "One `match` uses two conventions: `Command::Run(ref args)` binds by reference and `run::execute` takes `&RunArgs`, while the other three arms move their args into entry points that take them by value and then silence the lint the move does not justify. `schema::execute` is airtight - its whole body is a `match` with an explicit `ref export_args` binding, so nothing is ever moved out and the by-value parameter exists only to be suppressed. `validate::execute` is equally airtight: all 10 `args.` uses in the file are `args.json` (a `Copy` bool read) or `&args.case_dir`, so no field is moved either, yet the suppression sits at :329. `init::execute` does move two fields out (`args.template.ok_or_else`, `args.directory.ok_or_else` at :63-69), so its by-value signature is defensible and only its missing rationale is at issue. The mirror's own allow-census defines the Load-bearing class as refactor-decision lints including `needless_pass_by_value` `each carrying a // Rationale: comment`; none of these three does, and the same manifest shows the standard being met elsewhere (progress.rs:93, validate.rs:244, version.rs:11)."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-00",
      "id": "TD-063",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two cfg(test) render helpers with formats the production path never emits, while the shipped header literal and the warning-entry render carry zero positive assertions anywhere in the crate — a coverage hole behind a parallel test-only shape: Sev B (the shipped output can drift green)",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Tiers — use the cheapest tier that catches the regression",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Narrower residue at the pin: production declares no report-rendering function anywhere in validate.rs 1-478, so the two cfg(test) helpers are not a drifted copy of a shipped function but a parallel shape, and exactly two of their observable formats are unreachable in production -- a header with both counts nonzero, contradicted by the hard-coded 1 errors, 0 warnings at 152 and 207, the hard-coded 0 errors at 436 and the warning_count 0 argument justified at 355, and the entry form file: message (entity), contradicted by the only production entry render at 441-450 which emits warning: file (entity): message. The harm that survives is therefore coverage, not duplication: the shipped header literal and the warning-entry render carry zero positive assertions in crates/cobre-cli/tests, whose only two warning: assertions, cli_validate.rs 733 and 782, assert absence. Conceded from the title: the shadowing mechanism is not load-bearing, because a module's own items outrank a glob import wherever use super::* sits and super declares no same-named item, so the placement at 531 changes nothing; and drifted is not established at the baseline, since no shipped renderer the copy could have tracked exists.",
      "title": "The seven inline tests of the validate subcommand assert a test-local reimplementation of the report renderer, not the shipped one, and the copy has already drifted",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "format_report_to_string"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "format_entry"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "execute"
        },
        {
          "path": "crates/cobre-cli/src/commands/validate.rs",
          "symbol": "print_prep_error"
        }
      ],
      "fixShape": "Extract the rendering that execute inlines at 436-448 into a private fn that returns the lines (the same shape summary.rs already uses for training_summary_lines and simulation_summary_lines, where tests and production share one code path), delete the test-module format_report_to_string and format_entry copies, and repoint the seven tests at the extracted fn. Consolidate the five format_report_* cases into one table-driven test over (report, expected substrings) since they all render make_report once. Byte-neutral: the extracted fn keeps the exact literals now inlined in execute, so no stdout or stderr byte changes and every substring cli_validate.rs pins stays asserted.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/commands/validate.rs | sed -n '479,611p'  #  then: git show 077dbe2c:crates/cobre-cli/src/commands/validate.rs | sed -n '1,478p' | grep -nE '^[[:space:]]*(pub )?fn ' and grep -n 'error:|warning:|format_entry|Validation: '",
        "output": "test module (479-611): fn format_report_to_string(report, path) -> String  /  fn format_entry(entry: &ReportEntry) -> String  /  fn make_report() -> ValidationReport  then `use super::*;` AFTER both definitions, then 7 #[test] fns, all of which call format_report_to_string or format_entry.  production half (1-478) declares: format_constraint_description, describe_prep_error, print_prep_error, prep_error_to_cli_error, run_prep_phase, format_boundary_error, reconcile_boundary, run_boundary_check, emit_validate_json, execute -- there is no format_entry and no report-rendering fn at all; the rendering is inlined in execute at 436-448: \"Validation: 0 errors, {} warnings in {}\", then let location = if let Some(entity) = &entry.entity { format!(\"{} ({})\", entry.file, entity) }, then style(\"warning:\").yellow().bold().",
        "reading": "Production renders the validation report inline inside execute (three hard-coded header shapes at 131, 152, 207, 436). The test module defines its own format_report_to_string plus format_entry and places `use super::*;` after them, so the glob import cannot shadow the local items: all 7 tests exercise the test copy and zero lines of the shipped renderer. The copy has drifted already -- format_report_summary_header_present asserts the output contains \"1 errors\" and \"1 warnings\", while the production success path at 436 emits a literal \"0 errors\" and the two error paths at 152 and 207 emit \"1 errors, 0 warnings\". No production path can emit the header the unit test pins. Real coverage of the shipped text exists only in the process tier (cli_validate.rs valid_case_stdout_contains_buses_count, missing_buses_json_stdout_contains_error), so the cheapest tier that could catch a renderer regression is occupied by a closed loop instead."
      },
      "yardstickSection": "§3.2 sustainability (the shipped renderer must be the asserted one) / §5.3 tier taxonomy",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-01",
      "id": "TD-064",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "four cfg(test) format twins hold the only asserted copy of the printers' header, labels, line set and mapping, so the four shipped printers can drift while their smoke tests stay green — Sev B (drift exposure on shipped renderers); the computed values are shared helpers and excluded",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [
        "S6b-over-engineering-01"
      ],
      "mergedFrom": [
        "S6b-over-engineering-01"
      ],
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Narrower than the title: only the header word plus its console::style bold wrapper, the label text and column padding, the set and order of lines, and the field-to-label mapping in print_provenance_summary are drift-exposed in the four shipped printers at summary.rs:29, 239, 303 and 375, because the cfg(test) twins at 224, 265, 330 and 395 hold the only asserted copy of those and the eight smoke tests over those four printers read no rendered byte (one of the eight does assert, but on the twin). The computed values are NOT exposed, since both sides call the same private format helpers; and the three printers routing through a shared lines helper plus banner.rs are correctly smoke-tested, so the eleven/fourteen panic-only count is not part of the defect.",
      "title": "Four cfg(test)-gated format_*_string twins in summary.rs let the shipped print_* renderers drift while the tests stay green; the shipped path is asserted only for absence of panic",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "format_hydro_model_summary_string"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "format_setup_summary_string"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "format_provenance_summary_string"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "format_boundary_summary_string"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "print_hydro_model_summary"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "training_summary_lines"
        }
      ],
      "fixShape": "Apply the pattern the module header already declares and the file already uses three times: convert each of the four print_* twins to build its lines through a private *_lines helper that returns Vec<String>, have print_* write those lines, and delete the four cfg(test) format_*_string functions, repointing their tests at the helper. The eleven does_not_panic tests then stop being the only coverage of the shipped path and collapse into the content assertions. Byte-neutral: the *_lines helpers carry the literals verbatim out of the current print_* bodies, so no stderr byte changes and every substring the integration binaries pin stays asserted.",
      "evidence": {
        "command": "git grep -n 'fn .*does_not_panic|fn .*no_panic' 077dbe2c -- crates/cobre-cli/src   and   git show 077dbe2c:crates/cobre-cli/src/summary.rs | grep -n 'cfg(test)|fn format_.*_string|fn print_'",
        "output": "14 panic-only tests in the S6b manifest, 11 of them in summary.rs: print_training_summary_does_not_panic 1505, print_simulation_summary_does_not_panic 1511, print_output_path_does_not_panic 1530, print_hydro_model_summary_does_not_panic 1788, print_hydro_model_summary_all_constant_does_not_panic 1794, print_hydro_model_summary_all_fpha_does_not_panic 1800, print_setup_summary_does_not_panic 1961, print_provenance_summary_does_not_panic 2010, print_provenance_summary_deterministic_does_not_panic 2016, print_provenance_summary_with_hydro_counts_does_not_panic 2154, print_boundary_summary_does_not_panic 2248; plus banner.rs test_print_banner_does_not_panic 93 and two no_panic in progress.rs (907, 1011). Four #[cfg(test)] pub fns re-implement their twin's literals: format_hydro_model_summary_string 224 vs print_hydro_model_summary 29, format_setup_summary_string 265 vs print_setup_summary 239, format_provenance_summary_string 330 vs print_provenance_summary 303, format_boundary_summary_string 395 vs print_boundary_summary 375.",
        "reading": "The module header at summary.rs lines 5-9 states the intended contract itself: most print_* writers pair with a format_*_string for tests, while print_training_summary, print_simulation_summary and print_output_path instead share their exact rendered lines with tests through a private *_lines helper 'so tests assert on the same code path production prints from'. Three functions honour that rule; four do not. For those four, every content assertion runs against a hand-copied format string, and the shipped print_* is covered only by a does_not_panic smoke test -- so a literal edited in print_hydro_model_summary but not in format_hydro_model_summary_string ships with a green suite. The same file already contains the correct pattern (training_summary_lines, simulation_summary_lines, output_path_line), so this is an internal asymmetry, not a missing idea."
      },
      "yardstickSection": "§3.2 sustainability / §5.10 golden standard (assert the rendered bytes, not a twin)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-02",
      "id": "TD-065",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a 24-line fixture constant byte-identical across five integration binaries and a full fixture set identical across two, with no tests/common/ in the crate — one schema change is a five-file edit against the Cost-discipline one-place property: Sev B; confidence med by the Legend's rule (the attacker's evidence is a grep count; the defender's read confirms the byte-identity but the evidence command on record is the count)",
      "category": "duplication",
      "effort": "S",
      "confidence": "med",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "The fixture fan-out, not the binary-invocation helper: with no crates/cobre-cli/tests/common/, the 24-line PENALTIES_JSON const is byte-identical across five test binaries (cli_color, cli_run, cli_run_anticipated, cli_run_anticipated_k2, cli_validate) and the full eight-const plus write_file plus make_valid_case fixture is verbatim identical between cli_run.rs and cli_color.rs, so one new required field in penalties.json or a system/*.json entity is a five-file edit against the Cost discipline one-place property; the thirteen copies of the one-line fn cobre() delegation to assert_cmd's cargo_bin! macro, and the unexercised run-size divergence in cli_validate.rs's copy, are not part of the surviving defect.",
      "title": "cobre-cli has no tests/common at all: the binary invocation helper is redefined in 13 of 14 integration binaries and the valid-case fixture in three, against the verbatim text of the standing testing contract",
      "anchors": [
        {
          "path": "crates/cobre-cli/tests/cli_color.rs",
          "symbol": "make_valid_case"
        },
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "make_valid_case"
        },
        {
          "path": "crates/cobre-cli/tests/cli_smoke.rs",
          "symbol": "cobre"
        },
        {
          "path": "crates/cobre-cli/tests/cli_schema.rs",
          "symbol": "cobre"
        },
        {
          "path": "crates/cobre-cli/tests/init.rs",
          "symbol": "cobre"
        },
        {
          "path": "crates/cobre-cli/tests/cli_color.rs",
          "symbol": "write_file"
        }
      ],
      "fixShape": "Add crates/cobre-cli/tests/common/mod.rs holding the single fn cobre() built on assert_cmd cargo_bin, the write_file helper, and one parameterised valid-case builder whose knobs are the fields that actually differ today (stage count, forward passes, iteration limit, openings), then have each binary declare mod common and delete its private copy. This is a consolidation inside the existing tests directory, not a new test binary and not a fixture crate: tests/common/mod.rs is compiled into each binary that declares it, so the binary count stays at 14 and no new solver link is paid. Byte-neutral: each call site keeps the field values it uses today by passing them to the builder, so no case JSON changes and no assertion is weakened.",
      "evidence": {
        "command": "git ls-tree -r --name-only 077dbe2c -- crates/cobre-cli/tests   and   for h in 'fn cobre()' 'fn write_file' 'fn make_valid_case' 'fn run_case'; do git grep -l \"$h\" 077dbe2c -- crates/cobre-cli/tests | wc -l; done",
        "output": "14 flat .rs files, no common/ directory: cli_color.rs, cli_e2e_run_end_block.rs, cli_run.rs, cli_run_anticipated.rs, cli_run_anticipated_k2.rs, cli_run_evaporation.rs, cli_run_generic_echo.rs, cli_schema.rs, cli_smoke.rs, cli_validate.rs, init.rs, output_metadata_active_backend.rs, python_parity_check.rs, setup_timings_metadata.rs. Redefinition counts: fn cobre() 13 files, fn write_file 5, fn make_valid_case 3, fn run_case 2. cli_color.rs:20 carries the admission in a comment: 'Minimal valid-case fixture, duplicated (not shared) with cli_run.rs to keep this test module self-contained.'",
        "reading": ".claude/rules/testing.md:118-121 (the standing contract) says shared harness helpers and entity construction 'live once in tests/common/ -- never re-defined per file', and gives the reason: adding a required field to an entity is then a one-place change and 'no tests/*.rs consumer is touched'. cobre-cli has no tests/common/ at all, so cobre-cli pays the inverse: the eight-const valid-case fixture is maintained in three copies that have already diverged (cli_validate.rs uses forward_passes 10, iteration_limit 100, 1 stage, num_openings 50; cli_color.rs uses 1, 2, 2 stages, 2), and a new required field in buses.json or hydros.json is a 3-file edit plus 5 write_file copies. The divergence is not itself wrong -- the two suites want different run sizes -- but it is undeclared: the copies are byte-similar enough that a reader cannot tell which differences are deliberate."
      },
      "yardstickSection": "§5.1 canonical layout (tests/common/ for cross-binary fixtures) / §3.2 sustainability",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-03",
      "id": "TD-066",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three full trainings in the colour binary for claims the unit tier already pins, one of which never exercises the banner it names — Sev B on the Tiers rule (three solver runs for a flag-parsing claim observable at a solve-free call site); the ten validate-binary runs are fixture production and stay",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Tiers — use the cheapest tier that catches the regression",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Narrowed to the colour binary alone: the three cobre run invocations at cli_color.rs 121/146/170 each pay a full SDDP training for claims already pinned at the unit tier (banner.rs:63-73 for the escape bytes, main.rs:118-132 for resolve_color setting the console global), whose only integration residue -- clap parsing --color into ColorWhen, main.rs:83 applying it before output, and global = true placement -- is observable through the solve-free print_banner call site at init.rs:148; and color_never_flag_suppresses_ansi_in_banner does not exercise the banner at all, because setup.rs:209-210 gates print_banner behind !quiet while the test passes --quiet. The ten runs in cli_validate.rs are explicitly NOT part of the surviving claim: nine are documented policy-checkpoint fixture production (cli_validate.rs:562-563) and the tenth asserts the validate-versus-run agreement that is the validate module contract.",
      "title": "Ten full cobre run trainings execute inside the validate binary and three more inside the color binary, to assert claims the unit tier already covers",
      "anchors": [
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "run_case"
        },
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "boundary_superset_source_under_strict_exits_nonzero_and_names_the_family"
        },
        {
          "path": "crates/cobre-cli/tests/cli_color.rs",
          "symbol": "color_always_flag_forces_ansi_in_banner"
        },
        {
          "path": "crates/cobre-cli/tests/cli_color.rs",
          "symbol": "color_never_flag_suppresses_ansi_in_banner"
        },
        {
          "path": "crates/cobre-cli/src/banner.rs",
          "symbol": "render_banner_string"
        },
        {
          "path": "crates/cobre-cli/src/commands/init.rs",
          "symbol": "execute"
        }
      ],
      "fixShape": "For the color suite, keep one test that exercises the flag-to-global wiring through cobre init (init calls print_banner and performs no solve) and drop the two remaining run-based cases, since banner.rs already pins the escape bytes at the unit tier. For cli_validate.rs, move the six boundary tests that call run_case, and invalid_simulation_scenario_source_fails_validate_and_run, into the existing cli_run.rs domain binary as a mod boundary block, leaving cli_validate.rs as a solve-free suite; this adds no binary, so no new solver link is paid. Byte-neutral: every stderr and stdout substring currently pinned stays pinned, only the binary that owns the assertion changes.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/tests/cli_validate.rs | grep -n 'run_case|\"run\"'   and   git show 077dbe2c:crates/cobre-cli/tests/cli_color.rs | grep -n '\"run\"|iteration_limit'   and   git grep -n 'print_banner' 077dbe2c -- crates/cobre-cli/src",
        "output": "cli_validate.rs: fn run_case at 564 shelling cobre run, called at 613, 638, 654, 694, 698, 718, 722, 763, 767 (nine calls), plus a tenth direct run at 334. cli_color.rs: three cobre run invocations at 121, 146, 170 over a 2-stage case with iteration_limit 2, each asserting only banner bytes on stderr. banner.rs: print_banner declared at 47, called from commands/init.rs:148 and commands/run/setup.rs:210; inline tests test_render_banner_colored_contains_ansi_escapes and test_render_banner_plain_no_ansi_escapes already assert the ESC[38;5;172m prefix and its absence against render_banner_string at 22.",
        "reading": ".claude/rules/testing.md:113-117 states the cost model: cobre-solver statically links HiGHS or CLP into every dependent tests/*.rs. Both binaries then spend solver-linked subprocess runs on claims that do not need a solve. The three color tests assert exactly what banner.rs already asserts at the unit tier (ANSI escapes present with color, absent without); their only residual claim is that resolve_color wires the console global from the flag, and print_banner is reachable from commands/init.rs:148, which performs no LP solve. In cli_validate.rs the ten trainings sit inside a binary named for the validate subcommand, so the cheap validate suite cannot be run without paying for ten SDDP trainings, and a training regression fails a validate test."
      },
      "yardstickSection": "§5.3 tier taxonomy (the cheapest tier that catches the regression)",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-05",
      "id": "TD-067",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "four of fifteen renderer-thread tests are strict subsets of retained ones, one cobre-core statistics assertion is homed in cobre-cli and one formatter branch is never executed — pruning and homing inside one inline module: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [
        "S6b-performance-01"
      ],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "The inline module's 608 test lines exceed the unratified ~500 test-LOC inline homing threshold in docs/design/testing-architecture.md 5.1 while its 22 fns sit under the ~40-fn threshold, so whether the consolidation lands inline or in an extracted src/progress/tests.rs sibling is the test-corpus station's ratification call, not this station's."
      ],
      "survivingClaim": "Four of the fifteen spawn tests assert nothing a retained test does not: test_progress_handle_training_events_returned (673) is a length-only subset of 727 over the same two Interactive arms; test_simulation_progress_five_events_no_panic (907) and test_simulation_progress_accumulator_three_events_no_panic (1011) are filtered-count subsets of 946; and test_simulation_progress_message_single_scenario (835) exercises no distinct arm because BarRenderer (277-285) holds no per-sample accumulator. Two further precise residues hold: 946's WelfordAccumulator mean and sample_std_dev block (987-1007) is a cobre-core unit assertion homed in cobre-cli that touches no progress.rs path, and every SimulationProgress event in the module sets lp_solves to zero, so the six Interactive progress tests leave fmt_avg_lp_time's populated branch (95, used at 376 and 504) unexecuted. The candidate's other two subsumption claims are rejected: 688 and 775 assert same-variant payload ordering that the widest test at 727 does not, so the remaining eleven spawns each carry a distinct arm, mode or ordering claim.",
      "title": "Fifteen of the twenty-two inline progress tests spawn a real renderer thread to re-assert one channel round-trip contract, five of them in cases the widest test already subsumes",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "run_progress_thread"
        },
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "ProgressHandle"
        },
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "RenderMode"
        },
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "fmt_hms"
        }
      ],
      "fixShape": "Collapse the fifteen spawn-and-inspect tests into one table-driven test over (event sequence, expected variant order) plus the empty-channel edge and one Log-mode case, keeping every distinct sequence currently exercised as a row. That preserves coverage of the join() contract training.rs:107 depends on while dropping about a dozen thread spawns. Byte-neutral by construction: none of these tests captures rendered output today, so no rendered text contract is touched.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/progress.rs | sed -n '533,1140p'   and   git grep -n '.join()' 077dbe2c -- crates/cobre-cli/src/commands/run/training.rs",
        "output": "The cfg(test) module starts at 533, so 608 of the file's 1140 lines are tests. Fifteen tests call run_progress_thread(rx, RenderMode::Interactive or Log, ...) and then assert only on the Vec returned by join(): simulation_started_event_is_preserved_and_renders_banner, test_progress_handle_training_events_returned (len 4), test_progress_handle_simulation_events_returned (len 3), test_progress_handle_returns_all_events (len 5 plus five matches! variant checks), test_empty_channel_returns_empty_vec, test_training_only_no_simulation_events (len 6 plus a matches! loop), test_simulation_progress_message_with_statistics, test_simulation_progress_message_single_scenario, test_non_ui_events_are_collected, test_simulation_progress_five_events_no_panic, test_simulation_progress_accumulator_costs_collected_correctly, test_simulation_progress_accumulator_three_events_no_panic, test_log_mode_training_events_round_trip, test_log_mode_simulation_events_round_trip, test_log_mode_ignores_non_ui_events_without_panic. One of them carries the comment 'The rendered banner is not captured; this asserts only the channel round-trip.' training.rs:107 shows the returned Vec is production-load-bearing: (Some(handle), _) => handle.join().",
        "reading": "The collected Vec is a real production contract, so these tests are not tautologies -- that framing would be wrong. The redundancy is in the case count: test_progress_handle_returns_all_events already sends a mixed training-plus-simulation sequence and checks both length and variant order, which subsumes test_progress_handle_training_events_returned, test_progress_handle_simulation_events_returned, test_training_only_no_simulation_events and the two accumulator no_panic cases; only test_empty_channel_returns_empty_vec adds a distinct edge. Each of the fifteen spawns an OS thread and constructs a renderer, and none captures rendered output, so the rendering half of run_progress_thread stays unasserted while its collection half is asserted fifteen times. The six pure-arithmetic tests in the same module (fmt_hms_pads_hours_minutes_seconds, three eta_millis cases, two fmt_time_cell cases) are the right shape and are listed as a positive."
      },
      "yardstickSection": "§5.3 tier taxonomy / §3.2 sustainability",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-06",
      "id": "TD-068",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "four exit-code-only integration tests are proper subsets of a named sibling on identical fixture and argv — deletion is coverage-neutral; the residue is duplicated fixture-build plus spawn, not link cost: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Exactly four exit-code-only tests in crates/cobre-cli/tests/cli_validate.rs assert a proper subset of a named sibling's predicates on an identical fixture and identical argv (valid_case_exits_0 under :108-117; missing_buses_json_exits_1 under :131-142 and :144-155; nonexistent_path_exits_2 under :256-264; fpha_hydro_without_production_models_json_fails_validate under :801-812), so their deletion is coverage-neutral by subsumption; the cli_smoke.rs version trio is NOT test-level redundant (only the contains(active_solver_name()) predicate at cli_smoke.rs:66 is dominated) and the residue rests on duplicated fixture-build plus spawn, not on the § Cost discipline link cost, which that rule attributes to the test binary and which expressly prefers more test functions in an existing binary.",
      "title": "One assertion per subprocess: several integration tests re-run the same invocation to check a second substring of the same output",
      "anchors": [
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "missing_buses_json_exits_1"
        },
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "missing_buses_json_stdout_contains_error"
        },
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "missing_buses_json_stdout_mentions_file"
        },
        {
          "path": "crates/cobre-cli/tests/cli_validate.rs",
          "symbol": "valid_case_exits_0"
        },
        {
          "path": "crates/cobre-cli/tests/cli_smoke.rs",
          "symbol": "version_exits_0_and_contains_version_string"
        },
        {
          "path": "crates/cobre-cli/tests/cli_smoke.rs",
          "symbol": "version_stdout_contains_active_solver"
        }
      ],
      "fixShape": "Merge each split family into a single test that builds the fixture once, spawns once, and chains the exit-code and substring predicates on one Assert. Byte-neutral: the set of asserted substrings is unchanged, only the number of spawns drops.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/tests/cli_validate.rs | grep -n '^fn |^    fn |#\\[test\\]' ; git show 077dbe2c:crates/cobre-cli/tests/cli_smoke.rs",
        "output": "cli_validate.rs holds 22 tests in 812 lines. Four split families each rebuild the same case and respawn the binary to assert one more substring of one invocation's output: missing_buses_json_exits_1 / missing_buses_json_stdout_contains_error / missing_buses_json_stdout_mentions_file (three builds, three spawns); valid_case_exits_0 / valid_case_stdout_contains_buses_count; nonexistent_path_exits_2 / nonexistent_path_stderr_mentions_path; fpha_hydro_without_production_models_json_fails_validate / fpha_hydro_without_production_models_json_stdout_mentions_file. cli_smoke.rs holds 10 tests in 112 lines, three of which spawn cobre version to assert one substring each: version_exits_0_and_contains_version_string, version_exits_0_and_stdout_contains_cobre_prefix, version_stdout_contains_active_solver.",
        "reading": "Eleven process spawns carry five distinct claims. assert_cmd allows chaining several predicates on one Assert, so each family can be one test that spawns once and asserts exit code plus every substring, with the failure message naming which predicate failed. The split buys no isolation here because all members of a family construct the same fixture and run the same argv; it only multiplies solver-linked process starts, which .claude/rules/testing.md:113-117 identifies as the expensive unit in this workspace."
      },
      "yardstickSection": "§5.3 tier taxonomy / §3.2 sustainability",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-07",
      "id": "TD-069",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one template test freezes a hand-maintained file count beside the table it counts and leaves the examples-side direction uncovered, against the crate's own derived-set pattern — one test, one direction: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Narrowed to one test and one direction: test_1dtoy_template_has_files (templates.rs:177-180) freezes the file count as a hand-maintained literal in the same file as the table it counts, against the same crate's documented derived-set pattern at cli_schema.rs:43-65, and leaves the examples-side-only direction uncovered (a file added to examples/1dtoy and to crates/cobre-cli/templates/1dtoy but omitted from DTOY1_FILES passes every test at the pin, because the byte-identity guard at templates.rs:204-221 iterates the array rather than walking the source directory). The other six tests are defended: the relative-path guard covers a Path::join absolute-component escape at init.rs:122, the description guard is the only assertion of that property, and the three registry-lookup tests already have live duplicates in init.rs so their retention rests on coverage rather than on the inert doc examples.",
      "title": "Six of the seven template tests assert compile-time literals or restate an inert doc example; the one real guard sits next to a frozen file count the sibling schema suite deliberately refuses",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/templates.rs",
          "symbol": "tests"
        },
        {
          "path": "crates/cobre-cli/src/templates.rs",
          "symbol": "TemplateFile"
        },
        {
          "path": "crates/cobre-cli/src/templates.rs",
          "symbol": "DTOY1_FILES"
        },
        {
          "path": "crates/cobre-cli/tests/cli_schema.rs",
          "symbol": "test_schema_export_writes_files"
        }
      ],
      "fixShape": "Delete test_1dtoy_files_have_descriptions and test_1dtoy_files_have_relative_paths (static literals, nothing can change them at runtime), and replace the frozen assertion in test_1dtoy_template_has_files with a derived comparison in the style of cli_schema.rs test_schema_export_writes_files: read the relative paths under examples/1dtoy into a BTreeSet and compare it with the set of relative_path values, which turns the count into a real guard against a file added on one side only. Keep the two find_template tests as the surviving home for the claim once the inert doc examples go, and keep test_1dtoy_embedded_files_match_canonical_source unchanged. Byte-neutral: no template bytes and no rendered output change.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-cli/src/templates.rs   and   git show 077dbe2c:crates/cobre-cli/tests/cli_schema.rs",
        "output": "templates.rs holds 7 inline tests. test_1dtoy_files_have_descriptions and test_1dtoy_files_have_relative_paths loop over DTOY1_FILES asserting each description and relative_path is non-empty and does not start with a slash -- DTOY1_FILES is a static array of string literals declared 120 lines above in the same file. test_available_templates_contains_1dtoy, test_find_template_1dtoy_returns_some and test_find_template_unknown_returns_none restate exactly what the two fenced examples at 17-25 and 144-149 assert. test_1dtoy_template_has_files asserts template.files.len() == 11. test_1dtoy_embedded_files_match_canonical_source iterates every file, reads examples/1dtoy/<relative_path> and asserts byte equality, covering JSON and parquet alike. By contrast cli_schema.rs test_schema_export_writes_files derives emitted and committed basenames as BTreeSets at test time and says in a comment that it is never a hard-coded count.",
        "reading": "One of the seven catches a real regression class: the embedded copy drifting from examples/1dtoy, which build.rs re-embeds and which no compiler checks. That one is right-sized and is listed as a positive. The other six do not: two assert non-emptiness of compile-time statics visible in the same file, three duplicate the doc examples at 17-25 and 144-149 (which, per candidate TB-S6b-05, never execute -- so the duplication is the only thing keeping the claim alive, and after deleting the examples these three become the sole home and should stay), and test_1dtoy_template_has_files freezes 11 as a hand-maintained literal in the same file where its sibling guard already walks the whole set. Two tests over one file set in one crate take opposite approaches to the same question, and cli_schema.rs shows which one this crate considers correct."
      },
      "yardstickSection": "§3.2 sustainability (derive the set, never freeze a count) / §5.10 golden standard",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6b-test-bloat-09",
      "id": "TD-070",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6b",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one of two byte-identical fmt_sci copies is wholly unasserted, so a divergence confined to it ships green — Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4-internal)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [],
      "survivingClaim": "Only the progress.rs copy of fmt_sci is wholly unasserted: its test module imports exclude fmt_sci and every progress test asserts on the collected TrainingEvent vector, never on a rendered bar message or log line, so a change confined to that copy (including a divergence from the summary copy) ships green; the summary copy's positive-exponent rendering is already pinned by two substring assertions on training_summary_lines, and the exponent cases unasserted on both sides are the negative and zero exponents.",
      "title": "fmt_sci is defined byte-identically in two S6b modules and neither copy has a direct test, so a divergence between the two renderers is unasserted",
      "anchors": [
        {
          "path": "crates/cobre-cli/src/progress.rs",
          "symbol": "fmt_sci"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "fmt_sci"
        },
        {
          "path": "crates/cobre-cli/src/summary.rs",
          "symbol": "training_summary_lines"
        }
      ],
      "fixShape": "Keep one fmt_sci, delete the other, and give the surviving copy one table-driven unit test over the exponent cases the two call sites exercise (positive and negative exponents, zero, and a value whose exponent is a single digit). Both modules are private modules of the same binary, so the surviving definition can live in whichever of the two files owns the other shared formatting helpers and be referenced by the second, with no new module and no new dependency. Byte-neutral: the two bodies are identical today, so no rendered digit changes and the existing training_summary_lines_scientific_notation assertion keeps passing unchanged.",
      "evidence": {
        "command": "git grep -n 'fmt_sci' 077dbe2c -- crates/cobre-cli",
        "output": "progress.rs:39 fn fmt_sci(v: f64) -> String, called at 337, 338, 351, 352, 490, 491. summary.rs:414 fn fmt_sci(v: f64) -> String, called at 800, 804, 805. Twelve hits in total and not one of them is inside a cfg(test) module: no test in either file calls fmt_sci directly. Both bodies are the same five lines, formatting with {v:.5e} and stripping the exponent's leading zeroes and plus sign.",
        "reading": "The two copies render the bound values the user reads in the progress table and in the post-run summary, so they are one user-visible contract implemented twice. Neither has a unit test; the only coverage is indirect, through summary.rs training_summary_lines_scientific_notation, which asserts on a whole rendered line. A change to the exponent handling in one copy and not the other therefore ships with a green suite, and the progress-table copy has no assertion path at all. Because the two bodies are identical today, this is cheap to close by deletion rather than by adding a second test."
      },
      "yardstickSection": "§5.3 tier taxonomy / §3.2 sustainability",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-architecture-01",
      "id": "CD-095",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a hand-kept ten-name copy of cobre-io's fourteen-family declaration that makes the documented load-everything path silently omit four written families — a cross-crate hand-mirror with a false completeness promise and no shared owner: Sev B on the CD-025 precedent (bounded: the explicit-name branch still reads the data)",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part IV §IV.1 (L2 cobre-io declares SIMULATION_FAMILIES; the reader consumes the declaration, never a hand-kept copy) / Part V §V.1 Phase 0",
      "defenderAlignmentHint": "advances-0a",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "alignment"
      ],
      "needsHuman": [],
      "survivingClaim": "Scoped to the entity_type=None branch of both readers (results.rs:1147 and :1389): the hand-kept ENTITY_TYPES omits four subpaths SIMULATION_FAMILIES declares, unconditionally for hydro_bus_generation (declared by family_has_hydros, hence written by every hydro deck) and deck-conditionally for in_transit and transit_seed (travel-time arcs) and anticipated_lanes (post-study stages), so the returned dict silently lacks those keys while the docstrings at :1095 and :1322 promise all available types; the explicit-name branch still reads them, so the defect is the completeness promise and the parameter-doc enumeration, not unreachable data.",
      "title": "results.rs re-enumerates the simulation entity-family list as a ten-name constant against the fourteen families cobre-io writes, so the documented load-everything path silently omits four written families",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "ENTITY_TYPES"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_simulation"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_simulation_arrow"
        }
      ],
      "fixShape": "The family-name set has exactly one owner already, and it is not the bindings. cobre-io should expose the family names it writes as public data next to SIMULATION_FAMILIES, and both readers should iterate that instead of a hand-kept copy, which makes the next family the writer gains visible to Python the moment it is written. Until that export exists the honest interim is to stop promising completeness: the docstring should say which families the call returns. Byte-neutrality bar: this is read-only, so the written tree does not change and all three write-path bars are untouched — the CLI-vs-Python golden test_cli_python_determinism_parity.py, test_cli_python_file_set_parity.py and the 18-name floor of scripts/ci/check_python_parity.py all hold by construction. The test this needs is a new one on the read side: load the deck that exercises travel-time arcs and post-study stages, then assert the no-argument load returns a key for every directory present under simulation/.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '1068,1079p'; git show 077dbe2c:crates/cobre-io/src/output/simulation_writer.rs | grep -oE 'subpath: \"[a-z_/]+\"' | sort; git show 077dbe2c:crates/cobre-python/src/results.rs | grep -n 'all available entity types'",
        "output": "const ENTITY_TYPES: &[&str] = &[\n    \"costs\", \"buses\", \"hydros\", \"thermals\", \"exchanges\",\n    \"pumping_stations\", \"contracts\", \"non_controllables\",\n    \"inflow_lags\", \"violations/generic\",\n];\n(cobre-io, sorted) anticipated_lanes, buses, contracts, costs, exchanges, hydro_bus_generation, hydros, inflow_lags, in_transit, non_controllables, pumping_stations, thermals, transit_seed, violations/generic\n1095:///   returned. When `None`, all available entity types are loaded and a dict of\n1322:///   returned. When `None`, all available entity types are loaded and a",
        "reading": "Ten names in the bindings against fourteen subpath values in the writer. The four absent from ENTITY_TYPES are anticipated_lanes, hydro_bus_generation, in_transit and transit_seed. cobre-io names itself the owner of this set: simulation_writer.rs:29 says 'SIMULATION_FAMILIES is the single table of every' family and the writer loops over it at :588. Both binding readers iterate ENTITY_TYPES in their entity_type=None branch (load_simulation body at results.rs:1147-1154, .exists()-gated), while both docstrings promise 'all available entity types are loaded' (:1095, :1322). A Python caller who omits the argument on a study with travel-time arcs or post-study stages gets a dict with no in_transit, transit_seed or anticipated_lanes key and no error, and the same for hydro_bus_generation on any multi-bus hydro deck. This is not an abort and not unreachable data: the explicit branch joins the name without validating it against ENTITY_TYPES (results.rs:1143-1145), so load_simulation(dir, 'in_transit') does work — which is why the rating is B rather than A. Nothing detects the gap: the file-set parity test compares what the two front ends WRITE, and the parity gate matches writer names, so neither sees a reader that reads less than the writer wrote."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-architecture-02",
      "id": "CD-096",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one written column dropped from a reader whose doc promises schema parity, guarded only by a key-presence test, with a doc-only mirror drift in the arrow sibling — a real one-function defect in the public Python API: Sev B (a promised column is silently missing; not C because the dict is the published contract)",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.1 (the schema is L2's; convergence_schema is pub(crate), so the fix needs cobre-io to expose the column list or the reader to iterate the parquet schema)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": ".claude/rules/comments.md § Contract-mirroring beats DRY (but a mirror is shape, never a number)",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "load_convergence drops exactly one written column, mean_rows_in_lp, from the mapping it returns while its own doc claims the keys match the convergence.parquet schema, and the drop is unguarded because test_load_convergence_dict_keys checks key presence only; the arrow sibling's fourteen-row table is a non-behavioural rustdoc mirror drift under the comments.md mirror clause rather than a data defect, and the residue is schema-driven iteration inside cobre-python, since convergence_schema is pub(crate) and cannot be named across the crate boundary.",
      "title": "load_convergence hand-lists fourteen of the fifteen columns cobre-io declares for convergence.parquet, and its sibling load_convergence_arrow documents the same fourteen while passing all fifteen through",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_convergence"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_convergence_arrow"
        }
      ],
      "fixShape": "A reader that must name columns should derive them from the schema that declares them rather than from a second list. For the dict reader that means iterating the file's own schema fields, which is what the arrow reader already does correctly and which removes the drift class entirely, not just this instance; for the arrow reader's doc table it means stating the column set by reference to cobre-io's convergence schema rather than re-typing it, per the doc-integrity rule against freezing an enumeration without a guard. Byte-neutrality bar: read-only, so the written tree is unchanged and the CLI-vs-Python golden, the file-set parity test and the 18-name parity floor are all untouched. The regression that pins it is an assertion that the keys of the returned mapping equal the field names of the written convergence file, which fails today on mean_rows_in_lp.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '243,336p' | grep -c 'get_column_by_name'; git show 077dbe2c:crates/cobre-io/src/output/schemas.rs | sed -n '371,389p' | grep -c 'Field::new'; git grep -c 'mean_rows_in_lp' 077dbe2c -- crates/cobre-python/src | wc -l; git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '385p'",
        "output": "14\n15\n0\n    let schema = builder.schema().clone();",
        "reading": "convergence_schema() declares fifteen fields (cobre-io schemas.rs:371-389), the last being mean_rows_in_lp. load_convergence reaches exactly fourteen of them by name through get_column_by_name, and mean_rows_in_lp appears zero times anywhere in cobre-python/src, so the dict-returning reader drops a written column with no error. The arrow sibling does not share the defect but misdescribes itself: load_convergence_arrow takes the file's own schema (results.rs:385), so all fifteen columns reach the caller, while its doc table (:337-352) lists fourteen rows under the claim that it 'matches the convergence Parquet schema written by the solver'. So the same file contains one reader that silently drops a column and one whose documentation of the same contract is wrong in the other direction. Both are hand-maintained copies of a schema cobre-io owns in one place."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-architecture-04",
      "id": "CD-097",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "eight message prefixes without a named constant at their mint sites and four branches pinned by no test — string coupling inside one L4 crate: Sev C",
      "category": "coupling",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 error mapping)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "The eight prefixes in message_prefix_to_pyerr share no named constant with any of their mint sites in run.rs, and exactly four of the eight branches are pinned by no test at either end: policy checkpoint error, config parse error, config read error, and the simulation error Message branch. The title's count of five does not survive, because output write error, policy validation error and config override error are each pinned end-to-end by a pytest assertion that a rename would break; nor does its claim that errors.rs pins simulation error and the training fallthrough as prefixes, since those two tests drive the typed ErrorSource::Sddp arms rather than the string classifier.",
      "title": "The Python exception class of a failed run is recovered by string-prefix matching on an error message that twenty-five hand-written literals in the run path mint, and five of the eight recognized prefixes are pinned by no test",
      "anchors": [
        {
          "path": "crates/cobre-python/src/errors.rs",
          "symbol": "message_prefix_to_pyerr"
        },
        {
          "path": "crates/cobre-python/src/errors.rs",
          "symbol": "ErrorSource"
        }
      ],
      "fixShape": "The classifier and the mint sites should agree by construction rather than by convention: one set of named constants for the prefixes, referenced at both ends, so a renamed prefix is a compile error instead of a silent downgrade to SolverError. That is a local change with no boundary impact and it does not require finishing the typed PhaseError migration, which is the larger and separate piece of work. Byte-neutrality bar: no output file is touched, so the CLI-vs-Python golden, the file-set parity test and the 18-name parity floor are unaffected; the only observable surface is the exception type, and the existing errors.rs tests pin three of the eight prefixes, so five new assertions complete the mapping before anything is renamed.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/errors.rs | sed -n '256,275p'; git show 077dbe2c:crates/cobre-python/src/run.rs | grep -cE 'format!\\(\"(output write error|policy checkpoint error|config override error|config parse error|config read error|policy validation error|simulation error|internal error)'; git show 077dbe2c:crates/cobre-python/src/errors.rs | grep -nE '\"(simulation|internal|training) error'",
        "output": "fn message_prefix_to_pyerr(py: Python<'_>, msg: &str) -> PyErr {\n    if msg.starts_with(\"output write error\") || msg.starts_with(\"policy checkpoint error\") {\n        case_io_error(py, msg)\n    } else if msg.starts_with(\"config override error\") || ... \"config parse error\" ... \"config read error\" {\n        validation_error(py, msg)\n    } else if msg.starts_with(\"policy validation error\") { ... POLICY_INCOMPATIBLE_ERROR\n    } else if msg.starts_with(\"simulation error\") { ... SIMULATION_ERROR\n    } else if msg.starts_with(\"internal error\") { ... INTERNAL_ERROR\n    } else {\n        // Unrecognized prefix (e.g. \"training error\" / \"training failed after\")\n        // falls through to SolverError.\n25\n430:            let sim_msg = \"simulation error: x\".to_string();\n442:            let solver_msg = \"training error: solver error: numerical failure\".to_string();\n465:            let msg = \"internal error: drain thread panicked\".to_string();",
        "reading": "Eight prefixes select five exception classes, and the else arm makes any unrecognized message a SolverError by design. The strings those branches test for are minted by twenty-five format! literals in run.rs, none of which shares a constant with the classifier. Three prefixes are pinned by a test in errors.rs (simulation error at :430, the training-error fallthrough at :442, internal error at :465); the other five — output write error, policy checkpoint error, config override error, config parse error, policy validation error — are pinned by nothing, so editing one literal at one mint site silently reclassifies that failure as SolverError, and the type a Python caller catches changes with no test failing. What holds the severity at C is that the typed path already exists next to it: PhaseError (run.rs:166-179) carries typed Load(LoadError) and Sddp arms, and its doc states the From<String> impl 'keeps every existing ? site unchanged' — so this is a deliberate, documented split rather than an oversight, and the classes that matter most already travel typed."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-architecture-05",
      "id": "CD-098",
      "class": "CD",
      "lens": "architecture",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two identical format strings across the two schema-export loops and no gate over the bindings' written filenames or bytes, while schema content cannot diverge because both consume the shared generator — a minor mirror with a small unguarded surface: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 front-end plumbing)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": "CLAUDE.md § Hard Rules — Python parity",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrower than the title on both halves: exactly two format strings are byte-identical and the output-directory message shares only its prefix (the four error constructions and the CLI terminal stderr line legitimately differ, so the copies are not verbatim), and the bindings side is covered for count, valid JSON and directory creation. The surviving defect is that no gate compares the bindings' written filenames or bytes against committed schemas/ or against the CLI export, so a change to the bindings' write mechanics alone (path join, pretty-print form, trailing bytes) is unguarded, while schema content cannot diverge because both front ends consume the shared generator.",
      "title": "The schema-export write loop is duplicated verbatim between the bindings and the CLI, down to three identical message strings, and only the CLI copy is covered by the schema drift job",
      "anchors": [
        {
          "path": "crates/cobre-python/src/schema.rs",
          "symbol": "export"
        }
      ],
      "fixShape": "The loop that turns the generated map into files belongs beside generate_schemas in cobre-io, returning the count and a typed error each front end maps to its own type; the CLI keeps its stderr line and the bindings keep returning the count, because that is the only part that legitimately differs. Byte-neutrality bar: the written bytes are serde_json::to_string_pretty over the same generated map either way, so schemas/ is unchanged and the existing CI schema drift job stays green as the neutrality check; the run output tree is not involved, so the CLI-vs-Python golden, the file-set parity test and the 18-name parity floor are untouched. The bindings side is pinned by test_export_writes_all_schemas_as_valid_json and test_export_creates_missing_directory in schema.rs, which must keep passing against the shared implementation.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/schema.rs | sed -n '35,58p'; git show 077dbe2c:crates/cobre-cli/src/commands/schema.rs | sed -n '56,88p'",
        "output": "bindings export():  create_dir_all -> \"creating output directory '{}'\"  |  generate_schemas() -> \"schema generation failed: {e}\"  |  let count = schemas.len();  |  for (filename, value) in schemas { output_dir.join(&filename); to_string_pretty -> \"failed to serialize schema '{filename}': {e}\"; fs::write }  |  Ok(count)\nCLI execute_export():  create_dir_all -> \"creating output directory '{}'\"  |  generate_schemas() -> \"schema generation failed: {e}\"  |  let count = schemas.len();  |  for (filename, value) in schemas { output_dir.join(&filename); to_string_pretty -> \"failed to serialize schema '{filename}': {e}\"; fs::write }  |  Term::stderr() \"Exported {count} schema files to {}\"",
        "reading": "Five steps in the same order with the same three message strings; the two copies differ only in the error type they build (PyOSError/PyValueError against CliError::Io/CliError::Internal) and in the CLI's trailing stderr line. This is a distinct duplicate from CD-025, whose anchors are the run-output orchestrators write_training_outputs and write_simulation_outputs and the five *_if_any helpers — none of which appears here, and the tree written is schemas/, not the run output tree — which is why reRaiseOf and waveRef are null rather than CD-025. The asymmetry that makes it matter: CI's schema drift job diffs schemas/ against the CLI export only, so the bindings copy could diverge in what it writes and nothing would fail. The schema CONTENT is not duplicated — both sides call the shared cobre_io::schema::generate_schemas, recorded as a positive."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-over-engineering-00",
      "id": "OD-047",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "three of seven model twins surface identity only while the other four surface their full parameter set, with nothing pinning the gap — an unpinned depth asymmetry, not speculative scaffolding: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings surface)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "Direction for the three cobre.model twins (EnergyContract, PumpingStation, NonControllableSource): fill the getters to the house pattern the other four twins follow, or withdraw the three classes and have the PySystem getters return untyped mappings until a caller needs typed objects. The station confirms the asymmetry and the unpinned status; only the owner can pick which way it resolves, and the mirror needs the resulting row either way."
      ],
      "survivingClaim": "Only the field-depth asymmetry and its unpinned status survive: three of the seven cobre.model entity lists surface identity alone where the four earlier twins surface the full physical and economic parameter set minus lifecycle fields, and nothing pins that gap (a reason-free minimal-stub section label at model.rs:288/323/358, no mirror row, and a presence-only hasattr assertion at test_model.py:93-107). The per-class scaffolding is conceded as the PyO3 minimum for a typed export, and the classes' existence is conceded as justified by PySystem mirroring all seven cobre-core System entity collections, so neither the scaffold nor the absence of an in-repo reader is a defect.",
      "title": "Three cobre.model entity twins are self-labelled minimal stubs: full pyclass scaffolding per class surfaces only id and name, and nothing outside a hasattr presence check ever reads them",
      "anchors": [
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "PyEnergyContract"
        },
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "PyPumpingStation"
        },
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "PyNonControllableSource"
        },
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "PySystem"
        }
      ],
      "fixShape": "Either complete the three twins so each exposes the field set its core entity actually carries, matching the shape PyBus, PyLine, PyThermal, PyHydro and PySystem already use, or drop the three classes and have the corresponding PySystem getters return plain dicts until a caller needs typed objects. Whichever direction the owner picks, record it as a reserved seam in docs/design/reserved-seams-and-deferred-debt.md so the next sweep does not re-derive it, and replace the hasattr presence assertion with a test that reads at least one field per class so the surface cannot silently stay empty. The byte-neutrality bar for either direction: every Python-visible name and value that exists today keeps its current value, the cobre.model export list is unchanged so test_model_classes_importable still passes, and neither the CLI-versus-Python determinism golden nor the file-set parity test is touched, because no output file and no writer is involved in the model read surface.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/model.rs | sed -n '288,398p'   AND   git grep -n -E '\\.(buses|lines|thermals|hydros|contracts|pumping_stations|non_controllable_sources)\\b' 077dbe2c -- crates/cobre-python/tests   AND   git grep -n -E 'EnergyContract|PumpingStation|NonControllableSource' 077dbe2c -- crates/cobre-python/tests/test_model.py   AND   git grep -n -E 'contracts|pumping_stations|non_controllable_sources|EnergyContract|PumpingStation|NonControllableSource' 077dbe2c -- examples/notebooks/quickstart.ipynb crates/cobre-python/tests/test_types.py README.md",
        "output": "model.rs 288-398 (trimmed): three sections each headed '// --- EnergyContract (minimal stub) ---', '// --- PumpingStation (minimal stub) ---', '// --- NonControllableSource (minimal stub) ---'. Each is #[pyclass(name = \"...\", frozen, skip_from_py_object)] + #[derive(Clone)] + a one-field struct { inner: <CoreEntity> } + a #[pymethods] block of exactly three items (#[getter] fn id -> i32, #[getter] fn name -> &str, fn __repr__ -> String) + a separate impl block with pub(crate) fn from_rust. -- getter-access grep over the whole pytest tree returns 5 hits, all in test_model.py: 31 'buses = system.buses', 44 'hydros = system.hydros', 56 'thermals = system.thermals', 69 'lines = system.lines', 79 'bus = system.buses[0]'. No hit for .contracts, .pumping_stations or .non_controllable_sources anywhere in crates/cobre-python/tests. -- the only pytest reference to the three class names is test_model.py:103-105, inside test_model_classes_importable, which asserts hasattr(cobre.model, name) and reads no field. -- the notebook, test_types.py and README greps return nothing at all.",
        "reading": "The three classes carry the full PyO3 twin scaffold that the five complete twins carry (frozen pyclass, Clone, skip_from_py_object, a pymethods block, a from_rust constructor, and a PySystem getter at model.rs:448-473 that clones the entire core entity vector to build them) while surfacing 2 of each core entity's fields. By contrast PyHydro exposes 10 getters and PyLine 8. Roughly 100 of model.rs's 526 lines are these three scaffolds plus their three PySystem getters. All three are declared public API in crates/cobre-python/python/cobre/model.pyi at lines 37, 41 and 45. Every counter-signal for a thin abstraction was checked and is absent: no second implementation is stated anywhere, no reserved-seam row exists for them, the notebook that CI executes with nbmake does not touch them, and the code's own section comments call them stubs rather than citing a requirement. The only thing holding them in place is the export-presence assertion in test_model.py, which is satisfied by the class existing and says nothing about its content."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-over-engineering-01",
      "id": "OD-048",
      "class": "OD",
      "lens": "over-engineering",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one test-module allow that repeats the crate-level cfg(test) allow verbatim — a dead attribute: Sev C",
      "category": "redundant-wrapper",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4-internal)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": "`.claude/rules/comments.md` D4 — Rationale above suppression. Every `#[allow(...)]` for a refactor-decision lint (`clippy::too_many_arguments`, `too_many_lines`, `type_complexity`, `dead_code`, `unused_*`) and every borrow-checker workaround carries a rationale: why the refactor that removes the lint is inappropriate.",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Exactly one site survives, and only as a dead-attribute deletion: errors.rs:359's '#[allow(clippy::unwrap_used, clippy::expect_used, clippy::panic)]' on the errors test module suppresses precisely the three lints lib.rs:17 already allows crate-wide under the same cfg(test) gate, adds no fourth entry the way run.rs:1759's float_cmp does, and is contradicted rather than supported by the policy.rs:558 and schema.rs:61 test modules that carry none. The needless_pass_by_value family is struck from the claim entirely: the counts are wrong (16 sites in-surface, 13 bare), the three study.rs rationales state three different facts rather than one reason copied, and the lint is outside D4's mandatory-rationale list so bare sites are not a defect.",
      "title": "Allow-attribute proliferation in the bindings crate: the errors test module re-declares the exact crate-level test allow lib.rs already sets, and 14 of the 17 needless_pass_by_value allows are bare while the single shared PyO3 reason is written out three times",
      "anchors": [
        {
          "path": "crates/cobre-python/src/errors.rs",
          "symbol": "tests"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_results"
        },
        {
          "path": "crates/cobre-python/src/schema.rs",
          "symbol": "export"
        }
      ],
      "fixShape": "Delete the allow attribute on the errors test module outright; the crate-level cfg_attr in lib.rs already covers all three lints for every test module in the crate, as the policy and schema test modules demonstrate by carrying none. For the needless_pass_by_value family, state the shared PyO3 extraction reason once in the crate-level module documentation in lib.rs and leave the per-site attributes bare, rather than repeating the sentence at three of seventeen sites. Do not hoist needless_pass_by_value itself to the crate root: a crate-wide allow would also silence a genuine by-value parameter in a future non-PyO3 helper, which is a real cost for a cosmetic gain, so the per-site attribute stays and only its rationale is centralised. The byte-neutrality bar: this is attribute-only and touches no signature, so the Python-visible API and every value it returns are unchanged, the CLI-versus-Python golden is unchanged, and the file-set parity test is unchanged; the only observable difference is the number of allow attributes and the clippy warning count, which must remain zero.",
      "evidence": {
        "command": "for f in convert errors io lib model policy results schema study version; do git show 077dbe2c:crates/cobre-python/src/$f.rs | grep -n '#\\[allow(\\|#!\\[cfg_attr(test' | sed \"s#^#$f.rs:#\"; done   AND   for f in policy schema errors run; do git show 077dbe2c:crates/cobre-python/src/$f.rs | grep -n -B2 '^mod tests'; done",
        "output": "17 allow sites across the 10 bindings files, plus one crate-level attribute: lib.rs:17 '#![cfg_attr(test, allow(clippy::unwrap_used, clippy::expect_used, clippy::panic))]'; errors.rs:359 '#[allow(clippy::unwrap_used, clippy::expect_used, clippy::panic)]'; io.rs:136 and io.rs:197, policy.rs:447, results.rs:146, results.rs:242 (with clippy::too_many_lines), results.rs:372, results.rs:740, results.rs:1125, results.rs:1362, results.rs:1502, schema.rs:32, study.rs:165 (with clippy::similar_names), study.rs:277, study.rs:482, study.rs:661, study.rs:715 -- all 16 of these carry clippy::needless_pass_by_value. The test-module comparison shows policy.rs:557 and schema.rs:60 declaring '#[cfg(test)]' immediately followed by 'mod tests {' with no allow attribute at all, while errors.rs:358-360 declares '#[cfg(test)]' then the three-lint allow then 'mod tests {'.",
        "reading": "errors.rs:359 is provably dead: it allows exactly the three lints (unwrap_used, expect_used, panic) that lib.rs:17 already allows crate-wide under cfg(test), and a #[cfg(test)] module only compiles in the configuration where that cfg_attr is active. The two sibling test modules in the same crate, policy.rs:557 and schema.rs:60, prove the crate-level attribute is sufficient by relying on it and carrying no allow of their own. The contrast case is crates/cobre-python/src/run.rs:1758-1765 (cited, not anchored, per rule 14), whose test-module allow adds clippy::float_cmp -- a lint lib.rs:17 does not cover -- so three of its four entries are redundant but one is load-bearing; errors.rs:359 has no such fourth entry. Separately, 14 of the 16 needless_pass_by_value allows are bare, and the single cause behind every one of them is written out three times in study.rs, at :163-164 ('state: Vec<f64> is required so PyO3 can extract from an arbitrary Python sequence'), :274-276 and :480-481 ('PyO3's from-Python extraction hands over owned values, so the PathBuf/Bound arguments cannot be borrowed here'). One reason, three copies, fourteen silent sites."
      },
      "yardstickSection": null,
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-00",
      "id": "PD-049",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one reader holds the GIL across a checkpoint decode that returns owned Rust data and that its sibling detaches for the same file — a departure from the crate's stated posture confined to one call: Sev B (real, bounded), UNMEASURED",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings; the GIL posture is the crate's own contract at lib.rs:11-13)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "Only load_policy survives: results.rs:1519 holds the GIL across cobre_io::read_policy_checkpoint, a decode phase that already returns fully owned Rust data (no lifetime, no Py type) and that the sibling Study::load_policy at study.rs:663 detaches for the same on-disk checkpoint, so the residue is that single call against the crate invariant at lib.rs:11-13. load_convergence is dropped: its decode is interleaved with the per-row PyDict projection that must hold the GIL, and load_convergence_arrow already provides a detached reader for the identical file.",
      "title": "load_convergence and load_policy hold the GIL across their entire Rust-side decode, while three sibling readers in the same file release it with py.detach",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_convergence"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "load_policy"
        }
      ],
      "fixShape": "No crate move: the fix stays in cobre-python (L4). Split each of the two readers into the two halves load_stochastic already separates, a detached decode that returns owned Rust rows and a GIL-held projection that builds the Python objects from them. For load_policy, read and decode the FlatBuffers checkpoint inside py.detach and construct the cut, basis and metadata dicts after reacquiring. Byte-neutrality bar: both symbols are read-only entry points that write no file, so the CLI-vs-Python golden crates/cobre-python/tests/test_cli_python_determinism_parity.py, the file-set parity test tests/test_cli_python_file_set_parity.py, the import-resolving gate scripts/ci/check_python_parity.py and the cobre validate --json single-object stdout contract are untouched by construction. The bar the change must meet instead is that the returned dicts keep identical keys, identical list order and bit-identical float values, which the existing results-reader pytest suite asserts.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | grep -n 'pub fn load_\\|py.detach\\|read_policy_checkpoint(\\|ParquetRecordBatchReaderBuilder::try_new'",
        "output": "243:pub fn load_convergence(py: Python<'_>, output_dir: PathBuf) -> PyResult<Py<PyAny>> {\n250:    let builder = ParquetRecordBatchReaderBuilder::try_new(file)\n373:pub fn load_convergence_arrow(py: Python<'_>, output_dir: PathBuf) -> PyResult<Py<PyAny>> {\n378:    let ipc_bytes = py.detach(|| -> PyResult<Vec<u8>> {\n741:pub fn load_stochastic(py: Python<'_>, output_dir: PathBuf) -> PyResult<Stochastic> {\n747:    let (par_rows, opening_rows) = py.detach(|| -> PyResult<(ParRows, OpeningRows)> {\n1363:pub fn load_simulation_arrow(\n1382:        let ipc_bytes = py.detach(|| entity_type_ipc_bytes(&entity_dir))?;\n1395:            let ipc_bytes = py.detach(|| entity_type_ipc_bytes(&entity_dir))?;\n1503:pub fn load_policy(\n1519:    let checkpoint = cobre_io::read_policy_checkpoint(&policy_dir)",
        "reading": "Five public readers open a bulk source. Three of them release the interpreter for the Rust-side work: load_convergence_arrow at :378, load_stochastic at :747 and load_simulation_arrow at :1382 and :1395. load_convergence builds its ParquetRecordBatchReaderBuilder at :250 and load_policy calls cobre_io::read_policy_checkpoint at :1519 with the GIL still held, and neither decode needs a Python object. load_stochastic is the in-file proof that the split is available: it detaches to decode into owned Rust rows (ParRows, OpeningRows) and only then builds Python values."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/results.rs::load_policy",
      "exercisingCallSites": [
        "cobre-python results.rs · load_policy → cobre_io::read_policy_checkpoint (GIL held across the decode)",
        "cobre-python study.rs · Study::load_policy (the detached sibling over the same checkpoint)"
      ],
      "mechanism": "GIL held across a bulk parquet decode and across a FlatBuffers checkpoint read, so every other Python thread is blocked for the whole duration of work that touches no Python object",
      "measurabilityNote": "exercised by a Python-side harness over the CAL run's policy checkpoint (a second Python thread progressing during load_policy), not by the cobre run deck itself; the perf epic owns the protocol",
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-01",
      "id": "PD-050",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "the Arrow data-type match and array downcast re-entered per cell in the bulk simulation reader although both are per-column invariants — per-element work in a bulk conversion loop: Sev B, UNMEASURED; the per-cell Python object and dict are the published list[dict] contract and are excluded",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings reader)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [
        "API posture the station cannot settle: is the dict-shaped cobre.results.load_simulation a supported bulk reader, or is load_simulation_arrow the intended path for large result trees? load_simulation's rustdoc at results.rs:1081-:1126 never points at the Arrow reader, so the owner decides whether the per-column hoist is worth doing or whether the dict reader is documented as a small-result convenience instead."
      ],
      "survivingClaim": "Narrowed to the type-resolution step alone: the match on col.data_type() at results.rs:781 and the as_any().downcast_ref in its arms are invariant per column yet re-entered per cell from the single call site at :993, and can be resolved once per column per batch inside read_parquet_partition_into without touching any other caller. The title's sibling-symmetry half does not survive, because get_column_by_name at :759 is a compile-time hoist over the statically known convergence schema and is unusable for a dynamically discovered entity schema, which needs a per-column resolved enum instead. Nor does the claim reach the rest of the per-cell cost: the is_null test, the Python object per cell, the dict key from field.name() and the PyDict per row are required by the published list[dict] contract and are unchanged by the hoist.",
      "title": "read_parquet_partition_into re-runs the Arrow data-type match and the array downcast once per cell, while the sibling load_convergence hoists the identical downcast to once per column per batch",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "read_parquet_partition_into"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "arrow_value_to_py"
        }
      ],
      "fixShape": "Stays in cobre-python (L4). Resolve the typed array view once per column per batch before the row loop, the shape get_column_by_name already provides for the convergence reader, and let the row loop index the resolved views; the schema-generic case needs a per-column resolved enum rather than a per-cell match. Counter-signal recorded: load_simulation_arrow is the zero-copy bulk alternative for large results, so this candidate is about the cost of the dict reader that remains the documented default of load_simulation, not about deleting it. Byte-neutrality bar: this is a read path that writes no file, so the CLI-vs-Python golden test_cli_python_determinism_parity.py, tests/test_cli_python_file_set_parity.py, scripts/ci/check_python_parity.py and the cobre validate --json contract are untouched. The bar is that the per-row dicts keep the same keys in the same insertion order, the same None for nulls and bit-identical numeric values.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '265,268p;781,786p;988,996p'",
        "output": "        let col_iteration = get_column_by_name::<Int32Array>(&batch, \"iteration\")?;\n        let col_lower_bound = get_column_by_name::<Float64Array>(&batch, \"lower_bound\")?;\n        let col_upper_bound = get_column_by_name::<Float64Array>(&batch, \"upper_bound\")?;\n        let col_upper_bound_std = get_column_by_name::<Float64Array>(&batch, \"upper_bound_std\")?;\n    match col.data_type() {\n        DataType::Float64 => {\n            let arr = col\n                .as_any()\n                .downcast_ref::<Float64Array>()\n                .ok_or_else(|| PyOSError::new_err(\"Float64 column downcast failed\"))?;\n        for i in 0..n_rows {\n            let row = PyDict::new(py);\n            row.set_item(\"scenario_id\", scenario_id_val)?;\n            for (col_idx, field) in schema.fields().iter().enumerate() {\n                let col = batch.column(col_idx);\n                let val = arrow_value_to_py(py, col.as_ref(), i)?;\n                row.set_item(field.name(), val)?;\n            }\n            result_list.append(row)?;",
        "reading": "load_convergence resolves its 14 typed columns once per batch at :265-:278 through get_column_by_name, then indexes them per row. read_parquet_partition_into does the opposite at :988-:996: for every row it loops the schema fields and calls arrow_value_to_py per cell, and arrow_value_to_py re-enters the col.data_type() match at :781 and re-runs as_any().downcast_ref::<T>() at :785 for that single value. The dispatch result is identical for every row of a column, so the work is per cell where the sibling reader already proves it can be per column."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/results.rs::read_parquet_partition_into",
      "exercisingCallSites": [
        "cobre-python results.rs · load_simulation → read_parquet_partition_into (per-cell type match + downcast from the single call site at :993)"
      ],
      "mechanism": "per-element conversion: an Arrow data-type match plus a dynamic downcast repeated for every cell of every row instead of once per column per batch",
      "measurabilityNote": "exercised by cobre.results.load_simulation over the CAL run's simulation tree (Python-side harness)",
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-02",
      "id": "PD-051",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "B",
      "reviewerRating": "B",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "per-element Python float materialisation on the one array whose size is not deck-bounded (active cuts × state dimension, per stage), with a buffer-based alternative available — Sev B, UNMEASURED; the deck-bounded reshape_f64 callers are excluded",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings reader)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": "CLAUDE.md § Hard Rules — Never allocate on hot paths — pre-allocate workspaces, reuse buffers",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "performance-sweep"
      ],
      "needsHuman": [],
      "survivingClaim": "Only Policy::cut_matrix (crates/cobre-python/src/study.rs:226-231) carries the per-element PyFloat materialisation on an array whose element count is not deck-bounded — active cuts times state dimension, paid per stage, with no buffer-based alternative — while the two reshape_f64 callers read deck-bounded parquet blocks and the extra opening_tree Vec copy is conceded as immaterial.",
      "title": "Bulk f64 arrays reach NumPy through a per-element Python list: reshape_f64 and Policy::cut_matrix materialise every coefficient as a Python float that numpy then re-parses",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "reshape_f64"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "opening_tree"
        },
        {
          "path": "crates/cobre-python/src/study.rs",
          "symbol": "cut_matrix"
        }
      ],
      "fixShape": "Stays in cobre-python (L4) and keeps numpy a soft, lazily imported dependency, which is the documented contract of these accessors. Hand numpy the bytes instead of a list: expose the flat f64 buffer as a Python bytes or memoryview object and build the array with numpy.frombuffer followed by reshape, so the transfer is one buffer copy rather than one Python object per element; the lazy import and its verbatim ImportError propagation stay as they are. Byte-neutrality bar: all three are read accessors that write no file, so the CLI-vs-Python golden test_cli_python_determinism_parity.py, tests/test_cli_python_file_set_parity.py, scripts/ci/check_python_parity.py and the cobre validate --json contract are untouched. The bar is bit-identical float64 values, the same dtype, the same C-contiguous row-major shape including the (0, dim) empty case cut_matrix comments on, and the same ImportError behaviour when numpy is absent.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '699,704p'; git show 077dbe2c:crates/cobre-python/src/study.rs | sed -n '226,231p'",
        "output": "fn reshape_f64(py: Python<'_>, flat: Vec<f64>, shape: (usize, usize)) -> PyResult<Py<PyAny>> {\n    let numpy = py.import(\"numpy\")?;\n    let array = numpy.call_method1(\"asarray\", (flat,))?;\n    let reshaped = array.call_method1(\"reshape\", (shape,))?;\n    Ok(reshaped.unbind())\n}\n        let np = py.import(\"numpy\")?;\n        let intercepts_arr = np.call_method1(\"asarray\", (intercepts,))?;\n        // `dim` fixes the column count even when `n_cuts == 0`, giving shape `(0, dim)`.\n        let coeffs_1d = np.call_method1(\"asarray\", (coeffs_flat,))?;\n        let coeffs_arr = coeffs_1d.call_method1(\"reshape\", ((n_cuts, dim),))?;",
        "reading": "Passing a Vec<f64> as a PyO3 call argument converts it to a Python list of PyFloat objects, one allocation per element, and numpy.asarray then walks that list and unboxes every element back into a float64 buffer. Three call sites feed whole arrays through this path: par_coefficients at :623 (n_rows times PAR_COEFFICIENT_COLUMNS values), opening_tree at :691 (n_openings times dim values, preceded by value_slice.to_vec(), so the data is copied twice before numpy sees it) and Policy::cut_matrix in study.rs, which flattens every active cut of a stage into coeffs_flat and hands both that and intercepts over the same list boundary."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/study.rs::cut_matrix",
      "exercisingCallSites": [
        "cobre-python study.rs · Policy::cut_matrix (per-element PyFloat over active cuts × state dimension, per stage)"
      ],
      "mechanism": "per-element Python object conversion for a numeric array: one PyFloat per f64 on the way in and one unbox per element inside numpy.asarray, plus an extra Vec copy at the opening_tree call site",
      "measurabilityNote": "exercised by Policy.cut_matrix over the CAL run's policy (Python-side harness)",
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-04",
      "id": "PD-052",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "seven collection getters rebuild N wrapper objects per property read with no memo; the sharpest instance copies Hydro's full payload to publish nine scalars — a per-read clone in a reader with no hot-path role: Sev C, not queued",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings surface)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [
        "A per-collection wrapper memo makes element object identity stable across reads (today system.buses[0] is system.buses[0] is False, since every read builds new objects). No test or stub pins that, so the owner must say whether per-read element identity is part of the published cobre.model contract before the memo shape is accepted."
      ],
      "survivingClaim": "The seven PySystem collection getters (model.rs:406-474) carry no memo, so the cost is per read of the COLLECTION PROPERTY (not per wrapper attribute access, which borrows at model.rs:42), and the sharpest instance is the hydros getter, where the clone copies Hydro's unit_groups vector and its tailrace, hydraulic_losses, efficiency, diversion, filling and penalties payload (entities/hydro.rs:217-291) to publish nine scalars, one of them always None. The from_arc share-by-refcount comparison and the deficit_segments extension do not survive: from_arc wraps one shared System while a getter must build N Python objects for a list-typed property (model.pyi:55-61, test_model.py:70), and deficit_segments must rebuild caller-mutable dicts per read because PyBus is frozen (model.rs:28, 51-65).",
      "title": "The seven PySystem entity-list getters deep-clone the whole entity vector on every attribute access, in a file whose from_arc constructor already establishes the share-by-refcount idiom",
      "anchors": [
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "buses"
        },
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "from_arc"
        },
        {
          "path": "crates/cobre-python/src/model.rs",
          "symbol": "deficit_segments"
        }
      ],
      "fixShape": "Stays in cobre-python (L4). Two shapes, both compatible with the module doc: hold the shared Arc<System> plus an index inside each entity wrapper so a getter becomes a refcount bump and a length, the pattern from_arc already establishes for the System wrapper; or keep the owned wrappers and build the list once per PySystem, handing out the cached sequence on later accesses. The second is the smaller change and keeps the documented lifetime simplicity. Byte-neutrality bar: model.rs is a read-only introspection surface that writes no file, so the CLI-vs-Python golden test_cli_python_determinism_parity.py, tests/test_cli_python_file_set_parity.py, scripts/ci/check_python_parity.py and the cobre validate --json contract are untouched. The bar is that the getters keep returning a Python sequence in declaration order with identical per-entity attribute values, and that the Python-visible mutability of the returned list does not change.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/model.rs | grep -n 'fn buses\\|fn lines\\|fn thermals\\|fn hydros\\|fn contracts\\|fn pumping_stations\\|fn non_controllable_sources\\|fn from_arc'; git show 077dbe2c:crates/cobre-python/src/model.rs | sed -n '406,413p'",
        "output": "407:    fn buses(&self) -> Vec<PyBus> {\n417:    fn lines(&self) -> Vec<PyLine> {\n427:    fn thermals(&self) -> Vec<PyThermal> {\n437:    fn hydros(&self) -> Vec<PyHydro> {\n447:    fn contracts(&self) -> Vec<PyEnergyContract> {\n457:    fn pumping_stations(&self) -> Vec<PyPumpingStation> {\n467:    fn non_controllable_sources(&self) -> Vec<PyNonControllableSource> {\n523:    pub(crate) fn from_arc(inner: Arc<System>) -> Self {\n    #[getter]\n    fn buses(&self) -> Vec<PyBus> {\n        self.inner\n            .buses()\n            .iter()\n            .cloned()\n            .map(PyBus::from_rust)\n            .collect()",
        "reading": "All seven entity collections are exposed as #[getter] properties with the identical body: iter().cloned() over the whole cobre-core entity vector, then a fresh wrapper per entity, then collect. Because they are properties, an idiomatic Python loop that reads system.buses inside the loop re-clones the entire vector on each iteration; nothing caches the wrapper list. PyBus::deficit_segments at :51 has the same per-access shape, rebuilding a Vec<Bound<PyDict>> each time it is read. The same file at :523 documents the alternative it already uses for the System itself, from_arc wrapping via a refcount bump rather than a full clone. The module doc sanctions cloning entity data into the wrappers because it is small and avoids lifetime complexity, which covers the cost of building a wrapper, not the cost of rebuilding every wrapper on every access."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/model.rs::buses",
      "exercisingCallSites": [
        "cobre-python model.rs · PySystem collection getters :406-474 (per read of a collection property)"
      ],
      "mechanism": "clone-per-access: a full deep clone of a cobre-core entity vector plus one wrapper allocation per entity on every property read, with no cached view",
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-05",
      "id": "PD-053",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "one avoidable whole-pool copy on the no-reservation arm of a once-per-export builder — Sev C, not queued",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrowed to the default arm and to the copy alone: on reserve_depth == None (policy.rs:411-415) the collect at policy.rs:391 duplicates the whole cut pool into owned Vec<f64> that no consumer ever owns, because PolicyCutRecord.coefficients is a borrowed slice (cobre-io records.rs:211, whose doc at :193 states the borrow exists to avoid copying large coefficient vectors) filled from &data.coefficients at policy.rs:480. Conceded and excluded: the Some(depth) arm's collect, required by reserve_boundary_inflow_lag_slots's &[Vec<f64>] parameter (policy_export.rs:341); and the GIL-placement half of the title, since the by-value FromPyObject signature at policy.rs:448-456 already materialises the same pool element-wise from Python under the held GIL.",
      "title": "build_stage_cuts_data clones every cut coefficient vector of every stage even on the branch that needs no reservation, and does it under the GIL before the detached write",
      "anchors": [
        {
          "path": "crates/cobre-python/src/policy.rs",
          "symbol": "build_stage_cuts_data"
        },
        {
          "path": "crates/cobre-python/src/policy.rs",
          "symbol": "StageCutsData"
        }
      ],
      "fixShape": "Stays in cobre-python (L4). Make the owned coefficient buffer conditional on the reservation, for example a borrowed-or-owned representation whose borrowed arm points at the PyCutRecord storage that already outlives the call and whose owned arm holds the widened vectors the reservation produces; the borrowed views that PolicyCutRecord and StageCutsPayload build then read from whichever arm is present. Independently, move the stage_data construction inside the existing py.detach block, since nothing in it touches a Python object once the payloads have been read. Byte-neutrality bar: this is on the policy write path, so the fix must leave the FlatBuffers checkpoint bytes identical, which means the same coefficients in the same cut order and slot order with the same state_dimension per stage; it is verified by the CLI-vs-Python golden crates/cobre-python/tests/test_cli_python_determinism_parity.py over the whole examples/1dtoy tree and by tests/test_cli_python_file_set_parity.py, with scripts/ci/check_python_parity.py unaffected because no writer name changes. The reservation rejection path reject_unreserved_lag_coefficients must keep raising on exactly the same inputs.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/policy.rs | sed -n '374,381p;391,392p;411,415p;467,468p;473,473p'",
        "output": "/// vector, either as supplied or widened by an inflow-lag reservation, plus the\n/// resulting `state_dimension`. Owning these lets the borrowed\n/// [`StageCutsPayload`]/[`PolicyCutRecord`] views reference the reserved\n/// coefficients (which the writer must own — the caller's `coefficients` are\n/// storage-only when a reservation runs).\nstruct StageCutsData {\n    manifest: Vec<EntitySlot>,\n    coefficients: Vec<Vec<f64>>,\n    let coefficients: Vec<Vec<f64>> = sc.cuts.iter().map(|c| c.coefficients.clone()).collect();\n    match reserve_depth {\n        None => Ok(StageCutsData {\n            manifest,\n            coefficients,\n            state_dimension: sc.state_dimension,\n        }),\n        .map(|sc| build_stage_cuts_data(sc, reserve_depth))\n        .collect::<PyResult<_>>()?;\n    py.detach(|| {",
        "reading": "Line :391 clones every cut's coefficient vector for every stage unconditionally, before the match on reserve_depth. Only the Some(depth) arm consumes those clones, by handing them to reserve_boundary_inflow_lag_slots; the None arm at :411 passes them straight through unchanged. The struct doc at :376-:377 states the ownership requirement exactly that narrowly, the caller's coefficients are storage-only when a reservation runs, and the consumer proves it: PolicyCutRecord takes coefficients as a borrowed view zipped over &data.coefficients, so on the None path the records could borrow the PyCutRecord storage directly. The clones are also built at :467 before the py.detach at :473, so the duplication of the whole cut pool happens with the interpreter still held."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/policy.rs::build_stage_cuts_data",
      "exercisingCallSites": [
        "cobre-python policy.rs · build_stage_cuts_data (default arm, the :391 collect)"
      ],
      "mechanism": "redundant materialisation of the full cut pool: one Vec<f64> clone per cut per stage on a branch whose consumer only borrows, executed under the GIL before the detached write",
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-06",
      "id": "PD-054",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "a per-stage linear sweep where the loader already guarantees the sort order — a repeated-access residue with no in-repo caller walking stages: Sev C, not queued",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings reader)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrowed to the repeated-access residue only: opening_tree re-derives each stage's block by comparing every stage_id entry rather than a partition_point over the order read_opening_rows already enforces, so a caller that walks stages pays a whole-column comparison sweep per stage. Not confirmed for a single call, where the per-element Python conversion in reshape_f64 over the returned block is the leading mechanism and the scan only repeats a sweep the loader's own sort check already performs. No in-repo or documented caller iterates stages at the pin, so the sweep must script the stage walk to observe it; the fix stays inside cobre-python and, because Stochastic is frozen, must precompute per-stage offsets at load or use partition_point on the sorted column.",
      "title": "Stochastic::opening_tree scans every opening row of every stage to locate one stage's block, although the reader already rejects unsorted rows and the code comments that the block is contiguous",
      "anchors": [
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "opening_tree"
        },
        {
          "path": "crates/cobre-python/src/results.rs",
          "symbol": "is_opening_order_sorted"
        }
      ],
      "fixShape": "Stays in cobre-python (L4). Locate the block with the sort invariant the reader already enforces, by partition point on the first and last index of the stage rather than a full scan, or by computing the per-stage offsets once when the rows are read and storing them on the handle. Both preserve the existing IndexError for an absent stage and its stage_range_message text. Byte-neutrality bar: opening_tree is a read accessor that writes no file, so the CLI-vs-Python golden test_cli_python_determinism_parity.py, tests/test_cli_python_file_set_parity.py, scripts/ci/check_python_parity.py and the cobre validate --json contract are untouched. The bar is that the returned array keeps the same shape, the same row-major element order and bit-identical values, and that the ragged-tree and overflow error messages stay exactly as they are.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/results.rs | sed -n '569,572p;645,655p;663,664p'",
        "output": "    if !is_opening_order_sorted(&rows) {\n        return Err(PyOSError::new_err(\n            \"noise_openings.parquet rows are not sorted by ... cannot reshape the opening tree\",\n        let mut start = None;\n        let mut end = 0usize;\n        for (i, &s) in rows.stage_id.iter().enumerate() {\n            if s == stage_i32 {\n                if start.is_none() {\n                    start = Some(i);\n                }\n                end = i + 1;\n            }\n        }\n        // Rows are sorted, so the matching rows form a contiguous block; dim and\n        // n_openings derive from the maxima within it.\n",
        "reading": "read_opening_rows enforces the sort invariant at :569 through is_opening_order_sorted and fails the load when it does not hold, and the comment at :663 states the consequence the code relies on, that the matching rows form a contiguous block. The lookup at :647 nevertheless walks the entire stage_id column for every call, never breaking after the block ends, so a caller iterating stages performs a full pass per stage over rows of all stages. The invariant that would make this a bounded lookup is already asserted at read time."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/results.rs::opening_tree",
      "exercisingCallSites": [
        "cobre-python results.rs · Stochastic::opening_tree (per-stage whole-column sweep; observable only under a scripted stage walk)"
      ],
      "mechanism": "redundant full pass per call: a linear scan of all opening rows to find one stage's contiguous block, repeated for every stage, despite a sort invariant enforced at read time",
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-performance-07",
      "id": "PD-055",
      "class": "PD",
      "lens": "performance",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "io.rs is the one module with no py.detach site; the harm is Python-level concurrency only (no native parallelism is gated) — a posture departure from lib.rs:11-13 and from the detached study.rs sequence: Sev C, not queued",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 bindings)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "asserted",
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": null,
      "needsHuman": [],
      "survivingClaim": "Narrower than the title on two axes. io.rs is the only module in cobre-python with no py.detach site, and validate (io.rs 200) is the single entry point that holds the interpreter across all three phases named, the six-layer load (io.rs 236), prepare_stochastic (io.rs 281) and prepare_hydro_models_from_artifacts (io.rs 297); load_case (io.rs 138) holds it across the six-layer load only (io.rs 145) and never runs the stochastic or hydro-model phases at all. The defensible harm is confined to Python-level concurrency, other Python threads cannot progress during either call, and explicitly does NOT include throttling cobre-sddp's own worker threads, which the interpreter lock never gated; so the surviving defect is the unexplained departure from the stated crate posture at lib.rs 11-13 and from the identical detached load-and-prepare sequence at study.rs 503, not any loss of native parallelism.",
      "title": "cobre.io.validate and cobre.io.load_case hold the GIL across the whole case load, stochastic preparation and hydro-model preparation, while Study in the same crate detaches at all four of its heavy entry points",
      "anchors": [
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "validate"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "load_case"
        }
      ],
      "fixShape": "Stays in cobre-python (L4). Convert the overrides as today, then run the phase pipeline inside py.detach and return a plain Rust outcome value that carries either the first failure with its kind and message or the warning list, and build the result dict from that outcome after reacquiring; the return_error macro becomes an early return of the failure variant instead of a direct dict write. Phase 9 stays where it is: running prepare_stochastic during validation is the deliberate parity contract with cobre validate and is recorded as a positive, not something this fix touches. Byte-neutrality bar: validate writes no file, and the CLI side is not changed, so the cobre validate --json single-object stdout contract is untouched, as are the CLI-vs-Python golden test_cli_python_determinism_parity.py, tests/test_cli_python_file_set_parity.py and scripts/ci/check_python_parity.py. The bar is that the returned dict keeps the same keys, the same valid flag, the same short-circuit semantics so that the first failing phase is still the one reported and later phases still do not run, and the same kind and message strings including the prep_phase_metadata file labels.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/src/io.rs | grep -n 'pub fn validate\\|pub fn load_case\\|validate_case_with_artifacts(\\|prepare_stochastic(\\|prepare_hydro_models_from_artifacts(\\|detach'; git show 077dbe2c:crates/cobre-python/src/study.rs | grep -n 'detach'",
        "output": "138:pub fn load_case(path: PathBuf) -> PyResult<PySystem> {\n200:pub fn validate(\n236:    let (loaded, report) = match validate_case_with_artifacts(&path) {\n281:    let prepared = match prepare_stochastic(\n297:        match prepare_hydro_models_from_artifacts(&prepared.system, &artifacts, false, None) {\n503:        py.detach(|| Self::new_native(&case_dir, output_dir, threads, overrides))\n626:        match py.detach(|| self.train_native(on_iteration)) {\n663:        py.detach(|| self.load_policy_native(output_dir))\n724:            .detach(|| self.simulate_native(policy, output_dir))",
        "reading": "The grep over io.rs returns no detach match at all, so the full eleven-phase pipeline runs with the interpreter held: the six-layer load at :236, the stochastic preparation at :281 and the hydro-model preparation at :297. study.rs shows the same crate doing the opposite for the same class of work at four sites, :503, :626, :663 and :724, and its construction path detaches around exactly the load-and-prepare sequence validate repeats. The dict-building is the only part of validate that needs the interpreter, and the override map is already converted to a serde map before the pipeline starts, so nothing between the two ends touches a Python object. This is not the validate phase-mirror duplication, which is the S6b and CD-029 question; it is a Python-only mechanism with no CLI counterpart."
      },
      "yardstickSection": null,
      "claimType": "single-process",
      "layout": "4t",
      "requires": "none",
      "profiledSymbol": "crates/cobre-python/src/io.rs::validate",
      "exercisingCallSites": [
        "cobre-python io.rs · validate (:200) holding the GIL across the six-layer load, prepare_stochastic and prepare_hydro_models_from_artifacts",
        "cobre-python io.rs · load_case (:138) across the load only"
      ],
      "mechanism": "GIL held across a bulk read and a compute pipeline: the six-layer case load, PAR estimation and hydro-model preparation all run with the interpreter held, blocking every other Python thread for work that touches no Python object",
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-test-bloat-00",
      "id": "TD-071",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "one missing unit oracle plus a rationale correction (effort S); the seven pytest tests are load-bearing and stay, so the routing claim does not survive",
      "upgradeNote": null,
      "calibrationBasis": "the converter pair has no direct oracle (its seven positive claims are inferred through validate) and its placement rationale is scoped to one build configuration — an oracle gap and a wrong rationale in one module pair: Sev C",
      "category": "asymmetry",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Carried from the attacker: correcting the two rationales (convert.rs:104-106, test_convert.py:5-9) and adding a Unit-tier oracle are separable. The owner decides whether this lands as a rationale correction only, as an added cfg(test) module that keeps the seven pytest merge-landing assertions in place, or as both in one change; my read rules out deleting the seven pytest tests, because no other test asserts per-type positive landing after the deep merge."
      ],
      "survivingClaim": "Narrowed to the oracle and the rationale, not the routing: py_to_json_value and pydict_to_json_map have no direct oracle anywhere in the tree, since the seven positive per-type claims are inferred from validate reporting valid true instead of compared against an expected serde_json Value, and the recorded reason for that placement (convert.rs:104-106, test_convert.py:5-9) is scoped to the default extension-module build and silent on the --no-default-features --features highs configuration in which ci.yml:567-568 runs the crate's three Python::initialize tests. Not surviving: that the seven pytest tests are surplus or coverage-neutral to relocate, since each also asserts its converted value lands on a real config-schema field through the deep merge, and not the eleven-phase cost framing, since phase 11 is skipped on 1dtoy and the structural cost is per binary.",
      "title": "Seven py_to_json_value unit claims are routed through the eleven-phase cobre.io.validate on a premise the crate's own Rust tests falsify",
      "anchors": [
        {
          "path": "crates/cobre-python/src/convert.rs",
          "symbol": "py_to_json_value"
        },
        {
          "path": "crates/cobre-python/src/convert.rs",
          "symbol": "pydict_to_json_map"
        },
        {
          "path": "crates/cobre-python/src/errors.rs",
          "symbol": "tests"
        },
        {
          "path": "crates/cobre-python/src/io.rs",
          "symbol": "validate"
        },
        {
          "path": "crates/cobre-python/tests/test_convert.py",
          "symbol": "_validate"
        }
      ],
      "fixShape": "Give convert.rs its own #[cfg(test)] mod tests following the pattern errors.rs already proves under --no-default-features: Python::initialize() then Python::attach, build the Bound scalar and dict handles in-test, and compare py_to_json_value / pydict_to_json_map output against an expected serde_json Value. The 7 per-type round-trip claims then assert the conversion directly instead of inferring it from an eleven-phase validate; the 4 rejection tests stay in pytest because they assert the Python-visible ValueError raised before any pipeline work. Correct the module docstring and the convert.rs closing comment in the same change so the recorded rationale matches the configuration the Rust tests run in. No claim loses coverage, which is what testing-architecture section 7 requires, and the output-parity suites are not touched, so the rule 8 byte-neutrality bar holds.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/tests/test_convert.py | sed -n '1,34p'; git show 077dbe2c:crates/cobre-python/src/convert.rs | tail -3; git show 077dbe2c:crates/cobre-python/src/io.rs | sed -n '168p'; git show 077dbe2c:crates/cobre-python/src/errors.rs | sed -n '360,400p'; git show 077dbe2c:crates/cobre-python/Cargo.toml | sed -n '37,41p'; git show 077dbe2c:crates/cobre-python/src/convert.rs | grep -c '#\\[test\\]'",
        "output": "test_convert.py:4-8  `Because those helpers require a live interpreter, they cannot be unit-tested from a #[cfg(test)] Rust module linked against the extension-module build. Instead they are exercised here through cobre.io.validate(..., config_overrides=...)`\ntest_convert.py:30-34  `def _validate(overrides): import cobre.io; return cobre.io.validate(VALID_CASE_1DTOY, config_overrides=overrides)`\nconvert.rs tail  `// GIL-bound: a Rust #[cfg(test)] calling Python::attach cannot link the extension-module build (no embedded interpreter), so coverage lives in tests/test_convert.py.`\nio.rs:168  `/// 9. [prepare_stochastic] - PAR estimation, opening trees, stochastic context`\nerrors.rs:360 `mod tests {`, :393 `Python::initialize();` then `Python::attach(|py| {`\nCargo.toml:37-39  `[features]` / `default = [extension-module, highs]` / `extension-module = [pyo3/extension-module]`\n#[test] count in convert.rs: 0",
        "reading": "The 7 per-type round-trip tests (test_py_to_json_value_int_/bool_/float_/str_/none_/list_/dict_round_trips) share exactly one body, `_validate`, which calls `cobre.io.validate`. That entry point's own doc enumerates eleven phases and its phase 9 is `prepare_stochastic` (PAR estimation plus opening trees), so each of the 7 conversion claims pays a full case validation to assert one serde_json Value shape. The stated reason is written twice, in the module docstring (:4-8) and in the closing comment of convert.rs, and both rest on the premise that a #[cfg(test)] Rust module cannot drive a live interpreter. That premise does not hold for the configuration the crate's Rust tests actually run in: `extension-module` is a default feature (Cargo.toml:37-39) and CI's bindings step runs cargo test with --no-default-features --features highs, which drops it, and errors.rs already calls Python::initialize() then Python::attach in three passing tests under exactly that invocation. convert.rs itself has zero inline #[test]. Both signatures take only Bound handles (py_to_json_value takes &Bound<PyAny>, pydict_to_json_map takes &Bound<PyDict>), so a Rust test needs no importable cobre package. The current oracle is also weaker than the Rust one: the docstring (:11-16) states the assertion is `valid is True` confirming the shape by inference from a successful serde deserialization, where a Rust test compares against an expected serde_json Value directly.",
        "readingNote": null
      },
      "yardstickSection": "§5.3 tier taxonomy (a Unit-tier oracle for a pure converter) / §5.7 feature/backend matrix policy",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-test-bloat-02",
      "id": "TD-072",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "C",
      "downgradeReason": null,
      "upgradeNote": null,
      "calibrationBasis": "two undocumented residues in the non-parity pytest modules — a cross-module private borrow and five re-derivations of the repo root the conftest already owns: Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": ".claude/rules/testing.md § Cost discipline — Cobre links a solver into every test binary",
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Fix scope for the case-path split: crates/cobre-python/pyproject.toml:52 sets testpaths = [\"tests\"], which makes a bare pytest run from the crate directory a configured invocation the nine cwd-relative modules cannot resolve, while every module docstring and ci.yml:572 name the repo-root invocation -- the owner decides whether the residue normalizes all nine onto a resolved-from-__file__ case path or pins the repo-root invocation as the sole supported one and leaves them."
      ],
      "survivingClaim": "Inside crates/cobre-python/tests/ the two shared homes cover neither case location nor case copying, leaving two undocumented residues that the candidate's own parity-untouched scope actually reaches: the cross-module private borrow at test_boundary_load.py:24 of test_policy_load_validation.py:36, and five non-parity re-derivations of the repo root conftest.py:20 already owns (module constants at test_boundary_load.py:26, test_policy_load_validation.py:26, test_d48_ic_seed_load.py:23 plus inline copies at test_errors.py:171 and test_readme.py:41). NOT surviving: the two-incompatible-conventions framing (all nine cwd-relative modules document the repo-root invocation CI runs at ci.yml:572), the fifteen parity modules' own re-derivations (the fix leaves them untouched), the per-item copy loop and the mkdtemp idiom as defects in themselves (the first is forced by the caller's pre-created destination, the second self-deletes and meets the pinned TempDir contract), and run.rs:2207 copy_dir_all.",
      "title": "conftest.py owns one fixture, so copy-the-case and find-the-case are each re-implemented per module in two incompatible conventions and borrowed across modules by a private import",
      "anchors": [
        {
          "path": "crates/cobre-python/src/run.rs",
          "symbol": "copy_dir_all"
        },
        {
          "path": "crates/cobre-python/tests/conftest.py",
          "symbol": "cli_binary"
        },
        {
          "path": "crates/cobre-python/tests/test_validate.py",
          "symbol": "copy_case_to_tempdir"
        },
        {
          "path": "crates/cobre-python/tests/test_policy_load_validation.py",
          "symbol": "_copy_case_with_renamed_hydro"
        },
        {
          "path": "crates/cobre-python/tests/test_boundary_load.py",
          "symbol": "_set_boundary_policy"
        },
        {
          "path": "crates/cobre-python/tests/test_model.py",
          "symbol": "test_system_entity_counts"
        }
      ],
      "fixShape": "Move the shared harness surface into conftest.py beside cli_binary: a session-scoped constant for the repo root that the modules import instead of re-deriving parents[3], a fixture returning the 1dtoy case path resolved from that root rather than from the working directory, and one case-copy fixture that yields a writable copy under tmp_path so the mkdtemp plus manual rmtree discipline disappears. The domain-specific part of _copy_case_with_renamed_hydro, the hydro-id rename, stays in test_policy_load_validation.py and takes the copy from the fixture, which removes the cross-module private import at test_boundary_load.py:24. Keep the Rust copy_dir_all where it is, since it serves the Rust test module and cannot use a pytest fixture, and leave every test_*_parity.py file untouched so the rule 8 byte-neutrality bar and testing-architecture section 5.11 hold; the two per-item copy loops in the parity suites are cited only as evidence that the idiom recurs.",
      "evidence": {
        "command": "git show 077dbe2c:crates/cobre-python/tests/conftest.py | sed -n '18,45p'; git grep -n -E 'shutil.copytree|mkdtemp' 077dbe2c -- crates/cobre-python/tests/; git show 077dbe2c:crates/cobre-python/tests/test_boundary_load.py | sed -n '24p'; for f in conftest.py test_errors.py test_model.py test_policy_load_validation.py test_boundary_load.py test_d48_ic_seed_load.py test_readme.py test_validate.py test_io.py test_results.py test_convert.py; do printf '%-34s repo_root=%s literal=%s\\n' $f $(git show 077dbe2c:crates/cobre-python/tests/$f | grep -cE '_REPO_ROOT|parents\\[3\\]') $(git show 077dbe2c:crates/cobre-python/tests/$f | grep -c 'examples/1dtoy'); done",
        "output": "conftest.py declares exactly one fixture: `@pytest.fixture(scope=session) def cli_binary(pytestconfig)` plus `pytest_addoption` and the module constant `_REPO_ROOT = pathlib.Path(__file__).parents[3]`. No case fixture, no case-copy fixture, no output-reader fixture.\ncopy-a-case implementations inside this cell's corpus: test_validate.py:27 `def copy_case_to_tempdir` (tempfile.mkdtemp plus copytree, called at :92, :181, :319); test_policy_load_validation.py:36 `_copy_case_with_renamed_hydro` (per-item iterdir loop at :50); test_io.py:64 inline `shutil.copytree(VALID_CASE, case_dir)` on tmp_path; test_boundary_load.py:66 and :105 the same inline pair; crates/cobre-python/src/run.rs:2207 `fn copy_dir_all` (recursive read_dir loop) in the Rust test module.\ntest_boundary_load.py:24 `from test_policy_load_validation import _copy_case_with_renamed_hydro`\ncase-path convention split: _REPO_ROOT / parents[3] re-derived in conftest.py, test_errors.py, test_policy_load_validation.py, test_boundary_load.py, test_d48_ic_seed_load.py, test_readme.py; cwd-relative literal `examples/1dtoy` in test_convert.py 1, test_io.py 1, test_results.py 1, test_validate.py 2, test_model.py 6",
        "reading": "The directory has a conftest.py and a _cobre_cli.py, so a shared home exists, but conftest.py owns only the CLI-binary fixture. Copy a case into a writable temp dir is implemented five separate ways across this cell's corpus, four in pytest and once in Rust, with three distinct temp-dir disciplines: mkdtemp with a manual try/finally rmtree, pytest tmp_path, and pytest tmp_path_factory. The sharpest symptom is test_boundary_load.py:24, which reaches into another test module for a private name because no shared home exists for it. Finding the case is split the same way: six modules re-derive the repo root from __file__.parents[3], a constant conftest.py:20 already owns, while five modules pass the cwd-relative literal examples/1dtoy instead, test_model.py six times with no module constant. Two incompatible conventions in one directory means half the files resolve the case from the file's own location and half only resolve it when pytest is invoked from the repo root.",
        "readingNote": null
      },
      "yardstickSection": "§5.2 uniform test-support convention (conftest.py / _cobre_cli.py are the shared homes) / §5.11 cobre-python parity",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    },
    {
      "candidateRef": "S6c-test-bloat-03",
      "id": "TD-073",
      "class": "TD",
      "lens": "test-bloat",
      "subStation": "S6c",
      "subSurfaces": null,
      "severity": "C",
      "reviewerRating": "B",
      "downgradeReason": "three redundant runs in two files with fixture hoists (effort S) and no assertion change",
      "upgradeNote": null,
      "calibrationBasis": "three removable solver runs across two modules, one served by an existing module fixture and two by a new module-scoped fixture — Sev C",
      "category": "duplication",
      "effort": "S",
      "confidence": "high",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV §IV.4 (L4 test corpus)",
      "defenderAlignmentHint": "neutral",
      "l2Rule": {
        "writerScope": false,
        "hits": [],
        "scopedOut": null
      },
      "partIRef": null,
      "waveRef": null,
      "priorId": null,
      "relatedTo": [],
      "relatedCandidates": [],
      "mergedFrom": null,
      "calibrationFolds": null,
      "byteNeutral": "n/a",
      "byteNeutrality": "n/a — test-only, visibility-only, doc-only or attribute-only change; no emitted byte can move (bounded by the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object as a formality)",
      "contractCited": null,
      "dismissalBasis": null,
      "conflicts": null,
      "actionable": true,
      "queuedTo": [
        "test-corpus"
      ],
      "needsHuman": [
        "Anchor vocabulary for pytest files: five of this candidate's six anchors are pytest def names, which tools/check-anchors.py resolves only under its Rust declaration vocabulary; the owner decides whether the checker grows a def/class branch for .py paths or whether pytest anchors are demoted to evidence."
      ],
      "survivingClaim": "Three solver runs are removable, not four, and only one of the two sites reuses an existing fixture: test_boundary_load.py:98 and :124 duplicate the source run at :59 and can be hoisted into a NEWLY authored module-scoped fixture (that module has none today), because the boundary path at run.rs:1299-1310 only reads the source checkpoint; and the bare default Study(...).train() at test_results.py:342 is the single site the already-present module-scoped run_output fixture (:27-37) fully satisfies, since load_stochastic reads only stochastic/*.parquet and the exports.stochastic gate is shared by both entry points at run.rs:1024.",
      "title": "Four redundant full solver runs pay for preconditions a module-scoped fixture already establishes in the same directory",
      "anchors": [
        {
          "path": "crates/cobre-python/src/study.rs",
          "symbol": "train"
        },
        {
          "path": "crates/cobre-python/tests/test_boundary_load.py",
          "symbol": "test_boundary_load_reports_source_date_and_reconciliation"
        },
        {
          "path": "crates/cobre-python/tests/test_boundary_load.py",
          "symbol": "test_boundary_load_strict_accepts_a_faithful_self_boundary"
        },
        {
          "path": "crates/cobre-python/tests/test_boundary_load.py",
          "symbol": "test_boundary_load_rejects_a_mismatched_source"
        },
        {
          "path": "crates/cobre-python/tests/test_results.py",
          "symbol": "run_output"
        },
        {
          "path": "crates/cobre-python/tests/test_results.py",
          "symbol": "test_load_stochastic_missing_artifacts_raises"
        }
      ],
      "fixShape": "In test_boundary_load.py add a module-scoped fixture that performs the source run once with tmp_path_factory and yields the source policy directory, then have the three tests take it instead of re-running the source case, which is safe because none of them writes into that tree. In test_results.py have test_load_stochastic_missing_artifacts_raises take the existing run_output fixture and drop its own Study(...).train(), since load_stochastic only needs a completed run whose stochastic directory is absent and the fixture already satisfies that. Both changes adopt a pattern the same directory already uses and documents, no claim loses coverage so testing-architecture section 7 is satisfied, and no parity suite is touched so the rule 8 byte-neutrality bar holds.",
      "evidence": {
        "command": "git grep -n -E 'cobre.run.run\\(|\\.train\\(\\)' 077dbe2c -- crates/cobre-python/tests/test_boundary_load.py crates/cobre-python/tests/test_results.py; git show 077dbe2c:crates/cobre-python/tests/test_results.py | sed -n '27,37p;332,346p'",
        "output": "test_boundary_load.py:59 `cobre.run.run(VALID_CASE, output_dir=str(source_output))`\ntest_boundary_load.py:70 `cobre.run.run(str(target_case), ...)`\ntest_boundary_load.py:98 `cobre.run.run(VALID_CASE, output_dir=str(source_output))`\ntest_boundary_load.py:109 `cobre.run.run(str(target_case), ...)`\ntest_boundary_load.py:124 `cobre.run.run(VALID_CASE, output_dir=str(source_output))`\ntest_boundary_load.py:136 `cobre.run.run(str(target_case), ...)`\ntest_results.py:36 inside `@pytest.fixture(scope=module) def run_output` with the docstring `Module-scoped so the solver only runs once per test session`\ntest_results.py:296 `.train()` inside the second module-scoped fixture stochastic_output\ntest_results.py:342 `cobre.Study(VALID_CASE, output_dir=str(tmp_path)).train()`",
        "reading": "test_boundary_load.py runs six solves for three tests. Three of the six, at :59, :98 and :124, are the identical source run of the same VALID_CASE with no per-test variation except the output directory name, and all three consume it read-only as `source_output / policy`: the mutations each test performs go into a separate copied target case, and the target solve writes to its own output directory. So three of that module's six solver runs are redundant. In test_results.py, test_load_stochastic_missing_artifacts_raises pays a fourth by calling Study(...).train() at :342 purely to obtain a completed default 1dtoy run with no stochastic exports, which is exactly what the module-scoped run_output fixture at :27-37 already produces; the module proves the point by using a second fixture, stochastic_output, for the with-exports case. The right-sized pattern is already in the same directory and even documents its own reason.",
        "readingNote": null
      },
      "yardstickSection": "§5.3 tier taxonomy / §5.1 fixtures",
      "claimType": null,
      "layout": null,
      "requires": null,
      "profiledSymbol": null,
      "exercisingCallSites": null,
      "mechanism": null,
      "measurabilityNote": null,
      "measuredEvidence": null
    }
  ],
  "reusedIds": [
    {
      "id": "CD-025",
      "registerId": "CD-025",
      "registerLine": 917,
      "wave": 5,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-cli/src/commands/run/outputs.rs",
        "symbol": "write_training_outputs"
      },
      "supersededClaim": "the 2026-08 framing of a THREE-helper `_if_any` family, of a Python side with several entry points, and of the direction wording naming cobre-sddp as a candidate home; also the ticket's premise that the parity gate sees 4 of 17 writers (import-resolving since fc81427a, 18 shared names, floor 18)",
      "supersededWording": {
        "old": "hoist the shared \"which outputs + guards\" helpers to a crate BOTH already depend on (cobre-sddp or cobre-io); the `_if_any` helpers prove the shape",
        "oldAsQuotedByTicket": "hoist the shared output helpers to a crate BOTH already depend on (cobre-sddp or cobre-io)",
        "registerLine": 917,
        "reason": "the cobre-sddp half of the disjunction would home front-end output policy in an engine (L3); Part IV.1 places shared output orchestration at L2 (cobre-io), and Part III.7 / V.1 name that entry point as the Phase-0a deliverable — the wording is superseded, not replaced",
        "newDestination": "cobre-io (L2)"
      },
      "survivingClaim": "the cross-crate hand-mirror is intact at 077dbe2c: write_training_outputs (outputs.rs:58) re-inlines the fpha/evaporation/deviation/echo/fixed-delivery writes (:95, :107, :119, :134, :138) that Python factors into five `_if_any` helpers (run.rs :594/614/636/658/675) reachable only from Study::train_native (study.rs:315); write_simulation_outputs (outputs.rs:178) and run_simulation_phase_py copy the scenario-summary reshape (:221 ↔ :799); 39 write_* call sites on the CLI run path against 23 in run.rs, 17 external writer names on each side (identical sets); the owner is still two, drift is caught by a golden test and the gate, not prevented",
      "restatedFixShape": "shared output-orchestration entry point in cobre-io (L2): ONE call-site list — which outputs, under which config/system guards — consumed by both the CLI and the Python bindings, taking the per-engine results value plus the config/system context it needs; CLI and Python both wire through it, the five `_if_any` helpers and the three CLI orchestrators collapse into it",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "CD-025 precedent: a cross-crate hand-mirror of the output set with no shared owner is Sev B, not A — the Python-parity hard rule (CLAUDE.md § Hard Rules) plus the runtime file-set parity test and the 1dtoy value golden bound the drift, even though nothing structurally prevents it",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V.1 Phase 0 (shared output-orchestration entry point in cobre-io, outputs wired in both CLI and Python through it); Part III.7 (the re-specified results seam); Part IV.1 (L2 owns shared output orchestration)",
      "conflictVariants": [
        {
          "variant": "cobre-sddp home (orchestration at L3)",
          "rule": "Part IV.1 — L2 cobre-io owns shared output orchestration; an engine owning the front ends' output policy means the second engine (cobre-direct, V.1) would route its outputs through a sibling engine — trigger 2 of the layering brief §3 (engine-to-engine dependency) — or duplicate the list again",
          "tag": "conflicts",
          "presentedAsDirection": false
        },
        {
          "variant": "cobre-cli-local helper (keeps the double-mirror)",
          "rule": "Part IV.1 — L2 cobre-io owns 'shared output orchestration'; forbidden at L2: 'an engine-specific output tree hard-wired in the CLI instead of orchestrated here'; a helper in cobre-cli is unreachable from cobre-python, so the hand-mirror and the parity hard rule survive unchanged",
          "tag": "conflicts",
          "presentedAsDirection": false
        }
      ],
      "supersessionNotes": [
        "SN-02",
        "SN-03",
        "SN-04",
        "SN-07",
        "SN-01"
      ],
      "e06_4Deltas": [
        {
          "candidateRef": "S6a-architecture-01",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-025's surviving claim, narrowed on three counts. (1) The simulation write ORDER diverges one-sidedly: write_simulation_results emits simulation/metadata.json with status complete plus the empty simulation/_SUCCESS sentinel (results_writer.rs:151-154) and runs FIRST on the CLI at outputs.rs:186 although every one of its inputs is already built by simulation.rs:220-249, so a failure in the three later writes leaves the completion sentinel beside a missing paths.parquet or scenario_summary.parquet; the bindings' opposite placement at run.rs:855 is data-forced, not a free choice. (2) The bindings have no peer symbol for write_simulation_outputs even though run.rs:532 declares write_training_artifacts as the training peer, so the four simulation calls sit inline in run_simulation_phase_py (run.rs:696) interleaved with aggregation and there is no unit to align. (3) A second verbatim copied transform beyond the scenario-summary reshape already in the prior claim: the eight-argument delta_to_stats_row call with its five None slots at outputs.rs:197-206 and run.rs:824-833. NOT surviving: that the two orders are exact reversals (CLI results/stats/paths/summary against bindings paths/summary/stats/results is not a reversal); that the bindings' ordering is free (run.rs:760-762 pins paths before finalize, and sim_out is incomplete until :767, :803 and :807); and a cobre-io home for the delta_to_stats_row fold, whose input SolverStatsDelta and helper are cobre-sddp-owned (outputs.rs:29, :35), so that piece of the fix shape is an owner call, not a station direction.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "asserted",
          "alignmentHint": "advances-0a",
          "needsHuman": [
            "C6 (carried from the attacker, still live after this read): the solver-stats fold cannot be homed at L2 as filed, because delta_to_stats_row and SolverStatsDelta are declared in cobre-sddp and imported by outputs.rs:29 and :35, so a cobre-io home would make L2 depend on an engine crate; the owner must choose between a cobre-sddp (L3) home for that mapping and leaving the two call sites duplicated while only the four writes move to L2."
          ]
        },
        {
          "candidateRef": "S6a-architecture-02",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-025: OutputContext ownership is split with a consequence the register entry does not name, namely that the CLI caller captures completed_at before any write (run/mod.rs:170 into :183) while the bindings writer captures it after three writes have run (run.rs:576 against :542, :558, :563), so one emitted metadata field means different things on the two sides and the parity golden masks it as wall-clock; the write-order difference is conceded as harmless everywhere except at that capture point; and WriteTrainingArgs.hydro_models (outputs.rs:52) duplicates setup.hydro_models at the single call site (run/mod.rs:198).",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "needs-rebaseline",
          "alignmentHint": "advances-0a",
          "needsHuman": [
            "C6 (carried, narrowed): five of the nine training writes fold cobre-sddp-typed rows before writing (outputs.rs:32-38), so the owner must decide where those folds live before a single L2 entry point can own the whole set; an L2 home for the folds would make cobre-io depend on an engine crate, and a cobre-sddp home is outside the writer boundary the register calls conflicts."
          ]
        },
        {
          "candidateRef": "S6a-architecture-06",
          "verdict": "confirmed",
          "survivingClaim": "Delta over the live hand-mirror claim: the CLI half of the mirror carries a falsified in-code rationale at outputs.rs:124-127, which tells the reader the echo writer at :134 must be called cobre_io-qualified rather than imported so the parity checker can see it; at the pin that attribution is wrong, because the checker resolves bare imported calls through the use map and the Python mirror already imports the same name (run.rs:69), calls it bare (:646) and is still counted in the gate's 18-name set. The claim is confined to that single clause and its one site: the qualified style at :138 has no stated cause and is not evidence of a gate-driven convention spreading, and the gate imposes no call-syntax requirement on either site.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "asserted",
          "alignmentHint": "neutral",
          "needsHuman": []
        },
        {
          "candidateRef": "S6a-over-engineering-03",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-025's surviving claim: the hand-mirror's per-artifact EMIT CONDITION is carried by prose only - three CLI comments (outputs.rs:99-100, :110-111, :124-127) naming a Python twin, covering 3 of the 5 guarded training artifacts - and for exactly two artifacts, evaporation_models and fpha_deviation_points, no enforcement layer asserts the condition: the gate compares writer name sets only, and neither the d28 file-set case nor the 1dtoy value golden ever reaches those write branches (no evaporation coefficients in either case, exports.fpha_deviation_points unset in both), so a one-sided condition change on those two stays green at all three layers; additionally the comment at outputs.rs:124-127 pins the CLI call-site spelling to one branch of the gate's regex on a premise the gate's import-map resolution contradicts. NOT surviving: the count of four comments (three at the pin), the _if_any misnomer (cobre_io::write_fixed_delivery returns early on an empty slice, so the name is accurate), the inverted-placement claim (the single Study::train_native funnel is documented on every helper), and the blanket claim that any one-sided guard change passes CI.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "asserted",
          "alignmentHint": "advances-0a",
          "needsHuman": [
            "Sequencing call: close the two uncovered emit conditions now with CLI-vs-Python coverage on an existing evaporation-modeling case plus a deviation-points-enabled case (pytest additions, no new integration binary, so cost discipline is not engaged), or wait for CD-025's shared cobre-io emit list to remove the duplicated condition entirely and gate it there?"
          ]
        },
        {
          "candidateRef": "S6a-test-bloat-07",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-025's surviving claim, which covers only the writer hand-mirror: the census assertion now also exists in its owning crate at cobre-sddp/src/setup/node_graph.rs:3005 over a strictly more general fixture than the front ends' one_node_graph, and the two front-end copies (cobre-cli commands/run/simulation.rs:453 plus the :428 fixture, cobre-python src/run.rs:2462) assert only cobre_sddp Traversal::simulation_weighting and reach no line of the file they live in, so contrary to the register's 'two test copies pinning two code copies' note they pin neither front-end derivation (cobre-cli simulation.rs:162-165 and cobre-python run.rs:781-784 would stay green if inverted) and are redundant against the owner's copy. The confirmed residue is those two duplicate test bodies and the inlined fixture only; no replacement front-end assertion is conceded and CD-025's writer residue is untouched.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "n/a",
          "alignmentHint": "neutral",
          "needsHuman": []
        },
        {
          "candidateRef": "S6a-performance-02",
          "verdict": "dismissed",
          "survivingClaim": null,
          "dismissalBasis": "cost-accepted-by-rule",
          "argument": "The premise is factually present at the pin and the dismissal rests on cost, not on premise: write_paths takes rows by value and sorts them in place (crates/cobre-io/src/output/simulation_writer.rs:1168-1172), while both front ends hold a borrow and copy at the call (crates/cobre-cli/src/commands/run/outputs.rs:212 through the borrowing WriteSimulationArgs.sim_path_rows at :168, and crates/cobre-…",
          "byteNeutral": "n/a",
          "alignmentHint": "neutral",
          "needsHuman": []
        },
        {
          "candidateRef": "S6a-performance-03",
          "verdict": "dismissed",
          "survivingClaim": null,
          "dismissalBasis": "cost-accepted-by-rule",
          "argument": "The named mechanism is one allocation per run, not per scenario, per stage or per call in a loop. The CLI reshape at outputs.rs:214-220 is reachable only through write_sim_outputs_on_root under the is_root guard (simulation.rs:204-205), after the collective aggregation has completed, and the Python twin at run.rs:793-799 runs once per simulate() call; neither site is on a hot path CLAUDE.md enume…",
          "byteNeutral": "n/a",
          "alignmentHint": "neutral",
          "needsHuman": []
        },
        {
          "candidateRef": "S6a-performance-07",
          "verdict": "dismissed",
          "survivingClaim": null,
          "dismissalBasis": "deliberate-and-documented",
          "argument": "Dismissed on the in-code rationale at crates/cobre-cli/src/commands/run/mod.rs:172-173 ('Write training outputs before simulation so they persist even if simulation fails'), which the crate's failure model makes contingent on COMPLETION, not merely on ordering: execute at mod.rs:128-136 renders the diagnostic and calls comm.abort(e.exit_code()) on any error whenever size > 1, and an aborting peer…",
          "byteNeutral": "n/a",
          "alignmentHint": "neutral",
          "needsHuman": [
            "Owner call: overlapping the rank-0 training-artefact write with rank 0 entering simulation trades the documented 'training outputs persist even if simulation fails' guarantee (mod.rs:172-173, contingent on completion because any rank's error reaches comm.abort at mod.rs:135) plus the stderr progress ordering for the stall. If the owner accepts that trade, the item re-enters the perf sweep at layout 2x2; otherwise the current serial shape stands."
          ]
        },
        {
          "candidateRef": "S6a-over-engineering-02",
          "verdict": "dismissed",
          "survivingClaim": null,
          "dismissalBasis": "contract",
          "argument": "The candidate's load-bearing step — inline the seven-parameter wrapper into its single call site — is unavailable at the pin, because the project's own configured complexity bar is what produces the extraction. clippy.toml:1 sets too-many-lines-threshold = 150 and Cargo.toml:65-67 puts clippy::all and clippy::pedantic at warn with zero warnings required in CI; run_simulation_phase (simulation.rs:…",
          "byteNeutral": "n/a",
          "alignmentHint": "neutral",
          "needsHuman": []
        }
      ],
      "dupOf": [],
      "byteNeutrality": "the restated shape is byte-neutral for the output tree (asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object); the confirmed deltas that move the `cobre validate --json` error object (CD-029) or an emitted metadata field (CD-025 completed_at capture) are held for an owner decision at the gate",
      "needsHuman": [],
      "actionable": true
    },
    {
      "id": "CD-029",
      "registerId": "CD-029",
      "registerLine": 1244,
      "wave": 5,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-sddp/src/validate_phases.rs",
        "symbol": "PrepPhase"
      },
      "supersededClaim": "the Python-parity half ('zero equivalent in cobre-python/src/io.rs', ticket AC: grep -c boundary returns 0) — fixed by fc81427a, the binding validator reconciles the boundary as phase 11; and the doc-drift half's table (now three rows)",
      "supersededWording": null,
      "survivingClaim": "PrepPhase's prose (validate_phases.rs:20) still claims four preparation steps over a three-row table and a three-variant enum, and the fourth step — boundary reconciliation — still bypasses PrepPhase / prep_phase_metadata in both front ends; since fc81427a it does so through two hand-mirrored copies (validate.rs::reconcile_boundary :219 and cobre-python run.rs::reconcile_boundary_policy :1192, same body, no shared owner), so the parity gap closed by extending the CD-025 shape to the validation path",
      "restatedFixShape": "fold the boundary check into PrepPhase in cobre-sddp (L3) as its fourth phase — ONE engine-owned reconcile function that both front ends call and that prep_phase_metadata classifies — and correct the prose to match the variant count; the two front-end copies collapse into that single owner",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "CD-029 precedent: an abstraction (PrepPhase) whose doc oversells its coverage while a fourth path escapes the shared discipline is Sev B",
      "alignmentHint": "advances-0a",
      "alignmentCites": "Part V.1 Phase 0 (the seam covers validate; engine-tagged setup stages; admission-gate rejection, never a silent ignore); Part IV.4 (the engine-selection seam)",
      "conflictVariants": [
        {
          "variant": "cobre-io home for the boundary reconciliation",
          "rule": "Part IV.1 — L2 forbids engine dispatch; the reconciliation is typed on cobre_sddp::StudySetup and calls cobre-sddp's policy loader, so an L2 home inverts the layering (cobre-io depending on an engine crate)",
          "tag": "conflicts",
          "presentedAsDirection": false
        },
        {
          "variant": "keep two front-end copies (cli-local fold into run_boundary_check, Python keeps reconcile_boundary_policy)",
          "rule": "Part IV.1 / III.7 — the hand-mirrored call-site list is the duplication mechanism the roadmap names for outputs; the same shape on the validation path keeps two owners of engine logic in L4",
          "tag": "conflicts",
          "presentedAsDirection": false
        }
      ],
      "supersessionNotes": [
        "SN-05",
        "SN-01"
      ],
      "e06_4Deltas": [
        {
          "candidateRef": "S6b-architecture-00",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-029's surviving claim: the PrepPhase bypass has a machine-readable consequence in cobre-cli that the register does not record. Because the boundary step has no PrepPhase variant and hence no prep_phase_metadata kind, format_boundary_error requires a human Term and run_boundary_check returns at validate.rs:299-301 before emit_validate_json, so cobre validate --json on a case whose configured config.policy.boundary is rejected prints zero JSON objects to stdout (exit 1, human message on stderr) while all three prep phases and the six-layer IO abort each emit an error object and cobre.io.validate returns a structured BoundaryReconciliationError dict; no committed test pairs --json with a boundary reject. Narrowed: the gap is conditional on a configured-and-rejected boundary, and the module sentence at validate.rs:7 (one such object or empty) means no documented contract is breached in letter, so the surviving defect is the front-end asymmetry, not a broken promise.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "needs-rebaseline",
          "alignmentHint": "advances-0a",
          "needsHuman": [
            "Which vocabulary supplies the boundary error object's user-visible `phase` string: the binding's literal `BoundaryReconciliationError` (cobre-python io.rs:305-328) or a fourth prep_phase_metadata row produced by the folded engine-owned phase? The two answers differ in a documented machine-readable field, and the second depends on CD-029's held destination."
          ]
        },
        {
          "candidateRef": "S6b-architecture-03",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-029: the two hand-mirrored boundary-reconcile copies are not the same body. The CLI copy re-resolves the boundary state requirements inside itself (validate.rs:232) although execute already holds the identical value (:401), while the Python copy takes a built StudySetup and reads them back off it (run.rs:1192, :1225); so on a case whose config.policy.boundary is set, and only then (policy_load.rs:589 short-circuits when absent), cobre validate performs one extra full read_policy_checkpoint that the bindings never perform, against the resolver's own once-per-entry-point invariant (policy_load.rs:576-578) and against the CLI run path, which resolves once (run/setup.rs:107). The residue is the duplicate requirements resolve alone, not the from_config or scenario-source re-execution inside new_with_boundary_requirements.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "asserted",
          "alignmentHint": "neutral",
          "needsHuman": [
            "Sequencing only, not the destination (already CD-029's owner question): patch this duplicate resolve as a standalone cobre-cli-local threading fix, or leave it to be absorbed when the boundary check folds into a phase that resolves the requirements once as a phase input?"
          ]
        },
        {
          "candidateRef": "S6b-over-engineering-00",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-029's surviving claim: the fourth-phase bypass is observable at the `--json` surface, not only structural. Because `run_boundary_check`'s `--json` arm returns `CliError::from(err)` (validate.rs:299) with `already_rendered: false` (error.rs:234-236), a boundary reject under `--json` emits no error object on stdout while the three ladder phases and the IO pipeline do (:172, :360), and it emits on stderr the self-referential hint (error.rs:116-125) that cli_validate.rs:219 forbids for the human path; human mode stays correct (:301-303) and no test pins the `--json` reject (cli_validate.rs:691, :760 are human-mode). Separately and independently, `json: bool` on `prep_error_to_cli_error` (:162) and `run_prep_phase` (:185) is fully derivable from `stdout_sink.is_none()` at all three call sites given :332, and `prep_error_to_cli_error`'s `Result<CliError, CliError>` return exists only to let the serializer error escape. NOT confirmed: the title's causality (the boundary arm already holds the mode in `stdout: Option<&Term>`, :289) and its doc-violation framing (the module doc at :7 allows an empty `--json` stdout, and :381, :396, :401 are equally silent).",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "needs-rebaseline",
          "alignmentHint": "advances-0a",
          "needsHuman": [
            "Should `cobre validate --json` emit an error object for a boundary-reconciliation reject? Today stdout is empty and stderr carries the self-referential hint that cli_validate.rs:219 forbids on the human path; no test pins either shape, so adding the object is a deliberate extension of the `--json` contract on an untested path and needs an owner call plus a new reject test in `--json` mode."
          ]
        }
      ],
      "dupOf": [
        "S6b-performance-00"
      ],
      "byteNeutrality": "the restated shape is byte-neutral for the output tree (asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object); the confirmed deltas that move the `cobre validate --json` error object (CD-029) or an emitted metadata field (CD-025 completed_at capture) are held for an owner decision at the gate",
      "needsHuman": [
        {
          "question": "The ticket (description + step 3) restates BOTH Wave-5 fix-shapes with destination = the shared output-orchestration entry point in cobre-io. CD-029's measured shape is a validation phase over cobre_sddp::StudySetup whose helpers all live in cobre-sddp, and the register's own fix is 'fold into PrepPhase' (a cobre-sddp enum). This record names cobre-sddp PrepPhase (L3) as the fold destination and tags a cobre-io home `conflicts`. Confirm, or override to cobre-io with a dated Owner-override line.",
          "recommended": "confirm cobre-sddp PrepPhase (L3) — output orchestration (L2 rule) does not cover a validation phase",
          "forGate": "E06-8"
        },
        {
          "question": "Severity: the parity half is fixed, but the bypass half now has two hand-mirrored copies (validate.rs::reconcile_boundary and run.rs::reconcile_boundary_policy). B is kept here; downgrade to C only if the gate treats the duplicated engine logic as local cleanup.",
          "recommended": "keep B",
          "forGate": "E06-8"
        }
      ],
      "actionable": true
    },
    {
      "id": "CD-002",
      "registerId": "CD-002",
      "registerLine": 168,
      "wave": 4,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-cli/src/commands/run/setup.rs",
        "symbol": "broadcast_and_build_setup"
      },
      "supersededClaim": "'Shrinks once CD-001 lands' — CD-001 landed (b051c410, retired at the sddp gate 2026-09-18) and the tuple did NOT shrink: the non-root reconstruction moved behind reconstruct_stochastic_context_non_root (:396) but every one of the 10 slots is still positional",
      "supersededWording": null,
      "survivingClaim": "broadcast_and_build_setup (setup.rs:246) destructures a positional 10-tuple (:253-263: raw_system, raw_bcast_config, root_config, root_stochastic, root_estimation_report, root_estimation_path, raw_bcast_tree, root_hydro_models, raw_scalar_parameters, load_err) with a `(None×9, Some(e))` error arm (:303-313) and a `(None×10)` non-root arm (:317); a field add still edits three positional arms with no compiler help on ordering; the OUTPUT is already the named LoadBroadcastResult (:151) — only the input side is unnamed",
      "restatedFixShape": "named input struct (`RootLoadArtifacts`, Option on non-root) replacing the three positional arms — a cobre-cli-local constructor-side change; the carrier's Phase-0a shape (which slots survive) is owned by the Wave-4 restatements: CD-004's single Config projection collapses the raw_bcast_config + root_config pair and D14 rank-0-executes (CD-003 restatement, sddp station) decides what non-roots receive at all",
      "registerSeverity": "B",
      "severity": "B",
      "calibrationBasis": "register severity carried (B); E06-2 proposed the downgrade to C on the register's own 2026-08-19 tiering ('Local cleanups (Sev C, batchable)') — asked at the owner gate, not applied here",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.4 (rank-collective setup stage; the D14 shape is owned by CD-003-construction-hop / CD-004 restatements)",
      "conflictVariants": [],
      "supersessionNotes": [
        "SN-08"
      ],
      "e06_4Deltas": [
        {
          "candidateRef": "S6a-architecture-03",
          "verdict": "confirmed",
          "survivingClaim": "Delta over CD-002: the carrier has a second positional level the register row does not record. `type LoadedCase` (setup.rs:68-75) is itself a six-slot positional tuple, constructed positionally at :139-146, re-destructured positionally at :266, and destructured positionally once more by the in-file unit test at :631, so one new loaded artifact edits these four inner-level sites in addition to the three outer arms CD-002 already names. The residue is edit fan-out inside one file only; it does NOT include any loss of type checking, because the six inner and ten outer slot types are pairwise distinct at the pin and a slot reordering is therefore a compile error.",
          "dismissalBasis": null,
          "argument": null,
          "byteNeutral": "asserted",
          "alignmentHint": "neutral",
          "needsHuman": []
        }
      ],
      "dupOf": [],
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object for the restated shape",
      "needsHuman": [
        {
          "question": "Proposed downgrade B → C (register header B; register's own 2026-08-19 tiering lists CD-002 under 'Local cleanups (Sev C, batchable)'). Ratify the downgrade or keep B.",
          "recommended": "downgrade to C",
          "forGate": "E06-8"
        }
      ],
      "actionable": true
    },
    {
      "id": "CD-009",
      "registerId": "CD-009",
      "registerLine": 312,
      "wave": 7,
      "disposition": "sharpen",
      "resolvingCommit": null,
      "baselineAnchor": {
        "path": "crates/cobre-cli/src/commands/run/policy.rs",
        "symbol": "load_policy_for_simulation"
      },
      "supersededClaim": "only the line anchors — the register cites the 2026-08 lines and the ticket the a136840d lines; the shape is unchanged",
      "supersededWording": null,
      "survivingClaim": "the resolve + `!exists()` guard is repeated exactly three times at policy.rs:172/201/334 (WarmStart :171, Resume :200, load_policy_for_simulation :323) and differs only in the trailing hint — 'Cannot warm-start without a prior policy.' / 'Cannot resume without a prior checkpoint.' / 'Cannot run simulation-only mode without a trained policy.'",
      "restatedFixShape": "cobre-cli-local `resolve_policy_dir(ctx, setup, missing_hint) -> Result<PathBuf, CliError>` — a path helper, not output orchestration: the S6a L2 destination rule does not apply and no `conflicts` variant exists; the WarmStart/Resume match arms stay separate (the register's own call)",
      "registerSeverity": "C",
      "severity": "C",
      "calibrationBasis": "register severity carried; a three-site path-resolution dedup inside cobre-cli",
      "alignmentHint": "neutral",
      "alignmentCites": "Part IV.4 (L4 owns the front-end plumbing around the engine)",
      "conflictVariants": [],
      "supersessionNotes": [
        "SN-09"
      ],
      "e06_4Deltas": [],
      "dupOf": [],
      "byteNeutrality": "asserted against the CLI-vs-Python value golden (crates/cobre-python/tests/test_cli_python_determinism_parity.py, examples/1dtoy through both entry points), the runtime file-set parity test (tests/test_cli_python_file_set_parity.py) with its Rust companion (crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes), the import-resolving gate scripts/ci/check_python_parity.py (--min-shared 18) and the `cobre validate --json` object for the restated shape",
      "needsHuman": [],
      "actionable": true
    }
  ],
  "supersessionNotes": [
    {
      "id": "SN-01",
      "fact": "Part-I I.5: the CLI's cobre_sddp import surface",
      "old": "'~70 imports' (plans/generalizing/beyond-sddp-generalization.md Part I §I.5, v0.12 snapshot)",
      "new": "97 `cobre_sddp` occurrences in 10 files, 70 of them `use` lines (88 distinct paths), at 077dbe2c; ranking policy.rs 20, error.rs 16, simulation.rs 15, outputs.rs 11",
      "command": "git grep -c 'cobre_sddp' 077dbe2c -- crates/cobre-cli/src | sort -t: -k2 -nr; git grep -n '^use cobre_sddp' 077dbe2c -- crates/cobre-cli/src | wc -l",
      "source": "inventory.json → sddpCoupling; partI-handoff.json i5Queue",
      "justifies": [
        "I.5-2 (sharpen)",
        "CD-025",
        "CD-029"
      ]
    },
    {
      "id": "SN-02",
      "fact": "per-side writer call-site figures",
      "old": "41 CLI / 29 Python `write_*(` call sites and 26 distinct Python writer names (the epic/ticket text, minted at a136840d)",
      "new": "39 CLI call sites (16 terminal `write_line`, 23 writer sites) / 23 Python (5 `_if_any` helpers); external writer names 17 / 17, identical",
      "command": "git grep -o 'write_[a-z_0-9]*(' 077dbe2c -- 'crates/cobre-cli/src/commands/run/*.rs' | wc -l; … | grep -c 'write_line('; git grep -o 'write_[a-z_0-9]*(' 077dbe2c -- crates/cobre-python/src/run.rs | wc -l; … | grep -c '_if_any('",
      "source": "enforcement-measurements.json → writerSurface; inventory.json → writerBoundary",
      "justifies": [
        "CD-025"
      ]
    },
    {
      "id": "SN-03",
      "fact": "the parity script's per-side name set",
      "old": "'4 of 17 writers seen; use-line skip + allowlist' (register CD-025 2026-08 text; ticket premise 'source layer covers only its allowlist name set')",
      "new": "import-resolving since fc81427a: 18 CLI / 18 Python names, 18 in both, floor --min-shared 18, no allowlist; exit 0, \"OK: 0 parity mismatch(es) (max allowed: 0). 18 write functions in both paths.\"; invisible by name only: write_checkpoint, write_scenario (each covered under another name)",
      "command": "python3 scripts/ci/check_python_parity.py --max 0 --root .",
      "source": "enforcement-measurements.json → sourceLayer; inventory.json → parityEnforcement.layer1_source",
      "justifies": [
        "CD-025"
      ]
    },
    {
      "id": "SN-04",
      "fact": "CD-025 line anchors",
      "old": "write_training_outputs at outputs.rs:57 (register) / :58 (ticket a136840d); write_training_artifacts at run.rs:478 / :479; the three `_if_any` helpers at run.rs:554, :574, :597 (register)",
      "new": "outputs.rs:58; run.rs:532; five `_if_any` helpers at run.rs:594, :614, :636, :658, :675 — every symbol still resolves; an anchor refresh, not a claim change (the helper family grew from three to five)",
      "command": "git grep -n 'fn write_training_outputs\\|fn write_training_artifacts\\|fn write_.*_if_any' 077dbe2c -- crates/cobre-cli/src/commands/run/outputs.rs crates/cobre-python/src/run.rs",
      "source": "inventory.json → priorAnchors",
      "justifies": [
        "CD-025"
      ]
    },
    {
      "id": "SN-05",
      "fact": "CD-029's Python-parity half",
      "old": "'zero equivalent in cobre-python/src/io.rs' (register CD-029; ticket AC `grep -c boundary` returns 0)",
      "new": "14 boundary references in io.rs — cobre.io.validate reconciles the boundary as phase 11 since fc81427a (the CD-029 Status bullet of 2026-09-17 already records the parity half as FIXED)",
      "command": "git show 077dbe2c:crates/cobre-python/src/io.rs | grep -c boundary",
      "source": "inventory.json → supersessions; wave-dispositions.json CD-029.halves",
      "justifies": [
        "CD-029"
      ]
    },
    {
      "id": "SN-06",
      "fact": "cobre-python Rust `#[test]` CI visibility",
      "old": "'compiled and run by nothing in CI (22 tests)' (ticket; mirror `### Python-binding Rust tests invisible to CI` docs/design/reserved-seams-and-deferred-debt.md:347; docs/design/testing-architecture.md §5.11)",
      "new": "19 tests (errors.rs 3, policy.rs 1, run.rs 13, schema.rs 2) run by ci.yml `Run Rust tests for the bindings crate` (:562-568, python 3.12 matrix) — CI-VISIBLE; the mirror entry and §5.11 are stale (E11 write-back; E07 build/CI cross-reference)",
      "command": "git grep -c '#\\[test\\]' 077dbe2c -- 'crates/cobre-python/src/*.rs'; git show 077dbe2c:.github/workflows/ci.yml | grep -n 'manifest-path crates/cobre-python'",
      "source": "enforcement-measurements.json → ciVisibility; inventory.json → testSurface.ciVisibility",
      "justifies": [
        "Cleared cross-reference (no TD)"
      ]
    },
    {
      "id": "SN-07",
      "fact": "the runtime file-set parity layer",
      "old": "'its _cli_binary() fixture skips when no compiled cobre binary exists … a runtime layer that skips in CI does not close a gap' (ticket premise)",
      "new": "guard is conftest.py::cli_binary → _cobre_cli.resolve_cli_binary(required=--require-cli-binary); test_cli_python_file_set_parity.py passed (5 passed, 0 skipped) with target/release/cobre (present, built 2026-09-17); CI builds the CLI (ci.yml:561) and passes --require-cli-binary (:572) before pytest — ciExecutes yes; third layer crates/cobre-cli/tests/python_parity_check.rs::python_parity_script_passes",
      "command": "python3 -m pytest crates/cobre-python/tests/test_cli_python_file_set_parity.py -v -rs -p no:cacheprovider; git show 077dbe2c:.github/workflows/ci.yml | grep -n 'require-cli-binary\\|cargo build --release -p cobre-cli'",
      "source": "enforcement-measurements.json → runtimeLayer / thirdLayer",
      "justifies": [
        "CD-025 (the measure-then-claim residues S6a-architecture-06 / S6a-over-engineering-03)"
      ]
    },
    {
      "id": "SN-08",
      "fact": "CD-002's 'shrinks once CD-001 lands' expectation and its line anchors",
      "old": "'Shrinks once CD-001 lands' (register CD-002); broadcast_and_build_setup at setup.rs:262 (register) / :248 (ticket)",
      "new": "CD-001 landed (b051c410, retired at the sddp gate 2026-09-18) and the tuple did NOT shrink: broadcast_and_build_setup at setup.rs:246 still destructures a positional 10-tuple (:253-263, :290-301, :303-314); the E06-4 delta adds the six-slot `type LoadedCase` inner level (:68-75, :139-146, :266, :631)",
      "command": "git show 077dbe2c:crates/cobre-cli/src/commands/run/setup.rs | sed -n '68,75p;246,263p;303,317p'",
      "source": "wave-dispositions.json CD-002; verdicts.json S6a-architecture-03",
      "justifies": [
        "CD-002"
      ]
    },
    {
      "id": "SN-09",
      "fact": "CD-009 line anchors",
      "old": "policy.rs:151, :180, :273 (register) / :167, :196, :316 (ticket a136840d)",
      "new": "policy.rs:172, :201, :334 (WarmStart, Resume, load_policy_for_simulation :323); the shape is unchanged — an anchor refresh only",
      "command": "git grep -n 'let policy_dir = ctx.output_dir.join(&setup.policy_path)' 077dbe2c -- crates/cobre-cli/src/commands/run/policy.rs",
      "source": "inventory.json → priorAnchors.policy_dir",
      "justifies": [
        "CD-009"
      ]
    },
    {
      "id": "SN-10",
      "fact": "Part-I I.5: the `report` / `summary` subcommands and the Command enum",
      "old": "seven Command variants incl. `report` and `summary` (Part-I I.5 v0.12; the E06 ticket texts)",
      "new": "five variants (Init, Run, Validate, Schema, Version) at main.rs:67 — `report` and `summary` removed by 797ba443 together with their Python and cobre-io mirrors; the I.5 claim survives for validate.rs and the src/summary.rs MODULE (`pub use cobre_sddp::{6 types}` at summary.rs:23) only",
      "command": "git show 077dbe2c:crates/cobre-cli/src/main.rs | grep -n -A 12 'enum Command'",
      "source": "inventory.json → supersessions; partI-handoff.json i5Queue",
      "justifies": [
        "I.5-1a/1b (retire)",
        "I.5 queue rows"
      ]
    }
  ],
  "cleared": [
    {
      "candidateRef": "S6a-architecture-04",
      "verdict": "dismissed",
      "dismissalBasis": "sanctioned-seam",
      "basisCitation": "docs/design/reserved-seams-and-deferred-debt.md:563",
      "sanctionedBy": "Setup config-projection sprawl + CLI non-root reconstruction",
      "contractCited": null,
      "priorId": null,
      "reason": "The mirror entry the candidate itself cites reserves exactly this caller shape. At docs/design/reserved-seams-and-deferred-debt.md:563 the open half is stated as 'the local StudyParams and the wire BroadcastConfig projection are still two structs kept in step by hand', the blocker is named (the pos…",
      "needsHuman": [
        "C5 (carried from the attacker): does the owner want the transport-flavoured name of cobre-sddp's sole StudySetup construction core (from_broadcast_params, entered by both front ends) filed as a rename against the station that owns cobre-sddp's public API, or left to the setup-layer redesign that the mirror :563 entry already triggers?"
      ]
    },
    {
      "candidateRef": "S6b-architecture-06",
      "verdict": "dismissed",
      "dismissalBasis": "deliberate-and-documented",
      "basisCitation": "crates/cobre-python/src/errors.rs:13-17",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The asymmetry is deliberate, documented at crates/cobre-python/src/errors.rs:13-17, and its load-bearing premise holds at the pin. ErrorSource::Sddp is constructed at exactly four production sites (run.rs:362, run.rs:428, run.rs:745, study.rs:387) and every one is a training or simulation phase hel…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6c-architecture-00",
      "verdict": "dismissed",
      "dismissalBasis": "premise-false-at-pin",
      "basisCitation": "crates/cobre-python/src/run.rs:1395-1444",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "Judging only the station delta the ingest note names (two separately registered public entry points over one engine), not the I.5 seam claim. The delta's load-bearing premise is that cobre.run.run and cobre.Study are two engine-reaching surfaces, each needing its own engine-admission site, and that…",
      "needsHuman": [
        "Coverage residue left by this dismissal: study.rs:583-585 / :694-696 admit that whole-tree byte-identity is unasserted for the cobre.Study methods, while the whole-tree goldens reach the same native calls only through run_via_study and the census golden compares selected columns of selected files. Owner call: does that warrant its own test-corpus coverage item, or is it already covered in substance by the shared single execution path?"
      ]
    },
    {
      "candidateRef": "S6c-architecture-03",
      "verdict": "dismissed",
      "dismissalBasis": "deliberate-and-documented",
      "basisCitation": "crates/cobre-python/src/results.rs:117-135 (load_results returns the metadata files' contents as a dict; ValueError only on malformed JSON) with crates/cobre-io/tests/metadata_back_compat.rs:1-7 (the metadata back-compat contract the typed readers assert)",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The two reads are not the same read, so there is no bypassed reuse. load_results is documented at crates/cobre-python/src/results.rs:117 and :123 to hand Python the CONTENTS of training/metadata.json and simulation/metadata.json, and it does exactly that: read_json_file (:86) parses to serde_json::…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6c-architecture-06",
      "verdict": "dismissed",
      "dismissalBasis": "premise-false-at-pin",
      "basisCitation": "crates/cobre-sddp/src/policy/policy_load.rs:55-61",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The load-bearing blast-radius claim does not hold at the pin. cobre-sddp declares TWO public constants at this value with opposite lifetimes: DEFAULT_COST_SCALE_FACTOR (crates/cobre-sddp/src/setup/params.rs:43), the movable default for an absent modeling.cost_scale_factor CONFIG FIELD, consumed onl…",
      "needsHuman": [
        "Owner call: whether the terminal fallback at crates/cobre-python/src/policy.rs:510 should spell the already-exported cobre_sddp::LEGACY_COST_SCALE_FACTOR (reachable with no new dependency) and whether write_policy_checkpoint's doc should state the both-markers-absent case, since the field doc at policy.rs:104-107 points at that doc and the doc is silent on it."
      ]
    },
    {
      "candidateRef": "S6a-performance-00",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "crates/cobre-cli/src/commands/run/setup.rs:319-380 (the once-per-run broadcast region, timed into SetupTimings) with its single callers crates/cobre-cli/src/commands/run/mod.rs:150 and :167",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The candidate's facts hold at the pin. In broadcast.rs, the is_root arm at lines 417-421 calls postcard::to_allocvec with no comm.size() guard, the encoded buffer is broadcast at lines 445-449, and the root arm at lines 451-455 returns the original value, so on a single-rank world the buffer is bui…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6a-performance-01",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "crates/cobre-cli/src/commands/run/simulation.rs:155-158 — both gathers' only call sites, post-simulate teardown after the drain join at :113, with the bulk payload already streamed to Parquet per :80-81",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "Every factual premise holds at the pin. Both helpers allgatherv their payload (simulation.rs:363, :408) after exchange_gather_plan allgathers the per-rank length vector (:324), and the sole consumer of global_path_rows and global_scenario_stats is write_sim_outputs_on_root behind if ctx.is_root (:2…",
      "needsHuman": [
        "Rooted gather at L0: closing the non-root replication needs a gatherv the cobre-comm Communicator trait does not expose (four collectives at traits.rs:64). Adding it grows the L0 FFI surface across ferrompi, LocalBackend, the factory enum and the documented precondition table for two call sites in one CLI file. Owner call: admit the primitive, or keep allgatherv and accept that non-root ranks rebuild a payload they discard. This verdict accepts the cost at the station and does not decide the L0 question."
      ]
    },
    {
      "candidateRef": "S6a-performance-02",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "CLAUDE.md § Architecture Guides — hot-path list (training/forward, training/backward, training/training.rs, simulation/pipeline.rs, training/lower_bound.rs); the write path is not in it",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": "CD-025",
      "reason": "The premise is factually present at the pin and the dismissal rests on cost, not on premise: write_paths takes rows by value and sorts them in place (crates/cobre-io/src/output/simulation_writer.rs:1168-1172), while both front ends hold a borrow and copy at the call (crates/cobre-cli/src/commands/r…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6a-performance-03",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "CLAUDE.md § Hard Rules — \"Never allocate on hot paths\" (hot-path list in CLAUDE.md § Architecture Guides); the write site is rank-0 once-per-run at crates/cobre-cli/src/commands/run/simulation.rs:204",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": "CD-025",
      "reason": "The named mechanism is one allocation per run, not per scenario, per stage or per call in a loop. The CLI reshape at outputs.rs:214-220 is reachable only through write_sim_outputs_on_root under the is_root guard (simulation.rs:204-205), after the collective aggregation has completed, and the Python…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6a-performance-04",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "crates/cobre-cli/src/commands/run/simulation.rs:156",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The mechanism is factually real at the pin and I am not disputing it. simulation.rs:397-402 builds local_stats_stripped by cloning every local (u32, i32, SolverStatsDelta) entry into a two-tuple purely to shed the opening field, and the only reason the clone exists is the packer signature at solver…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6a-performance-05",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "crates/cobre-cli/src/commands/run/setup.rs:352-363 (sole production call of prepare_hydro_models, setup phase); crates/cobre-sddp/src/production/hydro_models/mod.rs:66-68",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The mechanism is real but it is entirely setup-phase work, which the station's perf rule accepts by name (case load, hydro-model fit, validation). prepare_hydro_models has exactly one production caller at crates/cobre-cli/src/commands/run/setup.rs:360, inside broadcast_and_build_setup, which runs o…",
      "needsHuman": [
        "Owner call, documentation only: should the shared-filesystem re-read on non-root ranks become a ratified deployment assumption with a docs/design entry? Two module docs state the behaviour (setup.rs:1-8, hydro_models/mod.rs:66-68) and no design doc records it, so the record is thinner than the assumption it rests on — not a perf defect at the pin."
      ]
    },
    {
      "candidateRef": "S6a-performance-06",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "crates/cobre-cli/src/commands/run/mod.rs:228",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "load_policy_for_simulation has exactly one call site (crates/cobre-cli/src/commands/run/mod.rs:228), reached once per run in the non-training branch of execute_inner immediately before run_simulation_phase, so the directory listing, the per-stage reads, the cut rescale, the manifest validation and…",
      "needsHuman": [
        "Owner call: is the shared-filesystem re-read on non-root ranks a ratified deployment assumption? setup.rs:4-8 states the behaviour with no rationale and no docs/design entry covers it, and the answer decides whether the per-rank policy-checkpoint read is ever revisited at large rank counts."
      ]
    },
    {
      "candidateRef": "S6a-performance-07",
      "verdict": "dismissed",
      "dismissalBasis": "deliberate-and-documented",
      "basisCitation": "crates/cobre-cli/src/commands/run/mod.rs:172-173",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": "CD-025",
      "reason": "Dismissed on the in-code rationale at crates/cobre-cli/src/commands/run/mod.rs:172-173 ('Write training outputs before simulation so they persist even if simulation fails'), which the crate's failure model makes contingent on COMPLETION, not merely on ordering: execute at mod.rs:128-136 renders the…",
      "needsHuman": [
        "Owner call: overlapping the rank-0 training-artefact write with rank 0 entering simulation trades the documented 'training outputs persist even if simulation fails' guarantee (mod.rs:172-173, contingent on completion because any rank's error reaches comm.abort at mod.rs:135) plus the stderr progress ordering for the stall. If the owner accepts that trade, the item re-enters the perf sweep at layout 2x2; otherwise the current serial shape stands."
      ]
    },
    {
      "candidateRef": "S6b-performance-01",
      "verdict": "dismissed",
      "dismissalBasis": "deliberate-and-documented",
      "basisCitation": "crates/cobre-cli/src/progress.rs:7-9 (module doc: both strategies collect every received event verbatim and return the full sequence via ProgressHandle::join)",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The receiver is taken by value at progress.rs:156 and moved into the spawned closure at :161, so once a progress thread exists the returned Vec is the only path by which any caller can observe the event stream; training.rs:107 then :111 depends on exactly that, feeding the joined Vec into build_tra…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6c-performance-03",
      "verdict": "dismissed",
      "dismissalBasis": "cost-accepted-by-rule",
      "basisCitation": "CLAUDE.md Hard Rules - 'Never allocate on hot paths' (hot-path scope list under CLAUDE.md Architecture Guides); crates/cobre-python/src/results.rs:147-197",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "Premise is true at the pin: crates/cobre-python/src/results.rs:184-185 call json_value_to_py on the same &metadata_val read at :160, so the recursive per-node conversion really does run twice. The mechanism is nonetheless once-per-call post-run work off every hot path, which rule 11's carve-out acc…",
      "needsHuman": [
        "Carried from the attacker and live only if the owner overrides this dismissal: is object identity between result['training']['manifest'] and result['training']['metadata'] part of the published cobre.results contract, or may one converted dict be bound to both keys (making the two views alias on mutation)?"
      ]
    },
    {
      "candidateRef": "S6a-over-engineering-02",
      "verdict": "dismissed",
      "dismissalBasis": "contract",
      "basisCitation": "clippy.toml:1 (too-many-lines-threshold = 150), with Cargo.toml:65-67 and Cargo.toml:71",
      "sanctionedBy": null,
      "contractCited": "CLAUDE.md § Hard Rules — `clippy::all` and `clippy::pedantic` at `warn` level, zero warnings in CI",
      "priorId": "CD-025",
      "reason": "The candidate's load-bearing step — inline the seven-parameter wrapper into its single call site — is unavailable at the pin, because the project's own configured complexity bar is what produces the extraction. clippy.toml:1 sets too-many-lines-threshold = 150 and Cargo.toml:65-67 puts clippy::all…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6a-over-engineering-05",
      "verdict": "dismissed",
      "dismissalBasis": "premise-false-at-pin",
      "basisCitation": "crates/cobre-python/src/run.rs:1412",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The grep in the candidate is accurate, but its load-bearing inference is not: the consumer that asks for crate visibility here is a signature, not a call site. RunSummary is the success arm of the return type of run_via_study, itself declared pub(crate) at crates/cobre-python/src/run.rs:1406 with t…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6b-over-engineering-04",
      "verdict": "dismissed",
      "dismissalBasis": "contract",
      "basisCitation": ".claude/rules/comments.md:365",
      "sanctionedBy": null,
      "contractCited": "`.claude/rules/comments.md` D4 — Rationale above suppression: \"Every `#[allow(...)]` for a refactor-decision lint (`clippy::too_many_arguments`, `too_many_lines`, `type_complexity`, `dead_code`, `unused_*`) and every borrow-checker workaround carries a rationale: why the refactor that removes the lint is inappropriate.\"",
      "priorId": null,
      "reason": "The factual half of the candidate holds at the pin: the nine production cast suppressions exist as filed (summary.rs:602, 648, 713, 757, 777, 821, 851, 866, 870) and none is preceded by a Rationale line, while the test-scoped one at :1436 is correctly excluded. The normative half does not hold. The…",
      "needsHuman": [
        "E11 census write-back owner call: correct the mirror's Load-bearing class clause (`each carrying a // Rationale: comment`) so it matches comments.md D4's closed lint list, which excludes numeric-cast lints, or else extend D4 to cast lints — the clause overclaims workspace-wide (496 cast allows under crates/*/src against 153 `Rationale:` lines in total), and until it is settled this candidate re-arises at every station."
      ]
    },
    {
      "candidateRef": "S6c-test-bloat-01",
      "verdict": "dismissed",
      "dismissalBasis": "premise-false-at-pin",
      "basisCitation": "crates/cobre-python/src/schema.rs:69",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "The load-bearing premise (the Rust test is a strict superset of the pytest pair) does not hold at the pin. Both Rust tests call the plain Rust export with an already-built PathBuf (schema.rs:69, schema.rs:92), so they exercise none of the public surface the pytest tests reach: the wrap_pyfunction r…",
      "needsHuman": []
    },
    {
      "candidateRef": "S6c-test-bloat-04",
      "verdict": "dismissed",
      "dismissalBasis": "premise-false-at-pin",
      "basisCitation": "docs/design/testing-architecture.md:430 (section 5.2, Adoption 2026-09-15: the bindings manifest does not yet name cobre-sddp with that feature)",
      "sanctionedBy": null,
      "contractCited": null,
      "priorId": null,
      "reason": "Both conjuncts of the title fail at the pin. The section 5.2 conjunct is refuted by the doc itself: the dated Adoption paragraph at docs/design/testing-architecture.md:425-431 states that the bindings crate's dev-dependencies do not yet name cobre-sddp with the test-support feature, and that this n…",
      "needsHuman": [
        "Confirm the E11 write-back's scope covers every design-doc trace of the superseded invisibility premise, not only reserved-seams-and-deferred-debt.md:347: testing-architecture.md:101-102 still lists any cargo test run for the workspace-excluded bindings crate as Absent and asserts its Rust unit tests never compile in CI (the unit-test half is false at the pin, the doctest half still holds because the lib target sets doc = false), and :17 still lists that register item among the entries section 5 closes."
      ]
    },
    {
      "candidateRef": null,
      "verdict": "sanctioned",
      "subject": "The facade crate `cobre` (crates/cobre/src/lib.rs, two doc lines, no `[dependencies]`)",
      "citation": "sanctioned by ARCHITECTURE.md:102-106 at the pin (the umbrella crate, 'reserved for a future single-dependency convenience re-export') — the Legend excludes a documented reserved seam from the OD class by construction; Cleared with that citation, no OD id. The mirror carries NO row for it (prior-register.md:46 precondition) — an E11 write-back the reconciliation epic owes; the keep-or-retire question (two attacker cells, two positives) rides to the owner gate as needs-human, never pre-judged here",
      "id": null
    },
    {
      "candidateRef": null,
      "verdict": "sanctioned",
      "subject": "cobre-python Rust `#[test]` population 'invisible to CI'",
      "citation": "already a registered deferred-debt entry (mirror `### Python-binding Rust tests invisible to CI`, docs/design/reserved-seams-and-deferred-debt.md:347, owner build / CI) — re-raise-blocked, Cleared as a cross-reference to the build/CI station (E07), no TD id; AND superseded at the pin: ci.yml:562-568 `Run Rust tests for the bindings crate` runs the 19 tests (see supersession-notes.md SN-06), so the entry itself is stale and travels to the E11 mirror write-back",
      "id": null
    }
  ],
  "dupOf": [
    {
      "candidateRef": "S6b-performance-00",
      "dupOf": "S6b-architecture-03",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "CD-029"
    },
    {
      "candidateRef": "S6b-over-engineering-01",
      "dupOf": "S6b-test-bloat-01",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "TD-064"
    },
    {
      "candidateRef": "S6b-over-engineering-03",
      "dupOf": "S6b-architecture-04",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "CD-093"
    },
    {
      "candidateRef": "S6b-over-engineering-05",
      "dupOf": "S6b-architecture-05",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "CD-094"
    },
    {
      "candidateRef": "S6b-test-bloat-04",
      "dupOf": "S6b-architecture-04",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "CD-093"
    },
    {
      "candidateRef": "S6b-test-bloat-08",
      "dupOf": "S6b-architecture-05",
      "priorRelation": "intra-station",
      "foldedAt": "ingest",
      "assignedId": null,
      "survivorId": "CD-094"
    },
    {
      "candidateRef": "S6a-test-bloat-00",
      "dupOf": "S6a-test-bloat-01",
      "priorRelation": "intra-station",
      "foldedAt": "calibration",
      "assignedId": null,
      "survivorId": "TD-057",
      "why": "the two single-#[test] solver-linked binaries (output_metadata_active_backend, setup_timings_metadata) and their byte-identical d01_case_dir() are two of the six run-path lanes S6a-test-bloat-01 already folds into cli_run.rs and one of the three helper bodies it names; the only residue TB-00 adds is the destination question (a metadata-domain binary per testing-architecture §5.1 versus cli_run.rs), carried as the survivor's needs-human — same defect, one id (the CD-025 'not a new ID — broader scope' precedent applied to a test-bloat pair)"
    }
  ],
  "sharpenMerged": [
    {
      "candidateRef": "S6a-architecture-01",
      "priorId": "CD-025",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-architecture-02",
      "priorId": "CD-025",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-architecture-06",
      "priorId": "CD-025",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-performance-02",
      "priorId": "CD-025",
      "verdict": "dismissed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-performance-03",
      "priorId": "CD-025",
      "verdict": "dismissed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-performance-07",
      "priorId": "CD-025",
      "verdict": "dismissed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-over-engineering-02",
      "priorId": "CD-025",
      "verdict": "dismissed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-over-engineering-03",
      "priorId": "CD-025",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-test-bloat-07",
      "priorId": "CD-025",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6a-architecture-03",
      "priorId": "CD-002",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6b-architecture-00",
      "priorId": "CD-029",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6b-architecture-03",
      "priorId": "CD-029",
      "verdict": "confirmed",
      "assignedId": null
    },
    {
      "candidateRef": "S6b-over-engineering-00",
      "priorId": "CD-029",
      "verdict": "confirmed",
      "assignedId": null
    }
  ],
  "conflicts": [],
  "counts": {
    "received": 76,
    "confirmed": 50,
    "minted": 40,
    "sharpenMerged": 9,
    "sharpenDismissed": 4,
    "calibrationFolds": 1,
    "dismissed": 20,
    "dupOf": 6,
    "downgrades": 5,
    "upgrades": 0,
    "conflicts": 0,
    "bySeverity": {
      "B": 13,
      "C": 27
    },
    "byClass": {
      "CD": 10,
      "OD": 6,
      "TD": 17,
      "PD": 7
    }
  }
}
